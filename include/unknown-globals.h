#ifndef UNKNOWN_GLOBALS_H
#define UNKNOWN_GLOBALS_H

#include "global.h"

/* Types and declarations for the still-unnamed globals, the counterpart to
 * unknown-functions.h. Every promoted file gets this through global.h.
 *
 * Why it exists: before this header each translation unit carried its own
 * `extern` and its own idea of the layout. They compiled because they were
 * separate TUs, not because they agreed -- gUnknown_08499598 had eight
 * different bodies and gUnknown_03002B6C had three different types. Nothing
 * caught the disagreement, and every wave of promotions added more.
 *
 * Rules for editing:
 *   * A struct here is a superset. Adding a field is fine; moving one is not.
 *     Offsets, member types and the total size are all load-bearing -- agbcc
 *     picks the instruction from the member type and the index stride from
 *     sizeof, so a change to any of them silently breaks a match somewhere
 *     else. The size in the comment on each struct is the checked value.
 *   * `filler_XX` is unexplored space, `unkXX` is a field something reads.
 *     Narrowing a filler into a field is the normal way this grows.
 *   * The display-register shadows (gUnknown_03002B6C, gUnknown_030030A4,
 *     gUnknown_030030E0 and neighbours) are NOT here -- they live in
 *     hardware.h, next to the register types they mirror.
 */

/* ---------------------------------------------------------------- types -- */

/* Raw IDs stored in UnitRecord. Holes are deliberate: they are reserved or
 * unused slots in the original type table, so never compact this enum. */
enum UnitType
{
    UNIT_INFANTRY = 1,
    UNIT_MECH = 2,
    UNIT_MEDIUM_TANK = 3,
    UNIT_TANK = 5,
    UNIT_RECON = 6,
    UNIT_APC = 7,
    UNIT_NEOTANK = 8,
    UNIT_ARTILLERY = 10,
    UNIT_ROCKETS = 11,
    UNIT_ANTI_AIR = 14,
    UNIT_MISSILES = 15,
    UNIT_FIGHTER = 16,
    UNIT_BOMBER = 17,
    UNIT_BATTLE_COPTER = 19,
    UNIT_TRANSPORT_COPTER = 20,
    UNIT_BATTLESHIP = 21,
    UNIT_CRUISER = 22,
    UNIT_LANDER = 23,
    UNIT_SUBMARINE = 24,
};

/* Raw IDs selecting an entry in gCoDataTable. They are in ROM-table order,
 * not faction or display order. */
enum CoId
{
    CO_NELL = 0,
    CO_ANDY = 1,
    CO_MAX = 2,
    CO_OLAF = 3,
    CO_SAMI = 4,
    CO_GRIT = 5,
    CO_KANBEI = 6,
    CO_SONJA = 7,
    CO_EAGLE = 8,
    CO_DRAKE = 9,
    CO_STURM = 10,
    CO_FLAK = 11,
    CO_LASH = 12,
    CO_ADDER = 13,
    CO_HAWKE = 14,
    CO_HACHI = 15,
    CO_COLIN = 16,
    CO_JESS = 17,
    CO_SENSEI = 18,
};

struct UnkVec
{
    u32 unk00;
    u32 unk04;
};

/* The GBA OBJ attribute triple. `struct UnkVec` is the SAME eight bytes seen
 * from the accessor side -- sub_08015608/sub_0801566C move it as two words and
 * therefore cannot see any structure in it. sub_0804D928 and sub_0804E3B4 do:
 * they fetch one into a stack local, poke four fields and hand it back, and
 * every access lands exactly where the hardware layout says.
 *
 * Evidence, all from those two (identical) functions:
 *   hFlip      `ldrb [fp,#3]` + mask ~0x10  -> attr1 bit 12
 *   paletteNum `ldrb [fp,#5]` + mask  0x0f  -> attr2 bits 12-15
 *   priority   `ldrb [fp,#5]` + mask ~0x0c  -> attr2 bits 10-11
 *   tileNum    `ldrh [fp,#4]` + mask 0xfc00 -> attr2 bits 0-9
 * The two `mov #N; neg` masks are the bitfield-store tell from
 * docs/agbcc-codegen.md, and the widths pin get_best_mode's choice of QI for
 * the three byte-contained fields and HI for the one that straddles bytes 4-5.
 * The u32 container for attr0/attr1 and the u16 one for attr2 are what put
 * hFlip in byte 3 rather than byte 1; a u16 container for every group is
 * byte-identical here and only a field spanning attr0/attr1 could tell them
 * apart. Names outside the four proved fields are the conventional GBA ones
 * and are NOT evidence. */
struct OamData
{
    /* 0x00 */ u32 y : 8;
               /* attr0 bits 8 and 9. SPLIT into two 1-bit fields in wave 29
                * (W29-B) and that is measured, not cosmetic: sub_08016944 and
                * sub_08016974 set and clear bit 9 alone with a bare
                * `orrs #2` / `movs #3; rsbs #0; ands` on byte 1, which is the
                * one-bit-field shape. A 2-bit `affineMode` makes gcc extract
                * and re-insert the pair (`lsl #0x1e; lsr #0x1e; orr; and; orr`)
                * and costs eight bytes. Nothing in the tree named affineMode,
                * so the split is free everywhere else; the hardware agrees --
                * bit 8 is the rot/scale enable and bit 9 is double-size (when
                * 8 is set) or OBJ-disable (when it is clear). */
               u32 affineEnable : 1;
               u32 doubleSize : 1;
               u32 objMode : 2;
               u32 mosaic : 1;
               u32 bpp : 1;
               u32 shape : 2;
    /* 0x02 */ u32 x : 9;
               u32 matrixNum : 3;
               u32 hFlip : 1;
               u32 vFlip : 1;
               u32 size : 2;
    /* 0x04 */ u16 tileNum : 10;
               u16 priority : 2;
               u16 paletteNum : 4;
    /* 0x06 */ u16 affineParam;
};

/* 0x10 bytes: sub_08017A58/sub_08017B8C/sub_08017BB0 all advance a cursor of
 * this type by exactly 0x10. unk04 is a `next` link in sub_08018BAC and the
 * destination address of a store in sub_08017B8C/sub_08017BB0, which cast it;
 * unk0c is the value written there.
 *
 * unk0c has BOTH readings and the handlers are interleaved, so do not settle it
 * from a neighbour. It is an immediate in sub_08017B8C/sub_08017BB0 (`strh`/
 * `str` of the word itself) and sub_08017B64 (`strb`), and a SOURCE ADDRESS in
 * sub_08017BD4/sub_08017BFC/sub_08017C24, which deref it `ldrb`/`ldrh`/`ldr`
 * before the store. Six one-instruction variants of one 40-byte template:
 * {u8, u16, u32} x {immediate, indirect}. The declared `u32` covers both; the
 * indirect trio casts it to a pointer in the .c.
 */
struct Unk0200C528Node /* 0x10 */
{
    /* 0x00 */ u8 filler_00[4];
    /* 0x04 */ struct Unk0200C528Node *unk04;
    /* 0x08 */ u16 unk08; /* ldrh at +8, sub_08018C54/sub_08018D90 */
    /* 0x0a */ u16 unk0a; /* ldrh at +a, sub_08018CBC/sub_08018D90 */
    /* 0x0c */ u32 unk0c;
};

struct Unk0200C528 /* 0x18 */
{
    /* 0x00 */ struct Unk0200C528Node *unk00;
    /* 0x04 */ struct Unk0200C528Node *unk04;
    /* 0x08 */ struct Unk0200C528Node *unk08; /* word slot; sub_08018B40
                                               * writes the node's unk04 here,
                                               * exactly as sub_08018B18 writes
                                               * it to gUnknown_03002F20 */
    /* 0x0c */ u16 unk0c;
    /* 0x0e */ s16 unk0e; /* wave 28, W28-A: a countdown/count-up the
                           * sub_080180A8 / sub_080180CC / sub_0801820C
                           * callbacks step by 1 or 4. SIGNED: the step itself
                           * is a plain `ldrh`/`strh` (movhi needs no
                           * extension), but every read into an int context is
                           * `ldrsh` (sub_080180A8, sub_080180CC) or a
                           * `lsls #0x10; cmp #0; bgt` signed test
                           * (sub_0801820C). */
    /* 0x10 */ s8 unk10; /* wave 29, W29-B: sub_0803D8C0 and sub_0803D92C
                          * `strb` into it through the pointer sub_080193B0
                          * returns, and sub_0803D8F8 reads it BOTH ways --
                          * `movs r0,#0x10; ldrsb r0,[r1,r0]` for the `== 6`
                          * test (the register-offset form an `ldrsb` is forced
                          * into) and a plain `ldrb` for the u8 argument of
                          * sub_08016C70. The signed load is the member's own
                          * type; the `ldrb` is the cast at that use. */
    /* 0x11 */ u8 unk11; /* wave 28, W28-A: `strb` from the node's u16 unk08 in
                          * sub_08018A64/sub_08018ADC, `ldrb` in the callbacks
                          * they install (sub_08018A28, sub_08018AA8) -- a
                          * producer and a consumer agreeing independently. */
    /* 0x12 */ s16 unk12; /* wave 28, W28-A: `strh` from the node's unk0a in
                           * sub_08018A64; sub_08018A28 reads it `ldrsh` and
                           * tests it `< 0`, so the sign is measured, not
                           * assumed. */
    /* 0x14 */ u32 unk14; /* whole-word `str`. sub_08019818 stores its own u16
                           * parameter here through the pointer sub_080193B0
                           * hands back, and that is the only access in the ROM,
                           * so the width is a floor and nothing fixes the
                           * signedness or tells a pointer from an integer.
                           * Wave 26. */
};

struct SpriteEntry /* 0x10 */
{
    /* 0x00 */ struct SpriteEntry *next;
    /* 0x04 */ u16 oam1;
    /* 0x06 */ u16 oam0;
    /* 0x08 */ u16 oam2;
    /* 0x0c */ u16 *object;
};

/* Merged from six variants. unk30 was `u32 unk30; u32 unk34` in the setters
 * and `struct UnkVec unk30` in sub_08015608/sub_08015638, which return it by
 * value; the pair form was never actually read, so the vector form wins.
 *
 * unk0c/unk10 and unk14/unk18 are TWO 8.8 fixed-point pairs, not four loose
 * words: sub_0801D98C sets the first from (s16 x, s16 y) << 8 and sub_0801D96C
 * the second, and sub_0801DDBE does `unk14 + unk0c` -- so unk0c/unk10 is the
 * step and unk14/unk18 the accumulated position.
 *
 * unk1e and unk28 are read by six functions and every one of them uses a bare
 * `ldrb`, so `u8` is the best available model -- but no access sign-extends
 * and none clears them, so an `s8` object read in a `u8` context would look
 * identical. Unproved in the direction the codegen doc warns about.
 */
struct Unk0200E438 /* 0x4c */
{
    /* 0x00 */ u16 unk00; /* Wave 33, W33-B: sub_0801DC50 zeroes both halves. */
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u32 unk04;
    /* 0x08 */ u32 unk08;
    /* 0x0c */ s32 unk0c;
    /* 0x10 */ s32 unk10;
    /* 0x14 */ s32 unk14;
    /* 0x18 */ s32 unk18;
    /* 0x1c */ u16 unk1c;
    /* 0x1e */ u8 unk1e;
    /* 0x1f */ u8 filler_1f[0x01];
    /* 0x20 */ u32 unk20; /* set by sub_08015778 */
    /* 0x24 */ u16 unk24; /* Wave 33, W33-B: sub_0801DC50 zeroes unk24/unk26. */
    /* 0x26 */ u16 unk26;
    /* 0x28 */ u8 unk28;
    /* 0x29 */ u8 filler_29[0x03];
    /* 0x2c */ u32 unk2c; /* returned by sub_0801DA44 */
    /* 0x30 */ struct UnkVec unk30;
    /* 0x38 */ s16 unk38; /* SIGNED and a halfword, both measured: sub_0801D81C
                           * reads it `movs r0,#0x38; ldrsh r1,[r2,r0]` -- the
                           * register-offset `ldrsh` an s16 member is forced
                           * into -- and compares it against -1 before handing
                           * it to sub_08015328, whose parameter is `s16`.
                           * Wave 29, W29-B. */
    /* 0x3a */ s16 unk3a; /* Wave 33, W33-B: sub_0801DC50 seeds it with -1. */
    /* 0x3c */ u16 unk3c;
    /* 0x3e */ u16 unk3e;
    /* 0x40 */ s16 unk40;
    /* 0x42 */ u8 filler_42[0x02];
    /* 0x44 */ u32 unk44;
    /* 0x48 */ u32 *unk48;
};

/* 0xe0 bytes, per the map (gUnknown_0200C500 is the next symbol) -- not the
 * u32[2] it was first declared as. Only the first two words are the save/restore
 * pair shared with gUnknown_0200C500, which really is 8 bytes; the rest of the
 * object is unrelated. unk20 is indexed by `a % 24` (sub_08017860 via __modsi3),
 * which is what fixes its extent at 0x18.
 */
/* Wave 38, W38-H. The per-unit record shared by gUnknown_0200C078,
 * gUnknown_0200C2D0 and gUnknown_0200C420.unk38, and it is a BITFIELD word
 * rather than four bytes: sub_08016A54 clears one with exactly three stores,
 * in this order --
 *   strb 0                        -> bits 0..7
 *   ldr; and 0xFFF000FF; str      -> bits 8..19
 *   ldrh [+2]; and 0xF; strh      -> bits 20..31
 * The middle mask is `~(0xFFF << 8)` and the third keeps only bits 16..19 of
 * the upper halfword, which is what an 8/12/12 split of one word compiles to
 * and what nothing else reproduces (a u8 + two u16s cannot emit the `and`
 * pair).
 *
 * Wave 43 (W43-E) PROVED the same type for gUnknown_0200C420.unk38, which
 * W38-H could only call "very probably" and so left as a `u32`. Four functions
 * in one batch write and read all three fields at the three predicted widths:
 * sub_08038368 stores field 0 with `strb`, field 1 with `ldr; and 0xFFF000FF;
 * orr; str` and field 2 with `ldrh [+0x3a]; and 0xf; orr; strh`, while
 * sub_0803840C and sub_08038434 read field 1 as `ldr; lsls #0xc; lsrs #0x14`
 * and field 2 as `ldrh [+0x3a]; lsrs #4`. Definition moved up here from its
 * old site below gUnknown_0200C020 so struct Unk0200C420 can name it. */
struct Unk0200C078Rec /* 0x04 */
{
    u32 unk00_00 : 8;
    u32 unk00_08 : 12;
    u32 unk00_14 : 12;
};

struct Unk0200C420 /* 0xe0 */
{
    /* 0x00 */ u32 unk00; /* UNSIGNED, and a spendable counter: sub_08017704 is
                           * "subtract if affordable" -- `cmp; bhs` is the
                           * unsigned compare, and it returns 0 rather than
                           * underflowing. Its one caller (sub_08049360)
                           * discards the result. */
    /* 0x04 */ u32 unk04; /* sub_0803BB90 is the predicate `unk04 != 0` */
    /* 0x08 */ u8 unk08; /* sub_08016A14 bumps it while bit 0 is clear */
    /* 0x09 */ u8 unk09; /* Wave 29, W29-B: sub_08016C9C is a `switch` returning
                          * the ADDRESS of one of unk09/unk0a/unk0b for cases
                          * 2/3/4. Its two callers sub_08016CD8 and sub_08016CEC
                          * `ldrb` / `strb` through the returned pointer, which
                          * is what fixes all three at one byte. The pool words
                          * the asm calls gUnknown_0808E540/_0808E544/_0808E548
                          * hold 0x0200C429/0x0200C42A/0x0200C42B -- they are
                          * agbcc's own -fforce-addr address constants for these
                          * members, NOT objects in ROM; nothing else in the
                          * tree references those three addresses and
                          * aw2bhr.lds binds no symbol there. */
    /* 0x0a */ u8 unk0a;
    /* 0x0b */ u8 unk0b;
    /* 0x0c */ u8 unk0c; /* Wave 34, W34-E: sub_08085F94 reads it `ldrb` and
                          * tests it `!= 0` to gate its second sub_08087974
                          * call. Width from the load; it is only ever used as a
                          * truth value, so the signedness is unproved. */
    /* 0x0d */ u8 unk0d; /* wave 27, W27-C: sub_0803BBA8 stores the WORD second
                          * member of the gUnknown_0849EACC dispatch entry here
                          * with a bare `strb`, so the field is one byte and the
                          * table entry is not. Nothing reads it yet, so the
                          * signedness is unsettled. */
    /* 0x0e */ u8 unk0e; /* Wave 34, W34-C: sub_08034780 copies it straight into
                          * the u8 gUnknown_03003FC0.unk09 with `ldrb`/`strb` and
                          * nothing else touches it, so only the width is
                          * proved. */
    /* 0x0f */ u8 unk0f; /* Wave 30, W30-D. A saturating one-byte counter:
                          * sub_08049B28 ends with `if (unk0f != 0xFF) unk0f++`,
                          * plain `ldrb`/`strb` at [rN, #0xf]. The 0xFF ceiling
                          * is what fixes it as UNSIGNED -- a signed byte would
                          * saturate at 0x7F, and comparing an s8 against 0xFF
                          * is never true. */
    /* 0x10 */ u16 unk10; /* `ldrh r0, [r4, #0x10]` in sub_0803C52C, twice, and
                           * fed straight to sub_08037DA4, which bands it into
                           * 2..5 at 0xc7 / 0xf9 / 0x117. A monotone counter of
                           * some kind; the compare in sub_08037DA4 is signed
                           * (`bgt`) but nothing narrows or sign-extends the
                           * value here, so only the 16-bit width is proved. */
    /* 0x12 */ u16 unk12; /* Wave 38, W38-H: sub_08016A54 zeroes unk10 and this
                           * with two consecutive `strh r0,[r1,#0x10]` /
                           * `strh r0,[r1,#0x12]` off the same base, so the
                           * second halfword is a field and not padding. Only
                           * the width is proved; nothing reads it yet. */
    /* 0x14 */ u8 unk14; /* Wave 34, W34-C: sub_08034780 sets
                          * gUnknown_03003FC0.unk0c to `unk14 == 0` -- a plain
                          * `ldrb` feeding do_store_flag (preset 0, `bne` over a
                          * `movs #1`, no unconditional branch). Width only. */
    /* 0x15 */ u8 filler_15[0xb];
    /* 0x20 */ u8 unk20[0x18];
    /* Wave 37 (W37-Q2) carved one WORD out of the old filler_38[0xa8] from
     * sub_0803BA1C's `ldr; ands =0xFFF000FF; str` at +0x38. Wave 43 (W43-E)
     * finished the job: it is an ARRAY of 0x2a bitfield records, not one word
     * plus 0xa4 of filler. sub_0803840C, sub_08038434 and sub_08038368 all walk
     * it with a stride of 4 off the 0x38 base and bound the index at 0x28
     * INCLUSIVE, and sub_08038368 clears element `i + 1` as a run terminator,
     * so 0x29 is live too. 0x38 + 0x2a * 4 = 0xe0 -- exactly the struct's size,
     * with the whole of filler_3c consumed and nothing left over, which is what
     * fixes the extent rather than borrowing it from a neighbour.
     *   Field 1 (bits 8..19) is the liveness key every walker tests against 0;
     * field 0 (bits 0..7) is the id sub_08038368 searches for; field 2
     * (bits 20..31) is the quantity sub_08038434 sums and averages.
     *   c_0803BA1C.c's `unk38 &= 0xFFF000FF` is now `unk38[0].unk00_08 = 0` and
     * was re-verified byte-identical after the retype. That also refutes
     * W37-Q2's note that a bitfield clear "narrows to a byte-sized
     * `movs #N; rsbs; ands`": this field spans bytes 1..2, so store_bit_field
     * has to use SImode and emits the 32-bit pool mask. */
    /* 0x38 */ struct Unk0200C078Rec unk38[0x2a];
};

/* Reached as a struct, not as the u16 array it was first declared as: there is
 * a `strb` at +6, which no u16 element addresses. The distinction is not
 * cosmetic -- sub_08003934's `|= 8` at +0 only matches through an aggregate
 * member lvalue (see the `orr` operand rule in docs/agbcc-codegen.md), so a
 * scalar spelling of this global cannot express it at all.
 */
struct Unk0200B0B0 /* >= 0x76 */
{
    /* 0x00 */ u16 unk00; /* flags; bit 3 set by sub_08003934, cleared by sub_08003948;
                           * bit 14 set by sub_08004BC0 */
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ s8 unk06; /* SIGNED, same tell as unk07/unk11 one line down:
                          * sub_08000694 (wave 13) reads it `movs r0,#6;
                          * ldrsb r0,[r2,r0]`. Was u8; its only other user,
                          * sub_0800056C's `= 1`, is a bare `strb` and is
                          * byte-identical either way (re-verified with
                          * trymatch after the change). */
    /* 0x07 */ s8 unk07; /* SIGNED: sub_08007328 reads it `movs r0,#7;
                          * ldrsb r0,[r2,r0]` -- the register offset is ldrsb
                          * having no immediate form, same shape as unk11.
                          * Used only as a zero test, selecting a ring size of
                          * 10 when clear and 8 when set. */
    /* 0x08 */ s16 unk08; /* The cell coordinate pair the whole 0x0800xxxx tile
                           * family is keyed by. SIGNED: sub_080085E0 reads both
                           * halves `movs r0,#8; ldrsh` / `movs r2,#0xa; ldrsh`
                           * and hands them to nineteen different callees. */
    /* 0x0a */ s16 unk0a;
    /* 0x0c */ s32 unk0c; /* A word countdown: sub_08000694 stores 2 and 10
                           * into it and steps it with `ldr; subs #1; str`,
                           * testing the result with `ble`/`bge` -- a signed
                           * word, so `int`/`s32` and not a narrow field. */
    /* 0x10 */ u8 unk10; /* set to 0, 1 or 2 by family F057 (sub_08004EDC /
                          * sub_08004F1C / sub_08004F5C), which is one body
                          * three times: each tests a different bit of unk00
                          * (0x200 / 0x400 / 0x800) and stores the matching
                          * 0 / 1 / 2 here before starting the same script.
                          * A mode selector, not a flag. Bare `strb` of a
                          * literal, so the width is not otherwise constrained. */
    /* 0x11 */ s8 unk11; /* SIGNED: sub_08000D9C reads it `movs r0,#0x11;
                          * ldrsb r0,[r1,r0]` -- the register offset is ldrsb
                          * having no immediate form, and the sign extension is
                          * the object's, not a cast. Written 0x14 when zero. */
    /* 0x12 */ u8 unk12; /* sub_08004E88 `strb`s sub_0800C874()'s result here
                          * with a bare store, so one byte and the width is a
                          * floor; no reader found. Wave 36, W36-I.
                          *
                          * Wave 48 (W48-A) finds the readers and CONFIRMS u8.
                          * It is the count of live gUnknown_03003150 records:
                          * sub_0800C574 appends at this index and stores the
                          * bumped value back, sub_0800C608 decrements it and
                          * clamps at 0, and sub_0800C874 recomputes it by
                          * scanning the list -- which is why sub_08004E88 and
                          * sub_08003B8C assign that scan's result here.
                          * 0x5B is the last valid index, and sub_0800C574
                          * refuses to append past it.
                          *
                          * Every reader casts: `(s8)gUnknown_0200B0B0->unk12`.
                          * Three matched functions read it that way and the
                          * INSTRUCTIONS DISAGREE -- sub_08010D80 gets
                          * `ldrb; lsl #24; asr #24`, sub_0800C574 and
                          * sub_0800C608 get `movs r0,#0x12; ldrsb r0,[r1,r0]`.
                          * At a constant displacement that choice is context,
                          * not signedness, so do NOT read either form here as
                          * evidence about the type; see the c_08010B34.c
                          * comment this wave corrected. The declaration stays
                          * u8 because all three match under it and the clamp
                          * at 0 is the only thing that wants a sign, which the
                          * cast supplies. */
    /* 0x13 */ u8 unk13; /* Wave 31 (W31-C): four consecutive counters
                          * sub_080088F0 fills with sub_08025308(1..4), one call
                          * per army, each result stored with a bare `strb`. */
    /* 0x14 */ u8 unk14;
    /* 0x15 */ u8 unk15;
    /* 0x16 */ u8 unk16;
    /* 0x17 */ u8 unk17[4]; /* Wave 45 (W45-G), carved out of filler_17.  Four
                             * parallel slots subscripted 0..3 by the pair
                             * sub_0800C75C (set) / sub_0800C7A4 (clear), which
                             * both switch their first parameter over
                             * 0x28/0x48/0x68/0x88 to produce the index.
                             * sub_0800C75C stores its second argument here
                             * with a bare `strb`, so the width is a floor.
                             *
                             * Wave 48 (W48-A) CONFIRMS u8, and confirms it
                             * against a real oracle rather than a load
                             * instruction.  sub_0800C8D8 ORs 0xFF over both
                             * planes in a four-iteration loop and the ROM emits
                             * a full read-modify-write, `ldrb; orrs; strb`, for
                             * each.  Under `s8` agbcc folds `x | 0xFF` to the
                             * constant -1 (representable in QImode once the
                             * type is signed) and emits a bare `strb` of
                             * `movs #1; rsbs` instead -- four bytes shorter and
                             * visibly not the ROM.  The fold fires for s8 and
                             * not for u8, so the instruction count settles both
                             * members.  A plain `= 0xFF` folds the same way, so
                             * the OR is genuinely in the source.
                             *
                             * W48-A first went the OTHER way on this and the
                             * mistake is worth recording, because it looked
                             * like exactly the evidence this project asks for.
                             * sub_0800C6E8 reads unk17 and unk1b four
                             * instructions apart, under the same `>= 0` test,
                             * at the same `base + K + i` register-offset
                             * addressing, and gets `movs r0,#0; ldrsb` for one
                             * and `ldrb; lsls #0x18; asrs #0x18` for the other.
                             * That reads as a controlled A/B in which the
                             * declaration is the only free variable -- and it
                             * is not one.  Both members are u8, both reads are
                             * `(s8)` casts, and the difference comes from
                             * context the comparison cannot see; unk12 above
                             * shows the same one spelling producing both forms
                             * across three matched functions.  **A load
                             * instruction is not evidence about a byte member's
                             * signedness in either direction** -- see the
                             * `ldrsb` chapter of docs/agbcc-codegen.md. */
    /* 0x1b */ u8 unk1b[4]; /* Wave 45 (W45-G), carved out of filler_17.  The
                             * companion plane of unk17 at the same index, and
                             * u8 for the same sub_0800C8D8 reason -- the `|=
                             * 0xFF` over this plane is a read-modify-write too.
                             *
                             * Wave 48 (W48-A) RETYPES this one from s8, the
                             * only declaration this batch changed.  W45-G chose
                             * s8 because sub_0800C7A4 clears it with `movs
                             * r1,#1; rsbs r1,r1,#0` (a literal -1) where unk17
                             * takes `movs r1,#0xff` -- and flagged that itself
                             * as a fact about the CONSTANTS the original wrote
                             * rather than about the declared signedness, which
                             * is how it came out: the source really does spell
                             * -1 into an unsigned plane.  Both stores are bare
                             * `strb`s and byte-identical either way, so
                             * c_0800C75C.c is unaffected; sub_0800C75C and
                             * sub_0800C7A4 were re-verified byte-identical
                             * after the change.  Its readers in sub_0800C6E8
                             * cast to (s8), which is what makes the negative
                             * "empty" marker work. */
    /* 0x1f */ u8 filler_1f[0x01];
    /* 0x20 */ u16 unk20; /* sub_080085E0 stores the map tile under (unk08,
                           * unk0a) here on entry -- `strh`, so 16 bits. */
    /* 0x22 */ u8 filler_22[0x02];
    /* 0x24 */ u16 unk24; /* Wave 37 (W37-D): a unit-ish id, read `ldrh` three
                           * times by sub_08008928 and once more by the same
                           * function's tail. Compared against 0x19 whole, and
                           * separately masked `& 0x3f` to subscript
                           * gUnknown_085D5ABC[] (stride 0x5c) -- so the low six
                           * bits are a type selector inside a wider word, the
                           * same split unk2a records one field over. */
    /* 0x26 */ u16 unk26; /* Wave 37 (W37-D): the sibling of unk2c. sub_08007354
                           * writes one or the other with a bare `strh` from
                           * gUnknown_0200B0D0[i].unk04, picking unk26 when unk07
                           * is set and unk2c when it is clear. */
    /* 0x28 */ u16 unk28; /* index into gUnknown_0200B224 (sub_08001CE8) */
    /* 0x2a */ u16 unk2a; /* The action code sub_080085E0 dispatches on: it
                           * switches on `unk2a & 0x1f` over cases 1..19 and
                           * separately passes the UNMASKED halfword to
                           * sub_0800C454, so the low five bits are a selector
                           * inside a wider word rather than the whole field. */
    /* 0x2c */ u16 unk2c; /* Wave 37 (W37-D): see unk26. */
    /* 0x2e */ u8 unk2e;  /* Wave 37 (W37-D): unk2e/unk2f are copied to
                           * unk30/unk31 by sub_08007354, byte for byte
                           * (`ldrb` then `strb`, no extension either way), so
                           * the two pairs are the same width. */
    /* 0x2f */ s8 unk2f; /* set by sub_080078D4. SIGNED, wave 37 (W37-D):
                          * sub_08008928 reads it `ldrb; lsls #0x18; asrs #0x18`
                          * (the base is already advanced by `adds rN,#0x2f`, so
                          * `ldrsb` has no addressing mode left) and then both
                          * `strh`s the value into the u16 gUnknown_030033EC and
                          * hands it to sub_08025308(int). A u8 member gives no
                          * sign extension at either use. Was u8; the only
                          * writer, sub_080078D4, is a bare `strb` and is
                          * byte-identical either way. */
    /* 0x30 */ u8 unk30;  /* Wave 37 (W37-D): the copy destinations for
                           * unk2e/unk2f. */
    /* 0x31 */ u8 unk31;
    /* 0x32 */ u8 filler_32[0x02];
    /* 0x34 */ s8 unk34;  /* Wave 37 (W37-E): sub_08001DAC reads it
                           * `adds rN,#0x34; ldrb; lsls #0x18; asrs #0x18` and
                           * hands the value to sub_0800272C/sub_08002844's
                           * fifth (int) parameter -- the same load form unk2f
                           * carries, and for the same reason: the base is
                           * already advanced past ldrsb's register-offset-only
                           * addressing. No writer found in asm/, so the width
                           * is a floor. */
    /* 0x35 */ u8 filler_35[0x01];
    /* 0x36 */ s8 unk36;  /* Wave 37 (W37-D): two (value, shadow) byte pairs,
                           * one per unk07 side -- sub_08007354 copies unk36 to
                           * unk37 and unk38 to unk39. SIGNED because
                           * sub_080079B8 reads unk36/unk38 `ldrb; lsls #0x18;
                           * asrs #0x18` before adding 4 / 3; the copy in
                           * sub_08007354 is byte-to-byte and says nothing. */
    /* 0x37 */ s8 unk37;
    /* 0x38 */ s8 unk38;
    /* 0x39 */ s8 unk39;
    /* 0x3a */ s16 unk3a; /* SIGNED: sub_08007328 reads it `movs r4,#0x3a;
                           * ldrsh r0,[r2,r4]`. A ring index -- 08007328 stores
                           * (unk3a + n - 1) % n into unk3c, with n from unk07. */
    /* 0x3c */ u16 unk3c;
    /* 0x3e */ s16 unk3e; /* SIGNED: sub_08003814 reads it `movs r1,#0x3e;
                           * ldrsh r0,[r0,r1]` -- the register offset is ldrsh
                           * having no immediate form. Used only as a zero
                           * test, selecting a sprite x of 0xCE when nonzero
                           * and 2 when zero. Wave 36, W36-I. */
    /* 0x40 */ u8 filler_40[0x02];
    /* 0x42 */ s16 unk42; /* SIGNED, corrected in wave 36 (W36-I): sub_08003890
                           * is the first reader, and it reads it
                           * `movs r0,#0; ldrsh r3,[r1,r0]` and then compares
                           * the value against -2, -3 and -10. A u16 member
                           * gives the zero-extending `ldrh` there. It is a
                           * screen y that walks between -10 and -2. The store
                           * described below is byte-identical either way
                           * (re-verified with trymatch after the change), so
                           * the reasoning it records is about the SOURCE's
                           * spelling of the constant, not about the width.
                           *
                           * Wave 30, W30-D. sub_08003910 stores the literal
                           * 0xFFF6 here with a `strh` through `adds rN, #0x42`
                           * (past strh's imm5*2 = 62 limit), immediately after
                           * `sub_080152EC(gUnknown_08485D8C, 0)`. The constant
                           * arrives from the pool rather than as
                           * `movs #0xa; rsbs`, which is what agbcc does for an
                           * unsigned 16-bit store of 0xFFF6; a signed -10 would
                           * be the two-instruction negate. So u16, value
                           * 0xFFF6. No reader found. */
    /* 0x44 */ u8 filler_44[0x02];
    /* 0x46 */ u16 unk46; /* An x/y pair, always written together as a literal
                           * `strh` pair by the 0x08004xxx screen setups:
                           * (0x15,0x10) by sub_08004B7C/sub_08004C5C,
                           * (0x57,0x10) by sub_080049E8, (0x15,0x18) by
                           * sub_08004C10. Reached as `adds rN, #0x46` because
                           * THUMB's strh immediate stops at 62. Nothing reads
                           * them yet, so the width is a floor. Wave 26. */
    /* 0x48 */ u16 unk48;
    /* 0x4a */ u16 unk4a; /* Wave 37 (W37-E): the second state machine in
                           * sub_08001DAC, switched over 0 / 0xA / 0x14 / 0x1E /
                           * 0x64 / 0x6E / 0x78 with a plain `ldrh` and no sign
                           * extension anywhere, so u16. */
    /* 0x4c */ u16 unk4c;
    /* 0x4e */ s16 unk4e; /* Wave 37 (W37-E): a screen x eased toward a target by
                           * `v += (K - v) >> 3`, exactly like unk82 -- the
                           * `K - v` read is a bare `movs rI,#0; ldrsh`, which a
                           * u16 member cannot produce, and the values run
                           * 0xFC40..0x1180. sub_08001DAC also reads it
                           * `ldrh; lsls #0x10; asrs #0x14`, the sign extension
                           * merged into a `>> 4`. */
    /* 0x50 */ s16 unk50; /* Wave 37 (W37-E): a screen y walked by unk54 and
                           * clamped to 0x6A..0xB8. SIGNED: every read in
                           * sub_08001DAC is `movs rI,#0; ldrsh`. */
    /* 0x52 */ u8 filler_52[0x02];
    /* 0x54 */ s16 unk54; /* Wave 37 (W37-E): the step added to unk50, capped at
                           * 8. SIGNED: sub_08001DAC does `ldrh; adds #n; strh`
                           * and then tests the STORED value `lsls #0x10;
                           * asrs #0x10; cmp #7; ble` -- the same shorten_binary_op
                           * pair unk7a carries. */
    /* 0x56 */ u8 filler_56[0x04];
    /* 0x5a */ u8 unk5a; /* Wave 37 (W37-E): sub_08002510's state byte, the same
                          * 0 -> 0xA -> 0x14 -> 0x1E -> 0x32 -> 0x3C -> 0x46 ->
                          * 0x50 machine sub_080030BC runs on unk72, but a single
                          * lane rather than an array. Plain `ldrb`/`strb`. */
    /* 0x5b */ u8 unk5b; /* Wave 37 (W37-E): sub_08002510's tail counter,
                          * `ldrb; adds #1; ands #0x3f; strb` -- the `adds #1`
                          * (not `adds #255`) says nothing about signedness
                          * because it is an increment; the `& 0x3f` keeps it in
                          * range either way, so u8 is the weakest fit. */
    /* 0x5c */ s16 unk5c; /* Wave 37 (W37-E): unk5a's companion, unk82's role for
                           * this lane -- eased by `v += (K - v) >> 3` with the
                           * `K - v` read a bare `ldrsh`, values 0xFD80..0x11A0. */
    /* 0x5e */ u8 filler_5e[0x02];
    /* 0x60 */ s16 unk60; /* Wave 50 (W50-E): carved out of filler_5e, which
                           * nothing outside this header ever named, so the
                           * layout is unchanged. sub_08000E48's opening reset
                           * writes 0xFC00 here and 0x1220 into unk5c through
                           * ONE base register (`adds r1,#0x60; strh; subs r1,#4;
                           * strh`), which is what pins the offset. SIGNED by
                           * companionship with unk5c, whose comment already
                           * records the 0xFD80..0x11A0 range for the same
                           * easing pair; nothing reads it under a load form
                           * that would settle the sign independently. */
    /* 0x62 */ u8 filler_62[0x03];
    /* 0x65 */ u8 unk65; /* Wave 37 (W37-E): a one-shot "cursor moved" flag.
                          * sub_08000BF8 sets it to 1 with a bare `strb` when the
                          * terrain or unit under (unk08, unk0a) differs from the
                          * cached unk2a/unk24; sub_08001DAC tests it `ldrb;
                          * cmp #0` to decide whether to run the unk66 animation,
                          * and clears it there. */
    /* 0x66 */ u8 unk66; /* Wave 37 (W37-E): that animation's state byte, switched
                          * over 0 / 8 / 9 / 0xA and stepped `ldrb; adds #1;
                          * strb`. */
    /* 0x67 */ s8 unk67; /* SIGNED, wave 37 (W37-E), on the W37-A `--` test rather
                          * than the load form: sub_08001DAC does
                          * `ldrb; subs r0,#1; strb`, and agbcc emits `subs #1`
                          * only through a signed lvalue -- a u8 `x--` gives the
                          * modulo-256 `adds rD,rS,#0; adds rD,#255`. The reload
                          * that follows tests it `ldrb; lsls #0x18; asrs #0x18;
                          * cmp #0; bge`, i.e. as a signed frame countdown. */
    /* 0x68 */ s16 unk68; /* Wave 37 (W37-E): the animation's angle/scale word,
                           * stepped `+= 0x20` and clamped at 0x100, then fed to
                           * gSinLut and Div. SIGNED: read `movs rI,#0; ldrsh`
                           * and the clamp tests the stored value
                           * `lsls #0x10; asrs #0x10; cmp #0xff; ble`. */
    /* 0x6a */ u8 unk6a;  /* A one-byte result code sub_080085E0 leaves behind:
                           * 0x2d, 0x4b, 0x87, 0x88, 0x89 or 0x8a depending on
                           * which of its nineteen cases ran. Reached as
                           * `adds rN, #0x6a; strb` because THUMB's strb
                           * immediate stops at 31. */
    /* 0x6b */ s8 unk6b;   /* a proc/sprite id with -1 as "none", and SIGNED:
                           * sub_08005F1C / sub_08007B74 read it `ldrsb`, test
                           * it against -1 and hand it to sub_08015328's s16
                           * parameter, then write 0xFF back. Reached as
                           * `adds rN, #0x6b; ldrsb` / `strb` for the same
                           * addressing-mode reason as unk6a above (wave 20,
                           * W20-C). */
    /* 0x6c */ u8 unk6c;  /* cleared alongside unk6e/unk70 by sub_08005D50,
                           * sub_08007A0C */
    /* 0x6d */ u8 unk6d;  /* Wave 37 (W37-D): the dwell counter for the unk6c
                           * state machine. sub_08007A30 seeds it with 0x1e on
                           * the 0x0a -> 0x14 edge and, in state 0x14, does
                           * `ldrb; subs #1; strb` and then tests the value it
                           * READ (not the one it stored) as signed --
                           * `lsls #0x18; cmp #0; bgt` -- so it is a
                           * post-decrement compared `<= 0`. Left u8 with the
                           * sign supplied by an (s8) cast at the one use: the
                           * load is a plain `ldrb`, which an s8 member does not
                           * produce here.
                           *
                           * WAVE 37, W37-A DISAGREES, with a DISCRIMINATING
                           * use rather than a load form: sub_08005D74 and
                           * sub_08005E30 (both matched) decrement it, and the
                           * ROM does `ldrb r1; subs r2, r1, #1; strb r2`.
                           * Spelled `gUnknown_0200B0B0->unk6d--` on a u8 member
                           * agbcc emits `adds r2, r1, #0; adds r2, #255` -- the
                           * modulo-256 wraparound -- and both functions miss by
                           * +2 bytes; through an `s8` lvalue they match. So the
                           * OBJECT is signed and the member should probably be
                           * `s8`; it is left `u8` only because W37-D's
                           * sub_08007A30 was in flight against this
                           * declaration in the same wave. Whoever touches it
                           * next: retype to `s8`, drop the `*(s8 *)&` in
                           * work/sub_08005D74 and work/sub_08005E30, and
                           * re-run trymatch on sub_08007A30 -- the `--` beats
                           * the load form, because the load form was measured
                           * in wave 30 to be a position artefact and not a type
                           * tell (see docs/agbcc-codegen.md). */
    /* 0x6e */ s16 unk6e; /* an x/y pair: (0x100,0x800) by sub_08005D50,
                           * (0x780,0x970) by sub_08007A0C.
                           * SIGNED, wave 37 (W37-D): sub_08007A30 reads both
                           * halves `movs r1,#0; ldrsh r3,[r0,r1]` while easing
                           * them toward a target with `v += (K - v) >> 3`, and
                           * separately reads them for the sub_0801BD00 calls as
                           * `ldrh; lsls #0x10; asrs #0x14`, which is the
                           * sign-extension merged into the `>> 4`. A u16 member
                           * gives `lsrs #0x14` there and no `ldrsh` at all. The
                           * two literal-store functions above are
                           * byte-identical either way (re-verified with
                           * trymatch after the change). */
    /* 0x70 */ s16 unk70;
    /* 0x72 */ u8 unk72[4]; /* An ARRAY, wave 36 (W36-I): sub_080030BC indexes
                             * it by its own first parameter (`adds rN,#0x72;
                             * adds r4,rN,r5`) and sub_08003088 calls that with
                             * 0, 1, 2, 3. It is a per-lane state byte stepped
                             * through 0 -> 0xA -> 0x14 -> 0x1E -> 0x32 ->
                             * 0x3C -> 0x46 -> 0x50 by two switches. Was four
                             * separate members, which is why sub_08003040 and
                             * sub_08003064 write all four one after another;
                             * both re-verified after the change. */
    /* 0x76 */ u8 unk76[4]; /* An ARRAY, wave 37 (W37-E), the same four lanes as
                             * unk72: sub_080032EC indexes it by its own first
                             * parameter (`adds rN,#0x76; add rN,r8`) and
                             * sub_080030BC -- which sub_08003088 calls with
                             * 0, 1, 2, 3 -- is what calls sub_080032EC. A second
                             * per-lane state byte, stepped 0 -> 0xA -> 0x14 ->
                             * 0x1E. */
    /* 0x7a */ s16 unk7a[4]; /* Wave 36 (W36-I): the same four lanes as unk72,
                              * a frame countdown. sub_080030BC does
                              * `ldrh; subs #1; strh` and then tests the stored
                              * value as SIGNED (`lsls #0x10; cmp #0; bgt`),
                              * which a u16 member cannot produce. Seeded from
                              * the eight-byte ROM table gUnknown_0808D754. */
    /* 0x82 */ s16 unk82[4]; /* Wave 36 (W36-I): four lanes again, a screen x
                              * eased toward a target by `v += (K - v) >> 3`.
                              * SIGNED: the `K - v` read is a bare `ldrsh`
                              * (`movs rN,#0; ldrsh r1,[r3,rN]`) and the values
                              * range over 0xFD80..0x1180, i.e. -640..4480.
                              * The `ldrh` in the `+=` is shorten_binary_op on
                              * the same member, not a second object. */
    /* 0x8a */ s16 unk8a[4]; /* Wave 37 (W37-E): four lanes again, the sprite
                              * scale sub_080032EC divides gSinLut by. SIGNED:
                              * every read is `movs rI,#0; ldrsh`, and the
                              * clamp at 0x100 tests the stored value
                              * `lsls #0x10; asrs #0x10`. */
    /* 0x92 */ s16 unk92[4]; /* Wave 37 (W37-E): the matching per-lane angle,
                              * counted down by 0x20 with a wrap to +0x148 and
                              * masked `& 0xff` before indexing gSinLut. SIGNED
                              * for the same `movs rI,#0; ldrsh` reason; the
                              * `ldrb` at the first gSinLut read is the `& 0xff`
                              * folded into the load, not a second object. */
    /* 0x9a */ u8 filler_9a[0x02];
    /* 0x9c */ u8 unk9c; /* Wave 30, W30-B: sub_0800517C is nothing but
                          * `gUnknown_0200B0B0->unk9c = 0`, reached as
                          * `adds rN, #0x9c` (past `strb`'s imm5) with a bare
                          * `strb`. No reader found, so u8 is the weakest fit,
                          * exactly as for unkae below.
                          * WAVE 40 (W40-G) FOUND THE READER, and it says this
                          * is really `u8 unk9c[0x13]` spanning 0x9c..0xae --
                          * i.e. absorbing filler_9d AND unkae -- a
                          * NUL-terminated name string. sub_080048D4 copies it
                          * byte by byte into a gUnknown_03001470 slot at +0x1e,
                          * stopping at the NUL, with the bound `i <= 0x12`
                          * exactly matching that extent; sub_0800487C strcmps
                          * the same two addresses through sub_0808B694.
                          * The codegen proves the ARRAY, not just the size: the
                          * ROM computes `g + 0x9c` and then adds `i`, which is
                          * the ARRAY_REF tree, where `(&g->unk9c)[i]` on this
                          * scalar lets fold reassociate to `g + i` then
                          * `+ 0x9c` and misses by exactly that swapped pair.
                          * NOT CHANGED HERE because it would rename unkae,
                          * which sub_0800492C writes -- src/decomp/c_080048D4.c
                          * uses a local struct to get the same tree and is
                          * byte-identical. Whoever owns unkae should merge
                          * them. */
    /* 0x9d */ u8 filler_9d[0x11];
    /* 0xae */ u8 unkae; /* sub_0800492C clears it with a bare `strb` through
                          * `adds rN, #0xae`; no reader found, so u8 is the
                          * weakest fit and the extent above is a floor, not a
                          * measurement. Wave 26. */
};

/* Wave 37 (W37-D). Stride 0x1c, proved by the `lsls #3; subs; lsls #2` multiply
 * (i * 8 - i, then * 4 == i * 28) that sub_08007354 and sub_080079B8 both use on
 * the bare symbol -- so the symbol IS the array, not a pointer to one. The two
 * agree on the element too: sub_080079B8 `strh`s its parameter into +4 and
 * sub_08007354 `ldrh`s the same slot back out into struct Unk0200B0B0's
 * unk26/unk2c. Nothing reaches any other offset, so the extent of filler_06 is
 * the stride and not a measurement, and the width of unk04 is the store width.
 * The index runs 0..9 (sub_08007354 wraps `i + 4` at 9 and `i + 3` at 7), which
 * is a floor on the element count.
 */
/* Wave 50, W50-G. sub_08002C38 and sub_08002AB0 walk this table eight entries
 * at a time and name four more offsets, which carves filler_00 and filler_06
 * down without moving unk04:
 *   +0x00 a word of flags, tested against 1 / 8 / 0x10 / 0x20 / 0x40 / 0x80 /
 *         0x100 with `ldr`, and cleared `&= ~8` with `str` -- so a full word,
 *         not a halfword.
 *   +0x06 SIGNED: both readers do `movs r1,#6; ldrsh r0,[r6,r1]`, the register
 *         offset being ldrsh having no immediate form.
 *   +0x0c / +0x10 word fixed-point coordinates: every use is `asrs #8` before
 *         being handed to sub_080029F4 / sub_08002844, and an arithmetic shift
 *         is what makes them signed.
 */
struct Unk0200B0D0 /* 0x1c */
{
    /* 0x00 */ s32 unk00;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ s16 unk06;
    /* 0x08 */ u8 filler_08[0x04];
    /* 0x0c */ s32 unk0c;
    /* 0x10 */ s32 unk10;
    /* 0x14 */ u8 filler_14[0x08];
};

extern struct Unk0200B0D0 gUnknown_0200B0D0[];

/* Wave 42 (W42-B2). A table of halfword PAIRS that sub_080077EC and
 * sub_080078E4 both copy into gUnknown_0200B224's 4-byte entries. Declared
 * FLAT because the two readers scale it differently and only a flat u16 array
 * spells both: sub_080077EC indexes it `row * 10 + col * 2` (and `+ 1`) in
 * halfword units, while sub_080078E4 indexes `(k * 5 + col) * 2` (and `+ 1`),
 * which folds to a byte scale of 4 with the second read as a `[rN, #2]`
 * displacement. Both readouts agree the pair stride is 4 bytes and the row
 * stride is 5 pairs; whether the original declared it 2-D or as a struct is
 * not recoverable from either use. */
extern const u16 gUnknown_084887AC[];

/* Wave 42 (W42-B2). Two 0xFF-terminated halfword scripts sub_080078E4 walks,
 * one per branch of its first argument. gUnknown_08488810's entries come in
 * pairs -- the first halfword's bits 5..7 select whether the pair is copied
 * verbatim or replaced out of gUnknown_084887AC, and its low five bits are the
 * switch selector -- while gUnknown_08488856 is a flat list of single
 * halfwords. Both are read `ldrh` with the pointer walking `+2`, so u16 is a
 * measurement and not a floor; the extents are unknown. */
extern const u16 gUnknown_08488810[];
extern const u16 gUnknown_08488856[];

/* Wave 37 (W37-D). Two ROM blobs sub_08007A30 hands to sub_0801BD00's `void *`
 * third parameter, one per axis, alongside the x/y it eases out of
 * gUnknown_0200B0B0's unk6e/unk70. Nothing dereferences either one here, so the
 * element type is the weakest thing that converts to `void *` without a
 * const-discard warning, and the extent is unknown. */
extern u8 gUnknown_08488880[];
extern u8 gUnknown_08488888[];

/* Wave 50 (W50-I). Four parallel 16-entry ROM tables sub_0800CAA0 searches in
 * lockstep, one per player slot, matching them against
 * gUnknown_03003FC0.unk33[1..4]. Every byte in 0x084888A0-0x0848891F is 2, 4, 6
 * or 8, and sub_0800CAA0 reads each one as `ldrb; lsrs #1` before comparing it
 * against a slot value -- so the stored values are the slot numbers 1..4
 * DOUBLED, and u8 is a measurement, not a floor.
 *
 * data/data.s carves this region into 16-byte symbols, and the four the search
 * uses are NOT consecutive: they are at 0x084888B0, 0x084888D0, 0x084888F0 and
 * 0x08488910, i.e. every other carve. The odd rows (0x084888C0, 0x084888E0,
 * 0x08488900) are a second, interleaved set of four that nothing in this tree
 * reads yet, and 0x084888A0 sits one row BEFORE the first -- so the whole
 * 0x084888A0-0x0848891F block is very likely two 4x16 tables and not eight
 * independent ones. Not proved; left as separate externs because that is what
 * the one reader shows.
 *
 * THE FOURTH ONE HAS NO SYMBOL OF ITS OWN, and this is the readable part: the
 * ROM's pool word holds 0x08488900 and the code adds 0x10 in a separate
 * instruction, so the original source reaches the fourth row as
 * `gUnknown_08488900[i + 0x10]`. agbcc splits a `symbol + constant` address
 * constant into a pool load plus an `adds` on Thumb rather than putting the
 * addend in the pool word, so this is the honest spelling and NOT a missing
 * declaration -- do not add a gUnknown_08488910. */
extern const u8 gUnknown_084888B0[];
extern const u8 gUnknown_084888D0[];
extern const u8 gUnknown_084888F0[];
extern const u8 gUnknown_08488900[];

/* Stride 4, proved by `lsls #2` in sub_08001CE8, sub_08001D24 and sub_08006288;
 * sub_080077EC reaches element 9 as `+ 0x24`. Member +0x00 is SIGNED --
 * sub_08001CE8 reads it `ldrsh`, while the sub_08001D24 readers use `ldrh` and
 * immediately mask with 0x1f/0x3f, which is a u16 context on a signed object.
 */
struct Unk0200B224 /* 0x04 */
{
    /* 0x00 */ s16 unk00;
    /* 0x02 */ u16 unk02; /* Wave 42 (W42-B2): named, not reshaped -- it was
                           * filler_02[2] and the layout is unchanged.
                           * sub_080077EC fills entries 9..13 by copying a
                           * halfword PAIR out of gUnknown_084887AC into +0 and
                           * +2 (`strh [rN]` / `strh [rN,#2]`, base stepped by
                           * 4), so the slot is a halfword. Only the writer is
                           * known; no reader reaches +0x02 yet, so u16 is the
                           * weakest fit rather than a measurement. */
};

/* Stride 0x0c, proved by `lsls #1; adds; lsls #2` in sub_08011C18, which is
 * also the only reader so far -- it clears all four members of all 48 entries.
 * The word members hoist their offset onto the base (`adds r5, r4, #4`) while
 * the byte and halfword ones fold into a displacement, which is the array-
 * global rule in docs/agbcc-codegen.md and is what fixes the layout.
 * unk08 and unk0a are VOLATILE: each store is preceded by a load of its own
 * address into a register nothing reads, which for a QI/HImode aggregate
 * member is the volatile tell. The two words carry no such signal -- SImode
 * never gets the dead load -- so they are left plain.
 */
struct Unk0200B3B4 /* 0x0c */
{
    /* 0x00 */ u32 unk00;
    /* 0x04 */ u32 unk04;
    /* 0x08 */ volatile u16 unk08;
    /* 0x0a */ volatile u8 unk0a;
    /* 0x0b */ u8 filler_0b[0x01];
};

struct Unk0200F720 /* 0x10 */
{
    /* 0x00 */ u16 unk00; /* wave 28: the first three halfwords, carved out of
                           * what was filler_00[6]. sub_0801E22C writes all
                           * three with `strh`, sub_0801E248 the first two and
                           * sub_0801E264 only unk04 -- three entry points into
                           * one record, which is what separates them as
                           * members rather than one wider field. Widths are
                           * the store width and nothing here reads them, so
                           * the signs are unproved. */
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ u16 unk0c; /* 0x0c/0x0e set together by sub_0801E294 */
    /* 0x0e */ u16 unk0e;
};

struct Unk0200F920Entry
{
    u16 unk00;
    u16 unk02;
};

/* The first eight bytes came out of filler in wave 29 (W29-B): sub_0801F150
 * fills the header of one element -- `str` at +0, `strb` at +4 (its own u8
 * fourth argument), `strb` of 0 at +5 and `strh` at +8, which is
 * unk08[0].unk00. +6/+7 are still untouched by anything matched. */
struct Unk0200F920 /* 0x88 */
{
    /* 0x00 */ void *unk00;
    /* 0x04 */ u8 unk04;
    /* 0x05 */ u8 unk05;
    /* 0x06 */ u8 filler_06[0x02];
    /* 0x08 */ struct Unk0200F920Entry unk08[32];
};

/* 0x02027F74 -- unk36/unk37 are a cursor and its inclusive upper bound, reset
 * together by sub_080374F0 (0, 0xff), sub_08037508 (0, 0xff) and
 * sub_0803753C/sub_08037570 (0, 0x6a).
 */
struct Unk02027F74 /* >= 0x38 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 unk03;
    /* 0x04 */ u8 unk04[0x32]; /* Wave 34, W34-B: was filler_04. sub_08087104
                                * indexes it with a word member of its own
                                * argument and reads a byte, reaching it as
                                * `gUnknown_02027F74 + 4` with a CLEAN pool word
                                * and a runtime add. The 0x32 extent is
                                * unchanged and still comes from the cursor pair
                                * that follows it, not from a bound this reader
                                * proves.
                                *
                                * CORRECTED, wave 46 (W46-C). The clean pool
                                * word is NOT evidence that sub_08087104 reads
                                * the list through this member array, and
                                * W34-B's added claim that it does so "rather
                                * than through the overlapping gUnknown_02027F78
                                * extern" is what kept that function parked at
                                * 96.0%. BOTH spellings emit the clean word plus
                                * a runtime `adds #4` -- one compile_probe with
                                * the two side by side settles it -- and they
                                * differ only in which scratch register the base
                                * and the index land in. The member array is the
                                * one that does NOT match. sub_08087104 is now
                                * matched with c_08086E54.c's idiom: a `u8 *`
                                * local set to `(u8 *)&gUnknown_02027F74` and
                                * bumped by 4 in a statement of its OWN, with
                                * the struct name still used for the +0..+3 byte
                                * reads, which is what keeps the base live in
                                * r5. This member array remains the right MODEL
                                * of the object; it is simply not how that
                                * reader spells the access. */
    /* 0x36 */ u8 unk36;
    /* 0x37 */ u8 unk37;
};

/* Wave 37, W37-Q1. A 16-entry table of 8-byte slots -- aw2bhr.lds puts
 * gUnknown_02027FB0 at 0x02027FB0 and the next symbol at 0x02028030, i.e.
 * exactly 0x80 bytes, and sub_0803C750 walks it with `adds r2, #8` for
 * `i = 0 .. 0xf`.
 *
 * unk00 is a POINTER and not an int: sub_0803C784 fills the table from
 * gUnknown_0849EDB0[].unk08, which it also calls through `bl _call_via_r1`.
 * unk04 is one byte (`ldrb`/`strb` at +4) and UNSIGNED -- sub_0803C750 rejects
 * an entry whose unk04 is above 1 with `cmp r0,#1; bhi`, the unsigned form.
 * It is a small use count capped at 2. */
struct Unk02027FB0 /* 0x08 */
{
    /* 0x00 */ int (*unk00)(int);
    /* 0x04 */ u8 unk04;
    /* 0x05 */ u8 filler_05[0x03];
};

struct Unk02028030 /* 0x48 */
{
    /* 0x00 */ u8 unk00[8];
    /* 0x08 */ u8 unk08[8];
    /* 0x10 */ u8 unk10[2];
    /* 0x12 */ u8 unk12[0x18];
    /* 0x2a */ u8 unk2a[3];
    /* 0x2d */ u8 unk2d[3];
    /* 0x30 */ u8 unk30[0x18];
};

struct Unk020280C0 /* 0x1c */
{
    /* 0x00 */ u8 filler_00[0x02];
    /* 0x02 */ u8 unk02[0x11]; /* a NUL-terminated byte string: sub_0803CCB8
                                * hands &g[i].unk02 to the copier sub_0803CC84,
                                * which walks it until a zero byte. The extent
                                * is the space available before unk13, not a
                                * proved bound. */
    /* 0x13 */ u8 unk13; /* 0xff = the slot is empty; sub_0803CCB8 refuses to
                          * copy the name out when it holds 0xff, and
                          * sub_0803CD14 returns it. sub_0803CCEC makes the
                          * same test and hands out gUnknown_0849F320 instead
                          * of &g[i].unk02 when it fires, which is what fixes
                          * that symbol as a fallback string of the same
                          * shape. */
    /* 0x14 */ u8 filler_14[0x08];
};

struct Unk02029A10 /* 0x24 */
{
    /* 0x00 */ u8 unk00; /* sub_08051F4C sets it to 1 with `strb` when unk01
                          * reads 1. Both are BYTES -- `ldrb [r1,#1]` and
                          * `strb [r1]` off the same element address -- which
                          * is what narrows the old filler_00[4]. */
    /* 0x01 */ u8 unk01; /* a three-way state: sub_08051F4C branches on == 1,
                          * then on == 0, and does nothing for anything else. */
    /* 0x02 */ u8 unk02; /* Wave 49, W49-D: sub_08057164 zeroes it with `strb`
                          * in the same five-store run that writes unk00/unk01
                          * (`strb`) and unk04/unk06 (`strh`), so it is a byte
                          * beside them and not padding. Carved out of
                          * filler_02, which keeps the layout unchanged. */
    /* 0x03 */ u8 filler_03[0x01];
    /* 0x04 */ u16 unk04; /* sub_0804D290/sub_0804DCA8 fill unk04/unk06 from a
                           * two-halfword ROM record and then copy them into
                           * x/y in the very next statement, so this pair is the
                           * source the position is seeded from. `strh`, hence
                           * 16 bits.
                           *   sub_0804FA2C seeds the same four from a 2-D view
                           * of that record -- `pos[gUnknown_03004580[i][5]]
                           * [i*5 + j]` with a 40-byte row -- and writes x/y
                           * from the table rather than from unk04/unk06, so the
                           * copy in the twins is a choice of that function and
                           * not a property of the fields. */
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 x;  /* UNSIGNED still, despite the `ldrsh` in sub_0804FA2C:
                        * a u16 member handed to sub_080155C0's s16 parameter
                        * compiles to `ldrsh` anyway, because combine folds
                        * sign_extend(truncate(zero_extend(mem:HI))). Verified
                        * by compiling it both ways. `ldrsh` is only the
                        * s16-object tell where there is no narrowing context. */
    /* 0x0a */ u16 y;
    /* 0x0c */ u16 xSub;
    /* 0x0e */ u16 ySub;
    /* 0x10 */ u16 xStep;
    /* 0x12 */ u16 yStep;
    /* 0x14 */ u16 frame;
    /* 0x16 */ u16 frameCount;
    /* 0x18 */ s16 unk18; /* A proc id, and SIGNED: sub_08053860 / sub_08053BB8
                           * read all ten of these with `ldrsh` and hand each to
                           * sub_08053614, which tests the value against -1
                           * before passing it to sub_080153F0. -1 is
                           * "no proc". */
    /* 0x1a */ u16 unk1a; /* Wave 32, W32-A. sub_080513FC and sub_08051920 both
                           * read it `ldrh` off the `* 0xb4` / `* 0x24` element
                           * address and test it against 0 as the "entry is
                           * idle" guard before raising bit 6 of
                           * gUnknown_02029664. Width from the load; nothing
                           * signs it and nothing writes it in the matched
                           * tree, so u16 is the weakest model. */
    /* 0x1c */ u16 unk1c; /* a flag, and the producer/consumer pair that types
                           * it is independent (wave 20, W20-C): sub_08052818
                           * SETS it with `movs r0,#1; strh r0,[r4,#0x1c]` off
                           * the entry base, and sub_08052650/sub_08052AF4 test
                           * `ldrh [r0,#0x1c]; cmp #1` before calling
                           * sub_08052E04. Halfword at both ends, so it is not
                           * the `u8 unk00`/`u8 unk01` pair's third byte. */
    /* 0x1e */ u16 unk1e; /* zeroed with `strh` by sub_0804D290/sub_0804DCA8 as
                           * the last of the five entry writes. */
    /* 0x20 */ u16 unk20; /* Wave 37, W37-I. An accumulator whose LOW NIBBLE is
                           * the phase: sub_0804CA98 does
                           * `unk20 += gUnknown_02029B94[side][slot]` with an
                           * `ldrh; adds; strh` pair, acts when
                           * `(unk20 & 0xf) == 1`, and parks 0xff in it to stop
                           * the sequence. Halfword from the read-modify-write;
                           * signedness unproved. Was the head of filler_20. */
    /* 0x22 */ u16 unk22; /* Wave 37, W37-I. The step counter unk20's phase
                           * drives: sub_0804CA98 does `unk22++` with `ldrh;
                           * adds #1; strh`, compares the STORED value against 3
                           * through a zero-extending `lsls #0x10; lsrs #0x10`
                           * (which is what makes it u16 and not s16), and uses
                           * it as the ENTRY index into this same group for the
                           * x/y pair it then writes. Was filler_20. */
};

/* Wave 32, W32-A. The per-side animation driver record, one per
 * gUnknown_02029A10 group: sub_0804B330 steps unk0e, consults unk00/unk08 and
 * then reads two rows of unk04's table; sub_0804B2A8 gates on unk08/unk00 and
 * sub_0804BA64 writes unk00/unk0a. The 0x10 stride is `lsls #4` at all four
 * sites. Every member is `ldrh`/`strh` and nothing sign-extends, so `u16` is
 * the weakest type that fits rather than a proof.
 *
 * unk04 is a `u16 *` and the table behind it has at least 0x28 columns:
 * sub_0804B330 reads `unk04[i]` and `unk04[i + 0x28]` off ONE CSEd address
 * (`adds r1, #0x50`), an (x, y) pair split into two rows rather than
 * interleaved. */
struct Unk02029690 /* 0x10 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u8 filler_02[0x02];
    /* 0x04 */ u16 *unk04;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ u8 filler_0c[0x02];
    /* 0x0e */ u16 unk0e;
};

/* Hoisted out of src/decomp/c_08056E28.c in wave 32 (W32-A) so that
 * sub_0804BCB8, which builds one of these on its own stack and hands over the
 * address, can name the same type. A five-field request record: two indices
 * then four values, all u16. */
struct Unk56E28
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ u16 unk0c;
};

struct Unk02029A10Group /* 0xb4 */
{
    /* 0x00 */ struct Unk02029A10 entries[5];
};

/* 0x02029BA8 -- an array of 0x20-byte records that is nothing but eight
 * pointers. 24 sites in code-0801D390.s reach it and every one of them is
 * `lsls #5` for the element and then a word `ldr`; the element offsets seen
 * are 0x00, 0x04, 0x08, 0x0c, 0x10, 0x14 and 0x18, and 0x04/0x08/0x0c/0x10/
 * 0x14 are each handed straight to sub_080156E8.
 *   THE "SO THEY ARE ADDRESSES, NOT COUNTERS" THAT USED TO BE ON THAT SENTENCE
 * IS WRONG and wave 20 (W20-C) retracts it. sub_080156E8 narrows its second
 * parameter `lsls #0x10; lsrs #0x10` at entry and its matched tail-callee
 * sub_080156FC uses that value as `(u16)x * 4` added to the pointer at
 * gUnknown_0200E438[..].unk48 -- a small TABLE INDEX. `void *` is kept anyway,
 * because every caller loads the member with a word `ldr` and passes it with
 * no narrowing in front of the `bl`, so the declaration is byte-identical at
 * all of them and a `u16` member would have wanted `ldrh`. See the note on
 * sub_080156E8 in include/unknown-functions.h.
 * Nothing reads any field narrower than a word and nothing writes
 * one, so `void *` is the weakest type that fits every use.
 *   unk18  is an ARRAY, not a scalar: sub_0804EAEC and sub_08050424 both
 *          compute `base + 0x18 + i*0x20 + j*4` with j in a register, while
 *          sub_0804D25C/sub_0804D6C8/sub_0804E100 take element 0 of it. Two
 *          words is all that fits inside the 0x20 stride, so the extent is
 *          bounded even though no site proves j reaches 1.
 * The count is unknown -- no loop over the array has been matched yet. */
struct Unk02029BA8 /* 0x20 */
{
    /* 0x00 */ void *unk00;
    /* 0x04 */ void *unk04;
    /* 0x08 */ void *unk08;
    /* 0x0c */ void *unk0c;
    /* 0x10 */ void *unk10;
    /* 0x14 */ void *unk14;
    /* 0x18 */ void *unk18[2];
};

/* 0x0202FDFC -- one record, not the three separate globals the disassembly
 * suggests. sub_08074670 loads a SINGLE pool word holding 0x0202FE38 and then
 * recovers the record base with `subs #0x3c` and the byte array with
 * `adds #0x12`; two independent symbols could never be related that way, so
 * 0x0202FE0E and 0x0202FE38 are members of this object at +0x12 and +0x3c.
 *   unk10  cleared by sub_08074AAC just before it starts its proc
 *   unk12  42 entries, scanned 0..0x29 by sub_08076858 (which reaches it by its
 *          own pool word, i.e. `&g.unk12`) and cleared whole by sub_080745C0
 *   unk3c  SIGNED: sub_08074670 reads it `ldrsh` and compares against -1, which
 *          is the value sub_080745C0 resets it to. Signedness is settled by
 *          that read, not by the reset.
 * The extent past +0x3e is unexplored.
 */
/* Wave 43 (W43-F), from sub_08038240 -- the whole layout comes from that one
 * function, which initialises every member. It sits DIRECTLY BELOW
 * gUnknown_0202FDFC (0x0202FDEC + 0x10 == 0x0202FDFC), so 0x10 is a hard
 * ceiling on its size, not a guess.
 *
 * unk00 and unk04 are two parallel 4-byte arrays, proved together by the clear
 * loop: one pointer walks with `adds r0,#1` while storing 0 at both [r0] and
 * [r0,#4], so the two runs are 4 apart and 4 long. unk08 and unk09 are their
 * fill counts -- each is read, tested `> 3` UNSIGNED, then stored back
 * incremented, which is what bounds both arrays at 4 entries. The two arrays
 * partition armies 1..4 on sub_080266DC's predicate.
 *
 * unk0a is a halfword (`ldrh`/`strh` at +0xa) holding
 * gUnknown_08499598[..].unk38, optionally doubled. unk0c is a halfword too and
 * takes the u32 gUnknown_0200C420.unk00 through a TRUNCATING `strh`, so the
 * narrowing is at the store and says nothing about the source's width.
 * Signedness is unproved throughout: every read is a plain `ldrb`/`ldrh` and
 * nothing sign-extends or compares signed. 0x0e/0x0f are untouched. */
struct Unk0202FDEC /* >= 0x0e */
{
    /* 0x00 */ u8 unk00[4];
    /* 0x04 */ u8 unk04[4];
    /* 0x08 */ u8 unk08;
    /* 0x09 */ u8 unk09;
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ u16 unk0c;
};

struct Unk0202FDFC /* >= 0x3e */
{
    /* 0x00 */ s16 unk00;      /* wave 19: a CAMERA ORIGIN pair. sub_080748A0
                                * subtracts them from a proc's own s16 +0x2c and
                                * +0x2e to get the two PutSprite coordinates, so
                                * they are the scroll offset of the view this
                                * struct describes. Both `ldrsh` off a zero index
                                * register, which is what pins them as SIGNED
                                * halfwords at 0 and 2 rather than part of a
                                * wider field -- an unsigned pair would have been
                                * `ldrh [.,#0]` / `ldrh [.,#2]` with no index. */
    /* 0x02 */ s16 unk02;
    /* 0x04 */ s16 unk04;      /* wave 30 (W30-C): the SECOND half of the camera
                                * pair -- sub_0807610C publishes unk00 + unk04
                                * and unk02 + unk06 as one (x, y), which is what
                                * pairs 04/06 with 00/02 rather than leaving them
                                * filler. `s16` follows the 00/02 precedent and
                                * is corroborated by sub_08075EC4, which reads
                                * +4 and +6 with register-offset `ldrsh` and
                                * divides each by 8 SIGNED (`cmp #0; bge; adds
                                * #7; asrs #3`). sub_0807610C's own reads are
                                * `ldrh` only because the sum is stored straight
                                * back with `strh` and the sign cannot survive
                                * the truncation. */
    /* 0x06 */ s16 unk06;
    /* 0x08 */ s16 unk08;      /* wave 34 (W34-J): sub_08076A68 zeroes it with a
                                * `strh` at an immediate offset, in the same run
                                * of stores as unk00 and unk02, so it is a
                                * halfword field and not filler. No reader has
                                * turned up yet, so the SIGN is unproven -- s16
                                * only follows the 00..06 precedent. */
    /* 0x0a */ u8 filler_0a[0x02];
    /* 0x0c */ s32 unk0c;      /* wave 24: a plain word `ldr`, added to a proc's
                                * own +0x58 counter in sub_0807831C to index the
                                * 8-byte records of gUnknown_0861500C, so it is a
                                * BASE INDEX into that table. Nothing narrows it
                                * and nothing proves the sign; s32 is the weakest
                                * type that gives the bare `ldr`. */
    /* 0x10 */ u8 unk10;
    /* 0x11 */ u8 unk11;       /* wave 30 (W30-C): a two-valued mode flag, read
                                * with a bare `ldrb` at an immediate offset by
                                * sub_08076B7C, sub_08076BF0 and sub_08076C1C --
                                * no sign extension anywhere, so u8. The three
                                * readers disagree on how they test it (`!= 0`,
                                * `== 1`, `== 0`), which is why only the width
                                * is claimed here. */
    /* 0x12 */ u8 unk12[0x2a]; /* Wave 48, W48-H: the gUnknown_0202FE0E the
                                * splitter invents is this array's base
                                * (0x0202FDFC + 0x12) and not a separate object
                                * -- sub_08076B20 matched spelling it
                                * `gUnknown_0202FDFC.unk12[...]`, whose pool
                                * word carries the 0x12 addend. Bit 0 of an
                                * element is a flag that function clears. */
    /* 0x3c */ s16 unk3c;
};

/* NOT GLOBALS, and this REPLACES a wrong entry that stood for two waves.
 * gUnknown_081CC590, gUnknown_081CC594 and gUnknown_081CC59C are three
 * `-fforce-addr` address-constant words and ALL THREE hold 0x0202FDFC, i.e.
 * &gUnknown_0202FDFC -- read straight out of baserom.gba, which is the only
 * thing that settles it. gUnknown_081CC598, four bytes into the same run, holds
 * 0x03002EE0 = &gpKeySt (aw2bhr.map). Same class as the 0x081CC4D8/0x081CC4DC
 * pair recorded beside gUnknown_0202FDFC below and the 0x081D92D8 quadruple in
 * the 0x08078 section: the splitter invents a `gUnknown_<addr>` for a pool word
 * and a real global alike and cannot tell them apart.
 *
 * The entry that used to live here declared a fictional `struct Unk81CC590 *`
 * pair and cost two waves. It made sub_08076CAC and sub_08076D68 park at 80.3%
 * with 37 bytes differing -- every one a register number -- because binding
 * `&gUnknown_081CC590` to a `**` local reproduces the ROM's instruction stream
 * but not its allocation. Naming gUnknown_0202FDFC directly matched both
 * outright (wave 35, W35-B). The park had ALSO measured "the honest spelling"
 * at 10.6% and concluded it was impossible; what it had actually measured was
 * `gUnknown_081CC590->unk04`, which is THREE loads because the fictional
 * pointer needs a force-addr word of its own. The real honest spelling is two.
 *
 * So sub_08076CAC clamps the camera's X pair (+0x04 against +0x00) and
 * sub_08076D68 the Y pair (+0x06 against +0x02) of gUnknown_0202FDFC itself,
 * which is why the horizontal limits (0x19F, 0xBF, 0xE0) and the vertical ones
 * (0xEF, 0x5F, 0x90) split exactly along the 00/04 and 02/06 offsets that
 * struct's own entry already documents as one camera block. Both matches need a
 * `rodata` entry at promotion. */

/* 0x03000640 -- the smooth-scroll state sub_08076E20 advances each frame. A
 * TARGET pair at +0x00/+0x02, written from gUnknown_08614588 doubled, and a
 * CURRENT pair at +0x04/+0x06 that eases toward it by half the remaining
 * distance. All four are `ldrsh` off a zero index register, so s16.
 * unk08 is a free-running frame counter and is UNSIGNED: both of
 * sub_08076E20's modulo tests on it compile to __umodsi3, not __modsi3. */
struct Unk03000640 /* 0x0c */
{
    /* 0x00 */ s16 unk00;
    /* 0x02 */ s16 unk02;
    /* 0x04 */ s16 unk04;
    /* 0x06 */ s16 unk06;
    /* 0x08 */ u32 unk08;
};

extern struct Unk03000640 gUnknown_03000640;

/* 0x08614588 -- s16 pairs read by sub_08076E20 at bits 4..7 of its argument.
 * FLAT, not an array of 4-byte records, and the ROM is decisive about it: for a
 * struct, agbcc computes the record address once and reaches the members at
 * displacements 0 and 2, which is two instructions shorter than what the ROM
 * does. The ROM instead shares the `lsls #2` index and re-adds it to a SECOND
 * base formed by `adds r1, #2` -- the signature of two independent subscripts,
 * one shared scaled index re-based onto a second pointer -- a TWO-DIMENSIONAL
 * array, `gUnknown_08614588[n][0]` and `gUnknown_08614588[n][1]`.
 * Measured wave 34 (W34-J), all three spellings against the same ROM: a struct
 * of two s16 folds the second member into a `ldrsh [rN, #2]` displacement and
 * loses the `adds r1, #2` entirely (two instructions short), while a FLAT
 * `s16 []` indexed `n * 2` and `n * 2 + 1` rebuilds the whole index for the
 * second subscript (`lsls #1; adds #1; lsls #1`, two instructions long). Only
 * the 2-D form shares `lsls #2` across both and re-bases. */
extern s16 gUnknown_08614588[][2];

/* 0x08614458 -- a byte table indexed by sub_080761C8's own +0x40 step counter
 * (0..3) and used as both a halfword index and sub_08071900's third argument.
 * SIGNED: the read is `ldrsb` off a zero index register, not `ldrb`. */
extern s8 gUnknown_08614458[];

/* Wave 45 (W45-I). 0x086145C8, sixteen bytes ahead of the 0x086145D8 run
 * below. sub_080772B8 hands the bare symbol to sub_080733C8's second parameter
 * and no other function in the ROM touches it, so nothing constrains the
 * element type or the length -- `u8 []` is only the weakest spelling that
 * gives the plain pool-word address, and is UNPROVEN. Left non-const to match
 * its 0x086145D8 neighbours. */
extern u8 gUnknown_086145C8[];

/* Wave 35 (W35-B). Four byte tables of the 0x086145xx run, all indexed by the
 * +0x44 step counter of the 0x08077xxx animation bodies, which runs 0..4 and
 * resets -- so each table is 5 entries, which is exactly the spacing of the four
 * symbols (D8, DD, E2, E7). They pair up: 086145D8 with 086145DD (sub_08077690,
 * and sub_08077DF0 uses the first alone), 086145E2 with 086145E7 (sub_08077870,
 * and sub_08077954 uses the first alone).
 *
 * THE TWO PAIRS ARE DIFFERENT TYPES AND THE LOADS PROVE IT. 086145D8 and
 * 086145E2 are read with register-offset `ldrsb` -- the only signed byte load
 * THUMB has -- so they are `s8 []`. 086145DD and 086145E7 are read with a plain
 * `ldrb` FOLLOWED BY `lsls #0x18; asrs #0x18`, which is a `u8 []` with an
 * explicit (s8) cast at the use; an `s8 []` would have emitted `ldrsb` like its
 * neighbour in the same function does. The widening is visible at all only
 * because both are stored into the `int` gUnknown_0300064C.
 *
 * The s8 pair is sub_08071900's third argument and also a halfword index into
 * the two frame buffers (`0x9e - v`, doubled); the u8 pair is the scroll
 * position that gets published to gUnknown_0300064C and then fed to
 * sub_08077620 as `0xA8 - v`. */
extern s8 gUnknown_086145D8[];
extern u8 gUnknown_086145DD[];
extern s8 gUnknown_086145E2[];
extern u8 gUnknown_086145E7[];

/* 0x08614410 -- the proc script sub_08075E68 Proc_StartBlockings under its
 * caller's proc, storing the started proc's own +0x58, +0x2c, +0x30 and +0x54.
 */
extern const struct ProcCmd gUnknown_08614410[];

/* 0x03000278 -- a pending DMA/CpuFastSet descriptor. sub_0801BB88 fills it
 * with src = gUnknown_03002520 (the OAM shadow), dst = 0x07000000 (OAM),
 * unk08 = 0 and unk0a = a halfword count; sub_0801BC08 skips the copy when
 * unk0a is zero and otherwise does CpuFastSet(unk00, unk04, unk0a << 1).
 * The neighbour gUnknown_03000268 is a second descriptor of the same shape.
 */
struct Unk03000278 /* 0x0c */
{
    /* 0x00 */ void *unk00;
    /* 0x04 */ void *unk04;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
};

/* 0x03000288 -- 16 entries of stride 0x2c. sub_0801C6E8 scans unk00 for a
 * key and returns the entry; sub_0801C1F8 clears every unk00 in reverse.
 * Both loops compare the cursor against the base with a SIGNED branch.
 */
struct Unk03000288 /* 0x2c */
{
    /* 0x00 */ u32 unk00;
    /* 0x04 */ u8 filler_04[0x28];
};

/* Merged from four variants. unk00 is a word in sub_080153F0 and unk08 is a
 * word in sub_08015C30, so this one carries two independent word fields.
 *
 * unk04 is a bytecode cursor and the record it points at is 8 bytes: every
 * advancing handler adds exactly 8 (sub_08015D78, sub_08015DA0, sub_08015E58,
 * sub_080160DC), and the two fields reached inside it are a word at +0 and a
 * halfword at +4. The word at +0 is read three ways -- into unk08
 * (sub_08015D78), into unk0c (sub_08015E58), and back into unk04 itself as a
 * jump target (sub_08015E04/sub_08015E2C, which also zero unk10 and unk38).
 * The halfword at +4 goes to unk10 (sub_08015DA0). Left as `const void *` and
 * cast at each use rather than given a struct: the +0 field is a u32 in two
 * handlers and a pointer in a third, so no single member type is right.
 *
 * Wave 11 added ten more handlers on the same cursor and they settle the +4
 * halfword as an UNSIGNED 16-bit operand, independently of unk10's declared
 * type. Eight of them (sub_08015FE4, sub_08016010, sub_0801603C, sub_08016068,
 * sub_08016104, sub_08016130, sub_0801615C, sub_08016188) are the same 44 bytes
 * differing only in the `bl` target: read `((const u16 *)unk04)[2]`, hand it to
 * one call, advance the cursor by 8, return FALSE. The read is a bare
 * `ldrh r0, [r0, #4]` in every one, with no `ldrsh` and no re-narrowing --
 * which is what forced sub_0803B4DC and sub_0803B524 off `s16` in
 * unknown-functions.h; see the note there.
 * sub_08016094 is the tenth and the only one that does not advance: when
 * gUnknown_03002F1C is set it clears the flag and takes the JUMP instead,
 * `unk04 = *(const void *const *)unk04` -- the same word-at-+0 target
 * sub_08015E04 uses, reached through a one-shot flag rather than an opcode.
 * All ten return a bool8 explicitly (`movs r0,#0` / `movs r0,#1` with
 * `pop {r1}`), so these handlers are non-void; the flag is presumably
 * "the interpreter should yield after this command".
 */
/* WAVE 40 (W40-G): THIS STRUCT IS AN OVERLAY UNION AND THE OFFSETS BELOW ARE
 * ONE VIEW OF IT, not a single layout. sub_080048D4 copies a 19-byte
 * NUL-terminated name into 0x1e..0x30 of a slot with `strb`, straight across
 * unk1e, unk20, unk22, unk24, unk26 and the first byte of unk28 -- all of which
 * have independent, measured evidence of their own (unk1e's signed compare in
 * sub_0803B118, the bare `ldrsh` reads of unk20/unk22/unk24). Both readings are
 * real, so different subsystems view the same 0x60-byte slot differently; wave
 * 40's W40-F reached the same conclusion independently from sub_08019A60 and
 * named its view `struct Unk8019A60`.
 * DO NOT widen a member here to fit a new function -- add a view instead, the
 * way c_080048D4.c does with a local struct. */
struct Unk03001470 /* 0x60 */
{
    /* 0x00 */ u32 unk00;
    /* 0x04 */ const void *unk04;
    /* 0x08 */ u32 unk08;
    /* 0x0c */ u32 unk0c;
    /* 0x10 */ u16 unk10;
    /* 0x12 */ u16 unk12; /* a halfword flag word. sub_0801527C stores 4 and
                           * sub_08015224 stores 0 with a plain `strh`, and
                           * sub_08015438 ORs bit 1 in (`ldrh; orrs #2; strh`)
                           * -- the read-modify-write is what fixes the width
                           * at 16 bits rather than 8. Signedness is unproved:
                           * nothing sign-extends it and nothing compares it.
                           * Wave 25. */
    /* 0x14 */ u8 unk14;
    /* 0x15 */ u8 filler_15[0x03];
    /* 0x18 */ int unk18; /* sub_08037610 is
                           * `sub_080152EC(gUnknown_0849D41C, 0)->unk18 = a`,
                           * a whole-word `str`. Its one caller sub_08037638
                           * computes the value as `base + ((n & 0x3ff) << 5)`,
                           * so this is very likely an address; `int` is kept
                           * because the shift is explicit in the caller rather
                           * than coming from a pointer type, and the store is
                           * byte-identical either way. */
    /* 0x1c */ u16 unk1c; /* wave 30 (W30-C): sub_08066580 `strh`s its own slot
                           * index here on the entry sub_080152EC just handed
                           * back -- the same index it then uses for
                           * gUnknown_08580934->unk74[] and ->unk70[]. Halfword
                           * width from the `strh`; signedness unproved, nothing
                           * reads it yet. Was filler. */
    /* 0x1e */ s16 unk1e; /* SIGNED, and measured: sub_0803B118 does
                           * `ldrh; adds #1; strh` and then sign-extends the
                           * stored value with `lsls #0x10; asrs #0x10` before
                           * `cmp r0,#0x5a; ble` -- a SIGNED compare of the
                           * value of `++p->unk1e`. A `u16` member gives the
                           * zero-extending `lsrs` there instead. A frame
                           * counter: it is bumped once per call and the
                           * function gives up at 0x5a (90 frames). Wave 24. */
    /* 0x20 */ s16 unk20; /* every sub_080152EC caller that stores anything
                           * stores a halfword here (sub_08035144,
                           * sub_08042B84).
                           * SIGNED, from wave 13: sub_0802966C reads it
                           * `movs r2,#0x20; ldrsh r1,[r0,r2]` to index
                           * gUnknown_0849A06C, and `ldrh` where only the low
                           * byte survives -- the s16-object pattern. The two
                           * plain `strh` writers are byte-identical either
                           * way (re-verified with trymatch). */
    /* 0x22 */ s16 unk22; /* SIGNED, same `ldrsh` tell in sub_0802966C, where
                           * it indexes gUnknown_030040D8->unk07[]. */
    /* 0x24 */ s16 unk24; /* sub_0802966C stores 1 here when sub_0802E7C8
                           * reports 1.
                           * SIGNED, corrected in wave 34 (W34-D): sub_080277BC
                           * reads it THREE times as an index into
                           * gUnknown_08090AA8 and every one is a bare `ldrsh`
                           * (`movs r2,#0x24; ldrsh r1,[r1,r2]`), which a u16
                           * member cannot produce -- it gives the zero-extending
                           * `ldrh` instead. The `++` in the same function still
                           * loads `ldrh` because its only consumers are the
                           * halfword store and the index, so force_to_mode drops
                           * the sign extension there; that is the same split
                           * c_08027B68.c records for Unk27B10Proc.unk48 and NOT
                           * a contradiction. Nothing promoted reads the member --
                           * c_080151B0.c and c_0802966C.c only `strh` a
                           * constant into it, which is byte-identical either
                           * way and was re-verified. */
    /* 0x26 */ u16 unk26;
    /* 0x28 */ u32 unk28; /* four consecutive words, all written as `str` with
                           * the offset hoisted onto the base -- the `g[i].m`
                           * shape from docs/agbcc-codegen.md. sub_0804D928 and
                           * sub_0804E3B4 zero unk28/unk2c and load unk30/unk34
                           * from the u16 globals gUnknown_0300453C /
                           * gUnknown_0300451C, so the members are at least 32
                           * bits and the sources are narrower. */
    /* 0x2c */ int unk2c; /* SIGNED, and that is measured rather than assumed
                           * (wave 17). sub_08050958 bumps it once per tick and
                           * then compares it against the u16
                           * gUnknown_02029710[side].unk16 with `cmp r0,r1;
                           * bgt` -- a SIGNED branch. `u32 unk2c` promotes the
                           * u16 to unsigned and emits `bhi` instead, so the
                           * member cannot be unsigned. The three `= 0` writers
                           * (c_0804D928, c_0804E3B4, c_0804EEFC) are
                           * byte-identical either way and were re-verified. */
    /* 0x30 */ u32 unk30;
    /* 0x34 */ u32 unk34;
    /* 0x38 */ u16 unk38; /* Wave 39, W39-F: probably SIGNED, and left alone.
                           * sub_0802A6B0 reads it FIVE times and every one is
                           * `movs rN,#0x38; ldrsh r0,[rM,rN]`, which only a
                           * signed halfword member produces; the `++` in the
                           * same function still loads `ldrh` because only the
                           * store consumes it. sub_0802A3FC, which reads the
                           * same member into a u16 cursor, gets the plain
                           * `ldrh` either way, so it does not discriminate.
                           * NOT changed to s16 here: other promoted units read
                           * this member and the declaration is not mine to
                           * flip mid-wave. c_0802A6B0.c writes the explicit
                           * `(s16)` cast instead, which is byte-exact --
                           * sign_extend(subreg(zero_extend(mem:HI))) folds
                           * straight back to `ldrsh`. */
    /* 0x3a */ u8 unk3a; /* Wave 29, W29-C. sub_0808A6A0 writes 6 here on the
                          * slot sub_08014740 hands back, as `adds r0, #0x3a;
                          * strb` -- past strb's 31-byte displacement, so the
                          * split is addressing rather than a member array.
                          * Bare `strb`, hence u8; nothing reads it yet. */
    /* 0x3b */ u8 filler_3b[0x01];
    /* 0x3c */ void (*unk3c)(void); /* Wave 32, W32-A. sub_08018758 stores
                                     * sub_080185A0 -- a `void (void)` -- here
                                     * with a whole-word `str` on the slot
                                     * sub_08014740 hands back, and that is the
                                     * only access in the ROM, so the width is
                                     * hard and the callback reading is from the
                                     * stored value alone. */
    /* 0x40 */ u8 filler_40[0x02];
    /* 0x42 */ u8 unk42; /* Wave 32 (W32-B): sub_0801A614 copies this byte, the
                          * halfwords at +0x48 and +0x4a and the byte at +0x4c
                          * into one gUnknown_03002F50 stack entry, so the four
                          * travel together. All bare loads (`ldrb`/`ldrh`), and
                          * the destination fields are s8, so the widths are
                          * from the loads and the signedness is unproved. */
    /* 0x43 */ u8 filler_43[0x01];
    /* 0x44 */ u16 unk44; /* Wave 29, W29-A. sub_080656E0 and sub_0806D820 both
                           * `strh` 0xe here, right after setting unk28/unk2c on
                           * the slot sub_080152EC just handed them. Reached as
                           * `adds rB,#0x44` because 0x44 is past `strh`'s
                           * displacement range. */
    /* 0x46 */ u8 filler_46[0x02];
    /* 0x48 */ u16 unk48; /* Wave 29, W29-A. sub_0802D33C reads it `ldrh` off
                           * the slot sub_080637AC returns and compares it
                           * against 0xf, picking 1 or 0x10. The width is hard
                           * (`ldrh`); the displacement is past `ldrh`'s 5-bit
                           * range so it arrives as `adds rB,#0x48`, which is
                           * addressing, not a member-array tell. Signedness
                           * unproved -- the compare is `bhi`, i.e. unsigned,
                           * which is what a u16 member promotes to anyway. */
    /* 0x4a */ u16 unk4a; /* Wave 32 (W32-B) -- see unk42. */
    /* 0x4c */ u8 unk4c;
    /* 0x4d */ u8 filler_4d[0x13];
    /* Wave 35, W35-I: everything from 0x3c to 0x5f is PER-SUBSYSTEM SCRATCH and
     * the members above are only one view of it. The float path-mover at
     * 0x080161B4 (sub_080161B4, sub_080162A4, sub_08016370, sub_0801642C,
     * sub_080164E0 -- all five matched) reads the same bytes as
     *     0x3c float x       0x40 float y
     *     0x4c float dx      0x50 float dy
     *     0x54 float ddx     0x58 float ddy
     *     0x5c int   frames_remaining
     * with whole-word `str`s at every one, which cannot coexist with unk3c's
     * function pointer, unk42/unk44/unk48/unk4a or the byte at unk4c. The
     * original almost certainly had a union here. It is NOT modelled in this
     * struct on purpose: a union would rename `.unk3c` and four other members in
     * every promoted file that uses them, for no byte. Those five functions
     * carry a file-local `struct Unk1470Path` plus a wrapper struct instead --
     * and the wrapper is load-bearing, see the W35-I section of
     * docs/agbcc-codegen.md, because a plain pointer subscript folds the member
     * offset into the load displacement where the ROM hoists it onto the base.
     * unk38 is the mover's tick, read `ldrsh` and incremented `ldrh`, which the
     * u16 declaration above is compatible with at every one of its writers. */
};

/* Defined here, out of address order, because struct Unk030020A8 below holds a
 * pointer to one. The ROM word gUnknown_0808E5C8 points at an array of these:
 * sub_0801A6C0 clears unk00 of entries [0..0x80] with a stride of 0x0c, then
 * zeroes entry 0's unk04. unk04 is a `next` link -- sub_0801A700 pops the list
 * whose head is gUnknown_030020A8.unk04 with `head = head->unk04` and returns
 * the popped node's unk00.
 *
 * Wave 41 (W41-A): unk08 is no longer unreached. sub_0801A718 is the SORTED
 * INSERT into this list -- it fills unk00 and unk08 of entry [count] and then
 * walks the chain comparing its s16 argument against unk08 (`movs r7,#8;
 * ldrsh r0,[r3,r7]`, a signed compare), so unk08 is the sort key. The two
 * bytes after it are still unreached. */
struct Unk0808E5C8 /* 0x0c */
{
    /* 0x00 */ u32 unk00;
    /* 0x04 */ struct Unk0808E5C8 *unk04;
    /* 0x08 */ s16 unk08;
    /* 0x0a */ u8 filler_0a[0x02];
};

/* Exactly 8 bytes -- gUnknown_030020B0 is the next symbol. sub_0801A6C0
 * initialises it (`strh` at +0, `str` at +4) and sub_0801A700 pops unk04. */
struct Unk030020A8 /* 0x08 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u8 filler_02[0x02];
    /* 0x04 */ struct Unk0808E5C8 *unk04;
};

struct Unk03002040 /* 0x58 */
{
    /* 0x00 */ u8 filler_00[0x50];
    /* 0x50 */ u16 unk50;
    /* 0x52 */ u16 unk52;
    /* 0x54 */ u16 unk54;
    /* 0x56 */ u16 unk56;
};

struct Unk03002B80 /* 0x35a */
{
    /* 0x000 */ u8 unk00;
    /* 0x001 */ u8 filler_01[0x357];
    /* 0x358 */ u16 unk358;
};

/* 0x03002F50 -- an 8-byte-entry stack, with gUnknown_03002F24 as its cursor.
 * sub_0801A604 resets the cursor to the base; sub_0801A614 fills one entry and
 * bumps the cursor by 8; sub_0801A664 pops (compares the cursor against the
 * base for empty, then steps back 8 and reads the entry it uncovered).
 * The four bytes are read back with `ldrb; lsls #24; asrs #24`, so they are
 * signed; +0x04 is a whole word copied from another struct's +0x20. */
struct Unk03002F50 /* 0x08 */
{
    /* 0x00 */ s8 unk00;
    /* 0x01 */ s8 unk01;
    /* 0x02 */ s8 unk02;
    /* 0x03 */ s8 unk03;
    /* 0x04 */ u32 unk04;
};

/* Three byte-sized flags reached off one symbol. Not three separate globals:
 * every access goes through one pool word for 0x030030F0 plus a displacement,
 * so the original had a single aggregate here.
 *   unk00  a countdown -- sub_08034F7C bumps it, sub_08034F8C decrements it
 *          with a floor at zero, sub_08034FA4 clears it. SIGNED: sub_08034F8C
 *          reads it with `ldrsb` and sub_08034F6C returns it sign-extended.
 *   unk01  set/cleared by sub_08034F48 / sub_08034F54, read by sub_08034F60
 *   unk02  set/cleared by sub_0803BD54 / sub_0803BD60, read by sub_0803BD6C
 * unk01 and unk02 are only ever read zero-extended, so u8 is the weakest type
 * that fits; s8 fits equally well if a sign-extending read ever turns up.
 */
struct Unk030030F0 /* 0x03 */
{
    /* 0x00 */ s8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02;
};

/* 8 bytes exactly: sub_080171B4 and sub_08017540 copy it whole through the
 * two-word thumb_load_double_from_address pair, to and from a caller struct at
 * +0xB98. unk00 is written 0/8/0xf as a byte (sub_08074384, sub_080743B8,
 * sub_08078468); unk02 is read `ldrh` by sub_08017FA8 and `ldrb` by
 * sub_080180DC -- the byte read is a u8-context read of the halfword.
 */
struct Unk03002F08 /* 0x08 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 filler_01[0x01];
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u8 filler_04[0x04];
};

struct Unk03003130 /* 0x12 */
{
    /* 0x00 */ u8 unk00; /* Wave 30, W30-B: sub_080276D0 stores 0 and
                          * sub_080276F0 stores 1 with a bare `strb` on the
                          * base register, immediately before seeding unk04
                          * from gUnknown_08090A98[0] / [1]. A mode selector
                          * paired with that word. */
    /* 0x01 */ u8 filler_01[0x03];
    /* 0x04 */ int unk04; /* Wave 30, W30-B: a whole-word `str` of the s16
                           * gUnknown_08090A98[unk00], sign-extended by the
                           * `ldrsh` that loads it -- so the member is word
                           * wide and the value that reaches it is signed. */
    /* 0x08 */ u8 unk08; /* Wave 31 (W31-C): sub_0802BB98 writes 0 here with a
                          * bare `strb` on the else side of a test where the
                          * then side writes 1 into unk00 instead, so the two
                          * are different fields and not one. */
    /* 0x09 */ u8 filler_09[0x03];
    /* 0x0c */ int unk0c; /* Wave 31 (W31-C): a whole-word `str`, 0x94 on one
                           * side of that test and 3 on the other -- the two
                           * stores are tail-merged into a single `str`. */
    /* 0x10 */ u8 unk10;
    /* 0x11 */ u8 unk11;
};

/* A 2 x u16 pair used for several independent cursor/position globals.
 * gUnknown_03003F24 and gUnknown_03003100 are also compared as a whole word,
 * so those two are declared through the union below.
 */
struct Unk802C57C
{
    u16 unk00;
    u16 unk02;
};

/* The SIGNED view of the same pair. gUnknown_03003100 is read both ways and the
 * two readings are distinguishable: sub_0802CBA0, sub_0802CBC8 and sub_0802CC04
 * all reach its two halves with `movs rI,#0 / ldrsh rD,[rB,rI]` and
 * `movs rI,#2 / ldrsh`, the s16-OBJECT tell -- a u16 member converted to the
 * s16 parameters those callees declare would emit `ldrh` and let the CALLEE
 * sign-extend, not `ldrsh` at the call site, and an (s16) cast on a u16 member
 * would emit `ldrh; lsls #16; asrs #16` instead. Added as a union member rather
 * than by retyping struct Unk802C57C, which a dozen promoted files share and
 * which several of them genuinely read unsigned. Layout is unchanged. */
struct Unk802C57CS
{
    s16 unk00;
    s16 unk02;
};

union Unk802C57CBuf
{
    struct Unk802C57C pos;
    struct Unk802C57CS spos;
    u32 raw;
};

/* 0x0202575C -- Wave 30, W30-A. A four-byte command record filled in one
 * breath by sub_080320CC and handed straight to sub_0802F588 as that
 * function's first argument: `strb 0xa8` at +0, the s8 gUnknown_0849B018->unk06
 * at +1, then `strh 0` at +2. The halfword store at +2 is what makes it a
 * struct rather than a byte array. */
struct Unk0202575C /* 0x04 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u16 unk02;
};

/* Wave 41, W41-D. The 12-entry per-slot record array at gUnknown_02025564+0x20
 * that sub_0802F28C resets. Layout is read straight off that reset: five
 * scalars at +0x00, +0x01, +0x13, +0x19, +0x1a and two byte runs cleared with
 * their own descending loops, +0x02..+0x12 (17 bytes) and +0x14..+0x18 (5).
 * Stride 0x1c from the `adds r3,#0x1c` per iteration and the `i * 28` giv.
 * NON-volatile: none of these stores carries the dead load in front of it that
 * every store to unk00/unk02/unk05 of the parent does. Nothing reads any of it
 * yet, so widths are from the `strb`s only. */
struct Unk02025584 /* 0x1c */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02[0x11];
    /* 0x13 */ u8 unk13;
    /* 0x14 */ u8 unk14[0x05];
    /* 0x19 */ u8 unk19;
    /* 0x1a */ u8 unk1a;
    /* 0x1b */ u8 filler_1b[0x01];
};

/* Wave 41, W41-D. ONE OBJECT, not the three or four separate globals the
 * splitter's symbol list suggests. gen_lds invents gUnknown_02025566 and
 * gUnknown_02025569 because two of sub_0802F28C's pool words hold 0x02025566
 * and 0x02025569, but those are `&g.unk02[0]` and `&g.unk05[0]` -- address
 * constants of members, which agbcc materialises as whole pool words, and for
 * a RAM address there is no relocation that could tell the two spellings apart.
 *   What settles it is loop 4: it reaches the 0x1c-byte record array at +0x20
 * by loading the SAME pool word loop 1 uses and adding 0x20. agbcc only shares
 * a base like that when the base symbol is shared; three separate `u8 []`
 * globals give the record array its own pool word and a different offset chain.
 *   unk00/unk02/unk05 are VOLATILE on the dead-`ldrb`-before-`strb` tell (the
 * same one that settled struct Unk0849B018); unk20 is not. */
struct Unk02025564
{
    /* 0x00 */ volatile u8 unk00[0x02];
    /* 0x02 */ volatile u8 unk02[0x03];
    /* 0x05 */ volatile u8 unk05[0x18];
    /* 0x1d */ u8 filler_1d[0x03];
    /* 0x20 */ struct Unk02025584 unk20[0x0c];
};

struct Unk03003FC0 /* 0x47 */
{
    /* 0x00 */ u8 unk00; /* wave 27, W27-C: sub_0803BDBC sets it to 1 with a
                          * bare `strb` on the base register. Width only. */
    /* 0x01 */ u8 unk01; /* a mode selector written immediately before a
                          * Proc_Start on tree 3, once per mode: 1 by
                          * sub_0803BADC, 2 by sub_0803BA00, 3 by
                          * sub_0803B8C4 -- three near-identical starters that
                          * differ only in this constant and the script */
    /* 0x02 */ u8 unk02; /* set by sub_0803BCD0; sub_0803BD14 uses it to index
                          * gUnknown_085C77A0[] */
    /* 0x03 */ u8 unk03;
    /* 0x04 */ u8 unk04; /* strb, sub_08018C54 */
    /* 0x05 */ u8 unk05; /* wave 28, W28-A: a two-valued flag written with a
                          * bare `strb r0, [r1, #5]` -- 0 by sub_08018800 and
                          * 1 by sub_080187C8, twin script handlers that
                          * otherwise differ in nothing. Width only; no reader
                          * is known. */
    /* 0x06 */ u8 unk06;
    /* 0x07 */ u8 unk07;
    /* 0x08 */ u8 unk08; /* a mode flag: the whole sub_08042E2C..sub_0804301C
                          * table-lookup family reads it and returns a fixed
                          * fallback when it is zero */
    /* 0x09 */ u8 unk09; /* a small slot id, plain `ldrb`: family F046
                          * (sub_0802C6CC/6FC/72C/75C) is four copies of one
                          * predicate that differ ONLY in whether they compare
                          * it against 0, 1, 2 or 3 -- the same 0..3 player /
                          * team range gUnknown_03003FC0.unk43..unk46 permutes. */
    /* 0x0a */ u8 filler_0a[0x02];
    /* 0x0c */ u8 unk0c; /* two predicates read it plain `ldrb`: sub_0802C78C
                          * is `!= 1` and sub_0802C7A0 is `!= 0`, so it is a
                          * small enum rather than a flag */
    /* 0x0d */ u8 unk0d; /* wave 15 (C): sub_08042998 gates a whole block on
                          * `unk32 == 0 && unk0d == 0`, then reuses the loaded
                          * zero to clear gUnknown_03004074 -- a second mode
                          * flag beside unk32, not part of the filler. */
    /* 0x0e */ u8 filler_0e[0x02];
    /* Wave 34, W34-C carved five WORDS out of the old filler_0e[0x1a]:
     * sub_08034780 clears all five with `str r4,[r7,#disp]` at displacements
     * 0x14/0x18/0x1c/0x20/0x24 -- whole-word stores at displacements `strb` and
     * `strh` cannot reach in that encoding, and the run is contiguous and
     * word-aligned.
     *
     * Wave 37, W37-Q2: the first FOUR of those are one ARRAY based at 0x10, a
     * fifth parallel per-slot array beside unk33/unk38/unk3d/unk42 and indexed
     * 1..4 the same way. sub_0803BE60 writes them in a loop whose index runs
     * 1..4 against a base hoisted as `adds r5,r2,#0; adds r5,#0x10` with the
     * element address `lsls r0,r1,#2; adds r0,r0,r5` -- a variable index on a
     * 0x10 base, which is the member-array spelling and nothing else. Element 0
     * (0x10..0x13) is never touched, exactly like the four byte arrays.
     * The old names unk14/unk18/unk1c/unk20 are unk10[1..4]; every existing use
     * was a constant index, so the rename is byte-neutral and c_08034780.c was
     * re-verified after it. unk24 stays a separate scalar -- sub_0803BE60's
     * loop stops at index 4 and sub_08034780 writes 0x24 out of order, before
     * the run, not after it.
     * Signedness unsettled; nothing reads any of them yet. */
    /* 0x10 */ u32 unk10[0x05];
    /* 0x24 */ u32 unk24;
    /* 0x28 */ u32 unk28; /* wave 27, W27-C: sub_0803BD78 arms it with 1000
                           * (`movs r0,#0xfa; lsls r0,#2`) through a whole-word
                           * `str` at a displacement `strb` could not reach --
                           * so a word, not four bytes of filler. Signedness
                           * unsettled; nothing reads it yet. */
    /* 0x2c */ u8 unk2c; /* sub_08035CF4 returns whether it equals 1 */
    /* 0x2d */ u8 unk2d; /* set to 0/1/2 by sub_08035558/sub_08035538/sub_08035548 */
    /* 0x2e */ u8 unk2e; /* plain `ldrb`, reached through the same
                          * `adds rB,#0x2e` on the base that unk32 needs (both
                          * are past `ldrb`'s 5-bit displacement, so the add is
                          * addressing and not a member-array tell).
                          * sub_0802CF6C hands it to sub_080344F0 as that
                          * function's only argument, guarded by unk32 -- so it
                          * is the payload of whatever mode unk32 selects. */
    /* 0x2f */ u8 unk2f; /* wave 27, W27-C: sub_0803BD78 clears unk2c and then
                          * unk2e, unk2f, unk30, unk31 through four `adds r0,#1`
                          * steps on one address register, which is what four
                          * separate byte fields emit (each displacement is past
                          * `strb`'s 5-bit range from the struct base). */
    /* 0x30 */ u8 unk30;
    /* 0x31 */ u8 unk31;
    /* 0x32 */ u8 unk32;
    /* Wave 37, W37-Q1: 0x33, 0x38, 0x3d and 0x42 are FOUR PARALLEL 5-BYTE
     * ARRAYS indexed 1..4 by player slot, not one array plus loose scalars.
     * sub_0803C1D4 settles it: one loop body, `i` counting 0..3, writes all
     * four with the SAME index expression `i + 1` (the increment `adds r2,
     * r6, #1` is computed once and reused as the index against every one of
     * the four bases 0x33 / 0x38 / 0x3d / 0x42) from the four parallel
     * gUnknown_08580934 tables at +0x09 / +0x0d / +0x11 / +0x20. Element 0 of
     * each is never touched anywhere in the tree.
     *
     * The old names were unk34..unk37 (= unk33[1..4]), unk3e[0..3]
     * (= unk3d[1..4]) and unk43..unk46 (= unk42[1..4]); every existing use is
     * a CONSTANT index, so the rename is byte-neutral and c_08026900.c,
     * c_08034780.c and c_0803CEB8.c were re-verified after it.
     *
     * Wave 34's W34-C note that sub_08034780 emits "four separate statements,
     * not one array store" is still true and is not evidence against an array:
     * that function seeds them with four different constants, which is four
     * statements either way. */
    /* 0x33 */ u8 unk33[0x05];
    /* 0x38 */ u8 unk38[0x05]; /* A byte array, not a scalar: sub_08018C0C
                                * writes it as `.unk38[i]` with a variable
                                * index (the `adds rB,#0x38` on the BASE that
                                * only the member-array spelling produces).
                                * The index is the same small slot id that
                                * indexes gUnknown_08499598, so 5 is borrowed
                                * from gUnknown_030040F8's player-slot count
                                * and is NOT proved -- the extent may be
                                * anything up to 0x43. */
    /* 0x3d */ u8 unk3d[0x05]; /* the third of the four parallel slot arrays --
                                * see the note above unk33. Wave 32 (W32-C)
                                * read sub_0803BF10's variable index against a
                                * 0x3e base (`adds rB,rI; adds rB,#0x3e`), i.e.
                                * that function's cursor is already biased and
                                * addresses unk3d[1..] as unk3e[0..]; nothing
                                * in this tree writes unk3d[0]. */
    /* 0x42 */ u8 unk42[0x05];
    /* 0x47 */ u8 unk47; /* wave 35 (W35-J): sub_08029CB8 reads it with
                          * `adds r0, #0x47; ldrb` (the displacement is past
                          * ldrb's 5-bit field, hence the separate add) and
                          * branches on `!= 0` to choose between storing 0 and
                          * storing its own u8 argument. A flag; width from the
                          * load, and it extends the struct from 0x47 to 0x48. */
};

struct Unk030040D8
{
    /* 0x00 */ u8 unk00; /* plain `ldrb` at offset 0, compared against 0x18 by
                          * sub_0802CC90 -- a small id, not a flag. sub_080421D0
                          * and sub_0804223C read the same byte off the same
                          * pointer and multiply it by 0x5c to index
                          * gUnknown_085D5ABC, so it is a record selector. */
    /* 0x01 */ u8 unk01;  /* wave 13 (A2): bit 0 tested by sub_0802E4B4, which
                           * also ASSIGNS this pointer from
                           * &gUnknown_08499594[gUnknown_03003F38] -- so the
                           * object is a `struct UnitRecord` and unk01 is that
                           * type's unk01. The two struct names should be
                           * merged, but retyping the global would change
                           * c_080424BC.c's `->unk05 &= 7` into the bitfield
                           * spelling, so the layout-preserving split of
                           * filler_00 is left here instead. */
    /* 0x02 */ u8 unk02; /* wave 26, W26-C: sub_080425B8 copies
                          * gUnknown_03003100.pos.unk00 here and .unk02 into
                          * unk03, both `ldrh` then `strb` -- the byte halves of
                          * the same cursor position the union holds as s16s. */
    /* 0x03 */ u8 unk03;
    /* 0x04 */ u8 unk04 : 7; /* Wave 36, W36-B. A 7-BIT BITFIELD, and the
                              * discriminating evidence is that sub_08042650
                              * reads it TWICE off one `ldrb` with two
                              * different extractions: `movs r0,#0x7f; ands
                              * r0,r3` where it is only tested against zero,
                              * and `lsls r0,r3,#0x19; lsrs r0,r0,#0x19` where
                              * the value is used (as Div's dividend, minus
                              * one). No plain-`u8` spelling produces both --
                              * `x & 0x7f` folds to the AND at BOTH sites, and
                              * `((u32)x << 25) >> 25` folds to the AND too,
                              * because fold() runs before bitfield extraction
                              * is expanded. UNSIGNED from the `lsrs`. */
    /* 0x04 */ u8 unk04_7 : 1;
    /* 0x05 */ u8 unk05;
    /* 0x06 */ u8 unk06 : 7;  /* Wave 49, W49-G. Named out of filler_06, which
                               * had no reader anywhere in src/decomp. A 7-BIT
                               * UNSIGNED BITFIELD on exactly the evidence the
                               * unk04 note above sets out: sub_0805D648 reads
                               * it `ldrb r0,[r0,#6]; lsls r0,#0x19;
                               * lsrs r0,#0x19` and then SUBTRACTS
                               * gUnknown_03004074 from it, so the value is
                               * used, not merely tested -- and `x & 0x7f`
                               * folds to an AND at a use site while the
                               * bitfield gives the shift pair. */
               u8 unk06_7 : 1; /* unproven; the container's remaining bit,
                                * split off so unk06 stops at 7 bits. */
    /* 0x07 */ u8 unk07[0x05]; /* Wave 47, W47-G: element [2] (offset 0x09) is
                                * really a BITFIELD container, and unk07[1] and
                                * unk07[4] are not. sub_0805BFDC writes bits
                                * 3..5 of offset 0x09 with store_bit_field's
                                * exact output -- `movs r0,#7; ands r5,r0;
                                * lsls r2,r5,#3` ahead of the member load, then
                                * a clear with -0x39 -- which no `u8` masking
                                * spelling produces (shorten_binary_op narrows
                                * that AND to `mov r0,#0xc7` and puts the load
                                * first). DELIBERATELY NOT RESHAPED: the same
                                * array is read as `unk07[0]`, `unk07[1]` and
                                * `unk07[4]` by promoted code, so sub_0805BFDC
                                * uses a file-local bitfield view of offset
                                * 0x09 instead. Anyone who does split this must
                                * re-verify c_0805B980.c and c_0805BC7C.c.
                                *   wave 13 (A4): sub_0802966C reads
                                * `->unk07[i]` with the `adds rB,#7` on the
                                * BASE that only the member-array spelling
                                * produces. The extent 5 makes this 0x0c
                                * bytes, which is exactly struct UnitRecord --
                                * independent corroboration of the note on
                                * unk01 above. */
};

/* 0x02028360 -- 8-byte entries. sub_0803F5C8 returns &g[i], sub_0803F5D4
 * recovers the index as a u8 with (p - g), so the stride is load-bearing.
 * unk02 carries a bitfield cleared with ~0x03C0 by sub_0803DE68.
 * gUnknown_020283E0 is a second object of the same shape.
 */
/* Wave 29, W29-B narrows +0x00 and +0x04 to bytes. Every access in the three
 * functions matched this wave is `ldrb`: sub_0803DFE0 reads +0 and +1 as the
 * entry's tile x and y (it writes them out as u16 after adding 1 and 2 for two
 * of the five kinds), sub_0803DE94 reads +0 the same way, and sub_0803DF54
 * gates on +4 being non-zero. Nothing promoted named unk00 or unk04 as a
 * halfword, so the split is layout-preserving.
 *
 * unk02 becomes a BITFIELD, and the READ is what settles it. sub_0803DFE0 takes
 * bits 6..9 with `ldrh; lsls #0x16; lsrs #0x1c` -- extract_bit_field's shape;
 * spelling the same thing as `(unk02 >> 6) & 0xf` on a plain u16 gives
 * `lsr #6; movs #0xf; and`, a different and longer stream. sub_0803DE68's
 * ~0x3C0 clear is byte-identical either way, so it discriminates nothing. The
 * six bits below and the six above are unnamed: sub_0803DE94 tests the whole
 * 0x3C0 mask and nothing matched touches the rest.
 *
 * Wave 38, W38-C SPLITS the low 6 bits into two 3-bit fields. sub_0803E01C is
 * the allocator that fills a fresh record and it writes bits 0..2 and bits 3..5
 * through SEPARATE inserts: `movs r2,#7; ands r4,r2` then `~7` (`movs r0,#8;
 * rsbs r0,r0,#0`), and `ands r5,r2; lsls r2,r5,#3` then `~0x38` (`movs r1,#0x39;
 * rsbs r1,r1,#0`). A single 6-bit field would mask the source with 0x3f and
 * clear with ~0x3f once. Nothing named unk02_0, so the split is layout-
 * preserving; unk02_6 and unk02_a keep their bit positions.
 *
 * The ACCESS WIDTH corroborates the boundaries: those two inserts are
 * `ldrb`/`strb` at +2 because bits 0..5 sit inside the first byte, while
 * sub_0803E01C's unk02_6 insert switches to `ldrh`/`strh` with a 0xFFFFFC3F
 * pool word because bits 6..9 straddle the byte boundary of the u16.
 *
 * Wave 44, W44-E confirms the layout from the READ side, in a pair of functions
 * that exercise both spellings of the SAME container. sub_0805C988 and
 * sub_0805C9CC each walk this record and both compile from these declarations
 * with no cast anywhere:
 *   - `unk02_6 != 0` and `unk02_6 == 1` come out as `ldrh [.,#2]; ands <0x3C0>;
 *     cmp #0` / `cmp #0x40`, a MASK-AND-COMPARE in the container's HImode with
 *     the 0x3C0 hoisted into a register by LICM. Comparing a bitfield against a
 *     CONSTANT folds that way; it is not evidence of a plain u16 member, and
 *     reading it as one is the trap here. sub_0805C9CC's `== 5` gives 0x140,
 *     which needs a register and is why only that twin gets a loop preheader.
 *   - `unk02_0` compared against a VARIABLE cannot fold, so it goes through
 *     extract_bit_field and gives `ldrb [.,#2]; lsls #0x1d; lsrs #0x1d` -- a
 *     QImode extract, bits 0..2 sitting wholly inside byte 2, exactly as the
 *     ACCESS WIDTH paragraph above predicts.
 * So the two access widths at +2 in one function are one member, and the
 * discriminator between a mask and an extract is the OTHER operand, not the
 * declaration. */
struct Unk02028360 /* 0x08 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u16 unk02_0 : 3;
               u16 unk02_3 : 3;
               u16 unk02_6 : 4;
               /* Wave 38, W38-C splits the top 6 bits 4+2. sub_0803EB40 copies
                * bits 10..13 into unk06 with `ldrb r0,[r2,#3]; lsls #0x1a;
                * lsrs #0x1c` -- a QImode extract, because bits 10..13 sit
                * wholly inside byte 3. The boundary is confirmed independently
                * by src/decomp/c_0803E088.c, whose local view of byte 3 is
                * {2,4,2} and whose middle field it clears with ~0x3c, i.e.
                * exactly these four bits. Neither half was referenced before. */
               u16 unk02_a : 4;
               u16 unk02_e : 2;
    /* 0x04 */ u8 unk04;
    /* 0x05 */ u8 filler_05[0x01];
    /* 0x06 */ s8 unk06; /* Wave 38, W38-C NARROWS this from u16. sub_0803EAD0
                          * does `ldrb r0,[r3,#6]; subs r0,#1; strb r0,[r3,#6]`
                          * -- a u16 member decrements through ldrh/strh. It was
                          * only ever DECLARED here, never read: no file in src/
                          * names Unk02028360.unk06, so the narrowing is
                          * layout-preserving. Corroborated independently by
                          * src/decomp/c_0803E088.c, whose own promoted view of
                          * this same record (struct Unk3E01C, the object
                          * sub_0803E01C returns) already declares +6 as `u8`
                          * and stores a7 into it with a plain `strb`. */
    /* 0x07 */ s8 unk07; /* Wave 49, W49-C, NAMED out of the old filler_07 --
                          * same offset, same byte. sub_0803ED60 forwards it to
                          * sub_0803E594, sub_0803E6C4 and sub_0803E808 as a
                          * whole word, and the SIGN is proved at the third
                          * site, which reads it `movs r2,#7; ldrsb r2,[r4,r2]`
                          * -- a genuine ldrsb, so the member itself is signed
                          * (a `(s8)` cast onto a u8 member cannot produce one;
                          * see the wave-37 sign_extend rule). The other two
                          * sites spell the same read `ldrb; lsls #0x18;
                          * asrs #0x18` only because THUMB's ldrsb has no
                          * immediate-offset form and those two have already
                          * clobbered their base register with the result. */
};

/* Wave 31 (W31-C): the pair of 8-byte ROM tables sub_0803E7C0 and sub_0803E7E4
 * select between on their first argument (3 or anything else -> 0849F688,
 * 4 -> 0849F698) and then index with their second. Stride 8 from `lsls #3`;
 * one returns the word at +0 and the other the word at +4, each with a bare
 * `ldr; bx lr` and no narrowing, so both members are word-wide. Nothing here
 * proves they are pointers rather than ints -- `void *` is chosen because the
 * sibling accessor pair sub_0803F110 / sub_0803F128 on the same switch feeds
 * Decompress. */
struct Unk0849F688 /* 0x08 */
{
    /* 0x00 */ void *unk00;
    /* 0x04 */ void *unk04;
};
/* A ROM u16 table of ten sprite-scale steps -- 0x10, 0x20, 0x38, 0x58, 0x80,
 * 0x110, 0x107, 0x103, 0x100, 0 -- stepped by sub_080277BC through the frame
 * counter gUnknown_03001470[gUnknown_03001FBC].unk24.
 *
 * UNSIGNED, and the THIRD read is what proves it. sub_080277BC reads this table
 * three times off the same index. The first two supply sub_080157A4's and
 * sub_080157F4's `s16` parameter and are single `ldrsh`s -- which looks like an
 * s16 array but is not evidence, because combine folds a u16 load feeding a
 * narrowing sign-extension straight into `ldrsh`. The third read feeds only
 * `== 0x100` and the ROM loads it with a bare `ldrh`. THAT is the
 * discriminating use: `const s16` makes the comparison an int compare of a
 * sign-extended value and emits `ldrsh` there too, measured at +4 bytes. Only
 * u16 gives all three. Extent 10 from data/rodata-0808F098.s: the next symbol
 * is 0x14 bytes later. (wave 34, W34-D) */
/* A u16 cursor/selection code sub_080253B0 compares against the TOP THREE BITS
 * of the gUnknown_08499590 +0x1432 terrain byte (`ands r3, #0xe0`), i.e. against
 * the same packed field the +0x1432 note describes. Bare `ldrh`, nothing
 * sign-extends it. (wave 34, W34-D) */
extern u16 gUnknown_03004084;
extern const u16 gUnknown_08090AA8[];
/* Two ROM blobs sub_08027A50 passes as sub_08015438's first and third
 * arguments -- the sprite/OAM pair every sub_08015438 caller supplies. Only
 * the ADDRESSES are evidence: nothing in this tree dereferences either, and
 * they are declared non-const because sub_08015438's parameters are plain
 * `void *`. (wave 34, W34-D) */
/* A ROM byte per unit type, indexed by struct UnitRecord.unk00 and compared by
 * sub_080253B0 against the low 5 bits of the terrain byte -- so it holds a
 * terrain code, in the same 0..0x1f space gUnknown_08551CA0 scores. Bare
 * `ldrb` off the unscaled index. (wave 34, W34-D) */
extern const u8 gUnknown_084995DA[];
/* Wave 35, W35-C. A 16-entry ROM byte table read at `(gUnknown_03004008 >> 2)
 * & 0xf` by sub_080246B4, whose value is then switched on (1 and 2 are the only
 * arms; everything else falls through). The SYMBOL is the array -- a single
 * `ldr rN,=gUnknown_08499CBC; adds rN,rN,idx; ldrb` with no intervening
 * dereference, which is what separates it from the -fforce-addr words in the
 * same address range. Extent 16 comes from the mask, which is a real bound and
 * not a guess. Bare `ldrb`, nothing sign-extends it. */
extern const u8 gUnknown_08499CBC[];
extern u8 gUnknown_08499DDC[];
extern u8 gUnknown_08499DE8[];
extern const struct Unk0849F688 gUnknown_0849F688[];
extern const struct Unk0849F688 gUnknown_0849F698[];
/* Wave 49, W49-C. Two more ROM tables in the same 0x0849F6xx run, both reached
 * off a `lsls #2` index and both from sub_0803E808 / sub_0803ED60.
 *
 * gUnknown_0849F6B8: stride 4, and only the halfword at +0 is ever read
 * (`lsls r0, a4, #2; adds r0, r0, rB; ldrh r3, [r0]`), where a4 is the same
 * 3-or-4 selector sub_0803E7C0/sub_0803E7E4 switch on. Modelled as pairs
 * because the stride is 4 and the read is 2 -- the second halfword has no
 * reader yet, so `[][2]` is the weakest shape that keeps the stride honest.
 *
 * gUnknown_0849F728: an array of POINTERS, subscripted `gUnknown_03004080 & 1`
 * -- a two-entry table selected by the low bit of the frame counter. Both
 * consumers agree on the element type without either being derivable from the
 * other: sub_0803ED60 hands element [g & 1] straight to sub_0803E764, whose
 * declared first parameter is `struct Unk02028360Pos *` and which walks it as a
 * 0xFFFF-terminated {x, y} list, and sub_0803EBA4 hands the same element to
 * sub_0803F2B8's third parameter, which that promoted file stores whole into an
 * `int` proc member (hence the cast at that one call site). */
/* Wave 49, W49-C. Seven ROM blobs in one run, at 0x0849FA08, FA22, FA3C, FA56,
 * FA5E, FA78 and FA9A -- lengths 0x1a, 0x1a, 0x1a, 0x08, 0x1a, 0x22 by
 * subtraction, so NOT a uniform-stride table and not an address pool. Every one
 * of them is used the same way and only that way: sub_0803FC28 selects one and
 * hands it whole to sub_0803F908's third parameter, never indexing or
 * dereferencing it, so `const u8 []` is the weakest model that gives the clean
 * pool word (the gUnknown_03004680 / gUnknown_08584CE8 reasoning). Which one is
 * chosen keys off the decoration record's unk02_6, unk04, unk02_e and
 * sub_0803866C(), i.e. they are per-kind graphic or script payloads. */
/* Wave 49, W49-C. Eleven 8-byte ROM blobs at 0x0849F990 through 0x0849F9E0, all
 * of them sub_0803F990's per-terrain-code payload for sub_0803F908's third
 * parameter, and two POINTER TABLES at 0x0849FAB0 and 0x0849FAC4 indexed
 * `lsls #2` by the terrain byte's TOP THREE BITS (`gUnknown_03003150[i].flags
 * >> 5`, the owning army). Same `const u8 *` element type as the blobs, reached
 * with `ldr r2, [r1]`; gUnknown_0849FAB0 also has its element 0 named directly
 * as the fallback when the +0x234A map plane is clear, which is what fixes 0 as
 * a real index rather than a distinct symbol. */
extern const u8 gUnknown_0849F990[];
extern const u8 gUnknown_0849F998[];
extern const u8 gUnknown_0849F9A0[];
extern const u8 gUnknown_0849F9A8[];
extern const u8 gUnknown_0849F9B0[];
extern const u8 gUnknown_0849F9B8[];
extern const u8 gUnknown_0849F9C0[];
extern const u8 gUnknown_0849F9C8[];
extern const u8 gUnknown_0849F9D0[];
extern const u8 gUnknown_0849F9D8[];
extern const u8 gUnknown_0849F9E0[];
extern const u8 *const gUnknown_0849FAB0[];
extern const u8 *const gUnknown_0849FAC4[];
extern const u8 gUnknown_0849FA08[];
extern const u8 gUnknown_0849FA22[];
extern const u8 gUnknown_0849FA3C[];
extern const u8 gUnknown_0849FA56[];
extern const u8 gUnknown_0849FA5E[];
extern const u8 gUnknown_0849FA78[];
extern const u8 gUnknown_0849FA9A[];
extern const u16 gUnknown_0849F6B8[][2];
extern struct Unk02028360Pos *const gUnknown_0849F728[];
/* The {u16, u16} out-parameter sub_0803DFE0 fills and sub_0803DF54 reads back.
 * A STRUCT and not a `u16 [2]`: sub_0803DF54 binds its address once and reads
 * the second half `ldrh r0,[r6,#2]`, where the array spelling recomputes
 * `sp + 2` into a scratch register first. Wave 29, W29-B. */
struct Unk02028360Pos
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
};

/* 0x03003338 points at ROM (set from gUnknown_0849FE74[0] by sub_0803486C).
 * Stride 8, four u16 written together by sub_080413B4.
 */
struct Unk03003338 /* 0x08 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
};

/* sub_0804B0B4 allocates 0x6c bytes for this record, so it is at least that
 * long. Wave 29 (C) added +0x58..+0x60 off sub_0804B0CC / sub_0804B10C and
 * SHORTENED unk2c from 0x31 to 0x2c bytes to make room. That is byte-neutral:
 * nothing in src/decomp/ names this struct, and an array's declared bound never
 * enters an address computation, so every existing `unk2c[i]` is unchanged and
 * unk5d keeps its 0x5d offset.
 *
 * unk58 is a word (`str`); the other four are bytes (`strb`), all past strb's
 * 31-byte displacement, which is why each store materialises its own address
 * with `adds r0, #0x5c` and friends. sub_0804B0CC writes 0 into unk5c and
 * sub_0804B10C writes 1, and they are otherwise the same function -- so unk5c
 * is the pair's discriminator. unk5f and unk60 are unk5e doubled and unk5e
 * times eight. */
struct Unk030044E0 /* >= 0x6c */
{
    /* 0x00 */ u8 filler_00[0x1e];
    /* 0x1e */ u8 unk1e; /* Wave 30, W30-E. sub_0804A64C builds sub_0804A18C's
                          * u8 argument as `unk20 * 15 + unk1e`; bare `ldrb`
                          * with nothing sign-extending it. */
    /* 0x1f */ u8 filler_1f[0x01];
    /* 0x20 */ s16 unk20; /* Wave 30, W30-E. Read `movs r0, #0x20;
                           * ldrsh r1, [r2, r0]` -- an `ldrsh` whose offset is
                           * in a REGISTER is a signed halfword at a constant
                           * displacement (THUMB has no ldrsh immediate form),
                           * not an array subscript. */
    /* 0x22 */ s16 unk22; /* Wave 37, W37-I. Two independent animation clocks in
                           * four consecutive halfwords, driven identically by
                           * sub_0804ABDC: `if (unkN+2 != 0) unkN+2--; else
                           * unkN++;`, with the counter wrapped to 0 above 0x78
                           * and `(unkN / 4) & 0xf` used as the frame index into
                           * gUnknown_0813204C / gUnknown_08131D8C /
                           * gUnknown_08131DEC. The DELAYS unk24/unk28 are
                           * SIGNED -- the `!= 0` test is a bare
                           * `movs rI,#0x24; ldrsh` while the decrement reuses a
                           * plain `ldrh`, the s16-object pattern -- and the
                           * counters unk22/unk26 are compared `> 0x78` after a
                           * bare `ldrsh` too but only ever hold small positive
                           * values. Was filler_22. */
    /* 0x24 */ s16 unk24;
    /* 0x26 */ s16 unk26;
    /* 0x28 */ s16 unk28;
    /* 0x2a */ s16 unk2a; /* Wave 34, W34-I. A SIGNED halfword, and sub_0804AFCC
                           * proves it by reading the same address both ways in
                           * one statement: `movs r4,#0x2a; ldrsh r0,[r1,r4]`
                           * for the `> 0xa0` compare (an ldrsh whose offset is
                           * in a register is the s16-OBJECT tell -- THUMB has
                           * no ldrsh immediate form) and a plain
                           * `ldrh r3,[r1,#0x2a]` for the `- 4` that is stored
                           * straight back as a halfword, where the truncation
                           * makes the signedness irrelevant. A u16 object
                           * would have used `ldrh` for the compare too. It is
                           * a screen Y that walks down to a 0xa0 floor. */
    /* 0x2c */ u8 unk2c[0x2c];
    /* 0x58 */ int unk58;
    /* 0x5c */ u8 unk5c;
    /* 0x5d */ u8 unk5d;
    /* 0x5e */ u8 unk5e;
    /* 0x5f */ u8 unk5f;
    /* 0x60 */ u8 unk60;
    /* 0x61 */ u8 unk61; /* Wave 34, W34-I. sub_0804A1E4 reads it twice with a
                          * bare `ldrb` off an `adds rB,#0x61` and hands it to
                          * sub_08012BC8 and sub_080149C0 as a tile/palette
                          * index. Width from the load; signedness unproved,
                          * both uses being u8-context. */
    /* 0x62 */ u8 unk62; /* Wave 30, W30-E. sub_0804AE20 post-increments it
                          * (`adds r1, #0x62; ldrb; adds #1; strb`) on the
                          * arm where sub_08019260 returns 0 -- a plain u8
                          * counter, and the `adds #0x62` is the strb
                          * displacement limit, not an address being taken. */
    /* 0x63 */ u8 unk63; /* Wave 43, W43-K. sub_0804A6D8 stores a bare 1 or 2
                          * here (`adds r0,#0x63; movs r1,#N; strb r1,[r0]`) on
                          * the two arms of its "any live entry left?" test --
                          * a mode byte. Width from the `strb`; signedness is
                          * unproved, nothing ever reads it in this tree. Was
                          * the first byte of filler_63[0x02]. */
    /* 0x64 */ u8 filler_64[0x01];
    /* 0x65 */ u8 unk65; /* Wave 34, W34-I. sub_0804A1E4 stores sub_08014CEC's
                          * result here, then re-reads it and subtracts its own
                          * u8 parameter when it was non-zero -- so a one-byte
                          * scratch, written and read through `adds rB,#0x65`
                          * (past the strb displacement limit). */
    /* 0x66 */ u8 unk66; /* Wave 34, W34-I. A FLAG: sub_0804A18C tests unk5c and
                          * unk66 against zero independently and picks one of
                          * four parallel ROM byte tables from the pair, so the
                          * two are companion booleans rather than filler. Only
                          * ever compared with 0, so signedness is unproved.
                          * Wave 34, W34-L: still u8, but bit 7 is a separate
                          * flag -- sub_0804AAF8 loads this byte TWICE in its
                          * prologue, `ldrb` into one register and `ldrsb` into
                          * another, testing the signed copy `< 0` and masking
                          * the unsigned one with 1. A plain `s8` field would
                          * have served both uses from one `ldrsb`, so the field
                          * is unsigned and the test is an explicit `(s8)`
                          * cast. */
    /* 0x67 */ u8 unk67; /* Wave 34, W34-L. A STEP COUNTER: sub_0804AAF8
                          * switches on it (cases 0..5, an if-else comparison
                          * tree) and post-increments it at the common tail,
                          * `ldrb; adds #1; strb` through `adds rB,#0x67`. Also
                          * scales it by 2 into a s16 and uses it as the inner
                          * subscript of gUnknown_084C3B3C[][5], which bounds
                          * the live range at 0..5. */
};

/* A flag byte plus a halfword. sub_080546BC writes seven of the eight bits
 * individually, and the four clears carry the `mov #N; neg` shape that only a
 * bitfield store produces (masks ~0x01, ~0x04, ~0x08 -- the last derived from
 * the previous by `subs r1, #4`), so this is a bitfield container and not a
 * plain u8. The three sets are OR-only, which a 1-bit field always is.
 * Width of the container is not recoverable from the access (wave 7 probe);
 * u8 is chosen so that unk02 lands at +2 as the `strh r0, [r2, #2]` requires. */
struct Unk03004504 /* >= 0x04 */
{
    /* 0x00 */ u8 bit0 : 1;
    /* 0x00 */ u8 bit1 : 1;
    /* 0x00 */ u8 bit2 : 1;
    /* 0x00 */ u8 bit3 : 1;
    /* 0x00 */ u8 bit4 : 1;
    /* 0x00 */ u8 bit5 : 1;
    /* 0x00 */ u8 bit6 : 1;
    /* 0x00 */ u8 bit7 : 1;
    /* 0x01 */ u8 filler_01[0x01];
    /* 0x02 */ u16 unk02;
};

struct Unk08090CD8Entry /* 0x88 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u8 unk06[128]; /* Wave 42 (W42-K) raises the EXTENT from 20. The
                               * offset, the element type and the 0x88 stride are
                               * all unchanged, so every existing reader compiles
                               * to the same bytes (c_080308B4.c re-verified after
                               * the change). 20 was sub_080308B4's payload
                               * length, not the array's: sub_08030930 fills
                               * unk06[0..127] from its `u8 *` argument and sets
                               * unk04 = 0x80 in the same breath, so unk04 is the
                               * LENGTH field and the payload runs to at least
                               * 128 of the 130 bytes the stride leaves. */
    /* 0x86 */ u8 filler_86[0x88 - 0x86];
};

struct Unk08090CD8Buf
{
    /* 0x0000 */ u8 filler_00[6];
    /* 0x0006 */ u8 unk06;
    /* 0x0007 */ u8 filler_07[0x20 - 0x07];
    /* 0x0020 */ volatile u16 unk20;
    /* 0x0022 */ u8 filler_22[0x12c - 0x22];
    /* 0x012c */ struct Unk08090CD8Entry unk12c[48];
    /* 0x1aac */ u8 filler_1aac[1];
    /* 0x1aad */ volatile u8 unk1aad;
};

struct Unk08090CD8
{
    /* 0x00 */ struct Unk08090CD8Buf *unk00;
};

/* The bitfields at 0x04..0x06 come from sub_08042998, which reads the same
 * three offsets with the same widths off BOTH gUnknown_08499594[i] (stride
 * 0x0c, so this type) and **gUnknown_08091364 -- so the two are one type, and
 * sub_08025B80's pointer parameter is this struct.
 */
struct UnitRecord /* 0x0c */
{
    /* 0x00 */ u8 unk00;      /* the unit-type id, and it is UNSIGNED on its own
                               * evidence rather than only by the
                               * gUnknown_085D5ABC[unk00] subscript: sub_0805CC88
                               * filters on `(u8)(unk00 - 0x10) <= 1`
                               * (`subs #0x10; lsls #0x18; lsrs #0x18; cmp #1;
                               * bhi`) -- a u8 truncation of the difference and
                               * an unsigned compare, which is how a two-value
                               * id range is spelled with one branch. 0 is the
                               * "empty slot" sentinel every builder tests
                               * first, so the ids 0x10 and 0x11 are a pair of
                               * adjacent types. */
    /* 0x01 */ u8 unk01;      /* a flag byte; the 0x0805Cxxx list builders skip
                               * an entry whose bit 0 is set (`ldrb [.,#1];
                               * movs #1; ands`). Plain mask, not a bitfield --
                               * a 1-bit field would have been extracted with a
                               * shift pair. */
    /* 0x02 */ u8 unk02;      /* wave 15 (C): a CELL COLUMN. sub_08058A2C adds
                               * it to gUnknown_08499590's rowOffset[unk03] to
                               * form the screen index, which is the x half of
                               * the c_08001158.c idiom. */
    /* 0x03 */ u8 unk03;      /* the matching row: `ldrb [.,#3]; lsls #1` is the
                               * `y * 2` that indexes rowOffset[] at +0x417A. */
    /* 0x04 */ u16 unk04_0:7; /* ldrb [.,#4]; lsls #25; lsrs #25 */
               u16 unk04_7:4; /* ldrh [.,#4]; lsls #21; lsrs #28 -- spans into
                               * byte 5, which is why the container is u16 */
               u16 unk05_3:5; /* byte 5 bits 3..7, kept by `movs #7; ands` */
                              /* Wave 44, W44-E: a relational compare on
                               * unk04_0 comes out UNSIGNED. sub_0805C208's
                               * `unk04_0 <= 0x5a` is `lsls #0x19; lsrs #0x19;
                               * cmp #0x5a; bhi`, and it matched with no cast --
                               * agbcc emits the compare against the
                               * zero-extended temporary rather than against the
                               * int the field would promote to. Do not add a
                               * `(u8)`/`(u32)` cast to chase a `bhi` on any of
                               * these fields; it is already there. */
    /* 0x06 */ u8 unk06_0:7;  /* sub_08025B80: ldrb [.,#6]; lsls #25; lsrs #25,
                               * cleared with `movs #0x80; rsbs` = ~0x7f */
               u8 unk06_7:1;  /* sub_08042998: lsrs #7 */
    /* 0x07 */ u8 unk07;      /* wave 15 (C): two flag bytes read as a pair by
                               * sub_08058A2C -- `if (unk08) v *= 2; else if
                               * (!unk07) v >>= 3;` -- so they are alternatives
                               * rather than one field. Both plain `ldrb`.
                               *   Wave 39, W39-F: they are very likely ONE
                               * 2-element array of unit IDS, not two flags.
                               * sub_0802A304 walks them with a VARIABLE index
                               * (`adds r0, p, #7; adds r4, r0, i`), stops at
                               * gUnknown_085D5ABC[unk00].unk14[0] -- the 1-or-2
                               * capacity byte -- and hands each non-zero one to
                               * sub_08029978/sub_08029A48 as
                               * `&gUnknown_08499594[id]`, i.e. as a subscript
                               * into this very array. That is a transport's
                               * cargo list, and it reconciles with wave 15:
                               * "unk08 set" means two units aboard.
                               *   DELIBERATELY NOT RESHAPED. Merging them into
                               * `u8 unk07[2]` would change a member several
                               * promoted functions read by name, and the
                               * offsets are all this function needs -- it uses
                               * a file-local view in work/sub_0802A304, which
                               * is byte-identical. Anyone who reshapes this
                               * must re-verify every `.unk08` reader. */
    /* 0x08 */ u8 unk08;
    /* 0x09 */ u8 unk09; /* Wave 32, W32-A. sub_08060110 zeroes unk09 and unk0a
                          * and then seeds unk0b from gUnknown_030046C0.unk06,
                          * all three with bare `strb` off ONE CSEd element
                          * address -- sibling members at constant offsets, the
                          * same shape c_08026588.c documents. Widths from the
                          * stores; nothing reads them yet.
                          *   Wave 39, W39-D: NOT a bitfield, and this is
                          * measured rather than assumed. sub_08025BE0's park
                          * turns on the ROM building a second zero register
                          * for this member alone, and work/sub_08025BE0/
                          * proposed a byte-wide bitfield as the explanation.
                          * Controlled compile_probe, one body and two struct
                          * declarations differing only in `u8 unk09;` versus
                          * `u8 unk09:8;`: both emit the same `strb r5,[r3,#9]`
                          * reusing the CSEd zero. A byte-wide, byte-aligned
                          * bitfield degenerates to the plain store in agbcc,
                          * so it explains nothing and this member must be left
                          * as a plain u8. */
    /* 0x0a */ u8 unk0a;
    /* 0x0b */ u8 unk0b;
};

/* Wave 48, W48-I. The 0x08047xxx/0x08048xxx panel record, passed by pointer
 * between sub_0804769C (matched this wave) and sub_080484CC (matched this
 * wave), which is why it lives here rather than in either .c: the two are
 * promoted into different translation units and both need the tag.
 *
 * c_08047B98.c's file-local `struct Unk08047B98` is the SAME object seen
 * through a narrower view (it names only unk1e/unk1f); it is matched and was
 * deliberately left alone. Fill it in here rather than there if you widen it.
 * Only the three members below are proved; everything else is filler. */
struct Unk0804769C /* >= 0x2a */
{
    /* 0x00 */ u8 filler_00[0x1f];
    /* 0x1f */ u8 unk1f;   /* sub_080484CC seeds the descending scan cursor from
                            * it, then sign-extends: `ldrb; lsls #0x10; asrs
                            * #0x10` with a `blt` exit, so the CURSOR is s16 and
                            * this member is the u8 it starts from. */
    /* 0x20 */ u8 filler_20;
    /* 0x21 */ u8 unk21;   /* how many gUnknown_02028DD8 entries are live;
                            * sub_0804769C bounds its walk with it and the
                            * compare is `bhs`, i.e. UNSIGNED. */
    /* 0x22 */ u8 filler_22[6];
    /* 0x28 */ u16 unk28;  /* sub_080484CC `strh`s the chosen gUnknown_02028DD8
                            * byte here. Width from the store; signedness
                            * unproved -- nothing reads it back yet. */
};

/* Merged from eight variants -- the widest disagreement in the tree. Every
 * variant agreed on 0x3c total; they differed only in which bytes they had
 * bothered to name.
 */
struct ArmyRecord /* 0x3c */
{
    /* 0x00 */ u32 unk00; /* word: sub_08025B28 does `-= a2` with ldr/str.
                           * Unsigned: sub_08025B58 clamps it with `cmp; bls`
                           * against 999999 (0xf423f), an unsigned compare. */
    /* 0x04 */ u32 unk04; /* word, unsigned, same 999999 clamp (sub_08025B28) */
    /* 0x08 */ u32 unk08; /* Wave 29, W29-A. Whole-word `ldr` at +8, and
                           * sub_08026F04 hands it straight to sub_08025B58's
                           * declared `u32` second parameter -- so a word, not
                           * four bytes of filler. Nothing writes it yet. */
    /* 0x0c */ u8 unk0c;
    /* 0x0d */ u8 unk0d;
    /* 0x0e */ u8 unk0e;
    /* 0x0f */ u8 unk0f;
    /* 0x10 */ u8 unk10; /* Wave 45, W45-D. Was filler_10[0x01]; same extent, so
                          * nothing moves. sub_08026D68's terrain census counts
                          * one map property per army into 0x0c..0x10, and this
                          * is the fifth counter: `ldrb r0,[r4,#0x10]; adds r0,#1;
                          * strb` in the arm for terrain id 20, exactly the shape
                          * of the 0x0c/0x0d/0x0e/0x0f arms beside it. Width from
                          * the load; nothing in src/ named the filler. */
    /* 0x11 */ u8 unk11; /* Wave 38, W38-I. sub_08028B70 walks slots 1..4 and
                          * returns the first whose +0x11 byte compares `bhs`
                          * against the u8 gUnknown_03003FC0.unk31 -- a plain
                          * `ldrb` and an UNSIGNED compare against another u8, so
                          * u8. Carved out of the old filler_10[0x03]; 0x10 and
                          * 0x12 stay unnamed and nothing in src/ named the
                          * filler. */
    /* 0x12 */ u8 unk12; /* Wave 41, W41-D. Was filler_12. sub_08026BAC's
                          * per-army reset clears 0x11, 0x12 and 0x13 as three
                          * separate `strb` statements, each off its own
                          * reload of the array pointer, so 0x12 is a field of
                          * its own and not padding between unk11 and unk13.
                          * Width from the store; nothing reads it. */
    /* 0x13 */ u8 unk13; /* Wave 38, W38-L. A FLAG BYTE, bit 5: sub_080270B8
                          * walks slots 1..4 and returns TRUE for the first
                          * whose unk1b is set AND whose +0x13 byte passes
                          * `movs r4,#0x20; ands`. A plain `ldrb` and a mask
                          * held in a register across the loop, so a byte with
                          * a mask, not a bitfield -- a 1-bit field would have
                          * been a constant shift pair (same reasoning as
                          * unk2c). Reached as element base + 0x13 with the
                          * giv, so no displacement evidence either way. */
    /* 0x14 */ u16 unk14; /* Wave 38, W38-L. Confirmed a HALFWORD from a store,
                           * not only from reads: sub_08027118 clears it with a
                           * bare `strh r0, [rB, #0x14]` -- inside `ldrh`/`strh`
                           * displacement range, so no address arithmetic hides
                           * the width. */
    /* 0x16 */ u16 unk16; /* a running count: sub_08026588 does `unk16++` and
                           * then keeps unk18 as its high-water mark, comparing
                           * the two with `cmp; bls` -- an unsigned compare, so
                           * both halfwords are unsigned */
    /* 0x18 */ u16 unk18;
    /* 0x1a */ u8 unk1a; /* sub_0803D6D0 snapshots [0..4] of these into
                          * gUnknown_030040F8, one per player slot.
                          * Wave 38, W38-L: sub_08027198 is the reverse lookup
                          * -- it walks slots 1..4 and returns the index of the
                          * first live slot whose unk1a EQUALS its argument, so
                          * unk1a is a key that is unique across the four
                          * slots, not a count. Plain `ldrb` compared straight
                          * against an `int` parameter with no narrowing at
                          * either end, so the width is from the load only. */
    /* 0x1b */ u8 unk1b; /* Wave 38, W38-L. The SLOT-OCCUPIED flag, and the
                          * guard every slot walk in block 0x08027000 opens
                          * with: sub_080270B8, sub_08027118 and sub_08027198
                          * all test it `!= 0` before touching any other member
                          * of the same element, and sub_080270F0 tests it
                          * `== 1` specifically -- so it carries more than one
                          * non-zero value and 1 is a distinguished one. Plain
                          * `ldrb` throughout. */
    /* 0x1c */ u8 unk1c;  /* a flag byte; sub_080359A4 tests bit 1 with a plain
                           * `movs #2; ands` mask (wave 13, A2) */
    /* 0x1d */ u8 unk1d; /* indexes gUnknown_085D3DD0 (sub_08043D84) */
    /* 0x1e */ u8 unk1e;
    /* 0x1f */ u8 unk1f;
    /* 0x20 */ u32 unk20;
    /* 0x24 */ u8 unk24; /* wave 25: zeroed with a plain `strb` by sub_0804438C
                          * right after it runs the per-turn income for the same
                          * army, so a companion counter to unk25 one byte over.
                          * Past `strb`'s 31-byte displacement, hence the
                          * `adds r0,#0x24` on the element base. */
    /* 0x25 */ u8 unk25;
    /* 0x26 */ u16 unk26;
    /* 0x28 */ u16 unk28;
    /* 0x2a */ u8 unk2a; /* sub_08026F9C/sub_08026FD0 compare it between two
                          * entries; both reach it as element [n + 1], so the
                          * runtime offset is 0x3c + 0x2a = 0x66 */
    /* 0x2b */ u8 unk2b; /* Wave 41, W41-D. Was filler_2b. THE ARMY'S OWN BIT:
                          * sub_08026BAC finishes its reset by storing 1, 2, 4
                          * and 8 into slots 1..4 at this offset (the ROM
                          * displacements 0x67 / 0xa3 / 0xdf / 0x11b are
                          * 0x3c * k + 0x2b), i.e. one set bit per army slot.
                          * That makes it the producer for the four-bit mask
                          * unk2c is documented as consuming, and its
                          * neighbour by construction. Plain `strb`. */
    /* 0x2c */ u8 unk2c; /* a FOUR-BIT MASK, one bit per 64-unit army slot:
                          * sub_08058254 / sub_08058318 loop i over 0..3 and
                          * skip army i when `(unk2c >> i) & 1` is set, then
                          * scan gUnknown_08499594[i * 64 .. i * 64 + 63].
                          * Plain `ldrb` and an `asrs` by a variable, so a byte
                          * and not a bitfield -- a bitfield would have been a
                          * constant shift pair (wave 20, W20-C). */
    /* 0x2d */ u8 unk2d; /* bit 7 is a flag: sub_08026D44 returns
                          * `(unk2d & 0x80) == 0` */
    /* 0x2e */ u8 unk2e; /* Wave 37, W37-I. The partner of unk2d: sub_080267AC
                          * masks BOTH with `& 0x7f` off the same `* 0x3c`
                          * element base (`adds rB,#0x2d` / `adds rB,#0x2e`,
                          * past the imm5 range) and hands the pair to
                          * sub_08022AAC(s16, s16) as one coordinate pair, the
                          * same role unk2f/unk30 play in the same function's
                          * tail. Byte from the `ldrb`; the 0x7f mask says bit 7
                          * is the flag unk2d's note describes and the value is
                          * seven bits wide. Was filler_2e. */
    /* 0x2f */ u8 unk2f; /* Wave 29, W29-A. An ADJACENT PAIR: sub_0802A4DC reads
                          * unk2f and unk30 off one element address (`adds
                          * rB,#0x2f` / `adds rB,#0x30`, both past `ldrb`'s
                          * 5-bit displacement) and hands them to
                          * sub_08029088(s16, s16) as its two coordinates. Width
                          * from the loads; the s16 parameters do not narrow
                          * them, so signedness is unproved. */
    /* 0x30 */ u8 unk30;
    /* 0x31 */ u8 unk31;
    /* 0x32 */ u8 unk32; /* wave 32 (W32-C): sub_08028944 tests it `!= 0` with a
                          * plain `ldrb` reached by `adds rB, #0x32`, the same
                          * past-the-imm5 address arithmetic unk30/unk31 use. */
    /* 0x33 */ u8 unk33; /* Wave 41, W41-D. Was filler_33. sub_08026520 fills
                          * 0x33..0x36 and 0x38 for slots 1..4 in one loop body:
                          * unk34 = sub_080263A4(i), unk35 = sub_08026424(i),
                          * unk36 = sub_080264BC(i), unk38 = sub_08026368(i) and
                          * finally unk33 = sub_08037D80(unk38). All four byte
                          * stores are plain `strb` off `adds rB,#0xNN` on the
                          * element base. So unk33 is the summary/grade byte
                          * derived from unk38, written last. */
    /* 0x34 */ u8 unk34; /* three adjacent byte counters summed by sub_08026368
                          * and clamped to 9999 above 999. Each needs its own
                          * `adds rB, #0x34` because 0x34 is past the `ldrb`
                          * displacement limit of 31 -- not an address being
                          * taken. */
    /* 0x35 */ u8 unk35;
    /* 0x36 */ u8 unk36;
    /* 0x37 */ u8 filler_37[0x01];
    /* 0x38 */ u16 unk38; /* Wave 41, W41-D. Carved out of the old
                           * filler_37[0x03]. A HALFWORD from a store, not only
                           * a load: sub_08026520 writes sub_08026368's u32
                           * result here with a bare `strh r0, [r1, #0x38]` --
                           * inside strh's displacement range, so no address
                           * arithmetic hides the width -- and immediately reads
                           * it back with `ldrh r0, [r1, #0x38]` to feed
                           * sub_08037D80. The store-then-reload is the source
                           * writing the field and then passing the FIELD, not
                           * the register. sub_08026368 sums unk34/unk35/unk36
                           * and clamps to 9999, which is why this needs 16
                           * bits where its three inputs are bytes.
                           * Signedness unproved: sub_08037D80 takes `int` and
                           * the ldrh is zero-extending by construction. */
    /* 0x3a */ u8 unk3a;
    /* 0x3b */ u8 unk3b;
};

struct Unk0849A2C8
{
    u16 unk00;
    u16 unk02;
};

struct Unk0849A354
{
    u16 unk00;
    u16 unk02;
};

/* 0x084995A0 -- ROM pointer to 8-byte records, at least 92 of them
 * (sub_08061CDC clears unk03[] over 0..91). unk03 is a 3-entry counter array:
 * sub_08058144 reaches it as `p[i].unk03[j]` with j a parameter, which is what
 * fixes it as an array rather than three named bytes. */
/* Wave 48 (W48-A) carves filler_00 into three named bytes.  sub_0800C574 is the
 * writer and it fills a record whose first three bytes MIRROR struct
 * Unk03003150's: the same three values (tile id, x, y) are stored into
 * gUnknown_084995A0[n] and gUnknown_03003150[n] back to back at the same index
 * n, so the two arrays are parallel and this one is the wider record.
 * sub_0800C608 removes an entry by zeroing unk00 in this array and flags in the
 * other, again at one index -- so unk00 carries the same "slot occupied"
 * meaning as Unk03003150's flags.
 *
 * sub_0800C574 also terminates the list by writing 0xFF to the NEXT record's
 * unk00 (`strb r0, [r1, #8]` off the same `n * 8` product), which is where the
 * 0x08 stride is confirmed independently of the +3 subscript evidence in
 * unknown-functions.h. */
struct PropertyListEntry /* 0x08 */
{
    /* 0x00 */ u8 unk00; /* tile id / slot tag; 0 is empty and 0xFF terminates
                          * the list, matching Unk03003150's flags byte */
    /* 0x01 */ u8 unk01; /* map column, the same value as Unk03003150's x */
    /* 0x02 */ u8 unk02; /* map row, the same value as Unk03003150's y */
    /* 0x03 */ u8 unk03[0x03];
    /* 0x06 */ u8 filler_06[0x02];
};

struct Unk0849B018 /* >= 0x0e */
{
    /* 0x00 */ volatile u8 unk00; /* Wave 30, W30-A: a small mode tag, plain
                          * `ldrb` and compared against 3 by sub_08031F28,
                          * sub_08031F5C and sub_08031F88 -- three siblings
                          * that gate their whole body on it.
                          *
                          * Wave 32, W32-C ADDS the volatile, on the same
                          * evidence as unk01 and unk04: sub_080310D8,
                          * sub_08031128 and sub_0803117C each write a plain
                          * constant here and each emits a DEAD `ldrb [rB]`
                          * immediately before the `strb`, which a
                          * non-volatile member does not. Byte-neutral at the
                          * three `== 3` readers, which are compares. */
    /* 0x01 */ volatile u8 unk01; /* sub_080303B0 returns whether it equals 2,
                          * with a plain `ldrb` that is byte-identical either
                          * way. Wave 31, W31-A ADDS the volatile on the same
                          * evidence unk04 was settled by: sub_08030C98's plain
                          * `= 1` emits a dead `ldrb [r2,#1]` immediately before
                          * the `strb`, which a non-volatile member does not.
                          * sub_080303B0 re-verified after the change. */
    /* 0x02 */ volatile u16 unk02; /* Wave 32, W32-A: a status halfword that
                                    * sub_0802F408 READS AND CLEARS -- bit 3 is
                                    * the "link stalled" flag it checks against
                                    * REG_SIOCNT's bit 3. VOLATILE on the same
                                    * evidence as unk04 below: the plain `= 0`
                                    * emits a dead `ldrh [r3,#2]` immediately in
                                    * front of the `strh`. */
    /* 0x04 */ volatile u16 unk04; /* volatile: sub_08030574's plain `= 5` emits a
                                    * dead `ldrh` of the field right before the
                                    * `strh`, which a non-volatile member does not */
    /* 0x06 */ volatile s8 unk06; /* signed: sub_0802F4F4 reads it
                                   * ldrb; lsls #24; asrs #24.
                                   *
                                   * Wave 30, W30-A adds the VOLATILE, and
                                   * sub_080321F0 is the only discriminating
                                   * use in the tree so far. It indexes unk0a[]
                                   * with this field; a plain `s8` member in an
                                   * ARRAY-INDEX position compiles to
                                   * `movs rI,#6; ldrsb rD,[rB,rI]` (agbcc
                                   * rewrites the address into the reg+reg form
                                   * ldrsb needs), which is 2 bytes short of the
                                   * ROM. Only a volatile member keeps the
                                   * `ldrb [rB,#6]; lsls #24; asrs #24` the ROM
                                   * has. Every OTHER reader -- sub_0802F4F4's
                                   * s8 return, sub_080326FC's and
                                   * sub_080321F0's compares -- emits
                                   * ldrb + shifts either way, so none of them
                                   * could have settled it. See the wave-30
                                   * entry in docs/agbcc-codegen.md. */
    /* 0x07 */ volatile u8 unk07; /* Wave 33, W33-B: sub_0802F348 clears it with
                          * the dead-load-before-store volatile tell. */
    /* 0x08 */ volatile u8 unk08; /* a second bitmask byte alongside unk09: sub_0802F480
                          * is sub_0802F460 with the offset changed and nothing
                          * else, `(unk08 >> index) & 1` on the same s8 index.
                          * Wave 33, W33-B ADDS the volatile: sub_0802F348's plain
                          * `= 0` emits the dead `ldrb [r0,#8]` before the `strb`,
                          * the same tell unk00/unk01/unk07 carry. Byte-neutral at
                          * the `>> & 1` readers, which load `ldrb` either way. */
    /* 0x09 */ volatile u8 unk09;
    /* 0x0a */ volatile u8 unk0a[0x04]; /* Wave 34, W34-C ADDS the volatile:
                                * sub_08033194's plain `unk0a[0] = 4` emits a
                                * dead `ldrb [r2,#0xa]` immediately before the
                                * `strb`, the same tell that settled unk00,
                                * unk01, unk04, unk07 and unk08, and the two
                                * stores either side of it in that function are
                                * to the already-volatile unk06 and unk09.
                                * Byte-neutral at the only promoted reader
                                * (sub_080321A8's `unk0a[unk06] != 2`), which was
                                * re-verified after the change.
                                *
                                * Wave 30, W30-A: a per-slot byte array indexed
                                * by this record's OWN s8 unk06 --
                                * sub_080321F0 emits `adds r1,#0xa; adds r1,r1,r0`
                                * on the record base, which is the member-array
                                * hoist and not a constant displacement. The
                                * extent 4 is borrowed from the 0..3 army range
                                * unk08/unk09 mask and is NOT proved; the record
                                * certainly runs to at least 0x1e, which
                                * sub_08032134 clears. */
    /* Wave 49 (W49-F), carved out of the old filler_0e[8]; nothing above or
     * below moves and no named member changes width.  sub_0802F03C clears
     * these four halfwords inside the SAME i = 0..3 loop that clears unk0a[i],
     * unk16[i] and unk24[i], and the address is built as
     * `adds rB,#0xe; adds rB,rB,rI` with rI = 2*i on the record base -- agbcc's
     * member-array hoist, the same tell that settled unk16 and unk24, not a
     * load displacement.  Four elements exactly fills the old filler and
     * matches the 0..3 army range every neighbouring array uses.
     *   VOLATILE on the dead-`ldrh`-before-`strh` evidence: the plain `= 0`
     * carries a dead load of the very address being stored to, the same tell
     * that settled unk20/unk22/unk24 two members down. */
    /* 0x0e */ volatile u16 unk0e[4];
    /* Wave 42 (W42-K), carved out of the old filler_0e[0x0e]; no offset or width
     * above or below changes. sub_08030C54 sends four of them as
     * `sub_0802BD54(0xa0, (i + 10) * 8, unk16[i])` for i = 0..3, and the address
     * is built as `adds rB,#0x16; adds rB,rB,rI` on the record base -- a variable
     * index on a 0x16 base, which is the member-array spelling and not a
     * constant displacement. unk1b is the fifth value the same function sends
     * after the loop, reached at a constant `ldrb [rB,#0x1b]`, so 0x1a stays
     * unnamed and the two are NOT one array of six.
     * Volatility unproved either way: both are read-only here and a volatile
     * read is a plain `ldrb` too, so the weakest spelling is what is declared.
     *   Wave 49 (W49-F) SETTLES IT: volatile.  This is exactly the evidence the
     * paragraph above was waiting for -- the first WRITE.  sub_0802F03C clears
     * unk16[i] over i = 0..3 and the plain `= 0` carries a dead `ldrb` of the
     * very address being stored to, immediately before the `strb`; declared
     * plain the load disappears and the loop is one instruction short.  The
     * offset, the element type and the extent are all unchanged, and the only
     * existing reader (c_08030C54.c) is a single `ldrb` either way -- it was
     * re-verified as a match after the edit. */
    /* 0x16 */ volatile u8 unk16[0x04];
    /* 0x1a */ volatile u8 unk1a; /* Wave 49 (W49-F), carved out of the old
                          * filler_1a[1]; unk16 above and unk1b below are
                          * untouched.  sub_0802F03C clears it with a plain
                          * `= 0` at a constant `[rB,#0x1a]`, immediately before
                          * the identical clear of unk1b -- so the two are
                          * adjacent scalars and NOT one array of six with
                          * unk16, which c_08030C54.c's note already argued from
                          * the other side.  VOLATILE on the usual
                          * dead-`ldrb`-before-`strb` tell. */
    /* 0x1b */ volatile u8 unk1b; /* Wave 49 (W49-B) ADDS the volatile, on the same
                          * dead-`ldrb`-before-`strb` evidence that settled unk00,
                          * unk01, unk04 and unk1c..unk1f below.  TWO independent
                          * functions show it, at four separate sites: sub_08030234
                          * (`unk1b++` and a plain `= n`) and sub_080303C8 (`= 0`
                          * twice and `unk1b++`).  A retry/desync counter -- bumped
                          * when a peer's packet disagrees, cleared when the frame
                          * is accepted.
                          *   The only existing reader is c_08030C54.c, which
                          * passes it to sub_0802BD54; a volatile read is the same
                          * single `ldrb`, and that file was re-verified as a match
                          * after this edit. */
    /* 0x1c */ volatile u8 unk1c; /* Wave 33, W33-B: sub_0802F348 clears these
                          * three with the same dead-`ldrb`-before-`strb` tell
                          * unk00/unk01 carry, so volatile on the same basis. */
    /* 0x1d */ volatile u8 unk1d;
    /* 0x1e */ volatile u8 unk1e;
    /* 0x1f */ volatile u8 unk1f; /* Wave 32, W32-A: a stall counter sub_0802F408
                                   * bumps while the link is idle and clears
                                   * whenever either bit 3 fires, then reports
                                   * as "still healthy" below 0xb. VOLATILE:
                                   * BOTH the `++` and the `= 0` carry a dead
                                   * `ldrb [rN,#0x1f]` in front of the `strb`,
                                   * which is the same tell unk00/unk01/unk04
                                   * were settled by. */
    /* Wave 31, W31-B: six VOLATILE halfwords, on the same evidence that made
     * unk04 above volatile and every member of struct Unk0849B01C volatile --
     * every store to them is preceded by a dead load of the very address being
     * stored to. Two independent functions show it at these exact offsets:
     * sub_08033638 (matched this wave) and sub_0802F03C, which is still asm.
     * They are HImode aggregate members, which with volatile is the pair of
     * conditions the dead load needs.
     *
     * unk22 takes unk20 and unk24/unk26/unk28 shift down from unk2a, so the
     * second group is a four-deep history queue being cleared. */
    /* 0x20 */ volatile u16 unk20;
    /* 0x22 */ volatile u16 unk22;
    /* 0x24 */ volatile u16 unk24[4]; /* Wave 49 (W49-B) re-expresses what were
                          * four separate volatile u16 at 0x24/0x26/0x28/0x2a as
                          * the array they are.  NOTHING is widened, re-offset or
                          * re-qualified: same four halfwords, same offsets, same
                          * volatile, and the only existing reader (c_08033638.c,
                          * a chained `= 0` with constant indices) is byte-for-byte
                          * unchanged and was re-verified after the edit.
                          *   The evidence is a RUNTIME INDEX, which no scalar
                          * spelling can produce: sub_080309AC and sub_08030B00
                          * both read and bump this at `[entry->unk01]`, and the
                          * ROM computes the address as `adds rB,rP,#0;
                          * adds rB,#0x24; adds rB,rB,rI` -- agbcc's member-array
                          * hoist (the same tell c_08030C54.c records for unk16),
                          * NOT a load displacement.  Every non-array spelling
                          * tried -- `(&p->unk24)[i]` and a cast to
                          * `volatile u16 (*)[4]` -- folds the 0x24 into the
                          * `ldrh` displacement instead, so this is discriminating
                          * and not a stylistic choice.
                          *   It also agrees with the wave-31 W31-B reading of the
                          * clear above, which already described 0x24..0x2a as one
                          * four-deep queue shifting down from unk2a.  Indexed by
                          * unk06 / an entry's unk01, i.e. one slot per link
                          * participant: a per-peer packet sequence number, bumped
                          * on accept and echoed in the 0xAE NAK. */
    /* Wave 33, W33-B: sub_0802F348 clears four more fields far up the record,
     * each with the dead-load-before-store volatile tell. The offsets come
     * straight off the `ldr rN,=0x1abX; adds` pool words. */
    /* Wave 49 (W49-F), carved out of the old filler_2c[0x100]; unk24 above and
     * unk12c below are untouched and no offset moves.  The EXTENT IS PROVED
     * twice over: sub_0802F03C clears it over i = 0..0x7f with
     * `adds rB,#0x2c; adds rB,rB,(i*2)`, and 0x2c + 128 * 2 = 0x12c lands
     * exactly on unk12c, so the array fills the old filler with nothing left
     * over.  The clear is interleaved with gUnknown_03004400[i] over the same
     * 0..0x7f range, which is that array's own proved extent -- the two are
     * parallel 128-entry tables.
     *   VOLATILE on the dead-`ldrh`-before-`strh` tell. */
    /* 0x2c */ volatile u16 unk2c[128];
    /* 0x12c */ struct Unk08090CD8Entry unk12c[48]; /* Wave 33, W33-B: sub_080307E0
                          * reads unk12c[unk1aac].unk00/unk04 -- the same 0x88-byte
                          * entry array, at the same 0x12c offset, that struct
                          * Unk08090CD8Buf carries. This record and that buffer
                          * share the entry-array tail. */
    /* 0x1aac */ volatile u8 unk1aac; /* read cursor, volatile like the twin
                          * buffer's write cursor unk1aad */
    /* 0x1aad */ volatile u8 unk1aad; /* Wave 42 (W42-K), carved off the FRONT of
                          * the old filler_1aad[2]; 0x1aae stays unnamed. The
                          * write cursor of the RECEIVE ring, the counterpart to
                          * unk1aaf two bytes up, and it indexes the LOWER half
                          * of unk12c: sub_08030930 pushes at
                          * `base + 0x12c + 0x88 * unk1aad` and closes with
                          * `unk1aad &= 0x1f`, bounding it to slots 0..31 -- the
                          * other half of the 0..31 read / 32..47 write split the
                          * unk1aaf note above describes.
                          *   VOLATILE on the same evidence as unk1aac and
                          * unk1aaf: sub_08030930 advances it as `unk1aad++;` and
                          * `unk1aad &= 0x1f;` in two separate statements and
                          * BOTH carry a dead `ldrb` of the cursor immediately
                          * before the `strb`. */
    /* 0x1aae */ volatile u8 unk1aae; /* Wave 49 (W49-B), carved off the last byte
                          * of the old filler; nothing is reshaped. The READ
                          * cursor of the SEND ring, i.e. the counterpart of
                          * unk1aaf one byte up: sub_080309AC and sub_08030B00
                          * both walk `unk12c[32 + unk1aae]` -- the same
                          * `base + 0x122c + 0x88 * cursor` address sub_08030838
                          * writes -- and both close each iteration with
                          * `unk1aae++; unk1aae &= 0xf;`, the same 16-slot bound
                          * unk1aaf carries. So unk12c serves FOUR cursors, two
                          * per half: 1aac/1aad on slots 0..31 and 1aae/1aaf on
                          * 32..47.
                          *   VOLATILE on the same evidence as its three
                          * neighbours: in both functions the `++` and the
                          * `&= 0xf` are separate statements and BOTH carry a
                          * dead `ldrb` of the cursor immediately before the
                          * `strb`. */
    /* 0x1aaf */ volatile u8 unk1aaf; /* Wave 41 (W41-C), carved off the end of
                          * the old filler_1aad[3]; 0x1aad and 0x1aae stay
                          * unnamed. The WRITE cursor of the send ring, the
                          * counterpart to unk1aac two bytes down, and it
                          * indexes the UPPER HALF of unk12c: sub_08030838
                          * pushes at `base + 0x122c + 0x88 * unk1aaf`, and
                          * 0x122c is exactly &unk12c[32], so the array is one
                          * declaration serving two rings -- 0..31 read, 32..47
                          * written. The `&= 0xf` at the end of that push bounds
                          * it to the 16 upper slots and confirms the split.
                          *   VOLATILE on the same evidence as unk1aac and
                          * gUnknown_03003128: sub_08030838 advances it as
                          * `unk1aaf++;` and `unk1aaf &= 0xf;` in two separate
                          * statements, and BOTH carry a dead `ldrb` of the
                          * cursor immediately before the `strb`. */
    /* 0x1ab0 */ volatile u16 unk1ab0;
    /* 0x1ab2 */ volatile u8 unk1ab2;
    /* 0x1ab3 */ volatile u8 unk1ab3;
    /* 0x1ab4 */ volatile s8 unk1ab4;
};

/* 0x0849B01C -- ROM pointer to a RAM record, distinct from gUnknown_0849B018
 * next door (different widths at the same offsets). EVERY member listed here is
 * volatile, and the evidence is uniform: in sub_0802F23C all seven stores are
 * preceded by a dead load of the very address being stored to, and sub_08031B30
 * does the same at unk06 and unk212. All of them are QImode/HImode aggregate
 * members, which is the pair of conditions the dead load needs.
 */
struct Unk0849B01C /* >= 0x214 */
{
    /* 0x000 */ volatile u16 unk00;
    /* 0x002 */ volatile u16 unk02;
    /* 0x004 */ volatile u8 unk04;
    /* 0x005 */ volatile u8 unk05;
    /* 0x006 */ volatile u16 unk06;
    /* Wave 41 (W41-E), carved out of the old filler_08[0x208]; no offset or
     * width above or below changes. sub_080301E8 copies a whole ROW of unk08
     * into unk208 with `unk208[i] = unk08[unk05][i]`, i = 0..3, and the two
     * shifts come out SEPARATE (`lsls #1` on i, `lsls #3` on unk05, added
     * together, then added to base+8) -- which is the two-dimensional address
     * form. Written flat as `unk08[unk05 * 4 + i]` agbcc folds the whole index
     * before scaling and puts one `lsls` in the wrong place, the same way
     * gUnknown_02025C18 does (see W41-D's note on it further up).
     *   The extent 64 is PROVED, not padded: sub_080301E8 finishes with
     * `unk05 = (unk05 + 1) & 0x3f`, so the row cursor wraps at 64, and
     * 64 * 4 * 2 = 0x200 lands exactly on unk208.
     *   unk08 is NOT volatile, and it is the only member of this record that
     * is not: sub_08030178's `unk08[0][i] |= 0xffff` over i = 0..3 emits a
     * single `ldrh; orrs; strh` per element with no dead load, where every
     * volatile member here carries one. (The `0xffff` living in a callee-saved
     * register instead of folding to a constant store is the plain-u16 `|=`
     * behaviour already documented under Bitfields, not a bitfield tell.)
     *   unk208 IS volatile, on the usual dead-load-before-store evidence in
     * sub_080301E8's copy loop. */
    /* 0x008 */ u16 unk08[64][4];
    /* 0x208 */ volatile u16 unk208[4];
    /* 0x210 */ volatile u16 unk210;
    /* 0x212 */ volatile u16 unk212;
};

/* 0x0849B060 -- ROM pointer to a RAM record. unk00 is round-tripped through
 * sub_080315E8 by sub_080319A8 and sub_08031BF0; unk0a is set from a byte by
 * sub_08031BE0 and cleared by sub_08031CD4.
 */
struct Unk0849B060 /* >= 0x0e */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ s16 unk04; /* Wave 30, W30-A: `movs r1,#4; ldrsh r0,[r2,r1]` in
                           * sub_08032420, which multiplies it by 40 and hands
                           * it to sub_08032340 as an s16 coordinate -- so
                           * SIGNED, not the u16 the `ldrh` in sub_08032234
                           * suggests (that one truncates straight into the u8
                           * unk09, where the load width is free). */
    /* 0x06 */ s16 unk06; /* the y twin of unk04, same `ldrsh` in the same
                           * expression (sub_08032420). */
    /* 0x08 */ u8 unk08; /* Wave 30, W30-A: sub_08032048 stores
                          * `(gUnknown_03003FC0.unk02 - 0xb4) % 3` here and
                          * `... / 3` into unk09 -- a two-byte split of one
                          * index, plain `strb` at a displacement `strb` can
                          * reach. */
    /* 0x09 */ u8 unk09; /* also compared against the s8 gUnknown_0849B018->unk06
                          * by sub_080326FC, so it is a player/army slot id. */
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ u8 unk0c; /* Wave 34, W34-C: a small count sub_08032A00 gates a
                          * whole block on with `cmp #1; bls`, an UNSIGNED
                          * compare, i.e. `> 1`. Plain `ldrb`. */
    /* 0x0d */ u8 unk0d; /* a small script selector: sub_08034208 reads it with
                          * a plain `ldrb` and uses it to index
                          * gUnknown_0849BC44[] (`lsls #2`). */
    /* 0x0e */ u16 unk0e; /* Wave 34, W34-C: the y coordinate the four
                           * gUnknown_0849B650 steppers (sub_08032734,
                           * sub_08032788, sub_080327FC, sub_08032850) drive and
                           * sub_08032A00 reads. Always `ldrh`, and every reader
                           * feeds it straight into the volatile u16
                           * gUnknown_03002B34 as `unk0e - 0x60`, where the load
                           * width is free -- so u16 is the weakest model and
                           * NOTHING sign-extends it, even though the steppers
                           * store negated table entries here. */
    /* 0x10 */ u8 unk10; /* Wave 34, W34-C: a 3-valued direction tag. The same
                          * four steppers test it `== 1` and `== 2` in two
                          * consecutive independent `if`s (not an else-chain --
                          * the pointer global is reloaded at the second test),
                          * and pick the sign of gUnknown_0849B650[unk0a] from
                          * it. Plain `ldrb`. */
};

/* Wave 28, W28-A: the row is NINE WORDS and sub_08035B3C indexes it as one --
 * `(unk36 * 9 + k) * 4` off the base, i.e. the k'th word of row unk36, with k
 * chosen at run time. unk14 is that array's word 5 under a constant index. The
 * flat `u8 filler` spelling below is kept because the two constant-index
 * readers (sub_08035B68, sub_08035B00, sub_08035F68) need named members; the
 * variable-index reader casts the row's address to `u8 **` instead. */
struct Unk0849CD88 /* 0x24 */
{
    /* 0x00 */ u8 filler_00[0x14];
    /* 0x14 */ u32 unk14;
    /* 0x18 */ s16 unk18; /* wave 28, W28-A: `ldrsh`, and sub_08035B00 returns
                           * it as `s16` both bare and doubled. */
    /* 0x1a */ u16 unk1a; /* wave 37, W37-H: sub_08035E90 and sub_08035FA8 both
                           * test it `!= 0` with a plain `ldrh` and then hand it
                           * to sub_0803B48C(s16) as an `ldrsh`. UNSIGNED and not
                           * s16: an s16 member would have made the `!= 0` test
                           * an `ldrsh` too, while `(s16)` of a u16 member is
                           * exactly what combine folds into the `ldrsh`. */
    /* 0x1c */ u8 filler_1c[0x02];
    /* 0x1e */ u16 unk1e; /* wave 28, W28-A: `ldrh`, compared against 0x8000 by
                           * sub_08035F68 -- a sentinel, so the width is a floor
                           * and the signedness is open. */
    /* 0x20 */ void *unk20; /* wave 36, W36-F: sub_08036024 copies word 8 of the
                             * row straight into a proc's +0x18 slot, and the
                             * ROM values are 0x030xxxxx RAM addresses. Named in
                             * place of the old filler -- byte-neutral, only a
                             * member's START offset enters address arithmetic. */
};

/* Wave 28, W28-A. An eight-byte ROM row indexed by a palette/theme id:
 * sub_08035020 hands +0x00 to ApplyPaletteExt as its `u16 *` source, and
 * sub_08035064 hands +0x04 to sub_0803B4DC after an `ldrsh`. Nothing reads
 * +0x06. */
struct Unk0849BD20 /* 0x08 */
{
    /* 0x00 */ u16 *unk00;
    /* 0x04 */ s16 unk04;
    /* 0x06 */ u8 filler_06[0x02];
};

/* 0x0849D5F8 -- ROM pointer to a >= 0x46 byte record, ~12 callers, reloaded
 * at every use (so not const). unk20 is indexed by unk45, which is signed:
 * every reader of +0x45 and of the +0x20 array either uses `ldrsb` or the
 * `ldrb; lsls #24; asrs #24` byte-twin, so both are s8 objects. */
struct Unk0849D5F8 /* >= 0x46 */
{
    /* 0x00 */ u8 filler_00[0x1e];
    /* 0x1e */ u8 unk1e;
    /* 0x1f */ u8 unk1f;
    /* 0x20 */ s8 unk20[0x0c];
                          /* Wave 32 (W32-B): SPLIT from one s8[0x25] into three
                           * parallel per-slot tables, and the split is what
                           * makes sub_08038B84 match rather than a guess about
                           * the data. All three are indexed by the SAME unk45,
                           * and the ROM materialises `base + 0x2c` and
                           * `base + 0x38` directly before adding the index --
                           * the member-array shape. A single s8[0x25] would
                           * spell them `unk20[0x0c + i]`, which computes
                           * `base + 0x20` first and then adds `0x0c + i`, and
                           * that is different code. Byte-neutral for the only
                           * other user (src/decomp/c_080386DC.c touches unk1e
                           * and unk1f only). The extents are "what fits below
                           * the next one", not proved. */
    /* 0x2c */ s8 unk2c[0x0c];
    /* 0x38 */ s8 unk38[0x0d];
    /* 0x45 */ s8 unk45;
};

/* 0x0849ECDC -- ROM pointer to a small record, reloaded at every use (so not
 * const: sub_0803BF10 re-`ldr`s it across a `strb`). unk00 is a limit and
 * unk01 a cursor -- sub_0803BF70 returns `unk01 == unk00`, sub_0803BEF8 resets
 * unk01 to 0 and sub_0803BF10 walks it. unk00 is set to 2/3/4 by
 * sub_0803BDF8/sub_0803BE10/sub_0803BE28 and to a u8 argument by sub_0803B930.
 */
/* A cursor/limit pair, in that order: sub_0803BF70 raises gUnknown_03002F1C
 * when unk01 differs from unk00, and sub_0803BFA4 loads gUnknown_020288B0 with
 * unk01 + 1. Both bytes are read with a bare `ldrb`, so the signedness is
 * open. */
struct Unk0849ECDC /* >= 0x02 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
};

struct Unk08580934_Sub
{
    /* 0x00 */ u8 filler_00[0x08];
    /* 0x08 */ u32 unk08;
};

/* The objects held in Unk08580934.unk44[]. Every field is written as a
 * halfword by the spawner family at 0x080646BC-0x0806517C (sub_080646BC,
 * sub_08064BC8, sub_08064CE0, sub_08064D44, sub_08064EE0, sub_08064FB0,
 * sub_08065098, sub_080650FC, sub_0806517C), which all take the object in r0
 * and all also bump Unk08580934.unk2d.
 *   unk1c  the object's own slot index -- sub_08064BC8 stores the argument
 *          here AND uses the same value to index gUnknown_08580934->unk44[]
 *   unk24  set to unk1c*2 at the sub_0806502C call site
 *   unk26  a kind/opcode; 0x0b, 0x0c, 0x0e and 0x10 are each written literally
 *   unk28  SIGNED: sub_0806502C reads its own unk28 with `ldrsh`, adds 8, and
 *          passes that as the argument stored back here
 *   unk38  0x4e (sub_08064D44), 0 (sub_080650FC, sub_0806517C)
 *   unk3a/unk3c  written together as 0 and 2 (sub_08064CE0, sub_08065098)
 * Widths are floors, not proofs: a bare `strh` setter cannot separate
 * int/u32/u16/s16 (see the setter table in docs/agbcc-codegen.md), so only
 * unk28's signedness is actually established, and that from the call site.
 * Whether this is the same type as struct Unk08580934_Sub is UNPROVED -- their
 * offsets do not conflict, so they could be merged, but nothing seen so far
 * puts one object into both unk44[] and unk74[].
 */
/* Wave 17 signs unk24/unk26 and narrows unk2a out of filler_2a, all from the
 * F092 tick handlers (sub_080646D4, sub_08064FC8, sub_08065118) and the two
 * emitters they call:
 *   unk24  SIGNED -- the handlers test it `movs r2,#0x24; ldrsh r0,[r4,r2];
 *          cmp r0,#0`, which is the s16-OBJECT tell, not a cast: reading a u16
 *          member through `(s16)` costs `ldrh; lsl #16; asr #16` instead. Its
 *          `--` in the same function still emits `ldrh`, because the result is
 *          only ever stored back with `strh` and the sign bits cannot survive.
 *   unk26  SIGNED, the same `ldrsh` at the table index, and confirmed
 *          independently by `--unk26 < 0` compiling to `lsl #0x10; cmp #0;
 *          bge` -- a sign test of the low half, which a u16 member cannot
 *          produce.
 *   unk2a  a y coordinate, and SIGNED on the consumer's evidence rather than
 *          the producer's: the handlers only `strh` into it (byte-identical
 *          for int/u32/u16/s16), but sub_08064E5C reads it `movs r2,#0x2a;
 *          ldrsh r1,[r6,r2]` and passes it beside the s16 unk28.
 * The three `u16 -> s16` changes are byte-neutral for the nine already-matched
 * setters in src/decomp/c_080646BC.c and its neighbours -- re-verified with
 * trymatch after the edit. */
struct Unk08580934_Obj /* >= 0x3e */
{
    /* 0x00 */ u8 filler_00[0x1c];
    /* 0x1c */ u16 unk1c;
    /* 0x1e */ u8 filler_1e[0x06];
    /* 0x24 */ s16 unk24;
    /* 0x26 */ s16 unk26;
    /* 0x28 */ s16 unk28;
    /* 0x2a */ s16 unk2a;
    /* 0x2c */ int unk2c; /* wave 35 (W35-D): the old filler_2c[0x0c] is exactly
                           * three words and the 0x0806D handlers name all three.
                           * unk2c is a whole-word `ldr` (sub_0806D208 adds a u16
                           * table entry to it to make unk28); unk30 accumulates
                           * unk34 into itself every frame and is then added into
                           * unk28, i.e. a fixed-point position/velocity pair
                           * (`ldr; ldr; adds; str`, so word-wide, and the sign is
                           * unproved -- nothing compares them). */
    /* 0x30 */ int unk30;
    /* 0x34 */ int unk34;
    /* 0x38 */ u16 unk38;
    /* 0x3a */ u16 unk3a;
    /* 0x3c */ u16 unk3c;
    /* 0x3e */ u16 unk3e; /* wave 46 (W46-E): a pair of tile/graphics bases, both
                           * built the same way by sub_0806D268 --
                           * `0x6000 + gUnknown_0816E0D0[i]` and
                           * `0x6000 + gUnknown_0816E0DE[i]`, from two parallel
                           * seven-entry u16 ROM tables. Both are plain `strh`,
                           * so the width is exact and the sign is unproved.
                           * unk3e reaches its `strh` at a displacement, unk40
                           * through `adds rN, #0x40` -- that is strh's imm5
                           * scaled limit of 62, not two different objects. */
    /* 0x40 */ u16 unk40;
    /* 0x42 */ u16 unk42; /* wave 46 (W46-E): sub_0806D53C writes it in both
                           * arms of a test on gUnknown_08580934->unk09[i] --
                           * 0x42b0 flat, or 0x42a0 + i * 4. Plain `strh`
                           * through `adds rN, #0x42` (past strh's imm5 scaled
                           * limit of 62), so the width is exact and the sign
                           * is unproved. Sits beside unk40, which the same
                           * function fills with 0x8290 + i * 4 -- the two look
                           * like a pair of OAM-ish bases. */
    /* 0x44 */ u16 unk44; /* wave 32 (W32-B): sub_08064E5C ORs it under 0xc00 to
                           * make sub_08043FD8's third argument. Plain `ldrh`. */
    /* 0x46 */ u8 unk46; /* a flag: sub_0806DDF4 clears it across all seven
                          * unk54[] entries and then sets it on the one unk33
                          * selects -- a "which of the seven is current" mark.
                          * That this is the same element type as unk44[] is an
                          * assumption of the array declaration, not proved. */
    /* 0x47 */ u8 unk47; /* wave 35 (W35-D): a mode flag paired with unk48/unk4b.
                          * sub_0806DC50 tests it against 1 and, when it is not 1,
                          * falls through to a bounds test on unk48 against
                          * unk4b - 1 -- the "wrap" half of the same pair
                          * sub_0806DD34 (src/decomp/c_0806DCB8.c) reads at these
                          * three offsets through its own local struct. Bare
                          * `ldrb`, so u8. */
    /* 0x48 */ u8 unk48; /* wave 29 (C): a small variant selector. sub_08064738
                          * reads it as a plain truth test to pick one of two
                          * sprite ids; sub_08064774 uses it to index a
                          * four-entry u16 table. Bare `ldrb`, so u8. */
    /* 0x49 */ u8 unk49; /* wave 32 (W32-B): sub_08064E5C's last statement is
                          * `unk49 = unk48`, a plain byte copy through the same
                          * materialised address (`adds r0,#0x48; ldrb; adds
                          * r0,#1; strb`), which is what fixes it at +0x49. */
    /* 0x4a */ u8 filler_4a[0x01];
    /* 0x4b */ u8 unk4b; /* wave 35 (W35-D): the COUNT unk48 cursors over --
                          * sub_0806DC50 compares `unk48 < unk4b - 1`, the same
                          * expression sub_0806DD34 uses to wrap. Bare `ldrb`. */
    /* 0x4c */ void (*unk4c)(struct Unk08580934_Obj *);
                         /* wave 32 (W32-B): sub_080645AC calls it through
                          * `bl _call_via_r1` with r0 still holding the object,
                          * so it is a one-argument callback taking the object
                          * itself. Whole-word `ldr`, and nothing writes it in
                          * this block, so the parameter type is from the call
                          * site alone. */
};

/* unk2d is a plain post-incremented counter: sub_0806D188, sub_0806D1F0,
 * sub_0806D458 and sub_0806D4C0 all open with `gUnknown_08580934->unk2d++`
 * (`ldr; ldr; adds #0x2d; ldrb; adds #1; strb`). u8 and NOT volatile -- there
 * is no dead reload of the store's own address, which a volatile aggregate
 * member of QImode would have (see "Aliasing and volatile").
 *
 * unk44 is a slot table indexed by the object's own unk1c: sub_08064BC8 and
 * sub_08064D44 do `lsls #2` then `str` at base+0x44. The 0xc extent is
 * inferred from the gap up to unk74 and is NOT proved -- no bound check on
 * the index has been seen anywhere.
 *
 * gUnknown_08580934 is NOT const, and does not need to be. sub_08064BC8 keeps
 * the deref live across the `str` into unk44[] and reuses it for unk2d, which
 * looks like the `const` tell in docs/agbcc-codegen.md ("a global loaded once
 * and kept live across stores wants const"). It is not: probed both ways in
 * one call, `g->unk44[i] = obj; g->unk2d++;` spelled with the global reached
 * by name twice and with a bound local pointer compile to the SAME bytes, and
 * both reuse the deref. The tell applies to a re-read of the global's own
 * word, not to a deref result already sitting in a register -- same
 * distinction sub_08044178 draws for computed addresses.
 */
/* unk44's extent was 0xc, "inferred from the gap up to unk74 and NOT proved".
 * sub_0806DDF4 disproves it: there is a SECOND pointer array based at +0x54,
 * and the evidence is a codegen split rather than an offset. It walks seven
 * entries from +0x54 with `lsls rI,rN,#2; adds rB,#0x54; adds rB,rB,rI` and a
 * counter that survives to `cmp #6; ble`. Spelled as unk44[i + 4] -- the same
 * addresses, one array -- agbcc folds the +4 into a derived induction variable
 * instead: `mov r2,#0x10; ... adds r2,#4` with a separate REVERSED counter
 * `mov r1,#6; subs r1,#1; bge`. Different instructions, not a permutation, so
 * the source cannot have written a biased index into unk44. Probed in one call
 * against the unk54[i] spelling, which reproduces the ROM exactly.
 * Byte-neutral: 0x44 + 4*4 == 0x54 and 0x54 + 7*4 == 0x70, so unk74 stays at
 * +0x74 and sizeof is unchanged. Both existing users (c_08064BC8.c and
 * c_08064D44.c) index unk44 with a variable, so the extent never reached their
 * codegen; both re-verified byte-exact after the change.
 * The 4 on unk44 is now the same kind of guess the 0xc was -- it is whatever
 * fits below unk54, not a proved bound.
 */
struct Unk08580934
{
    /* 0x00 */ u8 unk00;  /* wave 32 (W32-B): sub_08065818 packs this header into
                           * the +0x84 run below, one field per byte, and that is
                           * where the widths come from. unk00, unk01, unk02,
                           * unk03, unk06, unk07 are all bare `ldrb`; unk04 is a
                           * SIGNED halfword (`movs r1,#4; ldrsh r0,[r4,r1]`,
                           * THUMB's only signed-halfword form) handed to
                           * __divsi3 against 500. unk00 and unk01 are tested
                           * only for zero, so their signedness is unproved. */
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 unk03;
    /* 0x04 */ s16 unk04;
    /* 0x06 */ u8 unk06;
    /* 0x07 */ u8 unk07;
    /* 0x08 */ u8 unk08; /* wave 36 (W36-G): a small mode selector, read with a
                          * BARE `ldrb [r1, #8]` -- no sign extension, unlike the
                          * s8 unk26/unk32/unk33 further down. sub_0806675C
                          * branches on it being 2 and, in that arm, stores the
                          * SAME register straight into unk26. */
    /* 0x09 */ u8 unk09[4]; /* wave 29 (C): three parallel per-slot byte tables,
                             * all reached as `base + K + index` with the index
                             * unscaled and an `ldrb`. sub_08064DDC reads +0x09
                             * and compares it against 2; sub_08064E1C reads
                             * +0x0d and adds 0x3d to make a sprite id;
                             * sub_08064BF4 reads +0x11 with obj->unk1c -- the
                             * SAME index that drives unk44[] -- and adds 0xbd.
                             * The extents are "whatever fits below the next
                             * one", except unk11's 4, which unk44[] bounds. */
    /* 0x0d */ u8 unk0d[4];
    /* 0x11 */ u8 unk11[4];
    /* 0x15 */ u8 unk15;  /* wave 32 (W32-B): both bare `ldrb`. unk16 is a
                           * countdown -- sub_08065818 does `unk16--` as its
                           * first statement and nothing else touches it there;
                           * unk15 is subtracted from unk07 + 1 to make the
                           * +0x88 field. */
    /* 0x16 */ u8 unk16;
    /* 0x17 */ u8 unk17;  /* wave 36 (W36-G): a modulus -- sub_08065DAC wraps a
                           * unk1c[] cursor with `(cur + unk17 - 1) % unk17` and
                           * `(cur + unk17 + 1) % unk17`, so it is the COUNT of
                           * live entries, read `ldrb [r5, #0x17]`. */
    /* 0x18 */ u8 *unk18; /* wave 36 (W36-G): `ldr [r0, #0x18]` then `adds r0,
                           * r0, r4; ldrb` -- a byte table indexed by the unk1c[]
                           * cursor, whose element sub_08065DAC hands to
                           * sub_08043E3C's int first parameter.
                           *
                           * Wave 37, W37-Q1: the length was a guess bounded by
                           * unk24 at 0x24; it is FOUR, because 0x20 starts a
                           * fourth per-slot byte table beside unk09/unk0d/unk11
                           * -- sub_0803C1D4 copies all four in one loop body
                           * with the same index (`ldr rB,[rP]; adds rB,#K;
                           * adds rB,rB,rI; ldrb` for K = 9, 0xd, 0x11 and
                           * 0x20). A declared array LENGTH never enters address
                           * arithmetic, so shrinking this is byte-neutral for
                           * sub_08065DAC. */
    /* 0x1c */ u8 unk1c[0x04]; /* wave 36 (W36-G): a per-slot cursor array,
                           * reached as `adds r0, #0x1c; adds r0, r0, r6` with
                           * the slot index in r6 and read/written `ldrb`/`strb`.
                           * The LENGTH is a guess bounded by unk24 at 0x24;
                           * only the 0x1c start offset enters the arithmetic. */
    /* 0x20 */ u8 unk20[0x04]; /* Wave 37, W37-Q1: the fourth of the parallel
                          * per-slot byte tables that start at 0x09, 0x0d and
                          * 0x11 -- sub_0803C1D4's loop reads all four with one
                          * index and stores them into four parallel 5-byte
                          * arrays in gUnknown_03003FC0. Extent 4 from the trip
                          * count (i <= 3) and from unk24 following. */
    /* 0x24 */ u8 unk24;  /* wave 30 (W30-C): a plain mode flag. sub_08066B6C
                           * reads it with a BARE `ldrb` and tests it against
                           * zero to pick sub_0806630C or sub_08066B40 -- no
                           * sign extension anywhere, unlike unk26 two bytes
                           * along, so u8 and the signedness is unproved. */
    /* 0x25 */ s8 unk25;  /* wave 46 (W46-F): named from filler. sub_0806574C
                           * writes it `unk24 ? sub_0802F4F4() : -1` -- a
                           * plain `strb` of either that function's declared s8
                           * return or the literal -1, so s8 is the type both
                           * sources agree on. The store is byte-neutral
                           * between s8 and u8; the -1 is the only evidence and
                           * nothing reads the field yet. */
    /* 0x26 */ s8 unk26;  /* wave 30 (W30-C): SIGNED -- sub_0806630C and
                           * sub_08066B40 both read it `ldrb; lsls #0x18;
                           * asrs #0x18`, which is Thumb's only way to reach a
                           * signed byte at an immediate offset. Each dispatches
                           * on 0 and 1 with a default that does nothing, so the
                           * observed range is 0..1 and the sign is a type fact
                           * rather than a used one. */
    /* 0x27 */ u8 filler_27[0x03];
    /* 0x2a */ s16 unk2a; /* wave 36 (W36-G): retyped u16 -> s16. sub_08066D74
                           * reads it `movs r1,#0x2a; ldrsh r0,[r0,r1]` -- a
                           * direct SIGNED load, which is the field's own type,
                           * not a cast: an `(s16)` cast applied to a u16 field
                           * would be `ldrh` plus `lsls #0x10; asrs #0x10`, three
                           * instructions where the ROM has one. Every other
                           * access is sign-agnostic (`strh 0`, and an
                           * `ldrh; adds #1; strh` increment), and no promoted
                           * file reads this field, so nothing else moves.
                           * Cleared with `strh 0` by sub_08066BF4 / sub_0806DCB8
                           * on the frame the unk33 cursor actually moves, in
                           * the same statement pair that plays sound 0x64 --
                           * an animation/scroll counter for the menu the
                           * cursor drives. Width is a floor, not a proof: a
                           * bare `strh` of 0 cannot separate u16 from s16 or
                           * from a wider field's low half (wave 21, W21-A). */
    /* 0x2c */ u8 unk2c;  /* wave 46 (W46-F): named from filler. sub_0806574C
                           * copies the u8 gUnknown_03003FC0.unk32 into it with
                           * a bare `ldrb`/`strb` pair, so the width is exact
                           * and the sign is unproved. Distinct from unk2d
                           * beside it -- both are reached through their own
                           * `adds rN, #0x2x` because strb's displacement stops
                           * at 31, not because they are one object.
                           *   Wave 46 (W46-E), independently: sub_0806D850 is
                           * the same producer over gUnknown_0202F2C8, and
                           * sub_0803BFBC -- which both sub_0806574C and
                           * sub_0806D850 call on the very next line -- is the
                           * CONSUMER, opening with `ldrb` at +0x2c and a test
                           * against 1. Producer and consumer agreeing
                           * independently is the discriminating use, so this
                           * one is no longer width-only evidence. */
    /* 0x2d */ u8 unk2d;
    /* 0x2e */ u16 unk2e; /* wave 35 (W35-D): the graphics id currently loaded.
                           * sub_0806DF58 picks an id from one of two ROM tables
                           * (or the constant 0x9de), compares it against this
                           * halfword, and only re-uploads the tiles when they
                           * differ -- then stores it back. `ldrh` / `strh` at a
                           * plain displacement, so the width is exact; the sign
                           * is unproved (equality compare only). */
    /* 0x30 */ u8 unk30; /* A byte flag bracketing a blocking sub-routine:
                          * sub_08065990 clears it, calls sub_08065238, then
                          * sets it to 1; sub_0806D944 only clears it, as its
                          * last statement. `adds rN, #0x30; strb` rather than
                          * `strb [rN, #0x30]` because THUMB's strb immediate
                          * stops at 31 -- the split is the addressing mode,
                          * not a separate object. */
    /* 0x31 */ u8 unk31; /* wave 36 (W36-G): a sub-mode flag read only when unk08
                          * is 2 -- sub_08066220 tests it against 1 to pick which
                          * key bit to fake into gpKeySt->held. Bare `ldrb`, no
                          * sign extension. */
    /* 0x32 */ s8 unk32; /* wave 30 (W30-C): SIGNED, same `ldrb; lsls #0x18;
                          * asrs #0x18` tell as unk26. sub_08066078 uses it to
                          * index unk44[], so 0..3 -- the twin of unk33's role
                          * for unk54[], one array along. */
    /* 0x33 */ s8 unk33; /* SIGNED, and proved twice: sub_0806DDF4 reads it
                          * `ldrb; lsls #24; asrs #24` and sub_0806DCB8 reads
                          * the same byte with `ldrsb`. It selects one of the
                          * seven unk54[] entries, so 0..6. */
    /* 0x34 */ struct Unk08580934_Obj *unk34[4];
                             /* wave 32 (W32-B): a THIRD parallel object table
                              * beside unk44[] and unk54[]. sub_08065EF4 indexes
                              * it with `unk32 / 2` (`lsrs #0x1f; adds; asrs #1`
                              * then `lsls #2`) and reads unk28 off the result,
                              * which is what fixes the element type. The extent
                              * is "what fits below unk44", not a proved bound;
                              * unk32 selecting it in halves suggests 0..3. */
    /* 0x44 */ struct Unk08580934_Obj *unk44[4];
    /* 0x54 */ struct Unk08580934_Obj *unk54[7];
    /* Wave 44 (W44-B): unk70[] is SIGNED and the u8 below is probably wrong,
     * but it is left alone deliberately. sub_080665D4 and sub_0806666C both
     * read it `ldrb; lsls #24; asrs #24` and then compare against 0 and 1 --
     * a sign extension a u8 member cannot produce -- and the three writers
     * agree: 0xFF (c_08066580.c), 1 (c_08066374.c), 0 (sub_08066710), which as
     * s8 are -1, 1 and 0, i.e. a three-valued mark with a negative "none"
     * state rather than three unrelated byte constants. Retyping it to s8 would
     * change the bytes of the promoted files that read it zero-extended, so the
     * two readers carry an `(s8)` cast at the use instead and both match. If a
     * future wave re-verifies those readers, s8 is the honest type. */
    /* 0x70 */ u8 unk70[4];  /* wave 18 (W18-C): a per-slot byte flag indexed by
                              * the SAME `obj->unk1c` that indexes unk44[],
                              * which is what fixes the extent at 4 -- it is the
                              * first bound on anything in this run that is not
                              * "whatever fits below the next member".
                              * sub_08066470 sets its slot to 0xff every frame
                              * and clears it on the last one, so it is a
                              * "this slot is drawing" mark. `adds rB, #0x70`
                              * before the index add is the strb displacement
                              * limit of 31, not an address being taken. */
    /* 0x74 */ struct Unk03001470 *unk74[4];
                             /* wave 30 (W30-C): RETYPED from
                              * `struct Unk08580934_Sub *unk74[1]`. sub_08066580
                              * is the WRITER, and it stores sub_080152EC's
                              * return value -- declared `struct Unk03001470 *`
                              * -- straight into this slot, which fixes the
                              * element type from the producing side rather than
                              * from a partial model of the consumer.
                              *
                              * Byte-neutral for the one existing reader:
                              * src/decomp/c_080665BC.c does
                              * `unk74[i]->unk08 = 0`, and Unk03001470's unk08 is
                              * a u32 at the identical +0x08 that
                              * Unk08580934_Sub modelled -- a producer and a
                              * consumer of the same field agreeing
                              * independently, which is the discriminating use
                              * the type model wants. Re-verified byte-exact.
                              *
                              * Extent 4, not 1, on the same evidence that bounds
                              * unk70: sub_08066580 indexes BOTH with the same
                              * argument, so whatever bounds one bounds the
                              * other. struct Unk08580934_Sub is now unreferenced
                              * and is left in place only so a concurrent editor
                              * does not trip over its removal.
                              *
                              * NOT unified further: sub_08066580 writes +0x28
                              * and +0x2a as two `strh`, while Unk03001470.unk28
                              * is a u32 written whole by five already-promoted
                              * files. That is a genuine word/halfword duality
                              * and wants a union; until one exists, the halfword
                              * writes are spelled `((u16 *)&p->unk28)[0..1]`. */
    /* 0x84 */ u8 unk84;  /* wave 32 (W32-B): seven consecutive bytes that
                           * sub_08065818 fills in one straight run from the
                           * header at +0x00. Every one is a `strb` through
                           * `adds rN, #0x8x` (past strb's 31-byte
                           * displacement), which is the addressing mode and not
                           * seven separate objects. unk84 = (unk00 == 0),
                           * unk85 = unk06, unk86 = unk04 / 500 - 2,
                           * unk87 = unk03 ? unk03 - 4 : 0,
                           * unk88 = unk07 ? unk07 + 1 - unk15 : 0,
                           * unk89 = (unk01 == 0), unk8a = unk02.
                           * Nothing READS them yet, so the widths come from the
                           * stores alone and the signedness is unproved. */
    /* 0x85 */ u8 unk85;
    /* 0x86 */ u8 unk86;
    /* 0x87 */ u8 unk87;
    /* 0x88 */ u8 unk88;
    /* 0x89 */ u8 unk89;
    /* 0x8a */ u8 unk8a;
};

/* 0x0816E1B8 -- a ROM word holding &gUnknown_08580934, i.e. one more level of
 * indirection in front of a global that is itself a pointer. Modelled the same
 * way as gUnknown_08090CD8: a `const` pointer to a one-member wrapper, with the
 * extra deref written out as `->unk00`.
 *
 * It is one slot of a block of such words at 0x0816E180-0x0816E1CC, and the
 * block is what identifies them: SEVEN of those slots hold the identical value
 * 0x08580934 and three more hold 0x03002EE0. A table of distinct globals does
 * not repeat a value seven times at consecutive addresses; one word per
 * (function, symbol) pair does. The same shape sits at 0x08090CC4-0x08090CEC,
 * where ten consecutive slots hold 0x0849B018 and gUnknown_08090CD8 is already
 * declared and matched this way.
 *
 * WHAT those words actually are was settled at 0x0816D9E0 (wave 12): they are
 * agbcc's own .rodata address-constant pool, emitted one word per (function,
 * symbol) reference when -fforce-addr needs a symbol's address to live across a
 * LOOP. The original source names the pointed-to global directly and never
 * mentions the ROM word. So this declaration and the `pp = &sym` idiom are a
 * WORKAROUND that reaches the same bytes with a relocation the harness can
 * check, not a model of the source -- keep it, it matches, but do not read it
 * as evidence that a global lives at 0x0816E1B8. See "The .rodata
 * address-constant reroute" in docs/agbcc-codegen.md; at 0x0805CA60 the
 * workaround falls 8 bytes short and only the direct spelling reaches the ROM.
 *
 * `const` is load-bearing and is what the ROM's shape proves: sub_0806DDF4
 * hoists the load of this word into its loop preheader and re-reads it after
 * the loop. An unqualified global gets neither -- the load stays in the loop
 * body. See "-fforce-addr" in docs/agbcc-codegen.md; the address still has to
 * be taken into a local (`pp = &gUnknown_0816E1B8`) for the reference after
 * the loop, or GCC routes the symbol through a synthesised .rodata word and
 * the pool relocation stops matching.
 */
struct Unk0816E1B8
{
    /* 0x00 */ struct Unk08580934 *unk00;
};

/* 0x085C77A0 -- ROM table, stride 0x5c, at least 0xc0 entries (the bound
 * `cmp r1,#0xbf; bhi` in sub_080206B0). The symbol address is added directly,
 * so this is the array itself and not a pointer to it.
 *   unk28    bit 0 tested by sub_080587FC
 *   unk2c    u32 compared against a caller's value (sub_080206B0 scans it)
 *   unk3c[]  4 bytes, 0xff = empty (sub_0803BD14 counts the leading non-0xff)
 */
/* A gfx/palette pointer pair; sub_08043A80 fetches unk00 and sub_08043A90
 * unk04, and sub_08044AB8 feeds both to sub_08039A5C (Decompress +
 * ApplyPaletteExt).
 */
struct Unk084A06F0 /* 0x08 */
{
    /* 0x00 */ void *unk00;
    /* 0x04 */ void *unk04;
};

/* 0x084C1430 -- ROM pointer to a RAM record, reloaded at every use (so not
 * const). unk28 is a 5-element word array summed into unk24 by sub_08046218.
 * unk58/unk59 are a counter and its delay, stepped by sub_080466A4.
 */
struct Unk084C1430 /* >= 0x5f */
{
    /* 0x00 */ u8 filler_00[0x24];
    /* 0x24 */ u32 unk24;
    /* 0x28 */ u32 unk28[5];
    /* 0x3c */ u32 unk3c;
    /* 0x40 */ u8 filler_40[0x10];
    /* 0x50 */ u8 unk50;
    /* 0x51 */ u8 filler_51[0x02];
    /* 0x53 */ u8 unk53;
    /* 0x54 */ u8 unk54;
    /* 0x55 */ u8 filler_55[0x03];
    /* 0x58 */ u8 unk58;
    /* 0x59 */ u8 unk59;
    /* 0x5a */ u8 filler_5a[0x04];
    /* 0x5e */ u8 unk5e;
};

/* 0x084C3240 -- ROM pointer to a RAM record. unk2c is a frame counter compared
 * against 0xdc by sub_08049E38's neighbour at 0x08049E5C.
 */
struct Unk084C3240 /* >= 0x2f */
{
    /* 0x00 */ u8 filler_00[0x20];
    /* 0x20 */ s16 unk20; /* Wave 36, W36-J. SIGNED, and measured: sub_08049C38
                           * reads it `movs r2,#0x20; ldrsh r1,[r1,r2]` -- the
                           * register-offset ldrsh, which a u16 member cannot
                           * produce. c_08049BEC.c only ever STORES 0x40 into
                           * it, so the sign was invisible there; that file was
                           * re-verified after the retype. */
    /* 0x22 */ u8 filler_22[0x08];
    /* 0x2a */ u8 unk2a; /* wave 32 (W32-C): sub_08049BEC clears it with a
                          * `strb` reached by `adds r1, #0x2a` off the SAME
                          * dereferenced pointer that just took unk20. */
    /* 0x2b */ u8 filler_2b[0x01];
    /* 0x2c */ u16 unk2c;
    /* 0x2e */ u8 unk2e;
};

/* 0x18 bytes of event-script pointers, reached only through
 * gUnknown_085C77A0[chapter].unk04 (wave 28, W28-B).  Six of the six words are
 * in use and every one of them is a `u8 *` handed to sub_08074484 as its first
 * argument, which walks it as the 8-byte-stride record array c_08074570.c
 * already describes.  Each slot is one trigger class: sub_08074460 runs unk00,
 * sub_080742FC unk04, sub_08074320 unk08, sub_080743E8 unk0c, sub_08074410
 * unk10 and sub_0807443C unk14, and each of those six is the same six-
 * instruction null-guard around it. */
struct Unk08074584 /* 0x18 */
{
    /* 0x00 */ u8 *unk00;
    /* 0x04 */ u8 *unk04;
    /* 0x08 */ u8 *unk08;
    /* 0x0c */ u8 *unk0c;
    /* 0x10 */ u8 *unk10;
    /* 0x14 */ u8 *unk14;
};

/* Wave 41 (W41-C). The element type of struct Unk085C77A0's unk44[5], carved
 * out of the old filler_40[0x18]. It MUST be a struct and not the `u8 [5][4]`
 * this started as, and the difference is byte-visible in sub_08026CD0: the ROM
 * builds ONE induction variable holding `4 * i - 4` and reaches the two bytes
 * off it as `adds r0,#0x48` and `adds r0,#0x49`, which is 0x44 + 4i and
 * 0x45 + 4i said through a shared giv. Declared flat, agbcc recomputes
 * `lsls r3, r5, #2` every pass and hoists a second base register holding
 * &unk44 + 1 instead -- same rule as struct Unk03003150 and the map header in
 * src/decomp/c_08024058.c. */
struct Unk085C77A0Slot /* 0x04 */
{
    /* 0x00 */ u8 unk00; /* 0xff means "no preset"; otherwise OR'd into the
                          * army's gUnknown_08499598 unk2d */
    /* 0x01 */ u8 unk01; /* OR'd into the same army's unk2e */
    /* 0x02 */ u8 filler_02[0x02]; /* never read */
};

struct Unk085C77A0 /* 0x5c */
{
    /* 0x00 */ u8 filler_00[0x04];
    /* 0x04 */ const struct Unk08074584 *unk04; /* wave 28, W28-B: sub_08074584
                           * returns this word and its six callers each `ldr`
                           * one of six word offsets off it, so it is a pointer
                           * to a record of pointers rather than a scalar. */
    /* 0x08 */ const u8 *unk08; /* A POINTER, settled in wave 24: sub_0802C7B4
                           * hands this word straight to sub_080193B0, whose
                           * declared parameter is `const u8 *` (the proc-list
                           * script blobs). The old `u32` reading came from
                           * sub_0802C7FC's bare null test, which cannot tell
                           * the two apart; retyping is byte-neutral there and
                           * c_0802C7FC.c was re-verified after the change. The
                           * `adds rB, #8` onto the BASE is the word-member
                           * hoist an array global's `g[i].member` always
                           * produces -- see the displacement-overflow rules --
                           * not evidence of anything else. */
    /* 0x0c */ const u8 *unk0c; /* Wave 45, W45-B. The SECOND script-blob slot,
                           * carved out of the old filler_0c: sub_080364F4 loads
                           * this word off
                           * gUnknown_085C77A0[gUnknown_03003FC0.unk02] with a
                           * whole-word `ldr`, null-tests it, and hands it
                           * straight to sub_080193B0(const u8 *) -- exactly what
                           * unk08 above does at sub_0802C7B4. Same evidence,
                           * same type; the record's unk1a selects between them
                           * (<= 2 runs this one, otherwise sub_080364E0's
                           * gUnknown_0849D10C default runs instead). */
    /* 0x10 */ u8 *unk10; /* Wave 33, W33-B: sub_0803FD80 reads this word off
                           * gUnknown_085C77A0[gUnknown_03003FC0.unk02] as the
                           * default Decompress source (`u8 *`, first argument
                           * of Decompress(u8 *, void *)). Non-const for the
                           * same reason. */
    /* 0x14 */ u16 unk14; /* Wave 29, W29-A. sub_08024944 reads it `ldrh` and
                           * uses it to index gUnknown_08610A38[], so it is a
                           * table row id. Width from the load; signedness
                           * unproved (a subscript is u16-context). */
    /* 0x16 */ u8 unk16; /* wave 24: sub_08078E48 reads it `ldrb` off the
                          * `muls #0x5c` element address and hands it to
                          * sub_0807A99C's first parameter, which is wide -- so
                          * the field's width comes from the `ldrb` here and
                          * nothing narrows it at the callee. */
    /* 0x17 */ u8 unk17; /* Wave 43 (W43-C), carved out of filler_17.
                          * sub_080346FC reads it `ldrb [rB, #0x17]` off
                          * gUnknown_085C77A0[gUnknown_03003FC0.unk02] and, when
                          * it is non-zero, sets gUnknown_03003FC0.unk0d -- a
                          * per-chapter enable flag. Width from the load; the
                          * compare is `beq`, so signedness is unproved. */
    /* 0x18 */ u8 unk18; /* Wave 29, W29-A. sub_0802490C returns it as its own
                          * `u8` result with no re-narrowing, which is the width
                          * evidence; it is the fallback this row supplies for
                          * ids outside the 0xb4..0xbf range that goes to
                          * sub_0803CD14 instead. */
    /* 0x19 */ u8 filler_19[0x01];
    /* 0x1a */ u16 unk1a; /* ldrh, tested against 0 by sub_0802C660 */
    /* 0x1c */ u16 unk1c; /* Wave 49, W49-C, carved out of the old
                           * filler_1c[0x04] -- NAMED, nothing reshaped.
                           * sub_0803E3D8 reads the pair `ldrh [rB,#0x1c]` /
                           * `ldrh [rB,#0x1e]` off the usual `muls #0x5c`
                           * gUnknown_085C77A0[gUnknown_03003FC0.unk02] element
                           * and hands both, unchanged and un-narrowed, to five
                           * of the sub_0803E1xx/E2xx map-decoration spawners as
                           * whole words. Width from the loads; nothing there
                           * discriminates signedness. They are a PAIR the way
                           * unk20/unk22 are: every consumer takes them
                           * adjacently and in this order. */
    /* 0x1e */ u16 unk1e;
    /* 0x20 */ u16 unk20; /* Wave 32, W32-A. A PAIR with unk22: sub_080263A4
                           * reads unk20 `ldrh` off
                           * gUnknown_085C77A0[gUnknown_03003FC0.unk02] and
                           * replaces it with unk22 from the same element when
                           * sub_0803866C() is true, then compares the result
                           * against gUnknown_03004080. Two thresholds for the
                           * same quantity, one per mode. Width from the load;
                           * the compare that follows is `bhi`, i.e. unsigned,
                           * which is consistent with u16 but not a proof of
                           * it -- agbcc knows a zero-extended value is
                           * non-negative and picks the unsigned condition. */
    /* 0x22 */ u16 unk22;
    /* 0x24 */ u16 unk24; /* wave 28, W28-B: sub_08043630 reads it `ldrh` off
                           * gUnknown_085C77A0[gUnknown_03003FC0.unk02] and
                           * returns it when non-zero, falling back to
                           * gUnknown_03003FC0.unk30. Width only. */
    /* 0x26 */ u8 filler_26[0x01];
    /* 0x27 */ u8 unk27; /* Wave 37, W37-K3. sub_08061788 reads it off
                          * gUnknown_085C77A0[gUnknown_03003FC0.unk02] with
                          * `adds rB,#0x27; ldrb` (past ldrb's imm5, so the add
                          * is addressing and not a member array), tests it
                          * against 0 and, when non-zero, uses it as the ROW
                          * index into gUnknown_0857690C[][19] -- i.e. a small
                          * override selector whose fallback comes from
                          * gUnknown_030046B8. Width from the load; the compare
                          * is `beq`, so signedness is unproved. Was the second
                          * byte of filler_26. */
    /* 0x28 */ u8 unk28;
    /* 0x29 */ u8 filler_29[0x03];
    /* 0x2c */ void *unk2c[2]; /* Wave 35, W35-C: an ARRAY of two words, the
                              * same shape (and the same evidence) as unk34
                              * below, which is why the old `u32 unk2c` plus
                              * `filler_30[4]` pair is folded into one member.
                              * sub_080247A4 reads it as
                              * `g[a1].unk2c[sub_0803866C()]` -- a variable
                              * index scaled `lsls #2` added to the `muls #0x5c`
                              * element offset, with the 0x2c hoisted onto the
                              * BASE -- and then falls back to `unk2c[0]` when
                              * the loaded word is NULL, which is what makes it
                              * two slots of the same kind rather than two
                              * unrelated members. A POINTER and not a scalar:
                              * the loaded word is handed straight to
                              * LZ77UnCompWram as the compressed SOURCE. Nothing
                              * in src/decomp read the old `u32` spelling, so
                              * the retype breaks no promoted file. `void *`
                              * until the blob is modelled. */
    /* 0x34 */ void *unk34[2]; /* An ARRAY of two words, not two members:
                              * sub_080196C0 reads it as
                              * `g[gUnknown_03003FC0.unk02].unk34[sub_0803866C()
                              * != 0]`, i.e. a variable index scaled `lsls #2`
                              * added to the `muls #0x5c` element offset, with
                              * the 0x34 hoisted onto the BASE the way an array
                              * global's `g[i].member` always is. A POINTER, and
                              * not merely null-tested: the loaded word is the
                              * argument of the `sub_080196F4(...)` guarded by
                              * that test, and sub_080196F4 dereferences it
                              * (`adds r5, r0, #0; ldrb r2, [r5]`). `void *`
                              * until that object is modelled. Wave 26. */
    /* 0x3c */ u8 unk3c[4]; /* 0xff means "empty". The extent 4 is proved:
                             * sub_0803BD14 scans for the first entry that is
                             * not 0xff with a literal `i < 4` bound and
                             * returns the index, i.e. a count of leading
                             * occupied slots. */
    /* 0x40 */ u8 unk40[4]; /* Wave 41 (W41-C), carved out of the front of the
                             * old filler_40[0x18]; 0x44..0x57 stay unnamed.
                             * The PAIRED array to unk3c, read with the SAME
                             * `[slot - 1]` subscript in the same expression:
                             * sub_08026AC0 takes unk3c[slot - 1] and, when it
                             * is not 0xff, uses unk40[slot - 1] as the value
                             * (falling back to its own second argument when it
                             * is). Both reached as `base + 0x3c` / `base +
                             * 0x40` plus the one shared index register, which
                             * is what shows they are two arrays and not one
                             * eight-byte record. Extent 4 is inherited from
                             * unk3c rather than proved here -- sub_08026AC0
                             * only ever subscripts it 0..3 because its callers
                             * pass slot 1..4. */
    /* 0x44 */ struct Unk085C77A0Slot unk44;
    /* 0x48 */ struct Unk085C77A0Slot unk48[4];
                            /* Wave 41 (W41-C), the rest of the old
                             * filler_40[0x18]; it reaches exactly to unk58 with
                             * nothing left over. One record per army slot 0..4.
                             * sub_08026CD0 walks the five slots and, when unk00
                             * is not 0xff, ORs unk00 into that army's
                             * gUnknown_08499598 unk2d and unk01 into its unk2e
                             * -- the unk2d/unk2e pair, so the two bytes are the
                             * preset halves of one flag word.
                             *   The 4-byte stride is read off the induction
                             * variable, which advances by 4 while the army
                             * offset advances by 0x3c. See the tag's own
                             * comment for why it has to be a struct.
                             *
                             * WAVE 45 (W45-D): SPLIT out of the wave-41
                             * `unk44[5]`, same 0x44..0x57 extent, nothing moves.
                             * This is what closed sub_08026CD0, parked
                             * size-exact since wave 41. The array base has to be
                             * 0x48 and the subscript has to run -1..3, and the
                             * ROM says so directly: it reaches both bytes as
                             * `add r0, r0, #0x48` / `#0x49` on a RUNTIME base
                             * (r8 = the plain symbol, added last) with the
                             * induction variable initialised to -4.
                             *   A constant of 0x48 surviving as a runtime `add`
                             * is only ever a MEMBER offset. Every spelling that
                             * reaches element 1 through the 0x44 base instead --
                             * `unk44[k + 1]`, `(&unk44[1])[k]` -- has fold
                             * reassociate the +4 back into the member constant
                             * and emits `#0x44` with the giv at 0, and pushing
                             * the 0x48 in through a pointer cast moves it into
                             * the POOL WORD as an addend (`gUnknown_085C77A0+0x48`)
                             * and reorders the base add ahead of the giv. Only a
                             * declared member at 0x48 produces all three at once.
                             *   So slot 0 is a separate object at 0x44 that the
                             * loop reaches as unk48[-1]. Type is unchanged and
                             * this member had exactly one consumer -- struct
                             * Unk085C77A0Slot appears nowhere else in src/ --
                             * so the split cannot regress a matched function. */
    /* 0x58 */ u8 unk58; /* Wave 29, W29-A. sub_08024984 reads it `ldrb` off
                          * gUnknown_085C77A0[gUnknown_03003FC0.unk02] and
                          * RETURNS it when it is non-zero, so it is a value and
                          * not only a flag. Reached as `adds rB,#0x58` on the
                          * element address, which is addressing (past `ldrb`'s
                          * 5-bit displacement), not a member array. */
    /* 0x59 */ u8 filler_59[0x03];
};

/* Stride 0x104; sub_08042E18 reads unk16. */
/* The 0x44-byte sub-record at +0x38 of struct CoData. Stride proved by
 * `lsls #4; adds` (x17) sharing the outer `lsls #2` with the x65 outer index in
 * sub_08042E2C, sub_08042E84, sub_08042EDC, sub_08042FC4 and sub_0804301C --
 * i.e. base + a*0x104 + b*0x44 + K, with K running 0x40, 0x46, 0x48, 0x4a,
 * 0x4e.
 *
 * The array base was +0x40 until wave 9. sub_08039F18 reaches this record with
 * the same a*0x104 + b*0x44 index math and K = 0x38, which +0x40 cannot express
 * (it would be member offset -8), so the array starts at +0x38 and every member
 * above moved up by 8. The new base also makes the record array cover the
 * struct exactly: 0x38 + 3 * 0x44 == 0x104, so the extent is 3 and the old
 * `filler_c8[0x3c]` tail is gone -- that tail was itself an artefact of the
 * wrong base. Anything citing the old numbering wants +8 added to it. */
struct CoModeData /* 0x44 */
{
    /* 0x00 */ u32 unk00; /* indexes gUnknown_08610A38 (sub_08039F18) */
    /* 0x04 */ void (*unk04)(void *); /* wave 32 (W32-C): sub_08044B28 loads it
                                       * and calls it through `bl _call_via_r1`
                                       * with its own ProcPtr argument in r0 --
                                       * r1 holds the pointer, so exactly one
                                       * argument. */
    /* 0x08 */ u32 unk08;
    /* 0x0c */ s16 unk0c; /* `ldrsh`, sub_080432A8 (K = 0x44 = 0x38 + 0x0c) */
    /* 0x0e */ s16 unk0e;
    /* 0x10 */ s16 unk10;
    /* 0x12 */ s16 unk12;
    /* 0x14 */ s16 unk14; /* `ldrsh`, sub_08043270 (K = 0x4c = 0x38 + 0x14) */
    /* 0x16 */ s16 unk16;
    /* 0x18 */ s8 *unk18[3]; /* Wave 34, W34-F. A pointer to a [movement type][32
                           * terrain] table of SIGNED cost bytes: sub_08041EA8
                           * reads `unk18[(cell & 0x1f) + gUnknown_085D5ABC[t]
                           * .unk19 * 32]` with `movs r0,#0; ldrsb r0,[r3,r0]`
                           * and rejects the cell when it is negative, so the
                           * pointee is s8 and the row stride is 32. Reached as
                           * the constant offset 0x154 == 1 * 0x104 + 0x38 +
                           * 0x18, i.e. gUnknown_085D3DD0[1].unk38[0].unk18.
                           *   WAVE 37 (W37-H): widened from a scalar `s8 *` to
                           * `s8 *[3]`, absorbing the old filler_1c[0x08] --
                           * 0x18 + 3*4 == 0x24 fills the gap up to unk24
                           * exactly. sub_08038848 selects between the three
                           * with gUnknown_03003FC0.unk2c (`(unk1e * 17 +
                           * unk2c) << 2` off the +0x50 member base, i.e. a
                           * 4-byte element stride inside a 0x44 entry stride).
                           * The start offset is unchanged, so this is
                           * byte-neutral; src/decomp/c_08041EA8.c was updated
                           * to `.unk18[0]` and re-verified byte-exact. */
    /* 0x24 */ s16 *unk24[8]; /* An array of POINTERS, not values: family F049
                               * (sub_080430B0/08043120/08043190/08043200)
                               * loads one word out of it and then reads an s16
                               * THROUGH it (`ldr r7,[r3]; movs r2,#K;
                               * ldrsh r0,[r7,r2]` with K = 0, 2, 4, 6).
                               * The extent 8 is proved the same way the +0x38
                               * re-base was: 0x24 + 8*4 == 0x44 fills the
                               * record exactly, and the two indices in use
                               * span it -- gUnknown_085D5ABC[c].unk18 and
                               * sub_080432E0(c), which returns 5, 6 or 7.
                               * The pointee is modelled as bare `s16 *` and
                               * indexed p[0..3]; a pointer to a four-s16
                               * struct is byte-identical and NOT ruled out. */
};

struct CoData /* 0x104 */
{
    /* 0x00 */ u32 unk00; /* Wave 36, W36-K: carved out of filler_00[0x04]. A
                           * subscript into gUnknown_08610A38[] (the same role
                           * unk38[..].unk00 plays at +0x38): sub_080686E8 and
                           * sub_08068810 both do `ldr` at the row base and then
                           * `lsls #2; adds` off gUnknown_08610A38 to reach the
                           * graphics blob they hand sub_08068038. Whole word,
                           * so not four filler bytes. Start offset unchanged,
                           * so the carve is byte-neutral for every other
                           * reader. */
    /* 0x04 */ u16 unk04;
    /* 0x06 */ s16 unk06; /* both `ldrsh`, sub_08042F7C and sub_08042F34 */
    /* 0x08 */ s16 unk08;
    /* 0x0a */ u8 filler_0a[0x02];
    /* 0x0c */ int unk0c; /* wave 25: two per-terrain WORD multipliers, read
                           * `ldr` off `adds r3,#0xc` / `adds r3,#0x10` on the
                           * bare table symbol with the x0x104 outer index, by
                           * sub_080441D4 and sub_08044208 respectively. Each
                           * multiplies sub_0804419C's percentage, so they are
                           * the two income/repair rates for the terrain named
                           * by gUnknown_08499598[army].unk1d. Signedness is
                           * unproved -- `muls` is byte-neutral -- so `int` is
                           * the weakest model that fits. */
    /* 0x10 */ int unk10;
    /* 0x14 */ u8 unk14; /* Wave 34 (W34-H): sub_08039948 switches on it with a
                          * bare `ldrb r1,[r0,#0x14]` over cases 0..3, each
                          * arm picking one of four (unk2c, unk30) offset pairs
                          * -- so it is a small enumerated direction/side tag. */
    /* 0x15 */ u8 unk15;
    /* 0x16 */ u8 unk16;
    /* 0x17 */ u8 unk17; /* Wave 37, W37-K2. sub_0805DB70 reads it with a
                          * plain `ldrb` off the 0x104 element and multiplies
                          * sub_08044208's result minus sub_080441D4's by it
                          * before `Div(.., 100)` -- a per-terrain percentage,
                          * the companion of unk0c/unk10 above. */
    /* 0x18 */ u8 unk18; /* Wave 37, W37-K2. Compared UNSIGNED (`cmp; bhs`)
                          * against `sub_080129E0() % 100` in the same
                          * function -- a percentage chance. Plain `ldrb`. */
    /* 0x19 */ u8 filler_19[0x03];
    /* 0x1c */ u8 (*unk1c)(int); /* Wave 37, W37-K2. A whole-word function
                          * pointer: sub_0805DB70 loads it, tests it against
                          * NULL and calls it through `bl _call_via_r1` with
                          * ONE argument (2 on one path, 1 on the other), then
                          * narrows the result with `lsls #0x18; lsrs #0x18`
                          * before `cmp #1` -- so u8 in, one int argument. The
                          * register index of the veneer is what fixes the
                          * arity. Carved out of filler_17, offsets
                          * unchanged. */
    /* 0x20 */ u16 unk20[0x06]; /* Wave 30, W30-A: sub_080398D0 picks one of
                                 * SIX with `sub_080129E0() % 6` and hands it
                                 * to sub_080397F4(u16). The `adds r4,#0x20` on
                                 * the bare table symbol -- kept in r4 across
                                 * the __umodsi3 call -- is the member-array
                                 * hoist, the same idiom sub_08039F18 uses for
                                 * unk38. The extent is proved by the modulus,
                                 * not by the layout. */
    /* 0x2c */ u8 filler_2c[0x0c];
    /* 0x38 */ struct CoModeData unk38[3];
};

struct UnitTypeData /* 0x5c */
{
    /* 0x00 */ u8 filler_00[0x02];
    /* 0x02 */ u16 unk02; /* Wave 35, W35-E: a pair with unk04, and three
                           * readers agree they are `ldrh` at +2 / +4 off the
                           * `unk00 * 0x5c` stride. sub_0803A2BC hands each to
                           * a separate sub_08014A5C (sprite ids 7 and 0xd),
                           * and sub_0803A5B8 subscripts gUnknown_08610A38[]
                           * with them, so they are a two-entry graphic id
                           * pair. */
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 unk08; /* Wave 38 (W38-J). Was filler; NAMED, not reshaped --
                           * same offset, same two bytes. sub_08029978 reads it
                           * `ldrh rN,[rM,#8]` off the usual `unk00 * 0x5c`
                           * stride and multiplies it by a per-step count whose
                           * product is then truncated to u16, so it is the
                           * per-step magnitude that goes with the unk0b cap two
                           * fields down. Width from the `ldrh`; signedness
                           * unproved, nothing sign-extends it. */
    /* 0x0a */ u8 unk0a;
    /* 0x0b */ u8 unk0b; /* wave 15 (C): a CAP, compared `bls` against a sum of
                          * two 4-bit fields of struct UnitRecord's unk04
                          * container (sub_08022618, sub_08042998), and the
                          * clamp value when the sum exceeds it. Same role as
                          * unk10 one field over, which caps the 5-bit unk06
                          * pair the same way. Plain `ldrb`. */
    /* 0x0c */ u8 unk0c;
    /* 0x0d */ u8 filler_0d[0x01];
    /* 0x0e */ u8 unk0e; /* a 3-valued mode tag: sub_080432E0 maps 0 -> 7,
                          * 1 -> 5 and everything else -> 6, which its callers
                          * use as a column index into gUnknown_085D3E2C. */
    /* 0x0f */ u8 unk0f;
    /* 0x10 */ u8 unk10; /* sub_08061E54 compares `unk10 - 5` against a 7-bit
                          * field of its pointer parameter; plain `ldrb`. */
    /* 0x11 */ u8 unk11; /* Wave 30, W30-A: sub_0805CA24 ORs it with unk0e and
                          * tests the pair against zero, so it is a flag byte
                          * beside that one rather than filler. Plain `ldrb`;
                          * signedness unproved. */
    /* 0x12 */ u8 filler_12[0x02];
    /* 0x14 */ u8 *unk14; /* Wave 34 (W34-D): a POINTER to a byte blob, and
                           * three readers agree independently. sub_0802706C
                           * only tests it `!= 0` (`ldr` at +0x14, whole
                           * word), but sub_08025EF0 and sub_08025F74 both
                           * deref it -- `ldrb [p]` for a 1/2 mode tag and
                           * `ldrb [p + 1 + n]` for a per-n permission byte,
                           * so the target really is `u8 []` and the word is
                           * an address rather than an int. Carved out of
                           * filler_12, which used to span 0x12..0x17. */
    /* 0x18 */ u8 unk18; /* plain `ldrb`, used by family F049 as an index into
                          * struct CoModeData.unk24[] and separately
                          * tested against 0. Signedness unproved: both uses
                          * are u8-context (a subscript and a `cmp #0`), so a
                          * signed object would be byte-identical here. */
    /* 0x19 */ u8 unk19; /* Wave 34, W34-F. The unit's MOVEMENT TYPE: sub_08041EA8
                          * uses it as `unk19 * 32`, a row index into the s8
                          * terrain-cost table at
                          * gUnknown_085D3DD0[1].unk38[0].unk18, whose column is
                          * the 5-bit terrain code. Plain `ldrb`, so unsigned by
                          * the load; nothing sign-extends it. */
    /* 0x1a */ u8 unk1a; /* Wave 29, W29-A. sub_080249EC compares it `ldrb`
                          * against 0x10 and returns 0 when it matches, so it is
                          * a small tag rather than a flag. Width from the load;
                          * the compare is `beq`, which says nothing about
                          * signedness. */
    /* 0x1b */ u8 unk1b; /* a small class tag, 1..6: the 0x0805Cxxx list
                          * builders each keep the units whose type's unk1b
                          * equals one fixed constant (`ldrb [.,#0x1b];
                          * cmp #N`). Wave 19 closed the last three builders
                          * and widens the observed range from 1..5 to 1..6 --
                          * the twelve that use this test span
                          * {1,1,2,2,2,3,4,4,5,6}, so 6 is real and the range
                          * is still a floor, not an extent. */
    /* 0x1c */ u8 unk1c; /* Wave 50, W50-A.  NAMED, not reshaped -- same offset,
                          * same single byte.  The SIBLING of unk1d below and the
                          * same kind of thing: sub_08062560 reads it `ldrb
                          * [.,#0x1c]` off the `unk00 * 0x5c` stride at two sites,
                          * masks the caller's u8 argument with it as a gate
                          * (`ands; cmp #0`), and then ORs it into the
                          * gUnknown_08499590 +0x3262 plane over every passable
                          * cell -- exactly unk1d's documented role, one byte
                          * earlier.  Width from the `ldrb`; it is a mask, so
                          * unsigned. */
    /* 0x1d */ u8 unk1d; /* Wave 36, W36-D. A BIT MASK over the +0x3262 plane of
                          * struct Map: sub_08058BB4 and sub_08058C54 both read
                          * it once (`ldrb [.,#0x1d]` off the `unk00 * 0x5c`
                          * stride) and pass it straight to sub_08058CE8, whose
                          * only use is `plane_byte & mask` tested against zero.
                          * Plain `ldrb`, so unsigned by the load; carved out of
                          * filler_1c, which used to span 0x1c..0x1d. */
    /* 0x1e */ u8 unk1e[2][0x1a];
    /* 0x52 */ u8 filler_52[0x02];
    /* 0x54 */ s8 *unk54; /* Wave 49, W49-J. Carved out of filler_52[0x06] --
                           * same start offset, and nothing in src/decomp names
                           * filler_52, so byte-neutral for every other reader.
                           * gen_lds.py calls this word gUnknown_085D5B10, which
                           * is exactly &gUnknown_085D5ABC[0] + 0x54; the
                           * x0x5c stride of the users (sub_08046914 and
                           * sub_08046A84, both `muls #0x5c` on a unit-type id
                           * out of gUnknown_084C20C0[]) is what ties it to this
                           * record rather than to a table of its own. A pointer
                           * to SIGNED bytes indexed by a small id and tested
                           * `!= 0`: the load is `ldrb` + `lsls #0x18; asrs
                           * #0x18`, which is the same spelling agbcc gives the
                           * proven-`s8 *` CoModeData.unk18 in
                           * sub_08046778, so the shifts are NOT evidence of an
                           * unsigned pointee. The pointee's meaning is
                           * unproved. */
    /* 0x58 */ const u8 *unk58; /* Wave 34 (W34-D): a POINTER to a per-terrain
                                 * cost table. sub_080253B0 loads the whole word
                                 * (`adds rB, #0x58; ldr`) and immediately indexes
                                 * it by the LOW FIVE BITS of the gUnknown_08499590
                                 * +0x1432 terrain byte, so the table is at least
                                 * 0x20 bytes and lives in the same 0..0x1f terrain
                                 * space gUnknown_08551CA0 scores. The byte it
                                 * yields is a movement cost: sub_080253B0
                                 * subtracts it from the unit's 7-bit unk06_0
                                 * fuel/move field. Carved out of filler_52, which
                                 * used to span 0x52..0x5b. */
};

/* Stride 0x30, proved by `lsls #1; adds rI,rI,r0; lsls #4` (x*3*16) in
 * sub_0807821C. 16 users across 0x08074xxx-0x08077xxx; only unk02 is named so
 * far, a flag byte whose bit 4 sub_0807821C reports as a bool. */
struct Unk08615194 /* 0x30 */
{
    /* 0x00 */ u16 unk00; /* wave 26: sub_08077F30 reads it with `ldrh` and
                           * stores it into the u8 gUnknown_03003FC0.unk02, so
                           * the slot really is a halfword and only its low byte
                           * survives. It was filler until this wave. */
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 filler_03[0x03];
    /* 0x06 */ s16 unk06; /* wave 28, W28-B: sub_080749FC reads unk06 and unk08
                           * with register-offset `ldrsh` and hands both to
                           * sub_08074C84's two `s32` parameters -- an (x, y)
                           * pair, and signed on the load's own evidence. */
    /* 0x08 */ s16 unk08;
    /* 0x0a */ u8 filler_0a[0x02];
    /* 0x0c */ const void *unk0c; /* wave 38 (W38-E): a nullable pointer to a
                             * 4-byte header plus a tile-id blob -- u8 x, u8 y,
                             * u8 width, u8 height, then a `const u8 *` at +0x04
                             * pointing at width*height palette-bank ids.
                             * sub_08075904 and sub_080759A0 both `ldr` it,
                             * null-check it (returning 0) and stamp the ids into
                             * the top nibble of gUnknown_08614280's tilemap. The
                             * WORD WIDTH and the null check are what is proved;
                             * the pointee is spelled locally in those two files
                             * for the same reason unk24/unk28 carry `void *`.
                             * Carved out of filler_0a -- every later offset is
                             * unchanged. */
    /* 0x10 */ u8 filler_10[0x04];
    /* 0x14 */ void *unk14; /* wave 35 (W35-B): sub_08077EDC `ldr`s it, null-
                             * checks it and hands it to sub_08078540's `void *`
                             * first parameter -- the same role and the same
                             * word-wide-pointer-with-unknown-pointee reading as
                             * unk18/unk1c below. Was filler. */
    /* 0x18 */ void *unk18; /* wave 30 (W30-C): a SECOND pair of word slots with
                             * exactly the unk24/unk28 shape one record along --
                             * sub_08076C1C picks between them on
                             * gUnknown_0202FDFC.unk11, null-checks the result
                             * and hands it to sub_08078540's `void *` first
                             * parameter, which is the proc-script/argument slot
                             * that callee stores at +0x54. Word width is proved
                             * by the `ldr`; the pointee is not, so `void *` for
                             * the same reason unk24/unk28 carry it. */
    /* 0x1c */ void *unk1c;
    /* 0x20 */ u8 filler_20[0x04];
    /* 0x24 */ void *unk24; /* wave 26: a pair of word slots sub_08077F30
                             * selects between with sub_0803866C() and publishes
                             * as gUnknown_030046B4. Both are plain `ldr`s and
                             * nothing in the ROM yet dereferences the result,
                             * so `void *` is a guess at the pointee -- the word
                             * width is what is proved. */
    /* 0x28 */ void *unk28;
    /* 0x2c */ u8 (*unk2c)(void); /* wave 35 (W35-B): a FUNCTION POINTER, and
                             * sub_08077EDC is decisive about both halves of the
                             * type. It loads the word, tests it against NULL and
                             * then calls it through `bl _call_via_r1` with no
                             * argument register set up -- r1 is simply where the
                             * value already sat, so the register index is not an
                             * arity readout here. The result is truth-tested
                             * after `lsls #0x18`, i.e. only the low byte
                             * survives, which is what makes the return `u8`
                             * rather than `int`. It gates the same
                             * sub_08078540(unk14, proc) call the null branch
                             * makes unconditionally, so it reads as a
                             * "should this entry run?" predicate. Was filler. */
};

/* ------------------------------------------------------- m4a / MP2K -- */

/* The Nintendo MusicPlayer2000 sound driver, 0x0806F734-0x080718E4. Its ARM
 * half and the ply_* command handlers are hand-written (see
 * data/asm-resident.json); the THUMB entry points around 0x080703B8-0x08070660
 * are ordinary compiler output and correspond to m4a.c in the public GBA
 * decomps. These two layouts are the canonical MP2K ones, and this ROM does not
 * merely fit them -- it PINS them, at eleven independent offsets:
 *
 *   MusicPlayerInfo
 *     +0x04 status   sub_080703B8 clears bit 31 with a 0x7FFFFFFF mask, i.e.
 *                    MUSICPLAYER_STATUS_PAUSE
 *     +0x24 fadeOI   sub_080703D4 and sub_08070620 write all three together
 *     +0x26 fadeOC   with 0x100 and 0x101 -- (64 << FADE_VOL_SHIFT) and the
 *     +0x28 fadeOV   same OR'd with TEMPORARY_FADE
 *     +0x34 ident    compared against 0x68736D53 ("Smsh") by every entry point,
 *                    and by the hand-written sub_0806F744 on SOUND_INFO_PTR
 *
 *   MusicPlayerTrack -- the nine ply_* handlers in asm-resident.json write
 *     exactly the byte offsets 0x1e, 0x1f, 0x24, 0x26, 0x27, 0x2c, 0x2d, 0x2e,
 *     0x2f. Those are echoVolume, echoLength and then tone.type, tone.length,
 *     tone.pan_sweep, tone.attack, tone.decay, tone.sustain, tone.release with
 *     a 12-byte struct ToneData at +0x24. Nine offsets landing on nine named
 *     fields is not a coincidence; the stride and the ToneData placement are
 *     settled. sub_0806FC50 independently gives patternLevel at +0x02,
 *     cmdPtr at +0x40 and patternStack[] at +0x44 (it is ply_pend), and
 *     sub_0806FC34 (ply_patt) repeats all three. sub_080717F8 (ply_xwave) adds
 *     the one word member of the block: it assembles four unaligned command
 *     bytes into a u32 and stores it at +0x28, which is tone.wav at +0x04 of a
 *     ToneData based at +0x24 -- a fourth, independent confirmation of that
 *     base, and the only one that lands on a non-byte field.
 *
 * Wave 9 added the tempo block and the modulation block, and the second of them
 * says this ROM's MusicPlayerTrack is NOT byte-for-byte the pokeemerald one:
 *
 *   MusicPlayerInfo -- sub_080713F8 (MPlayTempoControl) is
 *     `tempoU = tempo; tempoI = (tempoD * tempoU) >> 8`, which names three
 *     consecutive halfwords at once and fixes them against the public layout:
 *     +0x1c tempoD (the multiplier, read), +0x1e tempoU (written from the
 *     parameter), +0x20 tempoI (the product). tempoC at +0x22 follows from the
 *     public layout and from fadeOI already being pinned at +0x24; it is the
 *     only unproved member of the block, so it stays `filler`.
 *
 *   MusicPlayerTrack -- the whole modulation/LFO block is now named, and it is
 *     the CANONICAL MP2K one with nothing inserted or moved. Read the tell
 *     first, because it is easy to talk yourself into a shifted layout here:
 *     the block runs modM, mod, modT, and a public layout quoted from memory
 *     that omits `mod` makes every field from +0x17 up look like it is +1.
 *     It is not. The proof is one basic block, the LFO update inside the
 *     hand-written MPlayMain at 0x0806FEF8-0x0806FF54, which names six fields
 *     in twenty instructions and leaves no room to slide any of them:
 *
 *       if (track->lfoSpeed && track->mod)          ldrb +0x19 ; ldrb +0x17
 *         if (track->lfoDelayC) track->lfoDelayC--; ldrb/strb  +0x1c
 *         else {
 *           track->lfoSpeedC += track->lfoSpeed;    ldrb/strb  +0x1a, += +0x19
 *           v = (lfoSpeedC >= 0x40) ? 0x80 - lfoSpeedC : (s8)lfoSpeedC;
 *           v = v * track->mod >> 6;                ldrb +0x17 ; muls ; asrs #6
 *           if (v != track->modM) {                 ldrb +0x16 ; eors ; lsls #24
 *             track->modM = v;                      strb +0x16
 *             ... test track->modT, OR the flags    ldrb +0x18 ; ldrb +0x00
 *
 *     +0x17 is therefore `mod`, the modulation depth: read twice, multiplied
 *     into the LFO value, never written here. +0x16 is `modM`, the applied
 *     depth. That tail is ClearModM inlined, which is what sub_08071564 is.
 *
 *     Three more offsets from the hand-written half, all consistent with it:
 *       +0x19 lfoSpeed   sub_08070328 (ply_lfos) stores its argument there
 *       +0x1b lfoDelay   sub_080700C0 (ply_note) does
 *       +0x1c lfoDelayC  `ldrb [r5,#0x1b]; strb [r5,#0x1c]`, then calls
 *                        ClearModM when the value is non-zero
 *       +0x1d priority   sub_080700C0 adds it to MusicPlayerInfo +0x09 and
 *                        clamps the sum to 0xff, which also pins that +0x09
 *
 *     So the public MusicPlayerTrack IS safe to lift here. Note the general
 *     point: the hand-written half will never match, but it is first-class
 *     evidence about the layout, and it is where most of these came from.
 *
 *
 * Fields marked `filler` are the canonical names carried over from the public
 * layout but unproved HERE; narrow one when a function needs it, do not move
 * anything.
 */
#define MPLAY_ID_NUMBER 0x68736D53 /* 'Smsh' */
#define MUSICPLAYER_STATUS_PAUSE 0x80000000
#define FADE_VOL_SHIFT 2
#define TEMPORARY_FADE 0x0001
#define FADE_IN 0x0002 /* sub_08070640 stores a bare 2 into fadeOV */

/* MusicPlayerTrack::flags. Only these two are reached from matched code:
 * sub_08071564 ORs 0x0c when modT is zero and 0x03 otherwise, which is
 * PITCHG/VOLCHG in the public layout and the only place either value occurs. */
#define MPT_FLG_VOLCHG 0x03
#define MPT_FLG_PITCHG 0x0C
/* Wave 38 (W38-A): all five MPlay*Control entry points at 0x08071420..
 * 0x080715F8 gate every per-track edit on `track->flags & 0x80`, hoisting the
 * 0x80 into a high register outside the loop. That is MPT_FLG_EXIST in the
 * public layout and 0x80 occurs nowhere else against this member. */
#define MPT_FLG_EXIST 0x80
/* Wave 47 (W47-B): sub_08070BAC (MPlayStart) both TESTS this bit on track 0 in
 * its entry guard -- "the running song is still playing" -- and stores it with
 * MPT_FLG_EXIST as 0xc0 on every track it starts. An install/test pair on one
 * bit, which is the discriminating evidence a single store never gives.
 * MPT_FLG_START in the public layout. (work/sub_08070BAC/sub_08070BAC.c still
 * spells it 0x40 -- that draft is parked on register allocation and is left
 * byte-for-byte as measured.) */
#define MPT_FLG_START 0x40

struct ToneData /* 0x0c */
{
    /* 0x00 */ u8 type;
    /* 0x01 */ u8 key;
    /* 0x02 */ u8 length;
    /* 0x03 */ u8 pan_sweep;
    /* 0x04 */ u32 wav;
    /* 0x08 */ u8 attack;
    /* 0x09 */ u8 decay;
    /* 0x0a */ u8 sustain;
    /* 0x0b */ u8 release;
};

/* The MP2K hardware/software mixer channel -- MusicPlayerTrack::chan points at
 * one. Four offsets are pinned by sub_08070090 (ChnVolSet, hand-written and in
 * asm-resident.json; the hand-written half is still first-class evidence about
 * layout). It takes the channel in r4 and the track in r5 and computes the
 * stereo pair, which fixes rightVolume/leftVolume at +0x02/+0x03 and reads
 * `ve` at +0x12 and `rp` at +0x14. `rp` is SIGNED and that is not lifted --
 * the ROM reads it `movs r0,#0x14; ldrsb r2,[r4,r0]`, and it is used as both
 * `0x80 + rp` and `0x7f - rp`. All four land exactly on the canonical MP2K
 * SoundChannel, so the rest of that layout is safe to narrow into as functions
 * reach it; nothing between +0x04 and +0x11 is reached yet.
 */
struct SoundChannel /* 0x40 */
{
    /* 0x00 */ u8 status;          /* wave 47 (W47-B): sub_08070A28 (SoundClear)
                                    * walks twelve of these from SoundInfo+0x50
                                    * at stride 0x40 and zeroes the first byte
                                    * of each. */
    /* 0x01 */ u8 type;            /* wave 47 (W47-A): sub_0807004C (TrackStop)
                                    * reads it `ldrb [r4,#1]`, masks with 7 and
                                    * calls SoundInfo::CgbOscOff on the result
                                    * when it is non-zero -- the canonical MP2K
                                    * "low three bits are the CGB channel 1..4"
                                    * test, and the only reader of this byte. */
    /* 0x02 */ u8 rightVolume;
    /* 0x03 */ u8 leftVolume;
    /* 0x04 */ u8 filler_04[0x0d]; /* attack decay sustain release ky
                                    * envelopeVolume envelopeVolumeRight
                                    * envelopeVolumeLeft echoVolume echoLength
                                    * d1 d2 gt */
    /* 0x11 */ u8 mk;              /* wave 47 (W47-A): sub_080702C0 (ply_endtie)
                                    * compares it against the track's key at
                                    * MusicPlayerTrack+0x05 to find the channel
                                    * playing that note. Both sides are bare
                                    * `ldrb`, so u8 is a floor as well as the
                                    * canonical MP2K width. */
    /* 0x12 */ u8 ve;
    /* 0x13 */ u8 filler_13[0x01]; /* pr */
    /* 0x14 */ s8 rp;
    /* 0x15 */ u8 filler_15[0x17]; /* wave 47 (W47-B): the 0x40 total size is
                                    * PROVED, not lifted. SoundInfo's channel
                                    * array starts at +0x50, sub_08070A28 walks
                                    * exactly twelve of them at stride 0x40, and
                                    * 0x50 + 12*0x40 == 0x350 -- which W35-D had
                                    * already pinned as pcmBuffer.
                                    * d3[3] ct fw freq wav cp */
    /* 0x2c */ void *track;        /* wave 47 (W47-A): sub_0807004C stores a null
                                    * pointer here for every channel it detaches.
                                    * Only ever written, never read, so the
                                    * pointee type is NOT proved -- `void *` is
                                    * the weakest that fits, the same choice
                                    * MusicPlayerTrack::chan makes in reverse. */
    /* 0x30 */ u8 filler_30[0x04]; /* pp */
    /* 0x34 */ struct SoundChannel *np;
                                   /* wave 47 (W47-A): the chain link. Both
                                    * sub_0807004C and sub_080702C0 walk the
                                    * channel list `chan = chan->np` with
                                    * `cmp #0` as the terminator. */
    /* 0x38 */ u8 filler_38[0x08]; /* d4 xpi xpc */
};

/* The MP2K CGB oscillator channel. sub_08070A28 (SoundClear) walks four of
 * these from SoundInfo::cgbChans at stride 0x40 -- indices 1..4 inclusive --
 * zeroing the first byte of each, and sub_080703F4 (m4aSoundInit) hands
 * gUnknown_030057D0 to sub_080706B0 (MPlayExtender) as the array base. Only the
 * stride and +0x00 are proved here; the canonical MP2K CgbChannel is 0x40 bytes
 * with `sf` (statusFlags) at +0x00, which is what the zeroing store hits. */
/* WAVE 47 (W47-C): eight bytes carved out of the old filler_01[0x3f] from
 * sub_08070F44 (CgbModVol), which reads or writes every one of them. `sf` at
 * +0x00 is untouched and no existing member changes width.
 *
 * rightVolume and leftVolume are `volatile`, and that is PROVED rather than
 * lifted -- it is the answer to residual 1 of the wave-32 park, which wave 35
 * left open after ruling out u8 locals and `u8 x : 8` bitfields.
 * sub_08070F44 reads each of them as `ldrb; lsls #0x18; lsrs #0x18` and then
 * takes the halving straight out of the `<<24` value (`lsrs r0, r2, #0x19`).
 * A plain `u8` member cannot produce that round trip: `ldrb` already
 * zero-extends, so combine deletes the pair and emits a bare `lsr #1` for the
 * halving. With the qualifier agbcc must load the byte into a QImode pseudo
 * (one read, exactly as the source asks) and zero-extend it afterwards, which
 * is the shift pair, and CSE then shares the `<<24` between the value and its
 * halving. Reproduced by compile_probe, instruction for instruction.
 * The same qualifier explains why the function RE-READS both bytes for the
 * `(left + right) >> 4` sum instead of reusing the two values it already has.
 *
 * The other six are plain. envelopeGoal is written and then re-read for the
 * multiply, but that is one non-volatile store/load pair CSE keeps in a
 * register -- it is not evidence for a qualifier. */
struct CgbChannel /* 0x40 */
{
    /* 0x00 */ u8 sf;
    /* 0x01 */ u8 filler_01[0x01];
    /* 0x02 */ volatile u8 rightVolume;
    /* 0x03 */ volatile u8 leftVolume;
    /* 0x04 */ u8 filler_04[0x02];
    /* 0x06 */ u8 unk06;
    /* 0x07 */ u8 filler_07[0x03];
    /* 0x0a */ u8 envelopeGoal;
    /* 0x0b */ u8 filler_0b[0x0e];
    /* 0x19 */ u8 unk19;
    /* 0x1a */ u8 filler_1a[0x01];
    /* 0x1b */ u8 pan;
    /* 0x1c */ u8 panMask;
    /* 0x1d */ u8 filler_1d[0x23];
};

/* The canonical MP2K WaveData. Only +0x04 is reached (sub_08070350 /
 * MidiKey2freq reads it as the sample's base frequency and hands it to
 * sub_0806F734); the rest is the standard layout and is NOT proved here. */
struct WaveData /* >= 0x10 */
{
    /* 0x00 */ u16 type;
    /* 0x02 */ u16 status;
    /* 0x04 */ u32 freq;
    /* 0x08 */ u32 loopStart;
    /* 0x0c */ u32 size;
};

/* WAVE 35 (W35-D), the m4a pitch tables.
 *   gUnknown_081B9DF4 -- MP2K's gScaleTable: one packed byte per MIDI key,
 *     low nibble a gFreqTable index and high nibble a right shift.
 *     sub_08070350 indexes it with key and key+1.
 *   gUnknown_081B9EA8 -- gFreqTable, whole words shifted RIGHT UNSIGNED
 *     (`lsrs`), so u32.
 *   gUnknown_081B9EF0 / gUnknown_081B9F74 -- the CGB pair sub_08070E4C uses
 *     the same way, except the value table is SIGNED halfwords (`ldrsh`) and
 *     both shifts are `asrs`, i.e. the packed byte reaches an int local.
 *   gUnknown_081B9F8C -- the noise-channel table sub_08070E4C returns
 *     directly for channel 4. */
extern const u8 gUnknown_081B9DF4[];
extern const u32 gUnknown_081B9EA8[];
extern const u8 gUnknown_081B9EF0[];
extern const s16 gUnknown_081B9F74[];
extern const u8 gUnknown_081B9F8C[];
/* Wave 47 (W47-B): MP2K's gPcmSamplesPerVBlankTable. sub_080708EC indexes it
 * `[freq - 1]` with a plain `ldrh` at stride 2 and stores the result into a
 * signed 32-bit member, so u16 is the element type and the array is
 * `const` -- it lives in ROM and nothing writes it. Its length is not proved
 * here; the freq nibble is 4 bits, so 15 is the ceiling. */
extern const u16 gUnknown_081B9ED8[];
/* Wave 48 (W48-C). Three sibling blobs sub_0806BF40 hands to
 * `void sub_080718F8(void *, u8 *, int)` as its second parameter, each with a
 * length of 0xe0, one per tag value 3, 4 and 5. `u8 []` off that parameter and
 * nothing else -- none of the three is dereferenced here, so this is the
 * parameter's type rather than a reading of the data. NOT const: the parameter
 * is a plain `u8 *`. The addresses are 0x74 and 0x84 apart, which does not
 * divide 0xe0, so they are not one array and are declared separately. */
extern u8 gUnknown_081B9BC8[];
extern u8 gUnknown_081B9C3C[];
extern u8 gUnknown_081B9CC0[];

struct MusicPlayerTrack /* 0x50 */
{
    /* 0x00 */ u8 flags;
    /* 0x01 */ u8 wait;
    /* 0x02 */ u8 patternLevel;
    /* 0x03 */ u8 repN;
    /* 0x04 */ u8 filler_04[0x01]; /* gateTime */
    /* 0x05 */ u8 key;             /* sub_080700C0 uses +0x05 as key and +0x06
                                    * as velocity, which agrees with the
                                    * canonical MP2K layout. Wave 47 (W47-A)
                                    * narrowed key out of the filler:
                                    * sub_080702C0 (ply_endtie) both stores the
                                    * incoming command byte here and reads it
                                    * back on the other arm of the same `if`,
                                    * then compares it against SoundChannel::mk
                                    * -- a producer and a consumer agreeing on
                                    * one byte, which is what settles it. */
    /* 0x06 */ u8 filler_06[0x02]; /* velocity runningStatus */
    /* 0x08 */ u8 keyM;            /* wave 35 (W35-D): the eight bytes +0x08..
                                    * +0x0f are all reached by sub_08070D98
                                    * (TrkVolPitSet) and land exactly on the
                                    * canonical MP2K names. keyM/pitM are the
                                    * two halves of one s32 pitch stored
                                    * `asrs r0,r1,#8; strb [+8]; strb r1,[+9]`.
                                    * keyShift and keyShiftX are read `ldrsb`
                                    * and shifted left 8 SEPARATELY (two adds,
                                    * not one shifted sum); tune is `ldrb` and
                                    * multiplied by 4 with the bend product;
                                    * pitX is `ldrb` and added unscaled; bend is
                                    * `ldrsb`, bendRange `ldrb`. */
    /* 0x09 */ u8 pitM;
    /* 0x0a */ s8 keyShift;
    /* 0x0b */ s8 keyShiftX;
    /* 0x0c */ s8 tune;
    /* 0x0d */ u8 pitX;
    /* 0x0e */ s8 bend;
    /* 0x0f */ u8 bendRange;
    /* 0x10 */ u8 volMR;           /* the stereo volume pair, proved together by
                                    * sub_08070090 (ChnVolSet, hand-written):
                                    * `chan->rightVolume = (chan->ve *
                                    * (0x80 + chan->rp) * track->volMR) >> 14`
                                    * clamped to 0xff, then the mirrored
                                    * `0x7f - rp` / volML for leftVolume. Only
                                    * these two of the six bytes at +0x10..+0x15
                                    * are reached, so vol/volX/pan/panX stay
                                    * filler. */
    /* 0x11 */ u8 volML;
    /* 0x12 */ u8 vol;             /* wave 35 (W35-D): sub_08070D98 reads vol and
                                    * volX with a bare `ldrb` and multiplies
                                    * them, and pan/panX with `ldrsb` -- the
                                    * canonical MP2K signedness. modM is read
                                    * `ldrsb` at all three of its uses there and
                                    * so is really s8; it is left `u8` and cast
                                    * at the use rather than retyped, because
                                    * nothing else in src/decomp/ reads it and a
                                    * cast on a u8 member emits the same
                                    * `movs rI,#K; ldrsb` pair. */
    /* 0x13 */ u8 volX;
    /* 0x14 */ s8 pan;
    /* 0x15 */ s8 panX;
    /* 0x16 */ u8 modM;
    /* 0x17 */ u8 mod;
    /* 0x18 */ u8 modT;
    /* 0x19 */ u8 lfoSpeed;
    /* 0x1a */ u8 lfoSpeedC;
    /* 0x1b */ u8 lfoDelay;
    /* 0x1c */ u8 lfoDelayC;
    /* 0x1d */ u8 priority;
    /* 0x1e */ u8 echoVolume;
    /* 0x1f */ u8 echoLength;
    /* 0x20 */ void *chan;
    /* 0x24 */ struct ToneData tone;
    /* 0x30 */ u8 filler_30[0x10];
    /* 0x40 */ u8 *cmdPtr;
    /* 0x44 */ u8 *patternStack[3];
};

/* The MP2K SOUND_INFO block, reached only through gUnknown_03007FF0 -- the
 * canonical SOUND_INFO_PTR, and this ROM keeps it at the canonical 0x03007FF0.
 * `ident` carries the same 'Smsh' MPLAY_ID_NUMBER as MusicPlayerInfo::ident and
 * the driver offsets it by +-10 to record that the vsync DMA is disarmed:
 * sub_08070A7C (SoundVSyncOff) adds 10 after `ident - ID_NUMBER <= 1`, and
 * sub_08070AF8 (SoundVSyncOn, matched) subtracts it again.
 *
 * pcmDmaCounter is `volatile`, and that is proved rather than lifted:
 * sub_08070AF8 emits a dead `ldrb r0,[r2,#4]` immediately ahead of the
 * `movs r0,#0; strb r0,[r2,#4]` that overwrites the register. A load into a
 * register nothing reads, on a QImode aggregate member, is the volatile tell --
 * see "A load of the store's own address whose destination is never read" in
 * docs/agbcc-codegen.md. Without the qualifier the function is 4 bytes short.
 *
 * WAVE 33 (W33-A) CONTRADICTS THAT, and it is left unresolved rather than
 * papered over. sub_0806FD98 (the m4aSoundVSync shape) decrements this field
 * and tests the result: the ROM is `ldrb r1,[r0,#4]; subs r1,#1;
 * strb r1,[r0,#4]; bgt`, ONE load. With `volatile` agbcc cannot produce that
 * -- `--p->pcmDmaCounter > 0` becomes ldrb / subs / ldrb / strb / ldrb / cmp,
 * three loads, because every read of a volatile lvalue has to happen. So the
 * two functions disagree about the qualifier and only one of them can be
 * right. NOT changed: sub_08070AF8 is matched and promoted, and sub_0806FD98
 * is blocked by a literal pool that sits outside its own extent (see
 * work/sub_0806FD98/sub_0806FD98.c), so flipping the qualifier would break a
 * real match to fix a function that cannot close either way. Whoever promotes
 * this region as a whole file should settle it then -- the discriminating test
 * is whether sub_08070AF8 still matches with a plain `u8` here.
 *
 * WAVE 47 (W47-H) MEASURES THE COST OF THE QUALIFIER, WHICH NOBODY HAD DONE,
 * AND IT SHARPENS THE CONTRADICTION RATHER THAN SETTLING IT. Restating
 * SoundInfo's first 12 bytes in a throwaway local struct with a plain `u8`
 * here, changing nothing else in sub_0806FD98's C, drops BOTH dead loads and
 * takes that function from 100 to 96 bytes:
 *
 *     vu8   ldrb r0,[r1,#4] ; subs r0,#1 ; ldrb r1,[r1,#4] ; strb r0,[r1,#4]
 *     u8    ldrb r0,[r1,#4] ; subs r0,#1 ;                   strb r0,[r1,#4]
 *     ROM   ldrb r1,[r0,#4] ; subs r1,#1 ;                   strb r1,[r0,#4]
 *
 * So `u8` reproduces the ROM's decrement EXACTLY and the whole residual on
 * that statement collapses to one `cmp r0,#0` before the `bgt`. That is the
 * strongest evidence yet that this member is not volatile, and it costs 4
 * bytes wherever it is wrong. STILL NOT CHANGED, for the reason above: only
 * re-running sub_08070AF8 can settle it, and that function is promoted.
 *
 * Also W47-H: the qualifier is NOT a build-configuration artefact. The same C
 * is BIT-IDENTICAL under old_agbcc and the default agbcc (both 100 bytes,
 * `cmp` included) and strictly worse at -O1 (104). The m4a block being an
 * old_agbcc block does not reach this, and neither does the flash block's
 * -O1 finding -- do not re-sweep those axes against this member.
 */
struct MusicPlayerInfo;
struct SoundInfo
{
    /* 0x00 */ vu32 ident;
    /* 0x04 */ vu8 pcmDmaCounter;
    /* 0x05 */ u8 reverb;  /* wave 47 (W47-B): sub_08070990 (m4aSoundMode)
                            * stores `mode & 0xff & 0x7f` here -- the canonical
                            * MP2K SOUND_MODE_REVERB field. Bare `strb`. */
    /* 0x06 */ u8 unk06;   /* wave 35 (W35-D): sub_080707F4 (SoundInit)
                            * seeds these two with 8 and 0xf right after
                            * the CpuSet that zeroes the whole block --
                            * the canonical MP2K maxChans/masterVolume
                            * pair. Bare `strb`, so the widths are a
                            * floor. */
    /* 0x07 */ u8 unk07;
    /* 0x08 */ u8 freq;             /* wave 47 (W47-B): sub_080708EC
                                     * (SampleFreqSet) stores `(mode & 0xf0000)
                                     * >> 16` here and uses the same value,
                                     * minus one, to index gUnknown_081B9ED8.
                                     * Bare `strb`. */
    /* 0x09 */ u8 filler_09[0x02];  /* mode c15 */
    /* 0x0b */ u8 pcmDmaPeriod;     /* wave 33 (W33-A): sub_0806FD98 (the
                                     * m4aSoundVSync shape) reloads
                                     * pcmDmaCounter from this byte when the
                                     * countdown reaches zero -- `ldrb [r0,#0xb];
                                     * strb [r0,#4]`. The canonical MP2K
                                     * SoundInfo has pcmDmaPeriod at exactly
                                     * this offset. Not volatile: it is read
                                     * once and nothing here needs the
                                     * qualifier. */
    /* 0x0c */ u8 filler_0c[0x04];  /* maxLines gap[3] */
    /* 0x10 */ s32 pcmSamplesPerVBlank;
                                    /* wave 47 (W47-B): the three s32 at +0x10/
                                     * +0x14/+0x18 are all set by sub_080708EC
                                     * and every one of them is an operand of a
                                     * `bl __divsi3`, not `__udivsi3` -- so they
                                     * are SIGNED, and that is the only thing
                                     * pinning the signedness. +0x10 is
                                     * gUnknown_081B9ED8[freq-1] (a u16 load
                                     * widened by the `str`), +0x14 is
                                     * (samples * 597275 + 5000) / 10000, and
                                     * +0x18 is (0x1000000 / +0x14 + 1) >> 1 --
                                     * note the last one divides by the value
                                     * just stored at +0x14, NOT by +0x10. */
    /* 0x14 */ s32 pcmFreq;
    /* 0x18 */ s32 divFreq;
    /* 0x1c */ struct CgbChannel *cgbChans;
                                    /* wave 47 (W47-B): sub_08070A28 null-checks
                                     * it, then walks four 0x40-byte records
                                     * from it. */
    /* 0x20 */ void (*func)(struct MusicPlayerInfo *);
                                    /* wave 47 (W47-B): the MP2K func/intp pair.
                                     * sub_08070B34 (MPlayOpen) chains them --
                                     * it moves any existing pair into the new
                                     * player's own +0x38/+0x3c, then parks
                                     * sub_0806FDE4 (MPlayMain) in +0x20 and the
                                     * MusicPlayerInfo it was handed in +0x24.
                                     * The stored function ADDRESS is what fixes
                                     * the member as a pointer-to-function
                                     * rather than a u32. */
    /* 0x24 */ struct MusicPlayerInfo *intp;
    /* 0x28 */ void (*unk28)(void); /* wave 35 (W35-D): SoundInit parks the
                                     * SAME address (sub_080718E4) in all
                                     * four of +0x28/+0x2c/+0x30/+0x3c and
                                     * sub_080700C0 in +0x38 -- the MP2K
                                     * hook block. None is CALLED from C
                                     * yet, so `void (*)(void)` is the
                                     * weakest shape that fits; +0x34 gets
                                     * the ADDRESS of gUnknown_03005740, the
                                     * table sub_0806FBD4 has just filled. */
    /* 0x2c */ void (*unk2c)(u8);   /* wave 47 (W47-B): retyped from
                                     * `void (*)(void)`. W35-D's note said
                                     * `void (void)` was the weakest shape that
                                     * fits BECAUSE nothing called it; that
                                     * condition is now gone. sub_08070A28
                                     * calls it through this member with exactly
                                     * one argument, zero-extended to u8 by an
                                     * `lsls #0x18; lsrs #0x18` pair at the call
                                     * -- the CGB oscillator-off hook. The four
                                     * stores in sub_080707F4 (c_080707F4.c)
                                     * pick up a cast, which is byte-neutral;
                                     * that file was re-verified as still
                                     * matching after this change. */
    /* 0x30 */ void (*unk30)(void);
    /* 0x34 */ void *unk34;
    /* 0x38 */ void (*unk38)(void);
    /* 0x3c */ void (*unk3c)(void);
    /* 0x40 */ u8 filler_40[0x10];
    /* 0x50 */ struct SoundChannel chans[12];
                                    /* wave 47 (W47-B): carved out of the old
                                     * filler_40[0x310] at the same offsets --
                                     * sub_08070A28 zeroes twelve `status`
                                     * bytes from +0x50 at stride 0x40, and the
                                     * array ends exactly where pcmBuffer
                                     * begins. */
    /* 0x350 */ u8 pcmBuffer[2][0x630];
                                    /* wave 35 (W35-D): the two DMA source
                                     * buffers, and the extents are PROVED
                                     * rather than lifted. sub_080707F4 points
                                     * DMA1SAD at +0x350 and DMA2SAD at +0x980,
                                     * so the stride is 0x630; sub_08070A7C's
                                     * CpuSet clears 0x318 WORDS from +0x350,
                                     * which is 0xc60 bytes == exactly the two
                                     * of them; and sub_080707F4's own CpuSet
                                     * clears 0x3ec words == 0xfb0 from +0, so
                                     * 0x350 + 0xc60 is the whole object. */
};

/* Wave 31 (W31-C): the m4a song and player tables, named from the shapes
 * sub_08070478 / sub_08070544 / sub_08070578 index them with.
 *
 * gUnknown_0824238C is the SONG table: stride 8 (`lsls #0x10; lsrs #0xd` on a
 * u16 index is `(u16)n * 8`), a word at +0 handed to MPlayStart as the header
 * and compared against `mplayInfo->songHeader`, and a `ldrh` at +4 used as the
 * player index. gUnknown_08242308 is the PLAYER table: stride 12
 * (`((n*2)+n)*4`), with a `struct MusicPlayerInfo *` at +0. Only those three
 * members have users; the rest is filler. */
struct Song /* 0x08 */
{
    /* 0x00 */ void *header;
    /* 0x04 */ u16 ms;
    /* 0x06 */ u16 me;
};
struct MusicPlayer /* 0x0c */
{
    /* 0x00 */ struct MusicPlayerInfo *info;
    /* 0x04 */ struct MusicPlayerTrack *track;
    /* 0x08 */ u8 trackCount;
    /* 0x09 */ u8 filler_09[0x01];
    /* 0x0a */ u16 unk_0a;
};

/* The MP2K song header, i.e. what struct Song::header points at and what
 * sub_08070BAC (MPlayStart) is handed as its second argument. Wave 47 (W47-B):
 * every member below is read by that function -- trackCount and priority
 * `ldrb`, reverb `ldrb` and tested against 0x80, tone `ldr`, and part[i] as a
 * word at `songHeader + 8 + i*4`. */
struct SongHeader
{
    /* 0x00 */ u8 trackCount;
    /* 0x01 */ u8 blockCount;
    /* 0x02 */ u8 priority;
    /* 0x03 */ u8 reverb;
    /* 0x04 */ struct ToneData *tone;
    /* 0x08 */ u8 *part[1];
};
extern const struct MusicPlayer gUnknown_08242308[];
extern const struct Song gUnknown_0824238C[];
/* WAVE 47 (W47-A): the m4a player COUNT, and it is an ABSOLUTE SYMBOL whose
 * link-time value is 11 -- not an integer constant. Three functions read it and
 * all three do it the same way: `ldr rN, =0x0000000B; lsls #0x10; lsrs #0x10;
 * cmp rN, #0; beq` (sub_080703F4 m4aSoundInit at pool 0x08070460, sub_080705AC
 * at 0x080705D0, sub_080705E4 at 0x08070608). Those are the ONLY three pooled
 * small integers in the whole ROM.
 *
 * Every part of that sequence is evidence for a symbol and against a literal:
 * 11 fits `movs rN, #imm8`, so agbcc would never pool it (measured -- a plain
 * `11`, `(u16)11`, a `u16` local, a `const u16` local, `sizeof(tbl)/sizeof(*tbl)`
 * and `(u16)` of that all emit `mov rN, #0xb`); the `(u16)` truncation survives
 * unfolded, which only happens when the value is unknown at compile time; and
 * the `cmp #0` zero-trip guard exists at all, which the loop optimiser would
 * have deleted for a known-positive bound. `(u16)(u32)&symbol` reproduces all
 * three at once and the whole of sub_080705AC byte-for-byte.
 *
 * NOT YET DEFINED ANYWHERE. tools/gen_lds.py invents no symbols and no data blob
 * carries this address, so before either reader can be PROMOTED the linker
 * script needs `gUnknown_0000000B = 0x0000000B;` alongside the other absolute
 * symbol definitions. Declaring it here is inert until then -- an unreferenced
 * extern costs nothing at link -- and per-function trymatch resolves it through
 * sym_addr()'s `gUnknown_<addr>` fallback, which is why the match verifies. */
extern const u8 gUnknown_0000000B;
/* Two adjacent IWRAM function pointers, each with a 20-byte forwarder of its
 * own -- sub_080707CC and sub_080707E0. Both are `bl _call_via_r1`, and the
 * register index is the argument count, so each takes one argument that its
 * forwarder passes straight through from r0 without touching it. The argument's
 * type is invisible on both sides; `int` is the weakest that fits.
 *
 * WAVE 47 (W47-B): gUnknown_030057CC's forwarder sub_080707E0 now has a caller.
 * sub_08070B34 (MPlayOpen) hands it the `struct MusicPlayerInfo *` it is about
 * to fill in, immediately before writing that player's members -- i.e. it is
 * MP2K's Clear64byte, and a MusicPlayerInfo is exactly 0x40 bytes. Retyped to
 * `void *` rather than to the concrete pointer, because the callee is a
 * memory-clearing primitive and nothing here says it only ever sees players.
 * gUnknown_030057C8 has no call site yet and is retyped only to keep the pair
 * spelled the same way; that half is NOT proved. src/decomp/c_080707CC.c was
 * updated with it and re-verified. */
extern void (*gUnknown_030057C8)(void *);
extern void (*gUnknown_030057CC)(void *);
struct MusicPlayerInfo /* 0x40 */
{
    /* 0x00 */ void *songHeader;
    /* 0x04 */ u32 status;
    /* 0x08 */ u8 trackCount;      /* wave 38 (W38-A): named, not lifted. All
                                    * five MPlay*Control entry points at
                                    * 0x08071420..0x080715F8 read it with a bare
                                    * `ldrb [r4,#8]` straight into the loop
                                    * counter that walks `tracks[]` one 0x50
                                    * stride at a time, guarded `cmp #0; ble` --
                                    * i.e. it is the element count of the array
                                    * at +0x2c. Was `filler_08[0x01]`; same
                                    * offset, same width, nothing else read it. */
    /* 0x09 */ u8 priority;        /* sub_080700C0 adds it to the track's own
                                    * priority at +0x1d and clamps to 0xff */
    /* 0x0a */ u8 filler_0a[0x01]; /* cmd */
    /* 0x0b */ u8 unk_0b;          /* wave 47 (W47-B): sub_080703F4 seeds it
                                    * from struct MusicPlayer::unk_0a, and
                                    * sub_08070BAC (MPlayStart) reads it `ldrb`
                                    * as the first term of its three-way entry
                                    * guard -- zero means "always accept the new
                                    * song". */
    /* 0x0c */ u32 clock;          /* zeroed by MPlayStart */
    /* 0x10 */ u8 filler_10[0x08]; /* gap[8] */
    /* 0x18 */ u8 *memAccArea;     /* wave 47 (W47-B): sub_080703F4 points every
                                    * player at gUnknown_03005BE0. */
    /* 0x1c */ u16 tempoD;
    /* 0x1e */ u16 tempoU;
    /* 0x20 */ u16 tempoI;
    /* 0x22 */ u16 tempoC;         /* wave 47 (W47-B): MPlayStart zeroes it
                                    * `strh` in the same run as tempoD/tempoU/
                                    * tempoI. */
    /* 0x24 */ u16 fadeOI;
    /* 0x26 */ u16 fadeOC;
    /* 0x28 */ u16 fadeOV;
    /* 0x2a */ u8 filler_2a[0x02];
    /* 0x2c */ struct MusicPlayerTrack *tracks;
    /* 0x30 */ struct ToneData *tone;
    /* 0x34 */ u32 ident;
    /* 0x38 */ void (*func)(struct MusicPlayerInfo *);
                                   /* wave 47 (W47-B): the receiving half of
                                    * SoundInfo's func/intp pair -- sub_08070B34
                                    * copies SoundInfo+0x20/+0x24 straight into
                                    * these two when it displaces them. Same
                                    * types as there, and the assignment is what
                                    * requires them to agree. */
    /* 0x3c */ struct MusicPlayerInfo *intp;
};

/* -------------------------------------------------------------- EWRAM -- */

extern struct Unk0200B0B0 *gUnknown_0200B0B0;
/* Wave 50 (W50-E). The single storage gUnknown_0200B0B0 points at, not a
 * second object of a similar shape: sub_08000E48 opens with
 * `gUnknown_0200B0B0 = &gUnknown_0200B000;` (`ldr r7,[=0x0808D6EC]; str r3,[r7]`
 * where the pool word holds &gUnknown_0200B0B0) and then clears it with
 * `sub_08001124(&gUnknown_0200B000, 0xB0)`. aw2bhr.lds puts gUnknown_0200B0B0
 * exactly 0xB0 bytes later, so that length is the whole object and confirms
 * sizeof(struct Unk0200B0B0) == 0xB0 -- the ">= 0x76" on the tag is now a
 * closed bound. The +0x9c clear right after the assignment goes through
 * gUnknown_0200B000 directly, not through the pointer, which is how the two
 * are known to be the same storage rather than a copy. */
extern struct Unk0200B0B0 gUnknown_0200B000;
extern struct Unk0200B224 gUnknown_0200B224[];
/* Wave 32 (W32-B): the HBlank scanline buffer sub_08011228 fills and
 * sub_08011298 / sub_0801137C hand to sub_080111C8 as a plain source address.
 * Nothing here reads an element, so `u8 []` is the weakest type that fits.
 *
 * Wave 37 (W37-P1): retyped to `u16 []`. sub_08011228 IS the filler and it
 * writes `strh` at `lsls #1` stride over i = 0..159, so the element is 16-bit
 * and the array is 160 entries. Byte-neutral at both call sites -- they only
 * hand the base to sub_080111C8's `void *` first parameter (c_08011298.c and
 * c_0801137C.c both re-verified). */
extern u16 gUnknown_0200B274[];
/* Exactly 48 entries: sub_08011C18 clears 0..0x2f (`cmp #0x2f; bls`) at stride
 * 0x0c, and 0x0200B3B4 + 48*0x0c == 0x0200B5F4, the next symbol. */
extern struct Unk0200B3B4 gUnknown_0200B3B4[];
/* Wave 32 (W32-B): the "defer this request" flag the gUnknown_0200B3B4 queue is
 * gated on. sub_08011E54 reads it `ldrh`, and when it is non-zero it performs
 * the copy immediately through sub_08011C68 and returns 0 instead of queueing.
 * Halfword; only ever tested against zero, so the signedness is unproved. */
extern u16 gUnknown_030044D0;
/* Exactly 0x20 bytes -- gUnknown_0200B614 is the next symbol, and every user
 * indexes 0..0x1f. SIGNED: the ARM routine sub_08000234 reads it with `ldrsb`
 * and adds the value to a palette component. sub_080136C4 clears all 32. */
extern s8 gUnknown_0200B5F4[0x20];
/* Wave 38 (W38-E). The unpacked RGB shadow that sits immediately after the
 * array above (0x0200B5F4 + 0x20). sub_08075A54 fills it from gPal: for each of
 * ten entries it writes THREE consecutive bytes at `(c * 16 + i) * 3`, holding
 * the 5-bit red, green and blue channels of `gPal[c * 16 + i]` (`& 0x1f`,
 * `>> 5 & 0x1f`, `>> 10 & 0x1f`). `u8` off the `strb`, and the stride of 3 is
 * read off the ROM's `idx * 2 + idx` synthesis, which shares its `idx * 2` with
 * the `&gPal[idx]` halfword address in the same statement. Left unsized: the
 * only writer indexes it by `c`, whose range is bounded by gUnknown_0200B5F4's
 * 0x20 above but never checked, so a declared extent would be a guess.
 * Wave 40 (W40-B): `u8` CONFIRMED, but note that sub_080139E0 reads it back
 * through an explicit `(s8)` cast, and the cast is load-bearing rather than
 * cosmetic. That function adds the row's s8 delta to a channel and stores the
 * result with `strb`; reading the sum back as SIGNED is what turns an overflow
 * past 0x7f into a negative that the following `< 0` arm saturates to 0. The
 * ROM distinguishes the two reads and so confirms the declared type: the
 * accumulate uses `ldrb` (only the low byte reaches the `strb`, so combine
 * narrows it) while the clamp's read is `ldrsb`. Declaring the array `s8`
 * would produce the same `ldrsb` and remove the need for the cast, so this is
 * a byte-neutral choice with no oracle -- left as `u8` because sub_08075A54,
 * which is already promoted, writes it as unsigned 5-bit components. */
extern u8 gUnknown_0200B614[];
/* Hoisted out of src/decomp/c_08014074.c in wave 21 (W21-A) so that
 * gUnknown_0200C020 and sub_080147B4 can name the same type. The two members
 * are sub_08014074's only accesses; sub_080147B4 writes +0x20/+0x24/+0x28 as
 * words, +0x2c/+0x2e/+0x34/+0x36 as halfwords and +0x31..+0x38 as bytes, so
 * everything below 0x39 is genuinely occupied and the filler is a floor. The
 * OBJECT is 0x58 bytes (aw2bhr.lds puts the next symbol at 0x0200C078); the
 * struct is not grown to match because nothing indexes it. */
/* Wave 32 (W32-C) fills the record in from its two whole-record writers.
 * sub_080147B4 builds it field by field and sub_080145E8 copies exactly the
 * same fields, in exactly the same order, out of gUnknown_0200C020 -- so the
 * two functions agree on every width below, and the tail now reaches 0x40:
 *   +0x20 is `gUnknown_08610A38[arg5]`, i.e. a `u8 *` (that array is declared
 *     `u8 *[]` above);  +0x28 is sub_080147B4's `u16 *` argument forwarded;
 *   +0x2c/+0x2e/+0x34/+0x36 are `strh`/`ldrh`;  +0x30..+0x33, +0x38 and +0x40
 *     are `strb`/`ldrb`;  +0x3c is written with `=sub_08013AEC`, a
 *     `void (void)` function pointer.
 * +0x24 is the one guess: it is only ever stored 0 and copied as a word. */
/* Wave 40 (W40-D): this struct is ALSO the view through which a
 * gUnknown_03001470[] slot is written, and that is not a coincidence --
 * sub_080149C0 and sub_08014A5C fill a slot with the same fields, in the same
 * order, that sub_080147B4 fills gUnknown_0200C020 with (they skip +0x31 and
 * stop after +0x33). They reach it as
 * `(struct Unk08014074 *)&gUnknown_03001470[sub_08015BD0(0)]`, the cast
 * src/decomp/c_08014740.c already uses. struct Unk03001470 CANNOT describe
 * those accesses -- it declares s16 at 0x20 and 0x24 and int at 0x2c where
 * these are a word, a word and a halfword -- and it is deliberately not
 * reshaped; this is the same per-subsystem-overlay situation the note on
 * Unk03001470's 0x3c..0x5f scratch describes.
 * TWO MORE PARTIAL VIEWS OF THE SAME OBJECT EXIST as file-local tags, and
 * unifying them onto this one is an open, self-contained job: `struct
 * Unk8013D4C` in src/decomp/c_08013D4C.c (unk28 as `u16 *`, unk30..unk33 as
 * u8 -- identical types at identical offsets, so that file is a drop-in
 * retype) and `struct Unk1B998` in src/decomp/c_0801B964.c (unk2c/unk2e/unk32
 * agree; its `u32 unk20` would need reconciling with the `u8 *` below at the
 * sub_0801B9C8 call, whose second parameter is declared `u32`). Until that is
 * done, sub_08013D4C, sub_08014084 and sub_0801B998 have NO declaration in any
 * header and work/sub_080149C0/ carries them locally -- see that file. */
struct Unk08014074
{
    /* 0x00 */ u8 filler_00[0x20];
    /* 0x20 */ u8 *unk20;
    /* 0x24 */ u32 unk24;
    /* 0x28 */ u16 *unk28;
    /* 0x2c */ u16 unk2c;
    /* 0x2e */ u16 unk2e;
    /* 0x30 */ u8 unk30;
    /* 0x31 */ u8 unk31;
    /* 0x32 */ u8 unk32;
    /* 0x33 */ u8 unk33;
    /* 0x34 */ u16 unk34;
    /* 0x36 */ u16 unk36;
    /* 0x38 */ u8 unk38;
    /* 0x39 */ s8 unk39;
    /* 0x3a */ s8 unk3a;
    /* 0x3b */ u8 filler_3b[0x01];
    /* 0x3c */ void (*unk3c)(void);
    /* 0x40 */ u8 unk40;
};
/* The single instance of the above -- sub_08014614, sub_08014668 and
 * sub_080146D4 each hand `&gUnknown_0200C020` to sub_080147B4 and then to
 * sub_08014074, and no other code in asm/ names the address. */
extern struct Unk08014074 gUnknown_0200C020;
/* struct Unk0200C078Rec (the 8/12/12 bitfield word) was MOVED UP to just above
 * struct Unk0200C420 in wave 43 (W43-E), because that struct now declares an
 * array of them at +0x38 and needs the type to be complete. Its evidence
 * comment moved with it. */
/* Two tables of the above, adjacent in RAM (0x0200C078 + 30 * 0x14 ==
 * 0x0200C2D0) and always walked together: sub_08016A54 clears both,
 * sub_08016B2C copies both into a save buffer and sub_08016BC0 copies them
 * back. The row counts and widths are the loop bounds of all three (0x1d/5 and
 * 0x29/2, inclusive), and the 0x14 / 0x8 strides are the outer address steps.
 * Wrapped in a struct rather than declared 2-D because the save/restore pair
 * indexes a ROW and then walks the row's words. */
struct Unk0200C078 /* 0x14 */
{
    /* 0x00 */ struct Unk0200C078Rec unk00[5];
};
struct Unk0200C2D0 /* 0x08 */
{
    /* 0x00 */ struct Unk0200C078Rec unk00[2];
};
extern struct Unk0200C078 gUnknown_0200C078[30];
extern struct Unk0200C2D0 gUnknown_0200C2D0[42];
/* Wave 38, W38-H. The 0x5CC-byte save block sub_08016B2C fills and
 * sub_08016BC0 reads back -- 0x5CC is the constant both of them return, and
 * every offset below is one of their statements. Declared here rather than in
 * either .c because sub_08016ED8 reloads two of its fields out of
 * gUnknown_02000000, which is a different translation unit.
 *
 * The two table regions are NOT the same size as the tables they hold: the
 * gUnknown_0200C2D0 region is 42 * 8 bytes but both copy loops walk FIVE words
 * per row (`movs r1,#4` with a `bge` bottom test) against an 8-byte stride, so
 * each row copy runs 12 bytes past its own row. It is a real bug in the
 * original -- the inner bound was copy-pasted from the 0x14-byte table above
 * it -- and it is harmless only because the 12-byte overrun of the LAST row
 * lands in unk3f0, which the very next statement overwrites wholesale.
 * Reproduced, not fixed. */
struct Unk08016B2C /* 0x5cc */
{
    /* 0x000 */ struct Unk02028030 unk000;
    /* 0x048 */ struct Unk0200C078 unk048[30];
    /* 0x2a0 */ struct Unk0200C2D0 unk2a0[42];
    /* 0x3f0 */ struct Unk0200C420 unk3f0;
    /* 0x4d0 */ u8 unk4d0[0xfc]; /* gUnknown_0202FDFC's first 0xfc bytes; the
                                  * block copy names that object and this
                                  * batch does not touch its type. */
};
/* Wave 38, W38-H: gUnknown_0808E53C and gUnknown_0808E54C are two MORE
 * -fforce-addr address constants for this object -- both dereference to
 * 0x0200C420 in baserom.gba, alongside the 0x0808E540/_544/_548 trio recorded
 * on unk09 below. Each function gets its own private copy, so the same address
 * appears under three different invented names; naming the global directly
 * matched sub_08016A54 and sub_08016ED8 and the split carves the duplicates. */
extern struct Unk0200C420 gUnknown_0200C420;
extern u32 gUnknown_0200C500[2];
extern struct Unk0200C528 gUnknown_0200C528[];
/* The sprite-entry pool the bump allocator hands out; gUnknown_03002B24 is
 * reset to it by sub_0801BE78. 0x0200D510 - 0x0200CD10 == 0x800, i.e. exactly
 * 128 entries before the layer-head array. */
extern struct SpriteEntry gUnknown_0200CD10[];
extern struct SpriteEntry gUnknown_0200D510[];
extern struct Unk0200E438 gUnknown_0200E438[];
extern struct Unk0200F720 gUnknown_0200F720[];
extern struct Unk0200F920 gUnknown_0200F920[];
/* Wave 33, W33-B: the sprite-request ring sub_0801E338/sub_0801E4B0/sub_0801E8D8
 * fill one entry of, at index gUnknown_03002510, then hand to sub_0801A718.
 * 0x14-byte stride (the `idx*5 << 2` element math). */
/* WAVE 42 (W42-G): unk00, unk02 and unk08 ARE SIGNED -- s16, not u16. Left
 * alone here per the brief's hard rule because three sibling drafts read them,
 * but the evidence is a discriminating use and is worth applying:
 * sub_0801DFE8 passes all three to sub_0801E3E8, whose parameters are `int`
 * (its own entry block has no `lsls #16; lsrs #16` pair, so they are not
 * narrow), and the ROM sign-extends them with `ldrsh`. A u16 member can only
 * reach an `int` parameter zero-extended, so the member is signed. Every other
 * access in the tree is a store or a read into an s16 parameter, both of which
 * are signedness-neutral, which is why nothing caught it before. sub_0801DFE8
 * matched with `(s16)` casts at that call site instead; if these are retyped,
 * drop the casts from src/decomp/c_0801DFE8.c. */
struct Unk0200ED20 /* 0x14 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u32 unk04;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ long long unk0c; /* sub_0801E338 copies its `long long` a5 here as
                                 * one 8-byte block (`str [0xc]; str [0x10]` off
                                 * &e); sub_0801ECE8 masks the two halves. */
};
extern struct Unk0200ED20 gUnknown_0200ED20[];
extern int gUnknown_03002510;
/* Wave 33, W33-B: sub_0801DC50's high-water index -- it stores a1+1 here when a1
 * reaches it. Word. */
extern int gUnknown_03003034;
/* FORCE-ADDR, NOT OBJECTS (wave 42, W42-F): the ROM words the disassembly calls
 * gUnknown_0808F09C, gUnknown_0808F0A0 and gUnknown_0808F0A4 hold 0x0200E438,
 * 0x03003034 and 0x03003034 -- verified against baserom.gba -- so they are
 * agbcc's own -fforce-addr address constants for gUnknown_0200E438 and (twice
 * over) gUnknown_03003034, not objects. Do NOT declare them. Naming the two
 * globals directly is what reproduces them: sub_0801D84C reads both through the
 * `ldr rN,=<word>; ldr rM,[rN]` double indirection and matches with
 * `gUnknown_0200E438[i].unk08` and `gUnknown_03003034` written plainly, and
 * c_0801D8E4.c already does the same for the 0x0808F0A4 copy. Two copies of one
 * address across sibling functions is normal -- each function gets its own.
 *
 * What TRIGGERS the pool here is worth recording because it is authorable, and
 * it is the loop's exit shape rather than anything about the globals: writing
 * sub_0801D84C's search loop with `break` emits both pool words and matches,
 * while the same loop with `return` in place of the `break` strength-reduces
 * into a pointer cursor, drops the reference count across the merge, and emits
 * neither word (also -8 bytes). That is the brief's `continue`/`return`
 * discriminator showing up on `break`, and it decided the whole function. */
/* A 0x800-byte staging buffer: sub_0803A174 blits it to VRAM 0x06015D00 with
 * sub_08011C68, and the caller two instructions above that registration fills
 * it with Decompress(<ROM blob>, gUnknown_0200FD50). Non-const and `u8 []`
 * because Decompress's second parameter is `void *`. The size is the copy
 * length, not a proved extent. */
extern u8 gUnknown_0200FD50[];
extern struct Unk02027F74 gUnknown_02027F74;
/* A candidate list filled from index 0 and consumed through the
 * gUnknown_02027F74.unk36 / .unk37 cursor pair. u8: every write is a `strb`
 * through a variable index (sub_08037448 stores a 0..0xbf id, sub_08037508
 * fills 0x2a entries with i + 0x6c, sub_0803753C and sub_08037570 fill 0x6b
 * entries with i + 1). Signedness unproved -- no reader has been matched.
 *
 * NOTE the two symbols OVERLAP and the ROM names both: 0x02027F78 is
 * gUnknown_02027F74 + 4, and .unk36/.unk37 sit at array indices 0x32/0x33.
 * sub_08037448 caps its fill at 0x32 entries, which is exactly consistent, but
 * sub_0803753C/sub_08037570 write 0x6b entries straight over the cursor pair
 * before resetting it. Kept as two separate externs because that is what the
 * original source did -- both pool words are CLEAN (`.4byte gUnknown_02027F78`
 * with no addend, `.4byte gUnknown_02027F74` with the 0x36/0x37 as runtime
 * adds), which a single-object spelling cannot produce. */
extern u8 gUnknown_02027F78[];
extern struct Unk02027FB0 gUnknown_02027FB0[16];
/* Wave 38, W38-H: the symbol the splitter calls `gUnknown_02028038` is NOT an
 * object -- it is `&gUnknown_02028030.unk08`, i.e. this symbol with an addend of
 * 8, which is what a `.4byte gUnknown_02028030+0x8` pool word disassembles to.
 * sub_08016EA4 copies eight bytes into it from a save buffer's own
 * struct Unk02028030 at +8, and spelling it as the member matches.
 *
 * Wave 42, W42-L: THREE MORE OF THIS OBJECT'S MEMBERS ARE NAMED AS GLOBALS BY
 * THE SPLITTER AND ARE NOT OBJECTS EITHER -- gUnknown_02028040, gUnknown_02028042
 * and gUnknown_0202805A are `&gUnknown_02028030.unk10`, `.unk12` and `.unk2a`
 * (0x02028030 + 0x10 / 0x12 / 0x2a). sub_08031A9C copies gUnknown_02025564's
 * unk00/unk05/unk02 into exactly those three at exactly those offsets and
 * matches byte-for-byte spelled as members, which is the same argument the
 * wave-41 note on struct Unk02025564 makes for gUnknown_02025566 and
 * gUnknown_02025569: for a RAM address there is no relocation that could tell
 * `.4byte sym+off` from `.4byte othersym` apart, so trymatch reports it as
 * "different symbols that resolve to the same address" and the member spelling
 * is the one that needs no new declaration. Do NOT declare the three. */
extern struct Unk02028030 gUnknown_02028030;
/* The shadow copy of gUnknown_02028030, and it is the SAME type: the two
 * symbols are adjacent (0x02028030 + 0x48 == 0x02028078) and sub_0803BCA0 /
 * sub_0803BCB8 are a save/restore pair that memcpy 0x48 bytes between them in
 * both directions -- one length, one struct, both sizeof(struct Unk02028030).
 * Nothing else in the ROM touches it, so the field layout is inherited rather
 * than independently proved. */
extern struct Unk02028030 gUnknown_02028078;
extern struct Unk020280C0 gUnknown_020280C0[];
/* Wave 50 (W50-H). sub_0803BFBC reads 4 bytes from here at a 28-byte stride,
 * `gUnknown_020280D4[k * 28 + 1 + i]` with `k = gUnknown_03003FC0.unk02 - 0xb4`,
 * guarded by that index being in 0..0xb. The pool word is a CLEAN
 * `.4byte gUnknown_020280D4` with the `+ 1` left as a runtime `adds r2, r0, #1`
 * in the preheader, which is why it is spelled flat here.
 *
 * UNPROVED, and worth a look by whoever types struct Unk020280C0: 0x020280D4 is
 * gUnknown_020280C0 + 0x14, and if that struct's stride is 28 then this is
 * `gUnknown_020280C0[k].unk15[i]` and not an object of its own -- the same
 * situation as gUnknown_02028040/42/5A above. For a RAM address no relocation
 * can tell `.4byte sym+off` from `.4byte othersym`, so both spellings match and
 * only the struct's stride decides which is honest. */
extern u8 gUnknown_020280D4[];
extern struct Unk02025564 gUnknown_02025564;
/* Wave 41, W41-D. Four rows of 13 bytes, cleared row by row by sub_0802F28C
 * with a real `muls` against a 13 held in a register -- so a 2-D table and not
 * a flat run with a folded shift. VOLATILE: every `strb` in the clear loop is
 * preceded by a dead `ldrb` of the same address. */
extern volatile u8 gUnknown_020257E4[][13];
extern struct Unk02028360 gUnknown_02028360[];
/* The second object of struct Unk02028360's shape named in that type's comment,
 * declared in wave 29 (W29-B) because sub_0803DE68 clears the same ~0x3C0 mask
 * out of both objects' unk02 back to back. 0x020283E0 - 0x02028360 == 0x80,
 * i.e. sixteen entries, but nothing bounds either array. */
extern struct Unk02028360 gUnknown_020283E0[];
/* Wave 41, W41-D. The template bank sub_08026040 copies into its 0xC00-byte
 * scratch buffer before handing the four 64-record quarters to sub_080260D0.
 * The element type is settled by that callee, whose promoted definition
 * (src/decomp/c_080260D0.c) takes `struct UnitRecord *`: 0x100 iterations of
 * a 12-byte `ldm/stm` block move is 0xC00 bytes exactly, and the four
 * sub_080260D0 arguments are &buf[0] / [0x40] / [0x80] / [0xc0], i.e. 64
 * records apiece -- the same 64-per-slot grouping gUnknown_08499594 itself
 * uses. So this is the pristine copy of the four army rosters. */
extern struct UnitRecord gUnknown_02022684[];
/* 0xff-terminated byte list scanned by sub_0803BFBC; sub_08043C98 hands
 * out its address.
 *
 * Wave 41, W41-D: ALSO reached through the ROM word 0x08090A5C, which holds
 * 0x020288A0 -- an agbcc -fforce-addr address word, not a global, exactly like
 * the 0x08090978 run W41-C documented near gUnknown_08499590. sub_08026254
 * names this symbol honestly (three references across the count loop's merge)
 * and matches with the pool word placed; gen_lds.py's `gUnknown_08090A5C` is
 * not an object. */
extern u8 gUnknown_020288A0[];
/* Scratch byte; sub_0803BFA4 loads it with gUnknown_0849ECDC->unk01 + 1 and
 * sub_08043D5C zeroes it. sub_0803BF10 copies it into gUnknown_03003FC0.
 */
extern u8 gUnknown_020288B0;
/* Wave 48, W48-I. A byte list of SLOT INDICES into gUnknown_08499594: sub_0804769C
 * walks gUnknown_02028DD8[i] for i < p->unk21 and uses each byte as
 * gUnknown_08499594[gUnknown_03003F2C + gUnknown_02028DD8[i]], the same
 * base-plus-offset subscript every 0x0805A army scanner uses. Width from the
 * load (`adds r0, i, base; ldrb`); the length is carried by the caller's
 * counter, not by the object. */
extern u8 gUnknown_02028DD8[];
extern u8 gUnknown_02028E3C;
extern u8 gUnknown_02028E40;
extern u8 gUnknown_02028E41[];
/* Wave 30, W30-E. sub_0804AF88 reads it with a bare `ldrb` and tests it
 * against 0 as the first arm of an `||` whose other arm is
 * gUnknown_030044E0->unk5c -- a flag byte, nothing narrower or wider is
 * ever touched. */
extern u8 gUnknown_02028E48;
/* Two-halfword rows, indexed by gUnknown_0300453C. The 4-byte stride is
 * `lsls #2` at all fourteen sites; [i][0] is a small state flag (sub_0804B3E0
 * compares it against 1 and clears both halves together) and [i][1] indexes
 * gUnknown_085644D4/gUnknown_085644C8 in sub_0804F0D8 and sub_080501DC.
 * sub_08051F4C sets [i][0] to 1. Unsigned is not proved -- every access is a
 * bare `ldrh`/`strh` -- and the row length is the stride, not an extent. */
extern u16 gUnknown_02028E5C[][2];
/* The ROM words the disassembly calls gUnknown_08136030 and gUnknown_08136034
 * BOTH hold 0x02029690 -- verified against baserom.gba -- so they are agbcc's
 * own -fforce-addr address constants for this array, one private copy per
 * function, and not objects in ROM. Same reading as gUnknown_0808E540 above. */
extern struct Unk02029690 gUnknown_02029690[];
/* Wave 37, W37-B: the SAME reading applies to the whole run at 0x08136130 and
 * up, which is more agbcc `.rodata` address-constant pool and not objects.
 * Dereferenced in baserom.gba:
 *   0x08136130 -> 0x02029C04    0x08136134 -> 0x03004580
 *   0x08136138 -> 0x020296B0    0x0813613C -> 0x03001470
 *   0x08136140 -> 0x02029A10    0x08136144 -> 0x03004548
 *   0x08136148 -> 0x03004538    0x0813614C -> 0x03004580
 * Wave 51 (W51-H) dereferenced the next four and they are the same thing, so
 * the confirmed run now reaches 0x0813615C:
 *   0x08136150 -> 0x02029BE8    0x08136154 -> 0x0300450C
 *   0x08136158 -> 0x03004580    0x0813615C -> 0x03004580
 * 0x08136154 and 0x08136158 were both reproduced by naming the target directly
 * -- sub_08055654 writes `gUnknown_0300450C` and sub_08055940 writes
 * `gUnknown_03004580[i][1]`, and trymatch prints `R_ARM_ABS32 .rodata` against
 * the ROM's invented symbol in each case, with the rest of the chain byte-exact.
 * so `ldr rN,=gUnknown_081361xx; ldr rM,[rN]` is ONE indirection of compiler
 * bookkeeping, not a pointer global. Name the target global directly and agbcc
 * rebuilds the word; sub_080542EC, sub_080543E0 and sub_08054BA0 all matched
 * that way and carry `rodata` entries. Note 0x08136134 and 0x0813614C both hold
 * 0x03004580 -- one private copy per function, so two of these symbols naming
 * one address is expected and is not evidence of two objects. */
extern struct Unk02029A10Group gUnknown_02029A10[];
/* One byte per gUnknown_02029A10 slot, five per side -- sub_08051BEC indexes
 * it `a * 5 + j` (the `a * 5` is CSEd with gUnknown_08552178's row scaling in
 * the same function) and reads it with `ldrb`, testing == 1 as a "skip"
 * guard. The [5] is the stride; nothing bounds the outer dimension. */
extern u8 gUnknown_02029C14[][5];
extern struct Unk02029BA8 gUnknown_02029BA8[];
/* A double-buffer pointer pair, swapped whole by sub_08073AE8. Both hold
 * addresses: sub_08073C1E feeds gUnknown_0202FDE0 straight to REG_DMA0SAD, and
 * sub_08073930 stores gUnknown_0202FDDC through a pointer read out of ROM.
 * gUnknown_0202FDE4 is a third slot filled from the same source by sub_08073A80
 * and is not declared yet -- nothing matched reaches it. */
extern void *gUnknown_0202FDDC;
extern void *gUnknown_0202FDE0;
/* The third slot of that group, now reached: sub_080737EC sets it to
 * &gUnknown_0202F8DC and then feeds it straight to REG_DMA0SAD, so it holds an
 * address exactly as its two neighbours do. gUnknown_0202F8DC is the HBlank
 * scanline table that DMA copies into REG_WIN1H -- one halfword per line, and
 * the transfer is 1 unit wide with the source auto-incrementing, so the extent
 * is the screen height and is not otherwise pinned. */
extern void *gUnknown_0202FDE4;
/* Wave 30, W30-A adds a SECOND, independent reader and it constrains the
 * pointee: sub_08073930 re-arms this slot from gUnknown_0202FDDC when VCOUNT
 * runs past 0xA0 and then reads a halfword at `base + 4*line + 2`. Stride FOUR
 * with the halfword at +2 -- two entries per scanline, not the one-halfword
 * table the note above describes. `void *` is kept because sub_080737EC's
 * REG_DMA0SAD use needs no type and the two readings are not yet reconciled;
 * sub_08073930 casts at the point of use and records the shape there.
 *
 * sub_08073930 reaches the slot through agbcc's own `-fforce-addr` word at
 * 0x081CC02C, whose ROM contents are 0x0202FDE4, so the three-level load in
 * that function is one indirection of compiler bookkeeping and not a pointer
 * to a pointer. */
/* Wave 44 (W44-I). The extent IS pinned now, and the halfword stride with it:
 * sub_080735EC sets the FDE4 slot to this table and immediately fills it with
 * `for (i = 0; i < 0xA0; i++) ((u16 *)gUnknown_0202FDE4)[i] = 0x10;` -- 160
 * entries, `lsls #1` index scaling, one halfword per scanline, which is exactly
 * the screen height the note above could only infer from the DMA width. This
 * does NOT contradict sub_08073930's stride-4 reading: that function re-arms
 * the slot from gUnknown_0202FDDC, a different buffer, and the two are still
 * unreconciled.
 *
 * The fill re-loads gUnknown_0202FDE4 from memory on EVERY pass rather than
 * hoisting it, and that is ordinary output, not a `volatile` tell: the u16
 * store through the pointer may alias the pointer variable itself and agbcc's
 * alias analysis does not rule it out. Only the global's ADDRESS is hoisted.
 * Writing it with the cast at the point of use, as here, reproduces that; a
 * typed local bound before the loop would hoist and lose 4 bytes. */
/* Wave 48, W48-K. gUnknown_081CC030 is NOT A GLOBAL: the ROM word there is
 * 0x0202F8DC, i.e. &gUnknown_0202F8DC, read straight out of baserom.gba -- one
 * more of agbcc's own `-fforce-addr` address constants, in the same .rodata run
 * as the 0x081CC024/28/2C words that hold 0x0202FDE4. sub_08073A00 (size-exact)
 * reproduces the two-level `ldr r4, =0x081CC030; ldr r0, [r4]` by naming
 * gUnknown_0202F8DC directly. Do not declare a symbol for it.
 *
 * That function also pins the buffer at 640 halfwords, not 160: it clears
 * [0 .. 0x13F] and [0x140 .. 0x27F] in two loops and then publishes
 * gUnknown_0202FDDC = gUnknown_0202FDE4 = the base with
 * gUnknown_0202FDE0 = base + 0x140, i.e. the two halves ARE the double buffer
 * the FDDC/FDE0 note above describes. That reconciles the halfword-per-scanline
 * reading with sub_08073930's stride-4 reading: 0xA0 scanlines x 2 halfwords
 * per half. */
extern u16 gUnknown_0202F8DC[];
/* A u16 pair handed out together by sub_08073F90(u16 *x, u16 *y). Two separate
 * pool words with zero displacement, so two scalars rather than one aggregate
 * -- the same reading as gUnknown_03000044 / gUnknown_03000046. */
/* Wave 45 (W45-I). An EWRAM byte buffer of 0xC00-byte records: sub_080620C0
 * zeroes bytes 0..0x5f of record gUnknown_030033EC with `strb` off a
 * `base + idx * 0xC00 + i * 0x20 + j` index and no scaling anywhere, which is
 * what fixes the element at `u8`. Nothing else in the ROM names the symbol, so
 * the record count and the meaning of the 0x60-byte head are unproven. */
/* Wave 48 (W48-B). The 0xC00 record's INTERNAL layout, now proved -- four
 * functions (sub_0805B4D8, sub_0805B5BC, sub_0805B6A0 and the parked
 * sub_0805B778's neighbours) walk it and the two extents tile the record
 * exactly:
 *     0x000  u8   head[3][0x20];            <- W45-I's 0x60-byte "head"
 *     0x060  struct { u8 x, y; u8 pad[6]; } rec[3][0x7c];
 * 3 * 0x7c * 8 = 0xBA0, and 0x60 + 0xBA0 = 0xC00 with nothing left over, which
 * is what fixes BOTH inner extents. The first subscript is the same value in
 * both arrays (a small player/army index, hence 3), and the head byte chains
 * into rec[] as a 0xFF-terminated index; 0xFE in rec[].x means "advance the
 * head cursor" rather than "stop". rec[].x / rec[].y are a map cell, handed
 * straight to the gUnknown_08499590 rowOffset/terrain idiom.
 *
 * ACCESS SPELLING, and it is load-bearing -- the two halves are written
 * DIFFERENTLY and only one of the two spellings matches for each:
 *   - the head byte is a FLAT index, base added last:
 *       gUnknown_02029ED8[a * 0x20 + b + gUnknown_030033EC * 0xc00]
 *   - the record bytes need the base joined BEFORE the 0x60/0x61 displacement,
 *     i.e. a locally-declared struct cast onto the element address:
 *       ((struct Rec *)&gUnknown_02029ED8[i * 8 + a * 0x3e0
 *                                         + gUnknown_030033EC * 0xc00])->x
 *     Folding 0x60 into the flat index instead emits `+0x60` before `+base`
 *     and is the whole diff; this is the same COMPONENT_REF association rule
 *     the gUnknown_08499590 comment records, and it applies here because 0x60
 *     is past `ldrb`'s 5-bit displacement. Do NOT reshape the symbol -- both
 *     spellings are per-.c casts onto the flat `u8 []` and rescale nothing. */
extern u8 gUnknown_02029ED8[];
/* Wave 50, W50-A.  gUnknown_02029F3C is 0x02029F3C == gUnknown_02029ED8 + 0x64,
 * i.e. offset 4 INSIDE the same `rec[]` element the comment above describes, and
 * aw2bhr.lds names it in its own right.  sub_080620FC writes a WORD there with
 * the SAME flat subscript it uses for rec[].x / rec[].y
 * (`n * 8 + a * 0x3e0 + gUnknown_030033EC * 0xc00`), the symbol added LAST and
 * no 0x64 displacement anywhere -- so the original source named this symbol
 * rather than reaching +0x64 through the record, and the record's 6 "pad" bytes
 * are really 2 bytes of padding plus a pointer.  The value stored is
 * `gUnknown_08499590 + rowOffset[y] + x + 0x193A`, a pointer into the map's
 * +0x193A plane, which is what fixes the element at pointer width.  Declared
 * `u8 []` and cast at the use, exactly like gUnknown_02029ED8 beside it: do NOT
 * give it an 8-byte element type, because the subscript is a BYTE offset that a
 * wider element would rescale. */
extern u8 gUnknown_02029F3C[];
extern u16 gUnknown_0202FDE8;
extern u16 gUnknown_0202FDEA;
/* Wave 43 (W43-F). aw2bhr.lds DOES name 0x0202FDEC, so this extern links; see
 * the layout note on struct Unk0202FDEC. sub_08038240 reaches it only through
 * the -fforce-addr pool word at 0x08090F00 (which holds 0x0202FDEC), so the
 * honest `gUnknown_0202FDEC.member` spelling emits the two-level read on its
 * own and the promotion carries "rodata": ["0x08090F00"]. */
extern struct Unk0202FDEC gUnknown_0202FDEC;
extern struct Unk0202FDFC gUnknown_0202FDFC;
/* NOT GLOBALS: gUnknown_081CC4D8 and gUnknown_081CC4DC are two consecutive
 * `-fforce-addr` address-constant words (addends 0 and 4) and BOTH hold
 * 0x0202FDFC, i.e. &gUnknown_0202FDFC -- dereferenced against baserom.gba,
 * which is the only thing that settles it (wave 28, W28-B). They are one
 * private copy per function: sub_08074BDC reads .unk00 through the first and
 * sub_08074C1C reads .unk02 through the second, so the two symbol names are a
 * splitter artefact and not two objects. Both functions match on the honest
 * spelling and need a `rodata` entry at promotion. */

/* -------------------------------------------------------------- IWRAM -- */

/* 16 entries: sub_08011B18 clears indices 0..15 and sub_08011B34 stops
 * scanning at gUnknown_03000040, the next symbol in the linker script. */
/* Wave 37 (W37-P1): the ROM word at 0x0808E518 holds 0x03000000. It is agbcc's
 * own -fforce-addr copy of THIS array's address, not a pointer global -- the
 * splitter names it gUnknown_0808E518 and sub_08011B5C reaches the array
 * through it with two `ldr`s. sub_08011B5C references the array four times
 * (read and write, in the peeled first iteration and in the rotated loop) and
 * gets the .rodata word; sub_08011B34 references it twice and gets the plain
 * `ldr rN, =gUnknown_03000000`. There is no object at 0x0808E518 to declare. */
/* Wave 40, W40-E: the ELEMENTS behave as `volatile` at one reader and the type
 * here has deliberately NOT been changed. sub_08011B98 tests a slot for null
 * and then calls it, and the ROM loads the slot TWICE while CSE-ing the
 * address across the branch; the plain spelling lets gcse fold the second load
 * away and is two bytes short. That function matches by binding a locally
 * `volatile`-qualified pointer (see its work/ comment and the wave-40 chapter
 * in docs/agbcc-codegen.md), which costs the other readers nothing.
 *   NOT PROVED, and that is why the declaration is untouched: one reader is a
 * single reader, sub_08011B18/B34/B5C are still asm, and adding the qualifier
 * here would change codegen at every one of them before any of them can
 * corroborate it. If a second reader turns up needing the same reload, promote
 * the qualifier onto this line and re-verify sub_08011B98. */
extern void *gUnknown_03000000[];
extern u16 gUnknown_03000040;
/* A u16 pair set together by sub_0801224C. Two separate pool words in that
 * function, so they are two scalars rather than one aggregate. */
extern u16 gUnknown_03000044;
extern u16 gUnknown_03000046;
/* The second of the two DMA/CpuFastSet descriptors -- sub_0801BB88 fills both
 * in one go, splitting the OAM shadow at object `a`: 03000278 gets
 * (shadow, OAM, 0, a) and 03000268 gets (shadow + a*8, OAM + a*8, a*8, 0x80-a).
 * Same shape as struct Unk03000278, hence the shared type. */
extern struct Unk03000278 gUnknown_03000268;
extern struct Unk03000278 gUnknown_03000278;
extern struct Unk03000288 gUnknown_03000288[];
/* Serial/link block, all four plain 32-bit words -- every access in the ROM is
 * `ldr`/`str`, and none of them is a bitfield: sub_0802EAFC clears the low two
 * bits of 0x03000564 with `movs #4; rsbs` (= ~3), which is only the bitfield
 * tell where a bare `movs` would have sufficed, and it would not here because
 * 0xfffffffc is not an 8-bit immediate. sub_0802ED00 reads those same two bits
 * as a plain `& 3` compared against 2, which settles it as a 2-bit enum living
 * in an ordinary word. Not const: sub_0802EAFC re-loads 0x03000564 for its
 * read-modify-write and 0x03000570 is stored through its own pool word.
 * Not volatile either: no dead load precedes any of the stores.
 *
 * CORRECTED, wave 13 (A2): 0x03000564 IS volatile. sub_0802EAFC could not see
 * it -- a lone read-modify-write is byte-identical either way -- but
 * sub_0802EB28 runs three of them back to back
 * (`&= ~3`, `|= 2`, then `& 0xC000`) and re-`ldr`s the word after every `str`.
 * A plain u32 folds all three into one load and one store and comes out eight
 * bytes shorter. c_0802EAFC.c is unaffected and still matches.
 */
/* A one-byte mode flag: sub_08022A08 clears it with `strb`, and the four
 * redraw wrappers at 0x08023DCC-0x08023EA4 read it back with `ldrb` and
 * compare against 1 to decide whether to run a third pass. Byte-wide in every
 * access in the ROM. */
extern u8 gUnknown_03000559;
extern u32 gUnknown_03000560;
extern volatile u32 gUnknown_03000564;
extern u32 gUnknown_03000568;
extern u32 gUnknown_03000570;
extern u32 gUnknown_03000574;
/* Wave 41, W41-D -- sub_0802F9BC's three tables, all u16 and all indexed 0..3,
 * one entry per link slot.
 *
 * gUnknown_03003128 is a per-slot read CURSOR into gUnknown_02025C18 and is
 * VOLATILE: sub_0802F9BC advances it with a bare `x++` and then masks it with
 * `x &= 0x3ff` as two separate statements, and BOTH carry the dead
 * `ldrh [r2]` immediately in front of the `strh` -- the same tell that made
 * every member of struct Unk0849B018 and struct Unk0849B01C volatile. A
 * non-volatile cell emits neither load, and folding the two statements into
 * one `x = (x + 1) & 0x3ff` loses the middle pair as well.
 *
 * gUnknown_03003F48 is the matching per-slot WRITE cursor: the function bails
 * as soon as any slot's read cursor has caught up with it, which is a
 * four-channel ring-buffer empty test. Plain `ldrh`, nothing writes it here,
 * so not volatile on this evidence.
 *   Wave 41 (W41-C): it IS volatile, and this is the write that shows it --
 * exactly the evidence the paragraph above was waiting for. sub_08030584 and
 * its two twins reset both cursors with `gUnknown_03003F48[i] =
 * gUnknown_03003128[i]` over i = 0..3, and each iteration is `ldrh` source,
 * DEAD `ldrh` of the destination, `strh` -- the same dead-load-before-store
 * tell that settled gUnknown_03003128 and every member of struct Unk0849B018.
 * Declared non-volatile the destination read disappears and the loop is one
 * instruction short. No promoted file read it before this wave, so nothing
 * needed re-verifying; sub_0802F9BC, the reader named above, was re-checked
 * anyway and still matches.
 *
 * gUnknown_02025C18 is the ring itself, and it is TWO-DIMENSIONAL, not flat:
 * the ROM computes `slot * 2` and `cursor * 8` as separate shifts and adds
 * them to the bare symbol, which is the `[cursor][slot]` address form. Written
 * flat as `[cursor * 4 + slot]` agbcc reassociates into
 * `((cursor * 4 + slot) * 2)` -- one `lsls` in the wrong place. Four
 * interleaved u16 channels; 0x400 rows, which is what the 0x3ff mask wraps.
 *
 * Wave 49 (W49-F): gUnknown_02025C18 is VOLATILE too, and this is the write
 * that shows it -- the same shape of evidence W41-C used for gUnknown_03003F48
 * one line up.  sub_0802F03C clears the whole ring with
 * `gUnknown_02025C18[j][i] = 0` over j = 0..0x3ff, and every iteration is a
 * dead `ldrh` of the destination immediately before the `strh`.  Declared
 * plain the load disappears and the inner loop is one instruction short.  The
 * `[][4]` shape and the element width are unchanged; the only promoted reader
 * (c_0802F9BC.c) is a single `ldrh` either way and was re-verified after the
 * edit. */
extern volatile u16 gUnknown_03003128[];
extern volatile u16 gUnknown_03003F48[];
extern volatile u16 gUnknown_02025C18[][4];
/* Wave 49 (W49-F), new.  A 512-entry VOLATILE u16 table sub_0802F03C clears in
 * one flat `*p++ = 0` walk of exactly 0x200 halfwords; every iteration is a
 * dead `ldrh` then the `strh`, the same tell that settled gUnknown_03003128.
 * Nothing in the tree reads it yet, so the extent is a floor and the
 * signedness is unsettled. */
extern volatile u16 gUnknown_02025818[];
/* Wave 49 (W49-F), new.  A SIX-WORD ROM TABLE OF POINTERS at 0x08090C80, and
 * the values are read straight out of baserom.gba rather than guessed -- this
 * is the wave-48 rule applied ("if a symbol is loaded and then immediately
 * dereferenced, read the ROM word before declaring anything"), and here the
 * words turn out to be genuine pointer objects, not -fforce-addr pool words:
 *
 *   0x08090C80 -> 0x0300410C   &gUnknown_0300410C   (send ring WRITE cursor)
 *   0x08090C84 -> 0x02025818    gUnknown_02025818   (the send ring)
 *   0x08090C88 -> 0x030040CC   &gUnknown_030040CC   (send ring READ cursor)
 *   0x08090C8C -> 0x03003128    gUnknown_03003128   (recv per-slot read cursors)
 *   0x08090C90 -> 0x03003F48    gUnknown_03003F48   (recv per-slot write cursors)
 *   0x08090C94 -> 0x02025C18    gUnknown_02025C18   (the recv ring)
 *
 * So each is a second, indirect NAME for an object that already has one, and
 * the element types below are taken from those objects and not re-derived.
 * sub_0802F588 reaches the send ring through C80/C84/C88 and then through the
 * bare symbols in the same function, which is what pins the correspondence
 * from the code side as well.  The pointers themselves are const (they sit in
 * ROM); the pointees keep the volatile their own declarations carry, and
 * C84's does discriminate -- sub_0802F588's stores through it each carry the
 * dead `ldrh`. */
extern volatile u16 *const gUnknown_08090C80;
extern volatile u16 *const gUnknown_08090C84;
extern u16 *const gUnknown_08090C88;
extern volatile u16 *const gUnknown_08090C8C;
extern volatile u16 *const gUnknown_08090C90;
extern volatile u16 (*const gUnknown_08090C94)[4];
/* Wave 49 (W49-F), new.  A word cell sub_0802F03C zeroes with a plain `str`
 * and NO dead load in front of it, which is what says it is not volatile --
 * every other cell that function touches carries one.  Word-wide off the
 * `str`; u32 is the weakest type that fits and no reader settles it further. */
extern u32 gUnknown_0300333C;
/* A cursor into gUnknown_08090C44, zeroed by sub_0802EA5C. VOLATILE: in
 * sub_0802EC64 the `++` stores the incremented word and then re-`ldr`s the very
 * same address to index the table, which a plain word cell CSEs away (two
 * instructions shorter). Word-wide: every access is `ldr`/`str`. */
extern volatile u32 gUnknown_03000578;
/* A pair of "current"/"requested" ids, both u16 and both compared against the
 * sentinel 0xFFFF (sub_0803B524, sub_0803B640, sub_0803B660). sub_0803B37C
 * resets both to 0xFFFF; sub_0803B5F4 resets only 0x030005CA.
 * Unsigned despite the `ldrsh` at the call sites -- those come from an (s16)
 * cast on the argument, and both are read `ldrh` for the 0xFFFF compare. */
extern u16 gUnknown_030005C8;
extern u16 gUnknown_030005CA;
/* Neighbours 0x030005C8 and 0x030005CA, all three u16 (ldrh). sub_0803B350
 * sets this one; sub_0803B37C sets it to 0x100 and the other two to 0xFFFF.
 * sub_0803B414 passes it as the third argument of sub_08071420 seven times. */
extern u16 gUnknown_030005CC;
/* Fourth of the run, and u16 like the other three: sub_0803B35C is the whole
 * of its evidence -- `strh r0, [=030005CE]` then an immediate `ldrh r2, [r2]`
 * of the same address to pass as sub_08071420's (MPlayVolumeControl) third
 * argument. The re-read is NOT a volatile tell: it is the ordinary reload of a
 * non-register global across a statement boundary, and the store is
 * byte-identical for int/u32/u16/s16. Wave 24. */
extern u16 gUnknown_030005CE;
/* SIGNED: sub_08057CA4 reads it back with `ldrsh`. Written 0 and 1 as flags
 * by sub_08057B2E and sub_08057BCC. */
extern s16 gUnknown_030005E8[];
/* Wave 48, W48-G. The two word-wide pairs sitting just below gUnknown_030005E8,
 * both filled by sub_08057AE8's two-iteration loop and both 16.16 FIXED POINT:
 * 030005D8[i] is `gUnknown_02029B78[i] << 16` (the current value, converted),
 * and 030005E0[i] is `(d << 16) / (d / 5 + 20)` where d is the s16 delta
 * `gUnknown_02029B78[i] - gUnknown_02029B7C[i]` (the per-frame step, a real
 * `__divsi3`). Word from the `str`; nothing constrains the signedness beyond
 * the divide being signed. sub_08057AE8 reaches both through agbcc's own
 * -fforce-addr constants at 0x081361A0 and 0x081361AC -- do NOT declare those
 * two addresses as objects; the ROM words merely HOLD these addresses, the
 * same trap gUnknown_081D943C/gUnknown_08499578 records above. */
extern int gUnknown_030005D8[];
extern int gUnknown_030005E0[];
extern u8 gUnknown_03000650[];
/* A relocated-code trampoline slot. sub_0808AD6C copies the 4-byte body of
 * sub_0808AD68 (`ldrb r0,[r0]; bx lr`) into a caller-supplied buffer and stores
 * `buffer + 1` here -- the +1 is the THUMB bit, so this is a function pointer,
 * not a data pointer. The signature is proved at the call site, not the setter:
 * sub_0808ADA4 does `ldr r1,[gUnknown_03000F6C]; bl _call_via_r1` with one
 * pointer argument in r0 and narrows the result with `lsls #24; lsrs #24`,
 * i.e. `u8 (*)(u8 *)`. (Per docs/agbcc-codegen.md a `str` setter is
 * byte-identical for a function pointer, a `void *` and a `u32`, so the
 * evidence is only ever at the reader.)
 */
extern u8 (*gUnknown_03000F6C)(u8 *);
/* 0x03000F68-0x03000F7C is ONE module: a driver for a single hardware timer,
 * chosen at registration time out of the four. Reading the four globals
 * together is what makes each of them obvious, so they are documented as a
 * set. sub_0808AC44 registers, sub_0808AC7C arms, sub_0808AC20 is the tick
 * and sub_0808AD24 disarms.
 *
 *   sub_0808AC44(u8 id, ...)  rejects id > 3, stores it in gUnknown_03000F70,
 *                             and computes gUnknown_03000F78 as
 *                             0x04000100 + id * 4 -- i.e. &REG_TMnCNT_L. It
 *                             also hands the caller &sub_0808AC20 as the tick.
 *   sub_0808AC7C(u8 slot)     saves REG_IME into gUnknown_03000F7C, masks
 *                             interrupts, stops the timer, loads the record at
 *                             gUnknown_03000F68[slot] into the countdown and
 *                             the two timer registers, sets REG_IE's timer bit
 *                             and acknowledges it in REG_IF (0x04000202).
 *   sub_0808AD24()            stops the timer, clears the same REG_IE bit and
 *                             restores REG_IME.
 */
/* A pointer to a table of SIX-byte records, indexed by a u8 slot id --
 * sub_0808AC7C computes the offset as `((id << 1) + id) << 1`, which is id*6
 * and nothing else. The record is exactly the three halfwords the arm routine
 * consumes, in order: +0x00 the frame countdown copied into
 * gUnknown_03000F72, +0x02 the TMnCNT_L reload, +0x04 the TMnCNT_H control
 * word. Not yet modelled as a struct because only one function reads it. */
extern u16 *gUnknown_03000F68;
/* The hardware timer index, 0-3, used both to scale the register address and
 * as the shift count for the timer's IRQ bit
 * (`REG_IE &= ~(INTR_FLAG_TIMER0 << gUnknown_03000F70)` in sub_0808AD24).
 *
 * VOLATILE, proved twice and independently:
 *   1. sub_0808AC44 does `strb r1,[r0]` and then `ldrb r0,[r0]` on the same
 *      address with nothing in between -- a read of a scalar global right
 *      after a store to it only reloads when the global is volatile, which is
 *      the store-forwarding rule in docs/agbcc-codegen.md. This one is
 *      conclusive on its own.
 *   2. In sub_0808AD24 the qualifier decides one instruction's position: a
 *      plain u8 emits `movs r1, #8` BEFORE the `ldrb`, because expand_binop
 *      force_reg's the shift's constant operand first and a non-volatile MEM
 *      is content to be sunk into the other operand slot. A volatile MEM
 *      cannot be, so its load lands at the read point and the ROM's
 *      `ldrb; movs #8; lsls` order falls out. No size change; see the
 *      "a volatile shift COUNT is loaded before the shifted constant" note in
 *      docs/agbcc-codegen.md, which lists the seven spellings ruled out.
 */
extern volatile u8 gUnknown_03000F70;
/* The frame countdown, loaded from gUnknown_03000F68[slot] by sub_0808AC7C and
 * ticked down by sub_0808AC20. VOLATILE: sub_0808AC20 reads it twice, once to
 * test `!= 0` and once to decrement, and agbcc CSEs the pair into a single
 * `ldrh` without the qualifier. */
extern volatile u16 gUnknown_03000F72;
/* The expiry flag: cleared by sub_0808AC7C when the timer is armed, set to 1
 * by sub_0808AC20 when gUnknown_03000F72 reaches zero. Both accesses are
 * `strb`, which per docs/agbcc-codegen.md constrains nothing, so u8 is the
 * weakest model rather than a proved width. */
extern u8 gUnknown_03000F74;
/* &REG_TMnCNT_L for the selected timer -- sub_0808AC44 builds it as
 * `0x04000100 + gUnknown_03000F70 * 4`, so it points into MMIO and the
 * elements are the two timer registers: [0] is TMnCNT_L, [1] is TMnCNT_H.
 *
 * Both users step the global itself rather than a local, which is what
 * produces the `str` write-backs around the stores: sub_0808AD24 disarms with
 * `*g++ = 0; *g-- = 0;` and sub_0808AC7C reloads with the same idiom. The
 * +/-2 step fixes the element width at 16 bits.
 *
 * `vu16 *` rather than `u16 *` because the target really is hardware; probed,
 * and it is byte-neutral in sub_0808AD24, so this is honesty rather than
 * evidence. */
extern vu16 *gUnknown_03000F78;
/* The saved REG_IME value: sub_0808AC7C stashes it before masking interrupts
 * and sub_0808AD24 restores it after clearing the timer's REG_IE bit.
 * `ldrh` at both ends, straight to and from REG_IME. */
extern u16 gUnknown_03000F7C;
/* A callback slot: sub_0801F4A4 stores &sub_0801F4B4 into it and nothing else
 * touches it yet, so the pointed-to signature is only as good as sub_0801F4B4's
 * own prototype in unknown-functions.h. Declared unprototyped on purpose.
 */
/* An IWRAM record whose ADDRESS is what gets passed around -- eleven functions
 * name it and every one of them loads a clean `.4byte gUnknown_030013D0` and
 * either hands it to a routine or reads a fixed offset off it. Offsets seen:
 *
 *   +0x00  a pointer (`ldr r0,[r5]`, then `ldrb [r0,#4]` / `strh [r0,#4]`)
 *   +0x08  read BOTH as s16 (`movs r1,#8; ldrsh r0,[r5,r1]`, sub_0802505C and
 *          sub_080250E8) and as a byte masked with 0x7f (`ldrb r1,[r5,#8]`)
 *   +0x0a  u16   +0x14  u16   +0x18  s16
 *
 * so at least 0x1a bytes are in use. NOT a scalar and not const (it is RAM and
 * sub_0802505C writes through +0x00). Two independent hints at the extent, and
 * they disagree, which is why nothing stronger is declared here:
 * sub_080252EC hands gUnknown_030013D0 and gUnknown_030013B0 to the same stub
 * one after the other, a 0x20 delta suggesting a 0x20-byte record; but
 * gUnknown_030013EC is a separate symbol only 0x1c in. sub_080250E8 calls
 * sub_0802505C twice, on this and on `*gUnknown_08090A40`, so whatever the
 * record is, there is more than one of them and one is reached by pointer.
 * `u8 []` is the weakest model that reproduces the clean pool word; widen it to
 * a struct when sub_0802505C or sub_080250E8 is matched. */
extern u8 gUnknown_030013D0[];
/* WAVE 35 (W35-C): the record's layout is now settled for +0x00..+0x14 and the
 * struct below is that model, derived from sub_08024DDC (MATCHED byte-for-byte
 * with it, first attempt) and corroborated by sub_08024E60. It is declared
 * here rather than locally in one .c because sub_08024DDC's prototype names it
 * and two units need to agree.
 *
 * +0x00 is a `struct UnitRecord *` -- sub_08024DDC reaches unk04_0 through it
 * as `ldr r0,[r4]; ldrb r1,[r0,#4]`, the SAME 7-bit HP field the
 * sub_08025D20 note in unknown-functions.h describes, and sub_08058A2C was
 * already read the same way (see the gUnknown_0816D948 note above). That is two
 * independent readers agreeing on the member type, which is what the brief asks
 * for before believing a byte-neutral type.
 *
 * Every one of +0x08/+0x0c/+0x0e/+0x12/+0x14 is s16 and that is MEASURED, not
 * assumed: sub_08024DDC reads all five with `movs rI,#K; ldrsh rD,[rB,rI]` (the
 * s16-OBJECT tell -- ldrsh has no immediate-offset form, hence the scratch) and
 * writes them with `strh`. A u16 member feeding the same signed multiply would
 * have given `ldrh` plus an explicit shift pair. +0x08 agrees with the +0x08
 * s16 already recorded for this record via sub_08058A2C.
 *
 * The gaps stay `filler_` because nothing this wave reaches them; +0x04 and
 * +0x0a are known u16 from the note above but are not needed here and are left
 * unsplit rather than guessed at. Extent is still open -- see the paragraph
 * above; this struct models the head of the record only. */
struct Unk030013D0 /* head only; extent unproved */
{
    /* 0x00 */ struct UnitRecord *unk00;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ s16 unk06;
    /* 0x08 */ s16 unk08;
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ s16 unk0c;
    /* 0x0e */ s16 unk0e;
    /* 0x10 */ s16 unk10;
    /* 0x12 */ s16 unk12;
    /* 0x14 */ s16 unk14;
    /* 0x16 */ u8 filler_16[2];
    /* 0x18 */ s16 unk18;
};
/* The second record of that same type, and the one that pins them as two
 * instances rather than one object: sub_08041B98 reads +0x18 as `ldrsh` and
 * +0x04 as `ldrh` through BOTH symbols, four instructions apart, with the same
 * displacements. sub_080252EC (wave 12) hands each of them in turn to the
 * sub_080252E8 stub -- gUnknown_030013D0 first, then this one, which is source
 * order and not address order. Same `u8 []` model and the same reason. */
extern u8 gUnknown_030013B0[];
/* Not a global: the `-fforce-addr` address-constant word for the symbol above,
 * and evidence that code-0801D390.s has a SECOND pool block besides the
 * documented 0x0809092C-0x08090C2x one. 0x0816D938-0x0816D9BC is 35 consecutive
 * fan-in-1 address words -- 0x08499590 appears nine times in it, alongside
 * 0x030040D8, 0x03004784 and 0x03003F2C -- bracketed by ordinary data
 * (0x04F504F8 above, 0x01010100 below). Declared as a pointer to the array's
 * element type so that `pp = &gUnknown_0816D948` supplies in the source the
 * extra level force-addr invented; sub_08058A2C reads +0x00 as a
 * `struct UnitRecord *` and +0x08/+0x12 as s16 through it. */
extern u8 *const gUnknown_0816D948;
extern void (*gUnknown_030013EC)();
/* VOLATILE -- wave 36 (W36-E). sub_0807A0C4 writes it and then tests
 * `(s16)gUnknown_03001400` in the very next statement, and the ROM RELOADS it
 * with `ldrh` instead of re-using the value it just stored; a non-volatile
 * global is store-forwarded there and the reload (plus the alignment word it
 * pulls in) is exactly the 4 bytes that draft was short. It is a BG-scroll
 * shadow -- sub_08012420 pushes it to REG_BG2VOFS -- so this puts it with its
 * neighbours gUnknown_03001FFC / gUnknown_03002020 / gUnknown_03002B28, which
 * hardware.h already declares volatile for the same reason. Every promoted
 * user touches it exactly once per function, which is why the qualifier was
 * invisible until a function read it twice. */
extern volatile u16 gUnknown_03001400;
/* SIGNED -- wave 37, W37-O2. Retyped u16 -> s16. sub_08019470 tests it
 * `!= 0` and the ROM reads it with the register-offset `movs r2,#0;
 * ldrsh r0,[r0,r2]`, which agbcc emits only for a signed HImode object: a
 * `(s16)` cast on a u16 global folds away entirely at a zero test (measured
 * with compile_probe -- the cast spelling gives a plain `ldrh`), so no cast
 * reproduces the sign-extending load. The only other users are the pair of
 * stores in src/decomp/c_08017E74.c (`= 1` / `= 0`), which are `strh` either
 * way; that file was re-verified with trymatch after the change. */
extern s16 gUnknown_03001404;
/* A byte buffer whose ADDRESS is what gets passed: family F059 hands it to
 * sub_0803CCB8, which forwards it to sub_0803CC84 as the destination of a
 * NUL-terminated copy out of gUnknown_020280C0[i].unk02 (0x11 bytes). Extent
 * unproved -- 0x11 is the most the one writer can produce, not a bound. */
extern u8 gUnknown_0200B204[];
/* VOLATILE, wave 40 (W40-F). sub_08019C40 opens
 * `gUnknown_03001FF8 = gUnknown_03001418 = 0;` and the ROM RE-READS the
 * halfword it has just stored (`strh r0,[r1]; ldrh r0,[r1]; strh r0,[r2]`)
 * to get the inner assignment's value. A non-volatile lvalue makes agbcc reuse
 * the constant 0 it just stored and drops that `ldrh` entirely, so the reload
 * is the volatile tell rather than a spelling choice -- writing the two stores
 * as separate statements does not reproduce it either, CSE folds the read.
 *
 * This is the same class as its neighbour gUnknown_03001FF8, already volatile:
 * both are BG scroll shadows that sub_08012420 pushes to BGxOFS.
 *
 * The other 22 promoted users read it at most once per statement, where
 * volatile is byte-neutral. The three that could plausibly have noticed were
 * re-verified with trymatch after this change and all three still match:
 * sub_0801258C (packs it as `gUnknown_03001FF8 | (gUnknown_03001418 << 16)`,
 * and that file's own note says its volatile qualifiers are load-bearing for
 * combine), sub_0802D918 (`gUnknown_03001418 = gUnknown_03001FF8 = 0;`, the
 * mirror of the chain that forced this change) and sub_0806B610 (`++`). The
 * remaining 19 are single stores of a constant. A full split build was not
 * run, so if a later sweep flags one of those, this is the change to look at
 * first. */
extern volatile u16 gUnknown_03001418;
/* VOLATILE: sub_08017EEC reads it twice -- once into gUnknown_030030A8 and once
 * for gUnknown_03001FF4 = it + 0x6f -- and the two named globals in between
 * cannot alias it, so plain agbcc CSEs the pair into one `ldrh`. The qualifier
 * keeps both loads and still emits `ldrh` rather than the `ldrsh` an s16 would.
 */
extern volatile u16 gUnknown_03001420;
/* At least 0x20 halfwords: sub_0801DA94 clears [0..0x1f] through a variable
 * index. SIGNED elements -- sub_0801DAB0 scans the same 0x20 with
 * `movs r5,#0; ldrsh r0,[r2,r5]`, which is the s16-object tell; the plain
 * `strh 0` writers (sub_0801DA94, sub_0801DAE8) cannot see the difference.
 * sub_0801DAE8 takes an s16 index and skips -1, so -1 is "no slot". */
extern s16 gUnknown_03001430[];
/* Wave 37, W37-Q3: the ROM words the disassembly calls gUnknown_0808E534 and
 * gUnknown_0808E538 BOTH hold 0x03001470 -- verified against baserom.gba -- so
 * they are two agbcc -fforce-addr address-constant copies of this array's
 * address, one per function, and not objects. Do not declare them. Naming
 * gUnknown_03001470 directly is what reproduces them; sub_08015D24 matches that
 * way and promotion carries `"rodata": ["0x0808E534"]`. Same reading as
 * gUnknown_08136030/_34 and gUnknown_08136090/_C8 recorded above. */
extern struct Unk03001470 gUnknown_03001470[];
/* Wave 35, W35-F: THREE -fforce-addr pool words point here or next door and
 * none of them is an object -- 0x0813606C and 0x08136070 both hold 0x03001FBC
 * (this symbol; sub_0804D0FC and sub_0804D4E8 each get a private copy), and
 * 0x08136074 holds 0x03001470 (gUnknown_03001470, used by sub_0804D5D8). All
 * three matched on the honest spelling with `trymatch` reporting the pool word
 * as "different symbols that resolve to the same address". */
extern s16 gUnknown_03001FBC;
/* gUnknown_03001FDC is DELIBERATELY NOT DECLARED HERE, and must not be.
 *
 * It is `s32 IWRAM_DATA gUnknown_03001FDC;` in src/proc.c -- upstream's own
 * source, which sits in BOTH builds. That is a TENTATIVE definition carrying a
 * section attribute. src/proc.c includes this header, so a plain `extern int
 * gUnknown_03001FDC;` here is seen FIRST and agbcc drops the section attribute
 * from the later definition: the symbol stops being placed at 0x03001FDC by
 * `src/proc.o(.bss)` in the linker script and becomes a COMMON symbol, which
 * the linker parks in a stray .bss at 0x08800000.
 *
 * Wave 32 added the declaration and the split build produced a ROM differing in
 * exactly three 4-byte pool words -- 0x08011210, and 0x0801C8F0 / 0x0801CB0C
 * INSIDE src/proc.o itself. Every per-function `trymatch` passed, because a
 * per-function check never links proc.o. The only symptom was the SHA.
 *
 * The rule this generalises to: `src/proc.c` and `src/title-screen.c` are
 * upstream's matching source and we do not edit them -- and DECLARING one of
 * their globals here is the same hazard, because this header reaches them.
 * A function needing that gate must be left unpromoted, or spell the address
 * without naming the symbol. */
/* Wave 32 (W32-B): the HBlank scanline cursor sub_08011228 walks. s16 rather
 * than u16 on sub_080114A0's evidence: it is read `ldrh` and then tested with
 * `lsls #0x10; cmp #0; bgt`, i.e. the SIGNED compare in the shifted domain that
 * c_080110A4.c records as agbcc's s16 idiom when the constant will not fit an
 * imm8. sub_08011298 seeds it with 0 and sub_0801137C with 0x140.
 *
 * The ROM word at 0x0808DF94 holds 0x03001408 -- it is agbcc's own -fforce-addr
 * copy of this symbol's address, not a global, which is why sub_080114A0
 * reaches it through one more `ldr` than sub_08011298 does. */
extern volatile s16 gUnknown_03001408;
/* Wave 32 (W32-B): the companion scroll accumulator, same s16 evidence
 * (`ldrh`; `lsls #0x10`; compared against a pooled 0x013F0000, i.e. 0x13f).
 * sub_080114A0 advances it by the proc's step and clamps it to 0x140. */
extern volatile s16 gUnknown_03002F3C;
/* A bare u16 counter, incremented (`ldrh; adds #1; strh`) by sub_080501DC on
 * one branch of a state test and never read there -- so its width is the load,
 * its signedness is unconstrained, and u16 is the weakest fit. */
extern u16 gUnknown_03004544;
/* An s16 counter reached only at +4, by the two functions of the 0x08063Bxx
 * pair: sub_08063BBC steps it with `ldrh; subs #1; strh` and sub_08063BE0 tests
 * it with `movs r1,#4; ldrsh r0,[r0,r1]` against 0. SIGNED on that `ldrsh`
 * (the register offset is ldrsh having no immediate form, not an address being
 * taken), and an ARRAY rather than a struct because +4 is the only offset any
 * reader touches -- `gUnknown_0202F0E8[2]` is the weakest spelling that fits and
 * it produces both forms. Nothing bounds the extent. */
extern s16 gUnknown_0202F0E8[];
extern u32 gUnknown_03001FD4;
extern u32 gUnknown_03001FE0;
/* Wave 29, W29-B: a ROM table of six 12-byte rows -- {graphics blob, palette,
 * base tile/palette index}. The stride is read off sub_0801F178's
 * `lsls #1; adds; lsls #2` (a multiply by 12) and the row count off
 * sub_0801F400's jump table, which has exactly six cases. That third member is
 * not a guess either: dumped from baserom.gba the six values are 0, 0x3e, 0x43,
 * 0xac, 0xb8 and 0xbc, which are precisely the constants sub_0801F400 returns
 * per index and the bounds sub_0801F3D4 compares against going the other way.
 * unk04 is `u16 *` because sub_0801F178 hands it straight to ApplyPaletteExt;
 * unk00 is untouched by anything matched, so only its width is known. */
struct Unk0848B738 /* 0x0c */
{
    /* 0x00 */ const void *unk00;
    /* 0x04 */ u16 *unk04;
    /* 0x08 */ int unk08;
};
extern const struct Unk0848B738 gUnknown_0848B738[];
/* Wave 33, W33-B: a 4-byte-stride table sub_0801F19C/sub_0801F234 index by a1;
 * unk00 and unk01 are a width/height pair multiplied together (tile count).
 * const ROM. */
struct Unk0848B780
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 filler_02[0x02];
};
extern const struct Unk0848B780 gUnknown_0848B780[];
/* Wave 38, W38-D: the `const` above is EVIDENTIALLY WRONG at at least one read
 * site and I have left it alone rather than change a type three other functions
 * share. sub_0801F2AC re-reads `gUnknown_0848B780[a1].unk00` and `.unk01` from
 * memory on EVERY iteration of two nested loops whose bodies `strh` through a
 * `u16 *` parameter. With the array `const` those loads carry RTX_UNCHANGING_P,
 * gcc 2.9's LICM proves the `strh` cannot alias them, hoists both out and
 * strength-reduces the inner loop to a countdown -- none of which the ROM did.
 * Casting the const away at the two read sites (`((struct Unk0848B780 *)
 * gUnknown_0848B780)[a1]`) reproduces the ROM's re-reads and its ascending inner
 * loop exactly, which is the controlled probe for it. So the original source
 * almost certainly declared this table WITHOUT const. I did not drop it here
 * because sub_0801F444 matched with the const spelling and the qualifier is
 * byte-neutral there; dropping it is the weakening direction and safe for every
 * caller, so a later wave that wants the honest spelling in sub_0801F2AC should
 * drop it and re-run trymatch on sub_0801F444 and sub_0801F34C. */
/* Wave 38, W38-D: a ROM table of OAM object-list blobs indexed by a sprite's
 * tile dimensions. sub_0801F34C reads it at
 * `gUnknown_0848B780[a1].unk01 * 17 + gUnknown_0848B780[a1].unk00` -- the
 * `lsls #4; adds` multiply by 17 and then `lsls #2` for the word stride -- and
 * hands the loaded word straight to PutSpriteExt's `u16 *` fourth parameter
 * with no dereference, which is what fixes the element type. The row width of
 * 17 is read off the multiply; nothing matched pins the row COUNT, so the
 * bound is left open. */
extern u16 *const gUnknown_0848BAE4[];
/* A word, written with `str` and never read by anything matched. Two writers,
 * both in the sub_08036C4C screen-setup family and both storing a constant --
 * 0xE28 here and 0xE29 in sub_08036C80, its byte-identical twin's neighbour.
 * `u32` on the store width alone (wave 20, W20-C). */
extern u32 gUnknown_030032CC;
/* Wave 49, W49-K. A SIX-BYTE per-army property tally scratch, and the first
 * evidence that an object exists at 0x030032D0 at all: nothing in `asm/` names
 * that address, so gen_lds.py never invented a symbol for it and upstream's
 * aw2bhr.lds had none either (0x030032CC and 0x030032D8 are both there). The
 * ROM word at 0x0809096C holds 0x030032D0 -- an -fforce-addr address constant
 * belonging to sub_08021810, which is its only reader. That function clears
 * [0..5] in one `i <= 5` loop, bumps [5] once per counted map cell and
 * [cell >> 5] beside it (so [1..4] are the four armies and [0] is the running
 * maximum it folds them into afterwards), and reads [5] and [0] back out.
 * Extent 6 exactly; the linker symbol was added to aw2bhr.lds to match. */
extern u8 gUnknown_030032D0[];
/* A sprite/animation descriptor blob: sub_080272C4 and sub_08027428 pass its
 * ADDRESS as sub_0801C210's first argument and nothing else names it. `u8 []`
 * for the same reason as the gUnknown_085533E4 family -- only the address is
 * used (wave 20, W20-C). */
extern u8 gUnknown_08112614[];
/* An OAM object counter. Every access in the ROM is 16-bit and there are only
 * four: sub_0801DF94 and sub_0801EFA8 zero it with `strh`, sub_0801EF6C zeroes
 * it alongside gUnknown_03002B54 = 0x10, and sub_0801EEnn bumps it with
 * `ldrh; adds #1; strh` inside the sprite-emit loop -- the read is the one that
 * fixes the width at 16 rather than merely "at least 16". Unsigned is not
 * proved: nothing sign-extends it, but nothing needs to, and it only ever
 * counts up from 0. */
/* VOLATILE, proved in wave 42 (W42-G) on sub_0801EE80. That function's guard
 * reads it and its `dst` expression reads it again with no store in between;
 * the ROM emits BOTH `ldrh`s, keeping only the ADDRESS in a register across the
 * compare. Non-volatile, CSE keeps the value instead and the second load
 * disappears -- measured with compile_probe, and it was the last difference in
 * an otherwise instruction-exact candidate. The four stores are bare constants
 * or the `ldrh; adds #1; strh` bump, all byte-identical either way
 * (sub_0801EF6C and sub_0801EFA8 re-verified after the change). Note this is a
 * different symbol from the volatile gUnknown_03002B54 beside it, and
 * sub_0801EF6C touching both is what keeps them distinct. */
extern volatile u16 gUnknown_03001FE4;
/* A callback, not a data word: sub_080198F0 does `ldr r0,[r0]; cmp r0,#0;
 * beq; bl _call_via_r0` and then tests the result with `lsls #24`. Cleared by
 * sub_080198C4 and sub_08017F0C; saved/restored alongside gUnknown_03002F20
 * by sub_080171B4 / sub_08017540. */
extern bool8 (*gUnknown_03001FF0)(void);
extern u16 gUnknown_03001FF4;
/* The BG0 x scroll shadow. VOLATILE, proved in sub_0801258C (wave 13): that
 * function packs it with the BG0 y shadow as `x | (y << 16)` into one word, and
 * the ROM accumulates the OR into the SHIFTED operand (`orrs r0, r1`) while a
 * plain read accumulates into the unshifted one (`orr r1, r1, r0`). combine
 * cannot fold a volatile MEM into the ior, so the commutative canonicalisation
 * that picks the destination goes the other way -- the same mechanism as the
 * REG_DISPSTAT_LO note in hardware.h, and the discriminator is free because it
 * costs no bytes. Every other promoted user is a single read or a bare
 * read-modify-write (sub_080122EC, sub_08012420, sub_080338C0, sub_080399D8,
 * sub_080453B0, sub_0804542C, sub_08084544, sub_0808A3A0) and is byte-identical
 * either way. */
extern volatile u16 gUnknown_03001FF8;
/* gUnknown_03002B6C is NOT declared here. It is `union BgCntBuf` in
 * include/hardware.h, which is upstream-owned and wins. Wave 41 (W41-B) added
 * an `extern u32` for it after sub_0802BE28/sub_0802BE80 read bits 3:2 with a
 * whole-word `ldr`; that broke EVERY compile in the repo, promoted files
 * included, because the two declarations conflict. The union is right on the
 * evidence: c_0800572C.c and c_08005874.c already consume it as
 * `gUnknown_03002B6C.bits.chr_block << 14`, which a scalar cannot express, and
 * the `ldr` is just the whole-union load. Readers that want the bitfield
 * include hardware.h. See this file's header note -- this symbol has now had
 * four different types proposed, and it is the repeat offender the note warns
 * about. */
/* Wave 41, W41-B. The wipe-transition tile source, handed to Decompress by
 * sub_0802BE28 and sub_0802BE80 as the bare symbol. Non-const for the same
 * reason as the other Decompress blobs -- the parameter is a plain `u8 *`. */
extern u8 gUnknown_0810BDC0[];
extern u16 gUnknown_03002000;
extern u16 gUnknown_0300200C;
extern struct Unk03002040 gUnknown_03002040;
/* Wave 32 (W32-B): the three-way "last input" latch sub_08064474 keeps beside
 * the frame counter. gUnknown_030005FC holds the frame the last call was made
 * on -- word-wide, compared against `gUnknown_03004008 - 1` and then assigned
 * from it -- and the s16 pair at 0x03000600/0x03000602 is the coordinate it was
 * made at, read with `movs rN,#0; ldrsh` and written with `strh`. When the
 * previous call was on the immediately preceding frame the new coordinate is
 * averaged with the stored one, which is what makes the pair a history rather
 * than a scratch. Reached through agbcc's own -fforce-addr words at
 * 0x0816E0B0 and 0x0816E0B4 (the ROM words there are 0x03000600 and
 * 0x03000602), so naming them directly gives the ROM's two-level read. */
extern s32 gUnknown_030005FC;
/* 0x03000600 and 0x03000602 CANNOT BE DECLARED and were removed in wave 32.
 * aw2bhr.lds names gUnknown_030005FC and then gUnknown_03000604, so both
 * addresses fall INSIDE the 8-byte gUnknown_030005FC region. gen_lds passes the
 * RAM symbol table through from that untouched upstream script, so a symbol
 * invented here has nowhere to live: sub_08064474 verified byte-for-byte with
 * `trymatch` and then failed the split build with `undefined reference`.
 * To promote a function that reads them, spell them as offsets into
 * gUnknown_030005FC (+4 and +6) -- do not re-add these two lines.
 * tools/proto_check.py now flags this class. */
extern struct Unk030020A8 gUnknown_030020A8;
/* gUnknown_030020B4 is the REG_DISPSTAT shadow -- declared in hardware.h */
extern u8 gUnknown_030020B8;
extern u8 gUnknown_030024E4;
/* 32 slot-in-use flags. VOLATILE: sub_0801DF94, sub_0801E13C and sub_0801E17C
 * all emit a dead `ldrb` of the element's own address immediately around the
 * `strb` -- the aggregate-element volatile tell in docs/agbcc-codegen.md. */
extern volatile u8 gUnknown_030024F0[];
/* A small mode enum, 0/1/2. sub_08013D40 and sub_08014BB4 clear it,
 * sub_08014468 sets it to 1, sub_080145BC resets 2 -> 0. */
extern u8 gUnknown_03002514;
/* gUnknown_0300251C is the fourth BG-control shadow (REG_BG3CNT) and is
 * declared in hardware.h with the other three -- it needs union BgCntBuf.
 * It was a bare u16 until wave 13: sub_08065990 and sub_0806D944 both read
 * `gUnknown_0300251C.bits.chr_block` to build a BG char base address, with the
 * `ldr` + `lsls #0x1c; lsrs #0x1e` pair that a bitfield read gives whatever
 * the container -- and they do the identical thing to gUnknown_030030B4, which
 * was already a union, three instructions later. The three scalar writers keep
 * their bytes exactly because they now spell it `*(u16 *)&gUnknown_0300251C`,
 * which is what c_080122EC.c and c_080573F0.c already did for the other
 * three shadows on the lines immediately above. */
extern u16 gUnknown_03002520[];
/* An OAM shadow: sub_0801EFD8 CpuFastSets 0x380 bytes of it to 0x07000080
 * through sub_08011C90, i.e. OAM entries 4..127 (0x400 - 0x80 == 0x380). The
 * 0x380 is the copy length and not a proved extent, and nothing dereferences
 * an entry yet, so `u8 []` is the weakest model that gives the clean pool
 * word. */
extern u8 gUnknown_030025A0[];
/* Paired with gUnknown_030030D0 -- an x/y scroll origin subtracted from
 * gUnknown_03001418 / gUnknown_03001FF8 when sub_08012420 writes BGxHOFS.
 * Always ldrh/strh. Cleared together by sub_08013324/sub_08013388/sub_0801339C.
 *
 * VOLATILE, and this pair is where it is proved: sub_08012420 subtracts each
 * of them from four different globals in eight consecutive straight-line
 * statements, and RELOADS the subtrahend every time while keeping its address
 * in a callee-saved register. Nothing between the reads writes IWRAM, and
 * agbcc's alias analysis proves a `strh` to an absolute 0x040000xx address
 * cannot touch a symbol, so a non-volatile read is CSEd to one `ldrh` and the
 * function is eight instructions short. The three promoted clearers store a
 * bare constant and are byte-identical either way (re-verified). */
extern volatile u16 gUnknown_03002B20;
extern struct SpriteEntry *gUnknown_03002B24;
/* A 0/1 toggle: sub_080129B4 flips it with `1 - x` and pushes the result into
 * REG_BLDCNT's effect field. volatile because that function re-reads it after
 * the store instead of reusing the value it just computed -- GCC is otherwise
 * free to forward the untruncated `1 - x`, since every consumer is a 16-bit
 * store, and the reload disappears. */
extern volatile u16 gUnknown_03002B2C;
extern u8 gUnknown_03002B30;
/* The BG1 x scroll shadow, and the twin of gUnknown_03001FF8 above -- volatile
 * for the same reason and proved the same way, by the `orrs r0, r1` in
 * sub_0801258C's `gUnknown_03002B3C = gUnknown_03002B34 |
 * (gUnknown_03002F18 << 16)`. Its y partner gUnknown_03002F18 was already
 * volatile on independent evidence. */
extern volatile u16 gUnknown_03002B34;
/* SIGNED: the getter sub_08017988 reads it with `ldrsh`. Set to 1 by
 * sub_08017970 and to 0 by sub_0801797C. */
extern s16 gUnknown_03002B38;
extern u8 gUnknown_03002B40;
extern u8 gUnknown_03002B44;
extern u8 gUnknown_03002B4C;
/* Always ldrh/strh. sub_0801DF94 sets it to 0x10, sub_0801E0F0 clears it,
 * sub_0801E0BC returns it -- an OAM-shadow write cursor. */
/* The OAM cursor the sprite-emit path advances. VOLATILE, proved in wave 42
 * (W42-G) on sub_0801DFE8 with two independent tells that a non-volatile u16
 * cannot produce, both measured with compile_probe:
 *   - Read into sub_0801E9B0's `s16` first parameter, the ROM emits
 *     `ldrh; lsls #16; asrs #16`. Non-volatile, combine folds the load into the
 *     sign_extend and gives `movs rN,#0; ldrsh` instead (2 bytes short). A
 *     volatile MEM cannot be folded, so the shifts survive. This is NOT the
 *     wave-37 `(s16)`-cast pattern -- the cast folds too; only volatile blocks
 *     it.
 *   - In `gUnknown_03002B54 = gUnknown_03002B54 + *p;` the ROM loads THIS
 *     symbol first and `*p` second. Non-volatile, agbcc emits `*p` first, and
 *     `+=` versus the spelled-out form are byte-identical (so the wave-40 note
 *     on sub_0801EE10 blaming the operand order on `+=` is wrong -- measured).
 * The three promoted clearers store a bare constant and sub_0801E0BC's read is
 * a lone `ldrh`; all four are byte-identical either way (re-verified). */
extern volatile u16 gUnknown_03002B54;
/* A 0/1 phase flag, always ldrh/strh. Six users test it and then write the
 * value they did not see (sub_08011050, sub_080110E8, sub_08011290 and
 * friends): a "switch to state N, or bail out if already there" guard. */
extern u16 gUnknown_03002B5C;
extern u8 gUnknown_03002B68;
extern struct Unk03002B80 gUnknown_03002B80;
/* Always ldrh/strh, compared against 0 and 1. sub_080199C4 clears it,
 * sub_080199D0 sets it from a u8 argument, sub_08019674 copies a u16 into it. */
extern u16 gUnknown_03002EE4;
extern u8 gUnknown_03002EFC;
extern u16 gUnknown_03002F00;
/* volatile is load-bearing: sub_0804BA4C ends with a self-assignment
 * (`ldrh r0,[r1]; strh r0,[r1]` on one address, nothing else), and agbcc drops
 * `g = g` entirely on a plain object -- the whole store disappears from the
 * emitted code. Only a volatile lvalue keeps both halves. The other user,
 * sub_080122EC's `gUnknown_03002F18 = 0`, is a bare scalar store and is
 * byte-identical either way (verified with trymatch after the change). */
extern volatile u16 gUnknown_03002F18;
extern struct Unk03002F08 gUnknown_03002F08;
/* Wave 28, W28-A. A halfword selector, and VOLATILE -- measured, not guessed.
 * sub_080184A4's only access is `ldrh` followed by `lsls #0x10; asrs #0xe`,
 * i.e. a cast to s16 fused with the scale by 4 that indexes gUnknown_0848A370[].
 * A plain `u16` global written `(s16)g` does NOT produce that: combine folds the
 * zero-extending load and the sign-extension together into `mov rN,#0;
 * ldrsh r0,[r0,rN]; lsl r0,#2` (probed both ways). `volatile` forces the load to
 * stay exactly one `ldrh`, leaving the sign-extension as its own shift pair for
 * combine to merge with the scale instead -- byte-for-byte the ROM. This is a
 * SECOND volatile tell alongside the broken-absolute-address-chain one in
 * docs/agbcc-codegen.md, and it costs zero bytes rather than four. */
extern volatile u16 gUnknown_03002F90;
/* A 0/1 flag, always ldrh/strh. sub_08013028, sub_080160DC and sub_0803BF70
 * set it, sub_08016094 tests it and clears it.
 * What it selects, now that sub_08016094 is matched: it is a ONE-SHOT
 * "the next command is a jump" flag on the gUnknown_03001470 script
 * interpreter. sub_08016094 is byte-for-byte sub_080160DC (which sets the flag
 * and steps the cursor 8 bytes on) with an `if` wrapped round it -- flag clear,
 * step 8; flag set, clear the flag and load the cursor from the word the
 * current command points at. Plain `u16`, not volatile: nothing between the
 * `ldrh` and the `strh` could alias it and the ROM does not reload it. */
extern u16 gUnknown_03002F1C;
/* A callback like gUnknown_03001FF0: sub_080183C0 null-checks it and calls it
 * through `bl _call_via_r0`, discarding the result. Set by sub_080198A0 and by
 * sub_08018B18 (from a list node), cleared by sub_080198AC. */
extern void (*gUnknown_03002F20)(void);
/* The stack cursor for gUnknown_03002F50 below. */
extern struct Unk03002F50 *gUnknown_03002F24;
/* u16 object, read back as s16: sub_08011BC4 is `ldrh; lsls #16; asrs #16`.
 * volatile is load-bearing -- without it agbcc folds the load and the
 * sign-extension into one `ldrsh`, and an `s16` declaration gives the same
 * `ldrsh`. Only a volatile u16 keeps the plain `ldrh` plus the shift pair. */
extern volatile u16 gUnknown_03002F30;
extern struct Unk03002F50 gUnknown_03002F50[];
extern void *gUnknown_03002FA0[];
/* 0x40 bytes = 16 entries. The IRQ handler table: crt0.s indexes it by the
 * interrupt's word offset and `bx`es to the entry; sub_0801BAE0 fills entries
 * 0..14 with sub_0801BAB8. sub_0801BB00(index, handler) is the setter. */
extern void *gUnknown_03002FE0[];
/* Wave 40 (W40-G), from sub_0801BABC -- the IRQ bring-up that owns the table
 * above. It fills entries 0..14 with sub_0801BAB8 (a bare `bx lr`, i.e. the
 * do-nothing default handler), then
 * `CpuFastSet(IrqMain, gUnknown_03000068, 0x40)` and
 * `gUnknown_0200BFFC = gUnknown_03000068`.
 *   0x03000068 is therefore a 0x100-byte IWRAM landing zone holding a COPY of
 * crt0.s's ARM IrqMain (0x40 words), and 0x0200BFFC is the word the copy's
 * address is published in. `u8 []` and `void *` are the weakest spellings that
 * reproduce the bare `ldr`/`str` and convert to CpuFastSet's parameters without
 * a cast; nothing here reads either one back, so neither element type nor
 * extent is measured beyond the 0x40-word count.
 *   0x0200BFFC OVERLAPPING gUnknown_0200BC14 is expected, not a bug -- see the
 * note on that symbol and the aw2bhr.lds chapter in docs/agbcc-codegen.md.
 *   The fill loop is written ASCENDING (`for (i = 0; i < 15; i++)`) even though
 * the ROM walks the table downwards and ends `subs r0,#4; cmp r0,r1; bge`:
 * probed both ways, and the ascending source is what agbcc's check_dbra_loop
 * turns into exactly those bytes. The descending spelling keeps a separate
 * counter and is 4 bytes longer. */
extern u8 gUnknown_03000068[];
extern void *gUnknown_0200BFFC;
/* Word-element array indexed by a u16 argument (sub_08010EE8 does
 * `lsls #16; lsrs #14`, i.e. (u16)i * 4). */
extern void *gUnknown_03003050[];
extern u16 gUnknown_030030A0;
/* VOLATILE (wave 33, W33-D). sub_08017880 reads it FOUR times with no call in
 * between -- the two `0x2f - (s16)g` / `0x2c - (s16)g` comparison sites and
 * both BG*VOFS writes -- and agbcc CSEs none of them, which a plain global
 * cannot produce in straight-line code. The volatile MEM is also what stops
 * combine folding `(s16)g` into a single sign-extending load: the ROM has the
 * unfolded `ldrh; lsls #0x10; asrs #0x10` at both comparison sites, where a
 * non-volatile u16 gives `movs r0,#0; ldrsh`. Its only promoted writer,
 * src/decomp/c_08017EEC.c, is byte-identical either way (re-verified). */
extern volatile u16 gUnknown_030030A8;
extern u16 gUnknown_030030C4;
/* The y half of the scroll origin pair whose x half is gUnknown_03002B20 --
 * volatile for the reason recorded there. */
extern volatile u16 gUnknown_030030D0;
extern volatile u16 gUnknown_030030E8;
extern struct Unk030030F0 gUnknown_030030F0;
extern union Unk802C57CBuf gUnknown_03003100;
/* At least 3 bytes: sub_08035568 clears [0..2] through a variable u16 index
 * (`cmp #2; bls`), so this is an array rather than a struct. `strb`, so the
 * element is byte-sized; nothing has read one yet, so the signedness is open. */
extern u8 gUnknown_03003124[];
extern struct Unk03003130 gUnknown_03003130;
/* Wave 39 (W39-E), from sub_08028580. A 0xFF-terminated list of at most 0x5c
 * four-byte entries. Byte 0 packs two fields -- `& 0xe0` selects the owning
 * army (compared against gUnknown_084995F4[army]) and `& 0x1f` a terrain code,
 * with 0xFF terminating -- and bytes 1 and 2 are the map x and y handed to
 * sub_080240B4 / sub_0802419C. Byte 3 is never read here.
 *
 * IT MUST BE A STRUCT, not the flat `u8 [][4]` this declaration started as, and
 * the difference is byte-visible: the ROM forms the element address ONCE
 * (`lsls #2; add base`) and takes the three fields off it as `ldrb [r, #0]` /
 * `[r, #1]` / `[r, #2]` displacements. Only a COMPONENT_REF preserves that --
 * the flat array spells each field as its own address expression, and agbcc
 * answers with separate `gUnknown_03003150 + 1` and `+ 2` force-addr pool words
 * plus enough extra register pressure to spill a value the ROM keeps in a
 * register. Same rule as the map header in src/decomp/c_08024058.c. */
struct Unk03003150 /* 0x04 */
{
    /* 0x00 */ u8 flags;      /* `& 0xe0` selects the owning army and is compared
                               * against gUnknown_084995F4[army]; `& 0x1f` is a
                               * terrain code, with 8 distinguished; 0xFF
                               * terminates the list. Plain masks off a `ldrb`,
                               * not bitfields -- both are extracted with an
                               * `ands` against a register-held constant. */
    /* 0x01 */ u8 x;          /* map column, passed to sub_080240B4's s16 first
                               * parameter with no narrowing in between */
    /* 0x02 */ u8 y;          /* map row; `lsls #1` indexes rowOffset[] at
                               * +0x417A, the c_08001158.c idiom */
    /* 0x03 */ u8 filler_03[0x01];
};
extern struct Unk03003150 gUnknown_03003150[];
extern u16 gUnknown_030032C0;
extern struct Unk802C57C gUnknown_030032C4;
extern u16 gUnknown_030032D8;
/* Halfword; sub_0802C480 zeroes it with `strh` on every call, unconditionally
 * after its guarded coordinate update. */
extern u16 gUnknown_03003334;
/* Wave 38 (W38-K). Two more -fforce-addr words for this symbol, both
 * dereferenced in baserom.gba and both confirmed by matching their owning
 * function byte-for-byte with the honest spelling (`relocs: different symbols
 * that resolve to the same address`):
 *   0x08091310 holds 0x03003338   -> sub_080415E4's word for gUnknown_03003338
 *   0x08091314 holds 0x03003338   -> sub_080416A4's word for the same
 * The splitter invents gUnknown_08091310 / gUnknown_08091314 as though objects
 * lived there; they do not. Two consecutive slots holding the IDENTICAL address
 * in two adjacent functions is the documented one-word-per-(function, symbol)
 * pattern. Their neighbours 0x08091300-0x0809130C hold 0x08499590, 0x08499598,
 * 0x08499590, 0x08499590, so the run 0x08091300-0x08091314 is one unit's pool.
 * Both functions read the global THREE levels deep -- `ldr rN,=<word>;
 * ldr rM,[rN]; ldr rK,[rM]` -- and reload it after the loop for a pointer
 * subtraction, which is what keeps the word's address in a callee-saved reg. */
extern struct Unk03003338 *gUnknown_03003338;
/* A row-pointer table for the gUnknown_08499590 screen: sub_0801F838 fills
 * gUnknown_03003340[y][x] for y < height (+2) and x < width (+0) with a u8.
 * Word elements (`lsls #2` on the index) each holding a byte pointer, and the
 * symbol address is added directly, so this is an array and not a pointer. */
/* Wave 41, W41-B: the ELEMENTS carry a negative sentinel. sub_08020020 reads
 * every cell with `ldrsb` and tests `< 0` (six sites), so that reader casts the
 * row to `(s8 *)`. The declaration is deliberately left as `u8 *` -- the writer
 * sub_0801F838 stores a u8 and the two are consistent; the sign is a property
 * of one reader's access, not of the table.
 *   Wave 51, W51-M corroborates it with a second reader and leaves the
 * declaration alone again: sub_08059050 reads one cell with `ldrsb` and rejects
 * it when negative. The `(s8)` cast is not optional there for a second reason
 * -- under -Werror agbcc refuses `g[y][x] >= 0` on the unsigned element
 * ("comparison is always true due to limited range of data type"), which is
 * itself the tell that the original source declared the rows signed. */
extern u8 *gUnknown_03003340[];
/* Wave 36, W36-H, all five read off sub_0801F6F0 (the movement-range flood
 * fill's per-neighbour step) and all five DEREFERENCED IN baserom.gba first,
 * because three of the six symbols the splitter handed that function are
 * -fforce-addr words and not objects:
 *   0x0809092C holds 0x084999C8   -> the force-addr word for gUnknown_084999C8
 *   0x08090930 holds 0x03003340   -> the force-addr word for gUnknown_03003340
 *   0x08090928 holds 0x0300409C   -> the force-addr word for gUnknown_0300409C
 * Wave 38 (W38-G) extends the same run two slots further, by the address-order
 * interpolation the `.LC` block note further down describes, and confirms both
 * by matching the owning function byte-for-byte with the honest spelling:
 *   0x08090934 holds 0x08499590   -> gUnknown_08499590's word for sub_0801F92C
 *   0x08090938 holds 0x08499590   -> the same, for sub_0801FD30
 * Two consecutive slots holding the IDENTICAL address in two adjacent
 * functions is the documented one-word-per-(function, symbol) pattern, not a
 * table -- and neither symbol needed declaring: writing `gUnknown_08499590`
 * plainly produced the ROM's `ldr rN,=<word>; ldr rM,[rN]; ldr rK,[rM]` chain
 * exactly, with `relocs: different symbols that resolve to the same address`.
 * Contrast gUnknown_080912FC below, where naming the word IS required; the
 * discriminator is whether the ROM materialises the address at a point the
 * honest spelling's lazy single materialisation cannot reach.
 *
 * The u8 width/height pair at gUnknown_084999C8 +0x28/+0x29 now has a PRODUCER
 * as well as the flood fill's consumer: sub_0801F92C stores the map's +0x00 and
 * +0x02 halfwords into them (`adds rN,#0x28` / `#0x29` then `strb`, since
 * strb's immediate displacement stops at 31). Two functions written apart
 * agreeing on the same two offsets is what settles them as u8 rather than as
 * the low halves of something wider.
 *
 * gUnknown_084999C8->unk00, the 0x20-byte cost table, likewise: sub_0801F888
 * fills all 32 entries from
 * `gUnknown_085D3DD0[..].unk38[..].unk18[..][gUnknown_085D5ABC[t].unk19 * 32]`,
 * the same signed per-terrain movement-cost row c_08041EA8.c reads per-cell.
 * So the fill's costs are s8 values stored into a u8 array -- the `ldrb` in the
 * copy is the store's truncation, not evidence the source row is unsigned.
 * The honest spelling names the object; the ROM's THREE-deep load chain
 * (`ldr rN,=word; ldr rM,[rN]; ldr rK,[rM]`) is what force-addr on a pointer
 * object looks like and is NOT evidence of a pointer-to-pointer. Only
 * gUnknown_0300409C appears twice in one function via its own inline pool word,
 * which is the documented single-reference case.
 *
 * gUnknown_084999C8 is a ROM word holding 0x03003400, i.e. the state block the
 * fill works against: a 0x20-byte cost table at +0, `s16` bounds at +0x20 and
 * +0x22, a 4-entry mask table at +0x24 indexed by the unit plane's TOP TWO BITS
 * and AND-ed with the byte at +0x2a, and u8 width/height at +0x28/+0x29. The
 * two s16s are `ldrsh` in the callee's own body. Everything past those is
 * unproved filler.
 *
 * gUnknown_03003F64 is a WRITE CURSOR, not an array: sub_0801F6F0 stores four
 * bytes (x, y, its own u8 first argument, the accumulated cost) and then adds 4
 * to the variable itself. It is re-read from memory before every one of those
 * stores, which is what pins it as a pointer variable the stores may alias.
 * gUnknown_030040E0 is that queue's length, `int` from the SIGNED `bgt` against
 * the 0x15C cap. */
/* Wave 49: extended from 2 bytes to 4. sub_0801F4B4 walks this queue with
 * `gUnknown_0300409C++` and the ROM's `adds r0, r2, #4`, so the element is 4
 * bytes -- which is also the record width sub_0801F6F0 WRITES through the
 * gUnknown_03003F64 cursor (x, y, tag, cost), i.e. the two cursors run over the
 * same record type. unk02 is that tag: sub_0801F4B4 switches on it over 1..5
 * with a jump table and clears it after each entry is consumed. Byte-neutral
 * for src/decomp/c_0801F6F0.c, the only other user, which does no pointer
 * arithmetic on it; re-verified by trymatch exit code. unk03 is the cost byte
 * sub_0801F6F0 stores fourth; nothing reads it back yet. */
struct Unk300409C
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 unk03;
};

struct Unk84999C8
{
    /* 0x00 */ u8 unk00[0x20];
    /* 0x20 */ s16 unk20;
    /* 0x22 */ s16 unk22;
    /* 0x24 */ u8 unk24[4];
    /* 0x28 */ u8 unk28;
    /* 0x29 */ u8 unk29;
    /* 0x2a */ u8 unk2a;
};

extern struct Unk300409C *gUnknown_0300409C;
extern struct Unk84999C8 *gUnknown_084999C8;
extern u8 *gUnknown_03003F64;
/* Wave 49: the flood fill's DOUBLE-BUFFER PARITY BIT. sub_0801F4B4 clears it,
 * then at the top of every pass tests it `!= 0` and swaps which of the two
 * queues at gUnknown_084999C8 +0x2c and +0x5A4 gUnknown_03003F64 writes and
 * gUnknown_0300409C reads, storing the opposite value back. Byte from the
 * plain `strb`/`ldrb`; it only ever holds 0 or 1, so the signedness is
 * unproved and u8 is the weakest model. sub_0801F4B4 reaches it through an
 * agbcc -fforce-addr pool word (the ROM word at 0x08090920 holds 0x03003FBC,
 * dereferenced from baserom.gba this wave) -- that word is NOT a global and
 * must not be declared; the honest spelling reproduces the two-level load. */
extern u8 gUnknown_03003FBC;
extern int gUnknown_030040E0;
extern struct Unk802C57C gUnknown_030033E0;
extern struct Unk802C57C gUnknown_030033E4;
/* Read `ldrh` in a dozen places across code.s and code-0806CFC8.s and stored
 * `strh`, so at least 16 bits; the bare `ldrb` readers (sub_08017658,
 * sub_0807A934) are u8-context reads of the same halfword.
 */
/* Wave 43, W43-A: at least one reader treats it as VOLATILE. sub_080350E4
 * loads it `ldr; ldrh` on one arm of an `if` and discards the value, which
 * -O2 would delete for a plain global. Do NOT add the qualifier here -- a
 * dozen matched functions read this global and requalifying it would change
 * all of them. Spell it at the use instead: `*(vu16 *)&gUnknown_030033EC;`
 * reproduces the two instructions exactly. See docs/agbcc-codegen.md. */
extern u16 gUnknown_030033EC;
/* The linker-visible spelling is retained for assembly callers; C uses the
 * semantic alias. It is the index of the army currently being processed. */
#define gCurrentArmyIndex gUnknown_030033EC
/* At least 4 bytes: sub_0803CB8C clears [0..3] and sub_0803CB74 tests bit
 * `id & 7` of byte `id >> 3` with a SIGNED shift, i.e. an int index.
 */
extern u8 gUnknown_030033F4[];
/* A word-sized state id. sub_08080FB8 tests it `== 6` with `ldr` and advances
 * it to 0xc, so it is a small enum held in a whole word rather than a flag. */
extern u32 gUnknown_030033FC;
/* A second cell for the same ROM record pointer gUnknown_03003338 holds:
 * sub_0803486C loads gUnknown_0849FE74[0] ONCE and `str`s the same register
 * into 0x03003338 and then into 0x03003F20. Word-sized, and nothing in this
 * tree reads it yet, so the type is inherited from its only writer.
 *
 * Wave 38 (W38-K): it now HAS readers, and they do not agree with the writer's
 * type. sub_0804151C and sub_08041758 (matched, byte-identical twins) walk this
 * buffer as 4-BYTE records -- `strb` at +0, `strb` at +1, `strh` at +2,
 * `adds r5,#4` -- terminated by a 0xFFFF halfword at +2 of the record past the
 * last, while struct Unk03003338 is 0x08 bytes of four halfwords and
 * sub_080415E4 / sub_080416A4 (also matched) walk the SAME allocation through
 * gUnknown_03003338 with `strh` at +0 and `adds r6,#8`.
 * The declaration is left as-is on purpose: the writer really does store the
 * Unk03003338 record pointer here, so this is one scratch buffer written with
 * two different record layouts depending on which list is being built, not a
 * wrong type. The two 4-byte readers cast to a local
 * `{ u8 x; u8 y; s16 v; }` in their own .c, which is byte-neutral. Do not
 * reshape struct Unk03003338 to accommodate them. */
extern struct Unk03003338 *gUnknown_03003F20;
extern union Unk802C57CBuf gUnknown_03003F24;
/* The base of the 0x40-wide unit-id window the 0x0805Cxxx list builders scan:
 * every one of them runs `for (i = g + 1; i < g + 0x40; i++)` over
 * gUnknown_08499594[i]. `ldrh` with no sign extension anywhere, and the `+ 1`
 * is the usual 1-based unit-id convention. */
/* Wave 48, W48-F. The base unit-slot index of the army whose turn it is: three
 * scans in the 0x0805A block walk `for (i = gUnknown_03003F2C; i <
 * gUnknown_03003F2C + 0x40; i++)` over gUnknown_08499594[i], which is that
 * array's 64-entries-per-army grouping. Re-read at the loop bottom, so not
 * volatile -- the reload is only because the body calls out.
 *   DO NOT DECLARE gUnknown_0816D994 OR gUnknown_0816D998. Both ROM words hold
 * 0x03003F2C: they are agbcc's own `-fforce-addr` address constants for THIS
 * symbol, one private copy per function, and gen_lds.py invents a name for each.
 * `ldr rA, =gUnknown_0816D99x; ldr rB, [rA]; ldrh rC, [rB]` is a plain read of
 * gUnknown_03003F2C. Spell the global honestly and let trymatch/promote place
 * the pool word (sub_0805A514 and sub_0805A5E0 both matched that way).
 * gUnknown_0816D9A0 -> gUnknown_030033EC and gUnknown_0816D9D0 ->
 * gUnknown_03003F20 are the same thing in the same range. */
extern u16 gUnknown_03003F2C;
/* Wave 43 (W43-G). A 5-byte scratch copy sub_0803D2F8 fills from its record
 * argument's +0x4C4..+0x4C8 with a `for (i = 0; i <= 4; i++)` byte loop
 * (`adds r0,r4,r5; adds r1,r3,r4; ldrb; strb`), the same five bytes
 * sub_0803D4A8 copies into struct Unk020280C0's +0x14 filler. Plain `strb`
 * off an unscaled index, so `u8 []`. The extent of FIVE is the loop bound,
 * a real bound and not a guess; aw2bhr.lds's next symbol is 0x03004002,
 * which only caps it at 0x0F. Nothing here proves the array is not longer
 * than the part this function writes. */
extern u8 gUnknown_03003FF3[];
/* Wave 37, W37-P3: the 0x0805Cxxx window above is the same one the 0x08045xxx
 * army scanners use, spelled `for (i = (u16)gUnknown_084995FE[gUnknown_03003F2C]
 * + 1; i < ... + 0x33; i++)` -- so this holds the ARMY NUMBER, not a unit id;
 * it is what indexes gUnknown_084995FE to get that army's base unit id.
 *   Beware its `data_refs`: sub_080459E4 and sub_08045A78 reach it through
 * agbcc -fforce-addr words at 0x0812A0F4 and 0x0812A0F8, and BOTH ROM words
 * contain 0x03003F2C. gen_lds.py invents `gUnknown_0812A0F4`/`gUnknown_0812A0F8`
 * at those addresses and the index lists them as data_refs, so the two functions
 * look like they touch two unknown ROM globals when they touch this one. Both
 * read it twice (preheader plus a bound recomputed at the bottom of a loop
 * containing a `Div` call); sub_08045924 reads it once and gets a plain
 * `ldr =gUnknown_03003F2C`. Honest spelling matched all three. */
/* At least 7 bytes: sub_0803BBD4 clears [0..6] through a variable index, so
 * this really is an array and not a struct. Elements are SIGNED --
 * sub_0803BB44/BB5C/BB74 and sub_0803B8E0/B904 all read one with
 * `ldrb; lsl #24; asr #24`. The getters at 0803BC7C/BC88/BC94 return the same
 * bytes zero-extended, i.e. as u8.
 * sub_0803BB44/BB5C/BB74 map elements 1/2/3 to 1 when zero and 2 otherwise,
 * and they return u8: sub_0803BB14 narrows sub_0803BB44's result with
 * `lsls #24; lsrs #24` before comparing it, which an int return would not
 * emit. Their own bodies cannot say so -- both return types are byte-identical
 * there, so the call site is the only evidence.
 */
extern s8 gUnknown_03003F30[];
extern int gUnknown_03003F40;
extern void *gUnknown_03003F68;
extern struct Unk0202575C gUnknown_0202575C;
/* Wave 30, W30-A. sub_08032048 is `gUnknown_03003F1C = gUnknown_030044C4 = 0;`
 * -- ONE chained assignment, proved by the pool order: gUnknown_03003F1C's
 * address is loaded first because the outer lvalue is evaluated before its
 * right-hand side, and two separate statements load gUnknown_030044C4 first
 * instead. The `ldrb` between the two `strb`s is old gcc re-reading a VOLATILE
 * lvalue for the value of the assignment expression; a plain u8 forwards the
 * stored 0 and emits no load at all. That settles the volatile on
 * gUnknown_030044C4 only -- gUnknown_03003F1C has exactly one writer and
 * nothing decides it either way.
 *
 * Wave 51, W51-B: it IS volatile, decided by its only READER. sub_080319AC
 * does `gUnknown_0849B018->unk09 != gUnknown_03003F1C` and the ROM spends a
 * third register on it (`ldrb r2,[r0,#9]; ldrb r0,[r1]; cmp r2,r0`); declared
 * plain u8 the read folds back into the address register it came through
 * (`ldrb r0,[r0,#9]; ldrb r1,[r1]; cmp r0,r1`) and the function misses by
 * exactly 3 bytes. All five promoted writers (sub_08030584, sub_08031948,
 * sub_08032048, sub_08032698, sub_08033030) were re-run with the qualifier on
 * and are unchanged, because `x = 0;` with the value discarded is a bare
 * `strb` either way.
 *
 * That also refutes the inference c_08031948.c used to carry, that the ABSENCE
 * of a dead `ldrb` before a `strb` proves NOT volatile. The dead load appears
 * only where the assignment's VALUE is consumed (a chained assignment, as in
 * sub_08032048); a store in statement position shows nothing either way. */
extern volatile u8 gUnknown_030044C4;
extern volatile u8 gUnknown_03003F1C;
/* Wave 30, W30-A. A halfword copied verbatim from gUnknown_030040CC by
 * sub_08031B30 and sub_08032104, both `ldrh` then `strh` and nothing else, so
 * the two are the same width and the signedness of neither is settled.
 *
 * Wave 49 (W49-F) ADDS the volatile to gUnknown_0300410C only.  sub_0802F03C
 * closes with `gUnknown_030040CC = gUnknown_0300410C = 0;` and the ROM stores
 * 0 to 0410C, RE-READS 0410C with an `ldrh`, and stores that into 040CC.  A
 * non-volatile cell yields the assigned constant instead and both stores come
 * off the same `movs r0,#0` -- one instruction short.  040CC takes no dead
 * load of its own, so it stays plain, and that asymmetry inside one statement
 * is what makes this discriminating rather than a style choice.
 *   Byte-neutral at the two existing readers (sub_08031B30, sub_08032104),
 * which are single `ldrh`s either way; both re-verified after the edit. */
extern volatile u16 gUnknown_0300410C;
extern u16 gUnknown_030040CC;
/* Wave 41 (W41-C), new. VOLATILE u8, and both halves are measured from
 * sub_08030768's closing statement
 * `gUnknown_03003F1C = gUnknown_030044C4 = gUnknown_030040AC = 0;`.
 *   Width: a bare `strb` of 0 followed by a `ldrb` of the same address.
 *   Volatile: that read-back is the tell. The chain's value is a compile-time
 * 0, so a non-volatile cell would let CSE reuse the register and emit three
 * `strb`s off one `movs r0,#0`; the ROM re-reads each link. gUnknown_030044C4
 * one line up is already volatile on the same grounds, and this is its
 * neighbour in the same chain. gUnknown_03003F1C is the tail of the chain and
 * is only ever STORED here, so this function says nothing about its
 * volatility -- it is left plain. */
extern volatile u8 gUnknown_030040AC;
extern struct Unk03003FC0 gUnknown_03003FC0;
/* The 0x08034350 screen's sub-state. Halfword: sub_08034350 dispatches on it
 * with a plain `ldrh` and sub_08034394 / sub_080343D8 write 4 and 0 with
 * `strh`. Nothing signs it, so u16 is the weakest model. */
/* Wave 32, W32-A. A whole-word `str` of 1 in sub_0802150C's reset run, sitting
 * between two halfword globals it is deliberately not grouped with. Width from
 * the store; nothing reads it yet, so the signedness is unconstrained. */
extern u32 gUnknown_03003F3C;
extern u16 gUnknown_03003F60;
/* At least 5 bytes. sub_08026340 counts the non-zero entries of [1..4] with a
 * u8 index, reaching them as `adds rI, rI, rBase` off the symbol address, so
 * this is an array and not a pointer. Index 0 is never touched -- the same
 * 1-based player-slot convention gUnknown_0810E6E0 uses. */
extern u8 gUnknown_03003FF8[];
/* gUnknown_03001FE8 IS NOT DECLARED HERE. It is `union BgCntBuf` in
 * include/hardware.h -- the IWRAM shadow of REG_BG1CNT -- and a wave-36
 * `extern u32` added at this spot broke every translation unit that includes
 * both headers with `conflicting types` (found and removed by W36-M). The
 * u32 reading was not wrong about the bits, only about the object: bits 2..3
 * scaled by 0x4000 is `gUnknown_03001FE8.bits.chr_block * 0x4000`, exactly the
 * VRAM character-base address c_08013C54.c already computes from it, and
 * c_08012420.c writes the same halfword straight into REG_BG1CNT. Ten promoted
 * files reach it through `.bits` / `.raw`. Include "hardware.h" and use a
 * member; if a whole-word `ldr` is really what the ROM does, that is the
 * adjacent REG_BG2CNT shadow being read with it, not a u32 here. */
extern u16 gUnknown_03004080;
/* Wave 36 (W36-L): the pair sub_080210C8 selects between on its `kind` switch.
 * Both are read `ldrh` with no sign extension and both scale the SAME way --
 * `* 1288` synthesised as *5, <<5, +1, <<3 -- to index a 0x508-byte record run
 * that starts at gUnknown_08499590 + 0x1E42. Cases 1 and 3 take the first,
 * case 2 the second; nothing matched writes either, so only the width is
 * proved. */
extern u16 gUnknown_03004070;
extern u16 gUnknown_03004088;
extern struct Unk802C57C gUnknown_03004090;
extern struct Unk802C57C gUnknown_030040A4;
/* Word cell; sub_0804138C clears it, sub_08041398 reads it. Callers keep
 * only the low halfword (sub_08029234 stores it with strh).
 */
extern u32 gUnknown_030040A8;
/* Two callback slots installed by sub_0803662C. sub_080366F4 calls
 * gUnknown_030040D0 with no arguments after a NULL test.
 */
extern void (*gUnknown_030040D0)(void);
/* Written once, in sub_0803C52C, with sub_08037DA4's 2..5 band result, by a
 * plain `str` -- so 32 bits wide, and nothing else in the ROM touches it, which
 * leaves it looking like a debug/cache cell rather than state anyone reads.
 * `int` rather than `u32` to agree with sub_08037DA4's signed return; the store
 * is byte-identical either way, so the signedness is inherited and not proved
 * here. */
extern int gUnknown_030040D4;
extern struct Unk030040D8 *gUnknown_030040D8;
/* A one-shot argument slot: sub_08042998 passes it to sub_08025B80 and clears
 * it in the same breath, reusing the zero it had just loaded from
 * gUnknown_03003FC0.unk0d. Plain `ldrb` / `strb`. */
extern u8 gUnknown_03004074;
/* A mode flag compared against 1. sub_0805C974 is the whole of
 * `if (gUnknown_030040DC == 1) return 1; return 0;` and is its only reader, so
 * the width rests on a single `ldrb` -- narrow by the "a lone ldrb constrains
 * the accessor, not the object" rule, and nothing else in the ROM touches it.
 * Signedness unproved. */
extern u8 gUnknown_030040DC;
/* A halfword flag. Every access found is a bare `strh` of 0 or 1
 * (sub_08035760 sets 1, sub_08035828 and sub_0803647C clear it), so the width
 * is proved to be at least 16 bits and u16 is the weakest model; nothing reads
 * it yet, so neither the signedness nor a wider type is settled.
 *
 * SETTLED, wave 13 (A2): the reader is sub_080345C8, and it is
 * `movs r1, #0; ldrsh r0, [r0, r1]` -- a SIGNED halfword load, which is what
 * s16 emits and what u16 (`ldrh`) does not. The three writers are unaffected;
 * a bare `strh` of 0 or 1 is identical either way. */
extern s16 gUnknown_030040E4;
/* SIGNED: sub_08034FC0 reads it back with `ldrsh` for its `!= 0` guard before
 * decrementing. sub_08034FB0 increments it. */
extern s16 gUnknown_030040E8;
extern void (*gUnknown_030040EC)(void);
/* Five bytes, one per player slot. sub_0803D6B8 fills [0..4] with the identity
 * permutation 0..4 (descending index loop); sub_0803D6D0 refills it from
 * gUnknown_08499598[i].unk1a and sub_0803D6FC from a caller-supplied buffer at
 * +0x4C4. All three write with `strb` through a variable index, so this is a
 * u8 array; no reader has settled the signedness. */
extern u8 gUnknown_030040F8[];
/* Wave 50, W50-L. Two IWRAM halfword tables read by sub_08037A78 as
 * `dst = (u16 *)a1 + gUnknown_03004010[x] + gUnknown_030032E0[y]` -- an
 * x-offset table indexed by the map column and a y-offset table indexed by the
 * map row, each scaled `lsls #1` into a halfword destination buffer. Both
 * addresses are bound by aw2bhr.lds under exactly these names (0x030032E0 at
 * lds:447, 0x03004010 at lds:487), so these externs name the objects and are
 * NOT the pool-word case. Element type is fixed at u16 by the bare `ldrh` at
 * every read; signedness is unproved -- the values are only ever added to a
 * pointer, so no read discriminates. */
extern u16 gUnknown_030032E0[];
extern u16 gUnknown_03004010[];
/* A word-wide flag set, tested a bit at a time. `s32` and not `u32` only
 * because src/title-screen.c already carries that spelling for it; every
 * access so far is `ldr` + a mask, so the signedness is not settled by
 * anything. sub_080338DC tests bit 0, sub_080369BC ANDs it with
 * gUnknown_030043F4, and title-screen.c tests bits 0 and 1.
 *
 * Wave 31, W31-A: the signedness IS now settled by TWO independent uses, and
 * it is UNSIGNED. sub_08022A6C shifts it right with `lsrs #2` before masking;
 * `s32 >> 2` is an arithmetic `asrs`, so that shift alone rules signed out.
 * sub_0806F064 divides it by a `u16` and the ROM calls __umodsi3; a plain
 * `s32 % u16` promotes to `int % int` and would call __modsi3, which is what
 * the SECOND division in that same function does. The declaration is left
 * `s32` because src/title-screen.c is upstream's own source and cannot be
 * gated per-function, so sub_0806F064 carries an explicit `(u32)` cast
 * instead. Every other reader masks before use, so retyping would have been
 * byte-neutral for them -- the cast and the retype are the same code here. */
extern s32 gUnknown_03004008;
/* A bitmask, ANDed against gUnknown_03004008 in sub_080369BC to gate a frame.
 * u16 (ldrh); cleared by sub_08036B28 and sub_08036B34. */
extern u16 gUnknown_030043F4;
extern struct Unk802C57C gUnknown_030044A4;
/* A whole word (ldr/str), set by sub_0803FF48 from its third argument and
 * stashed in the same breath at +0x54 of the proc it starts. sub_0803FF2C is
 * the Proc_ForEach filter that reads it back: it ends every 0849FB04 proc whose
 * own +0x54 does not equal this. So it is the "currently active" handle for
 * that proc family; nothing dereferences it, so int rather than a pointer. */
extern int gUnknown_030044D4;
/* A parking slot for gUnknown_030032D8: sub_08028CD8 moves 030032D8 here and
 * writes 0x10 in its place, sub_08034ED0 moves it back and zeroes this one.
 * ldrh/strh throughout, and sub_080346BC compares it against 0xe. */
extern u16 gUnknown_030044DC;
extern struct Unk030044E0 *gUnknown_030044E0;
extern struct Unk03004504 gUnknown_03004504;
/* Three u16s in the sprite-attribute block that sub_0804D928 / sub_0804E3B4
 * (and ~40 siblings between 0x0804B180 and 0x08053614) read with `ldrh`.
 * 0300453C is the one everything hangs off: it selects a row of
 * gUnknown_08551D0C, is XORed with 1 into OamData.hFlip, is XORed with
 * gUnknown_0300450C to index gUnknown_085523A4, and supplies the high byte of
 * the tile number. Small -- the hFlip use only reads bit 0. NOT volatile: the
 * pair keeps one loaded value live across three stores while reloading for
 * two others, which a volatile read could not do. 0300451C is copied straight
 * into gUnknown_03001470[i].unk34 and never inspected. */
extern u16 gUnknown_0300450C;
/* Wave 31, W31-B. A halfword frame counter for the 0x08053xxx cutscene player:
 * sub_0805316C increments it and breaks its proc at 0x12C, and sub_08054278,
 * sub_08054488, sub_08053FBC and sub_0805414C all compare it against a script
 * timestamp. `ldrh` / `strh` everywhere; no reader constrains the signedness. */
extern u16 gUnknown_03004508;
/* Wave 31, W31-B. The row of u16 script ids sub_08053F0C indexes with
 * gUnknown_0300450C on its way into gUnknown_08553734. Read `ldrh` off a bare
 * `lsls #1` from the symbol -- no load in front -- so the symbol IS the array. */
extern u16 gUnknown_030045A0[];
/* Wave 31, W31-B. A ROM table of NULLARY function pointers -- the cutscene
 * step handlers. sub_08053F0C picks one with
 * `gUnknown_030045A0[gUnknown_0300450C]` and falls back to element [2]
 * (`ldr r2, [r3, #8]`) when the 0x03004504 flags say so, then calls it through
 * `bl _call_via_r2`. Nothing sets up an argument at that call: the pointer
 * lands in r2 only because r0 and r1 were needed to evaluate the flag test
 * first, so the r2 index does NOT count arguments here. */
extern void (*const gUnknown_08553734[])(void);
/* Wave 31, W31-B. A flag byte for the 0x08053820 stage machine: bits 0-1 are a
 * small state (compared against 1 under a `& 3` mask), and bits 1, 4 and 7 are
 * set as the stage progresses. Plain `ldrb` / `strb`; signedness unproved. */
extern u8 gUnknown_02029664;
extern u16 gUnknown_0300451C;
extern u16 gUnknown_0300453C;
/* A function-pointer global, and the only evidence for its shape is
 * sub_0807166C's one call site: `ldr r2,[r0]; adds r0,r4,#0; adds r1,r6,#0;
 * bl _call_via_r2` -- the r2 index counts TWO arguments, and both are the
 * function's own pointer parameters passed straight through. Nothing writes it
 * in `asm/`, so the parameter types are the caller's and are spelled `void *`
 * here; sub_0807166C is a bytecode opcode handler and this is the "the
 * predicate held, run the branch" continuation it hands its two contexts to.
 * (wave 18) */
extern void (*gUnknown_03005744)(void *, void *);
/* Wave 48, W48-G. A per-side BYTE pair: sub_08057AE8 reads [0] and [1] with
 * `ldrb` and scales each by 0x20 to pick a gUnknown_0816D498 palette bank.
 * Byte-wide from the loads; nothing signs them, and the extent is the two
 * elements that function touches. */
extern u8 gUnknown_03004500[];
extern u16 gUnknown_03004518;
extern u16 gUnknown_03004538;
/* One 16-byte-stride RAM record per gUnknown_0300453C, viewed through TWO
 * linker symbols. sub_0804C098 reaches +2 as `gUnknown_03004580` with an
 * `adds #2`, but sub_0804D290/sub_0804DCA8 hold a pool word for 0x03004582
 * itself WHILE also holding one for 0x03004580 (both are defined in
 * aw2bhr.lds), which only a second named symbol can produce. So the source
 * really does have two overlapping declarations of the same memory and the
 * halfwords at +4 and +6 belong to the 03004580 view.
 *   [i][0] off 03004582  a small index into gUnknown_085D6A48 and the outer
 *                        index of gUnknown_08552FB8
 *   [i][2] off 03004580  the middle index of gUnknown_08552FB8
 *   [i][3] off 03004580  the second argument of sub_08057D44
 *   [i][5] off 03004580  the ROW index into the halfword-pair table that
 *                        sub_08057D44 returns -- sub_0804FA2C scales it by 40
 *                        (ten pairs) before adding the element index, so that
 *                        table is 2-D there and flat in sub_0804D290
 * The `[8]` row length is the 16-byte stride, not a proved extent.
 *   The ROWS ARE AN ARRAY, not a struct: every column read in sub_0804FA2C
 * ([3], [5], [2]) is emitted as `base + C` first and then `+ i*16`, which is
 * the reassociated array form, while gUnknown_085D6A48's column 9 in the SAME
 * function keeps its offset in the `ldrh` displacement. See the
 * "column-offset fold" section of docs/agbcc-codegen.md -- the two spellings
 * are distinguishable and this one is settled. */
extern u16 gUnknown_03004580[][8];
extern u16 gUnknown_03004582[][8];
/* A u16 lookup table, not a pointer: sub_0804B830 adds the symbol address to
 * `index * 2` directly and reads it with `ldrh`. Extent unproved -- its one
 * reader indexes it with an unchecked u16 argument. */
extern u16 gUnknown_030045A8[];
/* Wave 45, W45-H. THIS GLOBAL IS VOLATILE, and the evidence is decisive but the
 * declaration is deliberately left alone -- see below.
 *
 * sub_0805FD64 switches on it and the ROM reads it `ldr rA,=g; ldrh; lsls
 * #0x10; asrs #0x10`. Every non-volatile signed spelling folds instead to
 * `movs r1,#0; ldrsh r0,[r0,r1]` (2 bytes shorter, which is the whole residual
 * of that function): `switch ((s16)g)`, an `s16` local assigned from it, and an
 * `int` local narrowed with `(s16)` at the switch all produce the `ldrsh`.
 * combine will not fold `sign_extend (mem:HI)` through a VOLATILE mem, so
 * `(s16)*(volatile u16 *)&g` is the one spelling that reproduces the ROM, and
 * it matches sub_0805FD64 exactly. That also qualifies the wave-37 rule in
 * docs/agbcc-codegen.md: `ldrh; lsls; asrs` is not an `(s16)` cast on an
 * ORDINARY global, but it IS one on a volatile.
 *
 * NOT retyped to `volatile u16` here because eighteen already-promoted stores
 * across eight src/decomp files write it (c_08034394, c_080600F0, c_080601F0,
 * c_08060424, c_080604A4, c_080604F4, c_0806050C, c_08060554) and re-verifying
 * all of them was outside this agent's budget. The plain-constant `strh` stores
 * should be byte-identical under volatile; whoever has the budget should make
 * the declaration honest, drop the cast in work/sub_0805FD64, and re-run
 * trymatch on those eight files. */
extern u16 gUnknown_030045D4;
/* Wave 31 (W31-C), all three from sub_08060A7C / sub_0806050C / sub_08060930.
 *
 * gUnknown_030045D8: `ldr r1, [r1]` and then `cmp r1, r0 / ble` against the
 * result of sub_08057FA8 -- a full word compared SIGNED, so `int`.
 *
 * gUnknown_030045E0: two consecutive words, both handed straight to
 * sub_08035828 after a `cmp #0` null test, and the base address is held in one
 * callee-saved register across both -- an array of the proc pointer that
 * function already takes. Extent unproved beyond index 1.
 *
 * gUnknown_030046B8: `ldrb` of byte 0, tested against 1 and then 2 with
 * `movs rD,#K / ands`. That form is byte-neutral between a bitfield and a
 * scalar mask -- only a CLEAR would discriminate and there is none here, so the
 * weaker scalar spelling is used. */
extern int gUnknown_030045D8;
struct Unk35828Proc;
extern struct Unk35828Proc *gUnknown_030045E0[];
extern u8 gUnknown_030046B8;
/* Wave 37, W37-K1. sub_0805A95C's only output, and its whole body: a single
 * `strb` of gUnknown_0857680F[gUnknown_030040D8->unk00], or of 0 when that
 * byte reads exactly 4 AND the current unit's three-bit +0x09 field is clear.
 * Byte from the `strb`; signedness is open because nothing reads it yet.
 * A REAL OBJECT and not a slice of a neighbour -- aw2bhr.lds names
 * gUnknown_030046AC at 0x030046AC in its own right, checked before declaring.
 * Reached through TWO separate inline pool words, one per arm of the if/else,
 * rather than a single -fforce-addr .rodata word: the documented two-use case
 * where CSE const-propagates the address instead of holding it. */
extern u8 gUnknown_030046AC;
/* A zero-terminated list of small ids. sub_0805D344 walks it with
 * `while (gUnknown_030045F0[i] != 0)`, sub_0805CDF0/sub_0805CE20 seed it with
 * {0x40, 0} and point gUnknown_030046B0 at it. Byte array, not a struct:
 * sub_0805D344 indexes it with a variable.
 *
 * Wave 44, W44-E: this is very likely `volatile u8 []`, and the type is LEFT
 * ALONE deliberately -- do not "fix" it without doing the second half below.
 * sub_0805D344 sorts this array in lockstep with gUnknown_030046E0 and the ROM
 * spends SIX accesses on each of the two swaps, the extra pair being a dead
 * `ldrb` of each element's own address immediately before the `strb` to it --
 * the aggregate-element volatile tell this header already documents on
 * gUnknown_03004730. Declaring the OTHER array of that swap
 * `extern volatile u8 gUnknown_030046E0[]` reproduces its half of the stream
 * exactly, dead loads and all, which is the controlled half of the comparison;
 * reaching this one through a `volatile u8 *` CAST does not, because gcc then
 * folds the element pair onto one base with a `#1` displacement and emits four
 * accesses. So the qualifier belongs on the array type, not on a pointer to it.
 *
 * The reason it is not applied: TWELVE promoted builders (c_0805CA60.c and
 * every sibling) do `gUnknown_030046B0 = gUnknown_030045F0;`, and a volatile
 * array decays to `volatile u8 *`, which discards a qualifier into the plain
 * `u8 *` gUnknown_030046B0 below -- a warning, and -Werror is on, so all twelve
 * break at once. Retyping this REQUIRES retyping gUnknown_030046B0 with it, and
 * then re-running trymatch on all twelve. They are all matched, so that check
 * is cheap and decisive -- but it is one edit, not two, and it belongs to
 * whoever closes sub_0805D344. See work/sub_0805D344/sub_0805D344.c. */
extern u8 gUnknown_030045F0[];
/* Wave 44, W44-E: NOT YET DECLARED anywhere before this wave, and a real object
 * rather than a pool word -- aw2bhr.lds binds gUnknown_030046E0 at 0x030046E0
 * in its own right and aw2bhr.map agrees, checked before declaring. It is
 * sub_0805D344's sort KEY array, filled with sub_08042D1C's result through a
 * bare `strb` and compared element-against-element with `blo`/`bls`, so
 * unsigned bytes. VOLATILE on the dead-load evidence recorded on
 * gUnknown_030045F0 above; unlike that one, nothing else reads this yet, so
 * the qualifier costs nothing to state here. Left in place ahead of the
 * function that needs it -- it has no other readers to disturb. */
extern volatile u8 gUnknown_030046E0[];
/* The 20-byte command block sub_080308B4 consumes: sub_080344B4 and
 * sub_08034534 fill it byte by byte (+0, +1, +6, +7, +0x12) and then pass the
 * bare symbol as sub_080308B4's `u8 *src`, whose body copies src[0..19]. So it
 * is a u8 array, at least 0x14 long. sub_0803446C also fills +2..+5 and hands
 * `gUnknown_030044B0 + 0xc` to sub_08034400 as a 6-byte destination. */
extern u8 gUnknown_030044B0[];
/* Read cursor into gUnknown_030045F0 -- sub_0805D438 dereferences it with
 * `ldrb`, compares against the 0x40 sentinel and bumps it by 1. */
extern u8 *gUnknown_030046B0;
/* A word slot four bytes past gUnknown_030046B0 and unrelated to it: wave 26's
 * sub_08077F30 publishes gUnknown_08615194[i].unk24 or .unk28 into it, picked
 * by sub_0803866C(). Plain `str`, so a word; `void *` follows the source
 * members and the pointee is unproved -- no reader has been matched yet. */
extern void *gUnknown_030046B4;
/* 64 bytes, indexed `& 0x3f`. VOLATILE: sub_0805AC88 (`= 0`) and the
 * increment at 0x0805AC28 (`ldrb; adds #1; ldrb; strb`) both emit a dead
 * `ldrb` of the element's own address around the `strb` -- the
 * aggregate-element volatile tell in docs/agbcc-codegen.md. */
extern volatile u8 gUnknown_03004730[];
/* A per-mode step callback: sub_0805D438 loads it and does `bl _call_via_r0`
 * with the result discarded, and sub_0805CDF0/sub_0805CE20 store
 * sub_0805DB64/sub_0805DB70 into it -- both `void f(void)`. */
/* A word-sized cursor into the two 0x08576xxx function-pointer tables:
 * sub_08061B00 reads it, indexes `tbl[g]` with `lsls #2`, and post-increments
 * it; sub_08061908 resets it to 0. UNSIGNED -- sub_0805C1C4 and sub_0805C208
 * both guard on it with `cmp #1; bls`, and a signed `< 2` would have been
 * `blt`. */
extern u32 gUnknown_03004770;
extern void (*gUnknown_03004778)(void);
/* 16-bit, and every writer agrees: `strh` in sub_0805D338 (writes 5) and in
 * sub_0805FFA0's shared epilogue (writes 2).
 *
 * SIGNED (wave 25). It was `u16` with "signedness unproved" because every
 * reader known at the time only stored constants. sub_080343D8 reads it with
 * `movs r1, #0; ldrsh r0, [r0, r1]` -- the register-offset `ldrsh` agbcc has to
 * use because THUMB has no immediate-offset form -- and compares against 2. A
 * u16 object cannot produce that load without an explicit `(s16)` cast in the
 * source, so `s16` is the honest model. Every existing writer stores a
 * `movs`-sized constant with `strh`, which is byte-identical either way, so
 * this retype changes no promoted file (spot-checked on c_0805D338.c). */
/* Wave 37, W37-K2. The s16 stays, but the two readers of this symbol CANNOT
 * both come from one declaration and that is worth recording. sub_080343D8
 * (src/decomp/c_08034394.c, matched) reads it `movs r1,#0; ldrsh r0,[r0,r1]`.
 * sub_0806171C switches on it with `ldrh; lsls #0x10; asrs #0x10` -- an
 * unsigned load and a separate sign extension. Seven spellings were probed on
 * this global and on the u16 gUnknown_030033EC ((s16)g, (s16)(u16)g, an s16
 * local, a u16 local, `& 0xffff`, `*(u16 *)&g`, and the bare switch); every one
 * folds to `ldrsh`, because combine simplifies
 * `sign_extend (subreg:HI (zero_extend (mem:HI)))` back to
 * `sign_extend (mem:HI)`. The shift pair is therefore an aliasing fact, not a
 * width fact: the original's two translation units declared this object
 * differently. c_0806171C.c spells its own view with a volatile-qualified
 * pointer read, which is the only load combine may not touch; that is a
 * reproduction of the foreign declaration and NOT a claim that the object is
 * volatile. See docs/agbcc-codegen.md, "`sign_extend (mem:HI)` ALWAYS folds to
 * `ldrsh`". */
/* Wave 50, W50-M. aw2bhr.lds binds 0x030045DC, so this is a real object and not
 * pool-word content. sub_08062DF0 copies it straight into gUnknown_03004780
 * with `ldrh` / `strh` and nothing else in the tree touches it, so the WIDTH is
 * proved at 16 bits and the SIGNEDNESS is not: a plain halfword copy into the
 * s16 below emits `ldrh` whichever way this one is declared, so there is no
 * discriminating use. Declared u16 as the weaker of the two. */
extern u16 gUnknown_030045DC;
extern s16 gUnknown_03004780;
/* A whole-word flag: sub_0802F3D8 stores a register into it with `str`, and
 * the 0x0805Cxxx list builders pass it straight to sub_0805D344, which spills
 * it and tests it `!= 0`. Nothing narrows it, so u32 is the weakest model. */
extern u32 gUnknown_0300477C;
/* The record payload array the gUnknown_03005944 cursor family writes into.
 * u8 elements: every access in the ROM is `ldrb`/`strb` through the symbol
 * address plus a variable index (sub_08078608/58/A4/sub_080786F0 write three
 * bytes per record, sub_08044C10 and the 0x0807Cxxx readers read them back).
 * Indexed by a caller-supplied cursor, not by gUnknown_03005944.
 */
extern u8 gUnknown_030058E0[];
/* Wave 44, W44-F. A whole-word cell read `ldr` and added to a
 * gUnknown_030059A0 element to make PutSprite's second (y) argument in
 * sub_08080A70 -- a scroll or row base for that sprite column. u32 is the
 * weakest model; the sign is byte-neutral in the one readable use (the sum
 * feeds a `u32` parameter) and is NOT pinned -- flagged. sub_08080A70 keeps
 * only its ADDRESS live across the loop and re-`ldr`s it every pass, which by
 * the const-across-a-call tell in docs/agbcc-codegen.md means the declaration
 * is not const. */
extern u32 gUnknown_030058D0;
/* Wave 44, W44-F. The compacted copy of the first sub_0803BD14() entries of
 * gUnknown_030058E0 that sub_0807F434 builds -- it copies element by element
 * and then re-`ldrb`s each one it just wrote to hand to sub_08043B14, and
 * sub_08043BA4 reads the same bytes back in a second pass. `u8 []` from every
 * access; extent unbounded, since the trip count is a function result. */
extern u8 gUnknown_030058D4[];
/* Wave 44, W44-F. A byte array of per-column sprite y offsets, indexed
 * `ldrb` by a 0..proc->unk58 loop counter in the twins sub_0807FC70
 * (`+ 8`) and sub_08080A70 (`+ gUnknown_030058D0`). u8 from the `ldrb`, extent
 * unbounded -- the loop trip count is a proc field, not a constant. */
extern u8 gUnknown_030059A0[];
/* A word counter read whole with `ldr`. sub_08087298 returns
 * `gUnknown_030058F4 * 2 - K` as a u16, with K picked by
 * gUnknown_03003FC0.unk01 -- a scroll or column offset derived from a turn or
 * frame count. */
extern u32 gUnknown_030058F4;
/* Word flag guarding an optional sub-proc; set/cleared by sub_08080F3C and
 * sub_08080F90, tested `!= 0` as a whole word by sub_08081290 and 0x080814E0.
 */
extern u32 gUnknown_030058FC;
/* Zeroed by sub_08085AF4's reset alongside gUnknown_0300596C/80/90 and
 * gUnknown_03005930, and read `ldrb` into an int by sub_08085F94 and
 * sub_08085C10. Unsigned: no access sign-extends. It pairs with
 * gUnknown_03005930 exactly as gUnknown_03005990[gUnknown_0300596C] pairs
 * with gUnknown_03005980 -- sub_08085F94 picks one pair or the other on
 * `gUnknown_081D940C->unk01 == 2` and sums it into a proc field at +0x58. */
extern u8 gUnknown_03005900;
/* Word cell, `ldr`/`str` everywhere (sub_08080FB8 tests it `!= 0`,
 * sub_08081E54 stores into it). */
extern u32 gUnknown_03005920;
/* 0x03002EE0 is `gpKeySt` (include/hardware.h), NOT an unnamed global. Wave 20
 * invented a `gUnknown_03002EE0` for it and the SPLIT build caught it as an
 * undefined reference: upstream's linker script already binds that address, so
 * no `gUnknown_` symbol exists there and none can be created. The reason it was
 * missed is worth keeping -- there is no direct `ldr rN, =gpKeySt` in asm/ at
 * all; every reader spells it `ldr rN, =<pool>; ldr rM, [rN]; ldr rM, [rM]`, a
 * -fforce-addr reroute plus the pointer's own deref, so the address only ever
 * appears as pool-word CONTENT. Three words of the 0x0816E180-0x0816E1CC run
 * hold it (0x0816E1B0, 0x0816E1B4, 0x0816E1C8) and so does 0x081D93B8,
 * sub_08082660's slot.
 *   The type analysis behind the wrong name was right and is now redundant:
 * `struct KeySt` already declares the u16 at +0x02, and sub_08082660 testing
 * bits 0x40 / 0x80 of it is the same D-pad idiom c_0802966C.c uses at 0x50 /
 * 0xa0. RULE: before declaring a `gUnknown_<addr>` for an address reached only
 * through a pool word, grep aw2bhr.lds for that offset -- a named symbol there
 * is invisible to every asm-side screen this project runs. */
/* Word cell holding a small state id -- sub_08080F3C and sub_08080F90 both set
 * it to 6. Read whole with `ldr` and stored into a u16 proc field at +0x66 by
 * 0x0808151C, which is where the `ldr; strh` pairing in sub_0807BE24 comes
 * from too. */
extern u32 gUnknown_03005924;
/* Word cell; the only reference in the ROM is sub_0807BE24, which reads it
 * `ldr` and stores it into a u16 proc field at +0x4c. `ldr` rather than `ldrh`
 * is what fixes it at 32 bits -- a u16 global would have loaded `ldrh`. */
extern u32 gUnknown_0300592C;
/* Halfword zeroed by sub_08085AF4's reset; sub_08085F94 reads it `ldrh` and
 * adds it to gUnknown_03005900. The unsigned `ldrh` in an int context keeps
 * it u16 rather than s16. */
extern u16 gUnknown_03005930;
/* Wave 44, W44-F. sub_08080F54's only output: it linear-searches the six-byte
 * permutation gUnknown_0861696C for its parameter and stores `DivRem(i + 4, 6)`
 * here, or the constant 4 when the search runs out. Word-sized -- the single
 * write in the ROM is `str`, and DivRem returns int. u32 rather than int is the
 * weakest model that fits: the stored range is 0..5 and nothing in this block
 * reads it back yet, so the sign is NOT pinned -- flagged. Reached only through
 * the 0x081D9384 .rodata address-constant pool (words at 0x081D9390 and
 * 0x081D939C both hold 0x03005934), so no `ldr rN, =gUnknown_03005934` appears
 * anywhere in asm/. */
extern u32 gUnknown_03005934;
/* Write cursor into the gUnknown_03005948/58 byte arrays, post-incremented
 * per record by sub_08078608 and its siblings.
 */
extern u32 gUnknown_03005944;
/* Two parallel u8 record arrays indexed by the gUnknown_03005944 cursor, both
 * `ldrb`/`strb` throughout. sub_08078608 and its siblings write one byte of
 * each per record; sub_0807C844 and sub_0807CF50 read them back. */
extern u8 gUnknown_03005948[];
extern u8 gUnknown_03005958[];
/* Wave 51, W51-M. Two five-byte RAM runs, each cleared by its own DESCENDING
 * byte loop -- `adds r0,r1,#4` then `strb; subs r0,#1; cmp r0,r1; bge` --
 * sub_08088044 for gUnknown_03005978 and sub_0807C588 for gUnknown_03005910.
 * `u8` from the `strb`; the extent 5 is what the loop bound proves, and neither
 * has a reader in src/decomp yet, so nothing constrains them further. */
extern u8 gUnknown_03005978[];
extern u8 gUnknown_03005910[];
/* A word-sized 0/1 flag: sub_08082412 toggles it with `~g & 1`, five sites
 * test the whole word, and sub_080846F4 returns just its low byte. */
extern u32 gUnknown_03005968;
/* A 0..7 selector, read `ldrb` at all six sites in code-0806CFC8.s and always
 * compared unsigned (`cmp #3; bhi`, `cmp #1; bhi`, `cmp #5; bhi`) -- u8. It
 * splits a menu/portrait layout into a low half (0..3) and a high half (4..7,
 * used as `g - 4`), and sub_080895E4 reads it FIVE times in a row across four
 * `bl`s while holding only its ADDRESS in r4, which per the const rule in
 * docs/agbcc-codegen.md means the declaration is NOT const. */
/* Wave 51, W51-I.  DO NOT DECLARE gUnknown_081D9470 OR gUnknown_081D9474.
 * Both ROM words hold 0x03005964 -- dereferenced in baserom.gba -- so they are
 * two -fforce-addr address constants for THIS symbol, one private to
 * sub_08089C14 and one to sub_08089F90, and gen_lds.py invented a name for
 * each.  The neighbouring 0x081D946C holds 0x0808F100 (&gSinLut), so the run
 * 0x081D946C-74 is one unit's address-constant pool, not "the menu's word".
 * Spelling gUnknown_03005964 and gSinLut honestly matched both functions;
 * the promotions carry "rodata": ["0x081D9474"] and ["0x081D946C",
 * "0x081D9470"].  sub_08089C14 reaches gSinLut BOTH ways in one body -- the
 * force-addr word for the first read and a plain inline pool word for the
 * later ones -- exactly as W48-J records for gUnknown_08499590. */
extern u8 gUnknown_03005964;
/* The cursor into gUnknown_03005990[], set to 2 by sub_08085AF4's reset.
 * SIGNED, and that is not negotiable: sub_08085F94 and sub_080860DC both read
 * it with `movs rN, #0; ldrsh r0, [r2, rN]` before adding it to the array
 * base, which per docs/agbcc-codegen.md is an s16 OBJECT rather than a cast.
 * (The stray `movs #0` alongside is just `ldrsh` having no immediate-offset
 * form, not an operand of anything.) sub_08085AF4 stores the literal 2 with a
 * plain `strh` and is byte-identical either way, so the reset could not have
 * said so -- this is the widest-access rule doing its job. */
extern s16 gUnknown_0300596C;
/* Halfword zeroed by sub_08085AF4's reset; sub_08085F94 reads it `ldrh` and
 * adds it to gUnknown_03005990[gUnknown_0300596C]. The unsigned `ldrh` in an
 * int context is what keeps it u16 rather than s16. Its opposite number on
 * the other arm of that branch is gUnknown_03005930. */
extern u16 gUnknown_03005980;
/* Exactly 9 bytes, and the extent is proved rather than guessed:
 * sub_08085AF4 clears it with the house reverse-fill idiom
 * (`p = base + 8; do { *p = 0; p--; } while ((int)p >= (int)base);`), so the
 * cursor starts at +8 and the loop's terminating compare is against the array
 * base itself. Really an array and not a struct -- sub_08085F94 and
 * sub_080860DC index it with the variable gUnknown_0300596C, `ldrb` to read
 * and `strb` to write. */
extern u8 gUnknown_03005990[];
/* Wave 51 (W51-J). sub_0807CAFC indexes it with the loop counter and `ldrb`s
 * the result, then uses THAT byte to index gUnknown_03005958 -- so it is a
 * table of subscripts into the record array, one per entry, and the load is a
 * real indexed read rather than a pool word. Length unknown; the loop bound is
 * a proc field. */
extern u8 gUnknown_0300599C[];
/* Five words, cleared/filled as a unit by sub_08078740 and sub_08078758. */
extern u32 gUnknown_030059C0[];
/* The m4a MusicPlayerInfo for the BGM player: it is the first argument of every
 * m4a MPlay* entry point in this tree -- sub_08071420 (MPlayVolumeControl) from
 * sub_0803B35C, sub_08070610 (m4aMPlayFadeOut) from sub_0803B5A4, and four more
 * sites in asm/code-0801D390.s -- and is only ever passed by address, never
 * dereferenced from C. `u8 []` is the weakest model that reproduces the bare
 * `ldr rN, =gUnknown_03005AE0` with no extra instruction; the real struct is
 * m4a's MusicPlayerInfo and nothing in this tree has needed a member of it yet.
 * Wave 24.
 *
 * WAVE 38: "the real struct is m4a's MusicPlayerInfo" -- the wave-24 note said
 * so and declared `u8 []` anyway, correctly, because no member was reachable
 * then. W38-A matched sub_08071420 (MPlayVolumeControl), whose body reads ident
 * at +0x34, trackCount at +0x08 and tracks at +0x2c through this pointer, so
 * the condition that note was waiting on is now met. Retyped with the other
 * seven of its group. */
/* Wave 47 (W47-B), the four objects sub_080703F4 (m4aSoundInit) wires up.
 *   gUnknown_03000FB0 -- the IWRAM landing zone for the copy of sub_0806F7C8's
 *     code: CpuSet's destination, with a mode word of 0x04000100, i.e. 0x100
 *     words == 0x400 bytes. Declared as an array so the bare name produces the
 *     `ldr r1, =sym` the ROM has; the element type is not pinned by anything.
 *   gUnknown_03004790 -- the one struct SoundInfo. Its address is handed to
 *     sub_080707F4 (SoundInit), which is already promoted taking a
 *     `struct SoundInfo *`, and that call is what fixes the type.
 *   gUnknown_030057D0 -- gCgbChans. Handed to sub_080706B0 (MPlayExtender) and
 *     thence parked in SoundInfo::cgbChans, which sub_08070A28 walks as four
 *     0x40-byte records. Four elements is the canonical count and is NOT proved
 *     here; only the element type is.
 *   gUnknown_03005BE0 -- gMPlayMemAccArea, pointed at by every player's
 *     memAccArea. Declared as a SCALAR and taken by address, not as an array,
 *     and that is forced rather than stylistic: sub_080703F4 stores the address
 *     inside a loop with a plain in-function `ldr rN, =sym` re-loaded every
 *     iteration. Spelled as an array, `-fforce-addr` builds a `.rodata`
 *     address-constant word instead, LICM hoists it out of the loop, and the
 *     store becomes a double indirection -- measured, 2 extra instructions.
 *     `&scalar` is an ADDR_EXPR and skips that path; c_080707F4.c already
 *     relies on the same thing for gUnknown_03005740. Nothing reads an element
 *     anywhere, so no size or element type is claimed. */
extern u32 gUnknown_03000FB0[];
extern struct SoundInfo gUnknown_03004790;
extern struct CgbChannel gUnknown_030057D0[];
extern struct MusicPlayerInfo gUnknown_03005AE0[];
extern u8 gUnknown_03005BE0;
/* SOUND_INFO_PTR. The AGB BIOS/driver convention puts it at exactly 0x03007FF0
 * and this ROM follows it: sub_0806F744 (SoundMain) loads it and compares
 * `[r0]` against 0x68736D53. NOT const -- sub_08070AF8 re-`ldr`s nothing, but
 * the driver installs the pointer at init, so nothing here justifies const. */
extern struct SoundInfo *gUnknown_03007FF0;

/* ---------------------------------------------------------------- ROM -- */

/* A ROM word holding the address of a RAM array of 0x0c-byte records; see
 * struct Unk0808E5C8. Non-const, but for the usual -Werror reason and NOT
 * because sub_0801A6C0 re-`ldr`s the deref after its `strh` to
 * gUnknown_030020A8 -- that reload is not evidence either way. Probed: under
 * `-fforce-addr` even the `.rodata` word GCC synthesises for a const-qualified
 * pointer is re-read across an intervening HImode store, so a const
 * declaration reloads exactly the same way. The const tell in
 * docs/agbcc-codegen.md is about a re-read surviving a store, and nothing here
 * survives one. */
extern struct Unk0808E5C8 *gUnknown_0808E5C8;
/* Wave 41 (W41-A), from sub_0801A718. Two more ROM words in the same run, both
 * agbcc -fforce-addr address constants and NOT objects. Values read out of
 * baserom.gba, not inferred:
 *   0x0808E5CC == 0x030020A8 == &gUnknown_030020A8 (the list header)
 *   0x0808E5D0 == 0x0200C618 == the record array's entry [0], the same address
 *                 gUnknown_0808E5C8 holds
 * 0x0808E5CC needs no declaration at all: naming gUnknown_030020A8 honestly
 * makes agbcc emit the slot, verified against sub_0801A718's listing.
 * 0x0808E5D0 would work the same way if the object at 0x0200C618 had a linker
 * symbol -- aw2bhr.lds jumps 0x00C528 straight to 0x00C624 and it does not, so
 * the pointer-object spelling below is a WORKAROUND that costs one extra `ldr`,
 * and it is exactly why sub_0801A718 is parked. See
 * work/sub_0801A718/sub_0801A718.c. Do not treat it as a model.
 *
 * gUnknown_0200C624 is that array's entry [1]: sub_0801A718 indexes it by the
 * header's count with a 0x0c stride and reaches the sentinel as
 * `gUnknown_0200C624 - 1`, which is entry [0]. aw2bhr.lds:38 already names
 * 0x0200C624, so that one can be declared. */
extern struct Unk0808E5C8 *gUnknown_0808E5D0;
extern struct Unk0808E5C8 gUnknown_0200C624[];
/* Wave 33, W33-G. gUnknown_0808E580/_84/_88/_8C are NOT objects: they are
 * agbcc's own -fforce-addr address constants, a private PAIR per function.
 * Dereferenced in baserom.gba, 0x0808E580 == 0x0808E588 == 0x0200C528
 * (&gUnknown_0200C528) and 0x0808E584 == 0x0808E58C == 0x03002514 (the u8 mode
 * enum declared below). So sub_080185D0 and sub_08018694 -- near-twins
 * differing only in the tileset, the sub_08014740 palette id and the installed
 * callback -- read the SAME two objects through four names, and both are
 * written with the honest spelling: `gUnknown_0200C528[a]` and
 * `gUnknown_03002514`, with the pool word carved at promotion.
 * MEASURED, not assumed: declaring them as pointers to what they point at and
 * writing `gUnknown_0808E580[a]` emits one indirection TOO MANY -- agbcc adds
 * its own .rodata level on top, so the draft reads .LC -> &g -> g -> element
 * where the ROM reads .LC -> g -> element. See the same correction on
 * gUnknown_080909E8 below. The force-addr level appears only for an address
 * referenced more than once: in the same function gUnknown_08610A38,
 * gUnknown_08499588 and gUnknown_03002F08 are each named once and each get an
 * ordinary direct pool word. */
/* A byte-stream script walked by sub_0801B8A8, which is a
 * `while (*p != 1)` loop doing `sub_0801B7C0(p, arg); p += sub_0808B6B0(p) + 1`
 * -- a variable-length opcode stream, hence `const u8 []` and not a record
 * array. sub_0801B750 is its only starter. Wave 14. */
extern const u8 gUnknown_0808EF64[];
/* 0x0808D7B4 -- referenced by ADDRESS ONLY and never dereferenced, so nothing
 * types the element. sub_080051EC compares the BYTE `*s` against this symbol's
 * address with a bare `cmp r0,r2` (no load through it), which cannot be a
 * meaningful test -- see the note on that function in src/decomp; the
 * declaration exists so the comparison can be spelled at all. */
extern const u8 gUnknown_0808D7B4[];
/* 0x0808EF84 / _88 / _8C / 0x0808F048 are NOT objects: they are agbcc
 * -fforce-addr .rodata address constants, the same situation as
 * gUnknown_0808E580 above. DEREFERENCED IN baserom.gba:
 *   0x0808EF84 -> 0x03000054   0x0808EF88 -> 0x03000058
 *   0x0808EF8C -> 0x0300005C   0x0808F048 -> 0x0808EF90
 * so the last three name objects THIS FILE ALREADY DECLARES or declares just
 * below, and sub_0801B8D0/sub_0801BA4C are written with the honest spelling
 * (`gUnknown_03000058`, `gUnknown_0808EF90[x]`) with the pool word carved at
 * promotion.
 *
 * WAVE 40 (W40-G) MEASURED THE ALTERNATIVE AND IT IS WRONG, which is worth
 * recording because the two readings look equally good on paper: declaring
 * them as pointers to what they point at (`extern u32 *gUnknown_0808EF84;`,
 * `*gUnknown_0808EF84 = 0`) emits ONE INDIRECTION TOO MANY -- agbcc puts its
 * own force-addr level on top, so the probe read .LC -> &g -> g -> cell where
 * the ROM reads .LC -> cell. Identical to the correction already recorded for
 * gUnknown_0808E580 and gUnknown_080909E8.
 *
 * It also REFUTES the "referenced more than once" qualifier in the
 * gUnknown_0808E580 note above, which was the reason for trying the pointer
 * model here: gUnknown_0808EF88 is named exactly ONCE in sub_0801B8D0 and
 * still got a force-addr word, in the ROM and in the probe alike. The
 * force-addr level is not conditional on the reference count. */
/* 0x03000054 CANNOT BE DECLARED -- the same class as 0x03000060 and
 * 0x03000600/0x03000602 elsewhere in this file. aw2bhr.lds names
 * gUnknown_03000050 and then jumps straight to gUnknown_03000058, so
 * 0x03000054 falls INSIDE gUnknown_03000050's region and an invented symbol has
 * nowhere to live; tools/proto_check.py flags it and the split build would fail
 * with `undefined reference` even though trymatch reports a match. (It did:
 * wave 40 declared it, was caught by proto_check, and re-verified the match
 * with the offset spelling below.)
 *   Spell it `(&gUnknown_03000050)[1]`, which is what src/decomp/c_0801B8D0.c
 * does. Byte-neutral -- the address is a link-time constant either way, so
 * -fforce-addr parks `gUnknown_03000050+4` in .rodata and the pool word is the
 * one the ROM has.
 *   What it IS: the third member of the counter group at 0x54..0x5C.
 * sub_0801B8D0 zeroes all three together and then stores sub_0801B738's int
 * return into this one once per character, never reading it back, so it is a
 * word latch holding the last glyph's result. */
/* 0x0808EF90 -- the DEGREE-BASED SINE TABLE, one s16 per degree for 0..90,
 * reached only through the force-addr word at 0x0808F048. sub_0801BA4C folds
 * an arbitrary angle into 0..90 and loads `gUnknown_0808EF90[x]` with `ldrsh`,
 * negating it for the lower half-turn; sub_0801BAA8 is the cosine wrapper over
 * it. Extent 0x0808EF90..0x0808F048 is 0xb8 bytes = 92 halfwords, i.e. the 91
 * entries the fold can reach plus one of padding.
 *   CARVED OUT OF data/rodata.s IN WAVE 40: 0x0808EF90 had no label of its own,
 * it sat inside gUnknown_0808EF8C's 0xbc .incbin block, and tools/proto_check.py
 * would have reported `rom-undef` the moment sub_0801BA4C was promoted. That
 * check only scans PROMOTED C, so a work/ draft naming a ROM symbol that does
 * not exist yet passes it silently -- worth knowing, because it means a clean
 * proto_check is NOT evidence that a matched draft will link. The block is now
 * 0x4 + 0xb8, byte-identical in total. */
extern const s16 gUnknown_0808EF90[];
/* 0x40 bytes of uncompressed 4bpp tile data (two tiles): sub_080059E4 queues
 * it for VRAM 0x060158C0 with sub_08011E54. Non-const because sub_08011E54's
 * first parameter is a plain `void *` -- the usual -Werror reason, not
 * evidence about the original declaration. */
extern u8 gUnknown_0808DF4C[];
/* ROM table of word-sized entries walked by the gUnknown_03000578 cursor;
 * sub_0802EC64 wraps the cursor back to 0 when the entry is 0, so the table is
 * 0-terminated.
 *
 * Wave 41, W41-D RETYPES IT FROM `void *[]` TO `int []`, which is what the note
 * above was waiting for -- a consumer. sub_0802F8FC takes the entry the cursor
 * selects and hands it straight to sub_0802ECEC, whose promoted definition
 * (src/decomp/c_0802ECEC.c) is `void sub_0802ECEC(int cycles)` and which arms
 * timer 3 with a reload of -cycles. So the entries are TIMEOUTS in timer
 * cycles, not pointers, and the terminating 0 is "no timeout" -- the same 0
 * sub_0802F8FC tests before deciding between the timer and a direct SIOCNT
 * write. The ROM word at 0x08090C44 is 0x00000000, which is entry [0] and fits
 * a table whose first slot means "none".
 * Byte-neutral for the one existing reader: c_0802EC64.c compares the entry
 * against 0, which is the same `ldr`/`cmp #0` for `int` and for `void *`. It
 * was re-verified with try_match after the change. */
extern int gUnknown_08090C44[];
extern struct Unk08090CD8 *const gUnknown_08090CD8;
/* Wave 33, W33-B: sub_080466DC reaches `gUnknown_0812A10C->unk00->unk50` (a u8
 * gate flag at 0x50) -- two `ldr`s then a `ldrb`, so a struct with a pointer
 * member at +0x00. */
struct Unk0812A10CInner
{
    /* 0x00 */ u8 filler_00[0x50];
    /* 0x50 */ u8 unk50;
};
struct Unk0812A10C
{
    /* 0x00 */ struct Unk0812A10CInner *unk00;
};
/* RETRACTED in wave 34 (W34-A). 0x0812A10C IS NOT AN OBJECT: it is an agbcc
 * `-fforce-addr` .rodata word holding &gUnknown_084C1430, exactly like the
 * 0x08090D0C / 0x0809095C / 0x08090A14 words documented elsewhere in this file.
 * sub_080466DC was read as `gUnknown_0812A10C->unk00->unk50` and modelled here
 * as a pointer to a struct whose first member is a pointer; the second arrow is
 * the force-addr indirection, and the real expression is the ONE-arrow
 * `gUnknown_084C1430->unk50` -- the same record src/decomp/c_080466A4.c walks
 * for unk58/unk59. With the two-arrow model the function is 6 bytes long and
 * carries three extra `ldr`s; with one arrow it matches byte-for-byte.
 *
 * The tell is general and worth reusing: a ROM word in .rodata that yields a
 * pointer after TWO loads is a force-addr word plus one real dereference, not
 * two real dereferences. Compare the load count against a function that reaches
 * the same object through a plain `ldr rN, =sym` before inventing a level. */
extern struct Unk0812A10C *gUnknown_0812A10C;
/* Wave 33, W33-B read gUnknown_080909E8 and gUnknown_080909EC as "the two
 * 'move toward target' cursors, each a POINTER to a u16 position pair", and
 * declared them `struct Unk802C57C *`. W33-G measured both against
 * baserom.gba: BOTH WORDS HOLD 0x030033E0. They are not two cursors and not
 * objects at all -- they are agbcc -fforce-addr address constants for the ONE
 * cursor gUnknown_030033E0 (declared above, and already written that way by
 * src/decomp/c_0802150C.c), one private copy per function. The declarations
 * are removed rather than corrected because the pointer spelling is a level
 * too deep: `gUnknown_080909E8->unk00` compiles to .LC -> &g -> g -> unk00,
 * where sub_080230DC's ROM reads .LC -> g -> unk00. Write
 * `gUnknown_030033E0.unk00` and let promotion carve the pool word. This is
 * exactly the "several pool words in a run can hold the SAME address" trap in
 * the wave brief; nothing in src/decomp had used either name.
 *
 * Same class, same measurement, for the four words W33-G's own batch needed:
 * 0x080909FC == 0x08090A14 == 0x08499590 (&gUnknown_08499590, the map header
 * pointer c_0800119C.c and c_08001158.c already read through as a `u8 *`),
 * 0x08090A00 == 0x030033E4 (W33-D's cursor cell) and 0x08090A04 == 0x08499C7C
 * (W33-D's (dx, dy) direction table). Two names for gUnknown_08499590 because
 * sub_0802361C and sub_08023860 each got a private copy -- not two objects.
 * None of the six needs a declaration. */
/* WAVE 36 (W36-A) EXTENDS THAT RUN BY NINE MORE WORDS, all dereferenced against
 * baserom.gba rather than inferred, and NONE of them is an object or needs a
 * declaration. The run 0x080909E8-0x08090A5C in code-0801D390.s is solid
 * -fforce-addr address constants:
 *
 *   0x080909F0 0x08090A08 0x08090A14 0x08090A18 0x08090A1C 0x08090A20
 *   0x08090A50                     -> 0x08499590  (&gUnknown_08499590)
 *   0x080909F4 0x08090A10 0x08090A04 -> 0x08499C7C  (&gUnknown_08499C7C)
 *   0x080909F8 0x08090A0C 0x08090A00 -> 0x030033E4  (&gUnknown_030033E4)
 *   0x08090A24 0x08090A28            -> 0x08499B0C
 *   0x08090A30                       -> 0x08499598  (&gUnknown_08499598)
 *   0x08090A34 0x08090A54 0x08090A58 -> 0x08499594  (&gUnknown_08499594)
 *   0x08090A38 0x08090A44            -> 0x030013D0  (&gUnknown_030013D0)
 *   0x08090A3C 0x08090A40            -> 0x030013B0  (&gUnknown_030013B0)
 *   0x08090A2C -> 0x03004008   0x08090A48 -> 0x085D5ABC
 *   0x08090A4C -> 0x030032C0   0x08090A5C -> 0x020288A0
 *
 * SEVEN copies of &gUnknown_08499590 in one run is the duplication screen at its
 * clearest: one word per (function, symbol) pair, not a table. Writing the
 * global's name honestly reproduces each function's three-level
 * `ldr rN,=<word>; ldr rN,[rN]; ldr rD,[rN]` exactly -- five functions matched
 * that way this wave (sub_08023EEC, sub_080251D8, sub_08024C58, sub_08024F20).
 * Do NOT declare any of them as a pointer variable; that adds a fourth level.
 *
 * ONE MEASUREMENT WORTH CARRYING, from sub_08024F20: whether a symbol gets a
 * force-addr word at all is decided by how many times the SOURCE NAMES IT, and
 * a cast to a different struct tag counts as a different name and gets its own
 * inline pool word instead. See the wave-36 section of docs/agbcc-codegen.md;
 * it is the only byte-visible oracle this project has found for a struct tag. */
extern const s16 gUnknown_08090EAC[];
/* 0x30 bytes = twelve words, and word-sized is measured rather than assumed:
 * sub_0803B3B0 indexes it `lsls r0,r0,#2; adds r0,r0,r1; ldr r0,[r0]` and hands
 * the entry straight to sub_08070990 (m4aSoundMode), whose parameter is `u32`.
 * So this is the ROM's table of twelve m4a sound-mode words, selected by a
 * small index. `const`: it is in .rodata and sub_08070990 takes it by value.
 * Wave 24. */
extern const u32 gUnknown_080910FC[];
/* 0x280 bytes = 20 uncompressed 4bpp tiles; sub_08037258 returns its address.
 * Non-const because nothing constrains it yet and const is the direction that
 * breaks under -Werror. */
extern u8 gUnknown_080913BC[];
/* 0x800 bytes of uncompressed ROM data blitted to VRAM 0x06017800 by
 * sub_080116E8 through sub_08011C68. Non-const for the same -Werror reason as
 * its neighbours above; sub_08011C68 takes `const void *`, so const would
 * compile here, but nothing else constrains the symbol and non-const is the
 * direction that stays safe. */
extern u8 gUnknown_080A1424[];
/* A ROM tile blob sub_0807420C blits to 0x06001F00 through sub_08011C68,
 * 0x100 bytes.  Same shape and same declaration as gUnknown_080A1424 one
 * line up, which c_080116E8.c reaches the identical way (wave 28, W28-B). */
extern u8 gUnknown_0812B29C[];
/* 0x400 bytes of uncompressed ROM data, copied verbatim over the buffer
 * gUnknown_0849959C points at by sub_080215D0 (`for (i = 0; i <= 0x3ff; i++)`,
 * one `ldrb`/`strb` per byte). Non-const for the usual -Werror reason. */
extern u8 gUnknown_080C1BC4[];
/* A ROM word holding 0x0200D510, i.e. &gUnknown_0200D510 -- the sprite-list
 * layer-head array. Its only user is sub_0801BE78, which derefs it twice with
 * `ldr rN,[r4]` on one pool word and gets no `.rodata` indirection despite the
 * two uses straddling a loop, so the address is NOT force-addr'd here. */
extern struct SpriteEntry *gUnknown_0808F090;
/* Wave 40 (W40-H). gUnknown_0808F08C IS NOT A GLOBAL AND MUST NOT BE DECLARED
 * AS ONE -- it is agbcc's own `-fforce-addr` address constant, and this note
 * exists so the next agent does not re-invent the declaration I did.
 *
 * The word at 0x0808F08C holds 0x03002620, which is `gUnknown_03002520 + 0x100`
 * -- the END of the 32-entry OAM shadow above (32 * 8 bytes). Its one reader,
 * sub_0801BC3C, does `ldr r7,=<word>` then `ldr r0,[r7]` and compares the
 * result against the gUnknown_03002F2C cursor, which reads exactly like a
 * pointer global loaded from ROM. It is not: the source says
 * `gUnknown_03002F2C < (void *)(gUnknown_03002520 + 128)` and agbcc parked that
 * address in the unit's .rodata. Declaring a `void *gUnknown_0808F08C` and
 * dereferencing it reproduces the compare but costs an EXTRA `ldr` at both the
 * entry guard and the loop bottom, because you get three levels of indirection
 * where the ROM has two -- that mismatch is what proves the force-addr reading.
 *
 * Confirmed by dereferencing baserom.gba, which the brief tells you to do
 * before believing any pool symbol. The whole run is the same story and the
 * neighbours corroborate it: 0808F090 -> 0200D510, 0808F094 -> 0200E390,
 * 0808F09C -> 0200E438, and 0808F0A0/A4/A8/AC/B0 all -> 03003034 (five copies
 * of one address; W40-I found the last four independently in the 0x0801D
 * block). gen_lds.py invents a `gUnknown_<addr>` for every one of these and the
 * index cannot tell an address constant from a real object.
 *
 * The one nearby word that IS a genuine global is gUnknown_0808F090 above --
 * see its note: its reader gets NO .rodata indirection, which is the
 * discriminator between the two cases. */
/* gUnknown_0808F0B4 IS NOT A GLOBAL -- do not declare it. It is agbcc's own
 * -fforce-addr address constant for &gUnknown_0200F720 (it dereferences to
 * 0x0200F720 in baserom.gba), emitted into sub_0801E2A4's .rodata. Declaring it
 * as a `struct Unk0200F720 *` and reading it costs one extra `ldr`, because the
 * honest source just says `&gUnknown_0200F720[i]`; spelling it that way matched
 * sub_0801E2A4 with `"rodata": ["0x0808F0B4"]` on the promotion (wave 40, W40-J).
 * The neighbouring gUnknown_0808F0B8 below IS real data.
 *
 * The two functions in this block make a clean controlled pair for when the
 * address constant appears at all: sub_0801E13C references gUnknown_0200F720
 * TWICE and gets a plain text pool word; sub_0801E2A4 references it ONCE and
 * gets the .rodata indirection. Fewer references is the harder case, exactly as
 * the literal-pool chapter says. */
/* Wave 40 (W40-J). 0x40 bytes = 32 u16 entries whose values are i << 9 (read
 * out of baserom.gba). sub_0801E3E8 ORs one entry into `attr1 & 0xC1FF`, and
 * bits 9..13 are exactly the field that mask clears -- the OAM attr1
 * affine-parameter selector, pre-shifted. */
extern u16 gUnknown_0808F0B8[];
/* 0x1a4-byte compressed blob, handed to Decompress(u8 *, void *) by
 * sub_0802D5B8 via the accessor sub_08037250 -- hence u8 *, not const. */
extern u8 gUnknown_080D3FE4[];
/* Wave 33, W33-B: sub_0803FD80's overlay blobs. The four 080D2xxx/080D3268
 * words are Decompress sources (`u8 *`, first argument of
 * Decompress(u8 *, void *)) and 080D3FC4 is an ApplyPaletteExt palette
 * (`u16 *`, first argument of ApplyPaletteExt(u16 *, u32, u16)). Non-const
 * because those prototypes take plain pointers; nothing dereferences them
 * here so the extents are unknown. */
extern u8 gUnknown_080D22C4[];
extern u8 gUnknown_080D24E0[];
extern u8 gUnknown_080D2AE8[];
extern u8 gUnknown_080D3268[];
extern u16 gUnknown_080D3FC4[];
/* Wave 33, W33-B: sub_0803F5E4's tile blobs. The 080Cxxxx/080D0B44 pair is
 * copied 0xb80 bytes through sub_08011E54(void *, void *, u16); the
 * 080D16C4/080D1BC4 pair is a tile atlas base indexed byte-wise
 * (`base + ((((n-1)<<3) & 0x3ff) << 5)`) and handed to
 * CpuFastSet(const void *, ...). All byte-addressed, so `u8 []`; non-const
 * because sub_08011E54 takes a plain pointer. */
extern u8 gUnknown_080CFFC4[];
extern u8 gUnknown_080D0B44[];
extern u8 gUnknown_080D16C4[];
extern u8 gUnknown_080D1BC4[];
extern const struct ProcCmd gUnknown_086140D4[];
/* The sprite/animation table sub_08043418 hands to sub_0801C7DC as its first
 * argument. u16 because that is how sub_0801C7DC reads it -- `ldrh [r0]` and
 * `ldrh [r0,#2]` are two halfword offsets into the table itself, and the
 * element it finally reaches is handed to PutSpriteExt's `u16 *` parameter.
 * const because it is ROM and nothing writes it; the one consequence is that
 * sub_0801C7DC's first parameter is declared `const u16 *`, which will need
 * revisiting if that function turns out to launder it into PutSpriteExt
 * without a cast. */
extern const u16 gUnknown_08101EC0[];
/* Wave 33, W33-B: sub_0803F140's palette, handed by name to
 * ApplyPaletteExt(u16 *, u32, u16) with no arithmetic, so `u16 []` and
 * non-const (ApplyPaletteExt takes a plain pointer). */
extern u16 gUnknown_08109564[];
/* Wave 33, W33-B: sub_0803FFA0's four overlay groups, one selected by a switch
 * on proc->unk54. Each group is (Decompress source `u8 *`, ApplyPaletteExt
 * palette `u16 *`, sub_0801C210 sprite descriptor `void *` modelled `u8 []`).
 * Non-const: Decompress, ApplyPaletteExt and sub_0801C210 all take plain
 * pointers. Nothing dereferences them here so the extents are unknown. */
extern u8 gUnknown_0810EB00[];
extern u16 gUnknown_0810F364[];
extern u8 gUnknown_0810F384[];
extern u8 gUnknown_0810F410[];
extern u16 gUnknown_0810FAE8[];
extern u8 gUnknown_0810FB08[];
extern u8 gUnknown_0810FB94[];
extern u16 gUnknown_0810FF4C[];
extern u8 gUnknown_0810FF6C[];
extern u8 gUnknown_0810FFE0[];
extern u16 gUnknown_08110CBC[];
extern u8 gUnknown_08110CDC[];
/* Two ROM blobs that sub_0803F128 selects between on a mode id and hands to
 * sub_0801C70C as its first argument. Nothing matched dereferences either, so
 * the element type is a guess -- only the symbol address is ever used. Their
 * siblings one function up (sub_0803F110 -> gUnknown_081095A4 /
 * gUnknown_0810A6F0) go to Decompress instead, so the two halves of that pair
 * of accessors are NOT the same kind of data. */
/* The Decompress half of the accessor pair the note above describes:
 * sub_0803F110 selects between these two on the same mode id sub_0803F128 uses
 * and hands the result to Decompress, so `u8 []` rather than the
 * `const u16 []` of its twin (wave 20, W20-C). Only the address is ever used;
 * nothing dereferences either. */
extern u8 gUnknown_081095A4[];
extern u8 gUnknown_0810A6F0[];
extern const u16 gUnknown_0810A3E8[];
extern const u16 gUnknown_0810AFC8[];
extern u8 gUnknown_0810BE60[];
/* Wave 30, W30-A: sub_08039EA8 hands it to sub_0801C210 as that function's
 * `void *` first argument -- an OAM/sprite descriptor blob, same role as every
 * other first argument of sub_0801C210. Nothing reads its shape here. */
extern u8 gUnknown_0810BB9C[];
extern u8 gUnknown_0810E6E0[];
/* Wave 29, W29-A. The second of a pair sub_08024330 alternates between on a
 * 40-frame cycle: both are handed to sub_08011C68(const void *, void *, u16) as
 * a 0x20-byte copy into PLTT + 0x1c0, so 0810E720 is 0810E6E0's sibling and
 * takes the same `u8 []` (only the address is used, and sub_08011C68's first
 * parameter is `const void *`). */
extern u8 gUnknown_0810E720[];
extern u8 gUnknown_0810E820[];
/* Two more 32-byte-tile banks in the same family as gUnknown_08106A64 and
 * gUnknown_08108264 below, reached by sub_0802A880 and sub_0802A8AC. The
 * `[][32]` shape is what turns the `lsls #5` into a subscript rather than an
 * open-coded byte offset; 08104464 keeps the family's `& 0x3ff` tile-index
 * mask, 08106864 has none. */
extern const u8 gUnknown_08104464[][32];
extern const u8 gUnknown_08106864[][32];
extern const u8 gUnknown_08106A64[][32];
extern const u8 gUnknown_08108264[][32];
/* Three LZ77 blobs and two 16-colour palettes, paired up by the five
 * sub_08044D70 wrappers at 0x08044C44-0x08044D34: each one hands a blob and a
 * palette to the same starter, which stashes them at +0x4c/+0x50 of the new
 * proc. Sizes from the map: 0x25c/0x274/0x7d0 for the blobs, 0x20 each for the
 * palettes, and 08112F00 opens with the LZ77 header 0x10 / 0x000800.
 * Non-const on both counts -- sub_080453CC hands 0811315C to
 * Decompress(u8 *, void *) and 08113BA0 to ApplyPaletteExt(u16 *, u32, u32),
 * and a const qualifier would break both prototypes under -Werror. */
/* Wave 32, W32-A. A REAL four-byte ROM object, unlike its neighbours
 * gUnknown_08136030/_08136034/_08136038, which baserom.gba shows to be
 * -fforce-addr address constants: this one holds 0x0001 / 0xffff. sub_0804BA64
 * memcpy's all four bytes onto its own stack with sub_0808B6E8 and then
 * subscripts the copy `[side]`, which is what makes it two u16 rather than one
 * word. `const` because sub_0808B6E8's source parameter is `const void *`. */
extern const u16 gUnknown_0813603C[2];
extern u8 gUnknown_08112F00[];
extern u8 gUnknown_0811315C[];
/* Wave 32 (W32-B): the Decompress source sub_08045358 and sub_080453CC share,
 * handed straight to `Decompress(u8 *, void *)` with no other use. */
extern u8 gUnknown_08112704[];
/* Wave 32 (W32-B): the 0x0803F overlay blobs. Each pair is a Decompress source
 * (`u8 *`, first argument of `Decompress(u8 *, void *)`) and an ApplyPaletteExt
 * palette (`u16 *`); the third of each group is the sprite descriptor handed to
 * `sub_0801C70C(const void *, ...)`. Types come from those three signatures --
 * nothing here indexes any of them, so the extents are unknown. */
extern u8 gUnknown_08115A78[];
extern const u8 gUnknown_081161CC[];
extern u16 gUnknown_081169B0[];
extern u8 gUnknown_08117380[];
extern const u8 gUnknown_081183EC[];
extern u16 gUnknown_081190D8[];
/* Wave 32 (W32-B): a tile blob sub_0803FE50 copies out of in 0x80-byte units
 * through sub_08011E54, addressed as `g + ((i & 0x3ff) << 5)` -- i.e. 32-byte
 * tiles with a 10-bit tile index. Byte-addressed, so `u8 []`. */
extern u8 gUnknown_081245F8[];
extern u8 gUnknown_081133D0[];
extern u16 gUnknown_08113BA0[];
extern u16 gUnknown_08113BC0[];
/* Compressed blobs, 0x2f8 and 0x37c bytes. Non-const because both are only
 * ever handed to Decompress(u8 *, void *), whose prototype takes u8 *. */
extern u8 gUnknown_08126244[];
extern u8 gUnknown_0812653C[];
extern u8 gUnknown_081268F8[]; /* handed out by sub_0801F49C */
/* Two ROM tables in the same slot as gUnknown_0849A0F0 -- blobs handed to
 * sub_080152EC / sub_080193B0 by the wrapper family at 0x08004AA0-0x08004B6C.
 * Both are genuine fixed-stride TABLES, not a run of unrelated blobs, and the
 * stride is readable off the addresses without dereferencing anything:
 *
 *   084873BC 08487404 0848744C 08487494 084874DC   stride 0x48, five entries
 *   08487754 084877F4 08487894 08487934 084879D4   stride 0xA0, five entries
 *
 * Corroborated in the ROM image rather than inferred from the deltas alone.
 * Each 0x48 entry holds a THUMB function pointer at +0x10 and the five of them
 * step in lockstep with the 12-byte leaves next door -- +0x10 is 08004A61,
 * 08004A6D, 08004A79, 08004A85, 08004A91, i.e. &sub_08004A60 + 0x0C*n with the
 * THUMB bit set. Every entry also shares the same tail (+0x38 = 08004819,
 * +0x40 = 00000000, +0x44 = 00070000), and the word at 0x08487524 (the sixth
 * 0x48 slot) does not, which is what caps the table at five. The five 0xA0
 * entries likewise share a byte-identical 0x20-byte header (00000013 0 0 0 0
 * 08034F7D 0 0), so the stride is real there too.
 *
 * Declared `const u8 []` anyway, on the same reasoning as gUnknown_0849A0F0:
 * that is the weakest model that reproduces the clean pool word, no matched
 * function indexes or dereferences either table, and both consumers
 * (sub_080152EC's `const void *`, sub_080193B0's `const u8 *`) take them as
 * opaque pointers. Widen them to real struct arrays when a consumer is matched
 * -- the strides above are what the element size will have to be. */
/* Wave 37 (W37-C). A ROM lookup table read as a SIGNED halfword
 * (`lsls r1,r5,#1; adds r1,r1,r0; movs r2,#0; ldrsh r0,[r1,r2]`) and returned
 * straight out of sub_0800A95C, whose one caller sub_0800A588 branches on the
 * result both `bge` and `ble` -- so the entries are signed, not a bool.
 * The index sub_0800A95C builds is a 9-bit neighbour mask (bits 8..0 = NW, N,
 * NE, W, <unused bit 4>, E, SW, S, SE), so the table is at most 0x200 entries;
 * the extent is not otherwise pinned and the array is left incomplete. */
extern const s16 gUnknown_08486BC4[];
/* The same shape, one table earlier: sub_0800A884 reads it with the identical
 * `lsls #1; adds; movs r2,#0; ldrsh` idiom off a 9-bit neighbour mask.  Wave 37
 * (W37-C). */
extern const s16 gUnknown_084867C4[];
/* Wave 50 (W50-E). A 2x49 SIGNED halfword translation table pairing two map
 * object-id spaces, walked in BOTH directions by the twins sub_08001704 and
 * sub_08001A04: `[0][i] -> [1][i]` when the cell to the LEFT of (x, y) carries
 * one of 27 terrain codes, `[1][i] -> [0][i]` otherwise, and -1 on a miss.
 * The 2x49 shape is FIXED, not inferred: data/data.s gives the symbol exactly
 * 0xC4 bytes (98 halfwords) and both loops run i = 0..0x30, so the second row
 * starts at +0x62 -- which is the `adds rN, #0x62` the ROM sets up alongside
 * the base. Every read is `movs rK,#0; ldrsh rD,[rN,rK]`, hence signed.
 * Row 0 begins 1, 0x40, 0x41, 0x42 and row 1 begins 0x21, 0x80, 0x81, 0x82;
 * the last 25 entries of the two rows are identical (0x1C0..0x1D8), which is
 * why a plain "one table read at two offsets" reading does not work. */
extern const s16 gUnknown_0848591C[2][49];
extern const u8 gUnknown_084873BC[];
extern const u8 gUnknown_08487404[];
extern const u8 gUnknown_0848744C[];
extern const u8 gUnknown_08487494[];
extern const u8 gUnknown_084874DC[];
extern const u8 gUnknown_08487754[];
extern const u8 gUnknown_084877F4[];
extern const u8 gUnknown_08487894[];
extern const u8 gUnknown_08487934[];
extern const u8 gUnknown_084879D4[];
/* Six more blobs from the same 0x08487xxx screen-setup table, wave 26 (W26-A),
 * all reached exactly the way their already-declared neighbours above are and
 * declared `const u8 []` for the same reason -- nothing matched indexes or
 * dereferences any of them, and that is the weakest model giving the clean pool
 * word for both consumers' opaque-pointer parameters.
 *   084872B4  sub_080049E8 -> sub_080152EC(blob, 0)
 *   084872FC  sub_08004B7C -> sub_0801A148(blob, 2, 2, 0)
 *   08487AC4  sub_08004BD8 -> sub_080193B0, the "flag clear" arm
 *   08487B64  sub_08004BD8 -> sub_080193B0, the "flag set" arm; the two are
 *             selected by one `?:` on gUnknown_0200B0B0->unk00 & 0x1000 and
 *             share a single `bl`
 *   08487C04  sub_08004C34 -> sub_0801A148(blob, 2, 3, 0)
 *   08487D44  sub_08004D10 -> sub_080193B0 */
extern const u8 gUnknown_084872B4[];
extern const u8 gUnknown_084872FC[];
extern const u8 gUnknown_08487AC4[];
extern const u8 gUnknown_08487B64[];
extern const u8 gUnknown_08487C04[];

/* sub_08004CA0 hands this to sub_0801A104(blob, 2, 2, 0), the same installer
 * shape gUnknown_08487C04 above is used with. Wave 36, W36-I. */
extern const u8 gUnknown_08487C84[];

/* An eight-byte ROM table, `01 03 05 07 03 01 07 05` (dereferenced in
 * baserom.gba, so this is a real object and not a -fforce-addr word -- the
 * word at 0x0808D75C right behind it IS one, holding &gUnknown_0200B0B0).
 * sub_080030BC copies all eight bytes to a stack buffer with sub_0808B6E8 and
 * then reads `buf[lane]` or `buf[lane + 4]`, so it is two four-entry rows of
 * frame counts selected by gUnknown_0200B0B0->unk3e. Wave 36, W36-I. */
extern const u8 gUnknown_0808D754[];
extern const u8 gUnknown_08487D44[];
/* A gUnknown_03001470 list blob, not a proc script: family F057
 * (sub_08004EDC/08004F1C/08004F5C) hands it to sub_080152EC(script, 0), the
 * same pairing gUnknown_08580C7C and gUnknown_08581F7C document. `const u8 []`
 * is what sub_080152EC's `const void *` first parameter takes. */
extern const u8 gUnknown_08487E14[];
/* A gUnknown_03001470 script blob: sub_08004958 hands it to
 * sub_080152EC(script, 0). Same `const u8 []` reasoning as gUnknown_084873BC
 * above -- the ROM shows 8-byte records of {THUMB fn ptr, u32 flags}
 * (0848721C = {0800494D, 00020000}, {08034F7D, 00020000}, {0, 001E0019},
 * {080116A1, 00180000}), which is the same record shape as the 0858xxxx blobs
 * below, but nothing matched indexes it yet. Wave 14. */
extern const u8 gUnknown_0848721C[];
/* Two proc scripts started as a pair -- sub_08011550/sub_080115E0 run 0848923C
 * and sub_0801156C/sub_080115F8 run 0848925C, in both cases stashing a u16 at
 * +0x64 of the new proc. Three more pairs do the same thing and additionally
 * set gUnknown_030030E0.bits.effect: sub_08011588/sub_080115B4 (Proc_Start on
 * tree 3) and sub_08011610/sub_0801163C (Proc_StartBlocking). */
extern const struct ProcCmd gUnknown_0848923C[];
extern const struct ProcCmd gUnknown_0848925C[];
/* A proc script with the ordinary start/stop pair: sub_080111C8 hands it to
 * Proc_Start and sub_08011218 to Proc_EndEach. 0x20 bytes in the split, i.e.
 * four ProcCmds. */
extern const struct ProcCmd gUnknown_0848927C[];
/* A second such pair, with four starters between them. Started on tree 3
 * without the blend write by sub_08011668 (0848929C) and sub_08011684
 * (084892C4), and via Proc_StartBlocking by sub_08013098 (0848929C) and
 * sub_080130B0 (084892C4); all four stash a u16 at +0x64 of the new proc,
 * exactly as 0848923C/0848925C do. */
extern const struct ProcCmd gUnknown_0848929C[];
extern const struct ProcCmd gUnknown_084892C4[];
/* The proc script sub_08013338 runs -- Proc_StartBlocking when its ProcPtr
 * argument is non-NULL and Proc_Start on tree 3 otherwise, per the
 * sub_08013338 note in unknown-functions.h -- and sub_08013378 stops with
 * Proc_EndEach. 0x160 bytes, i.e. 44 ProcCmds. */
extern const struct ProcCmd gUnknown_084893AC[];
/* A proc script: sub_08014BC0 and sub_08014C74 both hand it to Proc_Start. */
/* Two more gUnknown_03001470 script blobs of the gUnknown_0849A108 shape --
 * 8-byte records of (odd THUMB function pointer, u16, u16), NOT the
 * {opcode, dataImm, dataPtr} of struct ProcCmd, whose blobs put the small
 * opcode word FIRST (compare 0x086140D4). `const u8 []` on the same reasoning
 * as gUnknown_0849A108: sub_080152EC takes `const void *` and nothing in the
 * tree dereferences either symbol. 0x20 and 0xbb8 bytes; sub_08014668 starts
 * the first and its byte-identical twin sub_080146D4 the second, which is the
 * only thing that tells the two apart (wave 21, W21-A). */
extern const u8 gUnknown_08489548[];
extern const u8 gUnknown_08489568[];
/* Wave 33, W33-D. Two more gUnknown_03001470 script blobs, and NOT ProcCmd:
 * sub_08014BE8 hands each ADDRESS to sub_08015BD0, the `!= -1` liveness
 * predicate, exactly as src/decomp/c_08014740.c does with gUnknown_08489530
 * and its neighbours -- same `(s32)` cast at the call, same `const u8 []`
 * model, and nothing in asm/ dereferences either. */
extern const u8 gUnknown_0848A120[];
extern const u8 gUnknown_0848A130[];
extern const struct ProcCmd gUnknown_0848A140[];
/* A proc script: sub_08045F80 hands it to Proc_Start on tree 3. */
extern const struct ProcCmd gUnknown_0848A150[];
/* A gUnknown_0200C528 list script, same shape as gUnknown_0849A8F0: its two
 * users hand its ADDRESS around -- sub_08019818 to sub_080193B0 (which stores
 * it into the slot's .unk00/.unk04) and sub_08019850 to sub_08019290, the
 * lookup that reports -1 when no slot holds it. Nothing indexes or
 * dereferences it, so `const u8 []` is the weakest model that gives the clean
 * pool word and it agrees with both consumers' `const u8 *` parameter. */
extern const u8 gUnknown_0848A3EC[];
/* Wave 28, W28-A. A word table of interrupt/handler function pointers:
 * sub_080184A4 indexes it with `(s16)gUnknown_03002F90` scaled by 4
 * (`lsls #0x10; asrs #0xe`) and hands the element straight to sub_08012A54,
 * whose parameter is `void *`. Nothing dereferences the element here, so
 * `void *[]` is the weakest model that agrees with the one consumer. */
extern void *gUnknown_0848A370[];
/* Wave 28, W28-A. Another gUnknown_0200C528 list script in the gUnknown_0849A8F0
 * / gUnknown_0848A3EC mould: sub_08018E7C hands its ADDRESS to sub_0801930C and
 * nothing else in asm/ names it, so `const u8 []` agrees with that consumer's
 * `const u8 *` parameter and gives the clean pool word. */
extern const u8 gUnknown_0848A378[];
/* The gUnknown_03001470 script OPCODE TABLE. sub_08015A30 indexes it with the
 * u16 at +6 of the slot's current command word and calls the entry with the
 * slot index, looping while the entry reports non-zero -- `bl _call_via_r1`,
 * the ordinary one-argument indirect THUMB call, whose register index is what
 * fixes the arity at one. The return is tested with a BARE `lsls #0x18`, i.e.
 * a truth test on a byte, which is why the handlers return `u8` and not `int`
 * -- though a truth test cannot tell `u8` from any wider type either, so this
 * is the weakest fit rather than a proof. Extent unproved: nothing in
 * sub_08015A30 bounds the opcode. sub_08015CF4 has the right shape for an
 * entry (one `u8` slot index in, a constant 1 out) but nothing dereferences
 * this table to confirm it. */
extern u8 (*const gUnknown_0848A160[])(u8);
/* A gUnknown_03001470 slot script, 0x20 bytes. Same start/stop pairing that
 * types gUnknown_0849D41C and gUnknown_08580C7C: sub_08019F90 is
 * `sub_080152EC(0848A42C, 0)` and sub_0801A168 is `sub_0801537C(0848A42C)`.
 * The other four references are sub_08019D0C (sub_08015BD0), sub_08019E68 and
 * sub_0801A614 and sub_0802D33C (all sub_080637AC) -- six in all, every one of
 * them handing the ADDRESS to a consumer that takes
 * it as an opaque pointer; nothing indexes or dereferences it, so `const u8 []`
 * is the weakest model that reproduces the clean pool word. */
extern const u8 gUnknown_0848A42C[];
/* A proc script found rather than started: sub_0801BFFC falls back to
 * Proc_Find(0848B418) when handed a NULL proc, then stores two words at
 * +0x2c/+0x30 of it. */
extern const struct ProcCmd gUnknown_0848B418[];
/* Wave 50 (W50-K). The two OAM shape/size tables, read off sub_0801C090 and
 * sub_0801C53C. Both are indexed by the standard pair (attr0 >> 14) = shape,
 * (attr1 >> 14) = size, and the ROM contents confirm the layout exactly:
 *
 *   0x0848B56C, 64 bytes, u16 pairs {width, height} in PIXELS, 4 shapes x 4
 *   sizes: (8,8)(16,16)(32,32)(64,64) / (16,8)(32,8)(32,16)(64,32) /
 *   (8,16)(8,32)(16,32)(32,64) / (8,8) x4 for the prohibited shape 3. It ends
 *   exactly at gUnknown_0848B5AC below. sub_0801C090 reads element [0] (width)
 *   only, as `base + size*4 + shape*16`.
 *
 *   0x0848B5C4, 24 bytes, u8 pairs {width, height} in TILES, 3 shapes x 4
 *   sizes: (1,1)(2,2)(4,4)(8,8) / (2,1)(4,1)(4,2)(8,4) / (1,2)(1,4)(2,4)(4,8).
 *   It ends exactly where the pointer run at 0x0848B5DC begins.
 *
 * FLAT arrays and not `[][2]`: sub_0801C53C reads both bytes of one entry and
 * the ROM recomputes the subscript for the second (`adds r3,#1; add r3,r8`)
 * rather than using a `ldrb [rN, #1]` displacement, which is what a trailing
 * `[2]` dimension would have produced. */
extern const u16 gUnknown_0848B56C[];
extern const u8 gUnknown_0848B5C4[];
/* A proc script; sub_0801C7B4 is the predicate "is it running", i.e.
 * Proc_Find(0848B5AC) != NULL. */
extern const struct ProcCmd gUnknown_0848B5AC[];
extern u16 *gUnknown_08499578;
/* Wave 46 (W46-G). Both reached by sub_08086BF8/sub_08086CE0.
 *
 * gUnknown_084C3F50 is handed to sub_080149C0's `u8 *` fourth parameter with NO
 * load in front of the pool word -- the symbol IS the data -- in the same
 * argument slot where the other arm passes sub_08024944's `u8 *` result, which
 * is what fixes the element type. 0x20 bytes in data/data-0848B688.s; nothing
 * indexes it, so that extent is the ROM's, not a proved bound.
 *
 * gUnknown_08499CE4 is read `ldrh` at `base + 2 * gUnknown_0300596C` and passed
 * to sub_08014A5C's `int` fourth parameter, so u16 and not an aggregate: the
 * index is scaled by the load, not by a struct stride. 0x18 bytes = 12 entries.
 *
 * NOTE gUnknown_081D943C is NOT an object and must not be declared: the word at
 * that address in baserom.gba is 0x08499578, i.e. it is agbcc's own
 * -fforce-addr address constant for gUnknown_08499578 above. sub_08086BF8
 * reaches the pointer through it three times across a control-flow merge, so
 * naming gUnknown_08499578 directly is what reproduces the double indirection;
 * the promotion carries the word as a `rodata` entry. */
extern u8 gUnknown_084C3F50[];
extern u16 gUnknown_08499CE4[];
/* Wave 41, W41-D. A 0xFF-terminated ROM list of sub_0801F234 ids: sub_0802D2EC
 * walks it from index 0 with a `u8` cursor and calls sub_0801F234 on every
 * entry until the terminator. Plain `ldrb` off the bare symbol at both the
 * pre-test and the loop's own read, so a flat byte array. Non-const because
 * sub_0801F234 takes `int` and nothing here is pointer-typed. */
extern u8 gUnknown_0849AAA8[];
/* Wave 41, W41-D. The -1-terminated menu-entry id list sub_0802D67C filters.
 * SIGNED: the loop's terminator test is `ldrb; lsls #0x18; asrs #0x18` against
 * -1, while the value the body keeps is a bare `ldrb` with no sign extension --
 * an s8 array read into a u8 local. Reached through the -fforce-addr word at
 * 0x08090C08, whose ROM content is 0x081BA054; that word is not an object.
 * Each element indexes gUnknown_085D5ABC. */
extern s8 gUnknown_081BA054[];

/* Wave 40 (W40-F). The option-list overlay: another per-subsystem VIEW of a
 * gUnknown_03001470[] slot, in exactly the situation the note on struct
 * Unk08014074 describes. sub_08019E68 gets the slot from
 * `sub_080637AC(gUnknown_0848A42C)`, which is declared to return
 * `struct Unk03001470 *`, and casts it; sub_08019A60 and sub_08019C40 take the
 * same object as a parameter. struct Unk03001470 CANNOT describe these
 * accesses -- it declares s16 at 0x20 and 0x24 where this is a pointer and the
 * head of a byte array -- and it is deliberately NOT reshaped.
 *
 * Declared here once rather than file-locally in three promoted units, which
 * is the trap the Unk08014074 note flags as an open job for the two views that
 * already went that way.
 *
 * unk20 points at a 0x20-byte entry table terminated by unk00 == 0xff
 * (sub_08019E68's loop test, and `lsls #5` is the stride everywhere).
 * unk24 is indexed by ENTRY index and unk31 by SELECTABLE index -- sub_08019E68
 * writes unk24[i] for every entry but unk31[n++] only for entries whose unk04
 * result has bit 0 clear, and unk40/unk41 are those two counts. That is what
 * bounds the two arrays against each other; neither length is otherwise proved.
 *
 * unk48/unk4a are SIGNED on sub_08019E68's evidence: it hands both to
 * sub_0801A444(s16, s16, s16, s16) with a bare `ldrsh`, which a u16 member
 * cannot produce. sub_08019C40 reading them `ldrh` is not a contradiction --
 * there the only consumer of `unk4a + 1` is an (s16) truncation, so
 * force_to_mode drops the sign extension, the same split recorded on
 * Unk03001470.unk24.
 *
 * unk04 returns `int`, not `u8`: sub_08019E68 stores the result straight into
 * unk24[i] with `strb` and NO `lsls #0x18; lsrs #0x18` first, and agbcc
 * re-narrows a narrow-returning callee at every call site. The three handlers
 * at unk10/unk14/unk18 are unchanged from the wave-33 draft of sub_08019A60. */
struct Unk8019A60Item /* 0x20 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 filler_01[0x03];
    /* 0x04 */ int (*unk04)(void);
    /* 0x08 */ u8 filler_08[0x08];
    /* 0x10 */ void (*unk10)(u8, u8, u8);
    /* 0x14 */ void (*unk14)(u8, u8, u8);
    /* 0x18 */ void (*unk18)(u8, u8, u8);
    /* 0x1c */ u16 unk1c;
    /* 0x1e */ u8 filler_1e[0x02];
};

struct Unk8019A60
{
    /* 0x00 */ u8 filler_00[0x20];
    /* 0x20 */ struct Unk8019A60Item *unk20;
    /* 0x24 */ u8 unk24[0x0d];
    /* 0x31 */ u8 unk31[0x0f];
    /* 0x40 */ u8 unk40;
    /* 0x41 */ u8 unk41;
    /* 0x42 */ u8 unk42;
    /* 0x43 */ u8 filler_43[0x05];
    /* 0x48 */ s16 unk48;
    /* 0x4a */ s16 unk4a;
};
/* A ROM word at 0x0812A178 holding a POINTER to a writable byte blob:
 * sub_0804A124 checks the first four bytes for the signature A5 5A C3 3C and,
 * when it does not match, zeroes bytes 0 and 1 through the same pointer. It is
 * declared non-const because the VALUE is re-loaded after the signature test
 * (`ldr r1,[r3]` off the kept address), which a `u8 *const` would have let
 * agbcc keep live. (wave 34, W34-I) */
/* gUnknown_0812A178 IS FICTIONAL and its declaration has been removed. Wave 35,
 * W35-F: the word at 0x0812A178 holds 0x02028E41, so it is agbcc's own
 * -fforce-addr address constant for gUnknown_02028E41 (the four-byte signature
 * block src/decomp/c_0804A17C.c clears), not an object. Declared `u8 *` it made
 * every read one indirection deeper than the ROM -- pc literal, .rodata word,
 * pointer value, byte, where the ROM has pc literal, .rodata word, byte. Write
 * gUnknown_02028E41 directly; agbcc emits the pool word by itself.
 * sub_0804A124 went 64.8% (+4) -> match on that change alone. */
/* Wave 34, W34-C. A STRIDE-19 EWRAM string table: sub_08032A00 reaches
 * `&gUnknown_02027C2C[gUnknown_0849B060->unk04 * 19]` (the `((n*4 + n) * 4) - n`
 * strength-reduced multiply) and hands it to sub_080149C0's `u8 *` fourth
 * parameter, the same text argument the neighbouring call supplies as a literal
 * "1"/"2"/"3". 19 is a byte count and not an element count -- the base is added
 * to the product with no further scaling. */
extern u8 gUnknown_02027C2C[];
extern u16 *gUnknown_0849957C;
/* Wave 30, W30-D. Two LZ77 blobs sub_08075314 decompresses back to back -- the
 * first to VRAM 0x06000800, the second to gUnknown_0849957C. Both ROM words
 * start with byte 0x10, the LZ77 marker (0x0000C010 and 0x00080010), which is
 * what identifies them. `u8 []` and NOT const because Decompress is declared
 * `void Decompress(u8 *, void *)`; nothing writes them. */
extern u8 gUnknown_081D1398[];
extern u8 gUnknown_081D13E0[];
/* Wave 30, W30-D. A halfword colour table: sub_08075340 is the single
 * statement `gPal[0x59] = gUnknown_081D1624[(a & 0x1F) >> 1];` followed by
 * sub_080135A4(). `u16` from the `ldrh` and from the gPal destination; const
 * because it is ROM and the only reader copies out of it. The `& 0x1F` then
 * `>> 1` caps the index at 15, so at least 16 entries. */
extern const u16 gUnknown_081D1624[];
/* A 0/1 mode flag for the gUnknown_08580DD8 slot script, and the exact twin of
 * gUnknown_0202F2C8 below (same flag, same script pairing, different
 * subsystem). Every one of its six accesses in the whole ROM is a bare
 * `ldrb`/`strb` at displacement ZERO -- so this is a scalar and not an
 * aggregate, by the clean-pool-word rule in docs/agbcc-codegen.md, and the
 * neighbouring gUnknown_0202F204 is a separate object rather than a member.
 * Only 0 and 1 are ever stored (sub_080670A0 and sub_080670BC respectively,
 * each immediately before `sub_080152EC(gUnknown_08580DD8, 2)`), and readers
 * test it `== 0` / `== 1`. Nothing sign-extends it, so `u8` is the weakest
 * model and the signedness is unproved. */
extern u8 gUnknown_0202F200;
/* A u8 ticket counter: sub_0806A054 post-increments it and feeds the OLD value
 * to sub_080674F4. `ldrb`/`strb` and nothing wider. */
extern u8 gUnknown_0202F204;
/* Wave 44 (W44-B). sub_08068038's glyph-lookup cursor into
 * gUnknown_08581160[]. A whole word (`ldr`/`str`), and a GLOBAL rather than the
 * local it obviously wants to be: the function resets it to 0 before the outer
 * loop and again after every hit, and re-loads it at the top of every inner
 * iteration, which is what pins it to memory. Sole reference in the ROM. */
extern s32 gUnknown_0202F208;
/* Another blob reached only by address, handed to sub_080670F8 by
 * sub_0806A054. `const u8 []` is the weakest model that gives the clean pool
 * word, as for the gUnknown_0849A8F0 family. */
extern const u8 gUnknown_085814A8[];
extern u16 *gUnknown_08499580;
/* Wave 35 (W35-B). A third buffer pointer in the same group, reached the same
 * way -- every use is `ldr rN, =sym; ldr rN, [rN]`. sub_08077B74 CpuFastSets
 * 0x140 words of zero into it and then reads it back at the halfword index
 * `proc->unk4a * 32 + t` for sub_08071900's second argument, so `u16 *` on the
 * same evidence as its two neighbours. Not const: the pointer is re-`ldr`ed
 * after the CpuFastSet rather than kept live across it. */
extern u16 *gUnknown_0849957C;
/* Not globals: two `-fforce-addr` address-constant words in code-0801D390.s's
 * pool block, holding &gUnknown_08499580 and &gUnknown_085D5ABC. The block
 * 0x0809099C-0x080909B4 is seven consecutive fan-in-1 address words with
 * &gUnknown_08499590 in four of them, bracketed by ordinary data -- the case
 * already worked in docs/agbcc-codegen.md, and the reason the screen for this
 * class must say "a value that looks like an address" rather than "a RAM
 * value". Declared as pointers to what they point AT so that
 * `pp = &gUnknown_080909A8` supplies the level force-addr invented;
 * sub_08022618 needs `**pp` at all four of its read sites and `(*pp2)[i]` at
 * both of its. */
extern u16 **const gUnknown_080909A8;
extern const struct UnitTypeData *const gUnknown_080909AC;
/* Two more of the same class, from the 0x08091350-0x0809138C block (fifteen
 * consecutive fan-in-1 address words: 0x03003FC0, 0x030040D8 x4, 0x03003100,
 * 0x085D5ABC, 0x08499598 x2 ...). gUnknown_08091364 holds &gUnknown_030040D8
 * and gUnknown_08091368 holds &gUnknown_085D5ABC. The first is typed
 * `struct UnitRecord **` rather than `struct Unk030040D8 **` because
 * sub_08042998 reads the SAME three bitfield offsets (unk04/unk05/unk06)
 * off both it and gUnknown_08499594[i] -- the merge that the note on
 * struct Unk030040D8 says is blocked only by c_080424BC.c's spelling of the
 * global itself, which this separate symbol does not touch. */
extern struct UnitRecord **const gUnknown_08091364;
extern const struct UnitTypeData *const gUnknown_08091368;
/* A fourth 0x400-entry tilemap buffer alongside 08499578/7C/80. NOT const:
 * sub_080616F0 re-`ldr`s it on every iteration of its clear loop. */
extern u16 *gUnknown_08499584;
/* The 0x08068AC4 / 0x080688E4 screen-setup blobs. 0817DA18 is a u16 palette
 * (two ApplyPaletteExt calls, BG palette 1 and OBJ palette 0, 0x20 bytes
 * each); 0817C408 and 0817DA38/0817E208 are Decompress sources; 0817D874 is
 * handed to sub_080718F8 as its second argument; 085813D4 goes to
 * sub_08012C58(void *). Non-const throughout because Decompress takes a plain
 * `u8 *`. */
extern u16 gUnknown_0817DA18[];
extern u8 gUnknown_0817C408[];
extern u8 gUnknown_0817D874[];
extern u8 gUnknown_0817DA38[];
extern u8 gUnknown_0817E208[];
extern u8 gUnknown_085813D4[];
/* A third member of the same 0817Dxxxx group, found in wave 26: sub_08067D04
 * hands it to ApplyPaletteExt(gUnknown_0817D9F8, 0x220, 0x20), i.e. BG palette
 * 0x11 for 0x20 bytes, so `u16 []` and non-const for ApplyPaletteExt's plain
 * `u16 *` -- the same reasoning as gUnknown_0817DA18 above. */
extern u16 gUnknown_0817D9F8[];
/* +0x04 and +0x06 are a SIGNED coordinate pair, and they are reached both ways:
 * sub_0802723C and sub_08027278 read each half as an s16 OBJECT
 * (`movs r3,#4; ldrsh r1,[r2,r3]` -- ldrsh has no immediate-offset form, hence
 * the scratch), while sub_0802C480 compares +0x04 as a whole WORD against a
 * snapshot and then re-reads the same two halves as u16 with an (s16) cast
 * (`ldrh; lsls #16; asrs #16`). That is the same word/halfword duality
 * union Unk802C57CBuf models elsewhere; nothing needs the union here because
 * every access goes through a cast off the u8 * base. */
/* Two ROM slots that both hold &gUnknown_08499590 -- checked against the ROM
 * image. sub_080081E0 and its twin sub_080083E0 are the same 512 bytes apart
 * from which of the two they name, and wave 18 (W18-B) upgraded that from a
 * resemblance to a proof: the two functions' normalised assembly differs in
 * EXACTLY ONE slot, the pool symbol, and a single generated draft compiled
 * against either word scores identically at every one of ~20 spellings.  The
 * source is the same source.
 *
 * CORRECTION (wave 18): the note that used to sit here read 0x0808D800 and
 * 0x0808D804 holding &gUnknown_0200B0B0 as showing "a table of screen handles
 * and not one object".  That is wrong.  Those two are pool words like these,
 * belonging to sub_080085E0 and sub_08008928, which force-addr a different
 * symbol -- the whole 0x0808D6DC..0x0808D8A8 block is one unit's `.LC` pool
 * with one word per (function, symbol) pair, so neighbouring words holding
 * different addresses is the NORMAL case and says nothing about a table.  See
 * the block note below.
 *
 * `u8 **const`, and the const is PROVED rather than assumed: sub_080081E0
 * keeps the outer value in r8 across a `bl sub_080015E4` and re-derefs it
 * afterwards, which a non-const global could not survive -- while the INNER
 * read (gUnknown_08499590 itself) is reloaded after every call, which is why
 * the const sits on the outer pointer only. */
/* Wave 40, W40-A. Three ROM-side tables from the 0x08003/0x08004 block, all
 * read by the `sub_0808B6E8(sp, tbl, N)` stack-copy idiom that opens
 * sub_080035C8, sub_08003640 and sub_08004724:
 *   0x0808D774  u16[4] = { 7, 1, 3, 4 }          (sub_080035C8, copies 8 bytes)
 *   0x0808D77C  u8 [4] = { 7, 1, 3, 4 }          (sub_08003640,  copies 4 bytes)
 *   0x0808D7A0  u16[4] = { 0x2A, 1, 0x87, 0x20 } (sub_08004724, copies 8 bytes)
 * The element width is settled twice over -- by the copy's size argument and by
 * the load at the use (`ldrh` off `i * 2` for the two u16 tables, `ldrb` off
 * `i` for the u8 one) -- and the values are dereferenced in baserom.gba.
 *
 * The two words BETWEEN them are not tables and are deliberately NOT declared:
 * 0x0808D780 holds 0x08485A2C and 0x0808D7A8 holds 0x08499590, so both are
 * ordinary `.LC` slots of the 0x0808D6DC..0x0808D8A8 force-addr pool described
 * below. Writing `gUnknown_08485A2C` honestly reproduces 0x0808D780's chain
 * exactly (sub_08003640, matched). 0x0808D7A8 is the one measured exception in
 * this block: see work/sub_08004724/ -- naming it as `u8 **const` and
 * dereferencing it DOES buy the ROM's register allocation for that function's
 * first pass, where the honest spelling costs one callee-saved register, but it
 * then loses the second pass. Neither spelling closes it; the honest one is
 * kept because it is the one the original source wrote. */
extern const u16 gUnknown_0808D774[];
extern const u8 gUnknown_0808D77C[];
extern const u16 gUnknown_0808D7A0[];
extern u8 **const gUnknown_0808D7F8;
extern u8 **const gUnknown_0808D7FC;
/* NOT globals -- these four are agbcc's own `-fforce-addr` address-constant
 * words (`.LC` pool), and the ROM image proves it: 0x0808D7EC..0x0808D874 is a
 * run of THIRTY consecutive 4-byte symbols, every one holding the identical
 * value 0x08499590, i.e. &gUnknown_08499590.  One word per (function, symbol)
 * pair is exactly what GCC emits; a table of distinct globals is not.  They are
 * declared here only so a matching source can name the word whose relocation
 * the ROM carries -- see the `.rodata` reroute section of docs/agbcc-codegen.md
 * and the c_local recipe, which is what every user below writes.
 *
 * Wave 18 (W18-B) measured the whole block rather than this sub-run, and it is
 * bigger and more regular than the paragraph above implies.  The pool is
 * 0x0808D6DC..0x0808D8A8, 116 slots, ONE translation unit's; 103 of them are
 * referenced from asm/, NO slot is referenced by two functions, and the map
 * from slot to owning function is STRICTLY MONOTONIC in function address with
 * zero inversions, from sub_08000694 through sub_08010DD4.  A function with N
 * distinct force-addr'd symbols owns N CONSECUTIVE slots.  0x0808D8AC is
 * referenced by three functions, so it is a real global and marks the end --
 * shared ownership is the boundary test, because a `.LC` word is never shared.
 * The practical consequence: the pool word belonging to any unmatched function
 * in code.s can be read off by interpolating between its address-order
 * neighbours, before any C is written.  The thirty-slot run named above is just
 * the stretch whose functions all touch the map descriptor.
 *
 * `u8 **const` on the same evidence as gUnknown_0808D7F8: the OUTER value
 * survives a `bl` (sub_0800AA30's case 1 keeps it in r8 across two calls) while
 * the inner read of gUnknown_08499590 is reloaded after every call. */
extern u8 **const gUnknown_0808D81C;   /* sub_0800977C */
extern u8 **const gUnknown_0808D83C;   /* sub_0800A3D4 */
extern u8 **const gUnknown_0808D854;   /* sub_0800AA30 */
/* Wave 37 (W37-C).  sub_0800ABD0 is the one function in the 0x0800A/0x0800B
 * block that did NOT close, and this word is why, so the declaration is kept
 * for the next attempt.  Eleven other functions in the same two blocks
 * (sub_0800A098, sub_0800A2EC, sub_0800A588, sub_0800A6AC, sub_0800A798,
 * sub_0800A884, sub_0800A95C, sub_0800BB2C, sub_0800BCD0, sub_0800BEE4 ...)
 * all matched with the HONEST `gUnknown_08499590` spelling and never needed
 * their pool word named, which is wave 20's finding replicated ten times over.
 *   sub_0800ABD0 is the exception because its ROM materialises the address in
 * the PROLOGUE (`ldr r0,=<word>; mov r8, r0` before the first `cmp`) and then
 * RE-materialises it at the third arm's bound.  Honest spelling gives the lazy
 * single materialisation (726 of 732); `pp = &gUnknown_0808D858;` as the first
 * statement buys the prologue pair but loses the re-materialisation (724);
 * and reading the bound through `*gUnknown_0808D858` -- the c_0800A3D4.c
 * spelling -- force-addrs the pool word's OWN address into a second .rodata
 * word and a second high register (736).  See work/sub_0800ABD0/. */
extern u8 **const gUnknown_0808D858;   /* sub_0800ABD0 */
extern u8 **const gUnknown_0808D86C;   /* sub_0800BF78 */
/* Three more owners of the same block, identified in wave 20 (W20-B) by the
 * address-order interpolation the note above describes: 0x0808D88C is
 * sub_0800CFDC's, 0x0808D890 sub_0800E8CC's, 0x0808D898 sub_0800F8D4's, and all
 * three hold 0x08499590. They are NOT declared here, and that is the finding:
 * writing `gUnknown_08499590` honestly reproduces the ROM's three-level
 * `ldr rN,=<word>; ldr r0,[rN]; ldr r1,[r0]` exactly -- agbcc's own
 * -fforce-addr copy supplies the middle level. Declaring the pool word as
 * `u8 **const` and dereferencing it instead adds a FOURTH level and is wrong.
 * Measured on sub_0800F8D4, wave 20; see docs/agbcc-codegen.md. */
/* A THIRD array besides the +0x0A22 u16 tiles and the +0x417A u16 rowOffset[]:
 * a plain BYTE array reached as `*(base + rowOffset[y] + K)`, with the row
 * offset used unscaled rather than doubled the way sub_08001158 scales it for
 * the tiles. The five predicates at 0x08045848-0x080458F8 read it at
 * K = 0x1432/0x1435/0x1438/0x143f/0x1441 off rowOffset[2], [5], [8] and [9]
 * (+0x417e/+0x4184/+0x418a/+0x418c), and each tests the top three bits of the
 * byte (`ldrb; lsrs #5`) for the value 1 -- so the array holds a small
 * per-cell field packed at bits 5-7. Base and extent are not pinned: nothing
 * yet indexes it with a variable column, so only these five constants are
 * evidence and the K values are absolute byte offsets, not an array base plus
 * a column. */
/* Wave 15 (C) adds four more parallel per-cell arrays, all indexed by exactly
 * the same `rowOffset[y] + x` the c_08001158.c idiom builds, which is what
 * fixes them as views of one grid rather than unrelated blobs:
 *   +0x0012  u8   sub_08058A2C hands it to sub_08026FD0 alongside the +0x1432
 *                 terrain byte, so it is the other half of that predicate's key
 *   +0x051A  u8   a unit id, 1-based: sub_08022618 and sub_08042998 both use it
 *                 to index gUnknown_08499594[] and both treat 0 as "empty"
 *   +0x193A  s8   `ldrsb`, and -1 is the sentinel (sub_08058A2C)
 *   +0x234A  u8   a second flag plane beside +0x051A; sub_08022618 requires
 *                 BOTH non-zero before it will draw the cell
 * and two viewport members that are not per-cell:
 *   +0x000C  u16  camera column, subtracted from x before `& 0xF`
 *   +0x000E  u16  camera row, likewise (sub_08022618)
 * The +0x02 height is now corroborated a second way: sub_08022618 rejects
 * `y >= *(u16 *)(p + 2)` with an UNSIGNED `blo`. */
/* Wave 33 (W33-D) adds one more absolute offset, in the same `*(u16 *)(p + K)`
 * spelling the rest of this block uses:
 *   +0x0010  u16  a mode/flag halfword. sub_0800105C returns 0 and does nothing
 *                 else whenever `(*(u16 *)(p + 0x10) & 0xf) != 0`, which is
 *                 that function's whole guard; nothing else in asm/ reaches it.
 * sub_0800105C also corroborates +0x00 and +0x02 a third way, bounds-checking
 * its cursor against both -- but with a SIGNED `bge`, where sub_08022618 uses
 * an unsigned `blo` on the same halfword. The two readers disagree on the
 * comparison's signedness, so the members' own signedness is NOT settled by
 * either; both sites are consistent with `u16` plus an int-typed cursor.
 *   DO NOT give this symbol a struct type. It is `u8 *` because 63 promoted,
 * byte-exact files do BYTE arithmetic on it (`p + 0x1432 + rowOffset[y] + x`),
 * which a struct pointer would silently rescale. A struct declaration was
 * briefly added here in wave 33 and removed the same session; the cast spelling
 * costs no instructions and every member above is reachable through it.
 *   REFINED, wave 34 (W34-F), and this does NOT contradict the paragraph above:
 * the SYMBOL stays `u8 *`, but the byte arithmetic must be written through a
 * struct declared LOCALLY in the .c and cast onto it --
 * `((struct Map *)gUnknown_08499590)->plane[idx]` -- not as arithmetic on the
 * pointer. The ROM computes every plane address as `(map + K) + idx`, and only
 * a COMPONENT_REF preserves that association:
 *   - `gUnknown_08499590[0x12 + idx]` folds K into the load displacement,
 *     emitting `adds rD,map,idx; ldrb rD,[rD,#0x12]` where the ROM has
 *     `adds rD,map,#0; adds rD,#0x12; adds rD,rD,idx`;
 *   - `*(u16 *)(gUnknown_08499590 + 0x417A + y * 2)` reassociates to
 *     `(map + y * 2) + 0x417A` -- fold's `associate` path pulls the constant
 *     outward past the variable, and a `(u16 *)` cast is not a barrier;
 *   - binding `u16 *rows = (u16 *)(gUnknown_08499590 + 0x417A);` DOES fix the
 *     association, but its `ldr` is emitted with the binding statement, which
 *     reorders the literal pool and loses the register the ROM spends on the
 *     0x417A constant.
 * Measured on four functions this wave: it took sub_08040790 from 89.3% to a
 * match, closed sub_08040200 in one attempt, and fixed all four plane reads in
 * sub_08041EA8 at once. The local struct is per-file and rescales nothing that
 * the 63 byte-exact promoted readers do, so both spellings coexist. */
/* Wave 38, W38-I. TWO MORE MEMBERS: +0x04 and +0x06 are an s16 pair holding the
 * PIXEL scroll origin -- distinct from the +0x0C / +0x0E u16 TILE camera in the
 * wave-15 list above, and reached the same way, through a struct declared
 * locally in the .c and cast onto the `u8 *` base.
 *   Two independent readers agree and neither could have been derived from the
 * other: sub_08028E24 subtracts them from a particle's u16 position before
 * PutSprite (`movs rI,#4 / ldrsh`, the s16-object tell, in int arithmetic), and
 * sub_08028EF0 copies them into two u16 members and then subtracts them from
 * two more. The second reads them with a plain `ldrh` -- that is NOT a
 * contradiction and NOT evidence for u16: every use there is stored straight
 * back into a u16 member, so only the low half is live and agbcc drops the sign
 * extension. The `ldrsh` at the site where the value DOES reach int arithmetic
 * is what settles it, per the wave-37 rule that sign_extend (mem:HI) always
 * folds to `ldrsh`. sub_08028E24 is matched; sub_08028EF0 is parked at 96.6%
 * for an unrelated reason (see work/sub_08028EF0/). */
/* Wave 39, W39-C. One more ROM word holding &gUnknown_08499590: 0x08090A20,
 * i.e. gen_lds.py's gUnknown_08090A20. sub_080240B4 reaches the map through it
 * with the three-level chain (`ldr rA,=gUnknown_08090A20; ldr rB,[rA];
 * ldr rC,[rB]`) while its near-twin sub_0802419C, three instructions of
 * difference away, names gUnknown_08499590 with an ordinary inline pool word
 * and the two-level chain. Same source spelling in both -- this is only
 * -fforce-addr choosing differently, so write the honest name and let the build
 * place the word (contrast the W38-C sub_0803E6C4 case below, where the honest
 * spelling genuinely did not reproduce). */
/* Wave 41, W41-C. FIVE more ROM words holding &gUnknown_08499590, verified by
 * reading baserom.gba directly: 0x08090978, 0x0809099C, 0x080909A4, 0x080909B0
 * and 0x080909B4 all contain 0x08499590. Two neighbours in the same run point
 * elsewhere and are the same kind of thing: 0x080909A0 holds 0x08499594
 * (&gUnknown_08499594) and 0x08090960 holds 0x08499598 (&gUnknown_08499598).
 * gen_lds.py invents a gUnknown_<addr> for each; none is a global. Honest
 * naming reproduced every function that reaches the map this way -- sub_08021D10,
 * sub_08022580, sub_080227A8, sub_08022990 and sub_080212AC, each matched first
 * try, each needing only its one .rodata word placed.
 *   The reference count is what picks the two-level chain over the three-level
 * one, and both spellings appear in this same block: sub_08021CB4 names
 * gUnknown_08499590 ONCE (through a `map` local) and gets the ordinary inline
 * pool word, while the three above name it two or four times inside a loop and
 * get the -fforce-addr word. So do not read the extra `ldr` as a different
 * source expression -- it is the same one, said twice.
 *   Members read through it here: +0x04 / +0x06 are the camera's PIXEL scroll,
 * unwrapped to tile units by `(s16)v >> 4` (`ldrh; lsls #0x10; asrs #0x14`, the
 * cast on a u16 member, not an s16 load), against +0x0c / +0x0e which are
 * already tile-granular. */
/* Wave 41, W41-D -- the 0x08090A5C..0x08090A64 run, and a LIMIT on the rule
 * above. Dereferenced in baserom.gba: 0x08090A5C holds 0x020288A0
 * (&gUnknown_020288A0), 0x08090A60 holds 0x03003FC0 (&gUnknown_03003FC0) and
 * 0x08090A64 holds 0x08499598 (&gUnknown_08499598). The first and third behave
 * exactly as W41-C describes -- naming the real symbol honestly matched
 * sub_08026254 and sub_08026424 first try, each needing only its one .rodata
 * word placed, and neither gUnknown_08090A5C nor gUnknown_08090A64 is an
 * object.
 *   0x08090A60 IS DIFFERENT and the ROM says so directly. sub_08026290's
 * literal pool carries R_ARM_ABS32 gUnknown_08090A60 at +0x68 AND R_ARM_ABS32
 * gUnknown_03003FC0 at +0x6c -- two entries in one function, the first
 * dereferenced (`ldr rA,=word; ldr rB,[rA]; ldrb rB,[rB,#2]`) and the second
 * used as a plain address (`ldr r2,=gUnknown_03003FC0`) at two in-loop sites.
 * The honest spelling produces only ONE word, force-addr'd, whose address then
 * stays live in a register; that is the whole of sub_08026290's remaining
 * +8-byte diff (see work/sub_08026290/). Declaring 0x08090A60 as a real
 * `struct Unk03003FC0 *` was tried and is NOT the fix either -- it reproduces
 * the three-instruction head exactly and then force-addrs gUnknown_03003FC0
 * anyway. So the address words in this run are NOT uniformly one thing, and
 * dereferencing the ROM word tells you the target but not the spelling.
 * UNPROVEN which of the two 0x08090A60 is; recorded so the next agent does not
 * re-derive it. */
/* Wave 48, W48-J.  DO NOT DECLARE gUnknown_0816D93C.  The ROM word at
 * 0x0816D93C holds 0x08499590 -- read out of baserom.gba -- so it is agbcc's
 * own -fforce-addr address constant for THIS symbol and gen_lds.py invented a
 * name for it.  sub_080585D4 reads the map HEIGHT through it
 * (`ldr rA,=gUnknown_0816D93C; ldr rB,[rA]; ldr rC,[rB]; ldrh rD,[rC,#2]`,
 * three levels) and the WIDTH through the plain symbol
 * (`ldr r0,=gUnknown_08499590; ldr r0,[r0]; ldrh r0,[r0]`) in the SAME loop:
 * one object, two spellings, one private pool word for one of the two
 * references.  Spell gUnknown_08499590 honestly; trymatch then reports
 * `different symbols that resolve to the same address` and the promotion
 * carries "rodata": ["0x0816D93C"].  Same reading as gUnknown_0816D994 /
 * gUnknown_0816D998 -> gUnknown_03003F2C recorded above. */
/* Wave 50, W50-A.  A FOURTH -fforce-addr run, dereferenced in baserom.gba and
 * complete -- 0x0816DB40 holds ASCII, so the run is exactly these four words:
 *     0x0816DB30 -> 0x08499590   &gUnknown_08499590   (sub_080620FC)
 *     0x0816DB34 -> 0x03004480   &gUnknown_03004480   (sub_08062560)
 *     0x0816DB38 -> 0x08499598   &gUnknown_08499598   (sub_08062C94)
 *     0x0816DB3C -> 0x08499594   &gUnknown_08499594   (sub_08062C94)
 * NONE of gUnknown_0816DB30 / 34 / 38 / 3C is an object -- gen_lds.py invented
 * a name for each, and it sits immediately after the 0x0816D93C word W48-J
 * documents.  Two of the four point at symbols that are NOT gUnknown_08499590,
 * which is the useful part: the run is one compilation unit's address-constant
 * pool, not "the map's word", so do not assume a 0x0816Dxxx word resolves to
 * the map before reading the ROM.  gUnknown_03004480 is a plain `u16` and the
 * ROM writes it through this word (`ldr rA,=0x0816DB34; ldr rB,[rA];
 * strh rC,[rB]`) while READING it three instructions later through an ordinary
 * inline pool word -- one object, two spellings, same function.  Naming
 * gUnknown_03004480 honestly matched sub_08062560 first try, needing only
 * "rodata": ["0x0816DB34"] placed. */
/* Wave 51, W51-I.  A FIFTH -fforce-addr run for this symbol, both words
 * dereferenced in baserom.gba: 0x0808D798 -> 0x08499590 (sub_080040C8) and
 * 0x0808D79C -> 0x08499590 (sub_0800449C).  Neither gUnknown_0808D798 nor
 * gUnknown_0808D79C is an object.  Both functions use BOTH spellings in one
 * body -- the force-addr word for the `MAP->height` / `MAP->width` loop guards
 * and a plain inline pool word inside the loops -- so honest spelling
 * reproduces both and the promotions carry "rodata": ["0x0808D798"] and
 * ["0x0808D79C"].  Do not read `data_refs` listing a 0x0808D7xx word as a
 * second map. */
extern u8 *gUnknown_08499590;
/* C-facing alias for the map descriptor. The physical spelling remains for
 * assembly references and ROM linkage. */
#define gMapData gUnknown_08499590

/* Wave 50, W50-G. What gUnknown_08499590 points AT, for the readers that reach
 * it by subscript rather than by a single `*(u16 *)(gUnknown_08499590 + K)`.
 * The `u8 *` declaration above is left alone -- every promoted file uses it
 * that way and it stays correct; this is the same object seen through a cast.
 *
 * The three members are not cosmetic, they are what makes sub_08023A4C match.
 * Written flat as `*(u16 *)(gUnknown_08499590 + 0xA22 + idx * 2)`, agbcc's fold
 * reassociates the constant to the END of the address sum, so 0xA22 becomes
 * loop-invariant, LICM hoists it into a register for the whole loop, and every
 * one of the eight reads costs an extra `add`. Reached as `m->unk0a22[idx]` the
 * COMPONENT_REF pins `m + 0xA22` as one value, agbcc loads the 0xA22 pool word
 * inside the arm that needs it and CSEs the sum across all four reads -- which
 * is exactly the ROM. Casting the flat expression does NOT work: STRIP_NOPS
 * removes the cast before fold associates. Verified byte-for-byte on
 * sub_08023A4C and sub_08023BAC.
 *
 * Extents are the gaps between the three known offsets, not measurements. */
struct MapData
{
    /* 0x0000 */ u8 filler_0000[0x0A22];
    /* 0x0a22 */ u16 unk0a22[0xC94];
    /* 0x234a */ u8 unk234a[0x1E30];
    /* 0x417a */ u16 unk417a[0x40];
};
/* Wave 39, W39-C. A 0xFFFF-terminated u16 list with a substitution table packed
 * behind it in the same array. sub_080240B4 and sub_0802419C both scan from
 * index 0 until either 0xFFFF or a match against the map's +0x0A22 tile
 * halfword, count the entries skipped, and read an element back out at
 * `n + (a3 >> 5)` (sub_080240B4) or `n + (a3 >> 5) + 0xa` (sub_0802419C) --
 * the +0xa being the second table's base. Bare `ldrh` off `index * 2` at every
 * one of the four sites, so a plain u16 array, not an aggregate.
 *   THE ROM WORDS 0x08090A24 AND 0x08090A28 BOTH HOLD 0x08499B0C. They are two
 * -fforce-addr pool words for this ONE symbol, and gen_lds.py invents a
 * separate gUnknown_ name for each, so `data_refs` shows the twins reading two
 * different lists when they read the same one. Dereferencing the two words in
 * baserom.gba is what settles it -- this is the wave-19 "several pool words in
 * a run can hold the same address" trap, and here it spans two functions rather
 * than three siblings. sub_080240B4 additionally uses BOTH spellings of the
 * symbol in one body: the 0x08090A24 word for the scan and an ordinary inline
 * pool word for the final read. The honest name reproduces both. */
extern u16 gUnknown_08499B0C[];
/* Wave 39, W39-C. sub_08024720 passes this to sub_0801368C's declared `u16 *`
 * first parameter, either as the bare symbol or as `&gUnknown_0809139C[idx]`
 * with a `lsls #1` scale, which is what fixes the element type at 2 bytes.
 * Unlike its neighbours in this block it is NOT reached through a force-addr
 * word: the ROM loads 0x0809139C straight into a register and adds the scaled
 * index, so the symbol IS the array. */
extern u16 gUnknown_0809139C[];
/* Wave 39, W39-C. Written by sub_080240B4 with a WORD `str`, so 32 bits wide;
 * the value is a zero-extended u16 tile id taken from the map plane at +0x0A22
 * immediately before the scan. Its near-twin sub_0802419C does the same scan
 * and does NOT write it, which is one of the three differences between them.
 * No matched reader, so the width is proved and the signedness is not. */
extern u32 gUnknown_030033F8;
/* Wave 38, W38-C. A ROM word at 0x080912FC holding &gUnknown_08499590, so
 * `*gUnknown_080912FC` IS the map pointer. sub_0803E6C4 reads its outer loop's
 * bound through it with a THREE-level chain -- `ldr r1,=gUnknown_080912FC;
 * ldr r0,[r1]; ldr r0,[r0]; ldrh r0,[r0,#2]` -- while the SAME bound at the
 * loop bottom and the plane reads in the loop body all name gUnknown_08499590
 * directly with the ordinary two-level chain.
 *   This looks like agbcc's -fforce-addr .rodata pool and the wave-18 rule says
 * to name the global honestly and let the build place the word. That was tried
 * FIRST here and does not reproduce: the honest spelling emits the two-level
 * read at the guard as well, one `ldr` short, and the missing register also
 * shifts the whole allocation (the ROM spends r1 on this address, which pushes
 * the loop counter to r2 and costs an `adds r3,r2,#0` copy of the third
 * argument that the honest spelling does not have). Naming the word is what
 * matches. Recorded as UNPROVEN whether the original source named a real
 * `u8 **` global here or whether agbcc force-addr'd only this one reference of
 * the three; the ROM cannot tell those apart, and the neighbouring 0x08091338 /
 * 0x0809133C / 0x08091364 words in this same range ARE documented as pool
 * words. */
extern u8 **gUnknown_080912FC;
/* Grouped 64 entries to a group and 1-based within the group: sub_08025308
 * counts the live `unk00` of `g[(a - 1) * 64 + i]` for i = 1..50, so a group is
 * 64 * 0x0c = 768 bytes (the `(a*2 + a) << 8` in the prologue) and 50 of the 64
 * slots are in use. */
extern struct UnitRecord *gUnknown_08499594;
/* The ROM symbol points at the 64-slot unit-record array. */
#define gUnitRecords gUnknown_08499594
/* Wave 48 (W48-B). A ROM table of NULLARY function pointers, stride 4.
 * sub_0805B3F4 indexes it twice with a plain byte (`lsls #2; adds; ldr; bl
 * _call_via_r0` -- register index 0, so no argument is passed) and does
 * nothing with the result: once by gUnknown_08499594[..].unk0b when that is
 * non-zero, once by a byte out of its own 0x19-byte stack table otherwise.
 * The extent is unproven -- only that both indices are u8-wide. */
extern void (*gUnknown_08576890[])(void);
/* Wave 33, W33-D. A ROM table of signed (dx, dy) pairs indexed by the high
 * nibble of gpKeySt->unk02's low byte, i.e. a direction table: sub_0800105C
 * adds [i][0] to the cursor x and [i][1] to the cursor y. `[][2]` and not flat
 * -- the second read is emitted `base + 2` then `+ i*4`, the reassociated array
 * form. SIGNED from the `movs rI,#0; ldrsh` register-offset reads. */
extern const s16 gUnknown_08499C7C[][2];
/* Wave 33, W33-D. The cursor cell the direction table above steps, and the
 * partner of the pixel-space gUnknown_030032C4 (which moves by four times the
 * same delta). Plain `ldrh`/`strh` at +0 and +2. */
extern struct Unk802C57C gUnknown_030033E4;
/* Wave 34 (W34-H). A two-state frame toggle: sub_08039ACC does
 * `gUnknown_030043F8++; if (gUnknown_030043F8 > 1) gUnknown_030043F8 = 0;` and
 * then uses it as the LAST index of gUnknown_084A0090's animation table, while
 * sub_08039A5C zeroes it with a word `str`. `int` and not `u32`: the wrap test
 * is `cmp r0,#1; ble`, a SIGNED branch. sub_08039ACC reaches it through a
 * -fforce-addr .rodata word (0x08090F6C, whose ROM content is 0x030043F8)
 * because it is referenced on both sides of the wrap's merge; sub_08039A5C,
 * which touches it once, loads the address directly. */
extern int gUnknown_030043F8;
/* Wave 38 (W38-F). sub_08039588's glyph table: a NUL-terminated run of
 * character codes, walked with a `u16` index and compared byte-for-byte
 * against the caller's string (`ldrb` on both sides, no sign extension
 * anywhere), the index doubling as the glyph's slot in the 0x3000-byte
 * decompression buffer. Width is hard from the `ldrb`s; signedness is unproved
 * -- nothing sign-extends it and the only comparison is against another `u8`
 * and against 0. Non-const for the same -Werror reason as the ROM blobs
 * above. */
extern u8 gUnknown_08090F30[];
/* Wave 41, W41-B. THE ROM WORD AT 0x08090940 (gen_lds.py's gUnknown_08090940)
 * HOLDS 0x08499598, i.e. &gUnknown_08499598, and it must NOT be declared as a
 * global. It is agbcc's own -fforce-addr address-constant pool entry, and the
 * honest spelling reproduces it: sub_080208C8 reads the array with the
 * THREE-level chain (`ldr rA,=<pool>; ldr rB,[rA]; ldr rC,[rB]`) at all five
 * sites, and writing plain `gUnknown_08499598[i]` emits exactly that, with
 * trymatch reporting only `relocs: name different symbols that resolve to the
 * same address`. The promotion carries "rodata": ["0x08090940"].
 *   This is the wave-18/39 case, NOT the W38-C sub_0803E6C4 case where naming
 * the word was required -- the difference is that here EVERY reference goes
 * through the pool word, so there is no two-level read for the honest spelling
 * to get wrong. Check that before reaching for a `**` declaration on one of the
 * other accessor words for this same array. */
extern struct ArmyRecord *gUnknown_08499598;
/* Keep the ROM symbol name above for assembly references. The five records
 * are one per army slot, as established by the slot walks documented on the
 * structure itself. */
#define gArmyRecords gUnknown_08499598
/* Wave 34, W34-F. The unit-sprite art sub_0804103C and sub_08041128 install.
 * gUnknown_081213F4 is one flat run of 16-colour palette banks, addressed
 * `gUnknown_08499598[army].unk1a * 0x20` BYTES by both functions and handed to
 * ApplyPaletteExt with a 0x20 length -- a bare `lsls #5; adds` off the symbol,
 * so a flat run and not an aggregate. `u16 []` because that is
 * ApplyPaletteExt's first parameter, which makes the source index the same
 * offset in ELEMENTS (`* 0x10`). The six 08122xxx/08123xxx symbols are
 * the 0x400-byte tile blobs sub_0804103C's switch on the low five bits of its
 * packed selector chooses between; sub_08011E54 copies 0x400 bytes from the
 * chosen one. gUnknown_081218BC is the only one that is itself indexed --
 * `(sub_08024984(sel) - 1) * 0x400` -- so it is a RUN of such blobs while the
 * others are single frames. Non-const: sub_08011E54 and ApplyPaletteExt both
 * take plain non-const pointers, the same reasoning as gUnknown_081D1398. */
extern u16 gUnknown_081213F4[];
extern u8 gUnknown_081218BC[];
extern u8 gUnknown_08122CBC[];
extern u8 gUnknown_081230BC[];
extern u8 gUnknown_081234BC[];
extern u8 gUnknown_081238BC[];
extern u8 gUnknown_08123CBC[];
/* Wave 34, W34-F. A table of Decompress sources indexed by sub_08042DE0's
 * result: sub_08041128 does `ldr r0,[base + i * 4]` and hands the loaded word
 * straight to Decompress, so an array of pointers rather than of data. */
extern u8 *gUnknown_0849FD6C[];
/* Wave 34, W34-F. A byte plane parallel to the unit-id plane at
 * gUnknown_08499590[0x12], addressed with the SAME index --
 * `rowOffset[y] + x` off the 0x417A row table of the gUnknown_08499590 map
 * header. sub_0804189C reads a cell, subtracts *(s16 *)(gUnknown_030013D0 +
 * 0x14) with a floor of zero, and stores it back.
 *   NOT a pool word, but reached through one: agbcc force-addr'd it, and the
 * ROM word at 0x08091338 holds 0x020288B4 while its neighbour 0x0809133C holds
 * 0x08499590 -- the run 0x08091338-0x0809138C is one unit's .LC pool, the same
 * block documented at gUnknown_08091364 below. So the honest spelling names
 * this symbol directly and lets the build place the word. */
extern u8 gUnknown_020288B4[];

/* 0x08090944 -- Wave 49, W49-I. REAL ROM DATA, not a -fforce-addr pool word,
 * and the distinction was checked by dereferencing baserom.gba: the sixteen
 * bytes there are the halfwords {0,3,1,1,1,1,1,0}, while the words on either
 * side (0x08090940, 0x08090954, 0x08090958, 0x0809095C) hold 0x08499598,
 * 0x08499598, 0x030033EC and 0x08499594 -- addresses of objects that already
 * have names, which must NOT be declared. sub_08020984 indexes this one with
 * `ldrb .unk1b; lsls #1; ldrh`, so u16 elements; it is only ever read, so
 * const. Extent 8 from the pool word that follows it. */
extern const u16 gUnknown_08090944[];
/* Wave 33 (W33-A). Four ROM blobs handed to sub_0801BD00's `void *` third
 * parameter with no arithmetic in front of them -- sub_08022AF8 draws a box and
 * passes one per corner, in the order top-left, top-right, bottom-left,
 * bottom-right. Eight bytes apart, so each is at most 8 bytes; nothing indexes
 * or dereferences them here, so the element type is unconstrained and `u8 []`
 * is the weakest that converts to `void *` without a const cast. */
extern u8 gUnknown_08499B6C[];
extern u8 gUnknown_08499B74[];
extern u8 gUnknown_08499B7C[];
extern u8 gUnknown_08499B84[];
/* Pointer to a 0x400-byte RAM buffer; sub_080215D0 copies gUnknown_080C1BC4
 * over all of it byte by byte. The deref is hoisted out of that loop despite
 * the `strb`, so the source bound it to a local. */
extern u8 *gUnknown_0849959C;
extern struct PropertyListEntry *gUnknown_084995A0;
/* Sentinel-terminated map-property coordinate records. */
#define gPropertyList gUnknown_084995A0
/* A ROM byte per unit type, indexed by struct UnitRecord's unk00 and only ever
 * tested against zero -- sub_0804209C rejects a unit whose entry is 0 before
 * looking at anything else about it, so a per-type "eligible" flag in the same
 * family as gUnknown_084995C1 and gUnknown_084995DA. Bare `ldrb` off the
 * unscaled index, so a plain byte array. Unlike those two the address is EVEN,
 * so the odd-address argument for "not an aggregate" is unavailable here; the
 * unscaled subscript is the only evidence for the stride. const is safe: no
 * prototype in unknown-functions.h takes it. (wave 34, W34-I) */
extern const u8 gUnknown_084995A8[];
/* ROM byte table indexed by a u8 read out of a caller-supplied pointer;
 * sub_08042084 returns whether the entry is non-zero. The address is odd, so
 * this is a plain byte array and not an aggregate. const is safe here: no
 * prototype in unknown-functions.h takes it. */
extern const u8 gUnknown_084995C1[];
/* The per-army base UNIT ID, indexed by the 1-based army number in
 * gUnknown_030033EC. sub_080257C0's only reader hands the element straight to
 * sub_08026FD0, whose first parameter is s16 and whose body is `a >> 6` -- and
 * gUnknown_08499594 is grouped 64 entries to an army, so `>> 6` recovers the
 * army from the id. The ROM bears that out: the first five halfwords are
 * 0x0000, 0x0000, 0x0040, 0x0080, 0x00C0, i.e. slot 0 unused and slots 1..4
 * holding 0, 64, 128, 192.
 * The element type is READ as s16 (`movs r1,#0; ldrsh r0,[r0,r1]`), but that
 * is forced by sub_08026FD0's s16 parameter and would be the same bytes for a
 * u16 array, so the signedness is not pinned; every stored value is positive.
 * `const` is safe -- the one consumer takes it by value.
 *   Wave 37, W37-P2: the underlying table really is UNSIGNED. sub_08045090
 * uses an entry as the base of a gUnknown_08499594 subscript and the ROM reads
 * it with a bare `ldrh`, so the matching source is
 * `(u16)gUnknown_084995FE[army]`; without the cast agbcc emits `movs r1,#0;
 * ldrsh r0,[r0,r1]` and that was the only remaining diff. work/sub_080287D0
 * measured the same thing independently in wave 34. The declaration is left
 * `s16` because retyping it would push a conversion onto the promoted
 * src/decomp/c_080257C0.c, whose sub_08026FD0 call wants s16 anyway -- the
 * cast at each unsigned reader is the cheaper contract. */
/*   Wave 38, W38-L: sub_080271CC replicates the `(u16)` reading a third time
 * (bare `ldrh`, no sign extension) and BOUNDS the block it addresses -- it
 * scans gUnknown_08499594[base + 1 .. base + 0x32] for a non-zero unk00, so an
 * army owns at most FIFTY units inside the 64-entry group the `>> 6` note
 * above describes. The 1-based start says entry `base + 0` is not a unit. */
/* Wave 39 (W39-E), from sub_08028580. Indexed by army slot and 16 bits wide:
 * the main loop reads it `lsls #1; adds; ldrh` and compares the whole halfword
 * against `entry->flags & 0xe0`. The other three sites narrow to `ldrb`, but
 * each of those either stores into a u8 map-plane byte or feeds sub_080240B4's
 * `u8` third parameter, so agbcc is narrowing the load to the use rather than
 * reading a byte table.
 *
 * NOT `const`, and that is MEASURED rather than assumed -- it is worth 4 bytes
 * in sub_08028580 and is the only oracle this project has found for a `const`
 * on a ROM table. The address is in .rodata and every site is a read, so the
 * obvious declaration is `const u16`; it is wrong. sub_08028580 stores into the
 * map plane THROUGH gUnknown_08499590 and then passes gUnknown_084995F4[team]
 * to sub_080240B4. With `const`, agbcc proves the store cannot touch the table
 * and reuses the register it loaded for the store; the ROM reloads it (`ldrb
 * r2,[r5]` after the `strb`), which only happens when the store through an
 * unknown pointer is allowed to alias the global. So the original source did
 * not spell these tables `const`, whatever section they landed in. */
extern u16 gUnknown_084995F4[];
/*   Wave 41 (W41-C): the `const` is GONE, and for the same measured reason the
 * sibling gUnknown_084995F4 lost its own two notes below -- LICM is the oracle.
 * sub_080212AC opens with `for (i = 0; i <= 0x32; i++)
 * sub_080211DC((u8)(gUnknown_084995FE[army] + i), 1);` and the ROM re-reads the
 * table element INSIDE that loop (`ldrb r0,[r6]` at the top of every
 * iteration, with r6 = &gUnknown_084995FE[army] hoisted).  With `const`, agbcc
 * proves the `bl` cannot write the table, hoists the load into the preheader
 * and emits one `ldrb` before the loop -- an instruction in the wrong place and
 * a different register assignment for the rest of the function.  Removing
 * `const` was re-verified against every existing reader: sub_080257C0,
 * sub_080287D0, sub_08045090, sub_08045924, sub_080459E4, sub_08045A78 and
 * sub_08045D10 all still match byte-for-byte (their reads sit in loops with no
 * intervening call or aliasing store, so the qualifier never bought them
 * anything).  The `(u16)` casts those files carry are about SIGNEDNESS and are
 * unaffected. */
extern s16 gUnknown_084995FE[];
/* A tree-3 proc script pair: sub_08027278 starts 08499CFC and sub_0802723C
 * starts 08499D2C, with identical bodies that differ only in the script and in
 * the constant stashed at +0x54 (0 and 2). sub_0802759C reports whether an
 * 08499CFC proc is live. */
extern const struct ProcCmd gUnknown_08499CFC[];
extern const struct ProcCmd gUnknown_08499D2C[];
extern void *gUnknown_08499E38[];
/* A proc script: sub_08028848 hands it to Proc_Start on tree 3 and stashes two
 * u16 arguments at +0x64 and +0x66 of the new proc. */
/* Wave 39 (W39-E), from sub_08028580: `(gUnknown_08499598[army].unk1a - 1) * 2`
 * indexes it with `ldrh` and the halfword goes straight to sub_08019818's `u16`
 * first parameter. Same 1-based unk1a index as the palette tables noted above,
 * so this is a parallel per-army table.
 * NOT `const`, by consistency with gUnknown_084995F4 rather than by
 * measurement -- this table is read once, before any store, so nothing here
 * discriminates. See that symbol's note for the aliasing oracle. */
extern u16 gUnknown_08499FA0[];
extern const struct ProcCmd gUnknown_08499FAC[];
/* Wave 38, W38-I. An OAM sprite blob: sub_08028E24 hands its ADDRESS straight to
 * PutSprite's `u16 *` fourth parameter (`ldr r3, =gUnknown_08499FE4` as the last
 * argument register set up before the `bl`) with no arithmetic and no
 * dereference, the same model as the other blobs on that parameter. NOT const,
 * because PutSprite's parameter is not. Extent unknown -- nothing indexes it. */
extern u16 gUnknown_08499FE4[];
/* A proc script: sub_08028ED0 is its sole user and hands it to
 * Proc_StartBlocking. */
extern const struct ProcCmd gUnknown_08499FEC[];
/* ROM blob handed to sub_080152EC by sub_08042B70 and sub_08042B84, which is
 * the same slot the routine stores in gUnknown_03001470[i].unk04 (a
 * `const void *`). Nothing indexes it, so `const u8 []` is the weakest model
 * that reproduces the clean pool word; widen it when the consumer is matched. */
extern const u8 gUnknown_0849A0F0[];
/* The same slot as gUnknown_0849A0F0, and the pairing is what proves it: every
 * one of these is named exactly twice in the ROM, once by a wrapper that hands
 * its ADDRESS to sub_080152C0/sub_080152EC and once by a wrapper that hands the
 * same address to sub_0801537C, which scans gUnknown_03001470[] for the slot
 * whose .unk00 equals it and stops that slot. Start/stop pairs:
 *
 *   0849A108  sub_0802A514 (sub_080152C0, arg 1) <-> sub_0802A528
 *   0849D41C  sub_08037610 (sub_080152EC, arg 0) <-> sub_08037628
 *   0849D55C  sub_08037F58 (sub_080152C0, arg 0) <-> sub_08037F70
 *   0849E6D4  sub_0803B240 (sub_080152C0)        <-> sub_0803B254
 *   084C2198  sub_080470F8 (sub_080152EC)        <-> sub_080470E8
 *   0849E670  sub_0803B16C (sub_08015BD0 slot)   <-> sub_0803B15C
 *   0849E700  sub_0803B2BC (a long builder)      <-> sub_0803B33C
 *
 * Sizes from the split: 0x20, 0x18, 0x10, 0x24, 0x30, 0x34, 0x10 -- all word
 * multiples but NOT a common stride, so these are individual blobs and not
 * entries of one table. `const u8 []` for the same reason gUnknown_0849A0F0 is:
 * it is the weakest model that reproduces the clean pool word, nothing matched
 * indexes or dereferences any of them, and both consumers take them as opaque
 * pointers (sub_080152EC and sub_0801537C are both `const void *`). Widen when
 * a consumer is matched. */
extern const u8 gUnknown_0849A108[];
extern const struct Unk0849A2C8 gUnknown_0849A2C8[];
extern const struct Unk0849A354 gUnknown_0849A354[];
/* Wave 35 (W35-J): a proc script blob, reached only as an address and handed
 * to sub_080152EC(const void *, u8) by sub_08029CB8 with the literal 0 second
 * argument every other wrapper passes. Same `const u8 []` model as
 * gUnknown_0849A3C0; the pool word stays clean. */
extern const u8 gUnknown_0849A0A8[];
/* Wave 35 (W35-J): a u16 ROM table indexed by sub_0802B6C8's 0..2 return.
 * sub_0802B768 and sub_0802B7E8 both index it with `lsls #0x18; lsrs #0x17`
 * -- a zero-extended byte scaled by TWO, i.e. a halfword element -- and read
 * the element with `ldrh`. The ROM holds 0x001e, 0x0042, 0x0058, 0x0000,
 * 0x001c, 0x003f at 0x0849A2A0.., consistent with halfwords rather than words.
 * Only elements 0..2 are reachable through sub_0802B6C8, so the extent is
 * left open. */
extern const u16 gUnknown_0849A2A0[];
/* Wave 35 (W35-J): three s16 per entry, indexed `[i * 3 + 1]` -- sub_0802AA14
 * scales i by 6 (`lsls #1; adds; lsls #1`) and reads with the register-offset
 * `movs rI,#0; ldrsh`, the s16-object tell. It starts two bytes past the
 * gUnknown_0849A2A0 halfword table above and is a separate table. */
extern const s16 gUnknown_0849A2A6[];
/* Wave 35 (W35-J): sub_0802AA14 reads +2 and +3 with plain `ldrb` and adds
 * them to an x and a y, so a small byte table of offsets. */
extern const u8 gUnknown_0849A284[];
/* Wave 35 (W35-J): an object template handed to sub_0801BD00's `void *` third
 * parameter -- same model as gUnknown_0849A3B8. */
extern u16 gUnknown_0849A248[];
/* Wave 35 (W35-J): a proc script, reached only as an address by
 * Proc_Start(gUnknown_0849A480, 3) in sub_0802BFD0. */
extern const struct ProcCmd gUnknown_0849A480[];
/* Wave 35 (W35-J): ROM graphics/palette blobs reached only as addresses.
 * gUnknown_0810E9E0 is sub_08011E54 source data (sub_0802B8C4);
 * gUnknown_081248F8 is Decompress source, gUnknown_08125190 an
 * ApplyPaletteExt palette and gUnknown_08124FB8 a sub_0801C70C object blob
 * (all three sub_0802BFD0). Same `const u8 []` model as gUnknown_0849A3C0;
 * ApplyPaletteExt's u16* and Decompress's u8* are taken by cast at the call,
 * which is byte-neutral. */
extern const u8 gUnknown_0810E9E0[];
extern const u8 gUnknown_081248F8[];
extern const u8 gUnknown_08124FB8[];
extern const u8 gUnknown_08125190[];
/* Wave 35 (W35-J): the ROM words at 0x08090BB8 and 0x08090BBC BOTH hold
 * 0x03003130, i.e. &gUnknown_03003130 -- agbcc -fforce-addr pool words, not
 * objects, and neither is declared here for that reason. They are the same
 * case as gUnknown_08090BC0 / gUnknown_08090BC4 (0x030033E4 and 0x03003130)
 * documented below: sub_0802B768 and sub_0802B7E8 name gUnknown_03003130
 * directly and the three-level read reproduces exactly, including the reload
 * of the address after every call. Dereferenced in baserom.gba before use. */
/* More ROM blobs handed to sub_080152EC with a 0 second argument, the same slot
 * as gUnknown_0849A0F0 -- one trivial wrapper each (sub_0802BF80, sub_0802BF94,
 * sub_0802BFA8, sub_0802BFBC, sub_0802C130). `const` because sub_080152EC's
 * prototype takes `const void *`; `u8 []` because nothing indexes or
 * dereferences them, so it is the weakest model that gives the clean pool word.
 */
extern const u8 gUnknown_0849A3C0[];
extern const u8 gUnknown_0849A3F0[];
extern const u8 gUnknown_0849A428[];
extern const u8 gUnknown_0849A450[];
extern const u8 gUnknown_0849A4A0[];
/* Three more scripts for the gUnknown_0200C528 list system, exactly the
 * gUnknown_0849A8F0 shape below: each has a one-line wrapper handing its
 * ADDRESS to sub_080193B0 (0849A520 -> sub_0802C144, 0849A5E0 -> sub_0802C1B0,
 * 0849A6B0 -> sub_0802C1C0). Same `const u8 []` reasoning. */
extern const u8 gUnknown_0849A520[];
extern const u8 gUnknown_0849A5E0[];
extern const u8 gUnknown_0849A6B0[];
/* A script for the gUnknown_0200C528 list system, not a proc script: its three
 * users hand its ADDRESS around (sub_0802C280 -> sub_080193B0, which stores it
 * into gUnknown_0200C528[i].unk00 and .unk04; sub_0802C290 -> sub_0801930C),
 * and sub_0802C2A0 returns whether its pointer argument equals it. Nothing has
 * indexed or dereferenced it, so `const u8 []` is the weakest model that gives
 * the clean pool word; widen it when sub_080193B0 is matched. */
extern const u8 gUnknown_0849A8F0[];
/* A ROM blob handed to sub_080152EC as its `const void *` first argument by
 * sub_0802C2B4, which does nothing else with it -- exactly the
 * gUnknown_0849A4A0 / gUnknown_0849A0F0 slot. `const u8 []` is the weakest
 * model that gives the clean pool word. */
extern const u8 gUnknown_0849A990[];
/* Four ROM blobs in the same slot, each named by exactly ONE function in asm/
 * and each handed straight to sub_08019F2C or sub_0801A104 as a `const void *`
 * that neither dereferences. `const u8 []` on the gUnknown_0849A0F0 reasoning.
 * They pair off by caller: sub_0802D458/sub_0802D558 take AAC0/AE28 through
 * sub_0801A104, sub_0802D4B0/sub_0802D504 take AC60/ABC0 through sub_08019F2C,
 * and each pair is a byte-identical duplicate whose ONLY difference is which
 * of the two it names (wave 21, W21-A). */
/* A proc script: sub_0802CD28 hands it to Proc_Start on tree 3 and writes no
 * field of the proc it returns. `const struct ProcCmd []` is what proc.h's
 * Proc_Start takes. */
extern const struct ProcCmd gUnknown_0849AA68[];
extern const u8 gUnknown_0849AAC0[];
extern const u8 gUnknown_0849ABC0[];
extern const u8 gUnknown_0849AC60[];
/* A ROM blob handed to sub_080152EC with the literal 0 second argument by
 * sub_0802CD78, which does nothing else with it -- the gUnknown_0849A0F0 slot.
 * `const u8 []` is the weakest model that gives the clean pool word. */
extern const u8 gUnknown_0849ADD0[];
extern const u8 gUnknown_0849AE28[];
/* Wave 41 (W41-E), EXTENDED BY WAVE 42 (W42-K) FROM 0x08090CDC TO 0x08090CF0.
 * THE WHOLE ROM RUN 0x08090CA4..0x08090CF0 IS agbcc
 * `-fforce-addr` ADDRESS CONSTANTS FOR THESE TWO POINTERS -- do NOT declare
 * gUnknown_08090CA8 / CAC / CB0 / CB4 / CB8 / CBC / CC4 / CC8 / CCC / CD0 /
 * CD4 / CD8 / CE0 / CE4 / CE8 / CEC / CF0 as globals, and do not read the
 * three-deep
 * `ldr rN,=sym; ldr rM,[rN]; ldr rK,[rM]` chain in front of them as a
 * pointer-to-pointer. baserom.gba holds &gUnknown_0849B018 at CA8, CB8, CBC,
 * CC8, CCC, CD0, CD4, CD8 and &gUnknown_0849B01C at CAC, CB0, CB4, CC4 --
 * every function gets its own private copy, so eight different invented names
 * all denote the one record.
 *   Wave 42 (W42-K) extends the run by five words on the same evidence:
 * baserom.gba holds 0x0849B018 at CE0, CE4, CE8, CEC and CF0 too, and
 * 0x08090CF4 holds 0x0CA50CA4, which is not an address -- that is where the
 * run ends. sub_08030930, sub_08030C54, sub_08030D84 and sub_08030E40 reach
 * the record through CDC / CE8 / CEC / CF0 and all four matched on the honest
 * spelling, each carrying its own one-word "rodata" entry. sub_08030DEC is
 * the control that shows what triggers it: it names gUnknown_0849B018 ONCE,
 * gets a direct `ldr =gUnknown_0849B018` with no ROM word at all, and matches
 * with `relocs: match`. The honest spelling `gUnknown_0849B018->member`
 * reproduces both the pool word and the register allocation; trymatch reports
 * `relocs: name different symbols that resolve to the same address` and the
 * promotion just carries a "rodata" entry.
 *   Wrongly modelling one of these as a real global is how struct
 * Unk08090CD8Buf / struct Unk08090CD8 came to exist next to struct
 * Unk0849B018: they describe the SAME RAM record reached through
 * &gUnknown_0849B018 at 0x08090CD8, which is why their 0x88-byte entry array
 * sits at the same 0x12c. sub_080308B4 is promoted against that model and
 * matches, so it is byte-neutral and is left alone -- but nothing new should
 * be typed against it. */
extern struct Unk0849B018 *gUnknown_0849B018;
/* Wave 42 (W42-K). A 13-byte ROM template that sub_08030D84 copies verbatim into
 * gUnknown_020256DA before handing the packet to sub_0802F588 -- `for (i = 0;
 * i <= 0xc; i++)`, a plain `ldrb`/`strb` pair with both bases hoisted out of the
 * loop. `const` is safe here and is NOT the case the gUnknown_084995F4 note
 * warns about: the destination is a named global rather than an unknown pointer,
 * so no aliasing conclusion turns on the qualifier. Verified byte-identical. */
extern const u8 gUnknown_0849B038[];
/* Wave 33, W33-B: sub_08030F60 reads this as a u16 table indexed
 * `[a1*4 + k]` (the `lsls #2` element index reused for +1/+2/+3 sibling
 * reads is the u16-array tell, not a struct). Four halfwords per a1: [0] and
 * [1] go straight to the u8 shadows gUnknown_03002B40 / gUnknown_03002EFC,
 * [2]+[0] and [3]+[1] to gUnknown_03002B4C / gUnknown_03002B44.
 *
 * NOT `const`, and wave 42 (W42-L) REMOVED the qualifier it used to carry --
 * that one word was the whole of sub_08030F60's -4 byte wave-33 park. It is a
 * ROM table, so `const` is what the original source "should" say, but it is an
 * aliasing oracle: with RTX_UNCHANGING_P set, the `strb` to gUnknown_03002B40
 * does not invalidate cse's memory entry for g[i], so the `g[i+2] + g[i]` that
 * follows REUSES the HImode value already in a register. Without it, cse must
 * reload -- and because the sum is stored to a u8, shorten_binary_op has
 * narrowed the addition to QImode, so the reload comes back as a second,
 * narrower `ldrb` off the address the previous statement left live. Two such
 * reloads, one per sum, exactly the 4 bytes that were missing. gUnknown_0849B188
 * has no other reader in src/, so nothing else moves.
 *
 * The same tell is worth reusing: A DEAD-LOOKING SECOND LOAD OF AN ARRAY
 * ELEMENT AT A NARROWER WIDTH THAN THE FIRST IS A NON-const TABLE, not a
 * spelling problem. Compare the gUnknown_0849B038 note above, where `const` was
 * verified byte-identical and is therefore safe -- the qualifier is only
 * load-bearing when a store to some other global sits between two reads of the
 * same element. */
extern u16 gUnknown_0849B188[];
extern struct Unk0849B01C *gUnknown_0849B01C;
extern struct Unk0849B060 *gUnknown_0849B060;
/* Proc scripts, all only ever handed to Proc_Start/Proc_StartBlocking:
 *   0849B3CC  sub_080342DC, blocking, parent forwarded
 *   0849B8B8  sub_08034308, blocking, parent forwarded
 *   0849BB50  sub_080338C0, Proc_Find, stashes a word at +0x38
 *   0849BC98  sub_0803433C, parent forwarded
 *   0849D56C  sub_08037F80, tree 3
 *   0849D77C  sub_08039634, tree 3, stashes words at +0x54/+0x58
 *   0849D7FC  sub_08039930, parent forwarded, stashes a word at +0x54
 *   0849D82C  sub_08039BB4, tree 3, stashes bytes at +0x29/+0x2a and a
 *             halfword at +0x2c
 *   0849E728  sub_0803B55C, tree 3, stashes a HALFWORD at +0x64
 *   0849E778  sub_0803B6E8, tree 3, stashes a word at +0x54
 *   0849E7A0  sub_0803B774, tree 3
 *   0849E7B8  sub_0803B788, tree 3
 *   0849EAAC  sub_0803B9EC, tree 3
 *   0849EBBC  sub_0803BADC, tree 3, after gUnknown_03003FC0.unk01 = 1
 *   0849EC1C  sub_0803BA00, tree 3, after gUnknown_03003FC0.unk01 = 2
 *   0849ECE0  sub_0803B8C4, tree 3, after gUnknown_03003FC0.unk01 = 3
 *   0849F5D0  sub_0803D960, blocking, stashes 6 at +0x64
 *   0849F888  sub_0803F2B8, blocking, parent forwarded (4th param), stashes
 *             words at +0x2c/+0x30/+0x4c
 *   0849F918  sub_0803F3E4, parent forwarded (3rd param), stashes words at
 *             +0x2c/+0x30 */
extern const struct ProcCmd gUnknown_0849B3CC[];
/*   0849B284  sub_08031418, parent forwarded, stashes 0 at +0x58
 *   0849B2A4  sub_08031E44, parent forwarded, stashes 100/100/72 at
 *             +0x68/+0x64/+0x66
 *   0849B62C  sub_08032454 starts it (parent forwarded) and sub_08032468
 *             Proc_EndEach's it
 *   0849B6B0  sub_08032D4C, parent forwarded, no payload
 *   0849BB28  sub_080337D8, blocking, parent forwarded (3rd param), stashes
 *             words at +0x20/+0x24 -- see c_080337D8.c, the object is NOT a
 *             Proc */
extern const struct ProcCmd gUnknown_0849B284[];
/*   0849B294  and 0849B304 are the odd two out of this group: each is named
 *             exactly ONCE in the whole ROM, by its Proc_EndEach wrapper
 *             (sub_08031CE4 and sub_080320F4). Nothing starts them, so
 *             `const struct ProcCmd []` here comes from Proc_EndEach's
 *             prototype and from the sizes being ProcCmd multiples (0x10 = 2
 *             cmds, 0xC8 = 25 cmds), not from a matching Proc_Start. */
extern const struct ProcCmd gUnknown_0849B294[];
extern const struct ProcCmd gUnknown_0849B2A4[];
extern const struct ProcCmd gUnknown_0849B304[];
extern const struct ProcCmd gUnknown_0849B62C[];
extern const struct ProcCmd gUnknown_0849B6B0[];
/* Wave 30, W30-A: the two scripts sub_080321A8 waits on -- it Proc_Break's its
 * own proc only when NEITHER is running. Typed from Proc_Find's prototype. */
extern const struct ProcCmd gUnknown_0849B7D8[];
extern const struct ProcCmd gUnknown_0849B868[];
extern const struct ProcCmd gUnknown_0849B8B8[];
extern const struct ProcCmd gUnknown_0849BB28[];
extern const struct ProcCmd gUnknown_0849BB50[];
/* Wave 50, W50-J. A proc script, only ever reached through Proc_GotoScript:
 * sub_08033230 jumps its OWN proc to it when the link-setup screen is idle
 * (proc->unk64 == 0) and gpKeySt->held carries 0x2. Typed from
 * Proc_GotoScript's declared `const struct ProcCmd *` second parameter, the
 * same model as gUnknown_08614284 below. */
extern const struct ProcCmd gUnknown_0849BA68[];
/* Wave 31, W31-B. Two proc scripts in the 0x0861 block: sub_0803343C starts
 * gUnknown_08614284 on tree 3 and touches no field of the proc it gets back,
 * and sub_08033404 Proc_Find's BOTH of them -- breaking its own proc only when
 * neither is alive, the same shape wave 30 recorded for gUnknown_0849B7D8 /
 * gUnknown_0849B868. Typed from Proc_Start's and Proc_Find's prototypes. */
extern const struct ProcCmd gUnknown_08614284[];
extern const struct ProcCmd gUnknown_0861429C[];
extern const struct ProcCmd gUnknown_0849BC98[];
extern const struct Unk0849CD88 gUnknown_0849CD88[];
/* A proc script, only ever handed to Proc_Find: sub_0803710C is the whole of
 * `return Proc_Find(gUnknown_0849D3BC) != 0;`. */
extern const struct ProcCmd gUnknown_0849D3BC[];
/* Two more gUnknown_03001470 script blobs -- see the gUnknown_0849A108 comment
 * above for the start/stop pairing that types them. */
extern const u8 gUnknown_0849D41C[];
extern const u8 gUnknown_0849D55C[];
extern const struct ProcCmd gUnknown_0849D56C[];
/* Wave 38 (W38-F). sub_08039188's marker-tile table, subscripted
 * `[sub_080390CC(i)][sub_08039064(i)]`: the ROM scales the first index by 10
 * and the second by 2 and adds them before one `ldrh`, so the row stride is 5
 * halfwords and the element is u16. The value is OR-ed with 0x3000 into a
 * PutSprite OAM word, i.e. a tile number plus palette/priority bits. Row count
 * is not proved -- only the stride is. */
extern u16 gUnknown_0849D5C4[][5];
extern struct Unk0849D5F8 *gUnknown_0849D5F8;
/* Wave 50, W50-L. Six ROM halfword tables, five of them in one contiguous blob
 * at 0x0849D3DC-0x0849D534. Every read is a bare `ldrh` off a `lsls #1` index,
 * which fixes the element at u16; const because the addresses are in ROM.
 *
 * NONE of them is a -fforce-addr pool word: each address is loaded and then
 * INDEXED by a computed subscript before any dereference, which is wave 49's
 * inverse test for a real table. (The trap is load-then-IMMEDIATELY-deref.)
 *
 * sub_080378A8 uses four. gUnknown_0849D3DC is indexed by the terrain byte's
 * low five bits and gives the base of that terrain's tile run;
 * gUnknown_0849D474 appears TWICE with different meanings -- once as
 * `[gUnknown_0849D3DC[cell & 0x1f] + (cell >> 5)]` to pick a tile id, once as
 * the x-offset table `[x]` -- because the tables are contiguous and agbcc CSEs
 * the one base register across both. gUnknown_0849D4F4 is the matching
 * y-offset table. gUnknown_0849D434 is a blank tile quad (halfwords at +0, +4,
 * +8, +0xc) copied whenever (x, y) falls outside the map.
 *
 * sub_08037A78 uses gUnknown_0849D534 as a four-halfword AND mask applied to a
 * destination tile quad before the source quad is OR'd in, and
 * gUnknown_08582E74 indexed by `gUnknown_030040F8[(terrain >> 6) + 1] + 0x12`.
 * Signedness is unproved for all six: every loaded value is only ever used as
 * an index or a mask. */
extern const u16 gUnknown_0849D3DC[];
extern const u16 gUnknown_0849D434[];
extern const u16 gUnknown_0849D474[];
extern const u16 gUnknown_0849D4F4[];
extern const u16 gUnknown_0849D534[];
extern const u16 gUnknown_08582E74[];
/* Wave 38 (W38-F). sub_08039850's scripted-line table: a table of POINTERS
 * indexed by `gUnknown_03003FC0.unk02 - 0x8a` (`ldrb; subs #0x8a; lsls #2;
 * ldr`), each pointing at a -1-terminated run of 8-byte records (`adds r5,#8`
 * per step, `movs r0,#1; rsbs r0,r0,#0` for the sentinel). A NULL pointer
 * means "no scripted lines for this map", which sub_08039850 tests first.
 * Member widths are all from sub_08039850's own loads; the entry is ROM data
 * so the whole thing is const. */
struct Unk0849D62C
{
    /* 0x00 */ u8 unk00; /* bare `ldrb`, compared against the proc's `int`
                          * unk54 (an army slot) and treated as "any" when 0 */
    /* 0x01 */ s8 unk01; /* `movs r0,#1; ldrsb r0,[r5,r0]` -- SIGNED, and it is
                          * the -1 terminator as well as the terrain id it is
                          * matched against */
    /* 0x02 */ u8 unk02; /* bare `ldrb`, compared against sub_08044374's int */
    /* 0x03 */ u8 filler_03[0x01];
    /* 0x04 */ u16 unk04; /* bare `ldrh`, handed to sub_080397F4(u16) */
    /* 0x06 */ u8 filler_06[0x02];
};
extern const struct Unk0849D62C *const gUnknown_0849D62C[];
/* A proc script: sub_08039674 hands it to Proc_Start, and it is stopped rather
 * than ended -- sub_080396F4 and sub_08039750 both Proc_BreakEach it, so the
 * script blocks somewhere and the pair is break/resume rather than start/end.
 * 0x5C bytes, i.e. 11 ProcCmds. */
extern const struct ProcCmd gUnknown_0849D6D4[];
/* Wave 34 (W34-H). sub_0803941C's sprite-install pair, occupying exactly the
 * two `void *` slots (parameters 1 and 3) of the same
 * sub_08015438(void *, int, void *, void *, int) call that
 * src/decomp/c_0803B264.c's sub_0803B2BC fills with gUnknown_0849E700 /
 * gUnknown_0849E6F8 -- the two functions share all seven callees. Typed `u8 []`
 * from that parameter alone. CONST-NESS IS UNPROVED: the twin pair declares
 * its first element `const` and pays a `(void *)` cast for it, but nothing in
 * sub_0803941C discriminates, and non-const is the weaker model that needs no
 * cast. Both spellings are byte-identical at this call site. */
extern u8 gUnknown_0849D730[];
extern u8 gUnknown_0849D73C[];
/* Wave 38 (W38-F). sub_08039544 is `sub_080152EC(gUnknown_0849D76C, 0)` and
 * then copies a NUL-terminated byte string into the returned slot's +0x26
 * scratch -- the same start-a-proc-then-seed-it shape as gUnknown_0849D41C /
 * gUnknown_0849A108 above. 0x10 bytes, ending where gUnknown_0849D77C begins.
 * `const struct ProcCmd []` follows sub_080152EC's `const void *` first
 * parameter, the same reading its 0849D77C neighbour already carries. */
extern const struct ProcCmd gUnknown_0849D76C[];
extern const struct ProcCmd gUnknown_0849D77C[];
extern const struct ProcCmd gUnknown_0849D7FC[];
/* Wave 34 (W34-H): sub_08039DBC's sprite descriptor, occupying sub_0801BD00's
 * third parameter and reached by name with no indexing. */
extern u8 gUnknown_0849D81C[];
/* Wave 34 (W34-H): sub_08039F80's sprite descriptor, handed to sub_0801BD00's
 * `void *` third parameter by name (twice, CSEd into r8) with no indexing. */
extern u8 gUnknown_0849D8A0[];
extern const struct ProcCmd gUnknown_0849D82C[];
/* Wave 30, W30-A: sub_08039B88's pair, the 0x08039xxx twin of the
 * gUnknown_0849B7D8 / gUnknown_0849B868 wait above -- it returns whether EITHER
 * is running. Typed from Proc_Find's prototype. */
extern const struct ProcCmd gUnknown_0849D84C[];
extern const struct ProcCmd gUnknown_0849D874[];
/* Another gUnknown_03001470 script blob: sub_0803ACB8 hands it to
 * sub_080152EC(script, 0) and sub_0803ACD0 is the matching
 * `sub_08015BD0(script) != -1` liveness predicate. */
extern const u8 gUnknown_0849E600[];
/* A proc script -- sub_0803B9D4 hands it to Proc_Start(script, PROC_TREE_3)
 * after sub_08044BB0. ProcCmd shape is visible in the ROM: the first word is
 * 0x00010006 (opcode 6 = PROC_CMD_START_CHILD_BLOCKING, imm 1) with
 * 0x08616DFC as its dataPtr, so this really is `struct ProcCmd []` and not one
 * of the 03001470 blobs either side of it. Wave 14. */
extern const struct ProcCmd gUnknown_0849EA94[];
/* Three more gUnknown_03001470 script blobs -- see the gUnknown_0849A108
 * comment above. Note 0849E670/0849E6D4/0849E700 sit interleaved with the
 * PROC scripts either side of them (0849E728 below is a real proc script), so
 * address adjacency is not evidence of kind here; the consumer is. */
extern const u8 gUnknown_0849E670[];
extern const u8 gUnknown_0849E6D4[];
extern const u8 gUnknown_0849E700[];
/* ---- wave 32 (W32-C): the 0x0803B block ---- */
/* 0x080910E8. A u16 ramp read at `(gUnknown_03004008 / 3) % 10` by
 * sub_0803B1F0 -- `lsls #1` off the bare symbol, so the symbol IS the array
 * and not an address-constant word. baserom.gba holds 0,1,2,3,3,3,2,1 there,
 * a ten-frame bob whose first eight entries are visible; `[0x0a]` is the
 * modulus, not a measurement of the object. */
extern const u16 gUnknown_080910E8[];
/* Two sub_0801BD00 blobs drawn by sub_0803B1F0, and one sub_08015438 argument
 * pair with gUnknown_0849E700 above. sub_0801BD00 and sub_08015438 both take
 * plain `void *`, so none of these may be const. */
extern u8 gUnknown_0849E6A4[];
extern u8 gUnknown_0849E6B8[];
extern u8 gUnknown_0849E6F8[];
/* A gUnknown_03001470 script blob: sub_0803B48C starts it with sub_080152EC
 * after checking sub_08015BD0 reports no live slot -- the same
 * lookup/start pairing as gUnknown_0849A108 and the rest of that group. */
extern const u8 gUnknown_0849E710[];
/* A proc script: sub_0803BA88 hands it to Proc_Start on tree 3. */
extern const struct ProcCmd gUnknown_0849EB7C[];

/* ---- wave 32 (W32-C): the 0x08067 block ---- */
/* sub_08067638's four screen blobs: three Decompress sources (`u8 *`) into
 * 0x06000000 / 0x0600D800 / 0x0600E000 and one ApplyPaletteExt source
 * (`u16 *`). None may be const -- both parameters are plain pointers. */
extern u8 gUnknown_0817B970[];
extern u8 gUnknown_0817BE90[];
extern u8 gUnknown_0817C138[];
extern u16 gUnknown_0817C3E8[];
/* Wave 48 (W48-C): sub_0806A6F0's two loader sources. gUnknown_0817B150 is the
 * `void *` first argument of `s16 sub_08011E54(void *, void *, u16)` copying
 * 0x800 bytes to VRAM 0x06013940 -- `u8 []` by the same convention the W32-C
 * block above uses, the pointer type itself is not proved by the call.
 * gUnknown_0817B950 is `u16 *` off ApplyPaletteExt's first parameter, and its
 * 0x20-byte length places its end at 0x0817B970, exactly where the already
 * declared gUnknown_0817B970 starts -- so the extent is corroborated, though
 * left unsized here because nothing else reads it. Neither may be const: both
 * parameters are plain pointers. */
extern u8 gUnknown_0817B150[];
extern u16 gUnknown_0817B950[];
/* Wave 33, W33-D. sub_0806A218's two `Decompress(u8 *, void *)` sources -- one
 * into VRAM 0x06000000, the other into whatever the `u16 *` gUnknown_08499578
 * points at. Only the addresses are used, so `u8 []` and non-const, as
 * Decompress's plain pointer parameter wants. */
extern u8 gUnknown_081866F8[];
extern u8 gUnknown_08186D4C[];
/* Wave 33, W33-D. A proc script: sub_0806A218 hands it to Proc_Start with its
 * own proc as the parent, and nothing else in asm/ names it. `const struct
 * ProcCmd []` is what proc.h's Proc_Start takes. */
extern const struct ProcCmd gUnknown_085814E8[];
/* sub_08067C94's OAM blob, PutSprite's `u16 *` fourth argument. */
extern u16 gUnknown_085810A8[];

/* ---- wave 32 (W32-C): the 0x08044 block ---- */
/* sub_0804402C's OAM blob (PutSpriteExt's `u16 *`) and sub_08044D70's blocking
 * proc script. gUnknown_08091388 next to them is NOT a global -- baserom.gba
 * holds 0x08499598 there, so it is agbcc's `-fforce-addr` word for
 * &gUnknown_08499598, which is the extra `ldr` sub_080442E4 does. */
extern u16 gUnknown_084A07DA[];
extern const struct ProcCmd gUnknown_084A08EC[];
/* A gUnknown_03001470 script blob, not a proc script, and the consumer is what
 * says so: sub_0803B83C hands it to sub_0801537C, whose parameter is already
 * `const void *` -- the same reader as the three blobs above. 0x18 bytes. Its
 * only other reference is sub_08031480. `const u8 []` takes the `const void *`
 * with no cast; a plain `void *` would discard the const and -Werror turns that
 * into an error. Wave 24. */
extern const u8 gUnknown_0849B048[];
/* Two proc scripts, both handed straight to Proc_Start on tree 3 and nothing
 * else -- gUnknown_0849E7F8 by sub_0803B83C (right after the blob above) and
 * gUnknown_0849EC8C by sub_0803B858. 0x20 and 0x50 bytes, i.e. four and ten
 * ProcCmd entries at the 8-byte stride. Wave 24. */
extern const struct ProcCmd gUnknown_0849E7F8[];
extern const struct ProcCmd gUnknown_0849EC8C[];
extern const struct ProcCmd gUnknown_0849E728[];
/* A proc script, only ever handed to Proc_Find: sub_0803B628 is the whole of
 * `return Proc_Find(gUnknown_0849E750) != 0;`. */
extern const struct ProcCmd gUnknown_0849E750[];
extern const struct ProcCmd gUnknown_0849E778[];
extern const struct ProcCmd gUnknown_0849E7A0[];
extern const struct ProcCmd gUnknown_0849E7B8[];
/* A proc script: sub_0803B7B4 hands it to Proc_Start on tree 3, but only when
 * gUnknown_030005CA still holds its 0xFFFF "unset" value. */
extern const struct ProcCmd gUnknown_0849E7D8[];
/* 0x084A0090 -- ROM table of per-slot graphics resources, stride 0x44 (proved
 * by the `x*16 + x` then `<<2` index every reader shares). Six of the eight
 * words are named so far, all of them pointers, and the type of each is fixed
 * by the callee its word is handed to (wave 28, W28-B):
 *   +0x00  a pointer to a two-element table of Decompress sources --
 *          sub_08043BC8 does `ldr r0,[p]; ldr r0,[r0]` and sub_08043BF8 does
 *          `ldr r0,[p]; ldr r0,[r0,#4]`, i.e. [0] and [1] of the same array
 *   +0x04  Decompress source (sub_08043B14)
 *   +0x08  a 16-colour palette run: sub_08043AC0 adds a BYTE offset of
 *          slot * 0x20 and hands it to ApplyPaletteExt's `u16 *`
 *   +0x0c  Decompress source (sub_08043E3C)
 *   +0x18  sub_08011E54's `void *` source (sub_08043FA8, count 0x180)
 * The 24-entry indexing (`a % 24` in sub_08043AA0/sub_08043AC0 and
 * `a / 24` / `a % 24` in sub_08043E3C) is the caller's, not the table's -- the
 * table itself is indexed by the remainder alone. */
/* Wave 32 (W32-C). The 0x28 bytes at +0x1c are an ARRAY of 0x14-byte entries,
 * read out of the three functions that index the table with
 * `(unk1d * 17 + (unk1f - 1) * 5) * 4` off gUnknown_08499598's record: 17 words
 * is the 0x44 row stride and 5 words the 0x14 entry stride, and the constant
 * displacements those three add -- 0x1c, 0x1d, 0x20, 0x28 -- are entry
 * offsets 0x00, 0x01, 0x04 and 0x0c once the 0x1c base is taken out.
 * TWO entries is what fits in 0x28 and is a CEILING; the index is `unk1f - 1`,
 * so unk1f is 1-based. */
struct Unk084A0090Entry /* 0x14 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 filler_02[0x02];
    /* 0x04 */ bool8 (*unk04)(void *); /* Wave 45, W45-A: a FUNCTION POINTER,
                              * retyped from `void *`. sub_080445A8 only tests
                              * it against NULL, which is byte-identical either
                              * way and could not settle it; sub_08044610 is the
                              * discriminating use -- it loads this exact word
                              * (entry offset 0x04, i.e. record + 0x20) and
                              * calls it `bl _call_via_r1` with the unit record
                              * in r0, then narrows the result `lsls #0x18;
                              * cmp #0`. One argument (the r1 index counts the
                              * arguments) and a narrow return, hence
                              * bool8 (*)(void *). Producer and consumer now
                              * agree independently. c_080445A8.c re-verified
                              * byte-identical after the retype. */
    /* 0x08 */ void (*unk08)(void *); /* Wave 45, W45-A: named out of the old
                              * filler_08, which nothing read. sub_08044610
                              * calls it `bl _call_via_r1` off record + 0x24 on
                              * both arms of its terrain test, again with the
                              * unit record in r0, and DISCARDS the result --
                              * so one argument and void. The companion of
                              * unk04 above: unk04 asks, unk08 acts. */
    /* 0x0c */ void (*unk0c)(void *); /* `bl _call_via_r1` with a literal 0 */
    /* 0x10 */ s16 unk10[2]; /* Wave 34 (W34-H): a TWO-frame animation pair,
                              * subscripted by the gUnknown_030043F8 toggle in
                              * sub_08039ACC and read `ldrsh`, which is what
                              * makes it signed and the extent exactly 2 --
                              * that toggle only ever holds 0 or 1. */
};

struct Unk084A0090 /* 0x44 */
{
    /* 0x00 */ u8 **unk00;
    /* 0x04 */ u8 *unk04;
    /* 0x08 */ u16 *unk08;
    /* 0x0c */ u8 *unk0c[3]; /* Wave 35 (W35-A): an ARRAY of Decompress
                              * sources, not a single pointer. sub_08043E3C
                              * loads `[rec + 0xc + (a / 24) * 4]` in one go --
                              * the +0xc and the scaled index are added to the
                              * record base before the `ldr`, so the words live
                              * IN the record. Extent 3 fills 0x0c..0x18
                              * exactly; the outer index is `a / 24` where the
                              * record index is `a % 24`, so `a` is a packed
                              * (frame, slot) selector. Nothing else in
                              * src/decomp/ named unk0c, so nothing had to
                              * change with it. */
    /* 0x18 */ void *unk18;
    /* 0x1c */ struct Unk084A0090Entry unk1c[2];
};
extern const struct Unk084A0090 gUnknown_084A0090[];
/* A single 16-colour palette in ROM. sub_08043B44 hands it to
 * ApplyPaletteExt's `u16 *` first parameter with no arithmetic, so `u16 []`;
 * NOT const, on the same grounds as gUnknown_0849957C -- ApplyPaletteExt's
 * parameter is unqualified and every other palette reaching it is declared the
 * same way (wave 28, W28-B). */
extern u16 gUnknown_080F6164[];
/* A sprite object-list blob in ROM, handed straight to PutSpriteExt's `u16 *`
 * fourth parameter by sub_08043B60 with no arithmetic -- the same shape as
 * gUnknown_0849B6D6, and declared the same way (wave 28, W28-B). Its
 * neighbours gUnknown_084A0790 and gUnknown_084A07DA, used identically by the
 * still-unmatched sub_08043FD8 / sub_0804402C, are the rest of the run. */
extern u16 gUnknown_084A0730[];
extern const struct ProcCmd gUnknown_0849EAAC[];
extern const struct ProcCmd gUnknown_0849EBBC[];
extern const struct ProcCmd gUnknown_0849EC1C[];
/* Wave 37, W37-Q2: a ROM byte table read `ldrb` with a variable index by
 * sub_0803B930 (`subs r1, r2, #1; add r1, ip; ldrb r1, [r1]`, i.e.
 * gUnknown_0849EA78[i - 1] for i = 1..gUnknown_0849ECDC->unk00). The bytes in
 * baserom.gba at 0x0849EA78 are 01 03 02 04 06 00 01 00, i.e. a small
 * per-slot seed table whose values land in gUnknown_03003FC0.unk3d[1..4].
 * The extent is not proved; the largest index reached is unk00 - 1, and unk00
 * is armed with 2/3/4 by sub_0803BDF8/sub_0803BE10/sub_0803BE28, so at least
 * four elements are live. Not `const` here -- nothing in the tree pins it. */
extern u8 gUnknown_0849EA78[];
extern struct Unk0849ECDC *gUnknown_0849ECDC;
extern const struct ProcCmd gUnknown_0849ECE0[];
/* The fallback name string sub_0803CCEC hands out in place of
 * &gUnknown_020280C0[i].unk02 when that slot's unk13 is 0xff (empty). Same
 * shape as the array member it substitutes for, hence u8 [] and not const --
 * one function returns either of the two through one return type. */
extern u8 gUnknown_0849F320[];
/* A proc script: sub_0803D73C hands it to Proc_Start on tree 3 (writing +0x64
 * u16 and +0x4c word of the new proc) and sub_0803D770 is the matching
 * `return Proc_Find(script) != 0;` existence predicate.
 * CORRECTION (wave 29, W29-B): this note used to credit the Proc_Start to
 * "sub_0803D75C". There is no such function -- 0x0803D75C is a label inside
 * sub_0803D73C, whose extent is 0x0803D73C..0x0803D770. */
extern const struct ProcCmd gUnknown_0849F330[];
/* Wave 29, W29-B. The script sub_0803D88C sends its own proc to with
 * Proc_GotoScript when gUnknown_03003FC0.unk01 is 1 and sub_0803861C reports
 * 0. */
extern const struct ProcCmd gUnknown_0849F388[];
/* Wave 29, W29-B. NOT a ProcCmd script: sub_0803D8C0 and sub_0803D92C hand it
 * to sub_080193B0, whose parameter is `const u8 *`, and then poke +0x10 of the
 * struct Unk0200C528 it returns. Same spelling as the other sub_080193B0 script
 * blobs (see the 0x08487AC4 / 0x08487B64 note above). */
extern const u8 gUnknown_0849F3A8[];
extern const struct ProcCmd gUnknown_0849F5D0[];
extern const struct ProcCmd gUnknown_0849F888[];
extern const struct ProcCmd gUnknown_0849F918[];
extern const struct ProcCmd gUnknown_0849FB44[];
/* Wave 31, W31-B. Three proc scripts, each only ever handed to
 * Proc_StartBlocking and each with the starter's own last argument as parent:
 * gUnknown_0849FB8C by sub_0804046C, gUnknown_0849FBBC by sub_08040554 and
 * gUnknown_0849FBEC by sub_0804074C. None of the three starters reads the
 * script itself -- only the symbol address is used. */
extern const struct ProcCmd gUnknown_0849FB8C[];
extern const struct ProcCmd gUnknown_0849FBBC[];
extern const struct ProcCmd gUnknown_0849FBEC[];
/* Wave 34, W34-F. Three more proc scripts from the same 0x0849Fxxx run.
 * gUnknown_0849FD14 is started by sub_080411FC, which then copies its own
 * +0x3c/+0x3e halfword pair into the returned proc -- and it is the script
 * whose PROC_ONEND callback is the already-promoted sub_08040AFC (the ROM holds
 * that command at 0x0849FD14, which is what c_08040AFC.c reads to prove its own
 * parameter is a Proc). gUnknown_0849FE54 is blocking-started beside it, and
 * gUnknown_0849FE78 is the unit-move proc both sub_08041820 and sub_0804189C
 * start under PROC_TREE_3. */
extern const struct ProcCmd gUnknown_0849FD14[];
extern const struct ProcCmd gUnknown_0849FE54[];
extern const struct ProcCmd gUnknown_0849FE78[];
/* A proc script: sub_08042C10 hands it to Proc_Start on tree 3. */
extern const struct ProcCmd gUnknown_0849FC0C[];
/* Wave 31, W31-B. NOT a ProcCmd script: sub_0804087C hands it to
 * sub_080193B0, whose parameter is `const u8 *`, and discards the
 * struct Unk0200C528 that comes back. Same spelling as the other
 * sub_080193B0 script blobs (gUnknown_0849F3A8 above). */
extern const u8 gUnknown_0849FC64[];
/* sub_08042C24 hands it to Proc_Start or Proc_StartBlocking depending on whether
 * its parent argument is a real ProcPtr or a small tree number. The ROM words
 * there open {4, 0x08034F8D, 2, 0x08034F7D, ...}, the same two-entry header
 * gUnknown_0849FC0C above opens with, so the same type. Wave 26, W26-C. */
extern const struct ProcCmd gUnknown_0849FCA4[];
/* A proc script: sub_08041180 hands it to Proc_Find and writes +0x40 (u16) of
 * the proc it returns. */
extern const struct ProcCmd gUnknown_0849FD44[];
/* Wave 29 (C). A proc script: sub_080411A0 hands it to Proc_StartBlocking with
 * its own parameter as parent and does not touch the proc that comes back. */
extern const struct ProcCmd gUnknown_0849FE0C[];
/* A proc script: sub_080411D0 hands it to Proc_StartBlocking under its own
 * parameter as parent, then widens two of that parameter's bytes (+0x48/+0x49)
 * into halfwords at +0x4c/+0x4e of the new proc. */
extern const struct ProcCmd gUnknown_0849FE34[];
/* Two proc scripts, each only ever handed to Proc_Find: sub_0802C550 is
 * `Proc_Find(FEF8) || Proc_Find(FFB0)`, the two-script variant of the bare
 * existence predicate gUnknown_0849D3BC and gUnknown_0849E750 carry. `const
 * struct ProcCmd []` is what proc.h's Proc_Find takes. */
extern const struct ProcCmd gUnknown_0849FEF8[];
extern const struct ProcCmd gUnknown_0849FFB0[];
/* Named proc scripts reached from this file. Declared non-const to agree with
 * the existing declaration in src/title-screen.c. */
extern struct ProcCmd ProcScr_MainMenu[];
extern const struct Unk084A06F0 gUnknown_084A06F0[];
extern const struct ProcCmd gUnknown_084A07E8[];
/* A proc script: sub_080443D8 hands it to Proc_StartBlocking. */
extern const struct ProcCmd gUnknown_084A0818[];
/* A proc script started blocking by sub_08044A88 and sub_08044AA0, which
 * then write 0x50 and 0x28 to a halfword at +0x64 of the new proc. */
extern const struct ProcCmd gUnknown_084A0858[];
/* 0x1c bytes at 0x084A08D0: a 24-entry menu-layout table terminated by 0xff
 * (01 02 04 00 0f 80 03 05 10 81 06 07 12 83 08 09 11 82 0b 0c 0d 0e 0a 84 ff).
 * sub_08044BB0 is the only reference. It walks the table with a pointer,
 * reading each entry BOTH `ldrb` and `ldrsb`: entries with the high bit set
 * (0x80..0x84) are group headers whose low 7 bits are a group index 0..4,
 * matching the five bytes of gUnknown_03005948 that the same function clears
 * on entry; the rest are item ids handed to sub_0803CAD4.
 * NOT `const`, and the evidence is specific: the entry is re-`ldrb`-ed after
 * the `bl sub_0803CAD4` rather than kept in a register across it. A `const`
 * array sets RTX_UNCHANGING_P, so cse would have carried the load over the
 * call instead of re-emitting it. */
extern u8 gUnknown_084A08D0[];
/* A proc script: sub_08045F40 hands it to Proc_Start on tree 3. */
extern const struct ProcCmd gUnknown_084B7628[];
/* A proc script started blocking by sub_08045448 and sub_08045460, which then
 * write 1 and 2 respectively to a word at +0x54 of the new proc. */
extern const struct ProcCmd gUnknown_084A09CC[];
/* A proc script started on tree 3 by sub_080452C0, which then stashes two
 * words at +0x3c/+0x40 and a byte at +0x2c of the new proc. */
extern const struct ProcCmd gUnknown_084A096C[];
/* The script sub_08044D70 forwards to Proc_Start; all five of its wrappers at
 * 0x08044C44-0x08044D34 pass this one and differ only in the blob, the palette
 * and four small integers. Confirmed a script by its ROM bytes: opcode 0x0e
 * (SLEEP) then CALL/REPEAT pairs pointing at 0x08045359/0x080453B1/0x080453CD/
 * 0x0804542D. */
extern const struct ProcCmd gUnknown_084A0994[];
/* A proc script: sub_08045790 hands it to Proc_Start on tree 3. */
extern const struct ProcCmd gUnknown_084A0A3C[];
extern struct Unk084C1430 *gUnknown_084C1430;
/* ROM blobs handed to sub_080152EC, the same slot as gUnknown_0849A0F0.
 * 084C3D6C is the only one started on tree 1 rather than 0. */
extern const u8 gUnknown_084C1824[];
/* Another gUnknown_03001470 script blob, and the neatest instance of the
 * pairing: sub_080470F8 hands it to sub_080152EC and sub_080470E8 hands the
 * same address to sub_0801537C. See the gUnknown_0849A108 comment. */
extern const u8 gUnknown_084C2198[];
/* Wave 35, W35-L. The blob sub_080470F8 starts with `sub_080152EC(g, 0)`
 * immediately before it does the same to gUnknown_084C2198 above, and typed
 * the same way for the same reason (sub_080152EC takes `const void *` and
 * nothing dereferences it). Its first ROM word is 0x08034F7D, an odd address
 * = &sub_08034F7C with the THUMB bit set, so it really is a script. */
extern const u8 gUnknown_084C2140[];
extern const u8 gUnknown_084C21C8[];
/* A proc script: sub_08049BD8 hands it to Proc_Start on tree 3. */
extern const struct ProcCmd gUnknown_084C3138[];
extern struct Unk084C3240 *gUnknown_084C3240;
extern const u8 gUnknown_084C325C[];
/* A proc script started blocking by sub_08049F08, which forwards its own
 * second argument as the parent. */
extern const struct ProcCmd gUnknown_084C327C[];
extern const u8 gUnknown_084C3824[];
/* Wave 48, W48-I. Two OBJ sprite blobs sub_08049F24 hands to sub_0801BD00 as
 * its `void *` third argument -- gUnknown_084C37E4 once per iteration of a
 * four-pass loop (which is what earns it the -fforce-addr .rodata address word
 * gen_lds calls gUnknown_0812A174), gUnknown_084C3800 once after it. Non-const
 * and `u8 []` for the reason gUnknown_0809181C records: the callee's parameter
 * is a plain `void *`, so a `const` spelling would need a cast at every call
 * and nothing here constrains the element type further. */
extern u8 gUnknown_084C37E4[];
extern u8 gUnknown_084C3800[];
/* Wave 40, W40-C. The per-character pixel-width table of the text renderer.
 * Both users index it by a raw character byte and add the result to a running
 * width: sub_08014D38 (which sub_08014D20 divides by 8 to get tiles) and
 * sub_08014CEC. `ldrb` off a `ldrb`-loaded index in both, so `const u8 []`;
 * unsized because neither caller bounds the character, so an extent would be
 * a guess. */
extern const u8 gUnknown_084C36E4[];
extern const u8 gUnknown_084C3D6C[];
/* Wave 29 (C). The second blob sub_0804B088 hands to sub_0801537C, right after
 * gUnknown_084C3D6C above and typed the same way -- sub_0801537C takes
 * `const void *`. */
/* Wave 30, W30-E. Sits between gUnknown_084C3D6C and gUnknown_084C3D8C and is
 * typed the same way: sub_0804AE50 hands its address to sub_080152C0, so it is
 * a proc script reached only as an address. The declared
 * `void sub_080152C0(s32, u8)` still forces the `(s32)` cast at the call. */
extern const u8 gUnknown_084C3D7C[];
extern const u8 gUnknown_084C3D8C[];
/* Another gUnknown_03001470 script blob: sub_0804B0CC/sub_0804B10C hand it to
 * sub_080152EC(script, 0) and sub_0804B160 is the matching
 * `sub_08015BD0(script) != -1` liveness predicate. */
extern const u8 gUnknown_084C3D9C[];
/* A gUnknown_0200C528 list script, same shape as gUnknown_0849A8F0: its sole
 * reference in the ROM is sub_0804AE10 handing its ADDRESS to
 * sub_080193B0(const u8 *), whose result that wrapper discards. `const u8 []`
 * is the weakest model that gives the clean pool word. */
extern const u8 gUnknown_084C38BC[];
/* Two ROM pointer pairs indexed by gUnknown_0300453C (0 or 1), each holding
 * the address of a u16 that sub_0804D290/sub_0804DCA8 subtract from an
 * entry's x and y before calling sub_080155C0 -- i.e. the camera/scroll
 * origin for that side. Two entries each and no more: the words are
 * 0x030030A0, 0x0300200C / 0x03001400, 0x03002000, and the next word after
 * 084C3F78's pair is 0x00400010, which is not an address.
 *   Both pointers are read into `u16 *` LOCALS before the call in the source:
 * the ROM materialises both table entries -- one of them into `ip` -- before
 * it computes even the first argument, and only a pair of locals forces that
 * order. Worth 8 bytes and 55 points on sub_0804D290 in combination with the
 * zero-trip loop; see docs/agbcc-codegen.md. */
extern u16 *gUnknown_084C3F70[];
extern u16 *gUnknown_084C3F78[];
/* Two more 0x400-entry u16 tilemap buffers. sub_08054C04 CpuFastSets 0x200
 * words out of gUnknown_08551A04 to 0x06002800, which fixes the extent; the
 * writers sub_0805701C/sub_08057110 store TILEREF-shaped halfwords. */
extern u16 *gUnknown_08551A00;
extern u16 *gUnknown_08551A04;
/* A ROM table of three-halfword rows indexed by gUnknown_0300453C -- the
 * address arithmetic in sub_0804D928/sub_0804E3B4 is `((i*2)+i)*2`, i.e. a
 * stride of exactly 6, and column 0 is read with `ldrh` into
 * OamData.paletteNum. Six is not reachable with a struct: agbcc's
 * STRUCTURE_SIZE_BOUNDARY is 32 bits, so `struct { u16 a, b, c; }` rounds up
 * to 8 and emits `lsl #3`. `u16 t[][3]` and `u16 t[]` indexed `[i*3]` are
 * byte-identical, so the row/flat split is not settled; the row form is used
 * because the two neighbouring columns are clearly part of the same record.
 * Only column 0 has a reader so far. */
extern u16 gUnknown_08551D0C[][3];
/* Wave 32, W32-A. Indexed by gUnknown_02029A10[side].entries[slot].unk00 -- a
 * u8 read `ldrb` -- and the u16 it returns then selects one of the two words in
 * gUnknown_02029BA8[side].unk18[], i.e. it maps a unit's state byte to a tile
 * source. `ldrh` off a `lsls #1` element in both sub_0804EA54 and sub_0804EAEC.
 * Nothing bounds it. */
extern u16 gUnknown_08551D1C[];
/* Two ROM u16 tables indexed by the same 10-bit `(dst->unk04 - oam.tileNum) &
 * 0x3FF` tile delta that sub_0804E8F0/sub_0804FE10 compute. 08552A40 is the
 * guard/selector -- 0xFFFF means "no entry", and the two live values seen so
 * far are 0 and 0x28, each selecting one of the two branches -- and 08552700
 * is the payload handed to sub_0804EA54/sub_0804EAEC as a u16. Extents
 * unproved: the index is masked to 0x3FF but nothing bounds the tables. */
extern u16 gUnknown_08552700[];
extern u16 gUnknown_08552A40[];
/* Six-halfword ROM rows, the row indexed by gUnknown_03001470[i].unk30 (the
 * side) and the column by sub_0804BDD8's u16 result -- `(side*2 + side)*4` for
 * the row and `col*2` for the column, in sub_0804E7A8 and sub_0804FCA4. The
 * value is ADDED to gUnknown_02029A10[side].entries[slot].x, so it is a signed
 * pixel step read through a `ldrh`; nothing sign-extends it, so unsigned is not
 * proved, only that the addition truncates back to 16 bits at the `strh`. */
extern u16 gUnknown_08553B28[][6];
/* Six-halfword ROM rows indexed [side][t], the row scaled `(s*2+s)*4` == 12
 * bytes and the column by a SIGNED 16-bit sum (`lsls #0x10; asrs #0xf`, i.e.
 * `(s16)t * 2` -- one shift pair doing the truncation and the element scale at
 * once). sub_0804C828 / sub_0804CD84 read one element and assign it straight
 * into struct OamData's 4-bit paletteNum, so it is a palette index table
 * (wave 20, W20-C). Read `ldrh`, nothing sign-extends the VALUE, so the table
 * is unsigned; only the index is signed. */
extern u16 gUnknown_08553B40[][6];
/* A ROM table of 28-byte rows, indexed by gUnknown_02029808[i].unk30[j] (wave
 * 17). The stride is `((n*8)-n)*4` in sub_08051DE0/sub_080524C0/sub_0805297C
 * and again in sub_08055058, i.e. exactly 28.
 *   unk00  a byte length: sub_08055058 gates the row on it and passes
 *          `unk00 >> 2` to CpuFastSet as the word count.
 *   unk02  an x offset per gUnknown_0300453C side, and unk06 the matching y --
 *          both are added to gUnknown_02029A10[..].entries[..].x / .y.
 *   unk08  the CpuFastSet SOURCE in sub_08055058 (`ldr` at +8), so a pointer.
 * A STRUCT and not a `u16 [][14]`: unk06 and unk08 are reached with the offset
 * in the load's immediate off `base + n*28`, where the two-dimensional
 * spelling folds the constant into the symbol's own address instead (probed
 * both ways -- two bytes each). Everything past unk08 is unread. */
struct Unk08552D80 /* 0x1c */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02[2];
    /* 0x06 */ u16 unk06;
    /* 0x08 */ const void *unk08;
    /* 0x0c */ u8 filler_0c[0x10];
};
extern struct Unk08552D80 gUnknown_08552D80[];
/* NOT globals in the source -- two slots of agbcc's own `-fforce-addr`
 * `.rodata` address-constant pool, both holding &gUnknown_03001470, one per
 * function. The surrounding words are 0x03001FBC, 0x0300453C, 0x0300451C,
 * 0x03004580, 0x02029A10 ... in function address order, which is a `.LC` block
 * by the test in docs/agbcc-codegen.md and not a table of distinct globals.
 * Declared anyway because that is the only spelling that reproduces the ROM's
 * extra `ldr` with a resolvable relocation -- the same workaround
 * gUnknown_08090CD8 already uses. The consequence worth recording: the two
 * functions that name them have IDENTICAL source, and the "twin discriminator"
 * between them is a compiler artefact, not a difference in the C.
 *   The WRAPPER STRUCT is load-bearing and is new in wave 18. docs/agbcc-
 * codegen.md had these two parked on the grounds that the ROM's HOISTED member
 * offset (`adds rB,#0x30; adds rB,rI,rB`) belongs to an array-global subscript
 * and that "a pointer global never hoists, structurally". Both halves are true
 * and the conclusion is not: a pointer to a struct whose MEMBER is the array
 * hoists exactly like an array global, because `p->arr[i].m` is the
 * `gStruct.unk20[i]` row of the fold table -- an aggregate member reached
 * through a pointer, not a plain subscript on one. `struct Unk03001470 *const`
 * with `g[i].m` folds the offset into the load's displacement and cannot reach
 * the ROM; `struct Unk08136090 *const` with `g->unk00[i].m` reproduces it
 * instruction for instruction. Probed side by side against sub_0804E8F0 --
 * six spellings, and this is the only one. */
struct Unk08136090 /* not a real object: see above */
{
    /* 0x00 */ struct Unk03001470 unk00[30];
};
extern struct Unk08136090 *const gUnknown_08136090;
extern struct Unk08136090 *const gUnknown_081360C8;
/* A ROM u16 table of small values (0..3), indexed by
 * `gUnknown_0300453C ^ gUnknown_0300450C` and masked with 3 into
 * OamData.priority -- so it is an OBJ-priority lookup. The first eight entries
 * are 3,2,2,3,3,2,0,0; what follows is signed pixel offsets, so the extent is
 * at most 8 and the array is probably longer than its priority use. */
extern u16 gUnknown_085523A4[];
/* The SAME priority table two rows earlier, and reached through its own linker
 * symbol: sub_0804DCA8 holds a pool word for 0x0855239C and indexes it
 * `(gUnknown_0300453C * 2 + gUnknown_0300450C) * 2`, i.e. a [2]-wide row, where
 * its twin sub_0804D290 uses gUnknown_085523A4 (== 0855239C + 8) with an XOR
 * index. Same eight halfwords 3,2,2,3,3,2,2,3 read two different ways.
 * FLAT, not `[][2]`: the ROM computes `(a * 2 + b) * 2` as one chain, and the
 * two-dimensional spelling scales each index separately (`b*2` then `a*4`),
 * which is four bytes of different code at the same size.
 *   CONFIRMED in wave 17: `gUnknown_0855239C[gUnknown_0300453C * 2 +
 * gUnknown_0300450C]` on the flat declaration is byte-exact in both
 * sub_0804DCA8 and sub_0804FA2C -- neither has any diff in that region. The
 * matching XOR spelling on gUnknown_085523A4 is confirmed the same way by the
 * three F088 members matched in wave 17 (sub_0804D738/sub_0804D818/
 * sub_0804E134), which are byte-for-byte. */
extern u16 gUnknown_0855239C[];
/* A ROM dispatch table of THUMB function pointers, indexed by
 * gUnknown_085D6A48[gUnknown_03004580[i][1]][2]. sub_08050B70 is its only
 * reader and calls the entry through `_call_via_r2` with two arguments in
 * r0/r1 -- and per docs/agbcc-codegen.md the register index on the trampoline
 * IS the arity, so the two-parameter signature is a readout rather than a
 * guess. Both parameters are u16: every reached target (sub_08050D44,
 * sub_08050E08, sub_08051BEC, sub_08052718, sub_08051F4C, sub_08052E04,
 * sub_08052BBC, sub_080523E8) opens `lsls #0x10; lsrs #0x10` on r0 and most on
 * r1 too, and the call site passes two u16 globals with no narrowing.
 * CAVEAT, and it is why the extent is not declared: slots 2 and 3 hold
 * 0x08553648 and 0x08553674, which are EVEN and point back into this same
 * 0x0855xxxx data region, so they are not THUMB code. Either those slots are
 * never reached or the array is heterogeneous; sub_08050B70 cannot tell, and
 * the function-pointer type is right for every slot it does reach. */
extern void (*const gUnknown_085535B0[])(u16, u16);
/* A ROM table of three-word rows, three rows per outer index: sub_0804D290/
 * sub_0804DCA8 compute `(w*9 + q*3 + 2) * 4` off the symbol, which is exactly
 * `[w][q][2]` on a `[][3][3]`. Column 2 holds an odd THUMB function pointer
 * (0x08050365 -> sub_08050364, 0x0804E8F1 -> sub_0804E8F0) that is handed
 * straight to sub_08015928(s16, u32) as a continuation; column 1 is another
 * one and column 0 is a data address, so the row is not uniformly typed and
 * u32 is the honest element type. */
extern u32 gUnknown_08552FB8[][3][3];
/* ROM u16 pair indexed by a 0/1 flag and its complement -- sub_0805741C uses
 * gUnknown_085538AE[a ^ 1] for the BG3 control shadow and [a] for
 * gUnknown_0300251C. The address is halfword- but not word-aligned, so this is
 * a plain u16 array and not an aggregate. */
extern const u16 gUnknown_085538AE[];
/* Two adjacent ROM u16 pairs, each indexed by gUnknown_0300453C: sub_08051BEC
 * uses 08553B10 and sub_08051F4C uses 08553B14, and each binds the element to
 * a local and hands it to sub_080157A4, which takes s16. The value is loaded
 * `ldrh` and sign-extended `lsls #0x10; asrs #0x10` at the call, so the TABLES
 * are unsigned and the conversion belongs to the parameter -- an s16 table
 * read into an s16 local would have been a bare `ldrsh` with no conversion,
 * which is the negative test. The two symbols are 4 bytes apart and are
 * probably one table of pairs; kept separate because that is what the two
 * pool words say. */
extern u16 gUnknown_08553B10[];
/* Wave 33, W33-E. sub_080523E8 passes the ADDRESS of this object -- `ldr
 * r0,=gUnknown_085536EC` with no dereference -- as sub_08015410's `void *` first
 * parameter, exactly where its 0x08051/0x08052 siblings pass an element of
 * gUnknown_085535B8. So it is the pointed-to blob rather than a table of
 * pointers, and `u8 []` is the weakest spelling that converts to `void *`. */
extern u8 gUnknown_085536EC[];
extern u16 gUnknown_08553B14[];
extern u32 *gUnknown_08555450[];
extern struct Unk08580934 *gUnknown_08580934;
/* Wave 32 (W32-B): two ROM halfword tables of per-frame Y offsets, indexed by
 * the object's own unk26 countdown (`ldrsh`, then `lsls #1`). sub_08064C34
 * stores gUnknown_085809D8[unk26] into unk2a directly; sub_08064D74 adds unk38
 * to gUnknown_085809F0[unk26] first. The extents are unknown -- unk26 is only
 * bounded by the countdown's start value, which neither function sets. */
extern const u16 gUnknown_085809D8[];
extern const u16 gUnknown_085809F0[];
/* THREE BASES INTO ONE RUN OF ROM HALFWORDS, and they are declared as three
 * arrays because that is what reproduces the ROM. The F092 tick handlers
 * (sub_080646D4, sub_08064FC8, sub_08065118) are byte-identical apart from
 * which of these three symbols they load, and the addresses are contiguous:
 * 0x08580948 + 160*2 == 0x08580A88, and 0x08580A88 + 27*2 == 0x08580ABE. Each
 * is read `t[obj->unk26]` as the vertical offset of a falling/arcing sprite,
 * and the values are plainly SIGNED -- 08580948 runs 0, -4, -6, -7, -7, -5,
 * -1, 3, 10, 18 ... and 08580ABE has -3, -13, -24 -- so `s16` and not the
 * `u16` the `ldrh` suggests. The load is `ldrh` because the sum is stored
 * straight back with `strh` and the sign bits cannot survive the truncation;
 * `s16`, `const s16` and `u16` are byte-identical here, probed all three.
 *
 * NOTE, against the wave-17 brief: 0x08580ABE not being 4-byte aligned does
 * NOT prove these cannot be independent objects. A halfword array needs only
 * 2-byte alignment, so an `s16 []` may legally start there. What the three
 * declarations do buy is the relocation -- each function's pool word names its
 * own symbol -- and that holds whether the source had one table or three. */
extern s16 gUnknown_08580948[];
extern s16 gUnknown_08580A88[];
extern s16 gUnknown_08580ABE[];
/* A gUnknown_03001470 script blob, not a proc script: sub_080670BC hands it to
 * sub_080152EC(script, 2) and sub_080670D8 is the matching
 * `sub_08015BD0(script) != -1` liveness predicate. Same `const u8 []` reasoning
 * as gUnknown_0849A3C0. */
extern const u8 gUnknown_08580DD8[];
/* Four more ROM blobs of the same 8-byte {THUMB fn ptr, u32 flags} record
 * shape, but a DIFFERENT consumer from the 03001470 list: sub_08063A30 stores
 * the pointer at +0x04 of the object in r0 (then zeroes +0x08 and +0x10), and
 * the four wrappers sub_08065F68/sub_08065F78/sub_08066200/sub_08066210 are
 * nothing but that store. So these are NOT proved to be 03001470 blobs --
 * there is no sub_080152EC/sub_0801537C install/remove pair on any of them,
 * which is the only thing that ever proves that, and the wave-13 note on
 * gUnknown_08580C7C is the reason not to assume it from the address. `const
 * u8 []` is the weakest model that gives the clean pool word. Wave 14. */
/* Wave 30, W30-E. sub_0806502C is `sub_080152EC(gUnknown_08580A38, 3)` --
 * a proc script handed to sub_080152EC's `const void *` first parameter and
 * reached only as an address, so `const u8 []` like the rest of that set. */
extern const u8 gUnknown_08580A38[];
extern const u8 gUnknown_08580A68[];
extern const u8 gUnknown_08580B18[];
extern const u8 gUnknown_08580C00[];
extern const u8 gUnknown_08580C20[];
/* Wave 36 (W36-G). Two ROM blobs handed to sub_0801BD00's `void *` third
 * parameter by sub_080655B0, picked on gUnknown_08580934->unk30 being zero or
 * not. 14 bytes apart and both open with the word 0x40000002, so they are the
 * same shape; nothing here indexes or dereferences them, so the element type is
 * unconstrained and `u8 []` is the weakest that converts to `void *` without a
 * const cast -- the same reasoning as gUnknown_08499B6C above. */
/* Wave 36 (W36-G). A halfword colour table. sub_08066EBC reads
 * `gUnknown_0817AF18[0x20 + ((gUnknown_03004008 >> 1) & 0xf)]` -- and the ROM at
 * index 0x20 holds exactly sixteen RGB15 words ramping 0x0000, 0x0C61, 0x18C2,
 * 0x2D64, 0x3DE7, 0x4E69, 0x672C, 0x7FEF and back down again, so the 16-entry
 * span the `& 0xf` selects is a symmetric fade ramp and the element type is u16.
 * NOT const: sub_08066EBC re-reads the same element after storing into gPal
 * rather than reusing the loaded value, which is agbcc refusing to assume the
 * two u16 objects cannot alias. */
extern u16 gUnknown_0817AF18[];
/* Wave 36 (W36-G). Three halfword graphics-id tables that sub_08066F20 selects
 * between and indexes, each read `lsls #1; adds; ldrh`. The values settle the
 * element type on their own: they are the contiguous run 0x09D1..0x09DA plus the
 * 0x09DB the same function's fourth arm uses as a bare literal, and the value
 * loaded is compared against and then stored into gUnknown_08580934->unk2e,
 * already typed u16 as "the graphics id currently loaded".
 *   Extents come from the addresses: 0x08580D6C holds SIX entries (0x09D1..
 * 0x09D6, indexed by the s8 unk33, whose 7th value 6 is exactly the case that
 * diverts to the 0x08580D88 table instead), and 0x08580D88 holds FOUR
 * (0x09D7..0x09DA, indexed by an unk54[] entry's u8 unk48) before unrelated
 * data. 0x08580D78 alternates 0x09DC/0x09DD over its eight slots. */
/* Wave 36 (W36-G). Three more proc scripts in the 0x0858xxxx table, on the same
 * evidence as gUnknown_08580DD8 below: sub_0806675C hands each one straight to
 * sub_0806377C, whose parameter is `const void *`, with no arithmetic in front.
 * gUnknown_08580AF0 additionally goes to sub_08063A00 as the script to match
 * against a Unk03001470 slot's unk00, which is the same use. */
extern const u8 gUnknown_08580A08[];
extern const u8 gUnknown_08580D90[];
extern const u8 gUnknown_08580AF0[];
extern const u8 gUnknown_08580B90[];
extern const u8 gUnknown_08580BC8[];
/* Wave 49 (W49-L). Two more proc-script blobs in the same table, on the same
 * evidence as the five above: sub_08066A20 hands gUnknown_08580D3C and
 * sub_08066874 hands gUnknown_08580D54 straight to sub_080152EC, whose first
 * parameter is `const void *`, with no arithmetic in front. data.s gives each
 * an extent of 0x18. */
extern const u8 gUnknown_08580D3C[];
extern const u8 gUnknown_08580D54[];
extern const u16 gUnknown_08580D6C[];
extern const u16 gUnknown_08580D78[];
extern const u16 gUnknown_08580D88[];
extern u8 gUnknown_08580C40[];
extern u8 gUnknown_08580C4E[];
/* Wave 36 (W36-G). A halfword ease table indexed by a s16 countdown: the ROM
 * holds 0, 2, 6, 0xc, 0x14, 0x1e, 0x2a ... (second differences constant at 2),
 * i.e. a quadratic ramp. SIGNED -- sub_080655B0 reads it with
 * `lsls #1; adds; movs rN,#0; ldrsh`, and the value feeds sub_0801BD00's s32
 * first parameter and a `0xc0 - x` subtraction. */
extern s16 gUnknown_08580C5C[];
/* Proc scripts in the 0x0858xxxx table, each proved by being the sole argument
 * of a Proc_Start/Proc_Find call. `const` because that is what proc.h's
 * prototypes take; they are ROM data and nothing writes them.
 *   08580E94  Proc_Start, sub_080673B0 (fills +0x2c/+0x38/+0x3c)
 *   08580EAC  Proc_Start, sub_080673D0 (fills +0x2c/+0x38/+0x3c)
 *   08580EC4  Proc_Start, sub_080673F0 (fills +0x2c/+0x38/+0x3c)
 *             -- 08580E94/08580EAC/08580EC4 are three consecutive 0x18-byte
 *             scripts with three identical starters, one per variant
 *   08580FE4  Proc_Find,  sub_0806780C (+0x5c)
 *   08580FF4  Proc_Find,  sub_080678BC (+0x60) and sub_080678D4 (+0x3c)
 *   08581014  Proc_Find,  sub_08067A24 (+0x50, u8)
 *   08581068  Proc_Find,  sub_08067C7C (+0x38)
 *   085810E4  Proc_Start, sub_08067DD4 (no fields written)
 *   08581420  Proc_Find,  sub_0806978C/sub_080697A4 (+0x36, u8 = 0/1)
 *   08582AF4  Proc_Start, sub_0806E210 (+0x58) and Proc_Find, sub_0806E228,
 *             both writing +0x58 = arg + 1
 *
 * 08580FE4 is reached twice with different arities: sub_0806780C finds it and
 * writes +0x5c = 1, while sub_080677BC starts it under its own fourth argument
 * and writes +0x2c/+0x30/+0x34/+0x38/+0x58 as well as the same +0x5c = 1.
 */
extern const struct ProcCmd gUnknown_08580E94[];
extern const struct ProcCmd gUnknown_08580EAC[];
extern const struct ProcCmd gUnknown_08580EC4[];
/* ROM table of pointers to SIGNED byte arrays, indexed by a plain int
 * (`lsls #2` on the argument, no `& 0xff`). sub_0806775C stashes one whole
 * element in a proc at +0x2c and its element [0] at +0x30 -- and that read is
 * `movs r1, #0; ldrsb r1, [r2, r1]`, which per docs/agbcc-codegen.md is an s8
 * OBJECT rather than a cast, so the pointee is s8 and not u8. */
extern s8 *gUnknown_08580FC0[];
/* 08580FCC  Proc_Start, sub_0806775C, started under the starter's own second
 *           parameter (r1 is never written before the bl). */
extern const struct ProcCmd gUnknown_08580FCC[];
extern const struct ProcCmd gUnknown_08580FE4[];
extern const struct ProcCmd gUnknown_08580FF4[];
extern const struct ProcCmd gUnknown_08581014[];
extern const struct ProcCmd gUnknown_08581068[];
extern const struct ProcCmd gUnknown_085810E4[];
/* Wave 26, block 0x08067000. Three ROM tables of Decompress/graphics pointers,
 * each indexed by a plain int (`lsls #2` on the index, no mask) and
 * dereferenced straight into Decompress's `u8 *` first parameter, which is why
 * the element type is `u8 *` and neither the elements nor the arrays can be
 * const -- Decompress takes non-const.
 *   08581050  sub_08067B90, into raw VRAM 0x06001400
 *   0858105C  sub_08067B90, into *gUnknown_0849957C
 *   085810C8  sub_08067D04, into raw VRAM 0x06010000                       */
extern u8 *gUnknown_08581050[];
extern u8 *gUnknown_0858105C[];
extern u8 *gUnknown_085810C8[];
/* A ROM table of SIGNED bytes: sub_08067D94 walks it with a s16 proc counter
 * running 0..0xc and reads the element as `movs r2, #0; ldrsb r2, [r0, r2]`,
 * which per docs/agbcc-codegen.md is an s8 OBJECT rather than a cast. The
 * values feed sub_08072C40's third (u16) parameter as a fade level. */
extern const s8 gUnknown_085810D4[];
/* A ROM halfword table indexed by a proc's u8 field at +0x2a (`lsls #1` then
 * `ldrh`), so the symbol address is the array base. Both readers hand the
 * element on as an OBJ tile id: sub_08067DF8 to sub_08043BF8's second
 * parameter, sub_08067E88 OR'd with 0x2000 into sub_08043C28's third. */
extern const u16 gUnknown_08581104[];
/* More 0x0858xxxx proc scripts, each the sole script argument of one starter:
 *   08581108  Proc_Find,  sub_0806A4B0 (+0x3d, u8 -- the argument narrows with
 *             `lsl #24; lsr #24` before the bl, which is PROMOTE_MODE on a u8
 *             parameter that has to survive a call, so the parameter really is
 *             u8 here rather than the int a bare strb would leave ambiguous)
 *   08581138  Proc_Start, sub_08068014 (+0x2c/+0x30/+0x34 = args, +0x40 = 0)
 *   08581480  Proc_Start, sub_08069FAC (+0x34/+0x38/+0x3c = args, +0x40 = 0)
 *   08581AC8  Proc_Start(.., PROC_TREE_3), sub_0806C874 (no fields written)
 */
extern const struct ProcCmd gUnknown_08581108[];
/* Wave 36, W36-K. Two more 0x0858xxxx proc scripts, each the sole script
 * argument of one Proc_Start:
 *   08581210  sub_080686E8 -- writes +0x29, +0x30 (halfword), +0x32, +0x2a and
 *             +0x4d, then clears +0x4f.
 *   08581264  sub_08068810 -- the same fields minus +0x4d, plus +0x38, +0x39
 *             and +0x4e. */
extern const struct ProcCmd gUnknown_08581210[];
extern const struct ProcCmd gUnknown_08581264[];
/* Wave 36, W36-K. A ROM blob handed straight to sub_0801BD00's `void *` third
 * parameter by sub_0806A7B4, with no arithmetic on the symbol -- the address
 * itself is the argument, so `u8 []`. Not const: sub_0801BD00's parameter is a
 * plain `void *`. */
extern u8 gUnknown_085815C0[];
/* Wave 36, W36-K. Two Decompress `u8 *` sources in sub_08068A00's cutscene
 * switch, each handed to Decompress by name with no arithmetic:
 *   0817DE24 -> 0x06009400 (a BG map/tile blob, uploaded in the same arm that
 *              turns BG0/1/2 and OBJ on and BG3 off)
 *   0818E364 -> 0x06010000 (OBJ tile VRAM)
 * Non-const, because Decompress takes a plain `u8 *`. */
extern u8 gUnknown_0817DE24[];
extern u8 gUnknown_0818E364[];
/* Wave 36, W36-K. sub_0806A8E4's camera path: a table of SIGNED BYTE (dx, dy)
 * pairs walked by the proc's own +0x5a step counter. Both halves are read with
 * a bare `movs rN,#0; ldrsb`, which is the s8-object pattern and decisive for
 * the sign -- dx is then negated and shifted left 12 into a Q12 offset, and dy
 * is compared against 0x5a as the end-of-path sentinel. Stride 2.
 *
 * FLAT, not `[][2]`, and that is a byte-level fact rather than a style choice:
 * `[i][1]` folds the +1 into the array BASE (`adds r0,#1` on the pool register)
 * while the ROM adds it to the INDEX (`adds r1,#1` on `i * 2`). Same rule as
 * `p[X + C]` vs `*(p + X + C)` in docs/agbcc-codegen.md. const: nothing writes
 * it. */
extern const s8 gUnknown_08581608[];
/* 0858168C  Proc_Find, sub_0806AAC4 -- the bare existence predicate
 *           `return Proc_Find(script) != 0;`, no proc field touched. */
extern const struct ProcCmd gUnknown_0858168C[];
/* 08581500  the same existence predicate again, sub_0806A474. */
extern const struct ProcCmd gUnknown_08581500[];
extern const struct ProcCmd gUnknown_08581138[];
/* Wave 44 (W44-B). The (x, y) offset pairs sub_08068770 indexes with
 * `proc->unk4f % 10 / 2` -- a 5-phase animation cycle over a 10-frame counter.
 * Both reads are `ldrsh`, so signed. const: nothing writes it, and it is the
 * only reference in the ROM.
 *
 * FLAT `s16 []` indexed `[k * 2]` / `[k * 2 + 1]`, NOT `s16 [][2]`, and the two
 * spellings are distinguishable here even though both scale by four. `[][2]`
 * folds the pair to `k * 4` and `k * 4 + 2`, which are trivially CSE-related:
 * agbcc computes the first, then reaches the second as `base += 2` off the
 * SAME product, and `k` dies at its first use. The ROM instead keeps `k` live
 * and computes `lsls #1; adds #1; lsls #1` for the second -- an undistributed
 * `(k * 2 + 1) * 2`, which is what the flat spelling generates and what `fold`
 * will not turn into `k * 4 + 2`. Two instructions' difference, measured both
 * ways. This is a THIRD case for the "column-offset fold is a type
 * discriminator" chapter in docs/agbcc-codegen.md, alongside struct row vs
 * array row: a flat array with a hand-scaled index is separable from a 2-D
 * array whenever the column index is a non-zero constant.
 *
 * gUnknown_0858125C is the sprite template that goes with it, handed straight
 * to PutSprite. Non-const `u16 []` because that is what PutSprite's fourth
 * parameter is declared as; the object itself is ROM data and nothing writes
 * it, so const would be honest if the shared prototype ever gains it. */
extern const s16 gUnknown_08581248[];
/* Wave 44 (W44-B). sub_08068038's glyph table: 8-byte records of a character
 * code and the compressed tile blob for it, scanned linearly until the code
 * matches (exactly, or with 0x20 added -- the case-folding half of the test)
 * and terminated by a record whose code is 0xFF, at which point record 12 is
 * used as the fallback glyph.
 *
 * ONE table, not two arrays, even though the splitter also invented
 * gUnknown_08581164 for the second field and the ROM carries a pool word for
 * each. That is -fforce-addr: `tbl[j].data` has the address
 * `symbol + j * 8 + 4`, and the constant folds into the relocation, so the
 * second field gets its own symbol+4 pool word rather than an `ldr [rN, #4]`
 * off the first. The sentinel arm's `ldr r0, [r0, #0x64]` is the same table at
 * a folded displacement -- 0x64 == 12 * 8 + 4 -- which is what identifies the
 * record size as 8 and not 4.
 *
 * `data` is `u8 *` to agree with Decompress's first parameter, and the three
 * filler bytes are unproved: nothing in the ROM reads +0x01..+0x03. */
struct Unk08581160Entry
{
    /* 0x00 */ u8 chr;
    /* 0x01 */ u8 filler_01[0x03];
    /* 0x04 */ u8 *data;
};

extern const struct Unk08581160Entry gUnknown_08581160[];
extern u16 gUnknown_0858125C[];
/* Wave 50 (W50-C). The 8-byte OAM blob the 0x08581210 menu proc draws every one
 * of its rows with: sub_080681B8 hands the address to PutSprite's `u16 *` fourth
 * parameter and sub_080682E8 / sub_080684E0 hand the same address to
 * PutSpriteExt's, with no arithmetic and no dereference in any of the three.
 * Non-const `u16 []` only because that is what those two shared prototypes
 * declare; the object is ROM data and nothing writes it. */
extern u16 gUnknown_08581208[];
extern const struct ProcCmd gUnknown_08581420[];
/* Wave 45 (W45-I). Four PAIRS of signed bytes, read by sub_0806974C alone: one
 * loop of four passes copies [i][0] and [i][1] into two parallel s16 runs at
 * +0x2a and +0x30 of the gUnknown_08581420 proc it has just started, walking
 * the table with a single `adds r3, #2`. `s8` is a hard readout -- both loads
 * are the register-offset `ldrsb`, THUMB's only signed byte form, and the
 * halfword stores keep the sign. Non-const only to follow the surrounding
 * 0x0858xxxx tables. */
extern s8 gUnknown_08581430[][2];
extern const struct ProcCmd gUnknown_08581480[];
extern const struct ProcCmd gUnknown_08581AC8[];
extern const struct ProcCmd gUnknown_08582AF4[];
/* Wave 36, W36-K. The proc script sub_0806F41C ends and restarts around its
 * screen rebuild -- Proc_EndEach then Proc_Start on the same symbol. */
extern const struct ProcCmd gUnknown_08582CAC[];
/* Wave 36, W36-K. A POINTER to a table of 0x10-byte screen-content records,
 * indexed by sub_0806F41C's proc `s8` at +0x38 (`ldrsb`, then `lsls #4`).
 * It is a pointer and not the table itself: every reader does `ldr rN, =sym`
 * followed by `ldr rM, [rN]` and indexes rM.
 *
 * unk00 is `u32` rather than a pointer type because it is BOTH: sub_0806F41C
 * compares it `cmp #0x11; bhi` -- an UNSIGNED compare, so not `int` -- and
 * treats values <= 0x11 as a small id handed to sub_0806AEC4, anything larger
 * as a Decompress source address. */
struct Unk0816E808Entry /* 0x10 */
{
    /* 0x00 */ u32 unk00;
    /* 0x04 */ u8 *unk04;  /* Decompress source, into whatever gUnknown_08499578
                            * points at */
    /* 0x08 */ u16 *unk08; /* ApplyPaletteExt's `u16 *` first parameter */
    /* 0x0c */ u8 unk0c;   /* palette length in 0x20-byte units: `lsls #5` gives
                            * ApplyPaletteExt's third argument */
    /* 0x0d */ u8 unk0d;   /* == 1 selects the 256-colour bit of the
                            * gUnknown_03002B6C BG shadow */
    /* 0x0e */ u16 unk0e;
};
extern struct Unk0816E808Entry *gUnknown_0816E808;
/* A gUnknown_03001470 script blob, not a proc script: sub_0806E17C hands it to
 * sub_080152EC(script, 2) and sub_0806E198 is the matching
 * `sub_08015BD0(script) != -1` liveness predicate -- the exact pair
 * gUnknown_08580DD8 above forms with sub_080670BC/sub_080670D8. */
extern const u8 gUnknown_08581F7C[];
extern const struct Unk085C77A0 gUnknown_085C77A0[];
/* Wave 34, W34-C. Three ROM blob boundaries used only as ADDRESSES, never
 * dereferenced, so `u8 []` is the weakest model that still gives the clean pool
 * word (the gUnknown_03004680 reasoning).
 *
 * sub_08033194 publishes gUnknown_08584CE8 as a base and
 * `gUnknown_085867D8 - gUnknown_08584CE8` as a byte length -- a bare `subs`
 * with no division, which only byte-sized elements produce.
 *
 * sub_08033470 DMAs gUnknown_0858D0D0 to 0x02000000 with a word count of
 * `(gUnknown_085C77A0 - gUnknown_0858D0D0) / 4`; gUnknown_085C77A0 is a struct
 * array above, so that subtraction is written through `(u8 *)` casts. */
extern u8 gUnknown_08584CE8[];
extern u8 gUnknown_085867D8[];
extern u8 gUnknown_0858D0D0[];
/* Wave 34, W34-C. A REAL four-entry ROM table of blob boundaries, NOT agbcc's
 * -fforce-addr address pool, even though it sits in data/rodata-0808F098.s
 * beside genuine pool words like gUnknown_08090D2C. The words are
 * { 0x08584CE8, 0x085867D8, 0x0858D0D0, 0x085C77A0 } -- two ascending
 * start/end pairs.
 *
 * WHAT DISTINGUISHES IT FROM A POOL: two DIFFERENT functions read different
 * elements of the one 16-byte run off a CSE'd base register --
 * sub_08033194 does `ldr r1,[r0]` / `ldr r0,[r0,#4]` and sub_08033470 does
 * `ldr r1,[r0,#8]` / `ldr r0,[r0,#0xc]`. A -fforce-addr pool is private to one
 * function, so a shared, indexed run cannot be one. Naming the four globals
 * directly (the honest spelling the wave-18 rule asks for) emits a separate
 * literal-pool word per symbol and is 4 bytes long in each function; indexing
 * this table is what matches.
 *
 * Each pair is consumed as base + length: sub_08033194 publishes [0] and
 * [1] - [0], sub_08033470 DMAs [2] with a word count of ([3] - [2]) / 4. */
extern u8 *const gUnknown_08090D5C[];
extern const s16 gUnknown_08580E64[];
extern const struct CoData gUnknown_085D3DD0[];
/* Three mode records (daily/COP/SCOP) per Commanding Officer. */
#define gCoDataTable gUnknown_085D3DD0
/* NOT const, and this was wrong here until wave 15. Family F049
 * (sub_080430B0/08043120/08043190/08043200) reads `gUnknown_085D5ABC[c].unk18`
 * twice with a `bl sub_080432E0` between the two reads, and the ROM RE-LOADS
 * it: it keeps the element ADDRESS in r6 across the call and does a second
 * `ldrb r0,[r6,#0x18]`. Controlled probe, one body, two declarations of the
 * same 0x5c layout: with `const` the load is CSEd across the call (the value
 * itself lives in r6, `cmp r6,#0`, two bytes short); without it, the reload
 * comes back and the function is instruction-for-instruction the ROM. Same
 * mechanism as the const tell on gUnknown_0849957C in docs/agbcc-codegen.md,
 * seen across a CALL rather than across a loop iteration -- a const global's
 * MEM is RTX_UNCHANGING and survives a call, a plain one is killed by it.
 * Byte-neutral for the four already-promoted users (c_080432E0.c,
 * c_080433B8.c, c_080433F8.c, c_08061E54.c), which read it once each;
 * re-verified with trymatch after the change. */
extern struct UnitTypeData gUnknown_085D5ABC[];
/* Per-unit-type metadata, indexed by the UnitRecord type id. */
#define gUnitTypeData gUnknown_085D5ABC
/* A ROM byte per unit-type id, indexed by struct UnitRecord's unk00 at an
 * ODD address, so a plain `u8 []` and not an aggregate. sub_08058254 /
 * sub_08058318 keep only the units whose entry reads exactly 2, the same
 * "class tag" role gUnknown_085D5ABC[type].unk1b plays for the 0x0805Cxxx
 * list builders. Extent unproved -- only ids > 2 are ever looked up
 * (wave 20, W20-C). */
extern u8 gUnknown_0857680F[];
/* Wave 48, W48-F. Another ROM byte per unit-type id, indexed by struct
 * UnitRecord's unk00 exactly like gUnknown_084995A8 beside it. sub_0805A5E0
 * scores a candidate with `(5 - gUnknown_08576828[p->unk00]) * 16`, so the byte
 * is a small rank in 0..5 and the subtraction inverts it -- lower table value
 * means higher score. Plain `ldrb`; signedness is not discriminated (every
 * observed value is below 6), so u8 is the weakest fit. */
extern const u8 gUnknown_08576828[];
/* Wave 49, W49-A. A MATCHED PAIR, and each one's meaning is fixed by the other
 * rather than by its own uses -- sub_0805A388 accepts a map cell exactly when
 *     gUnknown_0857685A[terrain & 0x1f] == gUnknown_08576841[unit->unk00]
 * so the two tables are a unit-type-to-class map and a terrain-to-class map
 * joined on a shared small class id.
 *   gUnknown_08576841 is indexed by a unit-type id, like gUnknown_08576828 and
 * gUnknown_084995A8 above it. Bytes out of baserom.gba:
 *     00 01 01 01 01 01 01 01 01 01 01 01 04 01 01 01 02 02 02 02 02 03 03 03 03
 * then zeros -- 25 meaningful entries, values 0..4, which is the same 25-code
 * extent the terrain tables use.
 *   gUnknown_0857685A is indexed by the low five bits of struct Map's +0x1432
 * plane, the same subscript gUnknown_085767D5 and gUnknown_085767F2 take.
 * Bytes: 00 00 00 00 00 00 01 00 01 00 02 03 00 00 01 00 then zeros, values
 * 0..3. Note it is very nearly gUnknown_085767F2 halved -- that table reads
 * 00 00 00 00 00 00 02 00 00 00 04 06 00 00 02 and is documented as being
 * `>> 1`ed before use -- but NOT identically: index 8 reads 1 here against 0
 * there, so they are two objects, not one address spelled twice.
 *   Left NON-const deliberately, unlike gUnknown_08576828 beside them. Nothing
 * needs const here (neither is indexed in a loop), and a const global's MEM
 * survives a control-flow merge where a non-const one does not -- the effect
 * c_0805A95C.c documents. Adding const could therefore CSE a load the ROM
 * recomputes, which would be a silent byte difference in some later function.
 * Extent unproved for both: only a masked or small subscript ever reaches
 * them. */
extern u8 gUnknown_08576841[];
extern u8 gUnknown_0857685A[];
/* Wave 36, W36-D. A ROM byte per TERRAIN code, indexed by the low five bits of
 * struct Map's +0x1432 plane at an ODD address, so a plain `u8 []` and not an
 * aggregate -- the same shape as gUnknown_0857680F above. sub_0805C128 uses it
 * as a predicate only (`!= 0` selects the cells that must additionally match
 * gUnknown_03004084 in their top three bits), so nothing constrains the value
 * range beyond zero/non-zero, and the extent is unproved: only codes 0..0x1f
 * can reach it, and 0x0b and 0x0d are rejected before the lookup. */
extern u8 gUnknown_085767D5[];
/* Wave 51, W51-N. A second ROM byte-per-TERRAIN table taking exactly the same
 * subscript as gUnknown_085767D5 above -- the low five bits of struct Map's
 * +0x1432 plane -- and read by sub_08059674 immediately after it. Indexed by a
 * computed subscript before any dereference (`adds r0, r4, r0; ldrb r3, [r0]`
 * with r4 = tile & 0x1f), which is the inverse test that separates a real table
 * from an -fforce-addr pool word, so this is a table.
 *   The value is not a flag: sub_08059674 compares it for equality against the
 * u8 gUnknown_030046AC as well as against zero, so it is a small id drawn from
 * the same space as that variable. Extent unproved -- only the masked 5-bit
 * subscript ever reaches it, and gUnknown_085767D5 begins 0x1d bytes later,
 * which caps the part this reader can touch at 29 entries. Not `const`, by
 * consistency with gUnknown_085767D5 and for the same CSE reason recorded
 * there. */
extern u8 gUnknown_085767B8[];
/* A ROM table of 24-byte rows indexed by gUnknown_03004582[i][0]. The stride is
 * `((n*2)+n)*8` in sub_0804C098 and sub_0804D290 alike, i.e. exactly 24, which
 * a struct cannot express any more cheaply here than the row form. Column 0
 * (`ldrh`) is the first argument of sub_08057D44; sub_0804C098 tests column 9
 * (+0x12) against zero. Rows 0..3 read 0/1/2/3 in column 0, so column 0 is the
 * row's own id. Left non-const: nothing indexes it inside a loop yet, so const
 * would buy nothing and the `const u8 *` hazard in the brief argues against
 * reflexive qualification.
 *   Wave 17: the ROWS ARE PROBABLY STRUCTS, not `u16 [12]`. sub_0804FA2C reads
 * column 9 as `adds r0, r0, r5; ldrh r1, [r0, #0x12]` -- the column offset in
 * the LOAD DISPLACEMENT. An array row cannot emit that: `fold` pulls the
 * INTEGER_CST out of the address PLUS, so `base + X*24 + 18` reassociates to
 * `(base + 18) + X*24` and costs two extra instructions. A `COMPONENT_REF`
 * applies its offset to the MEM instead and reproduces the ROM exactly
 * (measured: 664 -> 660 bytes on sub_0804FA2C, the only one of six spellings
 * that reaches it). Left as `[][12]` anyway because the change is NOT free at
 * column 0 -- rewriting sub_0804D290's `[X][0]` as `.unk00` moved it from 96.3%
 * to 95.3% at the same size -- and the four already-promoted readers all use
 * column 0 -- and sub_0804D290/sub_0804DCA8 are now MATCHED with the array
 * spelling, which settles column 0 as an array read whatever the row type is.
 * Retyping this is a job for whoever closes sub_0804FA2C; see the
 * "column-offset fold" section in docs/agbcc-codegen.md. The 24-byte row would
 * be `{ u16 unk00; u8 filler_02[0x10]; u16 unk12; u8 filler_14[4]; }`. */
extern u16 gUnknown_085D6A48[][12];
/* Wave 36, W36-C. An 8-byte record hanging off the same index pair as
 * gUnknown_085D6A48: the outer subscript is gUnknown_03004580[i][1] with a
 * 40-byte stride (`j * 5` then `<< 3`), the inner is
 * gUnknown_020296B0[i].unk18 with an 8-byte stride, and every stride is read
 * off sub_08050E08's address chain. Inside the record sub_08050E08 reads
 * `unk00[i]` -- one halfword per gUnknown_0300453C side -- and `unk04`, and
 * adds the pair to a coordinate as a (width, height) style offset. It HAD to
 * be a struct: as a flat `u16 [][5][4]` the constant last subscript is folded
 * into the base with `adds rBase,#4` where the ROM keeps it in the `ldrh`
 * displacement. unk06 is unobserved. All u16 from bare `ldrh`s. */
struct Unk085D81E8 /* 0x08 */
{
    /* 0x00 */ u16 unk00[2];
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u8 filler_06[2];
};
extern struct Unk085D81E8 gUnknown_085D81E8[][5];
/* Wave 33, W33-D. A ROM table of 20-byte rows, the row indexed by
 * gUnknown_085D6A48[gUnknown_03004580[i][1]][0] -- so it hangs off the same
 * chain gUnknown_08552FB8 does. sub_0805198C reads column 0 with `ldrh` and
 * tests it against 0 as an "this side has one" guard; nothing else in `asm/`
 * reaches any other column, so the `[10]` row length is the measured 20-byte
 * stride (`(n * 5) << 2`) and not a proved layout. */
extern u16 gUnknown_085D6EC8[][10];
/* A ROM table of five 8-byte records per row, the row indexed by
 * gUnknown_03004580[i][1] and the record by gUnknown_0300451C -- so it is the
 * per-slot pixel offset that goes with gUnknown_02029A10[i].entries[j]. The
 * strides are measured in sub_08051DE0/sub_080524C0/sub_0805297C:
 * `((n*4)+n)*8` = 40 for the row and `j*8` for the record. unk00 is indexed by
 * gUnknown_0300453C (the side) and is added to the x sum; unk04 is NOT
 * side-indexed and is added to the y sum -- the asymmetry is the ROM's, not a
 * guess. Only those two of the four halfwords have a reader. Same struct-vs-
 * `[][5][4]` argument as gUnknown_08552D80: unk04 wants the constant in the
 * `ldrh` immediate, which the array spelling folds into the symbol address. */
struct Unk085D7E28 /* 0x08 */
{
    /* 0x00 */ u16 unk00[2];
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
};
extern struct Unk085D7E28 gUnknown_085D7E28[][5];
/* A ROM table of SIGNED halfwords -- the only `ldrsh` reader in the family --
 * handed straight to sub_0803B48C, which unknown-functions.h types `s16` and
 * which is the sound-id call. Three sites index it, in sub_0804E7A8 and
 * sub_0804FCA4, and between them they fix the shape:
 *   byte offset = (gUnknown_020296B0[i].unk1a & 1) * 2
 *               + (gUnknown_03004580[i][2] - 1) * 4
 *               + gUnknown_03004580[i][1] * 24
 * so the row is 24 bytes = 6 halfwords = [6][2], the inner pair selected by the
 * alternating phase bit and the middle index by a 1-based count.
 *   TWO SYMBOLS, one table, exactly as gUnknown_0855239C / gUnknown_085523A4
 * already are: sub_0804E7A8 reaches it through 0x085D6C94 with all three
 * indices variable, so the `+0xc` sank into its pool word's relocation, while
 * sub_0804FCA4 has two sites with different constant offsets (+0xc and +0x10),
 * shares one pool word for 0x085D6C88 and adds each constant at run time. The
 * base symbol is therefore 0x085D6C88 and 0x085D6C94 is row [3] of it; only
 * rows 3 and 4 have a reader. Extents unproved in both directions.
 *   The two views are DIFFERENT TYPES and that is load-bearing, not tidiness.
 * sub_0804FCA4 needs the constant offsets to stay as run-time `adds rB,#0x10`
 * / `adds rB,#0xc` off one shared pool word, which only a struct MEMBER array
 * produces; sub_0804E7A8 needs the whole `+0xc` inside the relocation, which
 * only a plain array subscript with no constant index produces. Probed both
 * ways on both functions. */
struct Unk085D6C88 /* 0x18 */
{
    /* 0x00 */ s16 unk00[2][2]; /* Wave 36, W36-C. A SECOND pair-of-pairs at the
                                 * head of the same row, on the same two indices
                                 * unk0c uses: sub_08053FBC reads
                                 * `unk00[g03004580[i][3] == 2][g02029C04[i] & 1]`
                                 * off `row * 24`, tests it against 0 with a
                                 * plain `ldrh` and then re-reads it with a bare
                                 * `movs r2,#0; ldrsh r0,[r0,r2]` to hand
                                 * sub_0803B48C its `s16` argument -- the
                                 * s16-object pattern, and decisive for the sign.
                                 * Was the head of filler_00. */
    /* 0x08 */ s16 unk08[2]; /* Wave 37, W37-B. A THIRD entry group on the same
                              * row, between unk00 and unk0c and on the FIRST of
                              * their two indices only: sub_080541F0 reads
                              * `unk08[gUnknown_03004580[i][3] == 2]` off
                              * `gUnknown_03004580[i][1] * 24` and hands it to
                              * sub_0803B48C through a bare
                              * `movs r1,#0; ldrsh r0,[r0,r1]` -- the s16-object
                              * pattern, decisive for the sign exactly as it is
                              * for unk00. The `== 2` index arrives as a
                              * conditional `adds r2,#2` on the row offset, not
                              * as a stored flag. Was filler_08. */
    /* 0x0c */ s16 unk0c[2][2];
    /* 0x14 */ u8 filler_14[0x04];
};
extern struct Unk085D6C88 gUnknown_085D6C88[];
extern s16 gUnknown_085D6C94[][6][2];
/* ROM table of string pointers, indexed by a u32 (`lsls #2`; sub_08039F18
 * takes the index out of gUnknown_085D3DD0[..].unk38[..].unk00). The pointed-to
 * bytes are a NUL-terminated string: sub_08039F18's result goes straight into
 * sub_08039544, which copies bytes until the first zero. Left non-const because
 * sub_08039544 is not prototyped yet and takes it as a plain u8 *. */
/* NOT GLOBALS -- 0x0816D9E0-0x0816DA43 is agbcc's own .rodata address-constant
 * pool, TWELVE consecutive PAIRS plus one lone word, one entry per LOOPING
 * builder, in address order. Wave 44, W44-E EXTENDS this note past the
 * 0x0816DA37 it used to claim as the end, and corrects "all thirteen builders
 * are now matched, so the block is fully accounted for": the builder set does
 * not stop at 0x0805D1F0.
 *   - 0x0816DA38/0x0816DA3C is a TWELFTH pair, and it belongs to sub_0805D2A0,
 *     which is c_0805CA60.c's shape exactly (`gUnknown_030046B0 =
 *     gUnknown_030045F0`, the 0x40-entry scan, the `*p++ = i` append) minus the
 *     sub_0805D344 call. Both words hold 0x030046B0 / 0x030045F0 like every
 *     other pair, re-dereferenced against baserom.gba.
 *   - 0x0816DA40 is a LONE word holding 0x030044B0, not part of a pair: it is
 *     sub_0805D5EC's -fforce-addr constant for gUnknown_030044B0, whose address
 *     is live across that function's 6-byte copy loop and re-read afterwards for
 *     the sub_080308B4 call. Same rule, different global.
 * The pair-per-looping-builder rule therefore held on two functions it had not
 * been tested on. Everything below still applies.
 *   Historical text, still accurate for 0x0816D9E0-0x0816DA37: ELEVEN
 * consecutive PAIRS, one pair per LOOPING list-builder function at
 * 0x0805CA60-0x0805D1F0, in address order. Every even slot holds 0x030046B0
 * and every odd slot 0x030045F0, i.e. &gUnknown_030046B0 and
 * gUnknown_030045F0 -- checked against the ROM image. Do NOT declare these as
 * globals; the source is the plain
 *     gUnknown_030046B0 = gUnknown_030045F0;
 * and -fforce-addr synthesised the pair because the address is live across the
 * builder's loop. See "The .rodata address-constant reroute" in
 * docs/agbcc-codegen.md; sub_0805CDF0/sub_0805CE20, the two builders with no
 * loop, have no pair, which is what pins the rule.
 *   Wave 19 closed the last four builders (sub_0805CB1C, sub_0805CC88,
 * sub_0805CF0C, sub_0805D1F0) and re-measured the whole block. Two corrections
 * to what this note said before, both from the ROM rather than from inference:
 *   - It is ELEVEN pairs, not thirteen. 0x0816DA38-0x0816D9E0 = 0x58 = 22 words,
 *     and there are thirteen builders of which two do not loop. The old
 *     "thirteen pairs" contradicted this note's own last sentence.
 *   - The pair-to-function mapping was confirmed rather than assumed, by
 *     grepping each builder's body for its own pool symbols. It is strict
 *     address order with the two loopless builders SKIPPED and no hole left
 *     behind: CA60 D9E0, CB1C D9E8, CBCC D9F0, CC88 D9F8, CD34 DA00,
 *     (CDF0 and CE20 none), CE50 DA08, CF0C DA10, CFBC DA18, D078 DA20,
 *     D134 DA28, D1F0 DA30. That is the "slot order IS function order" rule in
 *     docs/agbcc-codegen.md holding across an absent entry, which is the case
 *     that rule had not been tested on.
 * All 22 words re-dereferenced against baserom.gba: 0x030046B0 then 0x030045F0,
 * eleven times, no exception. All thirteen builders are now matched, so the
 * block is fully accounted for and nothing in it is a global. Note that
 * data/families.json's F089 `varies` lists six DIFFERENT pool symbols for three
 * functions reading two globals; that entry is correct about the .text
 * literal-pool contents and misleading about the source, and it is the worked
 * instance of the "several pool words in a run can hold the SAME address"
 * rule. */
/* 0x0816E164 is another slot of the same block and holds the same value,
 * 0x08580934 -- checked against the ROM image, not inferred. sub_08066D30 is
 * sub_0806DDF4's twin over it, down to the loop bound and the +0x46 flag, so
 * it takes the same type and the same `const` + `pp = &sym` treatment. */
/* Wave 32 (W32-B): four halfwords (0x00cb, 0x00ce, 0x00cf, 0x00d0 in the ROM)
 * that sub_080649D0 memcpy's onto an 8-byte stack buffer with sub_0808B6E8 and
 * then indexes with obj->unk48 to pick a sprite id. `const u16 [4]` -- the
 * extent is the copy length, and the element width is the `ldrh` at the use. */
extern const u16 gUnknown_0816E0C8[4];
extern struct Unk0816E1B8 *const gUnknown_0816E164;
/* Wave 32 (W32-B): 0x20 bytes of ROM that sub_08065EF4 copies onto its stack
 * with three ldm/stm pairs and then indexes with `unk32 & 1`, reading the s16
 * at +8/+0xa for one call and +0xc/+0xe for the other. Values (baserom.gba):
 *   { 0, 0, 0x30, 0x30, 0x10, -8, 0x10, 0x30 }
 *   { 0x28, 0, 0x10, 0x10, 0x28, -9, 0x28, 0x10 }
 *
 * WHICH SIDE OF THE .rodata QUESTION THIS IS, RECORDED: an auto aggregate with
 * that initialiser compiles to the SAME 116 bytes -- 100.0%, verified -- but
 * relocates its pool word against a fresh `.rodata` label rather than against
 * this address, and try_match cannot resolve that (unlike a pool word whose
 * content is a known symbol, which it accepts and annotates). Naming the
 * object here is the spelling that verifies. The two readings are
 * indistinguishable in the ROM; if the next wave finds a second reader at this
 * address, that settles it as a real global. */
struct Unk0816E120Entry
{
    /* 0x00 */ s16 unk00;
    /* 0x02 */ s16 unk02;
    /* 0x04 */ s16 unk04;
    /* 0x06 */ s16 unk06;
    /* 0x08 */ s16 unk08;
    /* 0x0a */ s16 unk0a;
    /* 0x0c */ s16 unk0c;
    /* 0x0e */ s16 unk0e;
};

struct Unk0816E120
{
    /* 0x00 */ struct Unk0816E120Entry unk00[2];
};

extern const struct Unk0816E120 gUnknown_0816E120;
/* See struct Unk0816E1B8 for why this is const and why it is an extra level of
 * indirection in front of gUnknown_08580934 rather than a global of its own. */
extern struct Unk0816E1B8 *const gUnknown_0816E1B8;
extern u8 *gUnknown_08610A38[];
/* A ROM u16 table read as `gUnknown_08616F0C[unk1d * 4 + gUnknown_03005940]` by
 * sub_080852A8 -- FLAT and not `[][4]`: the ROM scales the row index and then
 * adds the column before the single element shift (`lsl #2; add; lsl #1`),
 * where a `u16 [][4]` scales each index separately. Both indices are variables
 * here, which is what makes the two spellings distinguishable at all. */
extern u16 gUnknown_08616F0C[];
/* Wave 44 (W44-C). A NULL-terminated table of six nullary predicates, walked by
 * sub_08084804 with `ldr r0, [r6]; bl _call_via_r0` -- `_call_via_r0` is the
 * register index that counts the arguments, so nullary -- and the result is
 * zero-extended with `lsls #0x18; lsrs #0x18`, so each returns u8. The seven
 * words at 0x08616B00 dereference in baserom.gba to
 *   sub_080848B5, sub_08084939, sub_08084921, sub_080848FD, sub_08084971,
 *   sub_080848D9, 0
 * (THUMB, hence the +1), and every one of the six is a `bool8 f(void)` "scan a
 * range, return FALSE on the first failure" predicate -- five of them are in
 * this wave's batch. sub_08084804's loop runs exactly six iterations
 * (`movs r5,#5` counting down with `bge`), so the terminator is never read
 * there; it is declared as an incomplete array rather than [6] on that basis. */
extern u8 (*const gUnknown_08616B00[])(void);
/* Proc scripts. sub_0807639C and sub_0807F8D0 hand 08614460 / 08616740 to
 * Proc_Start(script, parent); sub_08078540 hands 08615AAC to
 * Proc_StartBlocking and stashes a pointer at +0x54 of the new proc. */
extern const struct ProcCmd gUnknown_08614460[];
extern const struct ProcCmd gUnknown_08615AAC[];
/* Proc_StartBlocking'd by sub_0807F82C under the proc it just found through
 * gUnknown_086165C0; sub_0807F618 is its existence predicate. */
/* Wave 31 (W31-C): two more proc scripts of the 0x0807B000 tree, each the
 * first argument of a `Proc_Start(script, proc)` -- 0861604C from sub_0807B760
 * and 08616570 from sub_0807BA68 and sub_0807BE90, all three gated on a
 * gpKeySt->held bit. */
/* Wave 31 (W31-C): the proc script sub_0803EF44 hands to Proc_StartBlocking
 * under its own third argument, writing +0x2c and +0x30 of the new proc from
 * its first two before passing the same pair, narrowed to s16, to
 * sub_0802909C. */
extern const struct ProcCmd gUnknown_0849F7F0[];
/* Wave 32 (W32-B): sub_0803F0A4's script. */
extern const struct ProcCmd gUnknown_0849F830[];
extern const struct ProcCmd gUnknown_0861604C[];
extern const struct ProcCmd gUnknown_08616570[];
extern const struct ProcCmd gUnknown_08616710[];
extern const struct ProcCmd gUnknown_08616740[];
/* Proc_Start'd by sub_08087884 under a caller-supplied parent, with one word
 * of payload stashed at +0x54 of the new proc. */
/* Wave 44 (W44-C). Two proc scripts sub_08086EB0 drives as an either/or pair:
 * it hands both to Proc_EndEach / Proc_Find and starts exactly one of them
 * under PROC_TREE_3 -- 08616D1C when the byte it looks up is <= 0xb3 and
 * sub_0803CA54 accepts it, 08616D6C otherwise. The 08616D1C proc carries an
 * s16 at +0x66, which is the same field src/decomp/c_08086D98.c reads back. */
extern const struct ProcCmd gUnknown_08616D1C[];
extern const struct ProcCmd gUnknown_08616D6C[];
extern const struct ProcCmd gUnknown_08616D94[];
/* A ROM halfword table: every user computes `index * 2 + &g` and reads it with
 * `ldrh` (sub_08074D28, sub_08074EEC, sub_08074FE4, sub_08075008 and four
 * more), so the symbol address is the array base and not a pointer.
 * NOT const, on the same evidence as gUnknown_0849957C: sub_08074EEC reads one
 * element inside a 5-iteration loop and the ROM re-`ldrh`s it every pass. With
 * `const` GCC hoists the load into the preheader and the function is a
 * register permutation away from matching. */
extern u16 gUnknown_0861433C[];
/* The 0x30-byte record array behind struct Unk08615194 above. */
extern const struct Unk08615194 gUnknown_08615194[];
/* Wave 33, W33-E. Two more scripts of the 0x08614xxx block, each fixed by the
 * starter it is handed to: 08614268 goes to `Proc_Start(script, PROC_TREE_3)`
 * in sub_08074320 and 086142CC to `Proc_StartBlocking(script, proc)` in
 * sub_08074A28, which then writes +0x2a/+0x2c/+0x2e/+0x30 and +0x38 of the
 * child -- the same field group c_080749FC.c's 086142E4 sibling uses. */
extern const struct ProcCmd gUnknown_08614268[];
extern const struct ProcCmd gUnknown_086142CC[];
/* 08614314 -- the scroll proc. sub_08074C84 Proc_Find's it as a busy check and
 * then starts it either way (Proc_StartBlocking under its own first argument if
 * that is non-null, Proc_Start under PROC_TREE_3 otherwise), filling +0x2c..
 * +0x36 of the child with the target and the current gUnknown_0202FDFC origin.
 * sub_08074B60 is its body. */
extern const struct ProcCmd gUnknown_08614314[];
/* Wave 33, W33-E. An EWRAM byte table sub_08074B60 indexes with its proc's
 * +0x3c countdown and subtracts from the +0x3a accumulator. `s8` off the load:
 * the ROM uses the register-offset `ldrsb`, which is the only form THUMB has, so
 * the SIGN is the readable half and the register-zero beside it is the offset.
 * A `u8 []` would need an explicit cast at the one use and emit `ldrb`. */
extern s8 gUnknown_0202FEF8[];
/* Wave 33, W33-E. sub_08074F2C hands the ADDRESS straight to PutSpriteExt's
 * `u16 *` fourth parameter with no dereference, so unlike its 0x081CC4D8 /
 * 0x081CC4DC / 0x081CC4E0 neighbours -- which c_08074BDC.c proved are agbcc
 * address constants holding &gUnknown_0202FDFC -- this one is real sprite data
 * at that address. NOT proved: nothing reads through it yet. */
extern u16 gUnknown_081CC4E8[];
/* Wave 34, W34-G. The 0x081CC4F0 neighbour of the blob above, on the same
 * evidence: sub_0807519C, sub_08075248 and sub_080750C0 each hand the ADDRESS
 * to PutSprite / PutSpriteExt's `u16 *` fourth parameter with no dereference.
 * NOT const, because that parameter is not. */
extern u16 gUnknown_081CC4F0[];
/* Wave 34, W34-G. A four-entry SIGNED byte table of sprite y-jitter: both
 * sub_08075008 and sub_08075248 index it with `(counter >> 3) & 3` and add the
 * result to the +0x2e / +0x36 y coordinate. `s8` is read straight off the two
 * loads -- `ldrb` followed by `lsls #24; asrs #24`, which is what a plain `s8`
 * array element widened to int emits; a `u8 []` would stop at the `ldrb` and a
 * `s8` reached through a register offset would use `ldrsb` instead. The add is
 * signed, so the table dips the sprite both ways. */
extern s8 gUnknown_0861436C[];
/* Wave 34, W34-G. The script behind sub_08075058, the per-entry child spawner
 * of the gUnknown_08614390 menu proc: sub_0807519C calls sub_08075058 once per
 * frame-group and stores the returned proc into the TEN-entry pointer table at
 * +0x3c..+0x60 of its own proc -- the same table sub_080752D8 walks. The
 * starter writes +0x2a/+0x2c/+0x2e/+0x30 halfwords and zeroes the +0x34 word,
 * and sub_080750C0 is its body (it reads +0x34..+0x3a and Proc_Breaks). */
extern const struct ProcCmd gUnknown_08614370[];
/* More proc scripts, from the 0806Exxx / 08073xxx / 08074xxx starters:
 *   08582BB4  Proc_StartBlocking, sub_0806E6C8 (+0x5c = arg)
 *   08582BE4  Proc_Start,         sub_0806E728 (+0x54 = the parent it was
 *                                 started under, so the field is a ProcPtr)
 *   08582C24  Proc_Start,         sub_0806E8C8 (+0x58/+0x5c = 0, +0x60 = arg)
 *   086141B4  Proc_Start(.., PROC_TREE_VSYNC), sub_08073900 (+0x5c = arg)
 *   086142E4  Proc_StartBlocking, sub_08074AAC (+0x2c = arg, +0x30 = 0)
 *
 * And the same family again:
 *   08582B2C  Proc_Start,         sub_0806E5CC (+0x34 u16 = arg, +0x2c = 0x140,
 *                                 +0x30 = 0xa0 -- 320x160, so the pair is a
 *                                 screen-sized extent rather than two flags)
 *   08582B74  Proc_StartBlocking, sub_0806E698, started under the starter's own
 *                                 first argument, and +0x5c of the new proc is
 *                                 copied from +0x5c of that parent -- so the
 *                                 parent is a proc of this same shape
 *   08582B94  Proc_StartBlocking, sub_0806E6B4 AND sub_0806E6E0, two identical
 *                                 20-byte starters on one script, neither
 *                                 writing a field
 *   08582D74  Proc_StartBlocking, sub_0806F710 (no fields written). This one
 *                                 sits immediately above the m4a/MP2K span, but
 *                                 it takes its parent in r0 like every other
 *                                 member of this family, so it is ordinary
 *                                 compiler output and not m4a_asm.s.
 *   0861418C  Proc_Start(.., PROC_TREE_VSYNC), sub_080736C4 (no fields written)
 *   086141DC  Proc_Start(.., PROC_TREE_VSYNC), sub_08073C88 (+0x5c = arg)
 */
extern const struct ProcCmd gUnknown_08582B2C[];
extern const struct ProcCmd gUnknown_08582B74[];
extern const struct ProcCmd gUnknown_08582B94[];
extern const struct ProcCmd gUnknown_08582BB4[];
extern const struct ProcCmd gUnknown_08582BE4[];
extern const struct ProcCmd gUnknown_08582C24[];
extern const struct ProcCmd gUnknown_08582D74[];
/* Proc scripts from the 0x080719C0-0x0807298C starter family. The +0x64 group
 * is six starters over four scripts, in two shapes that pair off: a tree-3
 * Proc_Start taking only the payload, and a Proc_StartBlocking that forwards
 * its own second parameter as the parent. Both stash one HALFWORD at +0x64
 * (`adds r0,#0x64; strh` -- 0x64 is past the strh displacement limit, so the
 * runtime add is forced and says nothing about the source).
 *   08613E08  Proc_Start tree 3,  sub_080719C0 (no fields written)
 *   08613E64  Proc_Start tree 3,  sub_08071EF0 (+0x64) and
 *             Proc_StartBlocking, sub_08071F28 (+0x64), parent forwarded
 *   08613E84  Proc_Start tree 3,  sub_08071F0C (+0x64) and
 *             Proc_StartBlocking, sub_08071F40 (+0x64), parent forwarded
 *   08613EA4  Proc_StartBlocking, sub_08071F58 (+0x64), parent forwarded
 *   08613EC4  Proc_StartBlocking, sub_08071F70 (+0x64), parent forwarded
 *   08613F0C  Proc_StartBlocking, sub_0807249C (+0x58 word), parent is that
 *             starter's FIRST parameter
 *   08613F34  Proc_Start tree 3,  sub_08072970 (+0x2c/+0x34 words)
 *   08613F44  Proc_Start tree 3,  sub_0807298C (+0x2c/+0x30/+0x34 words) */
extern const struct ProcCmd gUnknown_08613E08[];
extern const struct ProcCmd gUnknown_08613E64[];
extern const struct ProcCmd gUnknown_08613E84[];
extern const struct ProcCmd gUnknown_08613EA4[];
extern const struct ProcCmd gUnknown_08613EC4[];
/* 08613EE4  Proc_Find, sub_080723C0 (+0x4c word = 0). Unlike the rest of this
 *           group the find is NULL-checked before the store, so the poker is
 *           `p = Proc_Find(s); if (p) p->unk4c = 0;` rather than the usual
 *           unchecked cast-and-store. */
extern const struct ProcCmd gUnknown_08613EE4[];
extern const struct ProcCmd gUnknown_08613F0C[];
extern const struct ProcCmd gUnknown_08613F34[];
extern const struct ProcCmd gUnknown_08613F44[];
extern const struct ProcCmd gUnknown_0861418C[];
extern const struct ProcCmd gUnknown_086141B4[];
extern const struct ProcCmd gUnknown_086141DC[];
extern const struct ProcCmd gUnknown_086142E4[];
/* Proc scripts from the 0x08074xxx-0x08078xxx starter family:
 *   086142B4  Proc_Start,         sub_08074714 (+0x58 = 0xc00), and
 *             Proc_Find,          sub_0807472C (+0x58 = arg << 10) -- one
 *             script with a starter and a poker, so +0x58 is one u32 field
 *   08614344  Proc_Start,         sub_08074ED0 (+0x54 = arg, +0x58 = 0), and
 *             Proc_Find,          sub_08074EEC -- which walks +0x40 as an array
 *             of FIVE pointers (`ldm r3!, {r1}` five times off proc+0x40) and
 *             pokes a halfword into +0x22 of each. So this proc owns a
 *             5-element pointer table at +0x40 and runs to at least +0x54
 *   086143E0  Proc_Start,         sub_080758BC (+0x2c/+0x30/+0x38 = args,
 *                                 +0x34 = 0 as a HALFWORD, +0x3c/+0x40 = 0)
 *   086144FC  Proc_Start,         sub_08076770 (+0x2c/+0x30/+0x58 = args,
 *                                 +0x5c = 0, +0x64 = 0 halfword), and
 *             Proc_Find,          sub_080767A8 (+0x64 = 1 halfword)
 *   0861485C  Proc_Start(.., PROC_TREE_3), sub_0807813C (no fields written)
 *   08614894  Proc_Start(.., PROC_TREE_3), sub_080780D0 (no fields written)
 *   0861598C  Proc_StartBlocking, sub_08078480 (+0x54 = arg) -- same shape as
 *                                 08615AAC / sub_08078540
 *   08615A8C  Proc_StartBlocking, sub_080784E4 (+0x58/+0x5c/+0x60 = args)
 */
extern const struct ProcCmd gUnknown_086142B4[];
extern const struct ProcCmd gUnknown_08614344[];
extern const struct ProcCmd gUnknown_086143E0[];
extern const struct ProcCmd gUnknown_086144FC[];
extern const struct ProcCmd gUnknown_0861485C[];
extern const struct ProcCmd gUnknown_08614894[];
extern const struct ProcCmd gUnknown_0861598C[];
extern const struct ProcCmd gUnknown_08615A8C[];
/* The scripts named by the second half of the 16-byte
 * `push {lr}; ldr r0,=g; bl f; pop {r0}; bx r0` forwarder family (family F000
 * in data/families.json). Each is `const struct ProcCmd []` because its
 * consumers are proc.h prototypes that take exactly that, and `const` because
 * they are ROM data nothing writes. Evidence, one line per script -- in every
 * case a Proc_EndEach forwarder from that family plus at least one starter or
 * poker that pins the object as a proc script rather than a bare blob:
 *   08580F24  sub_0806A444 -> sub_08067504, which is Proc_BreakEach open-coded
 *             over sProcArray. Sole reference in the ROM, so nothing here
 *             constrains the proc's fields.
 *   085810B8  Proc_Start,   sub_08067D04 (+0x34/+0x38 = args, +0x3c = 0);
 *             Proc_EndEach, sub_08067D4C
 *   08613E54  Proc_Start,   sub_08071B28 (+0x2c = a gUnknown_0202F2DC record
 *             the starter also fills);  Proc_EndEach, sub_08071B88
 *   08613F2C  Proc_Start,   sub_080725A8 (+0x2c word, then halfwords at
 *             +0x30/+0x32/+0x34/+0x36/+0x38/+0x3a); Proc_EndEach, sub_08072598
 *   08614220  Proc_Start,   sub_08073FF4 (+0x2c word, then 8 halfword pairs
 *             written from +0x30 and +0x40);        Proc_EndEach, sub_08074028
 *   08614390  Proc_Start,   sub_08075298 (+0x2c word, +0x34/+0x36/+0x38
 *             halfwords); Proc_Find, sub_080752D8, which pokes +0x38 and then
 *             walks a TEN-entry pointer table at +0x3c..+0x60 poking +0x30 of
 *             each non-NULL entry;                  Proc_EndEach, sub_08075304
 *   086143B8  Proc_Start,   sub_0807548C (+0x2a..+0x34 halfwords, +0x38/+0x3c
 *             words); Proc_Find, sub_0807553C (same fields);
 *             Proc_EndEach, sub_080755E0
 *   08615CA0  Proc_Start,   sub_08078D80 under the caller's own parent, no
 *             fields written;                       Proc_EndEach, sub_08078E04
 *   08616638  Proc_Start,   sub_0807C978 under the caller's own parent, no
 *             fields written;                       Proc_EndEach, sub_0807F8C0
 *   08616DB4  Proc_Find,    sub_08087974 and sub_08087C14 (+0x54 word);
 *             Proc_Start,   sub_08087974;           Proc_EndEach, sub_080879A0
 */
extern const struct ProcCmd gUnknown_08580F24[];
extern const struct ProcCmd gUnknown_085810B8[];
/* Wave 32, W32-A. The gUnknown_08613E54 proc's per-slot record, one per palette
 * bank: sub_08071B28 allocates &gUnknown_0202F2DC[index], CpuSets 0x10
 * HALFWORDS of &gPal[index * 16] into its head -- which is what fixes both the
 * 0x20-byte snapshot and the 0x30 stride (`lsls #1; adds; lsls #4`) -- and then
 * fills the five fields behind it. unk20 is the source palette the caller
 * supplied, unk24 the gPal slice to fade towards it, unk28 a step counter
 * started at 0, and unk2a/unk2c a duration and that duration plus one. */
struct Unk0202F2DC /* 0x30 */
{
    /* 0x00 */ u16 unk00[0x10];
    /* 0x20 */ const void *unk20;
    /* 0x24 */ u16 *unk24;
    /* 0x28 */ u16 unk28;
    /* 0x2a */ u16 unk2a;
    /* 0x2c */ u16 unk2c;
    /* 0x2e */ u8 filler_2e[0x02];
};
extern struct Unk0202F2DC gUnknown_0202F2DC[];
/* Wave 33, W33-D. A small ROM step table indexed by the debug proc's own
 * 0..4 cursor (+0x5c): sub_080719EC adds gUnknown_08613E48[unk5c] to the value
 * at +0x58 on Up and subtracts it on Down, so the entries are the per-digit
 * increments of a decimal editor whose value is clamped to 0x1869f (999999).
 * `ldrh` off a `lsls #1` index, nothing sign-extends. */
extern const u16 gUnknown_08613E48[];
extern const struct ProcCmd gUnknown_08613E54[];
extern const struct ProcCmd gUnknown_08613F2C[];
/* 08614014  Proc_StartBlocking, sub_080729AC under the starter's own only
 *           parameter, stashing sub_08034F6C()'s result at +0x64 (s16);
 *           sub_080729CC is the matching wait, breaking the frame
 *           sub_08034F6C() comes back equal to that snapshot. */
extern const struct ProcCmd gUnknown_08614014[];
extern const struct ProcCmd gUnknown_08614220[];
extern const struct ProcCmd gUnknown_08614390[];
extern const struct ProcCmd gUnknown_086143B8[];
extern const struct ProcCmd gUnknown_08615CA0[];
extern const struct ProcCmd gUnknown_08616638[];
extern const struct ProcCmd gUnknown_08616DB4[];
/* NOT proc scripts, despite sitting inside the same 0x0858xxxx table as the
 * ones above: these two are gUnknown_03001470-list blobs, the same slot as
 * gUnknown_0849A0F0 and gUnknown_0849A3C0. Each has an install/remove pair that
 * repeats verbatim -- sub_080656E0 is `sub_080152EC(08580C7C, 3)` and
 * sub_08065700 is `sub_0806377C(08580C7C)`; sub_0806D820 / sub_0806D840 are the
 * same two lines over 08581F40. `const u8 []` is the weakest model that
 * reproduces the clean pool word: nothing indexes or dereferences either, and
 * both consumers take them as an opaque `const void *`. Widen when
 * sub_080152EC's slot payload is settled. Address proximity to the proc scripts
 * is therefore NOT evidence of what a 0x0858xxxx symbol is -- read the
 * consumer, not the address. */
extern const u8 gUnknown_08580C7C[];
extern const u8 gUnknown_08581F40[];
/* Wave 46 (W46-E): three more gUnknown_03001470-list blobs of exactly the same
 * kind, one per reader -- sub_08064B68 (0858096C), sub_0806D34C (08581E94),
 * sub_0806D620 (08581ECC). Each reader is `if (sub_08015BD0((s32)blob) != -1)
 * { loop; sub_0806377C(blob); }`, i.e. the same two consumers as the pair
 * above, so the same `const u8 []`.
 *
 * WHERE THE ADDRESSES CAME FROM, because they are NOT in the readers' own
 * literal pools: the three readers all open `ldr r4, =gUnknown_0816Exxx;
 * ldr r0, [r4]` and the splitter invents a symbol for that word. Those words
 * are agbcc's own -fforce-addr address pool -- reading the blob twice across
 * the loop is the trigger -- and the three ROM words 0816E0F4 / 0816E194 /
 * 0816E198 hold 0858096C / 08581E94 / 08581ECC respectively, read out of
 * baserom.gba. Naming the BLOB is what reproduces the double load; declaring
 * the 0816Exxx word itself as a `const u8 *` global instead adds a third `ldr`
 * at both use sites, because agbcc then force-addrs that global in turn.
 * Probed side by side. This is the same object the gUnknown_0816E1B8 comment
 * describes, reached from the other end. */
extern const u8 gUnknown_0858096C[];
extern const u8 gUnknown_08581E94[];
extern const u8 gUnknown_08581ECC[];
/* Wave 46 (W46-E): the four parallel seven-entry ROM tables sub_0806D268 walks
 * as it builds the seven gUnknown_08580934->unk54[] objects. Extents are the
 * trip count (i <= 6) and, for each, the address of the next one:
 *   gUnknown_0816E0D0[7]  u16, 0x0050 0x0058 0x0060 0x0068 0x0070 0x0078 0x0122
 *   gUnknown_0816E0DE[7]  u16, 0x0000 0x0010 0x0020 0x0030 0x0030 0x0040 0x0112
 *   gUnknown_085809A4[7]  u16, 2 4 0x12 0x60 0x64 2 4 -- read `ldrh` and stored
 *                         `strb` into Unk08580934_Obj.unk4b, so the u16 is the
 *                         table's own width and the truncation is at the use
 *   gUnknown_085809D0[7]  u8, all 1
 * gUnknown_085809B4 is the seven handlers those objects run: the words are
 * 08064739 08064775 080647BD 0806486D 08064919 08064739 080649D1, all odd, so
 * THUMB entry points with the T bit set, and they land in
 * Unk08580934_Obj.unk4c, which sub_080645AC already types as
 * `void (*)(struct Unk08580934_Obj *)`. Element 0 and element 5 are the SAME
 * handler -- a table of distinct globals does not repeat, which is the same
 * argument the gUnknown_0816E1B8 comment makes.
 * gUnknown_085809B4 and gUnknown_085809D0 are reached through agbcc's own
 * -fforce-addr words (the ROM words at 0x0816E18C and 0x0816E190 hold
 * 0x085809B4 and 0x085809D0); naming the tables is what reproduces that. The
 * other three are reached by a plain literal-pool `ldr`. Values read out of
 * baserom.gba. */
extern const u16 gUnknown_0816E0D0[7];
extern const u16 gUnknown_0816E0DE[7];
extern const u16 gUnknown_085809A4[7];
extern void (*const gUnknown_085809B4[7])(struct Unk08580934_Obj *);
extern const u8 gUnknown_085809D0[7];
/* Two 0xFF-terminated u8 id lists handed to sub_08074AAC by sub_08078440 and
 * sub_08078454. The ROM contents are {8, 9, 0xa, 0xff} and {0x10, 0x11, 0x12,
 * 0xff} -- read out of baserom.gba, not inferred.
 *
 * They are TWO symbols and not one array, and the evidence is sub_08078358,
 * which is the third caller of sub_08074AAC: it walks a run of 4-byte records
 * based at gUnknown_08615974 (`adds r4, #4`, i = 0..3, so 0x08615974/78/7c/80)
 * and passes `&record[i][2]` -- i.e. the last two bytes of each record, which
 * are also a 0xFF-terminated list. So the whole 0x08615974-0x0861598B block is
 * a mixture of two unrelated things at 4-byte granularity, and the 0x84/0x88
 * pair is reached only by two separate wrappers naming two separate addends of
 * zero. An `(&g)[1]` spelling of the second would relocate as
 * gUnknown_08615984+4 and still link identically -- so this choice is
 * byte-neutral and could not be proved from the wrappers alone; it is the
 * weakest model that gives the ROM's two clean pool words. */
extern const u8 gUnknown_08615984[];
extern const u8 gUnknown_08615988[];
/* A gUnknown_0200C528 list script, exactly the gUnknown_0849A8F0 shape: its
 * sole reference in the ROM is sub_08078958 handing its ADDRESS to
 * sub_080193B0(const u8 *). `const u8 []` for the same reason -- nothing
 * indexes or dereferences it, so that is the weakest model giving the clean
 * pool word. */
extern const u8 gUnknown_08615B4C[];

/* The start/exists proc-script family, scattered across code-0806CFC8.s from
 * 0x08078150 to 0x0808AA88. Each script below gets exactly two wrappers, and
 * where both exist they are ADJACENT at 0x14 spacing -- a 20-byte starter
 *     void f(void)  { Proc_Start(script, PROC_TREE_3); }
 * followed immediately by a 24-byte existence predicate
 *     int  g(void)  { return Proc_Find(script) != 0; }
 * Ten such pairs plus three predicates whose starters sit up at 0x080780D0 /
 * 0x0807813C. All thirteen scripts take PROC_TREE_3 and neither wrapper reads
 * or writes a single proc field, so these say nothing about the proc layout --
 * unlike the 086142B4 family above, they constrain no struct offsets.
 *
 * Verified uniform across all 23 wrappers: every starter is `movs r1, #3`,
 * every predicate is the same cmp/beq/movs shape. There is no per-member
 * variation in tree number, shift kind or masking.
 *
 *   086147FC  Proc_Find only, sub_08078150   (starter not in this file)
 *   08615CB0  sub_0807A970 / sub_0807A984
 *   08615FB4  sub_0807B790 / sub_0807B7A4
 *   086164A0  sub_0807C55C / sub_0807C570
 *   086165C0  sub_0807F524 / sub_0807F538
 *   086166C8  sub_0807F550 / sub_0807F564
 *   08616990  sub_080846C8 / sub_080846DC
 *   08616B74  sub_08085AC8 / sub_08085ADC
 *   08616C54  sub_08087858 / sub_0808786C
 *   08616DFC  sub_0808A638 / sub_0808A64C
 *   08616FD4  sub_0808AA74 / sub_0808AA88
 * and reusing two scripts already declared above:
 *   0861485C  sub_0807813C  / sub_08078168
 *   08614894  sub_080780D0  / sub_08078180
 *
 * The PREDICATE half of the family is wider than the starter half, and the two
 * do not have to travel together. Three more 24-byte predicates of exactly the
 * shape above sit on scripts whose starters are ordinary payload-writing
 * wrappers rather than the 20-byte PROC_TREE_3 form, and they are nowhere near
 * 0x08078150:
 *   086141B4  sub_08073918  (starter sub_08073900, PROC_TREE_VSYNC, +0x5c = arg)
 *   086141DC  sub_08073CA0  (starter sub_08073C88, PROC_TREE_VSYNC, +0x5c = arg)
 *   08616710  sub_0807F618  (Proc_StartBlocking'd by sub_0807F82C)
 * Each is the immediate neighbour of its own starter, at the same 0x14/0x18
 * spacing. So when scoping a batch: a 24-byte leaf that is `bl Proc_Find`
 * followed by cmp/beq/movs is a member of this family whatever its address, and
 * `return Proc_Find(script) != 0;` matches it first try.
 */
extern const struct ProcCmd gUnknown_086147FC[];
extern const struct ProcCmd gUnknown_08615CB0[];
extern const struct ProcCmd gUnknown_08615FB4[];
extern const struct ProcCmd gUnknown_086164A0[];
extern const struct ProcCmd gUnknown_086165C0[];
extern const struct ProcCmd gUnknown_086166C8[];
extern const struct ProcCmd gUnknown_08616990[];
extern const struct ProcCmd gUnknown_08616B74[];
extern const struct ProcCmd gUnknown_08616C54[];
extern const struct ProcCmd gUnknown_08616DFC[];
extern const struct ProcCmd gUnknown_08616FD4[];

/* ---- wave 12 ----
 * gUnknown_03001470-list script blobs, all reached ONLY as an address handed to
 * sub_080152EC (`const void *`) or sub_080152C0, exactly the slot
 * gUnknown_0849A0F0 / gUnknown_0849A3C0 / gUnknown_0849A108 occupy. `const u8
 * []` is the weakest model that reproduces the clean pool word: nothing indexes
 * or dereferences any of them, and both consumers take an opaque pointer.
 * `const` is safe here for the same reason it is on the other blobs -- neither
 * consumer's declaration is non-const, so there is no -Werror hazard of the kind
 * the Decompress(u8 *, void *) note in docs/agbcc-codegen.md warns about.
 * Extents from data/data*.s, which is where the split drew the symbol
 * boundaries, so they are upper bounds on the record and not proved sizes:
 *   08485D9C 0x18   08485DB4 0x10   08499E4C 0x98   08499EE4 0x68
 *   0849A128 0x70   0849A1C0 0x30   0849D10C 0x60   0849E610 0x38
 *   0849E648 0x28   0849F628 0x30   084C3128 0x10
 * Which consumer each one has, since the two are NOT interchangeable in the
 * source (sub_080152C0's first parameter is declared s32 and needs a cast):
 *   sub_080152EC  08485D9C 08485DB4 08499E4C 08499EE4 0849E610 0849E648 0849F628
 *   sub_080152C0  0849A128 0849A1C0 0849D10C 084C3128 (and 0849A108, 0849E6D4)
 */
/* Wave 30, W30-D. One more of the same species, one record earlier:
 * sub_08003910 is `sub_080152EC(gUnknown_08485D8C, 0)` followed by
 * `gUnknown_0200B0B0->unk42 = 0xFFF6`. `const u8 []` for the same reason as
 * its neighbours -- sub_080152EC's first parameter is `const void *`. */
extern const u8 gUnknown_08485D8C[];
/* Wave 50, W50-G. sub_08002AB0 and sub_08002C38 both pass it as sub_0801BD00's
 * third argument, which is `void *`; nothing indexes it, so `const u8 []` plus
 * the cast at the call site is all the evidence supports. It lives in
 * data/data.s, not rodata. */
extern const u8 gUnknown_08485B52[];
extern const u8 gUnknown_08485D9C[];
extern const u8 gUnknown_08485DB4[];
extern const u8 gUnknown_08499E4C[];
extern const u8 gUnknown_08499EE4[];
extern const u8 gUnknown_0849A128[];
extern const u8 gUnknown_0849A1C0[];
extern const u8 gUnknown_0849D10C[];
extern const u8 gUnknown_0849E610[];
extern const u8 gUnknown_0849E648[];
extern const u8 gUnknown_0849F628[];
extern const u8 gUnknown_084C3128[];
/* Two more of the same blobs, but reached from the REMOVER side: sub_0803A59C
 * and sub_08049FF4 stop each of these pairs with two back-to-back
 * sub_0801537C(const void *) calls. 0849E240 also appears at sub_08014C94 and
 * three times around sub_0803A94C, and 084C3814 again at sub_0804A09C, so both
 * have starters elsewhere in the ROM. Same `const u8 []` model; 0849E240 and
 * 0849E280 are 0x40 each, 084C3814 is 0x10 and sits immediately below the
 * already-declared gUnknown_084C3824. */
extern const u8 gUnknown_0849E240[];
extern const u8 gUnknown_0849E280[];
/* The script sub_0803A65C starts, the sibling of gUnknown_0849E280 above. */
extern const u8 gUnknown_0849E2C0[];
/* Wave 35, W35-E. The 0x0803Axxx "flag control" debug screen's tables.
 *
 * gUnknown_0849E2C8 is another gUnknown_03001470 script blob (sub_080152EC).
 *
 * gUnknown_0849E2F8 is FLAT `u16`, not an array of pairs: sub_0803AAC0 reads
 * it at `[i * 2]` (`lsls #2`) and `[i * 2 + 1]` (`lsls #1; adds #1; lsls #1`).
 * A `u16 [][2]` would have folded the odd element into a `#2` displacement,
 * which the ROM does not have. baserom holds {0, 0x49, 0x1a, 0x49, 0x34,
 * 0x49, 0x4e, 0x49, ...} -- an (x, 0x49) coordinate pair per row.
 *
 * gUnknown_0849E318 / gUnknown_0849E358 are 4-byte records read `ldrb` at +0
 * and +2 (`[i][0]` / `[i][2]` off a `lsls #2` index), subscripted by the
 * SIGNED gUnknown_0849D89C->unk09 in sub_0803A5B8.
 *
 * gUnknown_0849E398 is 0x20-byte rows (`lsls #5`) whose first `u16` is the
 * sprite id sub_0803A4A8 hands to sub_08014740.
 *
 * gUnknown_0849E5F8 holds {"OFF", "ON"} -- the two words live at 0x08090F90
 * and 0x08090F8C in baserom, immediately after this file's -fforce-addr pool
 * (see the gUnknown_08090F74 note). */
extern const u8 gUnknown_0849E2C8[];
/* NON-const, and that is load-bearing (wave 35, W35-E): sub_0803AAC0 keeps the
 * two element ADDRESSES in callee-saved registers across its first
 * sub_0801F34C and re-`ldrh`s both afterwards. A `const u16 []` lets CSE carry
 * the VALUES across the call instead, which is 4 bytes and the wrong
 * registers. */
extern u16 gUnknown_0849E2F8[];
/* STRUCTS, not `u8 [][4]`: sub_0803A5B8 reads +0 and +2 off ONE `lsls #2`
 * address (`ldrb [r2]` then `ldrb [r2,#2]`), which is the member-displacement
 * form. A flat `[i][2]` folds the +2 into the address constant instead and
 * costs two extra instructions. (Wave 35, W35-E) */
struct Unk0849E318
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 filler_01[0x01];
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 filler_03[0x01];
};
extern const struct Unk0849E318 gUnknown_0849E318[];
extern const struct Unk0849E318 gUnknown_0849E358[];
extern const u16 gUnknown_0849E398[][0x10];
extern const char *const gUnknown_0849E5F8[];
/* Wave 35, W35-E: a table of `void *` blobs sub_0803A07C hands to
 * sub_0801BD00 as its third argument, indexed
 * `[unit->unk00 * 15 + sub_08042DE0(...) - 1]`. */
extern void *gUnknown_0849DC18[];
/* Wave 35, W35-E: a plain byte table sub_0803A07C subscripts with
 * gUnknown_0849D89C->unk04->unk00 and passes to sub_0801F34C as a sprite id. */
extern const u8 gUnknown_0849E224[];
/* Wave 35, W35-E: the tileset blob sub_0803A338 / sub_0803A4A8 hand to
 * sub_08071948 with the size 0x8360 as the fifth argument. */
extern const u8 gUnknown_080D4228[];
/* Wave 35, W35-E: SIGNED. sub_0803A4A8 loads it `ldrb` and immediately
 * re-extends with `lsls #0x18; asrs #0x18` before `- 1`, which is the signed
 * spelling; baserom holds 0xFF sentinels between the runs
 * (05 02 01 FF 07 03 04 FF ...), so -1 is a real value in the table. */
extern const s8 gUnknown_081BA068[];
/* Wave 35, W35-E: the descriptor sub_0803CE28 hands to sub_0803CFA4 by
 * ADDRESS (no deref), so it is data and not a pool word. */
extern const u8 gUnknown_0809113C[];
/* The two string literals inside the 0x08090F74 pool block, declared as
 * objects rather than written as `"FLAG CONTROL"` / `"FLAG"` in the C. Both
 * spellings emit the identical .rodata; the literal one costs 2 bytes because
 * agbcc relocates the pool words against the SECTION with addends 0x00/0x10
 * where the ROM relocates against a symbol at each string with addend 0.
 * Wave 35, W35-E. */
extern const char gUnknown_08090F94[];
extern const char gUnknown_08090FA4[];
/* WAVE 35, W35-E -- AUDITED -fforce-addr POOL WORDS, DO NOT DECLARE AS
 * OBJECTS. 0x08090F74..0x08090FAB is agbcc's own `.rodata` address-constant
 * pool for the 0x0803Axxx debug screen, and baserom proves it word by word:
 *
 *   0x08090F74 -> 0x0849D89C   &gUnknown_0849D89C
 *   0x08090F78 -> 0x03002EE0   &gpKeySt
 *   0x08090F7C -> 0x0849D89C   &gUnknown_0849D89C   (a second private copy)
 *   0x08090F80 -> 0x08499578   &gUnknown_08499578
 *   0x08090F84 -> 0x08499590   &gUnknown_08499590
 *   0x08090F88 -> 0x0849D89C   &gUnknown_0849D89C   (a third private copy)
 *   0x08090F8C -> "ON"         0x08090F90 -> "OFF"
 *   0x08090F94 -> "FLAG CONTROL"                    0x08090FA4 -> "FLAG"
 *   0x08090FAC -> 0x03002EE0   &gpKeySt
 *
 * THE SAME ADDRESS APPEARING AT THREE SCATTERED WORDS IS THE TELL (the brief's
 * criterion (a)); the `ldr =sym; ldr [sym]` shape is NOT, because a real ROM
 * pointer variable emits the identical two loads -- gUnknown_0849D89C itself,
 * one level further in, is exactly such a variable. The honest spelling for
 * every one of these is the named object, and the string words are ordinary
 * string literals. */
/* WAVE 50, W50-B. The run above starts one word LOWER than wave 35 recorded.
 * Dereferenced in baserom.gba:
 *   0x08090F70 -> 0x0849D89C   &gUnknown_0849D89C   (a FOURTH private copy)
 * (0x08090F6C -> 0x030043F8 was already noted at wave 34, W34-H, so the pool
 * block is 0x08090F6C..0x08090FAB.) sub_0803A190 reaches the record ONLY
 * through 0x08090F70, so the honest spelling there is
 * `gUnknown_0849D89C->unk08`; do NOT declare gUnknown_08090F70. */

/* WAVE 50, W50-B -- the 0x0803Axxx "BACKUP UTL" / "EDIT" debug screens.
 * Every word below was dereferenced in baserom.gba before being declared.
 *
 * REAL OBJECTS (declared here):
 *   0x08090FB0  9 `const char *` -- sub_0803AD48's row labels. INDEXED by a
 *               computed subscript BEFORE the deref (`lsls #2; adds base; ldr`),
 *               which is the inverse test for a real table. A tenth word would
 *               be 0x4c462020 ("  FL"), i.e. string data, so it is exactly 9 --
 *               and 9 is also the trip count of both loops that walk it.
 *   0x08091038  9 u16 -- per-row maxima (1,1,1,1,0x270f,0x270f,1,1,1)
 *   0x0809104A  9 u16 -- per-row steps  (1,1,1,1,0x64,  0x64,  1,1,1)
 *   0x0809105C  "EDIT"      0x08091064  "//"      0x080910D4  "BACKUP UTL"
 *   0x0809106C  3 `const char *` -> the "  FLASH..." strings at 0x080910B8 /
 *               0x08091098 / 0x08091078 (the table is in reverse address order)
 *
 * -fforce-addr POOL WORDS -- NOT declared, they hold another ADDRESS:
 *   0x08091068 -> 0x03002EE0   &gpKeySt              (sub_0803AD48)
 *   0x080910E0 -> 0x0809106C   &gUnknown_0809106C    (the table above)
 *   0x080910E4 -> 0x03002EE0   &gpKeySt              (sub_0803AFA0)
 * Declaring a gUnknown_03002EE0 for the &gpKeySt pair would not link --
 * aw2bhr.lds already binds 0x002EE0 as gpKeySt. Write `gpKeySt->held` and let
 * agbcc emit the word into the unit's own .rodata. */
extern const char *const gUnknown_08090FB0[];
extern const u16 gUnknown_08091038[];
extern const u16 gUnknown_0809104A[];
extern const char gUnknown_0809105C[];
extern const char gUnknown_08091064[];
extern const char *const gUnknown_0809106C[];
extern const char gUnknown_080910D4[];
/* Wave 29, W29-B. A REAL ROM POINTER WORD, not a -fforce-addr address constant:
 * it holds 0x0200FC50 in baserom.gba, and THREE separate functions
 * (sub_0803A65C, sub_0803A9C8, sub_0803AA78) reach the same 0x0849D89C, where
 * -fforce-addr gives every function its own private copy. Every use is the
 * two-level `ldr rN,=gUnknown_0849D89C; ldr rM,[rN]`, and the reloads fall out
 * of ordinary aliasing -- sub_0803A9C8 re-reads it after its own `strb` through
 * the pointer, sub_0803AA78 does not re-read across a plain word store.
 *
 * The record itself: unk00 is a mode byte (sub_0803A9C8 stores 0x80,
 * sub_0803A190/sub_0803A2BC consume it), unk02/unk03 are the x/y
 * sub_0803A65C hands to sub_0801A444, unk04 is the live
 * struct UnitRecord, unk08 takes the low byte of gUnknown_030033EC, and
 * unk09/unk0a are a pair sub_0803A65C resets to 0 / 0xff. Nothing bounds the
 * record past +0x0a. */
struct Unk0849D89C
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 filler_01[0x01];
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 unk03;
    /* 0x04 */ struct UnitRecord *unk04;
    /* 0x08 */ u8 unk08;
    /* 0x09 */ s8 unk09;  /* SIGNED (wave 35, W35-E): sub_0803A5B8 loads it
                           * `movs r4,#9; ldrsb r4,[r5,r4]` -- the register-offset
                           * form gcc is forced into because `ldrsb` has no
                           * immediate-displacement encoding, and nothing but a
                           * declared `s8` produces it. It is also consistent
                           * with the wave-29 note below: sub_0803A65C resets
                           * unk09/unk0a to 0 / 0xff, i.e. to 0 / -1. */
    /* 0x0a */ u8 unk0a;  /* WAVE 50, W50-B: the evidence says this is SIGNED,
                           * but I have LEFT IT ALONE -- retyping a member a
                           * dozen promoted files share is the brief's standing
                           * prohibition. sub_0803A69C compares it with unk09
                           * using `movs r1,#0xa; ldrsb r1,[r2,r1]` against
                           * `movs r0,#9; ldrsb r0,[r2,r0]` -- BOTH sides
                           * sign-extended, which a declared u8 cannot produce
                           * on the left. It also agrees with the wave-35 note
                           * on unk09 above, which already reads the
                           * sub_0803A65C reset of unk09/unk0a as "0 / -1".
                           * c_0803A69C.c therefore spells its own access
                           * `(s8)gUnknown_0849D89C->unk0a`, which is
                           * byte-identical to a declared s8 (sign_extend of a
                           * QImode mem folds to ldrsb either way) and costs
                           * every other reader nothing. If a later wave wants
                           * the member itself retyped, the only writer to
                           * check is c_0803A65C.c's `unk0a = 0xff`, which is
                           * byte-identical under both spellings. */
};
extern struct Unk0849D89C *gUnknown_0849D89C;
/* Wave 43, W43-I. A table of byte-script pointers selected by
 * `gUnknown_0849D89C->unk04->unk00`: sub_0803AB3C scales that u8 index by 4
 * (`lsls r0,r0,#2`) and `ldr`s the element, then walks the result with bare
 * `ldrb`s -- 0x06 is a row break and 0x07 terminates -- so an array of `u8 *`
 * and not an aggregate. Left non-const: it is read once, outside the loop, so
 * nothing here discriminates const from non-const. */
extern u8 *gUnknown_084997C8[];
/* The struct UnitRecord sub_0803A9C8 / sub_0803AA78 initialise with
 * sub_08025BE0 (whose first parameter already carries that type) and then park
 * in gUnknown_0849D89C->unk04. Wave 29, W29-B. */
extern struct UnitRecord gUnknown_03004100;
extern const u8 gUnknown_084C3814[];
extern const u8 gUnknown_084C383C[];
/* FOUR PARALLEL ROM byte tables, and the spacing proves they are one family:
 * the addresses are 0x5b apart exactly (0x084C3BA6, +0x5b, +0x5b, +0x5b), so
 * each holds 0x5b entries. sub_0804A18C picks one of the four from the pair of
 * flags gUnknown_030044E0->unk5c and ->unk66 and subscripts it with its u8
 * argument, which sub_0804A64C builds as `unk20 * 15 + unk1e`. Bare `ldrb` off
 * the unscaled index, so plain byte arrays and not aggregates. (wave 34,
 * W34-I) */
extern const u8 gUnknown_084C3BA6[];
extern const u8 gUnknown_084C3C01[];
extern const u8 gUnknown_084C3C5C[];
/* Wave 34, W34-L. Five `u8 *` graphics sources per value of
 * gUnknown_030044E0->unk66's bit 0, handed to sub_080149C0 as its `u8 *`
 * fourth argument. sub_0804AAF8 indexes it `[unk67 + (unk66 & 1) * 5]`, so the
 * table is FLAT with a stride-5 row selector rather than declared `[][5]`:
 * the two-dimensional spelling computes `i * 5 + j` and lands the sum in the
 * multiply's register, where the ROM accumulates into the register holding
 * unk67. Same instruction count, opposite destination. */
extern u8 *const gUnknown_084C3B3C[];
/* Wave 34, W34-L. An (x, y) offset PAIR per gUnknown_03003F40, read as
 * `[gUnknown_03003F40 * 2]` and `[... * 2 + 1]`: the second subscript's
 * `lsls #1; adds #1; lsls #1` is a stride-2 element index, not the
 * `lsls #2; adds #2` a flat byte table would give. Both reads are `ldrb`
 * rather than `ldrh` only because sub_080428F0 adds them to the low byte of
 * gUnknown_03003100 and stores the sum with `strb`, which narrows the load --
 * the same narrowing the union's own s16 members get at that site, so the
 * byte access is not evidence of a byte type. */
extern const s16 gUnknown_0849FFF8[];
/* Wave 34, W34-L. Two Decompress sources for the same 0x060103E0 destination,
 * selected by gUnknown_030044E0->unk66 bit 0. `u8 []` and non-const to match
 * Decompress(u8 *, void *), as the other blob declarations here do. */
extern u8 gUnknown_0813593C[];
extern u8 gUnknown_081358A0[];
extern const u8 gUnknown_084C3CB7[];
/* A ROM word at 0x0812A2A8 holding a POINTER to a writable u16 that
 * sub_0804AFCC decrements by 10 and then re-reads to derive a 0..0x1f phase.
 * Non-const: the pointer VALUE is loaded again for that second read off the
 * same kept address. (wave 34, W34-I) */
/* gUnknown_0812A2A8 IS FICTIONAL and its declaration has been removed. Wave 35,
 * W35-F: the word at 0x0812A2A8 holds 0x030030A0, i.e. it is a -fforce-addr
 * address constant for the u16 gUnknown_030030A0. Same one-indirection-too-many
 * symptom as gUnknown_0812A178 above; sub_0804AFCC went 20.6% (+8) -> match. */
/* Proc scripts, each named only as a Proc_EndEach argument by a wave-12
 * two-call wrapper. 085815D0/085815E8 are ended together by sub_0806AA64 and
 * are separately started at sub_0806A7AC / sub_0806A938; 08614134, 0861418C
 * (already declared above) and 08614200 are each ended by their own wrapper
 * that then re-registers sub_080735B0 with sub_08011AAC. Sizes 0x18, 0x20,
 * 0x20, 0x20 -- 3 and 4 ProcCmd entries. */
extern const struct ProcCmd gUnknown_085815D0[];
extern const struct ProcCmd gUnknown_085815E8[];
extern const struct ProcCmd gUnknown_08614134[];
extern const struct ProcCmd gUnknown_08614200[];

/* ---- wave 13 (A1) ----
 * Two parallel 11-entry tables at 0x08485C9C and 0x08485CC8, indexed by the
 * same first argument of sub_0800272C. They are word tables -- both are read
 * with a plain `ldr` off `index << 2`, which is what pins the element size --
 * and the pair is exactly 0x2C apart, so the first one ends where the second
 * begins and 11 is the entry count, not a guess.
 *   08485C9C  25a 26a 272 27a 282 28a 292 29a 2a2 2aa 2b2
 *             OBJ tile numbers: sub_0800272C masks with 0x3FF and scales by
 *             32 to reach 0x06010000 + n*0x20, i.e. OBJ VRAM. The mask is in
 *             the source (`ldr =0x3FF; ands`), so the stored words carry more
 *             than the tile number and `u32` is the honest model.
 *   08485CC8  pointers, 0x08485B5A + 8*n for n = 0..10; entry 11 is
 *             0x08485BFA and breaks the stride, which is the second
 *             confirmation of the 11-entry extent. Each element is handed
 *             straight to sub_0801BD00's third parameter, declared `void *`
 *             in this file's counterpart -- so the element type must NOT be
 *             `const void *` or -Werror rejects the call. `void *const []`
 *             keeps the ROM honesty without breaking it. */
/* Wave 40, W40-A: the `const` came off this one, and the evidence is
 * sub_08003640. It reads gUnknown_08485C9C[i + 1] twice per iteration with a
 * `bl sub_08011E54` in between, and the ROM RELOADS it (`ldr r1,[r5]` before
 * the call, `ldm r5!,{r1}` after) instead of keeping the first value. A
 * `const` array is RTX_UNCHANGING_P, so agbcc CSEs the second read away across
 * the call and emits ONE load -- which is exactly what the const spelling
 * produced, and it is 4 bytes short. Only three functions touch this symbol
 * (sub_0800272C, sub_08002FE4, sub_08003640); sub_0800272C is promoted and was
 * re-run through trymatch after this change -- see the report for the result.
 * The array is still ROM-resident and never written; `const` was a guess about
 * the original declaration, not an observation, and this is the first site
 * that could discriminate. */
extern u32 gUnknown_08485C9C[];
extern void *const gUnknown_08485CC8[];
/* Wave 42, W42-A. sub_08001D04's search table: byte PAIRS walked two at a time
 * (`ldrb r1,[r2]` for the key, `ldrb r0,[r2,#1]` for the value, `adds r2,#2`),
 * terminated by a 0xFF key. Flat `const u8 []` rather than a struct array
 * because both loads are bare `ldrb` off one walking pointer with no member
 * offset folded into a wider access. */
extern const u8 gUnknown_084859E0[];
/* Wave 40, W40-A. A byte table walked one element per map cell by
 * sub_08004724's second pass, where each element indexes a 4-entry u16 array
 * the function has just copied to its own stack -- so every element is < 4.
 * Bare `ldrb` off a post-incremented pointer held in a callee-saved register
 * for the whole of both passes, which is what fixes it as a byte array rather
 * than a wider one. */
extern const u8 gUnknown_08486FC4[];
/* The same two-table pairing one record further along, read by sub_08002844
 * with the identical `& 0x3FF`, `<< 5`, `+ 0x06010000` idiom. Nine entries
 * each, pinned the same two ways: 0x08485D44 - 0x08485D20 == 0x24 == 9 words,
 * and 08485D44's own stride-8 run of pointers (0x08485BB2 + 8*n) stops after
 * n = 8.
 *   08485D20  25a 268 26c 270 274 278 27c 280 284
 *   08485D44  -> 08485BB2 08485BBA ... 08485BF2
 * Same `void *const []` reasoning as the pair above -- the element goes
 * straight into sub_0801BD00's `void *` third parameter. */
extern const u32 gUnknown_08485D20[];
extern void *const gUnknown_08485D44[];
/* A single 16-colour OBJ palette, applied to slot 30 by sub_08002844 on its
 * 0x19 path. `u16 []` and not the `u8 []` its 0812xxxx neighbours carry,
 * because it is handed to ApplyPaletteExt(u16 *, u32, u32) by name with no
 * arithmetic on it -- the neighbours are indexed as bytes, this one is not.
 * NOT const, for the same -Werror reason: that parameter is a plain `u16 *`. */
extern u16 gUnknown_081268D8[];

/* ---- wave 13 (A4) ----
 * NOT a global. 0x0808D6DC is inside the `.LC` address-constant block that
 * follows code.s's .text, and it holds &gUnknown_0200B0B0 -- see the
 * `-fforce-addr` section of docs/agbcc-codegen.md. The block gives itself away
 * by duplication, which is the documented screen:
 *
 *   0808D6DC 0200B0B0   0808D6E0 0200B0B0   0808D6E4 030033E4
 *   0808D6E8 03002EE0   0808D6EC 0200B0B0   ...   0808D70C..0808D718
 *   0200B0B0 four more times
 *
 * so the original source of sub_08000694 says `gUnknown_0200B0B0->unk02` and
 * the compiler put the word there.
 *
 * The gUnknown_08090CD8 `**const` workaround does NOT work here and the
 * declaration below is kept only so the next reader does not re-derive it: it
 * reproduces the ROM's CSE exactly (the const load is cached in r4 across the
 * whole body) but force-addr then fires on THIS symbol in turn and adds a
 * second `ldr` at every access -- 400 bytes against 392.
 *
 * WHY, measured (work/wave13/probes/fa*.c): force-addr's trigger is NOT "used
 * before and after a loop", it is "referenced on BOTH SIDES OF A CONTROL-FLOW
 * MERGE". `if (a) g->x = 1; g->y = 2;` fires with no loop and no call;
 * `if (a) g->x = 1; else g->y = 2;` does not, because nothing references it
 * after the join; five references in five switch arms do not either. A loop is
 * just the merge everyone happened to notice first. So the workaround can only
 * work for a function whose references are all straight-line or all inside
 * mutually-exclusive arms -- which is not this one, and not most of the 0x0800
 * block. Write the direct spelling and park. Do not add users of this
 * symbol. */
extern struct Unk0200B0B0 **const gUnknown_0808D6DC;

/* Four ROM blobs sub_08023360 loads, each typed from the callee that consumes
 * it and nothing else -- none of them is `const`, because the three consumers
 * (`Decompress(u8 *, void *)`, `sub_08012C58(void *)` and
 * `ApplyPaletteExt(u16 *, u32, u32)`) all take non-const pointers and -Werror
 * rejects the discard. gUnknown_0809175C is reached at +0 and at +0xa0 by the
 * same function, which is why it is an array rather than a scalar. */
/* Five-byte ROM records indexed by struct Unk03001470's unk20, and NOT a
 * struct: agbcc's ARM STRUCTURE_SIZE_BOUNDARY rounds every struct up to a
 * multiple of 4, so a five-member byte struct strides by 8 (`lsls #3`) where
 * the ROM strides by 5 (`lsls #2; adds; adds #k`). It has to be a flat byte
 * array indexed `[i * 5 + k]`.
 * The elements are u8 with the two signed READS spelled as casts: the ROM
 * does `ldrb` followed by `lsls #0x18; asrs #0x18` at +1 and +2, and an s8
 * element would have emitted `ldrsb` with a register offset instead. +3 and +4
 * are only ever copied byte-to-byte. */
extern const u8 gUnknown_0849A06C[];
/* Wave 38 (W38-J). Three-halfword ROM records, read by sub_08029D3C as
 * [i * 3 + k] for k = 0, 1, 2 with i counting 0..3. Flat, not a struct, for the
 * same STRUCTURE_SIZE_BOUNDARY reason as gUnknown_0849A06C above: a
 * three-halfword struct rounds to 8 bytes and would stride by `lsls #3`, where
 * the ROM strides by 6 (`lsls #1; adds; ... lsls #1`).
 * u16 elements. +1 and +2 are plain `ldrh` and are added to
 * gUnknown_03003100.pos.unk00 / .unk02 before being handed to
 * sub_0804209C(s16, s16); +0 is `movs r3,#0; ldrsh r1,[r4,r3]`, which on a u16
 * element is an explicit (s16) cast at the read -- wave 37 established that
 * sign_extend of a HImode load always folds to `ldrsh`, so the `ldrsh` says
 * nothing about the declared signedness. Signedness of +1 and +2 unproved;
 * u16 is the weakest model that produces the bare `ldrh`. */
extern const u16 gUnknown_0849A0D8[];
/* Two OAM sprite blobs handed to PutSprite / PutSpriteExt, whose fourth
 * parameter is `u16 *`. 0x0849B6D6 is halfword- but not word-aligned, which
 * independently fixes the element width. */
extern u16 gUnknown_0849B6C8[];
extern u16 gUnknown_0849B6D6[];
/* Two more OAM sprite blobs on the same `u16 *` PutSpriteExt parameter, used
 * as a pair by sub_08076494 and its twin sub_0807662C. Leading halfword is the
 * OBJ count (3 and 1) followed by that many (y, x, tile) triples, which is the
 * gUnknown_0849B6C8 shape and is why the count is not part of the type. */
extern u16 gUnknown_086144C0[];
extern u16 gUnknown_086144D4[];
/* Two parallel ROM pointer tables selected by sub_0802B91C with a ternary and
 * indexed `[(s16)a4 - 1]`; each element is handed straight to sub_0801BD00's
 * `void *` third parameter, so the element type must not be `const void *`
 * (same reasoning as gUnknown_08485CC8). */
extern void *const gUnknown_0849A218[];
extern void *const gUnknown_0849A22C[];
/* sub_08027DD8's pair: gUnknown_08499E18 is the proc script it hands to
 * Proc_EndEach (and that sub_08027B10 starts), gUnknown_08499D90 is a
 * gUnknown_03001470 script blob passed to sub_0801537C(const void *) -- the
 * same `const u8 []` model as gUnknown_0849A3C0. */
extern const struct ProcCmd gUnknown_08499E18[];
extern const u8 gUnknown_08499D90[];
/* A ROM pointer table indexed by struct Unk08580934_Obj's unk1c -- the same
 * 4-entry slot index that reaches unk44[] and unk70[]. sub_08066470 is its
 * only reader and hands the element straight to sub_0801BD00's third
 * parameter, so the element type must NOT be `const void *` or -Werror
 * rejects the call: exactly the gUnknown_08485CC8 / gUnknown_0849A218 model,
 * and `void *const []` keeps the ROM honesty without breaking it. */
extern void *const gUnknown_08580CFC[];
/* The OAM sprite blob the gUnknown_08499E18 proc's two tick functions
 * (sub_08027B68, sub_08027CC8) hand to PutSpriteExt's `u16 *` fourth
 * parameter -- the same model as gUnknown_0849B6C8. Those are its only two
 * readers, so nothing constrains it beyond that parameter; it is not `const`
 * because PutSpriteExt's parameter is not. It sits 8 bytes below
 * gUnknown_08499E18, so the blob is at most 4 halfwords. */
extern u16 gUnknown_08499E10[];
/* The sub_080339B0 screen-init batch. Each is typed from its one consumer and
 * nothing else: ApplyPaletteExt(u16 *, ...) for the palettes, Decompress and
 * sub_08011C68 for the blobs, sub_08073304's `void *` for the EWRAM buffer.
 * None is `const` -- Decompress, sub_08012C58 and ApplyPaletteExt all take
 * non-const pointers and -Werror rejects the discard.
 * gUnknown_0849BC3E is a u16 lookup indexed by a proc byte field
 * (`ldrb; lsls #1; adds; ldrh`), extent unproved. */
extern u8 gUnknown_02010C50[];
extern u16 gUnknown_0809165C[];
extern u8 gUnknown_0812B49C[];
extern u8 gUnknown_0812B61C[];
extern u8 gUnknown_0812B6FC[];
extern u16 gUnknown_081320AC[];
/* Wave 33, W33-D. sub_08031018's three blobs.
 *   081D3E48  the `u16 *` source of FOUR sub_080135F4 calls, 0x20 bytes each
 *             into palette slots 0x300/0x320/0x340/0x360 -- so a palette, and
 *             non-const because that wrapper takes a plain pointer.
 *   081D3810  `Decompress(u8 *, void *)`'s source, into 0x060114A0.
 *   0849B0A0  an ApplyPaletteExt `u16 *` source, 0x20 bytes into slot 0xe0.
 * Nothing indexes any of the three; only the address is used. */
extern u16 gUnknown_081D3E48[];
extern u8 gUnknown_081D3810[];
extern u16 gUnknown_0849B0A0[];
extern u16 gUnknown_081D3E88[];
extern u8 gUnknown_081D8A54[];
/* Wave 30, W30-A: sub_08032D70's `Decompress(gUnknown_081D3C34,
 * (void *)0x06015780)` -- LZ77-compressed tile data, the same destination
 * sub_080339B0's Decompress of gUnknown_081D8A54 uses. */
extern u8 gUnknown_081D3C34[];
extern u16 gUnknown_081D92B8[];
extern u8 gUnknown_08239FA4[];
extern u8 gUnknown_0823A3D4[];
extern u16 gUnknown_0849BC3E[];
extern u8 gUnknown_085802F0[];
/* Wave 51, W51-M. sub_08088044's sub_08073304 blob, i.e. exactly the slot
 * gUnknown_085802F0 fills for sub_080339B0 -- so `u8 []` for the same reason:
 * only the symbol address is used and sub_08073304's parameter is
 * `const void *`. */
extern u8 gUnknown_085802C0[];
extern const struct ProcCmd gUnknown_0849BB68[];
extern const struct ProcCmd gUnknown_0849BB80[];
extern const struct ProcCmd gUnknown_0849BC50[];
extern const struct ProcCmd gUnknown_08616EFC[];
/* A ROM table of proc scripts selected by gUnknown_0849B060->unk0d:
 * sub_08034208 does `lsls #2; adds; ldr` off the symbol and hands the loaded
 * word to Proc_StartBlocking, so the elements are `const struct ProcCmd *`. */
extern const struct ProcCmd *const gUnknown_0849BC44[];
/* 0x0849B650, 0x20 bytes -- a 16-entry u16 ROM table of y offsets, driving the
 * four steppers sub_08032734 / sub_08032788 / sub_080327FC / sub_08032850.
 *
 * STRIDE EVIDENCE: read as `ldrh r1,[base + unk0a*2]` (`lsls r1,r1,#1; adds
 * r1,r1,r0; ldrh r0,[r1]`), so u16 elements. EXTENT: data-0848B688.s gives the
 * symbol 0x20 bytes before gUnknown_0849B670, i.e. 16 elements, and the two
 * constant-index reads bracket exactly that range -- sub_080327FC folds
 * `unk0a = 0` into `ldrh r0,[r0]` (element 0) and sub_08032734 folds
 * `unk0a = 15` into `ldrh r0,[r0,#0x1e]` (element 15). sub_08032850 breaks at
 * unk0a == 15 and sub_08032788 at unk0a == 0, which agrees.
 *
 * UNSIGNED is NOT proved: every read is `ldrh` and the value is either negated
 * or stored straight into the u16 unk0e, so an s16 declaration would be
 * byte-identical at all four readers. u16 is the weakest model that fits. */
extern u16 gUnknown_0849B650[];
/* Wave 34, W34-C. The rotating 2-sprite ring sub_08032340 and sub_08033800
 * drive: a FLAT s16 array of interleaved x/y pairs, read as `[n*2]` and
 * `[n*2+1]` with n a 5-bit counter (`(n + 1) & 0x1f`), so at least 64 entries.
 *
 * FLAT and not a 2-member struct: the y read is `lsls #1; adds #1; lsls #1`,
 * i.e. `(2n + 1) * 2`, where `tab[n].y` on a 4-byte record would have folded the
 * +2 into the load displacement as `(n << 2) + 2`. Both reads are `ldrsh`, so
 * SIGNED. */
extern const s16 gUnknown_0849B108[];
/* Wave 34, W34-C. A ROM byte table sub_08032A00 indexes with
 * `(gUnknown_03004008 >> 3) & 3` -- a 4-entry animation bob, used once as
 * `0x58 - t[i]` and once as `t[i] + 0xd8`. Plain `ldrb`; nothing signs it. */
extern const u8 gUnknown_0849B0C0[];
/* Wave 34, W34-C. PutSpriteExt's `u16 *` fourth argument, picked out of this
 * word table by the 0..2 cursor sub_08034130 steps (sub_08033FFC and
 * sub_0803405C both read it as `gUnknown_0849BC18[proc->unk36]`). */
extern u16 *const gUnknown_0849BC18[];
/* Wave 50, W50-J. The SECOND row of the same table family, three words at
 * 0x0849BC24 sitting exactly between gUnknown_0849BC18[3] and
 * gUnknown_0849BC30. sub_08033B3C reads it as `gUnknown_0849BC24[i]` for the
 * 0..2 loop counter -- `ldr r0,=sym; add r0,rI*4; ldr r3,[r0]` -- and hands
 * the loaded word straight to PutSpriteExt's `u16 *` fourth parameter with no
 * arithmetic, the same model as gUnknown_0849BC18. The two-level
 * gUnknown_0849BC30 almost certainly holds &gUnknown_0849BC18 and
 * &gUnknown_0849BC24 as its rows, which is why its element type is
 * `u16 *const *`; not proved, so nothing here depends on it. */
extern u16 *const gUnknown_0849BC24[];
/* Wave 43 (W43-C). A TWO-level word table: sub_08033EC8 and sub_08033F1C both
 * read it as `gUnknown_0849BC30[proc->unk30[i]][i]` -- one `ldr` scaled by the
 * 0..2 cursor byte, then a second `ldr` scaled by the loop counter -- and hand
 * the final word to PutSpriteExt's `u16 *` fourth parameter. Same destination
 * as gUnknown_0849BC18 above, one indirection deeper. */
extern u16 *const *const gUnknown_0849BC30[];
/* Wave 34, W34-C. Only byte 2 is ever read, by sub_08033FFC and sub_0803405C,
 * both feeding it to PutSpriteExt's third (OAM x|flags) word -- sub_0803405C
 * ORs 0x100 into it and sub_08033FFC does not. Plain `ldrb`, so the width is
 * fixed but nothing else about the object is; it runs to gUnknown_0849BC3E.
 *   Wave 43 (W43-C): the object is really THREE u16s, not opaque bytes.
 * sub_08033F1C reads halfwords at +0, +2 and +4 (`ldrh` at base + i*2 for
 * i = 0..2, plus a second `ldrh [base, #2]`), which is the whole 6-byte extent
 * this comment already deduced. The `u8` spelling is KEPT rather than widened:
 * the promoted c_08033FFC.c depends on `gUnknown_0849BC38[2]` being a `ldrb`
 * of byte 2, and retyping the array to u16 would turn that into `ldrh` of
 * byte 4 and regress two matched functions. The u8 readers are reading the low
 * halves of the halfwords (little-endian), so both views are consistent;
 * c_08033F1C.c casts at its own use with `((const u16 *)gUnknown_0849BC38)[i]`
 * instead, per the "evidence about YOUR access, not about the object" rule. */
extern const u8 gUnknown_0849BC38[];
/* Wave 34, W34-C. sub_08034A7C indexes it by its own second argument and uses
 * the loaded word to index gUnknown_08610A38[], i.e. a table of string ids. */
extern const int gUnknown_08499CCC[];
/* A ROM word holding the base of the stride-8 record array gUnknown_03003338
 * and gUnknown_03003F20 point into; sub_0803486C copies element 0 into both. */
extern struct Unk03003338 *const gUnknown_0849FE74[];
/* 20-byte ROM records; only +0x10 is ever named, and always as `unk10 * 10`
 * (sub_08036F68, sub_080249EC and sub_0802AB70 all do the same `lsls #2;
 * adds; lsls #1` on the loaded word). Word-sized -- the load is `ldr` -- and
 * the index is a plain u8 unit id in every user. */
struct Unk085D583C /* 0x14 */
{
    /* 0x00 */ u8 *unk00;  /* Wave 36, W36-J. sub_08046E48 hands the word at
                            * +0x00 straight to `Decompress(u8 *, void *)` and
                            * the word at +0x04 straight to
                            * `ApplyPaletteExt(u16 *, u32, u16)`, with no
                            * arithmetic between the `ldr` and the `bl` -- so
                            * each member takes its callee's parameter type. */
    /* 0x04 */ u16 *unk04;
    /* 0x08 */ u16 *unk08;  /* Wave 49, W49-J. Carved out of filler_08[0x06];
                             * start offset unchanged, so byte-neutral for every
                             * other reader (nothing in src/decomp names
                             * filler_08 of this tag). A whole WORD -- `ldr` --
                             * that sub_08046A84 hands straight to
                             * sub_0801C7DC's `const u16 *` first parameter with
                             * no arithmetic between the load and the `bl`, the
                             * same reasoning W36-J used for unk00/unk04 above.
                             * sub_08046A84 reaches it through all THREE tables
                             * of this record type (gUnknown_085D583C,
                             * gUnknown_0849982C, gUnknown_084998A4), which is
                             * what makes it a member and not a one-table
                             * accident. */
    /* 0x0c */ u16 unk0c;   /* Wave 49, W49-J. The remaining two bytes of the old
                             * filler_08. A HALFWORD (`ldrh [.,#0xc]`) used by
                             * sub_08046914 as a word subscript into
                             * gUnknown_08610A38[] -- `lsls #2; adds` off that
                             * table's base, the same role struct
                             * CoModeData.unk00 plays. */
    /* 0x0e */ u16 unk0e; /* wave 13 (A2): sub_08046D30 reads it `ldrh [.,#0xe]`
                           * and passes it as sub_08014668's tile argument */
    /* 0x10 */ int unk10;
};
extern const struct Unk085D583C gUnknown_085D583C[];
/* An EWRAM buffer whose ADDRESS is published: sub_08036F68 clears its byte 1
 * and then stores &gUnknown_02027F68 into both slots of gUnknown_03004528. It
 * is the array spelling and not a scalar because that is what makes the byte
 * write and the address publication share one pool word. */
extern u8 gUnknown_02027F68[];
extern u8 *gUnknown_03004528[];
extern u16 gUnknown_03004520;
extern u8 gUnknown_0809175C[];
extern u16 gUnknown_0809163C[];
extern u8 gUnknown_080BD1EC[];
/* Wave 50, W50-G. The 2x2 tile template table behind sub_08023A4C and
 * sub_08023BAC: both scale the index by 8 (`lsls #3`) and then read four
 * halfwords from FOUR SEPARATE address constants -- base, base+2, base+4 and
 * base+6 (the last is what the splitter named gUnknown_080BFBCA) -- rather
 * than one base with #0/#2/#4/#6 displacements. That is the 2-D array
 * spelling: with a constant second subscript agbcc folds `base + j*2` into the
 * address constant and adds only the row offset, where a struct array would
 * have kept one base and used load displacements. */
extern const u16 gUnknown_080BFBC4[][4];
extern u8 gUnknown_0849D16C[];
/* Three more sub_0806B708 screen-setup blobs: 0822DE80 is a u16 palette
 * (ApplyPaletteExt at 0x200/0x20, i.e. OBJ palette 0), 081918A4 a Decompress
 * source, and 0858193C a proc script handed to Proc_Start with the caller's
 * own proc as parent. */
extern u16 gUnknown_0822DE80[];
extern u8 gUnknown_081918A4[];
extern const struct ProcCmd gUnknown_0858193C[];

/* ---- wave 13 (A2) ---- */
/* The link-session descriptor sub_0802EA5C is handed as its only argument and
 * parks here. Members seen so far: +0x00 a word copied straight into
 * gUnknown_03000564, +0x04 the halfword sub_0802EB28 complements into
 * REG_SIODATA8, +0x06 a halfword timeout defaulted to 10 by sub_0802EA5C,
 * +0x08 whose low two bits become gUnknown_03000560, and +0x0a a halfword
 * sub_0802EF10 tests before arming timer 3. */
struct Unk03003F6C /* 0x0c */
{
    /* 0x00 */ u32 unk00;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
};
extern struct Unk03003F6C *gUnknown_03003F6C;
/* The link handshake's result code, and the fifth member of the serial block
 * above. VOLATILE, and this one is proved rather than inferred: sub_0802EB28
 * stores `v & 4` and then re-`ldr`s the very same address for the `!= 0` test
 * with nothing in between. `int` and not u32 because sub_0802EB28 returns it
 * from the same expression that returns -1 elsewhere. Values seen: 0xF0
 * (sub_0802EA5C's initial value), 0, and a 0..3 slot index. */
extern volatile int gUnknown_0300055C;
/* A word flag in EWRAM cleared by sub_0802EA5C and by sub_0802EB28's case 0,
 * and tested `!= 0` by case 1 to decide whether the link is up. */
/* Wave 49 (W49-B) ADDS the volatile. sub_080303C8 spins on it as
 * `while (gUnknown_02023894 == 0 && gUnknown_0849B018->unk04 != 2);` -- the
 * "wait for the link interrupt" gate -- and the ROM re-loads it from memory on
 * every iteration. Without the qualifier agbcc hoists the load into the
 * preheader and the loop can never terminate, so this is a CORRECTNESS fact and
 * not only a codegen one.
 *   Worth recording because the obvious hypothesis is wrong: the loop already
 * contains a volatile read (unk04), and that does NOT stop gcc 2.9 hoisting an
 * unqualified load beside it. Measured, not assumed.
 *   Byte-neutral for the two existing users (c_0802EA5C.c, c_0802EB28.c), both
 * re-verified as matches after the edit. */
extern volatile int gUnknown_02023894;
/* NOT a global -- this is agbcc's own `-fforce-addr` address-constant pool
 * word, and the ROM proves it: the word at 0x08090D88 is 0x030032D8, i.e.
 * &gUnknown_030032D8, and 0x08090D84 / 0x08090D8C hold &gUnknown_030044B0 and
 * &gUnknown_03003FC0 beside it -- the duplication-free but obviously-a-pool
 * run the docs describe. sub_080345C8's source says plain `gUnknown_030032D8`;
 * the compiler parked its address here because the address pseudo is live
 * across the switch's calls, and every OTHER function in the ROM that touches
 * 0x030032D8 (sub_08034ED0, sub_08034C90, ...) names the symbol directly
 * because there the address dies inside one statement.
 *
 * Declaring it and reading `**state` off a local `u16 *const *` is the
 * gUnknown_0808D6DC workaround: writing the honest `gUnknown_030032D8` gives
 * byte-identical .text but relocates the pool word against THIS unit's own
 * `.rodata`, which the .text-only split cannot place at 0x08090D88. The local
 * is load-bearing -- taking the address into a local pseudo is what stops
 * -fforce-addr from adding a second .rodata level of its own. */
extern u16 *const gUnknown_08090D88;
/* Its neighbour at 0x08090D84 is the same thing for gUnknown_030044B0: the ROM
 * word there is 0x030044B0. sub_080344F0 is the one function that reaches the
 * command block this way rather than by name -- it relocates its .text pool
 * word against gUnknown_08090D84 itself, so the honest `gUnknown_030044B0`
 * spelling is NOT what the original wrote here (it relocates against this
 * unit's own .rodata instead, which is size-exact but a different relocation).
 * Take the address into a local, exactly as the gUnknown_08090D88 note above
 * requires. Wave 25. */
extern u8 *const gUnknown_08090D84;
/* Wave 43 (W43-C). 0x08090D7C is the same thing again, one word further down
 * the same pool run: the ROM word holds 0x0849BC38, i.e. &gUnknown_0849BC38,
 * and its one referrer is sub_08033EC8.
 *
 * THIS IS AN EXCEPTION TO THE "do not add more of them" RULE BELOW, and the
 * measurement is the reason. That rule rests on the premise that naming the
 * honest symbol reproduces the original exactly and merely relocates against
 * the unit's own .rodata -- true for 0x08090C04, and true here for this
 * function's SIBLINGS: sub_08033F1C (0x08090D80) and sub_080346FC
 * (0x08090D8C) both matched in wave 43 from the honest spelling, with
 * trymatch reporting `relocs: name different symbols that resolve to the same
 * address`. For sub_08033EC8 the premise FAILS. Writing plain
 * `gUnknown_0849BC38[i * 2]` hoists the address into a callee-saved register
 * for the whole loop and emits NO pool word at all, coming out 4 bytes SHORT
 * (80 vs 84, missing the in-loop `ldr r1, [r7]`) -- not a relocation
 * difference but two fewer instructions. Every argument-ordering and
 * local-binding spelling tried reproduced the instruction order exactly and
 * still folded the address into a register.
 *
 * The difference from the siblings is register pressure: sub_08033F1C's loop
 * body carries enough live values (r8, r9, sl are all in use) that agbcc
 * spills an address constant to .rodata on its own, and sub_08033EC8's
 * straight-line loop body does not. So bind `&gUnknown_08090D7C` to a local
 * `const u8 *const *` and read `(*tbl)[...]`, exactly as c_080345C8.c does
 * for gUnknown_08090D88 -- the local is what stops -fforce-addr adding a
 * second .rodata level of its own. */
extern const u8 *const gUnknown_08090D7C;
/* 0x08090C04 is another word in that same pool run and is NOT a global either:
 * the ROM word there is 0x030033E8, i.e. &gUnknown_030033E8, and its immediate
 * neighbours are &gUnknown_03003FC0 (0x08090C00) and &gUnknown_030033EC
 * (0x08090BFC / 0x08090C0C). Its one referrer, sub_0802CFFC, reads
 * gUnknown_030033E8[0] and [1] on both sides of a branch, so the ADDRESS is
 * live across the merge -- the reference-count-across-a-control-flow-merge
 * trigger, not loop liveness.
 *
 * NO DECLARATION IS NEEDED AND NONE IS ADDED. The paragraph above says the
 * honest spelling "relocates the pool word against THIS unit's own .rodata,
 * which the .text-only split cannot place" -- that wall was removed in wave 18
 * and the sentence is now stale for anything new. Naming gUnknown_030033E8
 * directly reproduces sub_0802CFFC exactly, including the `adds r2, r0, #0` /
 * `ldr r2, [r2]` pair that holds the pool word's address across the branch;
 * trymatch reports `relocs: name different symbols that resolve to the same
 * address` and asks for "rodata": ["0x08090C04"] in the promoted entry. That is
 * a match, not a park. The gUnknown_08090D88 declaration is kept only because a
 * promoted file already uses it -- do not add more of them (wave 21, W21-C). */
/* Wave 39, W39-G: 0x08090C28 and 0x08090C2C are two more words in that run and
 * are NOT globals either. BOTH hold 0x03003F68, i.e. &gUnknown_03003F68, and
 * they are read by sub_0802E010 and sub_0802E130 respectively -- one
 * -fforce-addr copy per function, which is why one address wears two invented
 * symbol names. NO DECLARATION IS NEEDED AND NONE IS ADDED. Naming the honest
 * gUnknown_03003F68 reproduces the three-load chain (`ldr rA,=word; ldr rB,[rA];
 * ldr rB,[rB]; ldrb`) exactly, and the same function also reaches the record
 * through a plain `.4byte gUnknown_03003F68` pool word elsewhere in its own
 * body -- the two levels coexist in one function and neither is a park reason.
 * Confirm the ROM word before believing any 0x08090Cxx name: two force-addr
 * copies of one address look like two objects in data_refs and in `varies`. */
/* A proc script; sub_080345C8 asks sub_08015BD0 whether an instance is live
 * with the usual `!= -1` predicate. */
extern const struct ProcCmd gUnknown_0849A00C[];
/* Wave 34, W34-C. The blocking progress-bar proc sub_0803376C starts; its own
 * body is sub_080335CC, which is what pins the record layout (unk20 callback,
 * unk24 byte cursor, unk2a total, unk2c done, unk2f percent). */
extern const struct ProcCmd gUnknown_0849BB08[];
/* Wave 34, W34-C. Two proc scripts always named together as a PAIR:
 * sub_08033030 gates on `Proc_Find(gUnknown_0849B688) == NULL &&
 * Proc_Find(gUnknown_0849B670) == NULL` and sub_080330C0 tears both down with
 * back-to-back Proc_EndEach in the same order. */
extern const struct ProcCmd gUnknown_0849B670[];
extern const struct ProcCmd gUnknown_0849B688[];
/* Wave 34, W34-C. The EWRAM landing zone sub_08033470 decompresses a routine
 * into and then CALLS through `bl _call_via_r4` -- so the object is code, and
 * the symbol is only ever used as an address. gUnknown_0203BFFC is the word
 * immediately below it, stamped with the 0x485153CD signature the routine
 * checks; it is a whole-word `str`, so u32. */
extern u32 gUnknown_0203BFFC;
extern u8 gUnknown_0203C000[];
/* The next two 0x20-byte slots after gUnknown_0849A00C, and proc scripts for
 * the same reason it is -- sub_0802B3AC and sub_0802B4D4 both hand
 * gUnknown_0849A02C straight to Proc_Find, which takes `const struct ProcCmd *`
 * and is not a shape any other blob class reaches. sub_0802C4B8 /
 * sub_0802C4D4 (wave 16, F075) start them with Proc_StartBlocking. */
extern const struct ProcCmd gUnknown_0849A02C[];
extern const struct ProcCmd gUnknown_0849A04C[];
/* The animation handle sub_0801C210 allocates and returns. Only the two words
 * sub_080355CC touches are named; +0x22 is an OAM-shaped `tile | pal << 12`
 * halfword and +0x24 takes the decompression buffer. */
/* Wave 30, W30-B: filled in from the 0x0801C block's own accessors.
 * +0x00 is the animation script the handle is currently running: every one of
 * sub_0801C254 / sub_0801C4D4 / sub_0801C51C / sub_0801C754 gates on
 * `handle != NULL && handle->unk00 != NULL`, and sub_0801C640 is the function
 * that STORES it. +0x04/+0x08/+0x0c are three cursors sub_0801C640 derives
 * from that script in one go (unk08 and unk0c always get the same value).
 * +0x18/+0x1a are a halfword pair sub_0801C67C zeroes and overrides (0x100)
 * around a sub_0801C2DC call, restoring +0x1a afterwards -- a "run one frame
 * at forced speed" idiom. +0x20 bit 1 selects between the script's two
 * encodings: set = u16 self-relative offsets with the low bit used as a flag
 * (hence the `& ~1` on every one), clear = a flat table of u32 pointers.
 * +0x22 / +0x24 are unchanged from wave 13. */
/* Wave 36, W36-H: the rest of the handle, read off sub_0801C69C (the
 * initialiser, which writes every scalar below), sub_0801C27C (the drawer) and
 * sub_0801C2DC (the stepper).
 *   +0x0c walks a list of 4-byte {delay, arg} records -- `adds r0,#4` on it and
 * `ldrh [r4]` / `ldrh [r4,#2]` off it -- hence struct Unk0801C210Cmd, with
 * +0x08 the loop-back anchor (the 0xFF command restores +0x0c from it).
 *   +0x18 is s16 and not u16: sub_0801C2DC reads it BOTH ways in one basic
 * block (`ldrh r1,[r3,#0x18]` for the decrement and `movs r2,#0x18; ldrsh r0`
 * for the test), and the post-decrement test is `bgt` on a sign-extended
 * value. Every promoted store to it is a 0, so the change is byte-neutral
 * there (c_0801C67C.c, c_0803FFA0.c re-checked).
 *   +0x10/+0x14 are the two OAM pointers sub_0801C2DC resolves and sub_0801C27C
 * hands to PutSpriteExt; +0x14 doubles as the "has affine data" flag.
 *   +0x1e is the OAM layer word, +0x21 the priority nibble shifted <<9 into the
 * x|flags word, +0x28 the last arg published by a script record. */
struct Unk0801C210Cmd
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
};

struct Unk0801C210
{
    /* 0x00 */ void *unk00;
    /* 0x04 */ u8 *unk04;
    /* 0x08 */ struct Unk0801C210Cmd *unk08;
    /* 0x0c */ struct Unk0801C210Cmd *unk0c;
    /* 0x10 */ u16 *unk10;
    /* 0x14 */ u16 *unk14;
    /* 0x18 */ s16 unk18;
    /* 0x1a */ u16 unk1a;
    /* 0x1c */ u16 unk1c;
    /* 0x1e */ u16 unk1e;
    /* 0x20 */ u8 unk20;
    /* 0x21 */ u8 unk21;
    /* 0x22 */ u16 unk22;
    /* 0x24 */ void *unk24;
    /* 0x28 */ u16 unk28;
};
/* A table of 16-colour palettes, indexed by gUnknown_08499598[n].unk1a - 1 in
 * sub_080355CC. `[][16]` and not a flat `u16 []` because the ROM's index math
 * is `(v - 1) << 5` off the bare symbol -- a flat array folds the -1 into the
 * relocation's addend and loses the `subs`. Not const: ApplyPaletteExt takes a
 * plain `u16 *`.
 *   WAVE 35 (W35-A) CORRECTION: `[][16]` is NOT sufficient on its own. The
 * identically-shaped gUnknown_08104264 below, declared the same way, still
 * emits `gUnknown_08104264+-0x20` when the index is written inline as
 * `g[record.unk1a - 1]`. Two further things are needed and they are
 * independent: bind the index to its OWN STATEMENT (`i = ...unk1a - 1;` then
 * `tbl[i]`), which is what keeps the clean symbol and the `subs r0,#1`; and,
 * if the ROM loads the table's pool word BEFORE the record chain, bind the
 * table's address to a `u16 (*tbl)[16]` local. Both together matched
 * sub_08043834; neither alone does. See docs/agbcc-codegen.md. */
extern u16 gUnknown_0810EA60[][16];
/* Wave 43, W43-B. sub_080353E8's 32-entry table, 0x0c bytes per record -- the
 * ROM's `lsls #1; adds; lsls #2` is i*12, an ordinary array index on a 12-byte
 * struct and not a shift idiom. Every member either function in this block
 * touches is a halfword and UNSIGNED: the accumulate loop reads both operands
 * with `ldrh`, and the position reads use `lsrs #8`, never `asrs`.
 * unk00/unk02 are 8.8 fixed-point screen positions, unk04/unk06 the per-frame
 * step added to them (`unk00 += unk04`, `unk02 += unk06`, once per entry), and
 * unk08 indexes gUnknown_0849BDA0 in units of eight bytes. 0x0a is untouched
 * here, so it is filler rather than a measured gap. */
struct Unk02027DE8 /* 0x0c */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u8 filler_0a[0x02];
};
extern struct Unk02027DE8 gUnknown_02027DE8[];
/* Wave 43, W43-B. The rows sub_080353E8 hands to sub_0801BDB4 as its `u16 *`
 * third argument: eight bytes (four halfwords) per entry, the ROM doing
 * `ldrh unk08; lsls #3; adds base`. Declared u16[] to agree with that already
 * promoted parameter type (src/decomp/c_0801BD00.c) rather than re-derived. */
extern u16 gUnknown_0849BDA0[];
/* Wave 43, W43-A. The array immediately BELOW gUnknown_02027DE8 and identical
 * to it: 0x02027DE8 - 0x02027C68 = 0x180 = 32 records of 0x0c. Same producer/
 * consumer shape too -- sub_08035224 fills it exactly as sub_08035354 fills
 * gUnknown_02027DE8, and sub_080352B4 steps and draws it exactly as
 * sub_080353E8 does the other. Declared with W43-B's tag rather than a second
 * identical one; every member access in all four functions agrees with it. */
extern struct Unk02027DE8 gUnknown_02027C68[];
/* Wave 43, W43-A. 0x08090E40 is NOT an address word -- the ROM holds
 * 0x00E000C0 there, so the -fforce-addr run at 0x08090E30..0x08090E3C stops
 * one word short of it (0x08090E30 -> 0x08499598, E34 -> 0x03004490,
 * E38 -> 0x030033EC, E3C -> 0x03003FC0, all checked against baserom.gba).
 * A flat u16 table read `gUnknown_08090E40[j * 3 + k]` for k = 0,1,2 with
 * j = i % 16 by sub_08035224 and sub_08035354 -- sixteen groups of three
 * halfwords. FLAT and not [16][3]: both functions recompute the address for
 * each of the three reads (`adds #1; lsls #1; adds base`) rather than folding
 * k into the load displacement, which a 2-D array would do. */
extern u16 gUnknown_08090E40[];
/* Wave 43, W43-A. Two 0x60-byte ROM blobs sub_08011C68 blits to VRAM, at
 * 0x06012E00 (sub_08035224) and 0x06012E60 (sub_08035354). Passed straight
 * through to that function's `const void *` first parameter and never indexed,
 * so these two callers do not settle an element type. */
extern const u8 gUnknown_0809169C[];
extern const u8 gUnknown_080916FC[];
/* Wave 43, W43-A. The row table sub_080352B4 passes as sub_0801BDB4's third
 * argument -- the gUnknown_02027C68 counterpart of the gUnknown_0849BDA0 that
 * sub_080353E8 passes, though this caller passes the base unindexed and hands
 * the record's unk08 over separately as the fourth argument. u16[] to agree
 * with the promoted parameter type in src/decomp/c_0801BD00.c rather than
 * re-derived from this one call site. */
extern u16 gUnknown_0849BD98[];
/* Wave 43, W43-B. The proc script sub_08035850 starts on PROC_TREE_5. Its proc
 * is the same object sub_08035760, sub_08035828 and sub_080358AC drive: +0x35
 * and +0x36 are u8 state bytes, +0x42/+0x44 an s16 position pair in 1/16ths
 * (sub_08035850 stores `arg * 16` into them and sub_08035760 divides by 16 on
 * the way back out) and +0x4c a sixteen-byte copy buffer. */
extern const struct ProcCmd gUnknown_0849BDB8[];
/* The proc script sub_080355CC starts on PROC_TREE_5. */
extern const struct ProcCmd gUnknown_0849BE38[];
/* Wave 28, W28-A. Three neighbours of gUnknown_0849BE38 in the same block.
 * gUnknown_0849BD20 is the palette row table above. The other two are
 * gUnknown_03001470 list scripts of the gUnknown_0849A108 shape, reached only
 * as an address handed to sub_080152EC(script, 0) (gUnknown_0849BD38, by
 * sub_08035144 and sub_080351F0) and to sub_0801537C(script) (gUnknown_0849BDE8,
 * by sub_08035F68) -- the usual start/stop pairing, nothing dereferences
 * either. */
extern const struct Unk0849BD20 gUnknown_0849BD20[];
extern const u8 gUnknown_0849BD38[];
extern const u8 gUnknown_0849BDE8[];
/* Wave 36, W36-F. Three ROM tile blobs handed to sub_08011C68 as raw byte
 * sources at a `(tile & 0x3FF) * 0x20` displacement, so `u8[]` is what the
 * address arithmetic shows and nothing constrains the element type further.
 * gUnknown_08090EC4 is exactly 0x20 zero bytes sitting between two
 * -fforce-addr pool words (0x08090EB8 and 0x08090EE4) -- it is a blank tile
 * used to erase, NOT a pool word: sub_0803678C passes its ADDRESS and never
 * dereferences it. */
extern const u8 gUnknown_081120B0[];
extern const u8 gUnknown_081251B0[];
extern const u8 gUnknown_08090EC4[];
/* Wave 36, W36-F. sub_08037200 hands it to sub_0801BD00's `void *` slot. The
 * ROM loads its ADDRESS (`ldr r2, =sym` with no deref), so it is the object
 * itself, not a pointer variable; the content is a word script
 * (1, 0x8000, 1, 0xc000, 0x40000001, 0), not tiles. */
extern u32 gUnknown_0848B698[];
/* Wave 36, W36-F. A dispatch table of THUMB function pointers (every word is
 * odd) indexed by a proc's u8 +0x35 state and called with the proc in r0 via
 * `bl _call_via_r1` -- the r1 index is the one-argument trampoline, which is
 * what fixes the arity. Entries 0..5 are 0x080362D9/DD/E1/D1/A9/8D, i.e. the
 * sub_080362xx neighbours of this very block. */
extern void (*const gUnknown_0849BE20[])(ProcPtr);
/* Wave 36, W36-F. Two ROM blobs sub_08037DC8 installs back to back: a
 * 0x200-byte tile run copied to 0x06000020 by sub_08011C68, and the 0x20-byte
 * palette handed to ApplyPaletteExt(pal, 0x20, 0x20) right after. Both
 * dereference to real content in baserom.gba (tile bitplanes / BGR555 entries),
 * so neither is a -fforce-addr pool word. */
extern const u8 gUnknown_080A0F38[];
extern u16 gUnknown_080A1138[];
/* Wave 36, W36-F. A four-entry direction table of s16 x/y pairs, read as a flat
 * s16 array indexed `dir * 2` / `dir * 2 + 1` by sub_080362E0. Dereferenced in
 * baserom.gba at 0x0849BE10: (-1,0) (1,0) (0,1) (0,-1) -- the negative entries
 * are what make it s16 rather than u16, and the run ends at 0x0849BE20 where
 * unrelated data begins. */
extern const s16 gUnknown_0849BE10[];
/* Wave 28, W28-A. Two IWRAM objects always named as a PAIR and always by
 * address: sub_08035DF4 hands each to sub_08070610(obj, 1), and sub_08035E24 /
 * sub_08035E6C hand each to sub_08071488(obj, 1, y). Nothing in this block
 * indexes or dereferences either, so `u8 []` is the weakest model that gives
 * the clean pool word.
 *
 * WAVE 38: they are `struct MusicPlayerInfo`. Weakest-model was right while no
 * callee's body was reachable from C; W38-A then matched sub_08071420 and
 * sub_08071488 (m4a MPlayVolumeControl / MPlayPitchControl), whose bodies read
 * ident at +0x34, trackCount at +0x08 and tracks at +0x2c through this very
 * pointer. Once a callee dereferences it, weakest-model is no longer the
 * weakest model that FITS -- it is a model the evidence contradicts.
 *
 * Kept as `[]` rather than a bare object deliberately: every one of the eight C
 * call sites passes the bare name and relies on array decay, so this retype
 * changes NO call site and no bytes. W38-A's header note predicted callers
 * would "convert implicitly" -- true of `void *`, but these were declared
 * `u8 []`, which does not convert, and that is what broke the wave-38 split
 * build. Check what the callers actually PASS, not what they could pass. */
extern struct MusicPlayerInfo gUnknown_030059E0[];
extern struct MusicPlayerInfo gUnknown_03005BA0[];
/* Wave 32 (W32-C): five more of the same objects. sub_0803B414 hands all
 * SEVEN of this group to sub_08071420(obj, 0xFFFF, gUnknown_030005CC) in one
 * run, each as a bare address with no indexing, so they carry the same `u8 []`
 * model and the same reasoning as the two above. */
/* WAVE 38: retyped to `struct MusicPlayerInfo` with the two above, for the
 * reason the wave-32 note gives -- all SEVEN are one group handed to the same
 * m4a entry point, so they carry one model, and W38-A's match of sub_08071420
 * settled what that model is. Retyping only the two that happened to break the
 * build would have split one evidenced group across two types. */
extern struct MusicPlayerInfo gUnknown_03005A60[];
extern struct MusicPlayerInfo gUnknown_03005AA0[];
extern struct MusicPlayerInfo gUnknown_03005B20[];
extern struct MusicPlayerInfo gUnknown_03005BF0[];
extern struct MusicPlayerInfo gUnknown_03005C30[];
/* Wave 28, W28-A. Two more gUnknown_03001470 list blobs in the 0x08036000
 * block, each reached only as an ADDRESS: sub_08036E54 hands gUnknown_0849D1AC
 * to sub_080152EC(script, 0) and sub_08036F20 hands gUnknown_0849D34C to
 * sub_080193B0, whose parameter is already `const u8 *`. Nothing indexes or
 * dereferences either. */
extern const u8 gUnknown_0849D1AC[];
extern const u8 gUnknown_0849D34C[];
/* NOT a global -- a second `-fforce-addr` address-constant pool word, this one
 * holding &gUnknown_08499590 (the ROM word at 0x08090EA8 is 0x08499590, and
 * 0x08090EA4 beside it is the same address again, which is the duplication
 * screen the docs describe). sub_080359A4 reads the screen pointer three times
 * across two calls, so agbcc kept the address in a callee-saved register and
 * parked it here. Use it exactly like gUnknown_08090D88: bind
 * `&gUnknown_08090EA8` to a local and read `**local`. */
extern u8 **const gUnknown_08090EA8;
/* A ROM blob handed to sub_0801C70C as its first argument by sub_08040640, and
 * never dereferenced by anything matched -- only the symbol address is used.
 * `const u16 []` to match its two siblings gUnknown_0810A3E8 /
 * gUnknown_0810AFC8, which reach the same parameter from sub_0803F128; the
 * element type is a guess for all three. */
extern const u16 gUnknown_08111D94[];
/* Wave 31, W31-B. The tile blob and the palette blob sub_08040430 loads into
 * OBJ VRAM 0x06010000. Neither can be `const`: the blob goes to
 * Decompress(u8 *, void *) and the palette to ApplyPaletteExt(u16 *, u32, u16),
 * and neither of those parameters is const -- the same reasoning that fixed
 * gUnknown_081214B4 / gUnknown_0812189C below. Only the symbol address is used,
 * so the extents are unknown. */
extern u8 gUnknown_08111000[];
extern u16 gUnknown_08111D74[];
/* A byte flag cleared to 0 by sub_0802E4B4 alongside gUnknown_030040DC and
 * gUnknown_030033E8. Single `strb`, no reader found, so u8 is the weakest
 * model and the signedness is unproved. */
extern u8 gUnknown_03000558;
/* At least two bytes: sub_0802E4B4 clears [0] and [1] with two `strb`s off one
 * pool word (`strb r0,[r1]; strb r0,[r1,#1]`), which is an array or a pair of
 * adjacent byte fields, not a scalar. */
extern u8 gUnknown_030033E8[];
/* A small byte-wide mode id; sub_0802E4B4 sets [0] to 4 with a bare `strb`.
 * At least two bytes and its ADDRESS is taken: sub_0802966C fills [0] and
 * [1] from gUnknown_0849A06C and then hands the symbol itself to
 * sub_0802E7C8 and sub_080357E0 as a pointer. Was a scalar u8. */
extern u8 gUnknown_03003110[];
/* Wave 45, W45-H. sub_0805FF64 passes its ADDRESS (not a load) as the SOURCE of
 * `sub_0803442C(src, dst)` with gUnknown_03003110 as the destination, so it is a
 * buffer of the same element type as that already-typed destination. Nothing
 * matched indexes it, so the extent is unbounded and u8 is inherited from
 * sub_0803442C's declared parameter rather than proved here. */
extern u8 gUnknown_030046CC[];
/* Wave 31 (W31-C).
 *
 * gUnknown_080C1FC4 and gUnknown_080C9FC4 are two ROM tile blobs the twins
 * sub_08021D64 / sub_08021DA0 copy into BG VRAM: the first at a 0x1000 stride
 * (`lsls #0xc` on a u8 index), the second at 0xc00 (`((n*2)+n) << 0xa`), each
 * copying exactly one element. Byte arrays -- nothing indexes inside a row.
 *
 * gUnknown_08552680 is the 0x10-word block sub_08057138 CpuFastSets to
 * 0x05000340, i.e. 32 palette entries starting at palette 26.
 *
 * gUnknown_085D64A8 / 085D6688 / 085D6868 are three tables sub_08057D58
 * selects between on its second argument (2, 3, anything else) and then indexes
 * TWICE: `((a*4)+a)<<2` is a 20-byte row and `c*4` a word within it, so five
 * words per row. The element is returned by a bare `ldr` with no narrowing.
 *
 * The row is a STRUCT holding `void *[5]`, not a plain `void *[][5]`. Both
 * spell the same layout, but the two-dimensional array makes agbcc fold the
 * whole address into one expression and schedule the COLUMN scaling first,
 * where the ROM computes `row*20 + base` and only then adds `c*4`. Measured
 * this wave on sub_08057D58: identical size, two registers swapped. */
extern const u8 gUnknown_080C1FC4[];
extern const u8 gUnknown_080C9FC4[];
extern const u16 gUnknown_08552680[];
struct Unk085D64A8 /* 0x14 */
{
    /* 0x00 */ void *unk00[5];
};
extern const struct Unk085D64A8 gUnknown_085D64A8[];
extern const struct Unk085D64A8 gUnknown_085D6688[];
extern const struct Unk085D64A8 gUnknown_085D6868[];
/* Wave 51, W51-F. A CpuFastSet source descriptor: sub_0805521C and sub_0805530C
 * index these tables as `[b][c]` with a THREE-column row (`(b*9 + c*3) << 3`,
 * so a 0x18 record and a 3-wide row), read unk00 with a bare `ldrh`, and if it
 * is non-zero CpuFastSet unk04 to OBJ VRAM with `unk00 / 4` words -- so unk00
 * is a BYTE length and unk04 the ROM source. Both callers then return
 * `tileIndex + (unk00 >> 5)`, i.e. unk00/32 = a 4bpp tile count, which
 * corroborates the byte-length reading. Confirmed against baserom: unk00 is
 * always a multiple of 0x20 (0x220, 0x400, 0x520, 0x720, 0x820, 0x1020) and
 * unk04/unk08 are both 0x0854xxxx/0x0855xxxx ROM pointers. unk08 and unk0c[]
 * are unread by either function -- extent past 0x18 is unproved, but the 0x18
 * stride is exact. Real tables, not pool words: the symbol is INDEXED by a
 * computed subscript before any dereference. */
struct Unk085D70A8 /* 0x18 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ const void *unk04;
    /* 0x08 */ const void *unk08;
    /* 0x0c */ u32 unk0c[3];
};
extern const struct Unk085D70A8 gUnknown_085D70A8[][3];
extern const struct Unk085D70A8 gUnknown_085D7768[][3];
/* The currently-selected unit id: sub_0802E4B4 fills it from the map cell byte
 * at gUnknown_08499590[0x12 + rowOffset[y] + x] and immediately re-reads it to
 * index gUnknown_08499594, so it is the same 1-based unit id that table is
 * keyed by. UNSIGNED -- the `>> 6` that derives gUnknown_03004480 from it is
 * `lsrs`, and the value doubles as an index. */
extern u8 gUnknown_03003F38;
/* 0x03004770 -- Wave 30, W30-A. A word-wide state counter compared UNSIGNED
 * (`bls` / `bhi`) against 0x10 and 1 by sub_0805C1D8, which also stores 1 into
 * it. sub_0805C1D8 reaches it through agbcc's own `-fforce-addr` word at
 * 0x0816D9D4 -- the ROM word there holds 0x03004770, and its neighbours
 * 0x0816D9D0 and 0x0816D9D8 hold 0x03003F20 and 0x08499590, so the whole run
 * is one unit's `.LC` pool. sub_0805C208 names the same global directly with a
 * two-level `ldr`, which is what makes it a real global and not a pool
 * artefact. */
extern u32 gUnknown_03004770;
/* Halfword written twice in a row by sub_0802E4B4 (`(gUnknown_03003F38 >> 6)
 * + 1`, then gUnknown_030033EC), both with `strh`. */
extern u16 gUnknown_03004480;

/* ---- wave 13 (A8) ----
 * sub_0801258C is the SECOND display-state block, and the whole 824-byte
 * function is one straight line of 43 copies out of the shadows that
 * sub_08012420 pushes to the hardware and into a parallel set of symbols. So
 * every symbol below is the snapshot partner of a display-register shadow, and
 * the widths are read straight off the ldr/ldrh/ldrb pairs: nothing here is
 * inferred from address adjacency.
 *
 * Six of them are written as PACKED words rather than as two halfwords --
 * `lo | (hi << 16)` with a single `str` -- which is why some destinations are
 * u32 where their neighbours are u16, and gUnknown_030030B8 is a genuine 64-bit
 * object built with `(u64)a | ((u64)b << 16) | ((u64)c << 32) | ((u64)d << 48)`
 * (the DImode shift sequence `lsrs r5,#0x10 / lsls r4,#0x10 / orrs` is not
 * reachable from any 32-bit spelling). */
extern u16 gUnknown_0300140C;
extern u16 gUnknown_03001410;
extern u16 gUnknown_03001414;
extern u16 gUnknown_03001424;
extern u32 gUnknown_03001FB0;
extern u16 gUnknown_03001FB4;
extern u16 gUnknown_03001FB8;
extern u16 gUnknown_03001FC0;
/* gUnknown_03001FC8, gUnknown_030024E0 and gUnknown_03002004 ARE NOT DECLARED
 * HERE.  Wave 37 (W37-G) moved them to include/hardware.h as
 * `union BgCntBuf` / `union BgCntBuf` / `union DispCntBuf` -- sub_08018254
 * writes all three field by field with byte-wide read-modify-writes that a
 * scalar u16 cannot produce.  Re-adding an `extern u16` here breaks every
 * translation unit that includes both headers with `conflicting types`,
 * exactly as the gUnknown_03001FE8 note above records. */
extern u16 gUnknown_03001FC4;
extern u16 gUnknown_03001FCC;
extern u8 gUnknown_03001FD8;
/* Wave 35, W35-I: VOLATILE, and that is measured -- but the qualifier is NOT on
 * the declaration, deliberately. sub_08017880 (the HBlank raster handler) only
 * matches when this and gUnknown_030030C0 are read through a `vu16`: the
 * volatile pair forces agbcc to materialise BOTH addresses before either load,
 * which is the four-register BLDALPHA block the ROM has and the whole of the
 * residual that kept the function parked at 92.5% for two waves. See the W35-I
 * section of docs/agbcc-codegen.md. It is the HBlank shadow copy of
 * REG_BLDALPHA's high byte, beside gUnknown_030030C0 (the low byte) and
 * gUnknown_030030A8, which is ALREADY declared volatile at line ~3792.
 * Left un-qualified here because c_08017880.c is the only function proved to
 * need it and retyping the global would force a re-verify of every other user;
 * promote the qualifier onto the declaration if a second function wants it. */
extern u16 gUnknown_03001FEC;
extern u16 gUnknown_03002008;
extern u32 gUnknown_03002010;
extern u16 gUnknown_03002014;
extern u16 gUnknown_03002018;
extern u16 gUnknown_0300201C;
extern u32 gUnknown_03002024;
extern u32 gUnknown_03002030;
extern u16 gUnknown_03002034;
extern u8 gUnknown_030020A4;
extern u16 gUnknown_030024C4;
extern u16 gUnknown_030024CC;
extern u16 gUnknown_03002518;
extern u32 gUnknown_03002B3C;
extern u16 gUnknown_03002B48;
extern u16 gUnknown_03002B50;
extern u16 gUnknown_03002B58;
extern u16 gUnknown_03002B64;
extern u8 gUnknown_03002B70;
extern u16 gUnknown_03002EDC;
extern u8 gUnknown_03002EE8;
extern u8 gUnknown_03002EEC;
extern u16 gUnknown_03002EF4;
extern u16 gUnknown_03002F10;
extern u16 gUnknown_03002F14;
extern u16 gUnknown_03002F28;
extern u8 gUnknown_03002F34;
extern u16 gUnknown_03002F38;
extern u16 gUnknown_03002F40;
extern u32 gUnknown_03003030;
extern u16 gUnknown_03003038;
extern u16 gUnknown_0300303C;
extern u16 gUnknown_03003044;
extern u16 gUnknown_03003090;
extern u8 gUnknown_03003094;
extern u16 gUnknown_03003098;
extern u8 gUnknown_030030AC;
/* Wave 35, W35-I: VOLATILE on the same evidence as gUnknown_03001FEC above --
 * see the note there. The low byte of sub_08017880's REG_BLDALPHA write. */
extern u16 gUnknown_030030C0;
extern u16 gUnknown_030030D8;
extern u16 gUnknown_030030E4;
/* The four window-bound shadows -- the WIN0H / WIN1H / WIN0V / WIN1V halves,
 * in that order. ALL FOUR ARE VOLATILE, and this is the cheapest volatile tell
 * found so far because it costs eight bytes rather than a register: in
 * sub_0801258C they are ORed into one 64-bit value, and a plain read lets
 * combine fold each `ldrh` straight into the DImode lowpart
 * (`ldrh r2, [r0]`), while a volatile read cannot be folded and leaves the
 * copy behind (`ldrh r4, [r0]; adds r0, r4, #0`). Four terms, four extra
 * copies, and the extra pressure spills a fifth address into r8 -- which is
 * also why the function has a three-high-register prologue that a
 * straight-line function has no other reason to want. */
extern volatile u16 gUnknown_030020B0;
extern volatile u16 gUnknown_0300309C;
extern volatile u16 gUnknown_03002B60;
extern volatile u16 gUnknown_03002028;
/* The 8-byte snapshot of those four, written as one 64-bit store pair. */
extern u64 gUnknown_030030B8;

/* sub_08053860 and sub_08053BB8 walk FOUR parallel proc-id tables, five ids at
 * a time, twice each. The two passes are what fixes the shape: every table is
 * touched at a group-0 offset and again one fixed stride higher, and the
 * palette argument switches from `gUnknown_085523A8[gUnknown_0300450C]` to
 * `[gUnknown_0300450C ^ 1]` in lockstep with the group. So each table is a
 * TWO-element array of a record holding five s16 ids, and the strides
 * (0xb4 / 0x0a / 0x6c / 0x28) are measured, not assumed. Only the id members
 * are proved; every filler below is the space between two proved offsets and
 * nothing more. */
/* unk2e/unk30/unk3a added in wave 17 from sub_08051DE0/sub_080524C0/
 * sub_0805297C, which all read the same two expressions:
 *   unk3a[unk2e]  -> OamData.tileNum, so a 10-bit-or-narrower tile index,
 *                    with unk2e its cursor (`ldrh` at +0x2e off the element).
 *   unk30[j]      -> the row index of gUnknown_08552D80, j being
 *                    gUnknown_0300451C, which is what fixes the [5].
 * Both are `ldrh`, so unsigned is not proved -- nothing sign-extends either,
 * and neither is ever cleared, so an s16 object read in a u16 context would
 * look identical. The [5] on unk30 is the same slot count as unk24[5] and as
 * gUnknown_02029A10's entries[5]; the [5] on unk3a is inherited from the
 * layout and only its 0x3a start is read directly. The record now tiles
 * exactly -- 0x24 + 5*2 = 0x2e, +2 = 0x30, +5*2 = 0x3a, +5*2 = 0x44,
 * +5*4 = 0x58, +5*4 = 0x6c -- with the last two arrays added from
 * sub_08051BEC/sub_08051F4C, so every byte from 0x24 to the end of the struct
 * is accounted for by a five-slot array. That is corroboration for the [5]s,
 * not a proof of any one of them. */
struct Unk02029808 /* 0x6c */
{
    /* Wave 34, W34-L: sub_08053670 splits filler_00 layout-preservingly. It
     * reads a halfword index at +0 and uses it to subscript THREE halfword
     * arrays at +2, +0xe and +0x1a, each with the `adds rB,#N` on the base that
     * only the member-array spelling produces. The extents are the [5] the rest
     * of this record uses throughout, which leaves a 2-byte scalar before each
     * of the second and third arrays; the accesses cannot tell [5]-plus-gap
     * from a gapless [6][6][5], and both total exactly 0x24, so the fillers are
     * the conservative reading and nothing here depends on the choice. */
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02[5];
    /* 0x0c */ u8 filler_0c[0x02];
    /* 0x0e */ u16 unk0e[5];
    /* 0x18 */ u8 filler_18[0x02];
    /* 0x1a */ u16 unk1a[5];
    /* 0x24 */ s16 unk24[5];
    /* 0x2e */ u16 unk2e;
    /* 0x30 */ u16 unk30[5];
    /* 0x3a */ u16 unk3a[5];
    /* 0x44 */ void *unk44[5]; /* two more slot-indexed arrays, this pair of
                                * words rather than halfwords, and they run to
                                * the end of the 0x6c record exactly.
                                * sub_08051F4C reads unk44[unk2e] and
                                * unk58[unk2e] with `ldr` and passes them as
                                * sub_08015410's third and fourth arguments,
                                * which unknown-functions.h already types
                                * `void *` -- the same graphics/palette pair
                                * gUnknown_08557978's columns 1 and 2 supply.
                                * Note the INDEX is unk2e here and the slot
                                * index elsewhere; only unk2e has been seen. */
    /* 0x58 */ void *unk58[5];
};

struct Unk020296B0 /* 0x28 */
{
    /* 0x00 */ u16 unk00; /* wave 18 (W18-C): sub_08050B70 reads it with a bare
                           * `ldrh` off the element base and assigns it to
                           * struct OamData's 10-bit tileNum, so it is the OBJ
                           * tile index for the slot. Halfword from the load;
                           * nothing narrows it, so 16 bits is the widest
                           * evidence and the bitfield does the masking. */
    /* 0x02 */ s16 unk02[5];
    /* 0x0c */ u16 unk0c[6]; /* Wave 35, W35-F. sub_0804D0FC and sub_0804D4E8
                              * read `unk0c[unk18]` as a script/command stream:
                              * `adds rBase,#0xc; adds rIdx*2` off the `* 0x28`
                              * element base, `ldrh`, compared against 0xff as
                              * the terminator, with unk18 post-incremented on
                              * every visit. Halfword from the load; the 0xff
                              * sentinel says nothing about the signedness of
                              * the other entries, so u16 is the weakest fit.
                              * Was the head of filler_0c[0x0e]; the index runs
                              * past 6 at runtime, the bound is only the
                              * distance to unk18. */
    /* 0x18 */ u16 unk18; /* Wave 35, W35-F. The read cursor into unk0c, bumped
                           * `ldrh [rN,#0x18]; adds #1; strh` by sub_0804D0FC
                           * and sub_0804D4E8 once per tick that sub_080156C4
                           * reports 2. Halfword from that pair. */
    /* 0x1a */ u8 unk1a; /* a phase counter, post-incremented once per emitted
                          * sound: sub_0804E7A8 and sub_0804FCA4 both do
                          * `sub_0803B48C(tbl[..][g[c].unk1a & 1]);
                          * g[c].unk1a++;` with a bare `ldrb`/`strb` pair, so
                          * only bit 0 is ever consumed. Byte, from the
                          * `ldrb r0,[r4,#0x1a]` / `strb r0,[r4,#0x1a]` pair
                          * off the same element address (wave 18). */
    /* 0x1b */ u8 unk1b; /* wave 33 (W33-C): a SECOND phase counter beside
                          * unk1a and read the same way -- sub_08050C8C does a
                          * bare `ldrb [r1,#0x1b]; adds #1; strb` when
                          * sub_080156C4 returns 3. Byte from that pair; nothing
                          * bounds it. */
    /* 0x1c */ u8 unk1c; /* wave 34 (W34-A): a small table selector.
                          * sub_08050D44 reads it with `ldrb [rN,#0x1c]` off the
                          * `* 0x28` element base and uses it to pick a
                          * gUnknown_08553B1C row pointer. Byte from the load;
                          * nothing bounds it. The 0x1c displacement only stays
                          * in the load when this is a NAMED member -- reached
                          * through a `(u8 *)` cast agbcc folds it into the pool
                          * word instead. */
    /* 0x1d */ u8 filler_1d[0x01];
    /* 0x1e */ u16 unk1e[5]; /* Wave 37, W37-B. The TIMESTAMP row that goes with
                              * the unk0c command row and shares its unk18
                              * cursor: sub_08054278 reads `unk1e[unk18]` off the
                              * SAME CSEd `i * 0x28 + unk18 * 2` it used for
                              * unk0c and compares it for equality against the
                              * u16 gUnknown_03004508 frame counter before
                              * dispatching the command through
                              * gUnknown_08553744. `adds rB,#0x1e` with a zero
                              * `ldrh` displacement -- the member-array form, and
                              * the shared index register is what rules out
                              * `unk0c[unk18 + 9]` as the spelling.
                              * gUnknown_020298E0's unk0c/unk1a pair plays the
                              * same two roles with the layout swapped. The [5]
                              * is what the 0x1e..0x28 span allows; like unk0c
                              * the cursor can run past it. Was filler_1d. */
};

extern s16 gUnknown_02029668[][5];
/* Wave 37, W37-J. 0x0202967C is gUnknown_02029668 + 0x14, i.e. ROW 2 of the
 * table above, reached through its own linker symbol because sub_0804FF44
 * indexes it as `base + i * 10` with a pool word of exactly 0x0202967C -- the
 * same 10-byte row stride (`(i * 4 + i) * 2`). Element [0] receives
 * sub_08015410's s8 return sign-extended and stored `strh`, which is what makes
 * the element 16 bits; nothing here proves the signedness beyond agreeing with
 * gUnknown_02029668. */
extern s16 gUnknown_0202967C[][5];
/* The per-side movement record: 0x58 bytes, indexed by
 * gUnknown_03001470[proc].unk30 (the 0/1 side), holding five parallel slots
 * indexed by gUnknown_03001470[proc].unk34. sub_08050958 is the whole
 * derivation and it steps two 8.8 fixed-point axes per tick:
 *   unk26[j] += unk30[j];   x_sub += x_step
 *   (x whole) += unk26[j] >> 8;
 *   unk44[j] += unk4e[j];   y_sub += y_step
 *   unk3a[j] += unk44[j] >> 8;
 * The `>> 8` is `lsls #0x10; asrs #0x18`, i.e. a SIGNED 16-bit shift, which is
 * what types unk26/unk44 (and by symmetry unk3a and the 0x1c row) as s16
 * rather than u16; the steps unk30/unk4e are only ever added and are left u16.
 * unk16 is a tick threshold compared against gUnknown_03001470[proc].unk2c,
 * and unk18/unk1a are the x/y bounds the same function tests the two whole
 * coordinates against.
 *
 * The 0x1c row is reached through its OWN linker symbol, gUnknown_0202972C
 * (== gUnknown_02029710 + 0x1c), exactly as gUnknown_03004582 overlaps
 * gUnknown_03004580: sub_08050958 holds a pool word for each. The 44-halfword
 * row length IS the 0x58 stride, not a proved extent. Kept as filler here so
 * the two spellings cannot drift apart. */
struct Unk02029710 /* 0x58 */
{
    /* 0x00 */ u16 unk00; /* wave 33 (W33-C): sub_08050C8C reads it `ldrh` off
                           * the `* 0x58` element base and tests it against 0 as
                           * the first of three guards before raising a
                           * gUnknown_02029664 bit. u16 from the load; nothing
                           * signs it and nothing matched writes it. */
    /* 0x02 */ u8 filler_02[0x02];
    /* 0x04 */ void *unk04; /* Wave 36, W36-C. sub_080505A4 loads unk04 and
                             * unk08 with whole-word `ldr`s off the `* 0x58`
                             * element base and hands them straight to
                             * sub_08015410's third and fourth parameters, which
                             * unknown-functions.h already types `void *` -- the
                             * same graphics/palette pair gUnknown_02029700's
                             * two columns supply for the other table. */
    /* 0x08 */ void *unk08;
    /* 0x0c */ s16 unk0c[5]; /* Wave 36, W36-C. sub_080505A4 stores
                              * sub_08015410's `s8` result here SIGN-extended
                              * (`lsls #0x18; asrs #0x18`) with a `strh`, indexed
                              * by the slot -- the same "one animation handle per
                              * slot" role gUnknown_020296B0.unk02 plays. Signed
                              * halfword holding a signed byte. Was the head of
                              * filler_02. */
    /* 0x16 */ u16 unk16;
    /* 0x18 */ u16 unk18;
    /* 0x1a */ u16 unk1a;
    /* 0x1c */ u8 filler_1c[0x0a]; /* == gUnknown_0202972C[i][0..4] */
    /* 0x26 */ s16 unk26[5];
    /* 0x30 */ u16 unk30[5];
    /* 0x3a */ s16 unk3a[5];
    /* 0x44 */ s16 unk44[5];
    /* 0x4e */ u16 unk4e[5];
};
extern struct Unk02029710 gUnknown_02029710[];
extern s16 gUnknown_0202972C[][44];
extern struct Unk020296B0 gUnknown_020296B0[];
/* Wave 51, W51-H. Two rows of u16 slots, one per side. sub_08055940 reads
 * `[0][i]` and `[1][i]` for i = 0..4 and counts the entries that are not 0xff
 * into a per-side counter, so 0xff is the empty marker. The two rows come off
 * ONE CSEd address in that function (a single pool word plus a `subs #0x28`),
 * which is what proves they are one object and not two neighbouring globals.
 *   The 40-byte row is PROVED by that 0x28: 0x020296E4 - 0x020296BC. The [20]
 * column count is the row divided by sizeof(u16); only columns 0..4 are read
 * here, so the extent USED is five and the rest of the row is unproved.
 * gUnknown_020296CE, which other functions hold a pool word for, is [0][9]. */
extern u16 gUnknown_020296BC[][20];
/* A pair of five-per-side u16 counters, read and written only as
 * `[i][j]` with the same (side, slot) index pair every other table in this
 * subsystem uses. sub_080501DC does `B94[i][j] += B80[i][j];` and zeroes BOTH
 * when the sum reaches 6, so B80 is a per-frame step and B94 the accumulator
 * that wraps at six. The [2][5] extent is PROVED by the addresses: 0x02029B94
 * - 0x02029B80 = 20 bytes = 2 * 5 * sizeof(u16), so B80 tiles exactly up to
 * B94 and there is no room for a sixth slot or a third side. Both `ldrh` /
 * `strh` with nothing sign-extending, and both are cleared, so u16 is the
 * weakest fit and the clearing half of the pair rules out a narrower one. */
/* Wave 36, W36-C. Two `void *` columns per gUnknown_0300453C side (the stride
 * is `i * 8` and the second word is reached as `base + 4`), read with whole-word
 * `ldr`s by sub_080505A4 and passed as sub_08015410's third and fourth
 * arguments -- the graphics/palette descriptor pair. */
extern void *gUnknown_02029700[][2];
extern u16 gUnknown_02029B80[2][5];
extern u16 gUnknown_02029B94[2][5];
/* Wave 36, W36-C. A per-(side, command) flag set to 1 by sub_08053FBC as it
 * retires each script command. The [2][5] extent is PROVED by the addresses:
 * 0x02029C00 - 0x02029BEC = 20 bytes = 2 * 5 * sizeof(u16), so it tiles exactly
 * up to gUnknown_02029C00 below. `strh` of a constant, hence u16. */
extern u16 gUnknown_02029BEC[2][5];
/* Wave 36, W36-C. The read cursor into gUnknown_020296B0[i].unk0c for
 * sub_08053FBC's script channel -- `ldrh; adds #1; strh` off `i * 2` once a
 * command is accepted, and also the subscript into gUnknown_08551E12. A
 * SEPARATE cursor from gUnknown_020296B0[i].unk18, which sub_0804D0FC and
 * sub_0804D4E8 drive over the same command array. u16 from the pair; the
 * [2] extent is the gUnknown_02029C04 boundary. */
extern u16 gUnknown_02029C00[2];
/* Wave 36, W36-C. A per-side alternating phase counter, bumped
 * `ldrh; adds #1; strh` once per emitted sound by sub_08053FBC and consumed
 * only as `& 1` -- the same role gUnknown_020296B0[i].unk1a plays for the
 * other channel. u16 from the read-modify-write. */
extern u16 gUnknown_02029C04[2];
/* Wave 37, W37-B. The read cursor into gUnknown_020296B0[i].unk0c for
 * sub_0805414C's channel -- a THIRD cursor over the same command row, beside
 * gUnknown_02029C00 (sub_08053FBC's) and gUnknown_020296B0[i].unk18
 * (sub_08054278's). `ldrh; adds #1; strh` off `i * 2` once a command is
 * accepted, and the same value subscripts gUnknown_08551E12, exactly as
 * gUnknown_02029C00 does. u16 from the pair; the [2] is the 4-byte gap to
 * gUnknown_02029C0C, i.e. one per side. */
extern u16 gUnknown_02029C08[2];
/* 0x90-byte per-side records -- the stride is `(s*8+s)*16` at all 55 sites in
 * code-0801D390.s. Two five-slot halfword arrays are reached here, at +0x26 and
 * +0x30, indexed by gUnknown_03001470[proc].unk34 (the slot) and summed into a
 * gUnknown_08553B40 column. A STRUCT and not `u16 [][72]`: the ROM adds the
 * member offset to the BASE register (`adds r0,#0x26`) and leaves the `ldrh`
 * displacement at zero, which is what a struct member holding an array gives;
 * the flat spelling folds the same constant into the symbol's relocation.
 * +0x0a, +0x16, +0x18, +0x1a, +0x3a, +0x80, +0x8c and +0x8d are also read
 * elsewhere in the file and are left as filler until something needs them
 * (wave 20, W20-C). */
struct Unk020298E0 /* 0x90 */
{
    /* 0x00 */ u16 unk00; /* Wave 37, W37-B. A tile base, and the THIRD member of
                           * a set written together: sub_08054EE0 and
                           * sub_08054F50 store one value into
                           * gUnknown_020297C0[i].unk00, gUnknown_020296B0[i]
                           * .unk00 and this one back to back, and the first two
                           * are already typed u16 tile bases. `strh` of a value
                           * built as `(u16)(i * 0x100 + 0x50)`. Was filler. */
    /* 0x02 */ s16 unk02[5];  /* Wave 33, W33-E. sub_08050F24 writes
                               * unk02[unk16 - 1] with sub_08015410's `s8` return
                               * SIGN-extended (`lsls #0x18; asrs #0x18`) and
                               * stored `strh`, so the element is a signed
                               * halfword holding a signed byte.
                               *   Wave 37, W37-B narrowed the extent from 10 to
                               * 5: sub_08054488 reads a SECOND halfword row at
                               * +0x0c off the same element base with unk16 as a
                               * bare `unk16 * 2` index CSEd with the +0x1a row
                               * below, which is the member-array form and not
                               * `unk02[unk16 + 5]` (that spelling would have to
                               * add 5 before the shift and could not share the
                               * index register). The two views do overlap if
                               * unk16 ever exceeds 5; a declared LENGTH never
                               * enters address arithmetic, so the narrowing is
                               * byte-neutral for sub_08050F24 either way. */
    /* 0x0c */ u16 unk0c[5];  /* Wave 37, W37-B. The TIMESTAMP row for the unk1a
                               * command row below, on the shared unk16 cursor:
                               * sub_08054488 compares `unk0c[unk16]` for
                               * equality against the u16 gUnknown_03004508
                               * frame counter. `adds rB,#0xc`, zero `ldrh`
                               * displacement. Same pair gUnknown_020296B0's
                               * unk0c/unk1e form, with the roles of the two
                               * offsets swapped. */
    /* 0x16 */ u16 unk16;     /* Wave 33, W33-E. The fill level of unk02 above:
                               * read `ldrh`, decremented by one and used as the
                               * subscript, i.e. the last written slot. */
    /* 0x18 */ u16 unk18;     /* Wave 37, W37-J. A three-state phase counter,
                               * ONE per side rather than per slot (no slot term
                               * in the address chain): sub_08050FF8 does
                               * `ldrh [rB,#0x18]; adds #1; strh` and then
                               * re-reads it and zeroes it when it reaches 3,
                               * while sub_08051454 bumps it the same way and
                               * uses it to index gUnknown_08553BFC. u16 from
                               * the read-modify-write. Was filler_18. */
    /* 0x1a */ u16 unk1a[6];  /* Wave 37, W37-B. The COMMAND row this record's
                               * unk16 cursor walks -- sub_08054488 reads
                               * `unk1a[unk16]`, stops on the 0xff terminator and
                               * otherwise dispatches through
                               * gUnknown_0855374C. Exactly gUnknown_020296B0's
                               * unk0c, for the other channel. u16 from the
                               * `ldrh`; the 0xff sentinel says nothing about the
                               * signedness of the other entries. [6] is the
                               * 0x1a..0x26 span and the cursor can run past it.
                               * Was the head of filler_18. */
    /* 0x26 */ u16 unk26[5];
    /* 0x30 */ u16 unk30[5];
    /* 0x3a */ s16 unk3a[5]; /* A third five-slot halfword array on the same
                              * [side][slot] index as unk26/unk30 -- a countdown.
                              * SIGNED on the discriminating half: sub_0804BDD8
                              * and sub_0804BECC do `ldrh; subs #1; strh; lsls
                              * #0x10; cmp #0; bgt`, i.e. `--x <= 0` tested on
                              * the sign of the low half, which a u16 member
                              * cannot produce (the same readout that signs
                              * struct Unk08580934_Obj.unk26). The `ldrh` is
                              * forced by the store-back, not by the type.
                              * Extent 5 by analogy with unk26/unk30, which is
                              * what the +0x44 boundary allows (wave 21, W21-A). */
    /* 0x44 */ u16 unk44[5]; /* == gUnknown_02029924[i][0..4], reached through
                              * its OWN linker symbol as well, the way
                              * gUnknown_0202972C overlaps gUnknown_02029710.
                              * BOTH spellings are real and which one a function
                              * needs is readable off its pool word: sub_080517BC
                              * holds `=gUnknown_02029924` and indexes it flat,
                              * while sub_08051454 holds `=gUnknown_020298E0`
                              * and reaches the same halfword as
                              * `adds rB,#0x44` with a zero `strh` displacement,
                              * i.e. the member-array form. Wave 37 (W37-J)
                              * narrowed it out of filler_44 for that reason;
                              * the two views must stay the same offset and
                              * length. u16 to agree with gUnknown_02029924,
                              * whose unsignedness W36-C measured. */
    /* 0x4e */ s16 unk4e[5]; /* Wave 36, W36-C. The whole (integer) half of a
                              * second 8.8 fixed-point axis, stepped by
                              * sub_080517BC as `unk6c[j] += unk76[j];
                              * unk4e[j] += (s16)unk6c[j] >> 8;`. SIGNED and
                              * measured: the same function then re-reads it
                              * with a bare `movs r0,#0; ldrsh r3,[r3,r0]` to
                              * hand sub_08050528 its s16 fourth argument, which
                              * a u16 member cannot produce. */
    /* 0x58 */ u16 unk58[5]; /* Wave 36, W36-C. The sub-pixel accumulator of the
                              * FIRST axis: `unk58[j] += unk62[j]` and the sum
                              * `>> 8` (as `lsls #0x10; asrs #0x18`) is added to
                              * gUnknown_02029924[i][j]. u16 -- only ever added
                              * and stored back. */
    /* 0x62 */ u16 unk62[5]; /* the first axis's per-tick step. */
    /* 0x6c */ u16 unk6c[5]; /* the second axis's sub-pixel accumulator, feeding
                              * unk4e the same way. */
    /* 0x76 */ u16 unk76[5]; /* the second axis's per-tick step. */
    /* 0x80 */ u16 unk80[5]; /* Wave 36, W36-C. A per-slot frame counter,
                              * `ldrh; adds #1; strh` once per sub_080517BC
                              * visit and compared for equality against unk8a
                              * below. u16 from the read-modify-write. */
    /* 0x8a */ u16 unk8a;    /* Wave 36, W36-C. The value unk80[j] is compared
                              * against -- one per side, not per slot (the
                              * address chain omits the slot term entirely). */
    /* 0x8c */ u8 unk8c; /* Wave 36, W36-C. The alternating phase counter that
                          * goes with unk8d: sub_08053520 does
                          * `sub_0803B48C(gUnknown_085643A8.unk04[unk8c & 1]);
                          * unk8c++; unk8d = 0;`, the same `ldrb`/`strb` pair
                          * and the same "only bit 0 is ever consumed" shape as
                          * gUnknown_020296B0.unk1a. Was filler. */
    /* 0x8d */ u8 unk8d; /* Wave 33, W33-D. sub_0805131C bumps it
                          * `ldrb; adds #1; strb` off the side element address
                          * with the offset added to the base (`adds r0,#0x8d`,
                          * past ldrb's displacement range). A counter; nothing
                          * in the matched tree reads it back, so the `ldrb` is
                          * the whole evidence for the width. */
    /* 0x8e */ u8 filler_8e[0x02];
};
extern struct Unk020298E0 gUnknown_020298E0[];
/* == gUnknown_020298E0 + 0x44, the whole (integer) half of the first 8.8
 * fixed-point axis whose sub-pixel accumulator is gUnknown_020298E0.unk58.
 * sub_080517BC holds a pool word for this symbol and another for
 * gUnknown_020298E0 in the same function, so the two spellings are both real.
 * The 72-halfword row IS the 0x90 stride, not a proved extent.
 * UNSIGNED, and that is the discriminating half rather than an assumption:
 * sub_080517BC adds `gUnknown_03001470[proc].unk28` (a u32) to it and narrows
 * the sum to s16 for sub_08050528. With a u16 element that is
 * `ldrh; adds; lsls #0x10; asrs #0x10`, which is what the ROM has; an s16
 * element makes the addition a genuine 32-bit one and the load becomes
 * `ldrsh`. (Wave 36, W36-C.) */
extern u16 gUnknown_02029924[][72];
/* Wave 37, W37-J. == gUnknown_020298E0 + 0x26, i.e. the unk26 row through its
 * own linker symbol, exactly as gUnknown_02029924 is the unk44 row through
 * one. sub_08050FF8 holds a pool word for THIS symbol and indexes it flat
 * (`i * 0x90 + k * 2` from the symbol, three times), which is what fixes the
 * spelling -- the member form would have given `=gUnknown_020298E0` plus
 * `adds rB,#0x26`. The 72-halfword row IS the 0x90 stride, not an extent. */
extern u16 gUnknown_02029906[][72];
/* Wave 49, W49-M. == gUnknown_020298E0 + 0x0c, i.e. the unk0c row through its
 * own linker symbol, the third of these after gUnknown_02029924 (+0x44) and
 * gUnknown_02029906 (+0x26). sub_0805634C holds a pool word for THIS symbol
 * and indexes it FLAT (`j * 2 + side * 0x90` off the symbol, zero `ldrh`
 * displacement) in the same inner loop where it reaches unk1a through the
 * member form (`=gUnknown_020298E0` plus `adds rB,#0x1a`) -- two spellings of
 * two rows of one record, side by side, which is what fixes both. The
 * 72-halfword row IS the 0x90 stride, not a proved extent. */
extern u16 gUnknown_020298EC[][72];
extern struct Unk02029808 gUnknown_02029808[];
/* Wave 48, W48-G. == gUnknown_02029808 + 0xe, i.e. the unk0e row through its
 * own linker symbol, exactly as gUnknown_02029924 and gUnknown_02029906 are
 * rows of gUnknown_020298E0. sub_080564B8 holds a pool word for THIS symbol
 * for its two `= 1` fill loops (flat `i * 2 + side * 0x6c` off the symbol) and
 * a SECOND one for gUnknown_02029808 in the same function for the `+=` that
 * follows, where the ROM shows the member form's `adds rB, #0xe`. The
 * 54-halfword row IS the 0x6c stride, not a proved extent. */
extern u16 gUnknown_02029816[][54];
/* Wave 48, W48-G. Two more rows of gUnknown_02029808 through their own linker
 * symbols, on the same 0x6c stride: gUnknown_0202980A == +0x02 (the unk02 row)
 * and gUnknown_02029822 == +0x1a (the unk1a row). sub_08056638 bubble-sorts
 * gUnknown_02029822[side][0..4] and carries gUnknown_0202980A along as the
 * payload, holding a pool word for each and indexing both flat off the symbol
 * (`j * 2 + side * 0x6c`). 0xFF is that sort's empty-slot sentinel and the
 * comparisons are unsigned (`bls`), which is the whole evidence for u16. */
extern u16 gUnknown_0202980A[][54];
extern u16 gUnknown_02029822[][54];
/* Wave 33, W33-D. A 0x24-byte per-side record indexed by gUnknown_0300453C
 * (`((i * 9) << 2)` at both sites). Two readers, both in the 0x08051 block:
 *   unk00     sub_08051A44 reads it `ldrh` and drops it straight into
 *             OamData.tileNum, so it is a tile base and at most 10 bits wide.
 *   unk02[j]  sub_0805198C stores sub_08015410's s8 result here with `strh`,
 *             j being gUnknown_0300451C -- the same "one animation handle per
 *             slot" role gUnknown_02029808[i].unk24[j] plays for the other
 *             descriptor family, hence the [5] and the signed type.
 *   unk18     the two `void *` arguments sub_0805198C hands sub_08015410 as
 *   unk20     parameters 3 and 4; whole-word `ldr` at both.
 * Every filler below is unreached space between two proved offsets. */
struct Unk020297C0 /* 0x24 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ s16 unk02[5];
    /* 0x0c */ u16 unk0c[5];
    /* 0x16 */ u8 filler_16[0x02];
    /* 0x18 */ void *unk18;
    /* 0x1c */ void *unk1c;
    /* 0x20 */ void *unk20;
};
extern struct Unk020297C0 gUnknown_020297C0[];
/* A halfword cell per gUnknown_0300453C side, read `gUnknown_02029BE8[s ^ 1]`
 * -- the OTHER side's slot -- and tested `!= 0` by sub_0804BDD8/sub_0804BECC
 * before they clear a per-slot counter. Bare `ldrh` off `index * 2`, nothing
 * sign-extends, so a plain u16 array; the extent is unknown (aw2bhr.lds puts
 * the next symbol 4 bytes later, which bounds it at 2 if that symbol is real)
 * (wave 21, W21-A). */
extern u16 gUnknown_02029BE8[];
/* Set to 1 with a bare `strh` by sub_08053BB8 and by nothing else matched. */
extern u16 gUnknown_030045B0;
/* Two ROM u16 tables indexed by gUnknown_0300450C (and, for the second, by its
 * complement): 08551E7C supplies sub_0805741C's argument in sub_08053860,
 * 085523A8 supplies the palette sub_08053614 pushes into bits 10-11 of an OAM
 * word. Both are read with a bare `ldrh` off `index * 2`, so they are plain u16
 * arrays rather than aggregates -- the same reasoning as gUnknown_085538AE. */
/* Two ADJACENT byte tables scoring the 5-bit terrain code that
 * gUnknown_08499590 + 0x1432 holds, read `tbl[cell & 0x1f]` by the twins
 * sub_0804B42C (0x08551CA0) and sub_0804B4C4 (0x08551CBD).
 *
 * The ODD address of the second is real and not a mis-attributed pool word:
 * 0x08551CA0 + 0x1d == 0x08551CBD and 0x08551CBD + 0x1f == 0x08551CDC, so the
 * three symbols tile the region exactly, and the two tables are byte-for-byte
 * the same in ROM apart from element 5 (3 vs 0) -- parallel tables of 29 and
 * 31 explicit initialisers, not one table read at two offsets. A 32-entry
 * reading is what the 0x1f mask suggests and it is WRONG: it would make the
 * two overlap. Values are 0..4, read `ldrb` into an UNSIGNED comparison (the
 * running best is `bhs`, not `bge`), so the running maximum is `u32` in the
 * source while the table itself is only ever zero-extended (wave 21, W21-A). */
/* Wave 35, W35-F. sub_0804B55C indexes both with `lsls #0x18; lsrs #0x17` off
 * an int-returning callee -- a `(u8)` cast scaled by 2 -- and reads them
 * `ldrh`, so they are u16 tables in the same 0..0xff index space
 * gUnknown_08551CA0 scores. A zero element means "no substitution". */
extern u16 gUnknown_08551C00[];
extern u16 gUnknown_08551C3A[];
/* Wave 35, W35-F. A genuine ROM POINTER TABLE, not a -fforce-addr word: the
 * entries at 0x08551C88..0x08551C9C hold 0x08551C78, 0x08551C78, 0x08551C7C,
 * 0x08551C80, 0x08551C84, 0x08551C74 -- five DISTINCT targets, which a
 * force-addr constant pool cannot be. sub_0804B55C loads one with
 * `lsls #2; ldr` and then reads `[ptr + (rand & 1) * 2]` with `ldrh`, so each
 * row is a two-element u16 pair. */
extern u16 *gUnknown_08551C88[];
extern const u8 gUnknown_08551CA0[];
extern const u8 gUnknown_08551CBD[];
/* The four cardinal neighbour offsets as (dx, dy) word pairs -- (1,0), (-1,0),
 * (0,-1), (0,1) -- walked with `ldr [r1]; ldr [r1,#4]; adds r1,#8` by both
 * twins above and added to signed cell coordinates. Words and signed: the
 * elements ARE -1 in ROM. */
extern const s32 gUnknown_08551CDC[][2];
extern const u16 gUnknown_08551E7C[];
extern const u16 gUnknown_085523A8[];
/* Wave 49, W49-M, block 0x08056. A ROM table of FIVE-halfword rows, and the
 * ROM content is the proof rather than a stride guess: 0x08551D22 reads
 * 0,50,60,110,111 / 0,50,60,110,120 / 0,50,20,180,190 / 0,50,50,170,180 --
 * four rows that each restart at 0 on a 10-byte period, running to
 * gUnknown_08551E12 (0xf0 == 24 rows).
 *   Both sub_0805634C and sub_080560A4 subscript it with
 * `gUnknown_030045A0[side ^ 1]`, a computed subscript ADDED to the loaded word
 * before any dereference, so this is a real table and not a -fforce-addr pool
 * word.
 *   gUnknown_08551D2A == gUnknown_08551D22 + 8, i.e. that table's column 4
 * through its own linker symbol, exactly as gUnknown_02029924 and friends are
 * rows of gUnknown_020298E0. It is a SEPARATE declared object and not the same
 * array read at another column: sub_0805634C reaches column 3 as
 * `gUnknown_08551D22[x][3]` -- bare pool word plus a run-time `adds rB,#6`,
 * the nested-array form -- and column 4 as a pool word that already carries
 * the +8, in the same statement and off the SAME CSEd `x * 10`. One declared
 * array cannot produce both spellings for two of its own columns.
 * Read `ldrh`, nothing signs them; the values are small y offsets that are
 * subtracted and added around gUnknown_020298EC. */
extern const u16 gUnknown_08551D22[][5];
extern const u16 gUnknown_08551D2A[][5];
/* Wave 49, W49-M. A genuine ROM POINTER TABLE of four words, not a
 * -fforce-addr pool run: the words at 0x08551E64..0x08551E70 hold 0x08551E12,
 * 0x08551E26, 0x08551E3A and 0x08551E4E -- four DISTINCT targets spaced 0x14
 * apart, which a force-addr constant pool cannot be. sub_0805634C and
 * sub_080560A4 both index it `gUnknown_030045A0[gUnknown_0300450C] * 4` and
 * `ldr` the word into a local held across a loop, then read the pointed-to row
 * `ldrh` at `gUnknown_08551E7C[side * 2 + gUnknown_0300450C] * 5 + i` -- ten
 * halfwords, which is exactly the 0x14 spacing. Non-const element type to
 * match gUnknown_08551C88, the other pointer table here. */
extern u16 *gUnknown_08551E64[];

/* The screen-setup blobs sub_08065990 / sub_0806D944 hand out. Typed from the
 * callee that receives each, which is the discriminating use in every case:
 * the two Decompress sources are `u8 *` because that is Decompress's parameter
 * and const would break it, and 082344CC is `u16 *` because it goes to
 * ApplyPaletteExt. */
/* A u16 palette source: sub_0806A054 hands it to ApplyPaletteExt with
 * (0x20, 0xa0), i.e. ApplyPalettes(src, 1, 5) -- five palettes starting at BG
 * palette 1. */
extern u16 gUnknown_0822FE50[];
extern u8 gUnknown_0822F9AC[];
extern u8 gUnknown_0822FEF0[];
extern u16 gUnknown_082344CC[];
/* Wave 44, W44-F. sub_0807F434's two blobs, both reached through the
 * 0x081D936C .rodata address-constant pool rather than a direct `ldr rN, =sym`.
 * 08234B10 is a Decompress SOURCE unpacked into gUnknown_0200FC50, so `u8 []`
 * and NOT const -- Decompress's first parameter is `u8 *` (same model as
 * gUnknown_0823DE38). 082352DC goes straight to ApplyPaletteExt's `u16 *`
 * first parameter with no arithmetic, 0x20 bytes at palette word 0x280, i.e.
 * one OBJ palette. Neither extent is pinned. */
extern u8 gUnknown_08234B10[];
extern u16 gUnknown_082352DC[];
/* A destination buffer in EWRAM, not ROM: sub_08065238 and sub_0806D944 both
 * pass it as sub_08073304's second argument alongside a ROM first argument. */
extern u8 gUnknown_0200FC50[];
/* Wave 44, W44-F. The EWRAM tile source sub_080801A8 CpuFastSets into the two
 * BG screen blocks (0x100 words each, eight times), so `const void *` is all
 * that is pinned and `u8 []` is the weakest model -- the same one
 * gUnknown_0200FC50 above carries. It sits exactly 0x400 bytes after
 * gUnknown_0200FC50, which is the 0x400 bytes that function copies out of
 * gUnknown_0200FC50 in its tail (0x80 words at +0 and at +0x200), so the two
 * are adjacent buffers rather than one. */
extern u8 gUnknown_02010050[];
/* A small step counter, `ldr`/`cmp` only. SIGNED: sub_080852A8 gates its first
 * block with `cmp r5, #3; bgt`, and it doubles as an array index. */
extern int gUnknown_03005940;
/* The sub_08080498 screen-setup blobs. 082391E8 / 080A36C8 / 08239DE4 are u16
 * palettes (ApplyPaletteExt at 0x00, 0x100 and 0x200 with 0x20 bytes each, i.e.
 * BG palettes 0 and 8 and OBJ palette 0); 08236294 / 08235D30 / 08239228 are
 * Decompress sources; 080A29A4 goes to sub_08011E54 and 080A31A4 to
 * sub_08012B70; 086168BC is a proc script Proc_Start runs under the caller.
 * Non-const on the blobs because Decompress and sub_08011E54 both take plain
 * `u8 *` / `void *`. */
extern u8 gUnknown_08235D30[];
extern u8 gUnknown_08236294[];
extern u8 gUnknown_08239228[];
/* Wave 33, W33-E. The three assets sub_080800B0 loads, typed off the call it
 * hands each one to: 08235558 and 082352FC go to `Decompress(u8 *, void *)`,
 * and 08235D10 to `ApplyPaletteExt(u16 *, u32, u16)` for 0x20 bytes -- one
 * palette. Non-const for the same -Werror reason the other two families here
 * carry: both prototypes take non-const pointers. */
extern u8 gUnknown_08235558[];
extern u8 gUnknown_082352FC[];
extern u16 gUnknown_08235D10[];
extern u16 gUnknown_082391E8[];
extern u16 gUnknown_08239DE4[];
extern u8 gUnknown_080A29A4[];
/* Wave 39 (W39-A): retyped u8 -> u16. sub_08012B70 is its ONLY reader and the
 * only thing any caller does with it (c_080399F8.c, c_0807FE90.c,
 * c_08080498.c all just hand it over), and that function walks it as
 * halfwords: `ldrh` for the first word, then `ldrh`/`adds #2` through the
 * body. The leading halfword is a size header -- low byte width, high byte
 * height -- and the rest is one halfword per cell. Byte-neutral at all three
 * call sites, which pass only the base address; all three re-verified. */
extern u16 gUnknown_080A31A4[];
/* Wave 34 (W34-H). A RUN of 16-colour palettes, not a single one: sub_08039948
 * addresses it `lsls #5; adds` off the bare symbol with proc->unk54 as the
 * index and hands the element to ApplyPaletteExt(.., 0x100, 0x20), i.e. 0x20
 * BYTES per entry. `u16 []` because that is ApplyPaletteExt's first parameter,
 * which makes the index `* 0x10` in ELEMENTS -- the same reasoning as
 * gUnknown_081213F4. gUnknown_080A36C8 just below is this run's ENTRY 1
 * (0x080A36A8 + 0x20), carved under its own name by whichever function reads
 * that entry by itself; the two are not independent objects. Non-const:
 * ApplyPaletteExt takes a plain pointer. */
extern u16 gUnknown_080A36A8[];
/* Wave 34 (W34-H): sub_08039A5C's second tile blob, handed to
 * `Decompress(u8 *, void *)` by name with no arithmetic, so `u8 []` and
 * non-const -- Decompress takes a plain pointer. */
extern u8 gUnknown_080A534C[];
extern u16 gUnknown_080A36C8[];
extern const struct ProcCmd gUnknown_086168BC[];
/* Read once with `ldr` and handed straight to sub_08043BA4(int, ...) by
 * sub_08080498. `int` is the weakest model that the single use supports -- it
 * is never dereferenced here, so pointer-ness is unproved. */
extern int gUnknown_03005970;
/* Wave 30, W30-D. sub_08080E74 writes gUnknown_03005970 and gUnknown_03005904
 * with a plain `str` from its first two parameters and then starts
 * gUnknown_08616794 or gUnknown_08616844 depending on which of 1 / 2 the second
 * one is. Word-sized from the `str`; `int` for the same weakest-model reason
 * gUnknown_03005970 above carries -- neither is dereferenced anywhere yet. */
extern int gUnknown_03005904;
/* Wave 30, W30-D. The three proc scripts of the 0x08080xxx block.
 * gUnknown_08616794 / gUnknown_08616844 are the pair sub_08080E74 starts with
 * Proc_StartBlocking and sub_08080EB0 queries with Proc_Find -- an install /
 * query pair naming the same two symbols, which is what fixes them as scripts
 * rather than as blobs. gUnknown_086167EC is Proc_Find'd by sub_0808006C.
 * All three decode as ProcCmd triples in the ROM (086167EC opens
 * {0x1000e, 0, 2}; 08616794 and 08616844 both open {2, 0x8034f7d, ...},
 * a code pointer in the dataPtr slot). */
extern const struct ProcCmd gUnknown_086167EC[];
/* Wave 51, W51-G. A fourth script of the same block, and the one that fixes
 * gUnknown_086167EC's role: sub_0807FCF8 ends its animation by
 * `Proc_Start(gUnknown_08616814, PROC_TREE_3); Proc_BreakEach(gUnknown_086167EC);`
 * -- a start / break-each pair naming both symbols in one statement pair, the
 * same kind of evidence W30-D used. Proc_Start's first parameter is what fixes
 * it as a script, and the ROM bears it out: the words at 0x08616814 decode as
 * ProcCmd pairs {0x11, 0}, {3, 0x0808006D}, {9, 0x08616794}, {2, 0x08080095} --
 * two THUMB code pointers (T bit set) in dataPtr slots and a reference to the
 * already-declared gUnknown_08616794. The symbol is reached by VALUE (`ldr r0,
 * =gUnknown_08616814; bl Proc_Start`, one level), so it is a real object and not
 * a -fforce-addr word; 0x08616814 is not bound by aw2bhr.lds. */
extern const struct ProcCmd gUnknown_08616814[];
/* Wave 34, W34-E. Proc_Start'd by sub_0806AF44 with the caller's own proc as
 * parent; the child's +0x2c/0x30/0x34/0x38/0x39 are filled in immediately
 * afterwards, so this script's proc is at least 0x3a bytes. */
extern const struct ProcCmd gUnknown_085818F4[];
extern const struct ProcCmd gUnknown_08616794[];
extern const struct ProcCmd gUnknown_08616844[];
/* Wave 33, W33-E. Three more scripts of the same 0x08616xxx block, each reached
 * only by `Proc_Start(script, proc)`: 086167BC from sub_0807FA34 (started
 * alongside gUnknown_086167EC on the same proc) and 0861693C from sub_080806B0.
 * Proc_Start's first parameter is what fixes them as scripts. */
extern const struct ProcCmd gUnknown_086167BC[];
extern const struct ProcCmd gUnknown_0861693C[];
/* Wave 33, W33-E. NOT a script despite sitting between two of them: it is
 * sub_0807F8FC's second parameter, and that function walks it with
 * `adds r1, r7, r2; ldrb r1, [r1]` one byte at a time until a zero byte, so it
 * is a byte string. Non-const because sub_0807F8FC's parameter is `u8 *` --
 * both call sites (sub_0807FA88, sub_080805E0) pass it by name with no cast. */
extern u8 gUnknown_08616750[];
/* Wave 33, W33-E. A pair of ROM words holding POINTERS to u16 counters in RAM:
 * sub_0807FF78 and sub_0807FFF0 each do `ldr r2, [r0]; ldrh r1, [r2]` and then
 * store back through the same pointer, so the ROM word is the pointer and the
 * pointee is a halfword. 081D937C counts UP to 0x10 (sub_0807FF78) and 081D9380
 * counts DOWN to 0 (sub_0807FFF0), each moving gUnknown_03002B28 the other way
 * -- a blend-coefficient fade in and its mirror.
 *
 * The POINTEE is VOLATILE, and that is measured, not decorative: both functions
 * compare the halfword and then immediately increment it, and the ROM RE-loads
 * it (`ldrh r1,[r2]; cmp; ... ldrh r0,[r2]; adds #1; strh r0,[r2]`) where a
 * plain u16 lets GCC forward the compared value into `adds r0,r2,#1`. Measured
 * wave 33: non-volatile is -4 bytes on sub_0807FFF0 and +8 on sub_0807FF78 (the
 * forwarding frees a register, which changes the address allocation with it);
 * volatile is byte-exact on both. Same tell as gUnknown_03001FFC in hardware.h.
 * The POINTER itself is not volatile -- it is reloaded after the merge only
 * because the store through it may alias. */
extern volatile u16 *gUnknown_081D937C;
extern volatile u16 *gUnknown_081D9380;
/* Wave 34, W34-E. A POINTER cell in the same 0x081D9xxx run as the two above,
 * reached the same way: `ldr rN, =gUnknown_081D940C; ldr rM, [rN]; ldrb
 * [rM, #1]`. sub_08085F94 reads unk01 twice and compares it against 2 to pick
 * between the gUnknown_03005900/30 pair and the gUnknown_03005990/80 pair (the
 * note on gUnknown_03005900 describes that choice). Only +0x01 is pinned down;
 * `u8 filler_00[1]` holds the offset without claiming what is at +0x00. */
struct Unk081D940C
{
    /* 0x00 */ u8 filler_00[0x01];
    /* 0x01 */ u8 unk01;
};
extern struct Unk081D940C *gUnknown_081D940C;
/* Wave 30, W30-D. An EWRAM byte array indexed by gUnknown_084C30F8->unk01e in
 * sub_0804931C, whose element goes straight into sub_080487B4's fourth
 * parameter. `u8 []` from the `ldrb`; nothing writes it in matched code yet, so
 * neither the extent nor const-ness is proved. */
extern u8 gUnknown_02028E1C[];
/* Wave 34, W34-E. A 4-byte-stride EWRAM table -- sub_0806AF44 indexes it with a
 * proc's u16 field (`lsls #2`) and reads a u16 at offset 0, which it then uses
 * as the `muls #0x5c` row index into gUnknown_085C77A0[]. So each entry maps a
 * slot to a chapter/unit row. The address is NOT a pool word: aw2bhr.lds binds
 * `gUnknown_0202F214` at 0x02F214 already, this declaration only gives it a
 * type. Stride 4 is proved by the shift; only +0x00 is read anywhere matched so
 * far, so unk02 is a placeholder holding the stride rather than a claim. */
struct Unk0202F214 /* 0x04 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02; /* Wave 48 (W48-C): no longer just a stride
                           * placeholder -- sub_0806AD04 is the first reader of
                           * +0x02 and reads it TWICE, as two fields packed in
                           * one halfword: the low 2 bits are a mode it stores
                           * into its child proc's `int` +0x4c, and `unk02 >> 2`
                           * is a count it renders as three decimal digits.
                           *   TYPE DELIBERATELY UNCHANGED, and this is the
                           * finding rather than an omission. The two reads are
                           * `ldrb [.,#2]; lsls #0x1e; lsrs #0x1e` and
                           * `ldrh [.,#2]; lsrs #2`, which is exactly what a
                           * `u16 lo : 2; u16 hi : 14;` pair looks like -- and a
                           * bitfield is RULED OUT, not merely unproved. agbcc
                           * gives any struct containing a bitfield 4-byte size
                           * and alignment, which would take this element from 4
                           * bytes to 8 and every index from `lsls #2` to
                           * `lsls #3`; the one layout that keeps the stride (an
                           * all-bitfield element) loads the enclosing word and
                           * emits `ldr`, never `ldrb [.,#2]`. The two
                           * constraints cannot both hold. See the Bitfields
                           * chapter of docs/agbcc-codegen.md for the five
                           * layouts measured.
                           *   So the halfword is genuine and the narrowing
                           * lives at the USE: sub_0806AD04 spells the 2-bit
                           * read `(u32)(u8)e->unk02 << 30 >> 30`, which is
                           * byte-exact. Note `& 3` is NOT it -- that gives
                           * `movs #3; ands`. Signedness still unproved: both
                           * reads are unsigned-shaped, but nothing writes it in
                           * matched code. */
};
extern struct Unk0202F214 gUnknown_0202F214[];
/* Proc scripts handed to sub_080152EC(script, 3). */
extern const u8 gUnknown_08580CB4[];
extern const u8 gUnknown_08580CC4[];
/* sub_08073304's first argument -- the sibling of gUnknown_085802A4, which
 * sub_08065238 passes in the same position with the same 0x230 third argument. */
extern const u8 gUnknown_085802AC[];
/* Wave 46 (W46-F): the gUnknown_085802AC above had been described from
 * sub_08065238's call site before this symbol itself was declared. It is
 * sub_08073304's `const void *` first argument in sub_08065238, sub_0806530C
 * and sub_0806540C, all three with the identical (0x230, 0xf, 1, 1, 3) tail --
 * one shared ROM blob, not three. Element type copied from the sibling; only
 * "address of a const ROM object" is proved. */
extern const u8 gUnknown_085802A4[];
/* A word-sized row/line counter. sub_080876B4 is the only reader so far and
 * uses it purely arithmetically -- `gUnknown_03005928 * 16 + 0x28` (or +0x48),
 * stored into the u8 gUnknown_030020B8 -- so the 4-byte `ldr` fixes the width
 * and nothing yet fixes the sign. */
extern u32 gUnknown_03005928;

/* ---- wave 13 (A8), third block ----
 * 0x0808D800 -- one more slot of the `-fforce-addr` .rodata address-constant
 * block described on gUnknown_0808D6DC above, holding &gUnknown_0200B0B0.
 * NOT a global of the original source; the original says gUnknown_0200B0B0 and
 * agbcc parked the address here.
 *
 * NON-const, and that is the whole difference between a match and a near-miss:
 * sub_080085E0 re-reads the ROM word AND the pointer at every use
 * (`ldr r0,[r4]; ldr r0,[r0]`) while keeping only the word's ADDRESS in r4.
 * A `**const` spelling -- the one gUnknown_0808D6DC needs, because that
 * function's reference lives across a loop -- lets agbcc CSE the outer read
 * into a register and drops one `ldr` per use site. Probed both ways in one
 * call against the target listing. */
extern struct Unk0200B0B0 **gUnknown_0808D800;

/* ---- wave 13 (A6) ----
 * The four-direction step tables, read together and indexed by the same 0..3
 * direction in sub_0800F564 (`lsls r3, r6, #1` is CSEd across both):
 *   gUnknown_0848894C = { -1, +1,  0,  0 }   dx
 *   gUnknown_08488954 = {  0,  0, -1, +1 }   dy
 * SIGNED and 16 bits wide -- both are read with `ldrsh rN, [base, rIdx]`, which
 * is the only THUMB form for a signed halfword and needs the scratch zero in a
 * register; the values in the ROM confirm the sign. Extent is 4 from the four
 * `dir` cases the one reader tests; the words after them at 0x08488958 repeat
 * the same two patterns, so this is one row of a longer table and the bound is
 * not proved. */
extern const s16 gUnknown_0848894C[];
extern const s16 gUnknown_08488954[];

/* ---- wave 13 (A7) ----
 * The debug status readout sub_08057464 (matched) paints. Every type below is
 * pinned by that one function and nothing else yet.
 *
 * The 0813617x-0813619C run is ASCII, not a compiler pool: the addresses are
 * handed to sub_080119A0 as its third argument and never dereferenced by the
 * caller, and the bytes read "C", "ATK", "ARMY", "SOLD", "WEP", "TER", "DEF".
 * Contrast the 08136050-081360E0 run just above them in data/data.s, which
 * holds RAM addresses, is loaded THROUGH (`ldr rN, [rM]`) and is agbcc's own
 * -fforce-addr constant pool -- see docs/agbcc-codegen.md.
 *
 * 08551A48/4C/50 are `u16 []`: indexed `* 2` and read with a bare `ldrh` whose
 * result goes straight into a `u16` parameter. The five 08551A60.. tables are
 * indexed `* 2 * 2` and read with `ldr`, and what comes out is sub_080119A0's
 * string argument, so they are pointer tables. 03004550 is `s16 []` -- every
 * one of its sixteen slots is read with `ldrsh`, including the eight that feed
 * sub_08011A20's unnarrowed `int` third parameter. */
extern u16 gUnknown_03004514;
extern u16 gUnknown_03004524;
/* Wave 37, W37-B. One halfword per gUnknown_0300453C side, `strh` of the
 * constant 1 by sub_08054500 off a bare `i * 2` from the symbol -- so the
 * symbol IS the array. Nothing matched reads it back, so the `strh` is the
 * whole evidence for the width and the signedness is unconstrained. */
extern u16 gUnknown_03004548[];
extern u16 gUnknown_03004540;
/* Wave 50, W50-F: RESHAPED from a flat [] to [][8]. sub_08052F84 subscripts it
 * as `[gUnknown_03004514][gUnknown_03004524]` with a `lsls #4` row term and a
 * `lsls #1` column term -- the flat spelling would have to add before shifting
 * and cannot produce that pair -- and then copies it element-for-element into
 * gUnknown_03004580, which is already `u16 [][8]`, with two nested loops
 * bounded at 2 and 8. The 16-byte row stride was already recorded below.
 * The reshape is byte-neutral: its one promoted reader, src/decomp/c_08057464.c,
 * used constant subscripts 0..15 and now uses [0][0]..[1][7] (re-verified). */
extern s16 gUnknown_03004550[][8];
/* Wave 50, W50-F. The per-column CEILING for gUnknown_03004550, same 2 x 8
 * shape: sub_08052F84 reaches it with the SAME byte offset it just used for
 * gUnknown_03004550 (one `adds r1, r1, base` off the shared index), wraps the
 * value past it back to 0 and wraps a negative value back up to it. 0x20 bytes
 * in data/data-0848B688.s, which is exactly 2 x 8 halfwords. Read `ldrh`. */
extern const u16 gUnknown_08551A28[][8];
/* Wave 45, W45-H. sub_08052F3C's ROM initialiser: it copies 2 x 8 halfwords
 * from here into gUnknown_03004550 with `ldrh`/`strh` and a 16-byte row stride,
 * so the element type is inherited from that already-typed destination. Extent
 * 16 is what the copy touches, not a proved bound. */
extern const s16 gUnknown_08551A08[];
extern u16 gUnknown_030045AC;
extern const u16 gUnknown_08551A48[];
extern const u16 gUnknown_08551A4C[];
extern const u16 gUnknown_08551A50[];
extern const char *const gUnknown_08551A60[];
extern const char *const gUnknown_08551A74[];
extern const char *const gUnknown_08551AD4[];
extern const char *const gUnknown_08551AE0[];
extern const char *const gUnknown_08551B8C[];
extern const char *const gUnknown_08551B98[];
extern const char gUnknown_08136170[];
extern const char gUnknown_08136174[];
extern const char gUnknown_08136178[];
extern const char gUnknown_08136180[];
extern const char gUnknown_08136188[];
extern const char gUnknown_0813618C[];
extern const char gUnknown_08136190[];
extern const char gUnknown_08136194[];
extern const char gUnknown_08136198[];
extern const char gUnknown_0813619C[];

/* Two free-running frame counters, each bumped once per call by sub_08021DD8
 * and immediately re-read for a `% 0x70` / `% 0x64` phase test. VOLATILE, and
 * that is measured rather than assumed: without it CSE satisfies the modulo's
 * operand from the register the increment just stored and the `ldr rN, [rM]`
 * after the `str` disappears, costing two bytes each. Nothing between the
 * store and the load can alias them, so `volatile` is the only reason agbcc
 * would reload. */
extern volatile u32 gUnknown_03004078;
extern volatile u32 gUnknown_030043F0;
/* WORD-WRITTEN, HALFWORD-READ -- declared u32 because the writers are what is
 * matched, and a u16 declaration would give them `strh`. Every one of the
 * eleven writers is `str` of a value the prologue narrowed with
 * `lsls #24; lsrs #24`, i.e. a u8 parameter widened to a word (family F045:
 * sub_0802C5B8, sub_0802C5D4, sub_0802C7DC, sub_0802CD28, sub_0802CE54,
 * sub_0802CE94 and neighbours), plus one `str r4` zeroing pair in sub_08021554
 * and sub_08034810. The only two READERS, sub_0802D4A0's body at 0x0802D4D8
 * and its twin at 0x0802D52C, do `ldrh` and pass the result as the fifth
 * (stacked) argument of sub_08019F2C(menuDef, ...) -- so whoever matches those
 * two needs a `(u16)` cast or a union here, NOT a narrower declaration.
 * The value is a menu-item selector: the tables handed to sub_08019F2C
 * alongside it are gUnknown_0849AC60 and gUnknown_0849ABC0, 0x20-byte records
 * whose +0x14/+0x18 callback slots hold the F045 functions themselves.
 * Wave 14. */
extern u32 gUnknown_030040F0;
/* The byte source sub_080344F0 packs into the command block: it copies [0],
 * [1] and [2] into the block's +7, +0x0c and +0x0d, then runs a four-iteration
 * pointer loop from [4] into +0x0e. Plain `ldrb` throughout and the loop
 * indexes it with a variable, so it is a u8 array at least 8 long. */
extern u8 gUnknown_03004490[];
extern u32 gUnknown_030044A0;
/* NOT OBJECTS -- these two are THUMB FUNCTION POINTER VALUES, and they are
 * here only because a `.c` may not declare its own externs.
 *
 * Both appear exactly once, as the pool word of a `ldr r1,=X; bl _call_via_r1`
 * indirect call (sub_0801B6EC and sub_0801B6FC). The addresses are ODD, which
 * is what a stored THUMB entry point looks like, and they are in IWRAM. No
 * object is allocated at either: aw2bhr.lds defines them because the SPLITTER
 * saw the pool word, and aw2bhr.map shows *fill* across the range. So the
 * original source had a constant or a macro, and `&gUnknown_0300619D` is a
 * spelling forced by the relocation the split produces -- see the comment in
 * work/sub_0801B6EC/. Do not read these as evidence that IWRAM holds a
 * variable at an odd address, and do not give them a wider type: the only
 * legal use is taking the address and casting it to a function pointer.
 * The pointee is code copied into IWRAM at run time; nothing in the ROM's
 * .text writes either address, so what lands there is not yet identified.
 * Wave 14. */
extern u8 gUnknown_0300619D;
extern u8 gUnknown_03006511;

/* Three more blobs of the 0817D874 group, all handed to sub_080718F8 as its
 * second argument by sub_08068BE4 (matched) exactly as 0817D874 is by its
 * sibling. Same `u8 []` model and the same reason for not being const --
 * sub_080718F8's declared second parameter is a plain `u8 *`.
 *
 * gUnknown_08581414 is SIGNED bytes and that is proved, not assumed: both
 * elements sub_08068BE4 reads end up in a `u16` argument, and the ROM
 * sign-extends each one to 32 bits first (`ldrb; lsls #24; asrs #24` for the
 * first, a bare `ldrsb` for the second) before re-narrowing to 16. A `u8 []`
 * would have gone straight from `ldrb` to the argument with no extension at
 * all. */
extern u8 gUnknown_0817D6FC[];
extern u8 gUnknown_0817D7B8[];
extern u8 gUnknown_0817D910[];
extern s8 gUnknown_08581414[];

/* Two adjacent halfword tables read by sub_0802216C. gUnknown_0809097C is a
 * base tile id per terrain class, indexed by `(v >> 6) + 1` with slot 0 used
 * for the v == 0x100 case, so at least 5 entries -- which is exactly the 10
 * bytes between it and gUnknown_08090986. gUnknown_08090986 is SIGNED: its
 * only use is `movs r2,#0; ldrsh r0,[r1,r2]; cmp r0,#0; bge`, a sign test that
 * a u16 table could not produce. */
extern const u16 gUnknown_0809097C[];
extern const s16 gUnknown_08090986[];

/* The 0x0804CEF8 unit-graphics loader's tables. Every type here is proved by a
 * candidate that compiles to bytes IDENTICAL to the ROM (see the wave-13 A7
 * report); only the relocation of agbcc's own .rodata pool word blocks that
 * function, so the layouts are as solid as a match.
 *
 * 08552178 is 10-byte rows -- `(i*4 + i) * 2` -- with u16 at +4 and +6, the
 * same shape as the RAM table gUnknown_02029668 declared above, and the two are
 * indexed in lockstep. The three 0855797x tables are 12-byte rows of three
 * words each (`(i*2 + i) * 4`): column 0 is a Decompress source, columns 1 and
 * 2 are sub_08015410's third and fourth arguments. 08552FB0 is a plain word
 * table indexed `i * 4` whose element is Decompress's destination.
 * 085533E4/085533FC/08553414 are 0x18 apart and are only ever named as
 * sub_08015410's first argument.
 *
 * WHAT IS IN THEM, dereferenced in baserom.gba in wave 17, because "opaque
 * blob" was hiding something useful. All five of these (the three above plus
 * gUnknown_085536A4 and gUnknown_085536BC below) are the SAME uniform 0x18-byte
 * record, and it is a table of CALLBACKS:
 *
 *   +0x00  an odd (THUMB) function pointer     +0x04  0x00020000
 *   +0x08  an odd (THUMB) function pointer     +0x0c  0x00010000
 *   +0x10  0x00000000  (a null terminator)     +0x14  0x00040000
 *
 * and the functions named are exactly the sprite setters in the surrounding
 * address range: 085533E4 -> sub_0804D818, 085533FC -> sub_0804D738,
 * 08553414 -> sub_0804D928 (matched), all three sharing sub_0804D8F8 in the
 * second slot; 085536A4 -> sub_08051DE0 (matched) and sub_08051F48;
 * 085536BC -> sub_08052154 and sub_08052270. So sub_08015410's first argument
 * is an animation descriptor, not a graphics blob, and these five records are
 * the reason those setters have no `bl` caller anywhere in `asm/`.
 * Declared `u8 []` anyway -- only the ADDRESS is ever used, the two u32s per
 * entry have no reader, and the record shape above is read off the data rather
 * than off any code. */
/* Wave 36, W36-C. A ROM halfword per script step, read
 * `gUnknown_08551E12[gUnknown_02029C00[i]]` and compared for equality against
 * the u16 gUnknown_03004508 -- the gate that decides whether sub_08053FBC
 * retires the command the cursor is sitting on. u16 from the `ldrh` and from
 * the unsigned compare against a u16 global. */
extern u16 gUnknown_08551E12[];
/* Wave 37, W37-B. The two ROM dispatch tables of the 0x08054xxx cutscene
 * player, one per channel: sub_08054278 and sub_08054488 pick a row with
 * `gUnknown_085D6A48[gUnknown_03004580[i][1]][2]`, load the word with `lsls #2`
 * and call it `bl _call_via_r2` with (side, command) already in r0/r1 -- the r2
 * index counts the TWO arguments here, and both are values the caller already
 * holds. Nothing narrows either at the call, so the parameter types are the
 * callers' own u16s. */
extern void (*const gUnknown_08553744[])(u16, u16);
extern void (*const gUnknown_0855374C[])(u16, u16);
/* Wave 37, W37-B. A halfword table sub_08054500 reads with
 * `gUnknown_02029A10[i].entries[j].unk00 * 2 + .unk01` -- the FACTORED index
 * (`(a*2+b)*2`), which is the flat-array form; a `[][2]` spelling would scale
 * the two subscripts separately. Only `== 1` is ever tested. u16 from the
 * `ldrh`. Extent unproved. */
extern u16 gUnknown_08553838[];
/* Wave 37, W37-B. Two CpuFastSet SOURCE blobs for the cutscene player's OBJ
 * VRAM at 0x06010000: sub_08054EE0 copies 0x288 words of gUnknown_08540FBC and
 * sub_08054F50 copies 0x348 words of gUnknown_0854E67C, both to
 * `0x06010000 + (u16)(side * 0x2000 + 0xa00)`. Only the symbol address is used,
 * so the element type is the weakest thing CpuFastSet's `const void *` accepts;
 * the word counts are transfer lengths, not proved extents. */
extern const u8 gUnknown_08540FBC[];

/* Wave 45 (W45-G).  gUnknown_08540FBC's opposite number for the third of the
 * three sub_08054E8C arms: sub_08055004 copies 0x2C8 words of it to
 * 0x06010000 + a1 * 0x2000 + 0x1400, exactly as sub_08054EE0 copies 0x288
 * words of gUnknown_08540FBC to the +0xA00 slot of the same block.  ROM, and
 * only ever a CpuFastSet source, so `const u8 []` for the same reason -- the
 * element type is unconstrained and the array form is what makes the bare
 * name the address. */
extern const u8 gUnknown_0855089C[];
extern const u8 gUnknown_0854E67C[];
/* Wave 37, W37-B. A ROM table of 20-byte rows of words, reached at a FIXED
 * +0x68 into the object: sub_08054F50 loads
 * `gUnknown_08553D80.unk68[gUnknown_03004580[i][0]][0]` as one pool word plus a
 * run-time `adds rB,#0x68`, with the row's `* 20` added after it and a ZERO
 * `ldr` displacement -- the struct-member-array form. The word is handed
 * straight to CpuFastSet as a source, hence `const void *`. Both extents are
 * unproved: one column of one row has a reader, and the 0x68 is a constant in
 * the address chain rather than a proved member boundary. */
/* Wave 51, W51-M. What filler_00 holds: ROWS OF FIVE 20-byte {length, source}
 * records, exactly the shape gUnknown_085D70A8 and gUnknown_085D7768 carry for
 * sub_0805521C / sub_0805530C. sub_08055288 subscripts them TWO-DIMENSIONALLY
 * -- a 100-byte outer stride off gUnknown_085D6A48[..][10] and a 20-byte inner
 * stride -- and reads +0 as a CpuFastSet length and +4 as its source, with the
 * `adds rB,#4` on the pool word that only the struct-member spelling produces.
 *
 * This CONFIRMS unk68 rather than competing with it: 0x68 == 4 + 5 * 20, i.e.
 * unk68[k][0] is row 1 column k's unk04, the same CpuFastSet source column read
 * with a flat index. unk68 is left exactly where it is -- src/decomp/c_08054E8C.c
 * reads it and reproducing that function's flat `adds rB,#0x68` through the 2D
 * member would take a `/5` and a `%5` it does not have.
 *
 * The outer extent 1 is a FLOOR, not a bound: sub_08055288's subscript is a
 * run-time table value and the rows plainly continue into unk68's territory.
 * It is written [1][5] so the member stops at 0x64 and nothing below moves. */
struct Unk08553D80Row /* 0x14 */
{
    /* 0x00 */ u16 unk00;      /* CpuFastSet length in BYTES: divided by 4 for
                                * the word count and by 0x20 for the tile
                                * advance, and only ever tested against 0. */
    /* 0x02 */ u8 filler_02[0x02];
    /* 0x04 */ const void *unk04;
    /* 0x08 */ u8 filler_08[0x0c];
};

struct Unk08553D80
{
    /* 0x00 */ struct Unk08553D80Row unk00[1][5];
    /* 0x64 */ u8 filler_64[0x04];
    /* 0x68 */ const void *unk68[8][5];
};
extern const struct Unk08553D80 gUnknown_08553D80;
extern u16 gUnknown_08552148[];
/* Wave 50, W50-F. A 0x50-byte ROM table (data/data-0848B688.s) of TWENTY THUMB
 * function pointers -- every word is odd and resolves to one of six routines:
 * sub_08052E04, sub_08051BEC, sub_08052718, sub_08051F4C, sub_08052BBC,
 * sub_080523E8. sub_080536D8 subscripts it with a `lsls #2` and calls the
 * result through `bl _call_via_ip` with three arguments, and sub_08052E04 is
 * already declared `void (u16, u16, int)`, which is the signature the whole
 * table takes -- three of the six others are the known callers of
 * sub_08052818, so the table is the per-slot action dispatch for one side. */
extern void (*const gUnknown_085535C0[])(u16, u16, int);
/* Wave 49, W49-M, block 0x08056. Two ADJACENT ROM tables of 0x28 bytes each --
 * 0x0855218C..0x085521B4 and 0x085521B4..0x085521DC -- holding a slot
 * PERMUTATION per side: 0,1,3,2,4 / 1,0,2,4,3 for the first and 0,1,3,4,2 /
 * 1,0,2,4,3 for the second, each value in a 4-byte cell whose upper halfword
 * is zero. sub_0805653C and sub_080560A4 read the low halfword `ldrh` at
 * `side * 20 + i * 4` and use it to subscript gUnknown_02029A10[side].entries,
 * so the elements are u16 with a 4-byte stride; 0x28 == 2 * 5 * 4 fixes both
 * extents exactly.
 *   The [5][2] spelling rather than a flat [10] is what produces the ROM's
 * address chain: `side * 20` expands as `(side * 4 + side) * 4` and the
 * intermediate `side * 5` is CSEd with gUnknown_02029C14[side]'s own
 * `side * 5` in sub_080560A4 -- a flat u16 table would have had no such
 * intermediate. Both are indexed by a computed subscript before any
 * dereference, so neither is a -fforce-addr pool word. */
extern const u16 gUnknown_0855218C[][5][2];
extern const u16 gUnknown_085521B4[][5][2];
/* Wave 49, W49-M. A ROM table of 4-byte rows read at a FIXED +400 halfwords:
 * sub_080560A4 binds `gUnknown_08554A00[side * 5 + slot]` to a pointer local
 * (the ROM spills that address to the stack and reloads it) and then reads
 * [200] and [201] off it into gUnknown_02029A10[side].entries[slot].x and .y
 * in consecutive statements. The +400/+402 are built as `movs #0xc8/#0xc9;
 * lsls #1`, i.e. halfword indices, and the pair at 0x08554B90 onwards reads
 * 63,107 / 51,129 / 28,92 / 8,115 / -4,149 -- plausible screen coordinates.
 * The row width and the 0xa50 extent are unproved; only the 4-byte stride and
 * the two columns 400 halfwords in are. */
extern const u16 gUnknown_08554A00[][2];
/* Wave 37, W37-J. One ROM halfword per (gUnknown_02029664 bit0 + bit3) sum,
 * i.e. an index in 0..2: sub_08050FF8 reads it with a bare `ldrh` off `n * 2`
 * from the symbol and drops the value straight into OamData.priority, so only
 * the low two bits are consumed. Extent unproved. */
extern u16 gUnknown_08552394[];
/* Wave 37, W37-J. A ROM halfword per gUnknown_0300453C side, `ldrh` off a bare
 * `lsls #1` from the symbol; sub_0804FFFC copies it into gUnknown_02029C0C[i]
 * (u16) in the same statement that sets gUnknown_02029C10[i] to 0x96. */
extern u16 gUnknown_0855357C[];
/* Wave 37, W37-J. Two ROM tables 4 bytes apart that sub_08051454 divides
 * element [1] of one by element [1] of the other (`ldrh [rN,#2]` on each, then
 * __udivsi3 -- an UNSIGNED divide, which is what keeps both u16). The quotient
 * becomes gUnknown_020298E0[side].unk8a. gUnknown_08553668 is 4 bytes further
 * on and is a per-(unk16 - 1) y offset added to a sprite coordinate in the same
 * function; all three extents are unproved. */
extern u16 gUnknown_08553660[];
extern u16 gUnknown_08553664[];
extern u16 gUnknown_08553668[];
/* Wave 37, W37-J. A per-side halfword pair, read `gUnknown_08553B58[side]` off
 * a bare `lsls #1`, and the four-halfword rows immediately after it, indexed
 * `gUnknown_08553B5C[gUnknown_085D6A48[..][4]][side]` with an 8-byte row stride
 * (`lsls #3`) and the side term folded into the same index register. Both are x
 * offsets summed into gUnknown_02029A10[..].entries[..].x by sub_08050FF8. The
 * [4] is the measured stride, not a proved extent; the two symbols overlap the
 * way gUnknown_03004580/gUnknown_03004582 do. */
extern u16 gUnknown_08553B58[];
extern u16 gUnknown_08553B5C[][4];
/* Wave 37, W37-J. Eight-byte ROM rows indexed by gUnknown_020298E0[side].unk18
 * (the 0..2 phase counter). sub_08050FF8 and sub_08051454 both read unk04 as
 * `adds rB,rIdx; ldrh [rB,#4]` -- the offset in the LOAD DISPLACEMENT, which is
 * the struct form; a flat `u16 [][4]` folds the +4 onto the symbol instead. The
 * value is a y offset added to gUnknown_02029A10[..].entries[..].y. Nothing
 * reaches the other three halfwords. */
struct Unk08553BFC /* 0x08 */
{
    /* 0x00 */ u8 filler_00[0x04];
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u8 filler_06[0x02];
};
extern struct Unk08553BFC gUnknown_08553BFC[];
/* Wave 37, W37-J. A ROM table of 0x24-byte rows, the row indexed by
 * `gUnknown_085D6A48[gUnknown_085D6A48[gUnknown_03004580[i][1]][0]][10]` -- the
 * two-step chain sub_080505A4 and sub_08050F24 already walk. The 36-byte stride
 * is `((m * 8) + m) * 4` at all four sites.
 *   unk08 is a MEMBER ARRAY indexed by gUnknown_0300453C and unk10 is a plain
 * scalar, and that asymmetry is the ROM's: sub_080506B0 reads the first as
 * `adds rB,#8` with the side term in the index register and a zero `ldrh`
 * displacement, and the second as a bare `ldrh [rB,#0x10]` with no side term at
 * all -- the same split struct Unk085D7E28 records. unk16/unk1c are a second
 * (x, y) pair read by sub_08051454, unk16 being the one that gets negated for
 * side 0. All u16 from bare `ldrh`s; the sign appears only at the use, as an
 * explicit `lsls #0x10; asrs #0x10` on the local. */
struct Unk08553C18 /* 0x24 */
{
    /* 0x00 */ u8 filler_00[0x08];
    /* 0x08 */ u16 unk08[2];
    /* 0x0c */ u8 filler_0c[0x04];
    /* 0x10 */ u16 unk10;
    /* 0x12 */ u8 filler_12[0x04];
    /* 0x16 */ u16 unk16;
    /* 0x18 */ u8 filler_18[0x04];
    /* 0x1c */ u16 unk1c;
    /* 0x1e */ u8 filler_1e[0x06];
};
extern struct Unk08553C18 gUnknown_08553C18[];
/* Wave 36, W36-C. Two ROM descriptors whose ADDRESS is used and never
 * dereferenced -- each is sub_08015410's `void *` first argument and nothing
 * else, in sub_080505A4's two arms. `u8 []` for the same reason every other
 * member of that family carries it: the width is unconstrained because no load
 * reaches inside. */
extern u8 gUnknown_08553610[];
extern u8 gUnknown_08553628[];
extern u16 gUnknown_08552178[][5];
/* Wave 33, W33-E. The two ROM tables sub_08050F24 chains: gUnknown_08553640 is
 * a halfword index table read at `gUnknown_03004580[c][2] * row[8]`, and its
 * element then subscripts gUnknown_085535B8, whose WORD element goes straight
 * into sub_08015410's `void *` first parameter -- which is what makes that one
 * an array of pointers rather than of scalars. */
extern u16 gUnknown_08553640[];
extern void *gUnknown_085535B8[];
/* Wave 33, W33-E. An EWRAM pair of word slots per side, stride 8: sub_08050F24
 * loads both off `c * 8` and hands them to sub_08015410's third and fourth
 * `void *` parameters. Pointee unknown -- the word width and the pairing are
 * what the two `ldr`s prove. */
extern void *gUnknown_02029A00[][2];
extern void *gUnknown_08557978[][3];
extern void *gUnknown_085579B4[][3];
extern void *gUnknown_085579F0[][3];
extern void *gUnknown_08552FB0[];
/* Two more of the `void *[3]` graphics/palette/animation record tables the
 * gUnknown_08557978 family is made of, and ONE table reached through two
 * symbols: 0x08557B94 is 0x08557B58 + 0x3c, i.e. row 5 of the same 12-byte
 * rows. sub_0804DB14 uses both in one function -- gUnknown_08557B58[A] for the
 * first sub_08015410 and gUnknown_08557B94[A] for the second -- and indexes
 * 08557B58 a third time by gUnknown_08562128[..] for a CpuFastSet source, so
 * the low rows and the high rows are genuinely separate records rather than
 * one array read at two biases. Columns: [0] a CpuFastSet/Decompress source,
 * [1] and [2] sub_08015410's third and fourth arguments -- the same
 * graphics/palette pair the 08557978 columns supply. */
extern void *gUnknown_08557B58[][3];
extern void *gUnknown_08557B94[][3];
/* Two more animation descriptors of the gUnknown_085533E4 kind: only the
 * ADDRESS is used, each is sub_08015410's first argument, and sub_0804DB14
 * passes 08553444 with the gUnknown_08557B58 record pair and 08553474 with the
 * gUnknown_08557B94 one. `u8 []` for the same reason as that family. */
extern u8 gUnknown_08553444[];
extern u8 gUnknown_08553474[];
extern u8 gUnknown_08562128[];
/* Wave 37, W37-J, from sub_0804FF44 -- the same CpuFastSet-source family as
 * gUnknown_08557B58 above, but a flat pointer table rather than 3-column rows:
 * `gUnknown_08552138[gUnknown_08562128[..]]` is `lsls #2` plus a word `ldr`
 * handed straight to CpuFastSet. baserom.gba holds 08504FD8, 085051D8,
 * 085053D8, 085055D8 at 0x08552138 -- four sources 0x200 apart -- and
 * gUnknown_08552148, already declared, starts immediately after, so the extent
 * is four. */
extern void *gUnknown_08552138[];
/* Wave 37, W37-J. Two more of sub_08015410's first/fourth argument pair, from
 * sub_0804FF44, which passes the ADDRESS of each and nothing else.
 * gUnknown_08553580 is an animation descriptor of the gUnknown_08553444 kind
 * (baserom.gba: 080404FD, 00020000, 080500D1, ... -- odd THUMB pointers
 * interleaved with counts), so `u8 []` for the same reason as that family.
 * gUnknown_08559420 is a table of pointers into its own neighbourhood
 * (08559414, 08559418, ...), which is why it is spelled as an array of void *
 * rather than u8 -- but only its base address is ever taken, so the element
 * type is inferred from baserom, not proved by a use. */
extern u8 gUnknown_08553580[];
extern void *gUnknown_08559420[];
/* Wave 34 (W34-A). A ROM table of POINTERS to 3x4 grids of (x, y) halfword
 * pairs, selected by gUnknown_020296B0[side].unk1c. Every stride is read off
 * sub_08050D44's address chain rather than guessed: the row index scales by 48
 * (`x*3` then `<<4`), the column index by 16 (`<<4`) and the innermost index by
 * 4 (`<<2`), so the pointed-to object is `struct Unk08553B1CPt [3][4]` and the
 * two halfwords come out of `ldrh [rN]` / `ldrh [rN,#2]` off one address.
 *
 * The pair is SIGNED on the consumer's evidence and not on the loads: both
 * halves are sign-extended (`lsls #0x10; asrs #0x10`) before being added to
 * gUnknown_02029A10[..].entries[..].x / .y, and sub_08050D44 negates the x half
 * outright for side 1. Nothing in the matched tree writes the table.
 *
 * Wave 36 (W36-C) tested u16 here and it is WORSE, not neutral: both consumers
 * hold each half in a 16-bit local, so the load is `ldrh` either way and the
 * member's signedness is invisible -- but retyping it forces the local to `int`
 * to keep the `ldrh`, and an `int` local makes every `(s16)` cast at a use fold
 * away (measured, sub_08050E08 went 52.8% -> 51.8% and -12 -> -28 bytes). s16
 * members with s16 locals is the only shape that reproduces the ROM's paired
 * sign extensions. Do not retype this. */
/* Wave 36, W36-C. A halfword per gUnknown_0300453C side, read
 * `gUnknown_08553B18[a]` with a bare `ldrh` off `base + a * 2` by sub_08050E08
 * and added to a coordinate taken out of the gUnknown_08553B1C grid when
 * gUnknown_03004580[a][2] == 2. u16 from the load; nothing signs it. The [2]
 * extent is bounded by the address: 0x08553B1C - 0x08553B18 = 4 = 2 * u16, so
 * it tiles exactly up to gUnknown_08553B1C and there is no room for a third. */
extern u16 gUnknown_08553B18[2];
/* WAVE 36 (orchestrator): the tag above was REFERENCED by this extern but its
 * body was never defined anywhere in the tree, so every draft that actually
 * dereferences gUnknown_08553B1C failed to compile with "dereferencing pointer
 * to incomplete type" while its siblings, which only pass the pointer around,
 * compiled and matched. That reads exactly like a bad decompilation and is not
 * one. Members are read off the evidence already recorded above: stride 4, two
 * halfwords at +0 and +2 (`ldrh [rN]` / `ldrh [rN,#2]` off one address), signed
 * because both halves are sign-extended (`lsls #0x10; asrs #0x10`) at every
 * consumer. See the note above for why u16 here is WORSE, not neutral. */
struct Unk08553B1CPt
{
    /* 0x00 */ s16 unk00;
    /* 0x02 */ s16 unk02;
};

extern struct Unk08553B1CPt (*gUnknown_08553B1C[])[3][4];
extern u8 gUnknown_085533E4[];
extern u8 gUnknown_085533FC[];
extern u8 gUnknown_08553414[];
/* Two more blobs of the same kind and reached the same way: sub_08051BEC names
 * 085536A4 and sub_08051F4C names 085536BC, each as sub_08015410's first
 * argument, and nothing else names either. Same `u8 []` model as the three
 * above, for the same reason -- the address is all that is used. */
/* Wave 33, W33-D. gUnknown_085536A4 - 0x18, i.e. the 0x18-byte
 * animation-descriptor record immediately before it, and the sixth of the same
 * family. sub_0805198C is its only reference and hands the ADDRESS to
 * sub_08015410's `void *` first parameter, exactly as sub_08051BEC does with
 * gUnknown_085536A4. Same `u8 []` model, for the same reason. */
extern u8 gUnknown_0855368C[];
extern u8 gUnknown_085536A4[];
extern u8 gUnknown_085536BC[];
/* Three more of the same 0x18-byte animation-descriptor record, each named as
 * sub_08015410's first argument and nothing else, so `u8 []` for the same
 * reason as the family above (wave 20, W20-C).
 *
 * Dereferenced in baserom.gba, the run 085536A4..0855371C is SIX consecutive
 * records and slot 0 / slot 2 of each name a (draw, install) pair of the
 * 0x08052 sprite cluster:
 *
 *   085536A4  sub_08051DE0 (matched) + sub_08051F48   <- sub_08051BEC
 *   085536BC  sub_08052154 (matched) + sub_08052270   <- sub_08051F4C
 *   085536D4  sub_08052154 (matched) + sub_08052358   <- sub_080520B8
 *   085536EC  sub_080524C0 (matched) + sub_08052650
 *   08553704  sub_0805297C (matched) + sub_08052AF4   <- sub_08052718
 *   0855371C  sub_08052CA4 (matched) + sub_08052E00   <- sub_08052BBC
 *
 * sub_08051F48 and sub_08052E00 are 4-byte `bx lr` stubs, so slot 2 is
 * optional; the arrow marks which function passes the descriptor to
 * sub_08015410, read off the assembly. 085536EC has no passer among the
 * matched or wave-20 set.
 *
 * That table IS why sub_08052650 and sub_08052AF4 have zero `bl` callers and
 * why they are byte-identical to each other: one C body, installed from two
 * different descriptors, so the two copies need distinct addresses. A zero
 * fan-in function in a block full of matched siblings is a CALLBACK, not a
 * dead function. */
extern u8 gUnknown_085536D4[];
extern u8 gUnknown_08553704[];
extern u8 gUnknown_0855371C[];
/* Wave 34, W34-B. Two more tables of the gUnknown_08557978 family -- 12-byte
 * rows of three words, `(i*2 + i) * 4` at every site -- and the two
 * animation descriptors that go with them, one pair per loader.
 * sub_0804C4A8 uses 08557680 with 0855333C and sub_0804C99C uses 08557CFC
 * with 0855339C, each in the identical four-step shape: column 0 is
 * Decompress's source, then the descriptor's ADDRESS is sub_08015410's first
 * argument with column 2 as its third and column 1 as its fourth.
 *   The 2-then-1 column order is read off the register assignment (r2 before
 * r3) and is the same in both functions, so it is the call's real order and
 * not an allocation accident. `u8 []` for the descriptors for the same reason
 * as the gUnknown_085533E4 family above -- only the address is ever used. */
extern void *gUnknown_08557680[][3];
extern void *gUnknown_08557CFC[][3];
extern u8 gUnknown_0855333C[];
extern u8 gUnknown_0855339C[];

/* sub_0806EB5C's screen-setup blobs. 0823BDE0 goes to ApplyPaletteExt, the
 * five 081Axxxx/08239FA4/0823A3D4 ones to Decompress, and 081A3E3C is
 * sub_080718F8's ROM-blob argument -- all `u8 []` and none const, for the same
 * Decompress(u8 *, void *) reason as the 0817Dxxxx group above. 085826E0 and
 * 08582C7C are opaque script/table addresses handed to sub_08073304 and
 * sub_08073FF4. 02010C50 is the RAM buffer sub_08073304 writes into.
 *
 * gUnknown_08582764 is a table of 8-byte rows with a u16 at +4, and the STRUCT
 * form is load-bearing rather than cosmetic: `u16 t[][4]` indexed `[i][2]`
 * folds the +4 into the symbol address (`add rB, #4` then the index), while a
 * struct member -- or a row pointer bound to a local -- keeps it as the
 * `ldrh rD, [rB, #4]` displacement the ROM has. See the wave-13 A7 report. */
/* Wave 32 (W32-C) names +0x00: sub_0806F000 reads it with `lsls rI,#3; add
 * rI,base; ldrh` off the same table, i.e. the row-0 halfword. */
struct Unk08582764 /* 0x08 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02; /* Wave 36, W36-K: carved out of filler_02[0x02].
                           * sub_0806F0EC reads it `ldrh` off the x8 index and
                           * both compares it against and assigns it to the
                           * proc's `int` +0x2c, and separately hands it to
                           * sub_0803B524 through an explicit `(s16)` cast
                           * (`lsls #0x10; asrs #0x10`) -- the cast is in the
                           * source, so the field itself is the unsigned
                           * halfword the `ldrh` says it is. Start offset
                           * unchanged, so the carve is byte-neutral. */
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u8 filler_06[0x02];
};

extern u8 gUnknown_0823BDE0[];
extern u8 gUnknown_081A3E28[];
extern u8 gUnknown_081A3E3C[];
extern u8 gUnknown_081A4000[];
extern u8 gUnknown_081A4450[];
/* The gUnknown_08581F7C twin of gUnknown_0202F200 -- same 0/1 mode flag set by
 * the same wrapper pair shape (sub_0806E160 stores 0, sub_0806E17C stores 1,
 * both immediately before `sub_080152EC(gUnknown_08581F7C, 2)`), and the same
 * evidence: five accesses, all bare `ldrb`/`strb` at displacement zero. */
extern u8 gUnknown_0202F2C8;
extern u16 gUnknown_0202F2D8;
extern u8 gUnknown_085826E0[];
extern u8 gUnknown_08582C7C[];
extern struct Unk08582764 gUnknown_08582764[];
/* Wave 32 (W32-C). A table of `u16 *` -- sub_0806F0A0 scales its index by 4,
 * `ldr`s the word and hands it straight to sub_0806F064's `u16 *list`
 * parameter. The index is the low byte of gUnknown_08582764[].unk00's sibling
 * field, so the element count is at most 0x100 and is not measured here.
 * gUnknown_08582C8C is one of the OAM blobs PutSprite takes (`u16 *`). */
extern u16 *gUnknown_0858273C[];
extern u16 gUnknown_08582C8C[];

/* The four saved-state words sub_0806ED7C copies into its proc, and the script
 * it starts afterwards. 0806ED7C is the resume twin of 0806EB5C: where EB5C
 * hard-codes `proc->unk30 = 1` and clears the halfword at 0202F2D8, ED7C loads
 * the same four proc fields out of this run of globals, which sit two bytes
 * apart in address order and match the fields' widths exactly:
 *
 *   0202F2CC  ldr  -> proc->unk2c   also the guard for the trailing Proc_Start
 *   0202F2D0  ldr  -> proc->unk30   and sub_0806F000's first argument
 *   0202F2D4  ldrh -> proc->unk34
 *   0202F2D6  ldrh -> proc->unk36
 *
 * Widths come from the load instruction and are corroborated by the proc
 * fields they feed, which sub_0806E5CC (u16 first parameter) reads back. The
 * two word ones are `u32` rather than a pointer type on the weakest-type rule:
 * 0202F2CC is only ever compared against zero and 0202F2D0 is only masked with
 * 1 and stored, so nothing dereferences either. 0202F2D8, declared above, is
 * the neighbouring halfword EB5C zeroes and ED7C leaves alone -- so this run is
 * 0202F2CC..0202F2D9 and nothing yet reaches 0202F2D2 or 0202F2DA. */
/* sub_0806C52C's two parallel graphics sets -- one per arm of a single
 * `if (sub_0803866C())`. The two arms are the same five calls with five
 * different symbols each, which is what fixes the types: the 081A2xxx/081A3Dxx
 * pair in each arm goes to ApplyPaletteExt so `u16 []`, the other three go to
 * Decompress(u8 *, void *) so `u8 []`, and none can be `const` because neither
 * prototype takes one (the -Werror reason recorded on the 0817Dxxxx group).
 * Sizes are unknown -- only the bare symbol is ever passed. The first
 * ApplyPaletteExt in each arm is (0, 0x200), i.e. ApplyPalettes(src, 0, 16), so
 * those two are at least 16 palettes; the second is ApplyPalette(src, 16). */
extern u16 gUnknown_081A21B4[];
extern u16 gUnknown_081A3D64[];
extern u8 gUnknown_0819C454[];
extern u8 gUnknown_081A1C50[];
extern u8 gUnknown_081A31C4[];
extern u16 gUnknown_0819C254[];
extern u16 gUnknown_081A3D44[];
extern u8 gUnknown_08195318[];
extern u8 gUnknown_0819BCF0[];
extern u8 gUnknown_081A2A04[];

extern u32 gUnknown_0202F2CC;
extern u32 gUnknown_0202F2D0;
extern u16 gUnknown_0202F2D4;
extern u16 gUnknown_0202F2D6;
extern const struct ProcCmd gUnknown_08582C5C[];
/* Wave 36, W36-K. Two more proc scripts sub_0806F0EC starts:
 *   08582E54  Proc_StartBlocking, and the caller writes +0x58 of the returned
 *             proc with a word -- either the gUnknown_08582764 halfword or -1.
 *   08582C3C  Proc_Start, no field written. */
extern const struct ProcCmd gUnknown_08582E54[];
extern const struct ProcCmd gUnknown_08582C3C[];

/* sub_0806BB08's screen blobs (parked near-miss, 94.2% at exact size, so the
 * types below are as good as the ones above it -- only one CSE decision on the
 * constant 0 separates that candidate from the ROM). 081951F4/08195214 are
 * ApplyPaletteExt sources so `u16 []` and non-const; the rest are Decompress
 * or sub_080718F8 arguments so `u8 []`. 0858175C is Proc_EndEach's script and
 * 085819D4 is Proc_Start's; 085819E4 goes to sub_080670F8(const u8 *).
 *
 * 081D9424 is 0x12 bytes memcpy'd onto a stack `u16 [9]` by sub_080867BC and
 * indexed as halfwords there. */
extern u8 gUnknown_08191ADC[];
extern u8 gUnknown_08194614[];
extern u8 gUnknown_08194A9C[];
extern u8 gUnknown_0819507C[];
extern u16 gUnknown_081951F4[];
extern u16 gUnknown_08195214[];
extern u8 gUnknown_08195234[];
extern u8 gUnknown_081952D4[];
extern u8 gUnknown_081B9A38[];
extern const u16 gUnknown_081D9424[];
extern const struct ProcCmd gUnknown_0858175C[];
extern const struct ProcCmd gUnknown_085819D4[];
extern const u8 gUnknown_085819E4[];

/* The five ROM blobs sub_08040CA4 hands to the graphics helpers. Each is typed
 * from its consumer and not from its address: 081214B4 goes to
 * `Decompress(u8 *, void *)` and 0812189C to `ApplyPaletteExt(u16 *, ...)`, so
 * neither can be `const`; 08121344, 08121870 and 081240BC are the three
 * animation scripts sub_0801C210 takes as `void *`. Sizes are unknown -- the
 * function only ever passes the bare symbol. */
extern u8 gUnknown_08121344[];
extern u8 gUnknown_081214B4[];
extern u8 gUnknown_08121870[];
extern u16 gUnknown_0812189C[];
extern u8 gUnknown_081240BC[];

/* wave 13 (A5), all six from sub_0805D438 -- the per-step body of the
 * gUnknown_030046B0 script interpreter.
 *
 * gUnknown_030044D8 is a gate: the step returns immediately unless it is zero.
 * gUnknown_03004680 has only its ADDRESS taken, as sub_08071908's sole
 * argument, so `u8 []` is the weakest model that still gives the clean pool
 * word -- same reasoning as gUnknown_084C38BC. */
/* Wave 34, W34-C ADDS the volatile. sub_080335CC reads it into a local and the
 * ROM materialises the load into a scratch and then COPIES it to the
 * callee-saved register (`ldrb r0,[r0]; adds r5,r0,#0`); a non-volatile read
 * lets gcc load straight into r5 and is 2 bytes short. That extra copy is the
 * volatile-read signature -- the MEM cannot be merged into the consuming insn.
 * The plain `= 0` / `= 1` stores in sub_0803355C and src/decomp/c_08033638.c are
 * byte-identical either way (a scalar volatile store is a bare `strb`); both
 * were re-verified with try_match after the change. */
extern volatile u8 gUnknown_030044D8;
/* Wave 31, W31-B. Two RAM blocks reached only by ADDRESS, so `u8 []` is the
 * weakest model that still gives the clean pool word -- the gUnknown_03004680
 * reasoning just above.
 *
 * gUnknown_03003F70 is the link-session record sub_08033230, sub_08033404 and
 * sub_08033470 hand to sub_08062FF4 as its only argument; that function reaches
 * +0x14..+0x4b of it and nothing else names the type.
 *
 * gUnknown_03004400 is at least 0x80 bytes -- sub_0802F03C clears [0..0x7f] --
 * and unknown-functions.h already records it reaching sub_080309AC's `void *`
 * first parameter, so no one struct type covers it. sub_08033678 reads its
 * first four bytes with `ldrb`. */
extern u8 gUnknown_03003F70[];
/* Wave 34, W34-C. Three cells sub_08033194 seeds from one ROM blob and
 * sub_08033470 consumes: gUnknown_030032DC is the blob's base, gUnknown_03003F28
 * its byte LENGTH (`gUnknown_085867D8 - gUnknown_08584CE8`, a bare `subs` with
 * no scaling, which is what fixes both endpoints as byte-addressed) and
 * gUnknown_03003F44 a fixed +0x2b0 into it that goes straight to Decompress's
 * `u8 *` source. sub_08033470 hands base+0xc0 and length-0xc0 to sub_08063454. */
extern u8 *gUnknown_030032DC;
extern int gUnknown_03003F28;
extern u8 *gUnknown_03003F44;
/* Wave 34, W34-C ADDS the volatile. sub_0803355C packs four bytes into [0..3]
 * before handing the buffer to sub_080308B4, and EVERY one of those four plain
 * `strb`s is preceded by a dead `ldrb` of the very address being stored to --
 * the same tell that settled struct Unk0849B018's members and
 * gUnknown_030044C4. Declared non-volatile the function is 8 bytes short, two
 * per store, and nothing else closes the gap.
 *
 * Byte-neutral at the only promoted reader (sub_08033678 in
 * src/decomp/c_08033638.c, four `ldrb`s), but that file also passes the array
 * to sub_080309AC's `void *`, which now needs an explicit cast to drop the
 * qualifier. Both functions in it were re-verified with try_match after the
 * change. */
extern volatile u8 gUnknown_03004400[];
extern u8 gUnknown_03004680[];
/* Two 1-bit flags at bits 0 and 1 of byte 0. BITFIELDS rather than a scalar
 * mask, and the evidence is the constant form: sub_0805D438 clears them with
 * `movs #2; rsbs` and `movs #3; rsbs`, whereas a scalar `&= ~1` narrows to a
 * bare `movs #0xfe`. That is the `mov #N; neg` tell recorded in the bitfield
 * rules of docs/agbcc-codegen.md. */
struct Unk030045CC
{
    /* 0x00 */ u8 unk00_0:1;
               u8 unk00_1:1;
               u8 :6;
};
extern struct Unk030045CC gUnknown_030045CC;
/* Wave 45, W45-F. A whole word: sub_0805BD40 reads it with a plain `ldr` and
 * compares it for equality against its own fourth (register) argument, and
 * rejects the candidate cell when the two are equal -- i.e. it is a
 * currently-selected id that the reachability probe must skip. Only ever
 * loaded and compared, so nothing constrains it beyond the width; `int` is
 * the weakest model that fits, and the signedness is unproved because `beq`
 * says nothing about it. */
extern int gUnknown_030045C8;
/* sub_0805D438 zeroes +0x00, +0x13, +0x06 and +0x07 in that order when it
 * begins a step, and gates its whole tail on +0x13 afterwards. Nothing else
 * about the object is proved; every filler is the gap between two proved
 * offsets. */
/* Wave 31 (W31-C): unk01/unk02/unk03 are three more plain `ldrb`s, read
 * together by sub_080600F0 as sub_08025E08(unk02, unk03, unk01) and by
 * sub_08060474 as the first two arguments of sub_08042C24. Byte-wide loads
 * only, so nothing narrower than u8 is possible and nothing wider fits. */
struct Unk030046C0
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 unk03;
    /* 0x04 */ u8 filler_04[0x02];
    /* 0x06 */ u8 unk06;
    /* 0x07 */ u8 unk07;
    /* 0x08 */ u8 filler_08[0x04];
    /* Wave 49, W49-E. sub_08060170 snapshots unk07, unk0c, unk0d and then
     * unk0e[0..3] into gUnknown_03004490[0..2] and [4..7]. unk0c/unk0d are
     * plain `ldrb r0,[r2,#0xc]` / `[r2,#0xd]` off the struct base, so they are
     * two scalars, not part of the array; the array itself is proved by the
     * loop, which hoists `adds r2,#0xe` as its base and walks it with
     * `adds r2,#1` for four iterations (`cmp r3,#3; ble`). Extent 4 is from the
     * trip count. Widths from the loads; signedness unsettled, nothing
     * compares any of them. */
    /* 0x0c */ u8 unk0c;
    /* 0x0d */ u8 unk0d;
    /* 0x0e */ u8 unk0e[0x04];
    /* 0x12 */ u8 filler_12[0x01];
    /* 0x13 */ u8 unk13;
};
extern struct Unk030046C0 gUnknown_030046C0;
/* Wave 32, W32-A. A whole-word `str` of 0 in three of the 0x08060 block's
 * state handlers (sub_080601F0, sub_08060264, sub_080602C4), always paired with
 * the gUnknown_030045D4 state advance right after a sub_08029088 call, and a
 * whole-word `ldr`/`adds #1`/`str` counter in sub_08060324, sub_08060384 and
 * sub_080603D4. SIGNED: those three re-read it after the increment and compare
 * `cmp r0, #0x1e; bgt`, and `bgt` is the signed condition -- an unsigned
 * counter would have taken `bhi`. The `= 0` writers are byte-identical either
 * way, so the compare is the only evidence and it is decisive.
 *
 * VOLATILE, and that is measured too: all three re-read it with a fresh
 * `ldr r0, [r1]` IMMEDIATELY after the `str` that increments it, for the
 * compare. A plain `int` keeps the stored value in r0 and drops that load --
 * 4 bytes short in each of the three. A re-read surviving a store to the same
 * address is docs/agbcc-codegen.md's volatile tell, and nothing else here
 * could produce it.
 *
 * The ROM words the disassembly calls gUnknown_0816DAD4 and gUnknown_0816DAD8
 * hold 0x030046D4 and 0x030046C0 -- verified against baserom.gba -- so they are
 * agbcc's own -fforce-addr address constants for this counter and for
 * gUnknown_030046C0, NOT objects in ROM. sub_08060894's apparent double
 * indirection (`ldr r0, [r5]; ldr r1, [r0]`) is one of them plus the volatile
 * read. Same reading as gUnknown_0808E540 and gUnknown_08136030 above. */
extern volatile int gUnknown_030046D4;
/* Wave 32, W32-A. A whole-word `ldr`, tested against 0 and then used as the
 * divisor of `gUnknown_030046D4 * 100` in sub_08060894 -- `__divsi3`, so
 * signed. Nothing writes it in the matched tree yet. */
extern int gUnknown_03004674;
/* Wave 47, W47-E. The AI's per-unit-type score array, written by sub_08060F00
 * (a scaled ratio, or 0xFF when the type's weight is zero) and by sub_08060F74
 * (0xFF to veto a type), and read back by sub_08060FFC. Index 1..24 in both
 * writers -- `ldr rN,=gUnknown_03004640; adds rM,rN,#2` seeds the giv -- so
 * element 0 is never touched and 25 is the extent the writers prove.
 *
 * VOLATILE, and this one is measured rather than inferred. All four `strh`s
 * across the two functions are preceded by an `ldrh` of the SAME address whose
 * result is immediately dead (it lands in a register the next instruction
 * overwrites, or in a scratch nothing reads). A plain `u16` array emits the
 * bare `strh`; `volatile u16` emits `ldrh` then `strh` for a simple assignment,
 * with no source-level read anywhere. Probed in isolation -- see the
 * "Volatile narrow stores" chapter in docs/agbcc-codegen.md. Note the contrast
 * with the volatile SImode gUnknown_030046D4 above, whose store in sub_08060718
 * is a bare `str`: the dead load is specific to the narrow store. */
extern volatile u16 gUnknown_03004640[];
/* Wave 47, W47-E. A whole-word counter incremented in sub_08060718's 64-cell
 * sweep once per cell whose gUnknown_08499594 record has a unit type that
 * gUnknown_08576877 classes non-zero -- the "units that matter" tally beside
 * gUnknown_03004674's "occupied cells" tally, which the same loop keeps. Both
 * are cleared together by one chained `a = b = 0`. `ldr`/`adds #1`/`str`, so
 * word-wide; nothing compares it in matched code, so the signedness is unproved
 * and `int` is the weakest fit (gUnknown_03004674 beside it is already `int`). */
extern int gUnknown_030045D0;
/* Wave 47, W47-E. A 25-byte ROM classification table indexed by a unit type
 * byte out of gUnknown_08499594[i].unk00, read by sub_08060718 as
 * `add r0, r8; ldrb r0, [r0]` with the symbol's address LICM-hoisted into r8 --
 * i.e. a bare `ldr rN, =sym`, which is what fixes the symbol as the array
 * itself rather than a pointer to one. The bytes at 0x08576877 are
 * 00 00 00 01 01 01 01 00 00 01 01 01 00 01 01 01 01 01 01 01 00 01 01 00 01,
 * i.e. a 0/1 flag over exactly the 1..24 type range the two AI scoring loops
 * walk, and 0x0857689C onwards is unrelated pointer data. Only tested against
 * zero, so nothing narrows it past `u8`. */
extern const u8 gUnknown_08576877[];
/* A ROM word holding a pointer to 12-byte records -- the stride is
 * `((n*2)+n)*4` in sub_0805D438, i.e. exactly 12. Only the +4 member has a
 * user: the step parks `&g[gUnknown_030040D8->unk00].unk04` in
 * gUnknown_03004784, so that member is an array rather than a scalar. */
/* Wave 32, W32-A: extent raised from 0x0c to at least 0x23 -- sub_08060A20
 * reads +0x22 as `adds r1, r0, #0; adds r1, #0x22; ldrb`, past `ldrb`'s 5-bit
 * displacement, so the split is addressing and not a member array. The first
 * three bytes are named at the same time: sub_08060894 reads +0 and +2 (and +1
 * after its second divide) with plain `ldrb`, all three as AI difficulty
 * thresholds compared against percentages. unk04[] keeps its array form --
 * elements 0..3 are the thresholds sub_0806096C, sub_080609B8 and sub_08060A20
 * test, and a displaced `ldrb` is byte-identical either way, so nothing here
 * discriminates array from members. */
struct Unk085766E0 /* >= 0x23 */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 unk03; /* Wave 31 (W31-C): `ldrb r0, [r0, #3]` in
                          * sub_08060D78, multiplied by the u16
                          * gUnknown_03004080 with a bare `muls` (both
                          * operands narrow, so shorten_binary_op keeps it a
                          * single instruction). */
    /* 0x04 */ u8 unk04[0x08];
    /* 0x0c */ u8 unk0c; /* Wave 51, W51-E. Carved out of filler_0c, which used
                          * to span 0x0c..0x0d -- same start offset, one byte,
                          * so byte-neutral for every existing reader.
                          * sub_0805EE40 and sub_0805EF00 both read it
                          * `ldrb [r0,#0xc]` off the loaded pointer and pass it
                          * as sub_08059B4C's SECOND argument -- exactly the
                          * slot sub_08059978 and sub_080598BC fill from
                          * unk04[6] and unk04[7]. So this is almost certainly
                          * unk04[8], i.e. the same percentage table is longer
                          * than it is declared. RECORDED, NOT ACTED ON:
                          * widening unk04 reshapes a member two matched
                          * functions already read, while carving the byte out
                          * of filler is byte-neutral for everyone. Plain
                          * `ldrb`, so unsigned by the load. */
    /* 0x0d */ u8 filler_0d[0x01];
    /* 0x0e */ u8 unk0e; /* Wave 36, W36-D. A multiplier: sub_08058F30 scales
                          * sub_08042D1C's result by it with a bare `muls` and
                          * stores the product as the u8 it then clamps to 0x78,
                          * so it is a per-unit damage/percentage factor. Plain
                          * `ldrb`; carved out of filler_0c, which used to span
                          * 0x0c..0x21. */
    /* 0x0f */ u8 unk0f; /* Wave 45, W45-F. Carved out of filler_0f, which used
                          * to span 0x0f..0x21 -- same start offset, one byte,
                          * so byte-neutral for every existing reader.
                          * sub_0805BE10, sub_0805BE54 and sub_0805BEF0 each
                          * read it `ldrb [r0,#0xf]` off the loaded pointer and
                          * pass it as the FOURTH argument of the
                          * gUnknown_030013EC indirect call, beside a
                          * 1/gUnknown_030046D4 selector -- so it is a small
                          * per-unit parameter of whatever that dispatch is.
                          * Plain `ldrb`, so unsigned by the load; signedness
                          * otherwise unproved. */
    /* 0x10 */ u8 filler_10[0x12];
    /* 0x22 */ u8 unk22; /* Wave 32, W32-A. sub_08060A20 compares
                          * sub_08057F54(7) against it with `ble` before the
                          * percentage test, so it is a COUNT floor rather than
                          * one of the unk04 percentages. Bare `ldrb`. */
};
extern struct Unk085766E0 *gUnknown_085766E0;
extern u8 *gUnknown_03004784;
/* Wave 44, W44-H. CONFLICT, recorded and NOT acted on: the extent above
 * (">= 0x23", wave 32) cannot be reconciled with the 12-byte stride the note
 * above cites for sub_0805D438, and two more subscripts confirm the stride --
 * sub_08061178 forms `base + i * 12 + 0x14` and sub_08061E98 forms
 * `base + unit->unk00 * 12 + 4` (the latter is exactly the
 * `&g[type].unk04` the note describes, and gUnknown_03004784 receives it).
 * A `sizeof` of 0x23 would rescale every one of those. The six promoted
 * readers only ever use `gUnknown_085766E0->m`, i.e. element 0, so sizeof has
 * never been exercised and either extent compiles them identically -- which is
 * why the conflict survived. Shrinking the tag to 12 would delete unk0e and
 * unk22 out from under c_08058BB4.c and c_0806096C.c, so the tag is LEFT ALONE
 * and the two subscripting functions cast to `u8 *` / to a file-local view
 * instead. Someone with the wave-32 assembly in front of them should decide
 * whether +0x0e and +0x22 are really element 1 and element 2 bytes. */
/* Wave 44, W44-H. The ROM pointer word four bytes past gUnknown_085766E0,
 * holding 0x0202EF78 (read out of baserom.gba). It addresses a
 * 0xFF-TERMINATED array of 4-byte records: sub_08061668 walks it with
 * `adds r1, #4` until unk00 == 0xFF, so the stride is proved by the walk and
 * the terminator by the exit test. unk00/unk01 are a coordinate pair -- both
 * are `ldrb`ed and `strh`ed into the caller's u16 pair -- unk02 is matched for
 * equality against gUnknown_0857680F[gUnknown_030046C0.unk06], and unk03 is a
 * cost/priority compared with `bhs` (unsigned) and then overwritten with 0xFE
 * on the winning record, i.e. "mark consumed".
 * The disassembly's gUnknown_0816DAFC is agbcc's -fforce-addr pool word for
 * THIS symbol (0x0816DAFC holds 0x085766E4, verified against baserom.gba) and
 * must not be declared -- same species as the gUnknown_0816DB00..0C run
 * documented beside gUnknown_085771C4. */
struct Unk085766E4
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01;
    /* 0x02 */ u8 unk02;
    /* 0x03 */ u8 unk03;
};
extern struct Unk085766E4 *gUnknown_085766E4;
/* Wave 44, W44-H. A byte accumulator: sub_08061F34 clears it and then ORs one
 * bit-mask byte into it per map cell that sub_0802700C rejects, with the
 * `ldrb; orrs; strb` read-modify-write. Its twin is the already-declared
 * gUnknown_030046B8, which the same loop accumulates unconditionally -- so the
 * pair is "reachable terrain kinds" vs "all terrain kinds". `u8` from the
 * `ldrb`/`strb`; nothing compares it in matched code, so nothing narrows the
 * signedness further. */
extern u8 gUnknown_030045C0;
/* Wave 44, W44-H. A ROM byte table indexed by a terrain cell's low five bits
 * (`ands #0x1f`) in sub_08061F34, whose result is halved (`lsrs #1`) and used
 * as the subscript of a 5-entry {0,0,1,2,4} bit-mask array -- so it maps a
 * terrain kind to twice a small class index, 0..8. The bytes at 0x085767F2 are
 * 00 00 00 00 00 00 02 00 00 00 04 06 00 00 02 ... and the largest seen is 8,
 * which is consistent with that 0..4 subscript range. Extent unbounded: only
 * the masked subscript reaches it, and 0x1f is a mask, not a proved length. */
extern const u8 gUnknown_085767F2[];
/* Wave 44, W44-H. A five-byte ROM template that sub_08061F34 copies onto its
 * own stack with `sub_0808B6E8(buf, gUnknown_0816DB20, 5)` -- the same idiom
 * as gUnknown_0816E0C8 above and the dozen other stack-copy sources in this
 * header. The bytes read 00 00 01 02 04 out of baserom.gba, and the loop
 * subscripts the copy with `gUnknown_085767F2[terrain & 0x1f] >> 1`, so they
 * are the bit masks that indexing table selects between.
 *   This is a REAL ROM object (data.s defines the symbol) and NOT one of the
 * -fforce-addr pool words at 0x0816DB00..0x0816DB1C that the gUnknown_085771C4
 * note warns against declaring -- those hold addresses of other globals; this
 * one holds data. Dereference the candidate in baserom.gba before deciding
 * which kind a 0x0816Dxxx word is; this block contains both. */
extern const u8 gUnknown_0816DB20[5];

/* Three more agbcc `.LC` constant-pool words, NOT globals -- same species as
 * gUnknown_08090D88 above, found in the 0x08136050-0x081360E0 block. They hold
 * &gUnknown_03001FBC, &gUnknown_0300451C and &gUnknown_0300453C respectively.
 *
 * CORRECTION (wave 19, W19-B). This entry used to say "use them the way
 * gUnknown_08090D88 is used: bind `&gUnknown_081360Ax` to a local and read
 * `**local`. Spelling the underlying global directly compiles to the right
 * BYTES but the wrong relocation, because agbcc then builds its own unplaceable
 * pool word." Both halves are now wrong. The word is placeable since wave 18
 * (tools/split_rodata.py), and naming the globals directly DOES make agbcc emit
 * all three words, in the ROM's own slot order -- but only once the function's
 * two-bit guard is spelled so that it does not fold into one test. With the
 * `&&` spelling agbcc emits no `.rodata` at all and the reroute is invisible;
 * with the nested-`if` spelling the three words appear. So the reachability of
 * a `.LC` word is decided by the CANDIDATE's control flow, not by whether the
 * object behind it is a scalar -- see docs/agbcc-codegen.md. sub_0804F18C is
 * still the worked case and is still open, but on register allocation now, and
 * its `c_local` draft is kept only as _clocal_nearmiss.c.
 *
 * These three declarations are therefore unused by any draft and are kept only
 * as documentation of what lives at those addresses. */
extern s16 *const gUnknown_081360A0;
extern u16 *const gUnknown_081360A4;
extern u16 *const gUnknown_081360A8;
/* TWO halfwords, not one. sub_0804F18C writes [0] with a bare `strh` (it
 * stores OamData.priority into it straight after setting that field), and
 * sub_08050958 writes BOTH -- `strh r3,[r6]` and `strh r0,[r6,#2]` off a
 * single pool word for gUnknown_0300454C -- with the pair copied out of
 * gUnknown_03001470[proc].unk30 / .unk34, i.e. the current side and slot. It
 * also reads [1] back (`ldrh r0,[r6,#2]`). One symbol reached at +2 is an
 * array or an aggregate, never two globals; `u16 []` is the weaker of the two
 * and nothing distinguishes them here. Was `extern u16 gUnknown_0300454C;`;
 * no promoted file names it, so nothing needed re-verifying. */
extern u16 gUnknown_0300454C[];
/* Wave 34, W34-L, all from sub_08053434 / sub_080534A0.
 * gUnknown_030045A4 and gUnknown_03004510 are PAIRS, not scalars: sub_08053434
 * fills [0] and [1] of each from the two rows of gUnknown_03004580, and
 * sub_080534A0 subscripts gUnknown_030045A4 with gUnknown_0300450C. Same
 * `strh r0,[r3]` / `strh r0,[r3,#2]` off one pool word that settled
 * gUnknown_0300454C above. */
extern u16 gUnknown_030045A4[];
extern u16 gUnknown_03004510[];
/* A frame or step counter: sub_080534A0 does `ldrh; adds #1; strh` and elsewhere
 * masks it with 1 to pick a column, and sub_08053434 zeroes it. Halfword from
 * the read-modify-write; signedness unproved, nothing sign-extends it. */
extern u16 gUnknown_03004530;
/* Wave 50, W50-F: SIGNED, retyped from u16. sub_080532D8 loads it `ldrsh` with
 * no narrowing context, compares it against -1 and only then passes it to
 * sub_080153F0/sub_08015328 -- the same "-1 is no proc" guard those two get from
 * struct Unk02029A10's s16 unk18. As a u16 the `!= -1` test could never be
 * false and gcc would have folded the branch away. Its one writer,
 * src/decomp/c_08053434.c, stores gUnknown_03001FBC with `strh` either way, so
 * the retype is byte-neutral there (re-verified). */
extern s16 gUnknown_03004570;
/* Two parallel 11-entry ROM u16 tables 0x16 bytes apart, both indexed by
 * gUnknown_03004580[side][5] and copied into the pairs above. */
/* A third table of the same 11-entry shape, 8 bytes below gUnknown_085537F4.
 * sub_080531D4 reads it through a second indirection,
 * gUnknown_085537EC[gUnknown_030045A0[gUnknown_0300450C]]. */
extern const u16 gUnknown_085537EC[];
extern const u16 gUnknown_085537F4[];
extern const u16 gUnknown_0855380A[];
/* Wave 34, W34-L. sub_080534A0 reads this with `ldrsh` at a byte offset of
 * `v * 12 + gUnknown_03004520 * 4 + (gUnknown_03004530 & 1) * 2`, i.e. halfword
 * index `v * 6 + x * 2 + y` -- so the extents are [3][2] and not a flat table.
 * SIGNED on the `ldrsh`, which is also the only evidence of the width. */
/* Wave 36, W36-C. The 8 bytes immediately before gUnknown_085643B0, holding two
 * halfword pairs on the same "alternating phase bit" index gUnknown_085643B0's
 * innermost subscript uses. sub_08053520 reads unk04[phase & 1] with a bare
 * `movs r1,#0; ldrsh r0,[r0,r1]` into sub_0803B48C's s16 parameter, which is
 * what signs it. A STRUCT and not `s16 [][2]` with a constant outer index: the
 * ROM adds the 4 to the BASE register at run time (`adds r1,#4`) and leaves the
 * `ldrsh` index at zero, which only a struct member array gives -- the flat
 * spelling sinks the same constant into the pool word's relocation. Same
 * distinction struct Unk085D6C88 records. unk00 has no reader yet. */
struct Unk085643A8 /* 0x08 */
{
    /* 0x00 */ const s16 unk00[2];
    /* 0x04 */ const s16 unk04[2];
};
extern struct Unk085643A8 gUnknown_085643A8;
extern const s16 gUnknown_085643B0[][3][2];
/* A ROM halfword per side, indexed [gUnknown_0300453C]. Two readers,
 * sub_0804F658 and sub_0804E584, and both do nothing but pass it as
 * sub_0804BCB8's third argument. `u16 []` rather than `s16 []`: the `ldrsh` at
 * both sites is the conversion to that parameter's narrow SIGNED type, not the
 * object's own width -- combine folds sign_extend(truncate(zero_extend(mem:HI)))
 * into one sign-extending load, so `ldrsh` says nothing about the table here.
 * Extent unproved; only [gUnknown_0300453C] is ever reached. */
extern u16 gUnknown_0855214C[];
/* ROM u16 pairs indexed [gUnknown_0300453C][gUnknown_0300450C] -- `i * 4 + j * 2`
 * off the symbol, the same [2]-wide row shape as gUnknown_0855239C. The value is
 * an x offset: sub_0804F18C sign-extends it and adds it to a sprite x. */
extern u16 gUnknown_085644E0[][2];
/* The u16 ROM table gUnknown_02028E5C's column 1 indexes, named in that
 * global's own comment since wave 13 and declared here in wave 18 from
 * sub_0804E7A8, which does
 * `gUnknown_02029A10[i].entries[j].y -= gUnknown_085644D4[row[1]]`
 * with `row = gUnknown_02028E5C[i]`. A y correction, read `ldrh`, extent
 * unproved. */
/* A ROM table of (x, y) pixel offsets indexed [side][phase * 2] and
 * [side][phase * 2 + 1], where `phase` is gUnknown_02029B94[side][slot], the
 * six-step counter above. Row stride is 24 bytes = 12 u16, emitted as
 * `(c * 2 + c) * 8`, and the two reads are 4 bytes apart within the row -- so
 * the row holds six (x, y) pairs, one per counter value, which is what makes
 * the [12] a real extent rather than a stride. `ldrh`; the x half is added to
 * gUnknown_02029A10[..].x and the y half is added to .y after a u16
 * truncation, so nothing here sign-extends. */
extern u16 gUnknown_085523B0[][12];
/* Wave 32, W32-A. A 0x90-entry cycle of SIGNED y offsets: sub_0804EDAC steps
 * the slot's own unk28 counter, wraps it at 0x90, and adds
 * gUnknown_085523E0[counter] into gUnknown_02029A10[..].entries[..].y. Signed
 * from the `movs r3, #0; ldrsh r0, [r2, r3]` that reads it -- the
 * register-offset form an `ldrsh` is forced into -- and the wrap is what bounds
 * the table. */
extern s16 gUnknown_085523E0[];
/* Wave 33, W33-D. The same table species as gUnknown_085523E0 above, with a
 * 0xc0-entry cycle instead of 0x90: sub_0804EE08 steps gUnknown_03001470[c]
 * .unk28, wraps it at 0xc0, and adds gUnknown_08552500[counter] into
 * gUnknown_02029A10[..].entries[..].y. SIGNED from the same
 * `movs r3, #0; ldrsh r0, [r2, r3]` register-offset read. */
extern s16 gUnknown_08552500[];
/* Wave 33, W33-D. A ROM halfword PAIR per side, and FLAT rather than `[][2]`:
 * sub_0804EE08 reads `[a * 2]` with the byte offset `a << 2` CSEd out of the
 * `a * 5 + b` chain it also needs, and `[a * 2 + 1]` as `((a * 2 + 1) * 2)` --
 * one index chain. The `[][2]` spelling reassociates the second read to
 * `base + 2 + a * 4` and is different code. The pair goes into
 * struct Unk56E28's unk04/unk06, i.e. it is a position. Plain `ldrh`. */
extern const u16 gUnknown_085534EC[];
/* Wave 37, W37-J. Three more of gUnknown_085534EC's species -- ROM halfword
 * (x, y) PAIRS, and FLAT for the same reason: the byte offset of the first
 * element of a pair is CSEd out of an index chain the function already needs,
 * and the second element is `(n + 1) * 2` off it, one chain.
 *   gUnknown_085534BC  a pair per SIDE. sub_0804EB78 reads `[c * 2]` with the
 *                      byte offset `c << 2` shared with gUnknown_02028E5C and
 *                      gUnknown_084C3F78, and `[c * 2 + 1]`; the pair becomes
 *                      struct Unk56E28's unk04/unk06, i.e. a position.
 *   gUnknown_085534C4  a pair per (side, frame): sub_0804EB78 reads
 *                      `[(c * 5 + f) * 2]` and `+ 1`, with `c * 10` shared
 *                      with the gUnknown_02029B94[c][e] row offset. The two
 *                      halves are added to another entry's x and y.
 *   gUnknown_08553524  gUnknown_085534C4's exact counterpart in sub_0804F3C8,
 *                      same index shape and same two consumers.
 * Plain `ldrh` at all six reads, so nothing signs them; the extents are the
 * index shapes, not proved bounds. */
extern const u16 gUnknown_085534BC[];
extern const u16 gUnknown_085534C4[];
extern const u16 gUnknown_08553524[];
/* Wave 32, W32-A. Indexed by gUnknown_02028E5C[side][1] after that counter is
 * bumped, `ldrh` off a `lsls #1` element -- sub_0804B3E0 hands the value
 * straight to `*gUnknown_084C3F78[side]`, which is `u16`. Nothing bounds the
 * table; the comment on gUnknown_02028E5C names the two readers. */
extern u16 gUnknown_085644C8[];
extern u16 gUnknown_085644D4[];

/* The twelve 0x14-byte animation descriptors sub_08022BB8 picks between, and
 * the halfword offset table it pairs them with. The descriptors are named one
 * at a time rather than indexed, so each gets its own declaration; they are
 * only ever passed on as `void *`. gUnknown_080909B8 is indexed with i and
 * i + 1 for an (x, y) pair, so the entries alternate. */
extern u16 gUnknown_080909B8[];
extern u8 gUnknown_08499B8C[];
extern u8 gUnknown_08499BA0[];
extern u8 gUnknown_08499BB4[];
extern u8 gUnknown_08499BC8[];
extern u8 gUnknown_08499BDC[];
extern u8 gUnknown_08499BF0[];
extern u8 gUnknown_08499C04[];
extern u8 gUnknown_08499C18[];
extern u8 gUnknown_08499C2C[];
extern u8 gUnknown_08499C40[];
extern u8 gUnknown_08499C54[];
extern u8 gUnknown_08499C68[];

/* The 0x08083 sprite-builder triple (sub_080831FC, sub_08083484, sub_08083738)
 * and their caller sub_080829B0 all key off this SIX-byte table, and the extent
 * is proved rather than guessed: the ROM bytes are 00 05 02 04 03 01, a
 * permutation of 0..5, and every reader indexes it with `DivRem(x, 6)`, so a
 * seventh element could never be reached. What sits at +6 is a DIFFERENT table
 * -- the s16 offsets that sub_080829B0 names separately as gUnknown_08616972 --
 * so the boundary is a type change, not an inference from the index.
 *   Left non-const: the three builders each read it four or five times with a
 * `bl DivRem` between every pair of reads and the ROM re-`ldrb`s every time,
 * which by the const-across-a-call tell in docs/agbcc-codegen.md is what an
 * unqualified declaration produces. The reads have different indices in
 * principle, so this is corroborating rather than decisive -- flagged. */
extern u8 gUnknown_0861696C[6];
/* The two s16 coordinate tables that sit immediately after gUnknown_0861696C,
 * read by the 0x08082-0x08083 sprite block (sub_080829B0, sub_08082C0C,
 * sub_08083034, sub_08083A44) as `gUnknown_08616972[i + 1]` /
 * `gUnknown_08616980[i + 1]` for i in a five- or six-step loop, plus a
 * constant-folded [3] on the loop's special index.
 *   s16 rather than u16 is PROVED, not assumed, and the discriminator is a pair
 * of sibling expressions in sub_080829B0's two i == 2 arms over the same
 * element of gUnknown_08616980:
 *     `g[3] | 0x100`        -> ldrh ; orr ; lsl #16 ; asr #16
 *     `(g[3] - 8) | 0x100`  -> ldrsh ; sub ; orr        (no re-extension)
 * The C front end's `shorten` fires on BIT_IOR when both operands narrow, so
 * the first form computes in HImode and needs a sign-extension back to SImode
 * -- which only exists if the element type is SIGNED. The second form's left
 * operand is a MINUS in SImode, which get_narrower cannot narrow, so no
 * shortening happens and the sign already came from the `ldrsh`. A u16 table
 * gives neither the `ldrsh` nor the trailing `asr`. `g[3] & 0x1FF` in the same
 * arms loads with `ldrh` and needs no extension because combine drops both
 * (the mask is below 0x8000) -- that one is NOT evidence either way.
 *   gUnknown_08616972's extent is proved by the next symbol: 0x08616980 -
 * 0x08616972 = 14 bytes = seven elements, ROM
 * {-0x80, -0x28, -0x08, 0x10, -0x08, -0x28, -0x80}, i.e. period six with the
 * wrap element materialised so `[i + 1]` at i = 5 need not be reduced.
 * gUnknown_08616980 is left unsized -- nothing bounds it; ROM begins
 * {0x83, 0x80, 0x70, 0x50, 0x30, 0x20, 0x1D}. Neither is const: every read is a
 * fresh load with a `bl DivRem` or `bl Interpolate` between. */
extern s16 gUnknown_08616972[7];
extern s16 gUnknown_08616980[];
/* Wave 44, W44-F. sub_0807F57C's source table: a run of `ldrb`-read byte ids
 * split into groups by an 0xFF sentinel, with ONE extra byte after each
 * sentinel that the function copies into gUnknown_03005958 (a group label) and
 * a second 0xFF terminating the whole run. u8 from every access; extent
 * unbounded, since the walk is sentinel-driven rather than counted. Not const:
 * the ROM re-`ldrb`s every element across the `bl sub_0803CAB8` in the inner
 * loop. */
extern u8 gUnknown_086166F0[];
/* Wave 44, W44-F. One more of the OAM sprite blobs on PutSprite's `u16 *`
 * fourth parameter, drawn once per column by the twins sub_0807FC70 and
 * sub_08080A70 and reaching nothing else. `u16 *` for that reason and NOT
 * const -- PutSprite's parameter is a plain `u16 *`. Extent unknown; nothing
 * indexes it. */
extern u16 gUnknown_0848B6E6[];
/* Two more of the OAM sprite blobs described above, on the `u16 *` fourth
 * parameter of PutSprite and PutSpriteExt and reaching nothing else. Their
 * extents are proved by each other and by gUnknown_0861696C's neighbours:
 * gUnknown_08615C76 is 0x0002 plus two (y, x, tile) triples = 14 bytes, which
 * lands exactly on gUnknown_08615C84, and that one is 0x0004 plus four
 * triples = 26 bytes. Same model, and the same single piece of evidence, as
 * gUnknown_08615C04 above. */
/* One more OAM sprite blob on the same model: ROM is 0x0001 followed by one
 * (y, x, tile) triple, and its single reader hands it straight to
 * sub_0801BD00's `void *` third parameter with no arithmetic. Not const --
 * that parameter is not. */
extern u16 gUnknown_085806F2[];
/* Wave 50, W50-M. One more of the same species, the record immediately BELOW
 * gUnknown_085806F2 and pinned by it: data-0848B688.s gives it 8 bytes and
 * baserom.gba holds 0x0001 followed by one (y, x, tile) triple, exactly the
 * gUnknown_085806F2 shape. sub_08063CCC hands it straight to sub_0801BD00's
 * `void *` third parameter with no arithmetic, so not const. */
extern u16 gUnknown_085806EA[];
/* Wave 50, W50-M. Two 16-entry ROM tables of POINTERS to blobs of the kind
 * above, and the one place in this file where the addresses are proved rather
 * than assumed: data-0848B688.s gives each table 0x40 bytes, and dereferencing
 * baserom.gba gives sixteen consecutive words stepping by 8 --
 * gUnknown_0858081C holds 0x0858071C..0x08580794 and gUnknown_0858085C holds
 * 0x0858079C..0x08580814 -- i.e. two runs of sixteen 8-byte sprite blobs, the
 * second run ending exactly where gUnknown_0858081C begins.
 *
 * These are REAL TABLES, not `-fforce-addr` pool words, by wave 49's inverse
 * test: sub_08063CCC indexes each by a computed subscript (`lsls rN,a4,#2;
 * adds; ldr`) before dereferencing, where a pool word is loaded and
 * dereferenced immediately with no index. Element type is `void *` because the
 * loaded word goes straight to sub_0801BD00's `void *` third parameter. Not
 * const, for the same reason as the blobs. */
extern void *gUnknown_0858081C[];
extern void *gUnknown_0858085C[];
/* Wave 30, W30-D. One more OAM sprite blob of the same species, one record
 * lower: sub_08087220 is a single `PutSprite(1, 0x1FE, y + x * 16,
 * gUnknown_08615C4E, 0x5470)`, and PutSprite's fourth parameter is `u16 *`.
 * The ROM there is 0x0003 followed by three (y, x, tile) triples, exactly the
 * gUnknown_08615C76 shape described below. */
extern u16 gUnknown_08615C4E[];
/* Wave 48, W48-K. The blob BETWEEN those two, and its extent is pinned from
 * both sides in baserom.gba: gUnknown_08615C4E is 0x0003 plus three
 * (y, x, tile) triples = 20 bytes, which lands exactly here, and this one is
 * 0x0003 plus three triples too, landing exactly on gUnknown_08615C76. Typed
 * from its only reader, sub_0808A978's
 * `PutSprite(0, 0x18, 8, gUnknown_08615C62, 0x40)` -- PutSprite's fourth
 * parameter is `u16 *`. Not const, for the same reason as its neighbours. */
extern u16 gUnknown_08615C62[];
extern u16 gUnknown_08615C76[];
extern u16 gUnknown_08615C84[];
/* Two OAM sprite blobs, both typed only by the `u16 *` fourth parameter of
 * PutSprite / PutSpriteExt, the same model as gUnknown_0849B6C8 and
 * gUnknown_08499E10. Each is an OBJ count followed by that many (y, x, tile)
 * triples: gUnknown_08615C04 begins 0x0002 and gUnknown_0848B6CE begins 0x0001,
 * which is why the count is not part of the type. Neither is const -- those
 * parameters are not. */
extern u16 gUnknown_08615C04[];
extern u16 gUnknown_0848B6CE[];
/* Wave 45 (W45-I). A word table sub_0807B51C indexes with its fourth argument.
 * The element is NOT a pointer: it is added to `digit * 4`, OR'd with 0x1000
 * and handed to PutSprite's `u32` fifth parameter, so it is an OAM attribute-2
 * base -- a tile number, four tiles per decimal digit, palette 1 forced on.
 * NOT const: sub_0807B51C hoists only the ADDRESS `&gUnknown_0861617C[i]` out
 * of its digit loop and re-`ldr`s the element every pass; with `const` GCC
 * hoists the load too. Same evidence as gUnknown_0861433C. */
extern int gUnknown_0861617C[];
/* Wave 30, W30-E. The OAM sprite blob sub_080859A0 hands to PutSprite's
 * `u16 *` fourth parameter -- its only reader, so nothing constrains it
 * beyond that. Not `const`, because PutSprite's parameter is not (same model
 * as gUnknown_08499E10). */
extern u16 gUnknown_0848B690[];
/* Wave 48 (W48-E), the 0x0807B block.
 *
 * 0848B6C6 is an OAM sprite blob on PutSprite's `u16 *` fourth parameter, same
 * model as gUnknown_0848B690 eight bytes below it. sub_0807B690 reaches it
 * ONLY through its own .rodata address-constant pool (-fforce-addr): the ROM
 * spells the read `ldr r4, =gUnknown_081D9328; ldr r3, [r4]`, and the word at
 * 081D9328 dereferences to 0848B6C6 in baserom.gba. 081D9324 holds the SAME
 * address for a neighbouring function's private copy. gUnknown_081D9328 is
 * therefore NOT a global and must never be declared as one. */
extern u16 gUnknown_0848B6C6[];
/* An ApplyPaletteExt `u16 *` source. sub_0807B690 indexes it with a bare
 * `lsls #1` off DivRem(Div(frame, 3), 0x10) and hands the address to
 * ApplyPaletteExt(u16 *, 0x238, 2) -- 2 bytes at palette word 0x238, i.e. one
 * COLOUR of BG palette 0x11, cycled through 16 source entries. `u16 []` from
 * the index scale; not const, ApplyPaletteExt takes a plain pointer. Left
 * unsized: 0x10 entries is what the DivRem bounds, but nothing pins the
 * element count beyond the modulus. */
extern u16 gUnknown_0822AC60[];
/* A per-column u8 table sub_0807BFB8 indexes with its loop counter and folds
 * into a sprite X as `(t * 5) >> 1` (a bare `asrs`, so the shift is on the
 * promoted int, NOT a divide). `u8` from the `ldrb`; unsigned is unproved --
 * the only consumer is that multiply, and `ldrb` would be the same for either
 * signedness with no sign-extend in sight because the value is only widened
 * through the multiply. Unsized: the bound is proc->unk_4c, a runtime count. */
extern u8 gUnknown_0202FF78[];
/* sub_0807BD5C's Decompress SOURCE, unpacked into gUnknown_0200FC50, so `u8 []`
 * and NOT const -- Decompress's first parameter is a plain `u8 *`. Same model
 * as gUnknown_08234B10. */
extern u8 gUnknown_0822DC08[];
/* Wave 48 (W48-E). An EWRAM tile buffer, and the object it sits in extends
 * BACKWARDS from the symbol: sub_0807BD5C CpuFastSets 0x20 words FROM
 * gUnknown_02010450 itself, then runs a ten-iteration double copy whose two
 * sources start at gUnknown_02010450 - 0x680 and gUnknown_02010450 - 0x280
 * (pool words 0xFFFFF980 and 0xFFFFFD80 added to the symbol). Those are real
 * subtractions, not a different base -- the ROM names 02010450 and offsets
 * from it, so the original source did too. The -0x280 run ends exactly at the
 * symbol. `u8 []` is the weakest model that fits; CpuFastSet takes
 * `const void *` and nothing here pins an element type. Extent unknown in
 * BOTH directions, which is why it is left unsized. */
extern u8 gUnknown_02010450[];
/* Wave 48 (W48-H). sub_08076B20's lookup table: 8-byte records, a key byte
 * followed by SEVEN payload bytes, terminated by a key of 0xFF. The stride is
 * the `adds r2, #8` that walks it and the payload base is `adds r4, r2, #1`,
 * so the split is 1 + 7 and not 8 loose bytes. Read straight out of
 * baserom.gba, the first six records are
 *   07 | 00 01 02 03 04 05 06      0F | 08 09 0A 0B 0C 0D 0E
 *   17 | 10 11 12 13 14 15 16      1F | 18 19 1A 1B 1C 1D 1E
 *   21 | FE FE FE FE FE FE FE      29 | 22 23 24 25 26 27 28
 * then the 0xFF terminator row.
 *
 * unk01 is `u8` and the SIGN lives at the use, not in the member: sub_08076B20
 * reads it `ldrb` and then `lsls #0x18; asrs #0x18`. A declared `s8` array
 * folds to `ldrsb rD, [rB, rO]` here (the index is a register), so the shift
 * pair proves an explicit `(s8)` cast over an unsigned member -- and the 0xFE
 * row is exactly the -2 that cast produces. */
struct Unk0861515C
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01[7];
};

extern const struct Unk0861515C gUnknown_0861515C[];
/* Wave 48 (W48-E). sub_0807B7BC's glyph table: a 12-byte record per drawable
 * character, walked with `adds r5, #0xc` and terminated by a zero unk00.
 *   unk00  the character code, compared against a `u8 *` string byte (`ldrb`).
 *   unk04  a Decompress SOURCE (`u8 *`, Decompress's first parameter).
 *   unk08  the glyph's advance WIDTH. Read with `ldr`, so a word; it is both
 *          accumulated into a u16 running total and stored back through a
 *          `u8 *` out-array with `strb`, which pins neither signedness nor a
 *          narrower width. `int` is the weakest word that fits.
 * filler_01 is real padding: nothing in the ROM touches +1..+3. Unsized --
 * the terminator is the only bound. */
struct Unk08616194
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 filler_01[3];
    /* 0x04 */ u8 *unk04;
    /* 0x08 */ int unk08;
};
extern struct Unk08616194 gUnknown_08616194[];
/* Same OAM-blob model again, and the same single piece of evidence: it reaches
 * nothing but PutSpriteExt's `u16 *` fourth parameter (sub_0807567C and one
 * neighbour, fanin 2). ROM begins 0x0001 followed by one (y, x, tile) triple. */
extern u16 gUnknown_086143D8[];
/* Another of the same OAM blobs, fanin 13, at the head of the 0x0848B6xx run
 * that gUnknown_0848B6CE also belongs to: ROM is 0x0001 plus one triple, i.e.
 * 8 bytes, and a second blob starts at +8. Only ever a PutSpriteExt `u16 *`.
 *
 * Wave 48, W48-H: gUnknown_081D92F8 is NOT A GLOBAL and neither is
 * gUnknown_081D92FC -- both ROM words hold 0x0848B6A0, i.e. &gUnknown_0848B6A0,
 * read straight out of baserom.gba. They are agbcc's own `-fforce-addr`
 * address-constant pool, a PAIR because the reference sits inside a loop. The
 * words at 0x081D9324 and 0x081D9328 are the same thing one blob along -- both
 * hold 0x0848B6C6. sub_0807A1B8 matched by naming gUnknown_0848B6A0 directly
 * (the promotion carries `"rodata": ["0x081D92F8"]`); do not declare a symbol
 * for any of the four addresses. */
extern u16 gUnknown_0848B6A0[];
/* Wave 43 (W43-D). The NEXT blob in that same 0x0848B6xx run: gUnknown_0848B6CE
 * is 0x0001 plus one (y, x, tile) triple, i.e. 8 bytes, so the next one starts
 * at 0x0848B6D6. Typed `u16 []` from the run it belongs to and from its
 * neighbours, NOT from its own use -- sub_08037170 is its only reader and hands
 * it to sub_0801BD00's `void *` third parameter, which constrains nothing.
 * Not const, matching every other blob in the run.
 *
 * Found from the ROM side: sub_08037170's literal pool points at 0x08090EEC,
 * whose contents are 0x0848B6D6. That word is agbcc's -fforce-addr copy of this
 * symbol's address, which is why naming this blob directly reproduces the
 * ROM's two-load sequence exactly. */
extern u16 gUnknown_0848B6D6[];
/* A TWELVE-byte u8 table, {2,3,4,5,2,3,4,5,2,3,4,5}, read as
 * `gUnknown_08615E40[proc->unk52]` with a plain `ldrb` by sub_08079EA4 and used
 * as `(entry + 4) << 12` in a PutSpriteExt OAM word -- so the entries are OBJ
 * palette or tile-bank indices, and the repeat of period 4 is what the caller's
 * +0x52 counter cycles through. The extent is proved by a type change rather
 * than by the index: +0x0c starts a run of address-shaped words
 * (0x081D9484, 0x081DA8D0, 0x081DBEC8), which cannot be part of a byte table.
 *   NOTE for tools/lc_screen.py: this symbol is reported as a `.LC` pool word
 * (fanin 1, density 4) and it is NOT one. The use is a direct base-plus-index
 * `ldrb`, not the double indirection a rerouted address constant produces, and
 * the ROM bytes are 02 03 04 05..., not an address. Dereference before
 * believing the classification -- the screen splits on fan-in and position in
 * a .rodata run, and a genuine low-fan-in ROM table inside such a run looks
 * identical to it. */
extern u8 gUnknown_08615E40[12];
/* Two more OAM blobs, this pair on PutSprite's `u16 *` rather than
 * PutSpriteExt's, drawn one after the other by sub_080748A0 through two
 * different OBJ affine slots. Each is 0x0001 plus one (y, x, tile) triple, i.e.
 * exactly 8 bytes, which the 8-byte gap between the two addresses confirms.
 *   NOTE for tools/lc_screen.py: BOTH are reported as `.LC` pool words and
 * neither is one -- same misclassification as gUnknown_08615E40 above, and the
 * spacing should have been the giveaway, since a -fforce-addr pool pair is 4
 * bytes apart and these are 8. Three of the four `.LC` words the screen
 * attributed to this batch were ordinary ROM tables. */
extern u16 gUnknown_081CC4C4[];
extern u16 gUnknown_081CC4CC[];
/* Wave 32, W32-A. Three more OBJ lists of the same kind, all handed to
 * PutSprite's `u16 *` fourth parameter by sub_08077620 -- hence not const. The
 * first goes with a sub_0804402C sprite at a different layer, the other two are
 * a pair drawn at x 0x80 and 0x78 off one y. */
extern u16 gUnknown_081CC5B0[];
extern u16 gUnknown_081CC5D0[];
extern u16 gUnknown_081CC5DE[];

/* ---- wave 20 (W20-B), the 0x08084580-0x080848B4 loader block ----
 * An array of TWO-POINTER records, and the stride is proved by the two
 * functions that read it rather than by the ROM bytes: sub_080845A8 reads
 * `[i][0]` (`lsls r0,#3; adds; ldr`) and sub_080845C4 reads `[i][1]`
 * (`lsls r2,#3` with `adds r0,#4` applied to the BASE, which is what agbcc
 * emits for the second member of an 8-byte record and NOT what a flat pointer
 * array with index `i*2+1` emits -- that spelling reassociates into
 * `lsl #1; add #1; lsl #2` and is three instructions longer). Both elements
 * go straight to Decompress's `u8 *` first parameter, so the element type is
 * `u8 *` and neither is const (Decompress does not take const). Member 0 is
 * decompressed to 0x06013300 and member 1 to 0x06013B00 + i*0x400, i.e. the
 * pair is (a shared/base tile blob, a per-index 1 KB tile blob). The count is
 * not part of the type -- nothing in this block bounds i. */
extern u8 *gUnknown_08616AC0[][2];
/* A single compressed blob, sub_080845E8's only argument to Decompress
 * (destination 0x06015300). `u8 *` for the same reason as above. */
extern u8 gUnknown_0823D980[];
/* A single compressed blob, sub_0803F374's only argument to Decompress
 * (destination 0x06013940). Same shape and same `u8 []` model as
 * gUnknown_0823D980 above -- the two accessors are a loose duplicate pair that
 * differs only in the blob and the destination (wave 20, W20-C). */
extern u8 gUnknown_081169D0[];
/* A run of 16-colour PALETTES -- 32 bytes = 16 u16 each -- flat rather than
 * 2-D on purpose. Retyped from u8 to u16 in wave 20 when a second agent settled
 * sub_08084864's return from its caller (sub_08082660 hands it straight to
 * ApplyPaletteExt's `u16 *`); the `u8` model was byte-neutral in both accessors
 * and had no oracle, which is exactly the case docs/agbcc-codegen.md warns has
 * none. The element COUNT per entry is what the 32-byte stride pins. sub_08084864
 * returns `gUnknown_0823DC38 + i * 32` and sub_0808488C returns
 * `gUnknown_0823DC38 + (i + 6) * 32`; declared as `u8 [][32]` the second one
 * DOES NOT MATCH, because fold distributes the element-size multiply over the
 * `+ 6` and folds the product into the symbol (`.word gUnknown_0823DC38+0xc0`,
 * with the runtime `adds r0,r4,#6` gone). Written as explicit pointer
 * arithmetic on a flat array the `+ 6` survives, which is what the ROM has.
 * See docs/agbcc-codegen.md, wave 20 (W20-B). The 32-byte stride and the fact
 * that entry 6 is where the second function starts are the only things pinned;
 * the element is not otherwise typed here because nothing in this block reads
 * through it. */
extern u16 gUnknown_0823DC38[];
/* Two compressed tile blobs, each Decompressed into *gUnknown_08499580 by one
 * of the duplicate pair sub_080858C0 / sub_08085908. `u8 []` and NOT `const`:
 * Decompress's first parameter is `u8 *`, the same reason gUnknown_0823D980
 * above is not const (wave 21, W21-A). */
extern u8 gUnknown_0823DE38[];
extern u8 gUnknown_0823DF48[];
/* The two fixed fallbacks returned by sub_08084864 and sub_0808488C when
 * sub_08084858(i) is non-zero -- same role, same shape, 32 bytes apart, so
 * they are almost certainly two entries of one table that the source names
 * individually. Same `u16 *` as gUnknown_0823DC38, and not proven further:
 * every caller of the two functions is still asm-resident. */
extern u16 gUnknown_0812596C[];
extern u16 gUnknown_0812598C[];
/* Only byte 1 is ever touched here, by sub_08084600 (`ldrb r0,[r0,#1]`, tested
 * against 0) and by sub_0808135C. An array rather than a struct because nothing
 * yet reads a second field, and `u8` because both reads are `ldrb`. Byte 0 is
 * untouched by either, so the extent is not pinned -- do not widen this to a
 * scalar on the strength of the two reads. */
extern u8 gUnknown_0300591C[];
/* Two u16 tables of animation frame ids, both indexed by the same 0..7 selector
 * sub_08084600 computes, and picked between by gUnknown_0300591C[1]. The first
 * is one-dimensional (`lsls r0, r5, #1`); the second is TWO frames per selector
 * (`lsls r1,r5,#1; adds r1,r1,r0; lsls r1,r1,#1` -- i.e. [i * 2 + parity]) with
 * the parity coming from DivRem(unk66, 2). u16 from the `ldrh`, and both are
 * handed to sub_08014668's fourth parameter. Neither is const: they sit 16 bytes
 * apart, which bounds gUnknown_08616FA4 at 8 entries and is consistent with the
 * selector's 0..7 range. */
extern u16 gUnknown_08616FA4[];
extern u16 gUnknown_08616FB4[];

/* ---- wave 24 (W24-C), the 0x08078 block ---- */

/* An 8-byte record table. sub_0807831C is the only reader: it indexes it with
 * gUnknown_0202FDFC.unk0c + its own proc's +0x58 (`lsls #3` = 8-byte stride)
 * and hands word 0 straight to sub_08074AAC, whose first parameter is the
 * 0xFF-terminated `const u8 *` id list -- same type as gUnknown_08615984 and
 * gUnknown_08615988, and settled by that call rather than by any read here.
 * Word 4 is not read anywhere in the ROM, so the record is bounded at 8 bytes
 * by the stride alone and filler is the honest spelling for it. */
struct Unk861500C /* 0x08 */
{
    /* 0x00 */ const u8 *unk_00;
    /* 0x04 */ void *unk_04; /* wave 30 (W30-C): sub_08076BF0 `ldr`s it and hands
                              * it straight to sub_08078480's `void *` first
                              * parameter, which that callee only parks at +0x54
                              * of the proc it starts -- so a word-wide pointer
                              * whose pointee nothing in the ROM dereferences.
                              * NOT const-qualified: sub_08078480 takes a plain
                              * `void *` and -Werror rejects the call otherwise.
                              * Was filler. */
};

extern const struct Unk861500C gUnknown_0861500C[];

/* What sub_0807831C's proc keeps at +0x54 and hands to sub_080782C0. That
 * function walks word 0 as a SIGNED byte list (`ldrsb` off a zero index
 * register, terminated by -1) using each entry to index
 * gUnknown_0202FDFC.unk12, and compares the resulting count against the two
 * `ldrb`s at +4 and +5. It is a pointer PARAMETER's type and would normally
 * live in the .c, but sub_080782C0's prototype needs it: typing the parameter
 * directly rather than assigning it to a local inside the body is what puts
 * `adds r3, r0, #0` in the prologue ahead of `mov ip, r1`, and a local costs
 * exactly that ordering. */
struct Unk80782C0
{
    /* 0x00 */ const s8 *unk_00;
    /* 0x04 */ u8 unk_04;
    /* 0x05 */ u8 unk_05;
};

/* Three ROM blobs handed to sub_080785CC's fourth parameter and to nothing
 * else -- sub_08078404, sub_08078420 and sub_080783BC respectively. That
 * callee only stores the pointer to +0x54 of the proc it starts, so nothing in
 * the ROM dereferences them and `const void *` is all the evidence supports.
 * Their sizes are unpinned. */
extern const u8 gUnknown_084B9F00[];
extern const u8 gUnknown_084BA210[];
extern const u8 gUnknown_084BA480[];

/* Two more proc scripts, both only ever handed to Proc_StartBlocking:
 *   08615ACC  sub_080785CC, which fills +0x2c/+0x30 (its first two arguments),
 *             +0x54 (the blob) and +0x58 (the third). sub_08078558 is one of
 *             its methods and reads +0x2c and +0x30 back.
 *   08615BBC  sub_08078968, no fields written.
 * `const struct ProcCmd []` for the same reason as every other entry in this
 * group -- it is proc.h's parameter type and nothing here reads the data. */
extern const struct ProcCmd gUnknown_08615ACC[];
extern const struct ProcCmd gUnknown_08615BBC[];

/* The two scripts sub_08078ED4 picks between on gUnknown_03003FC0.unk01 == 3.
 * Both go to Proc_Start under the caller's own parent. */
extern const struct ProcCmd gUnknown_08615D88[];
extern const struct ProcCmd gUnknown_08615DD8[];

/* NOT GLOBALS: gUnknown_081D92D8, gUnknown_081D92DC, gUnknown_081D92E0 and
 * gUnknown_081D92E4 are four consecutive `-fforce-addr` address-constant words
 * (addends 0, 4, 8, 0xc, the textbook pattern), and all four hold the SAME
 * value -- 0x03001FFC, i.e. &gUnknown_03001FFC, the volatile BLDY shadow
 * declared in include/hardware.h. Dereferencing them in baserom.gba is what
 * settles it; the splitter invents a `gUnknown_<addr>` for a pool word and a
 * real global alike and cannot tell them apart.
 *
 * So sub_08078BDC and sub_08078C18 are a fade-in / fade-out pair written
 * against gUnknown_03001FFC directly, and their apparent double indirection
 * (`ldr r2, =sym; ldr r1, [r2]; ldrh`) is the pool word, not a pointer
 * variable. Declaring a `u16 *` here instead compiles to THREE loads and does
 * not match. */

/* ---- wave 24 (W24-C), the 0x08078 block, second pass ---- */

/* Four 4-byte records, read out of baserom.gba as
 *     {0x61, 0x69, 0x28, 0xff}  {0x62, 0x6a, 0x0e, 0xff}
 *     {0x63, 0x6b, 0x16, 0xff}  {0x64, 0x6c, 0x1e, 0xff}
 * and walked by sub_08078358 alone (`adds r4, #4`, i = 0..3). Bytes 0 and 1 are
 * tags handed to sub_0803CBD8; bytes 2 and 3 are a 0xFF-terminated id list
 * handed to sub_08074AAC, and byte 2 is ALSO read on its own to index
 * gUnknown_0202FDFC.unk12. `s8` for that pair because the read is `ldrsb` and
 * because 0xff is the -1 terminator sub_080782C0 walks its own list against --
 * the same element type, one indirection down. The `const u8 *` cast at the
 * sub_08074AAC call is the cost of that choice and is byte-free.
 *
 * This is the record the gUnknown_08615984 comment above predicted: the
 * 0x08615974-0x0861598B block really is two unrelated things at 4-byte
 * granularity, and reading the ROM confirms it rather than inferring it. */
struct Unk8615974 /* 0x04 */
{
    /* 0x00 */ u8 unk_00;
    /* 0x01 */ u8 unk_01;
    /* 0x02 */ s8 unk_02[2];
};

extern const struct Unk8615974 gUnknown_08615974[];

/* The fourth script sub_08078198 tests for existence, alongside 086147FC,
 * 0861485C and 08614894 which are already declared above. */
extern const struct ProcCmd gUnknown_08614614[];
/* Proc_Start'd by sub_08078E94 under a caller-supplied parent when
 * sub_08078E20's predicate is false. */
extern const struct ProcCmd gUnknown_08615D70[];

/* One of the word-stride run of RAM pointers at 0x08499588..0x084995A0, of
 * which 0849959C and 084995A0 are already declared above. NOT an -fforce-addr
 * pool word: three functions in two translation units (sub_08017994,
 * sub_080179AC, sub_080185A0) load this same address, and -fforce-addr gives
 * each function its own private copy. `u16 *` because sub_080179AC hands it
 * straight to sub_08012BC8(u16 *, ...) and sub_080179D0(u16 *) clears a
 * 23-column, 4-row halfword window through it. Wave 26, W26-C. */
extern u16 *gUnknown_08499588;
/* The next word of that same 0x08499588..0x084995A0 run. Wave 35 (W35-H):
 * sub_0802C214 is two sub_0802C1F0 byte-copies whose sources are
 * gUnknown_08499588 and this word and whose destinations are gUnknown_08499578
 * and gUnknown_08499580 at the same +0x380 offset and the same 0x180 length --
 * so 08499588/0849958C pair up exactly as 08499578/08499580 do, and it takes
 * the same `u16 *` those two carry. NOT an -fforce-addr pool word: it sits in
 * the middle of a declared run of RAM pointers and holds a different address
 * from each of its neighbours. */
extern u16 *gUnknown_0849958C;

/* Wave 35 (W35-H). Two more gUnknown_03001470 scripts: sub_0802D2A0 hands
 * gUnknown_0849A9F8 to Proc_Start on PROC_TREE_3 and sub_0802D5E8 hands
 * gUnknown_0849AFE8 to sub_080152C0, the same two entry points every other
 * script symbol in this block reaches. */
extern const struct ProcCmd gUnknown_0849A9F8[];
extern const struct ProcCmd gUnknown_0849AFE8[];

/* Wave 35 (W35-H). A table of 4-byte records whose FIRST byte is all anything
 * reads: sub_0802D9B8 does `lsls #2; adds; ldrb` off a proc member and
 * sub_0802D918 reads element 0 the same way, both handing the byte to
 * sub_0803A9C8 / sub_0803AA78's u8 parameter. Declared as the byte array the
 * accesses actually show rather than as a struct -- the stride is visible, the
 * other three bytes are not. */
/* Wave 41, W41-D. The build-menu entry list sub_0802D67C fills: 4 bytes per
 * entry, [0] the unit id and [1] a "greyed out" flag, terminated by a final
 * entry whose [0] is 0. The stride is a bare `lsls #2` on this flat spelling
 * and the two promoted readers (c_0802D918.c, c_0802D9B8.c) index it flat.
 *   `u8 [][4]` WAS TRIED and is WORSE, which is worth recording because the
 * row form looks obviously right: it does put the flag's `+1` in the store
 * displacement where the flat form spends an `adds r0,#1`, but agbcc then
 * hoists `base + 1` into its own callee-saved pseudo for the whole loop, which
 * costs two more instructions than it saves and displaces the `-1` sentinel
 * that the ROM keeps in sl. Left flat; see work/sub_0802D67C/. */
extern u8 gUnknown_02023830[];

/* Wave 35 (W35-H). The link-error screen's graphics, all three confirmed
 * against the ROM image. 0x0816DB88 is 8 WORDS (sub_0802E960 CpuFastSets it to
 * 0x05000000, i.e. one 16-colour BG palette), and the next blob starts at
 * 0x0816DBA8 -- exactly 0x20 bytes later, which is what fixes the extent.
 *
 * 0x0849B020 and 0x0849B024 are POINTER VARIABLES and not -fforce-addr pool
 * words: they hold 0x0816DBA8 and 0x0816DF40, two DIFFERENT addresses neither
 * of which is their own, and sub_0802E960 reaches each with the `ldr =sym;
 * ldr [sym]` a real ROM pointer emits. Each is handed straight to Decompress,
 * whose first parameter is `u8 *`. */
/* Wave 35 (W35-H): a halfword count. sub_0802D918 reads it `ldrh`, subtracts 1
 * and `strh`s the result into gUnknown_03001470[i].unk22, which is s16 -- so
 * this is the extent that member is initialised from. Width from the load;
 * nothing sign-extends it, so u16 is the weakest fit. */
extern u16 gUnknown_0300055A;
extern const u16 gUnknown_0816DB88[];
extern u8 *gUnknown_0849B020;
extern u8 *gUnknown_0849B024;
/* A gUnknown_03001470 script blob: sub_08017688 is
 * `sub_080152EC(gUnknown_0848A1EC, 0)->unk1e = a`. The ROM words there are
 * {0, 0x001E001E, 0x0803B5E9, 0x00020000}, exactly the gUnknown_0849A00C
 * shape, so `const struct ProcCmd []` like its 0848A140 / 0848A150
 * neighbours. */
extern const struct ProcCmd gUnknown_0848A1EC[];
/* 0x0808E558 is NOT a global -- do not declare one. It is agbcc's own
 * -fforce-addr address-constant pool and the ROM proves it: the four words
 * there are 0x0200C420, 0x0200C078, 0x0200C528, 0x0200C528, a run of the
 * ADDRESSES of globals that are already declared above. sub_080176C0 reads it
 * as `ldr r1, =0x0808E558; ldr r4, [r1]`, which is the two-level -fforce-addr
 * load and not a pointer variable. The honest `gUnknown_0200C420.unk00`
 * spelling reproduces that function exactly, including the `ldr r1, [r1]`
 * reload before the second field -- the pool word's address is what stays live
 * in r1, so each use re-loads the object address from it. Wave 26, W26-C. */

/* ------------------------------------------------------------------ *
 * Wave 27, W27-B: address-locality block 0x08013                      *
 * ------------------------------------------------------------------ */

/* The proc script sub_080130DC starts, and the ROM names the proc's own
 * callback: the words are {0x00000011, 0, 0x00000004, 0x0801311D, 0x0000000E,
 * 0}, which as ProcCmds is PROC_CMD_END_IF_DUP, then PROC_CMD_ONEND with
 * dataPtr 0x0801311D (&sub_0801311C with the THUMB bit set), then
 * PROC_CMD_SLEEP. That is what ties the two together: sub_080130DC stores three
 * halfwords at +0x64/+0x66/+0x68 of the proc it starts here, and sub_0801311C
 * is the ONEND handler that reads +0x68 back as an `ldrsh`. */
extern const struct ProcCmd gUnknown_0848936C[];

/* A word table of proc-script POINTERS, not a script itself: the first three
 * words are 0x084893DC, 0x084893F0 and 0x08489404, all inside the 0x08489
 * proc-script run, and the fourth is 0. sub_08013338 indexes it by words and
 * stores the word whole at +0x4c of the proc it just started, so the elements
 * are `const struct ProcCmd *` -- the same reasoning as gUnknown_086147FC's
 * neighbours. */
extern const struct ProcCmd *gUnknown_0848950C[];

/* The proc script sub_08034CD4 starts on tree 3. ProcCmds: {ONEND, 0,
 * 0x08034F8D}, {CALL, 0, 0x08034F7D}, {CALL, 0, 0x0803ED55}. */
extern const struct ProcCmd gUnknown_0849F790[];

/* A byte flag sub_08013580 tests against the literal 1 before re-uploading the
 * whole gPal shadow to palette RAM -- a "palette is dirty" latch. Plain
 * `ldrb`, and nothing sign-extends or arithmetically compares it, so `u8`. */
extern u8 gUnknown_03000048;

/* Wave 35, W35-K: the key-repeat timing pair, read only by sub_0801348C (the
 * key-state update it feeds gUnknown_03002090 through sub_08013510).
 * gUnknown_030030C8 is the INITIAL delay -- it is reloaded on every frame the
 * held mask changes or is empty -- and gUnknown_03002F94 the repeat INTERVAL,
 * reloaded each time the countdown at ->unk10 reaches 0. Both are reached with
 * a whole-word `ldr` and truncated by the `strh` into that u16 member, so the
 * objects are 32 bits wide; signedness is unproved, nothing compares them and
 * the store is byte-identical either way. Neither is ever written in the ROM,
 * so the values are set at their definition. */
extern int gUnknown_03002F94;
extern int gUnknown_030030C8;

/* Wave 35, W35-K. The key-state buffer sub_08013510 refreshes every frame.
 *
 * It is the SAME TWENTY BYTES as `struct KeySt` in hardware.h -- 0x03002090 is
 * `((struct KeySt *)&gUnknown_03002040)[4]`, the same array c_08064410.c
 * indexes -- and it is deliberately NOT spelled that way, because
 * sub_0801348C is the object's PRODUCER and it contradicts KeySt's names for
 * every member from 0x04 on. Those names were inherited from FE8 and no AW2
 * function has ever corroborated them; what sub_0801348C actually does is
 *
 *   unk08  the raw held mask this frame (the value handed in)
 *   unk0e  the raw held mask LAST frame (unk08 copied before it is overwritten)
 *   unk0c  newly-pressed = unk08 & ~unk0e
 *   unk0a  the repeat output: newly-pressed, plus the whole held mask again on
 *          each tick of the unk10 countdown
 *   unk10  that countdown, reloaded from gUnknown_030030C8 (initial delay)
 *          whenever the mask changes or is empty and from gUnknown_03002F94
 *          (repeat interval) each time it expires
 *   unk00/unk02/unk04/unk06  published copies of unk08/unk0a/unk0c/unk0e
 *
 * so 0x04 is newly-pressed rather than KeySt's `held`, 0x06 is last frame
 * rather than `repeated`, 0x08 is the held mask rather than `pressed`, and
 * 0x10 is a countdown rather than `pressed2`. The two 0x00/0x02 names KeySt
 * does carry AGREE with this: its own note has consumers testing unk00 against
 * L and R (a held mask) and unk02 against Right|Up and Left|Down to step a
 * cursor (a repeat mask).
 *
 * Renaming KeySt is the right fix and is byte-neutral -- same offsets, same
 * widths -- but it touches every promoted file that uses `.held`/`.repeated`/
 * `.pressed`, so it is left for a wave that can re-verify all of them rather
 * than done mid-wave beside three other agents. */
struct Unk03002090 /* 0x14 */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u16 unk04;
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
    /* 0x0c */ u16 unk0c;
    /* 0x0e */ u16 unk0e;
    /* 0x10 */ u16 unk10;
    /* 0x12 */ u16 unk12;
};
extern struct Unk03002090 gUnknown_03002090;

/* ------------------------------------------------------------------ *
 * Wave 27, W27-B: address-locality block 0x0803C                      *
 * ------------------------------------------------------------------ */

/* A ROM dispatch table of 24-byte rows. The stride is measured, not assumed:
 * sub_0803C864 indexes it `((a << 1) + a) << 3`. Two members are readable,
 * and the ROM names the callee outright -- the words of row 0 are
 * {0x0AEE0014, 0x000003E8, 0x0803C40D, 0x0803CBA1, 0x00000020, 0x084C2F88}
 * and of row 1 {0x0AEF0014, 0x00001388, 0x0803C435, 0x0803CBA1, 0x00000028,
 * 0x084C2FB8}. 0x0803CBA1 is &sub_0803CBA0 with the THUMB bit set, and
 * sub_0803CBA0 is already declared `void (int, int)`, which is exactly how
 * sub_0803C864 calls through unk0c: `f(row->unk10, 1)`, a two-argument
 * indirect call (`bl _call_via_r2`, and the register index counts the
 * arguments). unk10 is 0x20 / 0x28, i.e. the bit ids sub_0803CBA0's third
 * range accepts.
 *
 * unk08 is a second callback (0x0803C40D / 0x0803C435) and unk14 a ROM
 * pointer (0x084C2F88 / 0x084C2FB8), but nothing matched reads either, so
 * they stay in the filler rather than being guessed at. */
struct Unk0849EDB0 /* 0x18 */
{
    /* 0x00 */ u8 unk00; /* Wave 35, W35-L. `ldrb`, handed straight to
                          * sub_0801F2AC's `int` first parameter by
                          * sub_080487B4. */
    /* 0x01 */ u8 filler_01[0x01];
    /* 0x02 */ u16 unk02; /* Wave 35, W35-L. `ldrh`, used by sub_080487B4 as
                           * the index into the `u8 *` table
                           * gUnknown_08610A38. */
    /* 0x04 */ int unk04; /* Wave 35, W35-L. A word (`ldr`) that sub_080487B4
                           * passes as sub_08014B0C's fourth argument. INTEGER
                           * and not a pointer: sub_08047B98 fills the same
                           * parameter with `p->unk1f + 1`, and the two call
                           * sites are what settle it -- nothing dereferences
                           * this field. */
    /* 0x08 */ int (*unk08)(int); /* Wave 37, W37-Q1. The second callback
                          * (0x0803C40D / 0x0803C435). sub_0803C814 calls it
                          * `bl _call_via_r1` -- the trampoline index counts the
                          * arguments, so exactly ONE -- with unk10 as that
                          * argument, and compares the result against -1;
                          * sub_0803C784 calls it the same way and compares
                          * against 1. So `int (*)(int)`. sub_0803C784 also
                          * hands the POINTER ITSELF to sub_0803C750, which
                          * `str`s it into gUnknown_02027FB0[].unk00, and that
                          * is what fixes the field as a pointer rather than an
                          * int the two callers happen to call through. */
    /* 0x0c */ void (*unk0c)(int, int);
    /* 0x10 */ int unk10;
    /* 0x14 */ u8 filler_14[0x04];
};
/* NOT `const`, and sub_0803C784 is the only thing in the tree that can tell:
 * it reads `gUnknown_0849EDB0[i].unk08` twice with an indirect call in
 * between, and the ROM keeps the ADDRESS of that member in r4 across the call
 * and RE-LOADS the pointer (`ldr r0, [r4]; bl sub_0803C750`). A `const` array
 * is TREE_READONLY, so gcc treats the MEM as unchanging, keeps the VALUE in
 * r4 and emits `adds r0, r4, #0` -- one instruction shorter and the wrong
 * bytes. Every other reader loads each member once and is byte-neutral either
 * way. Wave 37, W37-Q1. */
extern struct Unk0849EDB0 gUnknown_0849EDB0[];

/* 0x08091140 is NOT a global -- do not declare one. It is agbcc's own
 * -fforce-addr address-constant pool, the same case as 0x0808E558: the word
 * there is 0x02000000, i.e. &gUnknown_02000000, and its neighbours are
 * 0x08499590, 0x030033EC and 0x03003F2C, a run of the ADDRESSES of other
 * globals. sub_0803CF04 reads it as `ldr r4, =0x08091140; ldr r1, [r4]` and
 * then RE-LOADS `ldr r1, [r4]` for the second use, which is the two-level
 * -fforce-addr pattern -- the pool word's address is what stays live, so each
 * use reloads the object address from it. The honest `gUnknown_02000000`
 * spelling reproduces both loads. Its sibling sub_0803CF3C names the same
 * object once and gets the ordinary one-level `ldr r0, =gUnknown_02000000`,
 * which is the whole difference between the two forms: how many uses. */

/* ------------------------------------------------------------------ *
 * Wave 27, W27-B: address-locality block 0x08037                      *
 * ------------------------------------------------------------------ */

/* A gUnknown_03001470 script blob: sub_08037D64 is
 * `sub_080152EC(gUnknown_0849D53C, 0)->unk1e = a`, the same instruction stream
 * as sub_08017688 with this pool word in place of gUnknown_0848A1EC.
 * `const u8 []` for the gUnknown_0849A0F0 reason -- nothing indexes it and
 * sub_080152EC takes `const void *`.
 *
 * NOTE on gUnknown_0848A1EC, which the sub_08017688 comment types
 * `const struct ProcCmd []`: the ROM does NOT support that, and this blob is
 * the counter-example. The four words at 0x0849D53C are
 * {0x080375BD, 0x00020000, 0, 0x00000001} -- word 0 is &sub_080375BC with the
 * THUMB bit set, i.e. a callback in the FIRST word, where struct ProcCmd would
 * put {s16 opcode, s16 dataImm}. Read as ProcCmd that is opcode 0x75BD, which
 * is not in proc.h's enum. gUnknown_0848A1EC's words {0, 0x001E001E,
 * 0x0803B5E9, 0x00020000} read as ProcCmd give entry 0 = PROC_END, which would
 * make the script a no-op. Both are 16-byte {callback, flags, ptr, imm}
 * records of the gUnknown_03001470 subsystem, not proc scripts. The typing is
 * byte-neutral (both reach a `const void *` parameter), so nothing catches it;
 * flagged rather than changed, since sub_08017688 is already promoted. */
extern const u8 gUnknown_0849D53C[];

/* Two proc scripts sub_08037124 hands to Proc_EndEach. Genuine ProcCmds, and
 * the ROM words prove it here: 0x0855379C is {opcode 0x0E SLEEP, dataImm 1,
 * dataPtr 0} then {opcode 0x02 CALL, dataImm 0, dataPtr 0x080531D5}. */
extern const struct ProcCmd gUnknown_0855379C[];
/* A gUnknown_03001470 blob, NOT a proc script: sub_08037124 hands it to
 * sub_0801537C(const void *), the stopper half of the start/stop pairing
 * documented at gUnknown_0849A108. Its words are {0x08053435, 0x00020000,
 * 0x080534A1, 0x00010000, ...}, i.e. THUMB callbacks in word 0 of each 8-byte
 * record -- the same non-ProcCmd shape as gUnknown_0849D53C above. `const u8 []`
 * as the weakest model that gives the clean pool word. */
extern const u8 gUnknown_08553820[];

/* An LZ77 blob: sub_08037150 is `Decompress(gUnknown_08124478, dst)`, and the
 * ROM header word 0x00048010 is the LZ77 (type 0x10) marker with an
 * uncompressed size of 0x480. `u8 []` and non-const because Decompress's first
 * parameter is a plain `u8 *`. */
extern u8 gUnknown_08124478[];

/* A small ROM byte table sub_080375A4 indexes with its own u8 parameter and
 * hands to sub_08037448 -- `adds r0, r0, r1; ldrb r0, [r0]`, so `u8` elements.
 * The first eight bytes are 00 01 07 02 07 00 00 00; the words that follow at
 * 0x08090EF8 are agbcc's own -fforce-addr address pool (0x03003F68,
 * &gUnknown_08499598, ...), which is what bounds the table's size. */
/* Wave 43 (W43-D): gUnknown_08090EEC is NOT a global and must never be
 * declared as one. The word at 0x08090EEC holds 0x0848B6D6 -- an ADDRESS -- and
 * sub_08037170 is its only referencer, which is the signature of agbcc's own
 * -fforce-addr .rodata pool word rather than a pointer variable. Its
 * neighbours in this run are the same thing (0x08090EF8 holds 0x03003F68,
 * i.e. &gUnknown_03003F68; the brief records 0x08090A60 and 0x08090B00 holding
 * &gUnknown_03003FC0), so check each gUnknown_0809xxxx against baserom.gba
 * before typing it. gUnknown_08090EF0 immediately below is the counter-example
 * and IS a real table.
 *
 * Declared as a pointer global, sub_08037170 needs THREE loads
 * (pool -> .LC -> value) where the ROM has two, and +12 bytes. Naming
 * gUnknown_0848B6D6 directly lets agbcc emit this very word itself. */
extern const u8 gUnknown_08090EF0[];

/* A whole-word scratch slot in the 0x08037 palette-cycling code, and it is
 * already in aw2bhr.lds. sub_08037750 stores its argument here with a plain
 * `str`; sub_08037790 reads it back with `ldr` and computes
 * `unk * 0x20 + 0x1c` as a palette byte offset. Word-wide both ways, so `int`
 * is the weakest fit -- nothing sign-extends or compares it. */
extern int gUnknown_0300057C;

/* Two palette blobs, and they are CONTIGUOUS: 0x081253F0 + 0x20 = 0x08125410.
 * sub_08037750 hands 081253F0 to ApplyPaletteExt(u16 *, u32, u16) for 0x20
 * bytes, so `u16 []` and non-const (ApplyPaletteExt takes a plain pointer).
 * 08125410 is the cycling table behind it: sub_08037790 adds a BYTE offset of
 * `(gUnknown_03004008 & 0x3c) >> 1` to it and hands it to sub_0801368C for 2
 * bytes, i.e. one colour picked by a 16-phase frame counter. `u16 []` for the
 * same consumer reason; the byte-offset arithmetic is spelled through a
 * `(u8 *)` cast at the one call site because the ROM shifts by 1, not 2. */
extern u16 gUnknown_081253F0[];
extern u16 gUnknown_08125410[];

/* --- the 0x0803B block (wave 27, W27-C) ---------------------------------- */
/* An 8-byte dispatch table: sub_0803BBA8 indexes it with gUnknown_030033FC
 * (`lsls #3`), calls the word at +0 through `_call_via_r3` with no arguments,
 * and `strb`s the word at +4 into gUnknown_0200C420.unk0d. The stride is read
 * off the shift and is not a guess; the second member being a full word is
 * read off the `ldr`, and the truncation to a byte happens at the store. */
struct Unk849EACC
{
    /* 0x00 */ void (*unk00)(void);
    /* 0x04 */ int unk04;
};
extern const struct Unk849EACC gUnknown_0849EACC[];
/* The script sub_0803BA4C starts on tree 3 after setting
 * gUnknown_03003FC0.unk01 = 1 -- the same shape as the 0849EBBC / 0849EC1C /
 * 0849ECE0 starters listed above, with 1 as its mode constant. */
extern const struct ProcCmd gUnknown_0849EB34[];

/* --- the sub_08036B34 / AgbMain unit (wave 27, W27-C) --------------------- */
/* All four of the RAM cells below are cleared by sub_08036B4C's boot reset and
 * every write there is a bare 0, which fixes only the WIDTH, never the
 * signedness. Each carries the weakest type its width allows. */
/* Whole word (`str`). sub_08000DA8 and sub_08001038 also write it -- the
 * comment on sub_0801F00C in unknown-functions.h describes sub_08001038 as
 * "raising" it, so a level/counter rather than a pointer. */
extern int gUnknown_030040A0;
/* One byte (`strb`). */
extern u8 gUnknown_03004094;
/* Whole word. sub_08022064 reads it back with `ldr` and tests it against a
 * value, so a word and not a narrower cell. */
/* Wave 36 (W36-L): RETYPED from `int` to `volatile u32`, both halves read off
 * sub_08022048's `gUnknown_03003330++; switch (gUnknown_03003330 % 0x32)`.
 * UNSIGNED because the modulo calls `__umodsi3` and not `__modsi3`; VOLATILE
 * because the ROM RE-LOADS the word after the increment's `str` (`ldr; adds #1;
 * str; ldr`) instead of reusing the value it just stored. Same pair as the
 * gUnknown_030043F0 / gUnknown_03004078 frame counters sub_08021DD8 drives.
 * The only promoted user, src/decomp/c_08036B34.c, just stores 0 to it, which
 * is one `str` either way; re-verified byte-identical after the change. */
extern volatile u32 gUnknown_03003330;
/* Halfword (`strh`); sub_0802BC40 is the other writer. No signed reader. */
extern u16 gUnknown_030033F0;
/* Three EWRAM/IWRAM regions AgbMain hands to other subsystems as bare
 * addresses and never dereferences itself: gUnknown_02003000 is the 0x8000-byte
 * buffer it passes to sub_08014DA8, and gUnknown_02000000 / gUnknown_03003064
 * are arguments 3 and 5 of sub_0801A79C. `u8 []` is the weakest spelling that
 * makes the address available; NOTHING here settles an element type. */
extern u8 gUnknown_02000000[];
extern u8 gUnknown_02003000[];
extern u8 gUnknown_03003064[];
/* The proc script sub_08036C2C starts on PROC_TREE_3. */
extern const struct ProcCmd gUnknown_08553754[];

/* ---- wave 27 (W27-A) ---- */

/* The m4a sound driver's XCMD dispatch table. sub_080717C4 (`ply_xcmd` in the
 * Fire Emblem decomps, on the shared Intelligent Systems engine) fetches one
 * byte from the track's command cursor, scales it by 4 and `ldr`s a function
 * pointer out of this table, then calls it through `_call_via_r2` -- so the
 * entries are `void (*)(mplay, track)` and the index is a plain byte.
 * `const` because it is ROM and nothing writes it. */
extern void (*const gUnknown_081BA00C[])(void *, void *);

/* A single RAM function pointer, NOT a table: sub_080717E4 is
 * `ldr r2,=gUnknown_03005740; ldr r2,[r2]; bl _call_via_r2`, i.e. one load of
 * the pointer VALUE with no index. Two arguments, off the `_call_via_r2`
 * register index, and both are this wrapper's own parameters passed through
 * untouched, so neither is typed by anything here. Already bound at
 * 0x03005740 by aw2bhr.lds, so this only declares it. */
extern void (*gUnknown_03005740)(void *, void *);

/* Two 0x20-byte ROM palettes, one 16-colour bank each, and NOT proc scripts --
 * see the sub_08071B28 note in unknown-functions.h, which is where the
 * mis-reading was. Each is CpuSet 0x10 halfwords into &gPal[a * 16] by
 * sub_08071C84 / sub_08071CA4, and handed to sub_08071B28 as the value it
 * stashes at the fade record's +0x20. `const u16 []` is the weakest model that
 * fits: 16 colours is what the 0x10-halfword count says, and nothing indexes
 * either symbol. */
extern const u16 gUnknown_08613F54[];
extern const u16 gUnknown_08613F74[];

/* ROM blob handed straight to sub_0801BD00's `void *` third parameter by
 * sub_08011704, with no arithmetic -- the same slot, and the same reasoning,
 * as gUnknown_0849957C and the other entries that note names. */
extern const u8 gUnknown_0848930C[];

/* ROM blob handed to sub_080152EC as its `const void *` first argument by
 * sub_08012FB8, with the literal 0 second argument -- the same install shape as
 * gUnknown_0849D41C and the rest of that group. */
extern const u8 gUnknown_08489354[];

/* Five more halfword IWRAM cells, all of them `strh`-only so far and all of
 * them addressed by aw2bhr.lds already.
 *
 * gUnknown_03000042 sits between the two named neighbours gUnknown_03000040
 * and gUnknown_03000044, and sub_0801220C copies gUnknown_03000044 into it
 * with a plain `ldrh`/`strh` pair -- so it is the same width as its source and
 * a snapshot of it, not a third member of an aggregate (three separate pool
 * words, the same reading gUnknown_03000044/46 already carry).
 *
 * The other four are only ever CLEARED, by that one function, so nothing here
 * settles their signedness or what they count -- `u16` is the store width and
 * no more. Flagged rather than claimed. */
extern u16 gUnknown_03000042;
extern u16 gUnknown_03001FD0;
extern u16 gUnknown_030024C8;
extern u16 gUnknown_03002F04;
extern u16 gUnknown_030030B0;

/* ---- the 0x0801B000 block (wave 28) ---------------------------------- */

/* 0x0200CD0C -- a one-byte gate read by all six wrappers at 0x0801B598 ..
 * 0x0801B66C (sub_0801B598, sub_0801B5C0, sub_0801B5E8, sub_0801B618,
 * sub_0801B648, sub_0801B66C). Every one of them does `ldrb; cmp #1` and
 * dispatches only when it equals 1; five of the six return a constant 1 and the
 * sixth returns its own second parameter when it does not. u8 is the access
 * width in all six. NOT const -- nothing in this block writes it, so whatever
 * arms it lives elsewhere; and nothing here says what the value 1 means, so the
 * comparison against a small constant is all that is claimed. */
/* Wave 31, W31-B. The five arguments sub_0801A79C parks before it starts the
 * link scan -- four whole words (`str`) and one byte (`strb`). Nothing that is
 * matched reads them back: the only other users, sub_0801A7D8, sub_0801ABF8 and
 * sub_0801ADC8, are all still asm, so `int` is the weakest model that fits the
 * store width and the shape is otherwise unproved. */
/* Wave 31, W31-B. The flash chip's "program one byte" routine, installed by
 * sub_0808AB8C from +0x0c of the descriptor it selects. sub_0808B184 calls it
 * `bl _call_via_r3`, so THREE arguments -- r0 a literal 1, r1 the destination
 * and r2 the byte -- and re-narrows the result with `lsls #0x10; lsrs #0x10`,
 * which is a `u16` return. */
extern u16 (*gUnknown_03005C70)(int, u8 *, u8);
/* Wave 31, W31-B. A whole-word "already initialised" latch: sub_0808BBA4 runs
 * the 0x086170EC constructor list only while it is still zero, and sets it to 1
 * on the way in. `int` is the weakest model -- the only test is `cmp r0, #0`,
 * so nothing here fixes the signedness. */
extern int gUnknown_03000F80;
extern int gUnknown_0200CC24;
extern int gUnknown_0200CC28;
extern int gUnknown_0200CC2C;
extern u8 gUnknown_0200CC30;
extern int gUnknown_0200CC34;
extern u8 gUnknown_0200CD0C;

/* Wave 40 (W40-F). 0x02002000 -- the 0x1000-byte staging buffer the flash
 * wrappers fill and verify (sub_0801B66C writes it, sub_0801B648 verifies it,
 * sub_0801B6A8 0xff-fills it). sub_0801B09C both checksums it byte by byte and
 * reads a save-block header out of its first eight bytes plus its last one, so
 * `u8 []` is the declared type and the header layout is a cast view local to
 * src/decomp/c_0801B09C.c rather than this array's type.
 *
 * gUnknown_0808EF60, which asm/code.s shows in sub_0801B09C's literal pool, is
 * NOT a variable and must not be declared as one: 0x0808EF60 is a word in
 * data/rodata.s whose contents are 0x02002000, i.e. agbcc's own -fforce-addr
 * address-constant slot for THIS array. `ldr r0, =gUnknown_0808EF60;
 * ldr r3, [r0]` is the ordinary two-load materialisation of the array's
 * address. Measured, not assumed: modelling it as
 * `extern struct X *gUnknown_0808EF60;` makes the compiler emit its own
 * address-constant slot on top of the pointer load and costs one extra `ldr`
 * at every read. */
extern u8 gUnknown_02002000[];

/* Wave 40 (W40-F). 0x0200CC38 -- six parallel per-slot arrays of 0x10 slots
 * each, reset together by sub_0801B4C0 and indexed one at a time elsewhere
 * (sub_0801B018 touches only unk20).
 *
 * ONE struct rather than six separate arrays, and the codegen is what says so:
 * sub_0801B4C0 keeps the base address in r6 across its whole loop and reaches
 * every member as `base + <const>` computed at RUNTIME, which is -fforce-addr
 * on a single object. Six separate arrays would each fold their own pool word.
 *
 * unk20 is also reachable as the folded address constant 0x0200CC58, which
 * asm/ prints as a separate symbol gUnknown_0200CC58. That is not a second
 * object: it is what LICM does to `gUnknown_0200CC38.unk20[i]` when `i` is
 * loop-invariant, so sub_0801B018's two spellings of the same member -- one
 * hoisted out of its retry loop, one after it -- are one member.
 *
 * Widths are the access widths and nothing here fixes any signedness: unk00
 * and unk10 take `|= 0xff`, unk20 is a bit field set and cleared a bit at a
 * time (bit 0 and bit 2), unk30 and unk40 are plain byte stores.
 *
 * The struct STOPS at 0x50, and that boundary is measured rather than assumed.
 * The two word arrays at 0x0200CC88 are reset by the same loop and would look
 * like two more members, but sub_0801B4C0 initialises their induction variable
 * from the FOLDED address constant 0x0200CC88 while it builds unk30's from the
 * common base register (`movs r1, #0x30; adds r1, r1, r6`). A member of this
 * struct folds against the base; a separate object folds against itself. */
struct Unk0200CC38
{
    /* 0x00 */ u8 unk00[0x10];
    /* 0x10 */ u8 unk10[0x10];
    /* 0x20 */ u8 unk20[0x10];
    /* 0x30 */ u8 unk30[0x10];
    /* 0x40 */ u8 unk40[0x10];
};

extern struct Unk0200CC38 gUnknown_0200CC38;

/* Wave 40 (W40-F). 0x0200CC88 -- two more per-slot arrays alongside
 * gUnknown_0200CC38 above, whole words this time and 0x10 slots each, cleared
 * together by sub_0801B4C0's reset loop. ONE declaration of 0x20 words rather
 * than two of 0x10: the ROM reaches both through a single induction variable
 * stepping by 4, with the upper array as a `str [rN, #0x40]` displacement off
 * it, which is what strength reduction does to two subscripts of one array and
 * not to two objects. Nothing types the contents beyond the store width. */
extern u32 gUnknown_0200CC88[];

/* Wave 50 (W50-K). 0x0200CD08 -- a single WORD, written by sub_0801B2FC and by
 * nothing else that has been read: cleared to 0 before the slot scan, then set
 * to `gUnknown_02002000's +8 word + 1` each time a newer save slot wins. It is
 * only ever stored, never loaded here, so nothing fixes the signedness beyond
 * the store width; u32 agrees with gUnknown_0200CC88, which holds the same +8
 * word and IS compared unsigned (`blo`/`bhs`) in the same function.
 *
 * It is reached through the -fforce-addr pool word at 0x0808EF54, which is part
 * of the 0x0808EF54-0x0808EF60 run; the address itself is bound by aw2bhr.lds,
 * which is why naming it directly links. */
extern u32 gUnknown_0200CD08;

/* 0x03005C74 / 0x03005C80 / 0x03005C84 -- three FUNCTION POINTER VARIABLES.
 * All three sit at even addresses and all three are read as
 * `ldr rN, =sym; ldr rN, [rN]` before the indirect call, i.e. a genuine
 * dereference -- unlike the odd-address pool words below, which are the
 * pointers' values.
 *
 * Their ARITIES are read straight off the veneer register, which is the one
 * wrapper-side tell that has ever held in this tree: gcc parks the callee
 * pointer in the first FREE scratch register, so the index counts the
 * arguments.
 *   sub_0801B5C0  ldr r0,[0x03005C80]; bl _call_via_r0  -> 0 arguments
 *   sub_0801B5E8  ldr r1,[0x03005C84]; bl _call_via_r1  -> 1 argument
 *   sub_0801B618  ldr r2,[0x03005C74]; bl _call_via_r2  -> 2 arguments
 * Each result is re-narrowed with `lsls #0x10; lsrs #0x10` after the call,
 * which is agbcc re-narrowing a u16-returning callee, so the return is u16.
 * The leading argument of the one- and two-argument forms is the wrapper's own
 * u16 parameter passed through. The second argument of the two-argument form is
 * invisible -- it rides r1 untouched -- and `int` is the weakest type that
 * costs no instruction there. */
/* Wave 32 (W32-B): the flash chip descriptor that sits beside the
 * gUnknown_03005C7x routine pointers. sub_0808B074, sub_0808B0E8 and
 * sub_0808B3C0 read two members -- unk10, the WAITCNT low two bits this chip
 * wants (`REG_WAITCNT = (REG_WAITCNT & 0xfffc) | unk10`), and unk08, the sector
 * shift (`0x0E000000 + (sector << unk08)`). Both are bare loads, `ldrh` and
 * `ldrb`, so the widths come from the loads and the signedness is unproved.
 * Nothing bounds the record; the fillers are "what fits below the next one".
 *
 * Wave 46 (W46-D): unk14 added -- an EXTENSION past the old end, no existing
 * member moved. sub_0808ADA4 reads `ldrh r1, [r0, #0x14]` and compares it
 * against 0x1CC2 to decide whether a timed-out write needs the 0xF0 reset
 * command, so +0x14 is a u16 chip ID. It is the SAME halfword as
 * gUnknown_0848548C's unk28, reached the other way round and that is what
 * makes it more than a guess: sub_0808AB8C sets gUnknown_03005C78 to
 * descriptor + 0x14, so this record's +0x14 is descriptor +0x28 -- and +0x28
 * is independently the id sub_0808AB8C matches ReadFlashId's result against.
 * A producer and a consumer of one field agreeing without being derived from
 * each other. */
/* Wave 47 (W47-D): unk04 added -- an EXTENSION inside the old filler_00, no
 * existing member moved or rewidened. sub_0808B2E0 reads `ldr r1, [r0, #4]`
 * and counts DOWN from it while walking a byte pointer, and sub_0808B31C reads
 * the same word and stores it to gUnknown_03005C7C with a truncating `strh`
 * before using it as that same walk's counter. A WORD at both sites, used only
 * as an unsigned count (`cmp #0` / `subs #1`), hence u32: it is the sector's
 * SIZE IN BYTES.
 *
 * It is the same halfword-pair as gUnknown_084856A4 +0x18, and that
 * correspondence is what makes the whole record legible -- see the
 * gUnknown_084856A4 note below. */
struct Unk03005C78
{
    /* 0x00 */ u8 filler_00[0x04];
    /* 0x04 */ u32 unk04;
    /* 0x08 */ u8 unk08;
    /* 0x09 */ u8 filler_09[0x07];
    /* 0x10 */ u16 unk10;
    /* 0x12 */ u8 filler_12[0x02];
    /* 0x14 */ u16 unk14;
};

extern struct Unk03005C78 *gUnknown_03005C78;
/* Wave 32 (W32-B): sub_0808B3C0 reads the halfword at +0x24 and uses it exactly
 * the way its twin sub_0808B074 uses gUnknown_03005C78->unk10 -- as the WAITCNT
 * low two bits. Spelled as a u16 subscript because a subscript is all the ROM
 * shows; whether +0x24 is one member of a larger flash record is not settled.
 *
 * Wave 47 (W47-D) SETTLES IT, and the declaration is deliberately left alone
 * anyway. 0x084856A4 is one `struct Unk0848548C` -- a flash chip descriptor of
 * the same shape as the entries of the gUnknown_0848548C table -- and the three
 * offsets the erase path reads off it are exactly the three members
 * `struct Unk03005C78` already names, shifted by the +0x14 that sub_0808AB8C
 * adds when it installs gUnknown_03005C78:
 *
 *     gUnknown_084856A4 +0x18  ldr   ==  Unk03005C78 +0x04  unk04  sector bytes
 *     gUnknown_084856A4 +0x1c  ldrb  ==  Unk03005C78 +0x08  unk08  sector shift
 *     gUnknown_084856A4 +0x24  ldrh  ==  Unk03005C78 +0x10  unk10  WAITCNT bits
 *     gUnknown_084856A4 +0x28  ldrh  ==  Unk03005C78 +0x14  unk14  chip id
 *
 * Four independent offsets agreeing in width AND in use across two symbols
 * nobody derived from the other. It is NOT respelled as a struct here because
 * `struct Unk03005C78` is declared 0x16 bytes wide against a 0x14-byte
 * reservation (see gUnknown_0848548C's note), so embedding it would move +0x28;
 * the subscript and the two casts in c_0808B430.c / c_0808B4B4.c are the honest
 * statement of what the ROM shows. Retyping this is a separate job that has to
 * bound Unk03005C78 first. */
extern const u16 gUnknown_084856A4[];
/* Wave 47 (W47-H). 0x08485678 -- read at ONE site only, sub_0808B5B8, which
 * does `ldr r0,=gUnknown_08485678; ldr r0,[r0,#0x18]` and stores the word
 * truncated into gUnknown_03005C7C as the byte count of a whole save slot,
 * then walks it down by gUnknown_084856A4 +0x18 (one sector) per iteration.
 * So +0x18 is a 32-bit member and it is NOT the same member gUnknown_084856A4
 * +0x18 is, despite the identical offset -- one is the slot size, the other
 * the sector size, and the loop would run exactly once if they agreed.
 * 0x084856A4 - 0x08485678 = 0x2C, so this is a DIFFERENT record, not the same
 * one at another offset; nothing else in the tree reads it and no other
 * member is observed, so the width is spelled the way the one access reads it
 * and the shape is left open. */
extern const u16 gUnknown_08485678[];
/* Wave 47 (W47-D). 0x03005C7C -- the flash sector-program loop's REMAINING
 * BYTE COUNT, and a genuine global rather than a local: sub_0808B31C seeds it
 * from gUnknown_03005C78->unk04 with a truncating `strh`, then re-loads it
 * (`ldrh [r6]`) at the top of every iteration and stores it back after every
 * `subs #1`. It needs no `volatile` for that -- the reload is forced by the
 * sub_0808B184 call in the loop body, which agbcc must assume can touch it.
 * u16 by every access, and unsigned by the `cmp #0` exit test. */
extern u16 gUnknown_03005C7C;
extern u16 (*gUnknown_03005C74)(u16, int);
extern u16 (*gUnknown_03005C80)(void);
extern u16 (*gUnknown_03005C84)(u16);

/* Wave 46 (W46-D). gUnknown_081D9478 and gUnknown_081D947C ARE NOT VARIABLES
 * AND MUST NOT BE DECLARED AS ANY. They are the two words of ONE agbcc
 * -fforce-addr address-constant block belonging to sub_0808A3DC -- adjacent,
 * addends 0 and 4, exactly the shape the .rodata chapter of
 * docs/agbcc-codegen.md describes. Dereferenced in baserom.gba they hold
 * 0x0849957C and 0x03001FE8, i.e. the addresses of gUnknown_0849957C (declared
 * above) and gUnknown_03001FE8 (`union BgCntBuf` in hardware.h) -- two globals
 * that already exist under real names.
 *
 * So the `ldr rN, =gUnknown_081D9478; ldr rN, [rN]` that asm/ shows is agbcc's
 * ordinary two-step materialisation of ONE global's address, not a pointer
 * variable being read. Declaring them costs an extra indirection at every use:
 * measured, `u16 **gUnknown_081D9478` compiles to four chained `ldr`s where the
 * ROM has three. Name the real globals and agbcc rebuilds this block itself.
 *
 * This is the wave-27 rule ("before declaring a gUnknown_08xxxxxx that is only
 * ever reached by a double ldr, read the ROM word at that address") paying off
 * twice in one function, and it is worth doing FIRST rather than after a failed
 * attempt: both symbols look exactly like ROM pointer tables in the index. */
/* Two LZ77 blobs handed to Decompress, hence `u8 []` per that prototype. These
 * two ARE real objects -- both are reached only by address (`ldr r0, =sym`,
 * never a load through them) and both relocate directly rather than through the
 * .rodata block above. */
extern u8 gUnknown_0823E684[];
extern u8 gUnknown_0823E7A0[];

/* Wave 46 (W46-D). 0x0848548C -- the flash CHIP DESCRIPTOR TABLE: an array of
 * pointers to records, scanned by sub_0808AB8C, which then installs five of
 * each record's members into the gUnknown_03005C7x / gUnknown_03000F68
 * routine-pointer globals above and points gUnknown_03005C78 at the record's
 * +0x14. An array of POINTERS and not of records: the scan steps its cursor by
 * 4 and re-loads `[r2]` at every use.
 *
 * Every member's TYPE here is copied from the global it is stored into, which
 * is why nothing new is being typed -- unk00, unk04, unk08, unk0c and unk10
 * are just gUnknown_03005C74, gUnknown_03005C80, gUnknown_03005C84,
 * gUnknown_03005C70 and gUnknown_03000F68 before installation, in that order.
 *
 * unk28 is a UNION and the ROM proves it: the loop's terminator test reads a
 * BYTE at +0x28 (`adds r0, #0x28; ldrb`) while the chip-id compare one
 * instruction later reads a HALFWORD at the same +0x28 (`ldrh r0, [r1, #0x28]`).
 * Two access widths at one offset in one basic block is the discriminating use
 * that a single scalar member cannot explain.
 *
 * unk14 is spelled as a RAW 0x14 BYTES rather than as an embedded
 * `struct Unk03005C78`, deliberately. sub_0808AB8C only ever takes its address
 * (`adds r0, #0x14`) and hands it to gUnknown_03005C78, so nothing here reads
 * the record's interior, and embedding the struct would tie this table's +0x28
 * offset to a shared declaration this function has no evidence about: measured,
 * embedding it puts unk28 at +0x2C and not the +0x28 the ROM loads. Whether
 * Unk03005C78's declared 0x12 or this reservation of 0x14 is the true width is
 * left where it was; the cast at the one use is the honest statement of what is
 * known. */
struct Unk0848548C
{
    /* 0x00 */ u16 (*unk00)(u16, int);
    /* 0x04 */ u16 (*unk04)(void);
    /* 0x08 */ u16 (*unk08)(u16);
    /* 0x0c */ u16 (*unk0c)(int, u8 *, u8);
    /* 0x10 */ u16 *unk10;
    /* 0x14 */ u8 unk14[0x14];
    /* 0x28 */ union
    {
        u16 joined;
        struct
        {
            u8 makerId;
            u8 deviceId;
        } separate;
    } unk28;
};

extern struct Unk0848548C *gUnknown_0848548C[];

/* 0x03005C88 .. 0x0300677C -- an IWRAM CODE OVERLAY, not a data buffer.
 * sub_0801B6BC copies 0x086173F0 over it with
 * `CpuSet(src, 0x03005C88, ((0x0300677C - 0x03005C88) / 4) | 0x04000000)`, and
 * three wrappers in the same block then CALL into the copied region through
 * pool words holding ODD addresses -- 0x03005C89, 0x03005E8D, 0x03006029 --
 * with no load in between. The low bit set is a THUMB entry point, and
 * aw2bhr.map allocates no object at any of the three (`*fill*`), so those words
 * are the pointers' VALUES that gen_lds.py invented symbols for, exactly as
 * already documented for sub_0801B6EC's 0x0300619D. They are spelled `&symbol`
 * at the call sites because the honest literal emits a bare `.word` where the
 * ROM has `.word 0` plus a relocation.
 *
 * u8 arrays and not u32: the length the ROM computes carries the
 * `cmp #0; bge; adds #3` rounding correction of a signed divide by four, which
 * a `u32 *` subtraction would not need because the compiler would know the
 * difference is already a word count.
 *
 * Arities of the three entry points, by the same _call_via_rN readout as above:
 *   0x03005C89  _call_via_r4 (sub_0801B738)  -> 4 arguments, first u8
 *   0x03005E8D  _call_via_r3 (sub_0801B724)  -> 3 arguments, third u16
 *   0x03006029  _call_via_r3 (sub_0801B70C)  -> 3 arguments, second and third u16
 * Nothing types the pass-through arguments; `int` is what costs no
 * instruction. */
extern u8 gUnknown_03005C88[];
extern u8 gUnknown_0300677C[];
extern u8 gUnknown_03005C89;
extern u8 gUnknown_03005E8D;
extern u8 gUnknown_03006029;
extern const u8 gUnknown_086173F0[];

/* 0x03000058 / 0x0300005C -- a pair of word counters advanced in step.
 * sub_0801B964 adds its parameter to BOTH and then wraps only 0x03000058:
 * `cmp #7; bgt` on the running total, and `-= 8` on the taken side, returning 1
 * when it wrapped and 0 when it did not. SIGNED and 32 bits: every access is a
 * bare `ldr`/`str` and the compare is `bgt`, not `bhi`. What they count is not
 * settled here -- a 3-bit sub-position and its unwrapped twin is consistent
 * with the shape but is not evidence. */
extern int gUnknown_03000058;
extern int gUnknown_0300005C;

/* 0x03002F2C / 0x0300141C / 0x030030D4 -- latches recording the source of the
 * most recent OAM flush. sub_0801BBC4 and sub_0801BCA8 store
 * gUnknown_03000268.unk00 into 0x03002F2C and gUnknown_03002520 (the OAM
 * shadow) into 0x030030D4; sub_0801BC08 stores gUnknown_03000278.unk00 into
 * 0x0300141C. A plain word `str` of a pointer in every case. Nothing in this
 * block READS any of the three, so `void *` records the store width plus the
 * one thing the stored value is known to be, and no more. */
extern void *gUnknown_0300141C;
extern void *gUnknown_03002F2C;
extern void *gUnknown_030030D4;

/* 0x030024C0 -- zeroed with `strh` by sub_0801BBC4 and sub_0801BCA8 in the same
 * breath as the two latches above, i.e. a halfword cursor reset once the
 * pending flush has been consumed. u16 is the store width; no reader appears in
 * this block, so the sign is NOT settled. */
extern u16 gUnknown_030024C0;

/* 0x080A5524 -- a 0x500-byte ROM blob. sub_0801B780 hands it to sub_08011E54
 * with a destination built as
 * `BG char base * 0x4000 + ((id + 1) & 0x3FF) * 32 + 0x06000000`, which is a
 * VRAM tile address, so this is tile graphics. Referenced by address only
 * (`ldr r0, =sym`, never a load through it). Not spelled const: sub_08011E54's
 * first parameter is a plain `void *`. */
extern u8 gUnknown_080A5524[];

/* ---- the 0x0806E000 block (wave 28) ---------------------------------- */

/* 0x08582754 -- an array of PALETTE POINTERS, not palette data. sub_0806EB28
 * indexes it with `lsls #2` and then LOADS through the result
 * (`adds r1, r1, r0; ldr r0, [r1]`) before handing it to ApplyPaletteExt, so
 * the stride is a word and the element is the pointer. At least four entries:
 * the index is `gUnknown_03004008 & 3`, bumped by one when it collides with the
 * proc's current selection, so 0..4 are reachable. */
extern u16 *gUnknown_08582754[];

/* 0x081A47E4 -- a halfword lookup table read at `(gUnknown_03004008 & 0x1F) / 2`
 * by sub_0806E7FC, whose result goes straight into gPal at halfword 0x1EC. The
 * `lsrs #1; lsls #1` pair is the /2 fused with the u16 array's own scale and is
 * NOT a mask -- counted as `(u32)x >> 1 << 1` it is the byte offset of element
 * x/2. So the table is at most 16 entries as reached from here. */
extern const u16 gUnknown_081A47E4[];

/* 0x08582BFC -- the proc script sub_0806E7C0 starts blocking. Its proc carries
 * unk2c/unk30/unk34/unk38 as words, which is the same field set sub_0806E740
 * steps, so 0x08582BFC's proc and sub_0806E740's are the same object. */
extern const struct ProcCmd gUnknown_08582BFC[];

/* ---- the 0x0801E000 block (wave 28) ---------------------------------- */

/* 0x0300054E -- a SIGNED halfword index, and the `ldrsh` off a ZERO index
 * register (`movs r0, #0; ldrsh r1, [r4, r0]`) is what pins it: an unsigned
 * halfword would have been `ldrh [r4, #0]` with no index register at all.
 * sub_0801E968 reads it TWICE in one body -- once to select an entry of
 * gUnknown_03001470 and once as the first argument of the callback it then
 * dispatches -- and the second read is a fresh load, so it is not const. */
extern s16 gUnknown_0300054E;

/* 0x03000548 -- only ever referenced BY ADDRESS: sub_0801E968 passes
 * `&gUnknown_03000548` as the second argument of the dispatched callback and
 * never loads through it here. So nothing in this block settles what it holds
 * or how wide it is; `u32` records the alignment its address implies and no
 * more. Flagged rather than claimed. */
extern u32 gUnknown_03000548;

/* 0x03000550 -- the most recently dispatched handler. sub_0801E968 loads the
 * pointer out of gUnknown_0200E438[...].unk44, stores it here, and then calls
 * it through the SAME register without re-reading the global, which is the
 * ordinary non-volatile store-forwarding and rules volatile out.
 *
 * The signature is read off the dispatch: `bl _call_via_r2` with r0 and r1 set
 * up means TWO arguments (gcc parks the pointer in the first free scratch
 * register). r0 is gUnknown_0300054E sign-extended, r1 is
 * `&gUnknown_03000548`. The stored value comes from a `u32` member, so the
 * assignment casts. */
extern void (*gUnknown_03000550)(s16, void *);

/* Wave 29, W29-A. A run of 16-colour palettes in ROM: sub_0802D5CC indexes it
 * with `lsls r0,#5` (32 BYTES per row) and hands the address straight to
 * ApplyPaletteExt's `u16 *` first parameter, so `u16 []` with 16 entries per
 * row. NOT const, for the same reason as every other ApplyPaletteExt source in
 * this header -- that prototype takes a plain `u16 *`. The row count is not
 * bounded by anything readable; the only caller passes a variable. */
extern u16 gUnknown_080D4188[];
/* Wave 29, W29-A. A 2-D halfword table with a 0x32-BYTE row, i.e. 0x19
 * halfwords per row -- sub_080261A4 scales the row index by `muls #0x32` and
 * the column by `lsls #1` and adds both to the symbol directly, so this is an
 * array and not a pointer. Its one reader shifts the loaded halfword left by 2
 * and sub_08002844 masks the result with 0x3ff, so the entries are tile
 * indices. Non-const is unproved either way: nothing writes it and nothing
 * takes its address. */
extern u16 gUnknown_08499608[][0x19];
/* Wave 29, W29-A. The proc script sub_0802A54C starts, parented to its own
 * second argument; the new proc's +0x4c is filled with the struct UnitRecord *
 * the caller passed, and sub_0802A588 reads it back out of the same slot. */
extern const struct ProcCmd gUnknown_0849A198[];
/* Wave 29, W29-A. Two gUnknown_03001470 list scripts of the gUnknown_0849A108
 * shape: sub_08005560 and sub_08005598 are one body twice, each
 * `sub_080152EC(blob, 0)` after clearing gUnknown_0200B0B0->unk02, and these are
 * the only two words they differ in. `const u8 []` because sub_080152EC's first
 * parameter is `const void *` and nothing dereferences either symbol. */
extern const u8 gUnknown_08488444[];
extern const u8 gUnknown_0848846C[];

/* ---- wave 29 (C): the 0x0808A block ---- */
/* A word-wide mode selector: sub_0808A340 zeroes it with `str` and
 * sub_0808A5C4 branches on it being 0 or 1 to pick between two nearly identical
 * sub_08014A5C calls. `int` -- the compares are `cmp r1, #0` / `cmp r1, #1`
 * with no narrowing anywhere. */
extern int gUnknown_03005908;
/* Handed to sub_080193B0(const u8 *) by sub_0808A844, exactly the way
 * gUnknown_0849A520 and its neighbours are; `const u8 []` on that precedent. */
extern const u8 gUnknown_084A0D58[];
/* A proc script sub_0808A884 starts TWICE under its own proc as parent -- once
 * when sub_08019260 reports false and once on a key press -- from the same
 * pool word. */
extern const struct ProcCmd gUnknown_08617094[];

/* 0x08616EB4 -- the proc script sub_08088004 Proc_Starts under its own argument.
 * Read straight out of the ROM rather than inferred: the first record is
 * `02 00 00 00 / 45 80 08 08`, i.e. opcode 2 with the THUMB entry 0x08088044,
 * followed by `0E 00 40 00` and then opcode 3 records pointing at 0x080880BC
 * and 0x0808844C -- the standard ProcCmd layout, and the callees are all in the
 * 0x08088 block this script drives. */
extern const struct ProcCmd gUnknown_08616EB4[];

/* ---- wave 29 (C): the 0x0806C block ---- */
/* A NULL-terminated table of POINTERS, and the two readings agree: sub_0806C114
 * indexes it with `lsls #2` off the bare symbol -- no load in front, so the
 * symbol IS the array, unlike gUnknown_08499590 -- and then tests the loaded
 * word against 0 to find the end; sub_0806C0E4 dereferences the same word at
 * +0x30 and compares that against the proc's own +0x30 with `blo`, an UNSIGNED
 * ordering, so both are u32. Nothing else about the pointee is readable. */
/* Wave 48 (W48-C): filler_00 CARVED into six 8-byte records. sub_0806BF40 walks
 * `gUnknown_0858265C[proc->unk38]` with a stride of 8 and a bound of `i <= 5`
 * INCLUSIVE, reading a `kind` word at +0 and a string pointer at +4. Six times
 * eight is 0x30 -- exactly where unk30 already sits -- so the extent is fixed by
 * the existing member rather than guessed, the same way struct Unk0200C420's
 * unk38[0x2a] is fixed by the struct size. unk30 is untouched at its old offset
 * and nothing in src/ named this struct before, so the carve cannot regress a
 * matched function.
 *   unk04 is `u8 *` from its use: it is sub_0806BD1C's second parameter, a
 * NUL-terminated byte string it renders a glyph at a time. unk00 is `int` from
 * the `ldr` and is a small tag -- sub_0806BF40 tests it against 1, 3, 4 and 5
 * with four independent `if`s (not a switch: each test re-loads and each arm
 * falls through to the next). */
struct Unk0858265CRec /* 0x08 */
{
    /* 0x00 */ int unk00;
    /* 0x04 */ u8 *unk04;
};
struct Unk0858265C
{
    /* 0x00 */ struct Unk0858265CRec unk00[6];
    /* 0x30 */ u32 unk30;
};
extern struct Unk0858265C *gUnknown_0858265C[];
/* The proc script sub_0806C114 starts blocking under its own proc before
 * jumping that proc back to label 0 -- a per-entry sub-sequence driven by the
 * gUnknown_0858265C cursor. */
extern const struct ProcCmd gUnknown_08581C48[];
/* 0x081A3D84 -- the twin of gUnknown_081A47E4 below: a halfword table read at
 * `(gUnknown_03004008 & 0x1F) / 2` by sub_0806C1E4, whose result goes into gPal
 * at halfword 0x12c. The `lsrs #1` then `lsls #1` is the /2 fused with the u16
 * array's own scale, not a mask, so at most 16 entries as reached from here. */
extern const u16 gUnknown_081A3D84[];

/* ---- wave 29 (C): the 0x08064 block ---- */
/* An 8-byte ROM image that sub_08064774 copies onto its own stack with
 * sub_0808B6E8 and then indexes as halfwords with obj->unk48, so four u16
 * sprite ids. It sits in the same 0x0816Exxx run as gUnknown_0816E1B8, which is
 * that unit's .rodata, so this is very likely a local array initialiser image;
 * it is spelled as an explicit copy because the ROM's call is a `bl` to the
 * game's own sub_0808B6E8 rather than to a compiler helper. */
extern const u16 gUnknown_0816E0C0[];

/* ---- wave 29 (C): the 0x0804B block ---- */
/* A 0x18-byte ROM record. The stride is read off `lsls #1; adds; lsls #3` in
 * both readers, which is a multiply by 3 << 3 = 24, i.e. an array index and not
 * hand-rolled arithmetic. sub_0804BB28 hands +0x04 to LZ77UnCompVram;
 * sub_0804BB44 indexes a word array at +0x0c with gUnknown_03004520 and hands
 * the entry to CpuFastSet. Three words fit between 0x0c and the 0x18 stride.
 * Nothing yet reads 0x00 or 0x08. Non-const: both consumers take
 * `const void *`, so const buys nothing and would only force casts on the
 * three unmatched readers (sub_0804B8BC, sub_0804BB74, sub_080566C8). */
struct Unk08555850 /* 0x18 */
{
    /* 0x00 */ u8 filler_00[0x04];
    /* 0x04 */ void *unk04;
    /* 0x08 */ u8 filler_08[0x04];
    /* 0x0c */ void *unk0c[3];
};
extern struct Unk08555850 gUnknown_08555850[];
/* Rows of FIVE words: sub_0804BD20 scales its second index by
 * `lsls #2; adds; lsls #2` = 5 * 4 and its first by 4, then loads a word and
 * uses it as a CpuFastSet source. */
extern void *gUnknown_08555D30[][5];

/* ---- wave 29 (C): the 0x0803F block ---- */
/* A sprite/animation descriptor blob: sub_0803F400 hands its ADDRESS to
 * sub_0801C210, exactly the way gUnknown_030032CC's neighbours above do.
 * `u8 []` on that precedent; extent unknown, and NOT const because
 * sub_0801C210's first parameter is still a plain `void *`. */
extern u8 gUnknown_081171EC[];
/* Proc scripts. gUnknown_0849F940 is handed to Proc_StartBlocking by
 * sub_0803F510 (which then writes +0x2c/+0x30 of the result);
 * gUnknown_0849FADC to Proc_Start(.., PROC_TREE_3) by sub_0803FEDC and
 * sub_0803FF04, neither of which touches the returned proc. */
extern const struct ProcCmd gUnknown_0849F940[];
extern const struct ProcCmd gUnknown_0849FADC[];
/* Wave 32 (W32-B): a four-entry byte table sub_0803FE50 indexes with
 * `(x & 0x18) >> 3` -- a SIGNED shift, so the index expression is `int` -- and
 * multiplies the result by 8 to make a tile index. Bare `ldrb`, hence u8. */
extern const u8 gUnknown_0849FAD8[];
/* Wave 32 (W32-B): the script sub_0803FF48 both sweeps with Proc_ForEach and
 * then starts a fresh instance of. */
extern const struct ProcCmd gUnknown_0849FB04[];
/* A ROM graphics blob read in 0x20-word chunks: sub_0803F8E0 CpuFastSets
 * `gUnknown_080D20C4 + ((n * 4) & 0x3FF) * 32` to 0x06010900, so the unit is a
 * 32-byte 4bpp tile and the transfer is four of them. `const u8 []` --
 * CpuFastSet's first parameter is `const void *`, so const costs nothing.
 * Extent not pinned. */
extern const u8 gUnknown_080D20C4[];
/* Wave 49, W49-C. Two ROM PALETTE BANKS 0x100 bytes apart, i.e. eight 16-colour
 * palettes each, reached only as whole palettes: sub_0803F80C indexes 0803EE4
 * with `lsls #5` off gUnknown_08499598[n].unk1a and also names element 6
 * directly (`adds rB, #0xc0`), and sub_0803F880 picks between the two banks on
 * its first argument and then takes `&bank[unk1a][6]` (`adds rB, #0xc`) as a
 * two-colour source. The `[16]` inner bound is what makes both the 0x20-byte
 * stride and the two member offsets fall out of ordinary subscripts.
 *   NOT const, and this is a type-compatibility choice rather than a codegen
 * one: every consumer is ApplyPaletteExt / sub_0801368C, both of which declare a
 * plain `u16 *` first parameter. Only the ADDRESS is ever computed here -- no
 * element is loaded -- so const would be byte-neutral either way and buys
 * nothing but casts. */
extern u16 gUnknown_080D3DE4[][16];
extern u16 gUnknown_080D3EE4[][16];

/* ---- wave 30 (W30-C): the 0x0806A block ---- */

/* A signed 32-bit horizontal scroll offset for the 0x0806A054 screen, and the
 * word width is proved three ways: sub_0806A158/sub_0806A180 read it with a
 * plain `ldr` before truncating to REG_BG0HOFS, sub_0806A1A8 stores -0xF0 with
 * `str`, and sub_0806A1D0 stores Interpolate's `s32` return into it directly.
 * Signed because both the negate (`rsbs r0, r0, #0`) and the stored -0xF0 want
 * it. Bound by aw2bhr.lds at 0x0202F20C. */
extern s32 gUnknown_0202F20C;
/* A signed 16-bit cursor read `movs r2,#0; ldrsh r1,[r1,r2]` by sub_0806A534 --
 * a register-offset `ldrsh` at displacement zero, which is a plain s16 scalar
 * and not an array index (Thumb has no immediate-offset ldrsh). Bound by
 * aw2bhr.lds at 0x0202F210. */
extern s16 gUnknown_0202F210;
/* The same shape one region up: sub_0806A534 reads it with the identical
 * zero-displacement `ldrsh` and adds 0x206 before masking with 0x1FF, so it is
 * a signed halfword. Bound by aw2bhr.lds at 0x0300060C. */
extern s16 gUnknown_0300060C;
/* A ROM blob handed straight to sub_0801BD00's `void *` third parameter by
 * sub_0806A534, with NO load in front of the pool word -- the symbol IS the
 * data, unlike the pointer globals in this region. Not const, because that
 * parameter is a plain `void *`. Extent not pinned. */
extern u8 gUnknown_085815C8[];
/* A 16-byte ROM row indexed by sub_0806AADC's `proc->unk4c` (`lsls #4` off the
 * bare symbol -- no indirection, so the symbol is the array). +0x00 goes to
 * Decompress's `u8 *` source and +0x04 to ApplyPaletteExt's `u16 *` source,
 * which is what fixes both element types; neither may be const, because both
 * of those parameters are plain pointers. +0x08..+0x0F is unread here. */
/* Wave 32 (W32-C) fills in +0x08 and +0x0C from sub_0806C380 / sub_0806C410,
 * which draw the row: +0x08 goes to PutSpriteExt's `u16 *` OAM argument and
 * +0x0C is read `movs rN,#0xc; ldrsh` -- a REGISTER-offset ldrsh, which in
 * THUMB is the only encoding there is, so the sign is the type's and not a
 * cast. */
struct Unk085816F0
{
    /* 0x00 */ u8 *unk00;
    /* 0x04 */ u16 *unk04;
    /* 0x08 */ u16 *unk08;
    /* 0x0C */ s16 unk0c;
    /* 0x0E */ u8 filler_0e[0x02];
};
extern struct Unk085816F0 gUnknown_085816F0[];
/* Wave 48 (W48-C). A ROM halfword table indexed by sub_0807B7BC's return value
 * (`lsls #1`) in sub_0806AB24, where the element is a per-line WIDTH in pixels:
 * it is multiplied by `lines - 1`, added to the residual width sub_0807B7BC
 * writes back through its `u16 *` out-parameter, and the total is centred
 * against 0xf0 (the screen width). `u16` from the `ldrh`; signedness unproved,
 * the only consumer is a multiply. Left unsized -- the index is a line count
 * with no checked bound. Not declared const: nothing proves it is never
 * written, and const would be a claim rather than a reading. */
extern u16 gUnknown_085816B4[];

/* ---- wave 32 (W32-C): the 0x0806C block ---- */

/* 0x0202F2C0. A word flag: sub_0806C218 clears it with `str`, and both
 * sub_0806C380 and sub_0806C410 gate a sub_0806C1E4 palette cycle on
 * `!= 0`. Nothing narrows it anywhere, so `int`. */
extern int gUnknown_0202F2C0;
/* sub_0806C218's two fixed blobs: 081A3D24 is an ApplyPaletteExt source
 * (`u16 *`) and 081A3BD4 a Decompress source (`u8 *`). Neither may be const --
 * both parameters are plain pointers. */
/* Wave 33, W33-D. sub_08067BD0's two blobs, typed from their consumers:
 * 08183780 is the `u16 *` ApplyPaletteExt loads into slot 0xa0, and 08581044 a
 * WORD TABLE of Decompress sources indexed by that function's first argument
 * (`lsls #2`, then `ldr`), so its element is `u8 *`. Neither may be const --
 * both of those parameters are plain pointers. The table's extent is not
 * measured; only the one indexed read exists. */
extern u16 gUnknown_08183780[];
extern u8 *gUnknown_08581044[];
/* Wave 33, W33-D. sub_0806C474's three blobs, typed from their consumers in
 * the same way as sub_0806C218's pair below: 081A29E4 is an ApplyPaletteExt
 * source (`u16 *`), 081A23B4 and 081A2854 are Decompress sources (`u8 *`) into
 * 0x06008000 and 0x0600F800. None may be const -- both parameters are plain
 * pointers -- and only the addresses are ever used. */
extern u16 gUnknown_081A29E4[];
extern u8 gUnknown_081A23B4[];
extern u8 gUnknown_081A2854[];
extern u16 gUnknown_081A3D24[];
extern u8 gUnknown_081A3BD4[];
/* OAM blobs. 08581A98 is PutSprite / PutSpriteExt's `u16 *` fourth argument in
 * sub_0806C380 and sub_0806C410; 08581A44 and 08581A5E are the two sub_0806C668
 * stores into the child proc's +0x54, which src/decomp/c_0806C1C8.c already
 * types `u16 *` from its own PutSprite call. */
/* WAVE 35 (W35-D), the 0x0806D block's ROM tables.
 * gUnknown_08581E70: a u16 step table indexed by struct Unk08580934_Obj's s16
 *   unk26 (`lsls #1; adds; ldrh`), added to unk2c by sub_0806D208 and
 *   subtracted by sub_0806D4DC.
 * gUnknown_08581F20: SIGNED halfwords -- sub_0806D688 reads it with
 *   `movs rI,#0; ldrsh` and then computes 0xc0 minus the result.
 * gUnknown_08581F04 / gUnknown_08581F12: two sprite scripts sub_0806D688
 *   selects between and parks in the object's +0x3c, which sub_0806D7A4 then
 *   compares against gUnknown_08581F12 to pick sprite id 0x8c or 0x8d. Only
 *   ever used as an opaque address, so the element type is a floor.
 *   0x0816E1A0 IS NOT A GLOBAL: baserom.gba holds 0x08581F12 there, i.e. the
 *   -fforce-addr pool word for this symbol, emitted because sub_0806D688
 *   references it twice.
 * gUnknown_08581F68 / gUnknown_08581F74: u16 tables sub_0806DF58 indexes with
 *   gUnknown_08580934->unk33 and ->unk02 to pick a graphics id. */
/* WAVE 35 (W35-D): the fade-kind dispatch table behind sub_080722B8
 * (StartFadeCore). Stride 12 (`lsls #1; adds; lsls #2`), and all three words
 * are reached: +0x00 is called with (gUnknown_08613EE4, parent) through
 * `bl _call_via_r2` -- two arguments, and it returns the proc the function
 * then writes +0x54/+0x4c on, so it is a Proc_Start-shaped starter; +0x04 is
 * called with ONE argument through `_call_via_r1`; +0x08 is the per-kind
 * multiplier that argument is scaled by, narrowed to s8 at the call. */
struct Unk81CBF68
{
    /* 0x00 */ ProcPtr (*unk00)(const struct ProcCmd *, ProcPtr);
    /* 0x04 */ void (*unk04)(int);
    /* 0x08 */ int unk08;
};
extern const struct Unk81CBF68 gUnknown_081CBF68[];
/* sub_08073304's two ROM assets: the compressed tile set it hands to
 * Decompress, and the 0x20-byte palette it uploads through ApplyPaletteExt.
 * Element types are the argument types of those two calls, nothing more. */
extern u8 gUnknown_081CC038[];
extern u16 gUnknown_081D2224[];
extern const u16 gUnknown_08581E70[];
extern const s16 gUnknown_08581F20[];
extern const u16 gUnknown_08581F04[];
extern const u16 gUnknown_08581F12[];
extern const u16 gUnknown_08581F68[];
extern const u16 gUnknown_08581F74[];
extern u16 gUnknown_08581A98[];
extern u16 gUnknown_08581A44[];
extern u16 gUnknown_08581A5E[];
extern const struct ProcCmd gUnknown_08581A80[];
/* 0x03000610. sub_0806CFC8 compares it against `gUnknown_03004008 - 1` and then
 * republishes the frame counter into it -- a "last frame I ran" latch, and the
 * same s32 as the counter it holds. */
extern s32 gUnknown_03000610;
/* 0x03000614 / 0x03000616. sub_0806CFC8's previous-frame cursor position, one
 * signed halfword each (`movs rI,#0; ldrsh rD,[rB,rI]` -- Thumb's only ldrsh is
 * the register-offset form, so a zero displacement is a plain s16 scalar).
 * They sit two bytes apart, right after gUnknown_0300060C / gUnknown_03000610.
 *
 * gUnknown_0816E17C AND gUnknown_0816E180 ARE NOT GLOBALS. They are agbcc's own
 * address-constant pool (`-fforce-addr`): baserom.gba holds 0x03000614 and
 * 0x03000616 at those two ROM addresses, four bytes apart, and gen_lds.py
 * invented a symbol at each. sub_0806CFC8 loads the ROM word and dereferences
 * it, which is exactly what agbcc emits when a global's ADDRESS is CSE'd into a
 * pseudo that lives across a call -- measured in wave 32 (W32-C) with five
 * probes: the same symbol referenced ONCE gets a direct inline pool word, and
 * referenced twice with a call in between gets the .rodata indirection. Naming
 * `gUnknown_0816E17C` as an `s16 *` reproduces the pool words but adds one
 * `ldr` per use, because the ROM word is already the indirection. */
/* WAVE 34 (W34-B) RETRACTS "CANNOT BE DECLARED". The claim was that
 * 0x03000600/0x03000602 and 0x03000614/0x03000616 fall INSIDE regions
 * aw2bhr.lds already names, so declaring them fails the split build with
 * `undefined reference`. The diagnosis was right and the conclusion was wrong:
 * the region boundaries are not a constraint, they are just the set of lines
 * anyone had written. Both linker scripts assign a strictly ASCENDING location
 * counter, so an interior address is only undeclarable until someone adds the
 * line for it -- and overlapping/interior symbols are already normal there (see
 * the gUnknown_0200BC14 / gUnknown_0200BFFC note).
 *
 * Wave 34 added four lines to aw2bhr.lds AND aw2bhr.split.lds:
 *     . = 0x000600 / 0x000602 / 0x000614 / 0x000616
 * which is what wave 32's own park note prescribed but declined to do. The
 * `undefined reference` was never evidence about the source spelling.
 *
 * This unblocks THREE functions that were parked on it across two waves --
 * sub_08064474, sub_08064500 (0x600/0x602) and sub_0806CFC8 (0x614/0x616) --
 * all of which had already been verified byte-for-byte with the honest
 * spelling. NOTE FOR THE NEXT WAVE: both .lds files are generated by
 * tools/gen_lds.py, so if that script is ever re-run these four lines must be
 * re-added or taught to it. */
extern s16 gUnknown_03000600;
extern s16 gUnknown_03000602;
extern s16 gUnknown_03000614;
extern s16 gUnknown_03000616;
/* sub_0806D050's set, one latch further along and the exact twin of
 * gUnknown_03000610 / gUnknown_03000614 / gUnknown_03000616 above: sub_0806D050
 * is sub_0806CFC8's source with a different sprite id (0x44 vs 0x43) and this
 * second triple. Same evidence -- s32 latch compared against
 * `gUnknown_03004008 - 1`, two s16 coordinates read `movs rI,#0; ldrsh`.
 *
 * WAVE 35 (W35-D): 0x0816E184 and 0x0816E188 ARE NOT GLOBALS -- baserom.gba
 * holds 0x0300061C and 0x0300061E at those two ROM addresses, four bytes
 * apart, i.e. agbcc's own -fforce-addr address-constant pool, exactly as
 * 0x0816E17C / 0x0816E180 are for the 0x614/0x616 pair. Naming the RAM
 * globals directly is the honest spelling and the build places the pool word.
 * Interior to gUnknown_03000618, so both linker scripts gained
 * `. = 0x00061C` / `. = 0x00061E` (see the wave-34 retraction above). */
extern s32 gUnknown_03000618;
extern s16 gUnknown_0300061C;
extern s16 gUnknown_0300061E;
/* sub_08064500's pair, one latch along and interior to gUnknown_03000604 --
 * declarable for the same reason, with the same two lines added. Same s16
 * evidence: `movs rI,#0; ldrsh rD,[rB,rI]`, Thumb's register-offset-only
 * ldrsh at a zero displacement, i.e. a plain signed halfword scalar. */
extern s16 gUnknown_03000608;
extern s16 gUnknown_0300060A;

/* ---- wave 32 (W32-C): the 0x0806E and 0x08049 blocks ---- */

/* Three OAM blobs handed straight to PutSprite's `u16 *` fourth argument
 * (sub_0806E1B8, sub_0806E830), with NO load in front of the pool word -- the
 * symbol IS the data. Confirmed in baserom.gba: each is `0001 xxxx 0000 xxxx`,
 * a one-entry sprite script. Eight bytes apart and therefore genuinely three
 * symbols, not agbcc's four-byte address-constant pool. */
extern u16 gUnknown_0816E7F0[];
extern u16 gUnknown_0816E7F8[];
extern u16 gUnknown_0816E800[];
extern const struct ProcCmd gUnknown_08582B14[];
/* Wave 46 (W46-G). The argument table sub_0806E510 walks to spawn the six
 * gUnknown_08582B14 procs: it is 0x78 bytes in data/data-08581E70.s and the
 * loop runs `i <= 5` with a 0x14 stride, so exactly SIX rows of 20 bytes and
 * the extent is pinned by the ROM's own extent, not inferred.
 *
 * The member widths come from sub_0806E4BC's promoted definition
 * (src/decomp/c_0806E4BC.c), which is authoritative: +0x00/+0x04/+0x08/+0x0c
 * are forwarded to `int` parameters and are read with `ldr`, and +0x10 is read
 * with `ldrh` into the one `u16` parameter. Bytes +0x12..+0x13 are never
 * touched by anything and are tail padding as far as the ROM shows.
 *
 * `const` because it lives in ROM and nothing writes it. */
struct Unk08582A7CEnt
{
    /* 0x00 */ int unk00;
    /* 0x04 */ int unk04;
    /* 0x08 */ int unk08;
    /* 0x0c */ int unk0c;
    /* 0x10 */ u16 unk10;
    /* 0x12 */ u8 filler_12[0x02];
};
extern const struct Unk08582A7CEnt gUnknown_08582A7C[];
/* A 3-byte-per-row table sub_0806E830 indexes with `proc->unk60 * 3` (built as
 * `i*2 + i`) and reads with three `ldrb`s: +0 and +1 are two x positions and +2
 * the shared y. A flat u8 array and not a struct -- the row offset is added to
 * the SYMBOL and the element offsets to the row, which a struct member would
 * have folded into the ldrb displacement. */
extern u8 gUnknown_08582C1C[];
/* sub_08049D88's palette source, ApplyPaletteExt's `u16 *`. */
extern u16 gUnknown_081268B8[];
/* 0x02028E3D. The slot counter sub_08049BEC post-increments and sub_08049D88
 * resets, both with bare `ldrb`/`strb` at displacement zero. Immediately after
 * gUnknown_02028E3C above and typed the same way. */
extern u8 gUnknown_02028E3D;
/* 0x03002EF0. sub_08017E0C publishes the script node's +0x08 halfword here with
 * a bare `strh` at displacement zero. */
extern u16 gUnknown_03002EF0;

/* ---- wave 32 (W32-C): the 0x08031 block ---- */

/* 0x02025760. The second gUnknown_0202575C-shaped packet: sub_080319AC and
 * sub_08031A24 fill +0/+1/+2 with the same three stores and hand it to
 * sub_0802F588, whose first parameter is already `struct Unk0202575C *`. */
extern struct Unk0202575C gUnknown_02025760;
/* 0x02025764 -- Wave 45, W45-C. The payload record sub_08031824 builds, sitting
 * immediately after the 4-byte command record gUnknown_02025760. Every offset
 * below is read straight off that function, which is the only writer:
 *
 *   +0x00  2 bytes  <- gUnknown_02028030.unk10
 *   +0x02  3 bytes  <- gUnknown_02028030.unk2a
 *   +0x05  0x18     <- gUnknown_02028030.unk12
 *   +0x22  3 x 0x1c per-slot records
 *
 * The three head arrays are VOLATILE and the slot records are NOT, which is
 * unusual enough to state the evidence: each of the first three copy loops
 * emits a dead `ldrb` of the DESTINATION immediately before its `strb` -- the
 * same tell that settled gUnknown_0300449C and gUnknown_020257E4 -- and the
 * four loops that fill the slot records emit no such load. Both halves are
 * plain `dst[i] = src[i];` in the source.
 *
 * The slot stride is 0x1c for 0x19 bytes of content, and the padding is real:
 * the ROM multiplies the slot index by 28 as `((j << 3) - j) << 2`. The slot
 * mirrors struct Unk020280C0 -- [0x00] takes that type's NUL-terminated
 * unk02[0x11] name (via sub_0803CCB8), [0x12]..[0x16]/[0x17]/[0x18] take its
 * filler_14[0]..[4], [5] and [6] -- so the three unnamed members of
 * filler_14 could be named from here, but nothing in this function
 * discriminates their widths and filler_14 is shared, so it is left alone.
 *
 * THE SLOT RUN CANNOT BE A STRUCT ARRAY, and this is a general readout rather
 * than a fact about this record: agbcc uses the old ARM
 * STRUCTURE_SIZE_BOUNDARY of 32, so EVERY struct has size and alignment
 * rounded to 4 no matter how narrow its members are. A 0x19-byte all-u8 slot
 * therefore gets sizeof 0x1c for free -- which is where the ROM's
 * `((j << 3) - j) << 2` stride comes from -- but it also gets ALIGNMENT 4,
 * and 0x22 is not a multiple of 4, so declaring the run as a struct array
 * silently pushes it to 0x24 and shifts every later member offset by two.
 * A 2-D byte array has alignment 1 and lands where the ROM has it. */
struct Unk02025764 /* >= 0x76 */
{
    /* 0x00 */ volatile u8 unk00[2];
    /* 0x02 */ volatile u8 unk02[3];
    /* 0x05 */ volatile u8 unk05[0x18];
    /* 0x1d */ u8 filler_1d[5];
    /* 0x22 */ u8 unk22[3][0x1c];
};
extern struct Unk02025764 gUnknown_02025764;
/* Wave 42 (W42-K). A third packet header of the same 4-byte shape, 0x88 below
 * gUnknown_0202575C: sub_08030D84 fills unk00 = 0xad, unk01 and unk02, then
 * hands `&gUnknown_020256D4` to sub_0802F588, whose first parameter is already
 * `struct Unk0202575C *`. The 13-byte payload at +6 is a SEPARATE symbol rather
 * than a member of it: the ROM builds the two addresses from two independent
 * pool words, in first-reference order (payload first), where one base plus a
 * displacement would have emitted a single word. */
extern struct Unk0202575C gUnknown_020256D4;
extern u8 gUnknown_020256DA[];
/* sub_08031DC0's ROM blob, sub_080149C0's `u8 *` fourth argument. Real data,
 * not a pool word: baserom.gba holds 0x00003143 there, and the symbol is used
 * as a value with no load in front of it.
 *
 * ITS FIVE NEIGHBOURS 0x08090D04 / 0x08090D08 / 0x08090D0C / 0x08090D10 /
 * 0x08090D14 ARE NOT GLOBALS -- they are agbcc `-fforce-addr` words holding
 * &gUnknown_0849B060, &gUnknown_0300449C, &gUnknown_0849B018 (twice) and
 * &gUnknown_03004008, four bytes apart, and every reader of them does the
 * extra `ldr` that gives away the indirection. Wave 42, W42-L verified all five
 * against baserom.gba and ADDED the first two: 0x08090D04 holds 0x0849B060 and
 * 0x08090D08 holds 0x0300449C. sub_08031948 reads the 0x08090D08 word with the
 * double `ldr` and matches with `gUnknown_0300449C[i]` named directly. */
extern u8 gUnknown_08090D18[];
/* 0x08090CF4 -- Wave 45, W45-C. Five halfwords, and REAL DATA rather than
 * another of this neighbourhood's -fforce-addr words: sub_080314A4 hands the
 * symbol straight to sub_0808B6E8 as the source of a 10-byte copy with no
 * load in front of it, which is the same argument that fixed gUnknown_08090D18.
 * The element width comes from the consumer, not the copy -- the destination
 * is a stack buffer that sub_080314A4 then indexes `ldrb; lsls #1; ldrh`, i.e.
 * as u16[5], by gUnknown_0849B018->unk0a[i] when that byte is <= 4. */
extern const u16 gUnknown_08090CF4[];
/* 0x0300449C. Wave 42, W42-L. A four-entry per-army counter, indexed 0..3 by
 * the same army loop that indexes gUnknown_0849B018->unk0a[]: sub_08031948
 * clears all four in that loop and sub_08031FD8 bumps entry i once per
 * non-empty slot it finds for army i.
 *
 * VOLATILE on the dead-`ldrb`-before-`strb` tell, and it carries in BOTH
 * functions independently: sub_08031948's plain `= 0` emits `ldrb r1,[r0]`
 * immediately before the `strb`, and sub_08031FD8's `++` emits a SECOND,
 * entirely dead `ldrb r1,[r3]` between the `adds r0,#1` and the `strb`. That is
 * the same tell that settled struct Unk0849B018's members and
 * struct Unk02025564's unk00/unk02/unk05.
 *
 * u8 from the `ldrb`/`strb` pair; signedness unproved, as neither reader does
 * anything with the value but store it back. The extent 4 is the army-slot
 * range both loops run over and is left off the declaration rather than
 * claimed. */
/*   Wave 42 (W42-K): this is now the SINGLE declaration of the symbol. For a
 * few minutes it was declared twice with conflicting types -- `u8` beside an
 * older extent note earlier in the file and `volatile u8` here -- which made
 * unknown-globals.h uncompilable for the whole wave. W42-L's `volatile` above
 * is the surviving model and the right one: the older entry's "plain ldrb
 * feeding cmp #0" reasoning is byte-neutral and never argued against it. */
extern volatile u8 gUnknown_0300449C[];
/* A three-entry ROM table of sub_0801BD00 blobs, cycled at
 * `(gUnknown_03004008 >> 3) % 3` by sub_08031CF4 and sub_08031D54. */
extern void *gUnknown_0849B074[];
/* The digit strips sub_08031C58 draws: 084C145E and 084C1466 are fixed blobs,
 * 084C170C and 084C178C two tables of them indexed by the tens digit and by
 * `10 - tens`. All four go to sub_0801BD00's `void *` third parameter. */
extern u8 gUnknown_084C145E[];
extern u8 gUnknown_084C1466[];
extern void *gUnknown_084C170C[];
extern void *gUnknown_084C178C[];
/* The halfword tile table sub_080315E8 hands to sub_08014668, indexed by its
 * own second parameter with a plain `lsls #1` off the bare symbol. */
extern u16 gUnknown_0849B0E2[];
/* A 20-byte ROM row indexed by `proc->unk2c`, and the stride is read off the
 * `lsls #2; adds; lsls #2` multiply-by-5-then-4 chain rather than guessed.
 * Three Decompress sources at +0x00/+0x04/+0x08, one per sibling
 * (sub_0806ADD0/sub_0806ADF0/sub_0806AE18), each written to the same
 * `proc->unk30` destination 0x1B00 bytes further along. +0x0C..+0x13 unread. */
struct Unk0858178C
{
    /* 0x00 */ u8 *unk00;
    /* 0x04 */ u8 *unk04;
    /* 0x08 */ u8 *unk08;
    /* 0x0C */ u8 *unk0c; /* wave 32 (W32-C): sub_0806AEC4 Decompresses all
                           * FOUR of these in a row, +0x0c into the buffer
                           * gUnknown_08499578 points at. */
    /* 0x10 */ u16 *unk10; /* the row's palette, ApplyPaletteExt's `u16 *`.
                            * sub_0806AEC4 reads it in both arms of an
                            * `a1 == 7` test and cse constant-folds the taken
                            * arm's index, which is why the ROM shows a bare
                            * `+0x9c` (== 7 * 0x14 + 0x10) with no `lsls`. */
};
extern struct Unk0858178C gUnknown_0858178C[];

/* ---- wave 30 (W30-C): the 0x08066 and 0x08076 blocks ---- */

/* A ROM blob handed straight to sub_0801BD00's `void *` third parameter by
 * sub_08066340, with no load in front of the pool word. Non-const for the same
 * -Werror reason as gUnknown_085815C8. */
extern u8 gUnknown_08580CD4[];
/* A gUnknown_03001470 script blob: sub_08066580 hands it to
 * sub_080152EC(script, 1) and stashes the returned slot in
 * gUnknown_08580934->unk74[]. `const u8 []` -- sub_080152EC's first parameter is
 * `const void *` -- on the same reasoning as gUnknown_08580DD8. */
extern const u8 gUnknown_08580D0C[];
/* A halfword colour table read at `(gUnknown_03004008 & 0x3F) / 4` by
 * sub_080763C0, which writes the result into gPal at byte offset 0x2AE. The
 * exact twin of gUnknown_081A47E4 and gUnknown_081A3D84, which the same idiom
 * reads at `& 0x1F` / 2. Sixteen entries are reachable; nothing bounds it. */
extern u16 gUnknown_081D22A4[];
/* The compressed tile blob sub_08076ADC and sub_08076B7C both unpack into
 * gUnknown_08614280. `u8 []` and non-const: Decompress's first parameter is a
 * plain `u8 *`. */
extern u8 gUnknown_081D0BAC[];
/* Wave 34, W34-G. Two more ROM blobs of the same 0x081Dxxxx run, both reached
 * only by sub_080755F0 and typed straight off the parameter each is handed to
 * with no arithmetic in between: 081D20AC goes to ApplyPaletteExt's `u16 *`
 * first parameter (0x20 halfwords at palette byte offset 0x2C0), and 081D18E8
 * is a compressed blob for Decompress's `u8 *` first parameter, unpacked to
 * VRAM at 0x06011480. Neither is const, because neither parameter is. */
extern u16 gUnknown_081D20AC[];
extern u8 gUnknown_081D18E8[];
/* Wave 34, W34-G. The same pair one screen over, for sub_080763F4: 081D2284 is
 * ApplyPaletteExt's `u16 *` at palette byte offset 0x2A0, and 081D33BC a
 * Decompress `u8 *` blob unpacked to 0x06012600. */
extern u16 gUnknown_081D2284[];
extern u8 gUnknown_081D33BC[];
/* Wave 34, W34-G. A ROM palette table of 0x20-byte (0x10-halfword) rows.
 * sub_08075DBC picks row `unk64 - 6` -- `lsls #5` on the s16 at +0x64 of its
 * proc -- and hands the row to sub_080135F4, whose first parameter is `u16 *`;
 * that is what fixes the element type, and the stride then makes the row count
 * the only free variable. Nothing bounds it. */
extern u16 gUnknown_081D1504[];
/* Wave 34, W34-G. The two alternative blobs sub_080767C0 picks between on
 * sub_0803866C's result and hands to sub_08073304's `const void *` first
 * parameter, everything else about the two calls being identical. `u8 []` and
 * non-const on the same model as gUnknown_085826E0 and gUnknown_085802F0, the
 * blobs the already-promoted callers of sub_08073304 pass there. Nothing
 * dereferences either one, so the element type rests on that parameter alone. */
extern u8 gUnknown_0861452C[];
extern u8 gUnknown_08614538[];
/* Wave 34, W34-G. An 8-byte record array indexed by +0x58 of the sub_080763F4
 * proc -- `lsls #3` for the stride, and the ROM reaches the second word by
 * bumping the base pointer (`adds r4, #4`) rather than by a second symbol, so
 * the two words are ONE record and not two parallel tables. unk00 is a
 * compressed blob handed to Decompress's `u8 *`; unk04 is copied straight into
 * +0x60 of the proc, which c_0807662C.c reads as an int x-offset added to the
 * +0x2c sprite coordinate. NOT const: the same reasoning as gUnknown_0861433C,
 * and Decompress's parameter is not const either. */
struct Unk086144DC
{
    /* 0x00 */ u8 *unk00;
    /* 0x04 */ int unk04;
};
extern struct Unk086144DC gUnknown_086144DC[];
/* An EWRAM-ish scratch buffer reached through one indirection -- every user
 * does `ldr rN, =gUnknown_08614280; ldr rN, [rN]` before touching it, so the
 * symbol is a POINTER and not the buffer. sub_08076ADC/sub_08076B7C Decompress
 * into it and then sub_08011E54 0x1000 bytes of it to 0x0600F000; sub_08075F44
 * indexes it as halfwords at `x + y * 32`, optionally biased by 0x7C0. `void *`
 * because the two element views disagree and neither is proved primary. */
extern void *gUnknown_08614280;
/* A fourth ROM blob of the gUnknown_084B9F00 family -- handed to
 * sub_080785CC's `const void *` fourth parameter by sub_08076BC4 and
 * sub_08076C64 and to nothing else, so nothing dereferences it. */
extern const u8 gUnknown_084BA6D0[];

/* ---- wave 30 (W30-C): the 0x08063 block ---- */

/* Two 16-colour ROM palettes, both handed to ApplyPaletteExt for 0x20 bytes:
 * 0812B81C by sub_08063760 at slot `(n & 0xF) + 0x10`, 08131B4C by
 * sub_080639E8 at a slot the caller supplies. `u16 []` and NOT const --
 * ApplyPaletteExt's first parameter is a plain `u16 *`, so const breaks the
 * call under -Werror. Extent not pinned beyond the 0x20 bytes transferred. */
extern u16 gUnknown_0812B81C[];
extern u16 gUnknown_08131B4C[];

/* HOISTED here in wave 30 (W30-C) from src/decomp/c_08062FB8.c, unchanged in
 * layout. sub_08063430 is the other half of the same link-hardware reset -- it
 * reads +0x18, and on the zero arm writes +0x4a, +0x1e and +0x18, all bytes at
 * the same displacements sub_08062FB8 uses -- and it PASSES the pointer to
 * sub_08062FB8, so the two units need one shared type rather than two
 * coincidentally-identical local ones. */
/* Wave 33 (W33-C) added +0x1c, +0x20 and +0x24 from sub_08063454, which sets
 * the record up for a transfer: +0x20 takes the caller's buffer pointer whole
 * (`str`), +0x24 that pointer plus the 16-byte-rounded length, and +0x1c one
 * PACKED BYTE built as `(mode << 1) | 0x81` -- see src/decomp/c_08063454.c.
 * The two words force the struct's alignment to 4, which changes no member
 * offset (0x20 and 0x24 were already aligned) and nothing allocates one.
 *
 * NOT the same record as struct Unk8063BE0 below, despite both carrying +0x24,
 * +0x28 and something at +0x1c: this one is written at +0x1c with `strb` and
 * that one is read with `ldrh`, and Unk8063BE0's consumer sub_08063CCC is
 * SetObjAffine sprite maths rather than link hardware. Address proximity in the
 * 0x08063 block is not kinship. */
/* Wave 46 (W46-B) carved +0x00 and +0x04 out of the old filler_00[0x16] from
 * sub_08063528, the SIO multiplayer poll. Both are whole-word `ldr`/`str`;
 * neither offset moves and the struct was already 4-aligned.
 *   +0x00 is the shift register: it is stored `<< 5`, read back `>> 5` with a
 * LOGICAL `lsrs`, tested against 0 as a full word, and its LOW HALF is read
 * separately with a bare `ldrh` as sub_080633E4's second argument. A word that
 * is both shifted unsigned and truncated to u16 at a use is `int` here -- the
 * ldrh is a (u16) cast at the call, not a narrower member, because the same
 * offset takes 0x100000 whole.
 *   +0x04 is the last value broadcast, compared for equality against each
 * SIOMULTI halfword. Equality only, so the compare does not discriminate the
 * sign; `int` is the weakest fit that also takes the `<< 5`-free whole-word
 * stores. */
struct Unk08062FB8
{
    /* 0x00 */ int unk00;
    /* 0x04 */ int unk04;
    /* 0x08 */ u8 filler_08[0x0e];
    /* 0x16 */ u16 unk16;
    /* 0x18 */ u8 unk18;
    /* 0x19 */ u8 filler_19[0x03];
    /* 0x1c */ u8 unk1c;
    /* 0x1d */ u8 unk1d;
    /* 0x1e */ u8 unk1e;
    /* 0x1f */ u8 filler_1f[0x01];
    /* 0x20 */ int unk20;
    /* 0x24 */ int unk24;
    /* 0x28 */ u8 *unk28; /* Wave 34, W34-C: sub_08033194 seeds it with the base
                           * of the blob gUnknown_030032DC also points at, a
                           * whole-word `str` at a displacement `strb`/`strh`
                           * cannot reach. Carved out of the old
                           * filler_28[0x20]. */
    /* 0x2c */ u8 filler_2c[0x1c];
    /* 0x48 */ u8 unk48;
    /* 0x49 */ u8 filler_49[0x01];
    /* 0x4a */ u8 unk4a;
    /* 0x4b */ u8 unk4b; /* Wave 34, W34-C: sub_08033194 clears it immediately
                          * before handing the record to sub_08062FB8, reached
                          * by `adds rB,#0x4b` on the record base. */
};

/* HOISTED here in wave 30 (W30-C) from src/decomp/c_08063BE0.c, unchanged in
 * layout (STRUCT_PAD spelled out, since this header does not use that macro).
 * sub_08063BBC seeds `unk44 = 8` and then tail-calls sub_08063BE0 on the same
 * pointer, which is the counter sub_08063BE0 decrements -- so the same type,
 * and the s16 width is sub_08063BE0's, not a fresh guess. */
/* Wave 33 (W33-C) added +0x2c, +0x42 and +0x48 from sub_08063B50, the frame
 * tick that feeds sub_08063CCC. +0x2c is `ldr`/`str` whole and gets a signed
 * `* 3 / 4` (the `bge`/`adds #3`/`asrs #2` round-toward-zero sequence), so it
 * is a signed word. +0x42 is a second countdown read exactly the way +0x44 is
 * -- `ldrh` for the decrement, `movs rN,#0; ldrsh` for the test -- which is
 * s16 by the same argument that typed +0x44. +0x48 is `ldrb`. */
struct Unk8063BE0
{
    /* 0x00 */ u8 filler_00[0x1c];
    /* 0x1c */ u16 unk1c;
    /* 0x1e */ u8 filler_1e[0x06];
    /* 0x24 */ int unk24;
    /* 0x28 */ int unk28;
    /* 0x2c */ int unk2c;
    /* 0x30 */ u8 filler_30[0x12];
    /* 0x42 */ s16 unk42;
    /* 0x44 */ s16 unk44;
    /* 0x46 */ u8 filler_46[0x02];
    /* 0x48 */ u8 unk48;
};

/* ---- wave 46 (W46-B): the 0x08064 block ---- */

/* 0x0202F140. EIGHT 0x18-byte entries, each holding TWO three-word vectors.
 * sub_0806412C seeds the first vector of every entry from gUnknown_0858089C
 * with `stm r3!, {r0}` three times at stride 0x18; sub_08064214 then walks the
 * same stride handing sub_08063DDC `&e->unk00` and `&e->unk0c` as two separate
 * arguments (`adds r4, #0xc` off the base, kept in its own register alongside
 * the unbiased one). That pair of arguments is what splits the entry into two
 * vectors rather than one six-word block.
 *
 * `int` throughout: every access is a whole-word `ldr`/`str`/`stm`, and the
 * seeded values are `gUnknown_0858089C[k] << 0xc`, i.e. 20.12 fixed point.
 *
 * The consumer names the same storage `struct Vec3 { s32 x, y, z; }` -- see
 * src/decomp/c_08063DDC.c, which defines that tag file-locally. It is NOT
 * defined here on purpose: a definition in this header would collide with that
 * file's own, so callers cast to `struct Vec3 *` at the call instead. Two
 * three-word vectors and one 0x18 stride is the same fact either way. */
struct Unk0202F140Entry
{
    /* 0x00 */ int unk00[3];
    /* 0x0c */ int unk0c[3];
};
extern struct Unk0202F140Entry gUnknown_0202F140[8];

/* 0x0202F110. SIX 8-byte entries. sub_0806412C writes `unk00` of entries 0..5
 * as six `strh` at displacements 0, 8, 0x10, 0x18, 0x20 and 0x28 off ONE pool
 * word, and fills `unk02` from gUnknown_085808CC four `strb` at a time off a
 * base pre-biased by 2 (`adds r4, r0, #2`, then `+ i * 8`). The trailing two
 * bytes are never touched by that function and are filler, not measured. */
struct Unk0202F110Entry
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u8 unk02[4];
    /* 0x06 */ u8 filler_06[0x02];
};
extern struct Unk0202F110Entry gUnknown_0202F110[6];

/* Wave 50 (W50-H): the two ROM operands of sub_08064288's two sub_0801BD00
 * calls, both undeclared until now.
 *
 * gUnknown_0858092C is the address ITSELF, not a pool word: sub_08064288 loads
 * it and hands it straight to sub_0801BD00's `void *` third parameter with no
 * dereference, where a -fforce-addr word would have shown the `ldr rN,=s;
 * ldr rM,[rN]` pair. Non-const because sub_0801BD00's parameter is `void *`.
 *
 * gUnknown_08580914 is a real table by wave 49's inverse test -- it is INDEXED
 * by a computed subscript before being dereferenced (`lsls r0, rI, #2; adds
 * r0, r0, rBase; ldr r2, [r0]`). Word elements feeding the same `void *`
 * parameter, so an array of pointers. The extent is unmeasured: sub_08064288's
 * first loop makes exactly one trip, so only element 0 is ever proved. */
extern u8 gUnknown_0858092C[];
extern void *const gUnknown_08580914[];

/* Two ROM tables read by sub_0806412C only, both walked linearly by a pointer
 * that is never reset between rows -- so the element counts are the totals it
 * consumes, 8 rows x 3 and 6 rows x 4, and not a stride.
 * gUnknown_0858089C is read with `ldrsh` (SIGNED halfwords, the sign is the
 * instruction's, not a guess) and gUnknown_085808CC with `ldrb`. */
extern const s16 gUnknown_0858089C[24];
extern const u8 gUnknown_085808CC[24];

/* 0x030005F4 / 0x030005F8. Written together at the end of sub_0806412C, one
 * whole-word `str` each, from `(u16)param * 0x1000` -- 20.12 again, the same
 * format the table above is scaled into. Nothing in this wave READS them, so
 * the width is the store's and the signedness is unproven. */
extern int gUnknown_030005F4;
extern int gUnknown_030005F8;

/* ---- Wave 30, W30-B: the 0x08005 / 0x08014 / 0x0801C / 0x08027 / 0x08028
 * address-locality blocks ---- */

/* 0x0200BC14. TWO PARALLEL u16 ARRAYS 0x408 apart, and it has to be one object:
 * sub_080148A0 materialises ONE pool word (=gUnknown_0200BC14), builds 0x408 as
 * `movs r6,#0x81; lsls r6,#3` and adds it, then adds the SAME `i * 2` byte
 * offset to both bases. Naming a second symbol would have emitted a second pool
 * word and folded the displacement into the relocation instead.
 *
 * NOTE THE OVERLAP, because it looks like a bug and is not: aw2bhr.lds binds
 * gUnknown_0200BFFC at 0x0200BFFC, i.e. 0x3e8 bytes in, so the +0x408 member
 * lands 0x20 bytes inside that symbol's range. gen_lds.py only emits
 * `. = addr; sym = .;` -- it reserves no space and two symbols may cover the
 * same bytes -- and the linker script's boundaries are "next symbol some
 * assembly happens to name", not object extents. The codegen above is the
 * evidence about the SOURCE; the linker script is not evidence at all here.
 * The element counts below are therefore a floor from the two known readers
 * (index 0, and an index that arrives as a u8), not a measurement. */
/* Wave 32 (W32-C): a THIRD array sits between them. sub_08014910 and
 * sub_0801496C write `*(u32 *)(g + 0x400 + i * 4)` off the SAME pool word,
 * between the +0x000 and +0x408 stores, so unk000 stops at 0x200 elements
 * (0x000..0x3ff) rather than the 0x204 that was only ever a filler to 0x408.
 * unk408 keeps its offset, which is the part any promoted code depends on.
 * unk400's element count is a floor of two, not a measurement -- the two
 * writers reach it with the same index they use on the u16 arrays, so either
 * that index is tiny here or the three arrays are not really parallel. */
struct Unk0200BC14
{
    /* 0x000 */ u16 unk000[0x200];
    /* 0x400 */ u32 unk400[2];
    /* 0x408 */ u16 unk408[0x100];
};
extern struct Unk0200BC14 gUnknown_0200BC14;
/* 0x0300004C. The allocator's "block being worked on" cursor. sub_08014EF4
 * stores a heap block header pointer through it three times (the block, then
 * its successor, then the block again) and once more with the split block in
 * the split arm. It is reached ONLY through agbcc's -fforce-addr address
 * constant pool, which is why the splitter never invented a name for it and
 * the map shows 0x03000048's `*fill*` covering it: the ROM word at 0x0808E52C
 * holds 0x0300004C, and 0x0808E52C's neighbours at +/-4 hold 0x030030E0 and
 * 0x03000050, i.e. it sits in a run of address constants, not a run of data.
 * `void *` because the pointed-to header type (struct MemBlock) is file-local
 * to src/decomp/c_08014E68.c; every store converts implicitly and costs
 * nothing. The three stores are plain, not volatile -- the reload of the
 * address between them is ordinary aliasing (the store is through an int-ish
 * pointer that may alias the .rodata address constant), not a volatile tell.
 * NO EXTERN CAN EXIST FOR THIS ADDRESS: upstream's aw2bhr.lds IWRAM table
 * jumps 0x48 -> 0x50 and gen_lds.py passes it through verbatim, so the symbol
 * is unlinkable in the split build (proto_check.py catches it). Spell accesses
 * as an offset into the covering symbol instead --
 * `*(void **)((u8 *)&gUnknown_03000048 + 4)` -- which relocates as
 * gUnknown_03000048+4 and resolves to the same address and pool word. See
 * work/sub_08014EF4/sub_08014EF4.c. Wave 42. */
/* 0x03000050. The arena handle sub_08014D7C returns -- sub_08014DA8 stores that
 * function's `int` result here with a plain `str`, and sub_08014E44 /
 * sub_08014ED4 both gate on `!= -1` before forwarding it to sub_08014DCC /
 * sub_08014E68. `int` and not a pointer because -1 is the sentinel and
 * sub_08014D7C is itself declared `int` (src/decomp/c_08014D7C.c). */
extern int gUnknown_03000050;
/* 0x030030F8. sub_080288D8 reads it with a bare `ldrb` and compares it against
 * a u16 team/army id. Width only; nothing in this batch writes it. */
extern u8 gUnknown_030030F8;
/* 0x08090A98. A pair of s16 constants: sub_080276D0 reads [+0] and sub_080276F0
 * reads [+2], both `movs rI,#K; ldrsh r0,[r0,rI]` -- Thumb has no `ldrsh` at an
 * immediate offset, so the register form is what ANY s16 read looks like and is
 * not itself an array tell; the two functions using consecutive elements is.
 * The value goes into the word gUnknown_03003130.unk04. */
extern const s16 gUnknown_08090A98[];
/* 0x08090A84. Wave 41 (W41-C). A REAL byte table, and the counter-example that
 * keeps the -fforce-addr rule honest: its three immediate neighbours
 * (0x08090A70 -> 0x030033EC, 0x08090A80 and 0x08090A8C -> 0x08499598) ARE
 * address words, so the 0x0809xxxx prefix decides nothing and each symbol has
 * to be checked against baserom.gba on its own. The discriminator is the ROM
 * CONTENT: the word here reads 0x04020100, which is no valid address, and the
 * access shape agrees -- sub_08026B28 indexes straight off the symbol
 * (`ldr r1,=sym; mov ip,r1; adds r1,r4,r0; ldrb r1,[r1]`) with no load through
 * it, where an address word always shows the extra `ldr`.
 *   Read as bytes it is {0, 1, 2, 4, 8} at indices 0..4: one BIT per army slot,
 * with index 0 unused, which is exactly the shape of struct ArmyRecord's unk2c
 * ("a FOUR-BIT MASK, one bit per 64-unit army slot"). sub_08026B28 ORs
 * gUnknown_08090A84[j] into army i's unk2c for every other live army j on a
 * different team, so this is the slot-number-to-mask table for that member.
 *   NOT declared `const`, by consistency with gUnknown_084995F4 and
 * gUnknown_084995FE rather than by measurement: sub_08026B28's read is indexed
 * by the inner counter and cannot be hoisted either way, so the LICM oracle
 * those two were settled with has nothing to say here. */
extern u8 gUnknown_08090A84[];
/* Wave 31 (W31-C): gUnknown_08090BC0 and gUnknown_08090BC4 are NOT globals --
 * they are two more of the agbcc `-fforce-addr` address words this 0x0809xxxx
 * run is made of, and the ROM contents settle it: the word at 0x08090BC0 is
 * 0x030033E4 and the word at 0x08090BC4 is 0x03003130. sub_0802BB98's source
 * therefore says `gUnknown_030033E4` and `gUnknown_03003130` directly, and
 * agbcc's own copy supplies the middle `ldr` -- the same mechanism the note on
 * gUnknown_0808D81C records for gUnknown_08499590.
 *
 * Declaring either as a pointer variable and writing `G->field` produces a
 * FOURTH level and is 2 bytes long per use (measured this wave, sub_0802BB98).
 * Note that gUnknown_08090A98 immediately below IS real data -- its first word
 * is 0x00AD0003, not an address -- so proximity is not the test; the content
 * is. */
/* The sprite blob sub_0802BB74 hands to sub_0801BD00's `void *` third
 * parameter, with attr1 masked to its nine coordinate bits and 0x400 set in
 * attr0. `u16 []` is the same model gUnknown_08485CC8's entries carry. */
extern u16 gUnknown_0849A3B8[];
/* An adjacent pair of plain bytes -- both `ldrb` off their own pool word --
 * that sub_08047078 forwards to sub_08046A84 as its two arguments. The odd
 * address of the first rules out an aggregate. */
extern u8 gUnknown_02028DD5;
extern u8 gUnknown_02028DD6;
/* Wave 35, W35-L. The other two bytes of the same run, both written by
 * sub_080470F8 and both plain `strb` through their own pool word.
 * gUnknown_02028DD4 is a 0/1 flag (`movs r0,#1; strb` then `movs r0,#0; strb`
 * on the fail arm of a range test); gUnknown_02028DD7 takes sub_08024984's
 * `int` result narrowed to a byte by the `strb`. Neither is read anywhere
 * matched, so only the width is proved. */
extern u8 gUnknown_02028DD4;
extern u8 gUnknown_02028DD7;
/* Proc/list script blobs, `const u8 []` for the reason gUnknown_0849A108 is:
 * sub_0801537C takes `const void *` and nothing dereferences any of them.
 * gUnknown_0848A398 / gUnknown_0848A3C4 are sub_080145BC's two stops;
 * gUnknown_08489530 is the first of sub_08014878's three (the other two,
 * gUnknown_08489548 / gUnknown_08489568, are already declared above as the
 * blobs sub_08014668 / sub_080146D4 start -- so this block is one start/stop
 * pairing and the const is proved from both ends). */
extern const u8 gUnknown_08489530[];
extern const u8 gUnknown_0848A398[];
extern const u8 gUnknown_0848A3C4[];
/* Blobs reached only as `sub_080152C0` / `sub_080152EC` / `sub_080193B0`
 * arguments in the 0x08005xxx menu block. sub_080152EC and sub_080193B0 take
 * `const void *` / `const u8 *`; sub_080152C0 takes `s32` and its callers cast,
 * as src/decomp/c_0802A508.c already does. */
extern const u8 gUnknown_08487E8C[];
extern const u8 gUnknown_08488594[];
extern const u8 gUnknown_08488614[];
extern const u8 gUnknown_0848863C[];
extern const u8 gUnknown_0848867C[];
extern const u8 gUnknown_084886BC[];
extern const u8 gUnknown_084886CC[];
/* Wave 37, W37-A: seven more of the same 0x08005xxx menu-block blobs, reached
 * only as arguments. gUnknown_08488164 / _224 / _2E4 / _394 are sub_080193B0's
 * four script stops in sub_0800520C, sub_080052D8 and sub_080053A8 (one triple
 * of near-identical functions, all four blobs shared between them);
 * gUnknown_08488494 and gUnknown_08488514 are sub_08019F2C's first argument in
 * sub_080057EC / sub_08005964; gUnknown_08488664 is sub_0801BD00's third
 * argument in BOTH sub_08005D74 and sub_08005E30, and that parameter is
 * declared `void *`, so those two call sites cast the const away. Nothing
 * dereferences any of them, so `const u8 []` is the weakest fit exactly as for
 * the block above. */
extern const u8 gUnknown_08488164[];
extern const u8 gUnknown_08488224[];
extern const u8 gUnknown_084882E4[];
extern const u8 gUnknown_08488394[];
extern const u8 gUnknown_08488494[];
extern const u8 gUnknown_08488514[];
extern const u8 gUnknown_08488664[];
/* An OBJ list of the gUnknown_08499E10 shape -- sub_08027C8C hands it to
 * PutSprite, whose fourth parameter is `u16 *`, so NOT const. */
extern u16 gUnknown_08499E08[];
/* sub_08027560's two ROM blobs: the first goes to `Decompress(u8 *, void *)`
 * and the second to `ApplyPaletteExt(u16 *, u32, u16)`. Both prototypes take
 * non-const pointers, which is what fixes the (missing) const here. */
extern u8 gUnknown_081121D0[];
extern u16 gUnknown_081126E4[];
/* Wave 30, W30-B extension work, 0x08049 block. A POINTER VARIABLE in ROM, the
 * same shape as gUnknown_08499598: every reader is `ldr rN, =sym; ldr rN, [rN]`
 * and then a displacement off the loaded word. The extent below is a FLOOR from
 * the three readers in that block (+0x836 and +0x837 in sub_08049B80, +0x83a in
 * sub_08049B28, +0x1e / +0x20 in sub_0804931C), not a measurement -- nothing
 * bounds the object. Only the two bytes wave 30 actually matched are named. */
/* Wave 43, W43-L. ONE ROM table of 8-byte entries beginning at 0x084C24A0,
 * declared TWICE because sub_08048FD8 reaches it through two pool words and
 * data-0848B688.s therefore carves two symbols out of it -- gUnknown_084C24A0
 * is 4 bytes and gUnknown_084C24A4 is the 0xB44 remainder. Each view is
 * offset-0 into its own symbol, so each relocates against exactly the symbol
 * the ROM names; a single struct array at ...A0 would relocate the second use
 * as `gUnknown_084C24A0 + 4` instead.
 *
 * The entry is { const u8 *script; u8 (*predicate)(void); }: sub_08048FD8
 * calls the second word through `_call_via_r0` with no argument and compares
 * the result against 1, then hands the FIRST word of the chosen entry to
 * sub_080485DC, whose parameter is `const u8 *` (src/decomp/c_080485DC.c).
 * The predicate's return width is NOT proved -- the `lsls #0x18; lsrs #0x18`
 * before `cmp #1` is a value comparison, so per the wave-43 W43-F rule it
 * pins only the narrower of (return type, receiving local). u8 is the
 * weakest spelling that fits. The entry COUNT is unbounded; the loop walks
 * indices 3..0x12. */
struct Unk084C24A0
{
    /* 0x00 */ const u8 *unk00;
    /* 0x04 */ u8 (*unk04)(void);
};
extern const struct Unk084C24A0 gUnknown_084C24A0[];

struct Unk084C24A4
{
    /* 0x00 */ u8 (*unk00)(void);
    /* 0x04 */ const u8 *unk04;
};
extern const struct Unk084C24A4 gUnknown_084C24A4[];

struct Unk084C30F8
{
    /* 0x000 */ u8 filler_000[0x1e];
    /* 0x01e */ u16 unk01e; /* Wave 30, W30-D. sub_0804931C reads both `ldrh`
                             * and uses unk01e twice: once as `unk01e - unk020`
                             * (the difference feeds sub_080487B4's u8 second
                             * parameter as `(x * 2 + 7)`) and once as the index
                             * into the u8 array gUnknown_02028E1C. u16 from the
                             * `ldrh`; nothing constrains the sign because the
                             * only arithmetic is the subtraction, which
                             * promotes either way. */
    /* 0x020 */ u16 unk020;
    /* 0x022 */ u8 filler_022[0x06];
    /* 0x028 */ u16 unk028; /* Wave 36, W36-J. sub_080499F8 reads it `ldrh
                             * [rN, #0x28]` and hands it straight to
                             * sub_08049944's first argument. u16 from the load;
                             * nothing constrains the sign. */
    /* 0x02a */ u8 filler_02a[0x06];
    /* 0x030 */ u8 unk030; /* Wave 35, W35-L. A small state/flag byte:
                            * sub_08048F4C gates its whole body on it being 0
                            * (`ldrb`; `cmp #0`; `bne`) and then writes the
                            * literal 2 or 3 into it with `strb`. Reached
                            * through `adds rN, rM, #0x30` -- past `ldrb`'s
                            * imm5 -- and byte-wide from the loads. */
    /* 0x031 */ u8 filler_031[0x01];
    /* 0x032 */ u16 unk032[0x400]; /* Wave 43, W43-L. A 32x32 halfword tilemap
                             * scratch buffer. sub_08048850 hands `&unk032[0]`
                             * to sub_08012BC8(u16 *, ...) as its `u16 *` first
                             * parameter and to sub_080487B4's `u16 *` third;
                             * sub_08049178 and sub_08049264 both read it
                             * `ldrh` at `row * 32 + col` with col bounded by
                             * `bls #0x13`, so the row stride is 32 halfwords.
                             * The EXTENT is inferred, not measured: 0x400
                             * halfwords is exactly the gap to unk832 and is
                             * the standard GBA 32x32 BG map, but nothing in
                             * the four readers bounds the row count. Reached
                             * through `adds rN, #0x32`, which is why the
                             * address decays before the index scale. */
    /* 0x832 */ s16 unk832; /* Wave 31, W31-A. SIGNED, and measured: sub_08048F10
                             * tests it `< 0` with `movs rI,#0; ldrsh rD,[rB,rI]`
                             * -- a plain u16 member cannot produce the ldrsh.
                             * Reached through a pool word (`adds rB, 0x832`),
                             * past ldrh's imm5*2 limit. sub_08048F10 is a
                             * saturating step: `if (x < 0) x += 8; else x = 0;`
                             * and it returns the new value, so 8 is the step of
                             * a countdown that rests at 0. */
    /* 0x834 */ u8 filler_834[0x01];
    /* 0x835 */ u8 unk835; /* Wave 36, W36-J. A one-shot "repaint me" flag:
                            * sub_080499F8 passes it to sub_08049944 and then,
                            * if it is non-zero, clears it and calls
                            * sub_08013AEC. ldrb/strb through
                            * `adds rN, rM, #0x835`, past ldrb's imm5. */
    /* 0x836 */ u8 unk836; /* compared for equality against unk837 by
                            * sub_08049B80, which picks between sub_0803BD60
                            * and sub_0803BD54 on the result. Both are plain
                            * `ldrb` through `adds rN, rM, #0x836` / `#0x837`,
                            * past `ldrb`'s imm5, so the pair is adjacent and
                            * byte-wide and nothing else about them is proved. */
    /* 0x837 */ u8 unk837;
    /* 0x838 */ u8 unk838; /* Wave 43, W43-L. sub_08049178 does `unk838++`
                            * (ldrb; adds #1; strb) on the frame it kicks the
                            * list redraw off, right beside sub_080490BC's
                            * `unk839++`. Only the width is proved. */
    /* 0x839 */ u8 unk839; /* Wave 35, W35-L. sub_080490BC does `unk839++`
                            * (ldrb; adds #1; strb) once per accepted frame and
                            * nothing matched reads it back, so only the width
                            * is proved. */
    /* 0x83a */ u8 unk83a; /* Wave 30, W30-D. sub_08049B28 reads it `ldrb`
                            * through `adds rN, rM, #0x83a` (a pool word, past
                            * ldrb's imm5) and uses it as the index into the
                            * four-entry pointer table gUnknown_084C30E8. u8
                            * from the load; the table has four ROM entries, so
                            * the value is small. */
    /* 0x83b */ u8 filler_83b[0x01];
    /* 0x83c */ u8 unk83c; /* Wave 35, W35-L. sub_080490BC increments it when
                            * gpKeySt->last carries 0x2 (right after calling
                            * sub_080485F8) and tests it `!= 0` in the same
                            * function, paired with unk836, to decide whether to
                            * Proc_Break. ldrb/strb through
                            * `adds rN, rM, #0x83c`. */
    /* 0x83d */ u8 unk83d[0x13]; /* Wave 43, W43-L. A compaction buffer:
                            * sub_08048FD8 walks the 16 predicates at
                            * gUnknown_084C24A4 and `strb`s each PASSING index
                            * into unk83d[n++], then picks one at random
                            * (`__umodsi3(gUnknown_03004008, n)`) and stores
                            * `entry + 3` into unk839. So it holds indices, and
                            * n is bounded by 16 -- the 0x13 extent is only the
                            * gap to unk850 and is not measured. */
    /* 0x850 */ const u8 *unk850; /* Wave 31, W31-A. sub_080485DC stores its own
                                   * parameter here (`movs r2,#0x85; lsls #4`
                                   * for the 0x850 displacement) and then hands
                                   * the SAME pointer to sub_080193B0, whose
                                   * parameter is `const u8 *` -- so the member
                                   * takes the callee's type. Word-wide from the
                                   * `str`. */
};
extern struct Unk084C30F8 *gUnknown_084C30F8;
/* Wave 30, W30-D. A four-entry table of script-blob pointers (the ROM words at
 * 0x084C30E8 are 084C3068 / 084C3088 / 084C30A8 / 084C30C8, evenly spaced by
 * 0x20; the fifth word is a RAM address and belongs to something else).
 * sub_08049B28 indexes it with `gUnknown_084C30F8->unk83a` and hands the
 * element straight to sub_0801930C, whose parameter is `const u8 *`, so the
 * element type is `const u8 *` and the table itself is const. */
extern const u8 *const gUnknown_084C30E8[];
/* A list script blob stopped FOUR TIMES IN A ROW by sub_08049EB4, after one
 * stop of gUnknown_084C325C. `const u8 []` for the reason gUnknown_0849A108 is:
 * sub_0801537C takes `const void *` and nothing dereferences it. The repeat is
 * in the ROM, not a transcription slip -- the address is held in r4 across all
 * four calls, which is what pays for the `push {r4, lr}`. */
extern const u8 gUnknown_084C3244[];

/* Wave 31, W31-A. The proc script sub_08001038 starts with
 * `sub_080152EC(gUnknown_084858DC, 0)` before writing that entry's unk1e --
 * the same shape as the gUnknown_08485D8C call recorded on Unk03001470.unk42.
 * `const struct ProcCmd []` follows sub_080152EC's first parameter and the
 * house convention for every other blob it is handed. */
extern const struct ProcCmd gUnknown_084858DC[];

/* Wave 31, W31-A. sub_08010EC4's memcpy DESTINATION: `sub_0808B6E8(this,
 * gUnknown_03003050, 0x3d)`, and sub_0808B6E8's first parameter is the
 * destination (sub_0802FA9C copies a ROM table into a stack buffer through it
 * and then reads the buffer). 0x3d bytes, no interior structure visible from
 * here, hence `u8 []`. NOT const: it is written, even though the address lies
 * in the cartridge window, so the store is inert on hardware -- flagged rather
 * than explained, since nothing in the tree reads it back. */
extern u8 gUnknown_08489200[];

/* Wave 31, W31-A. 0x03007FFC is the BIOS interrupt-vector word; src/crt0.s
 * writes it too. sub_08010EC4 is a one-line setter that `str`s its parameter
 * here and nothing narrows or dereferences it, so `void *` is the weakest type
 * that fits. The real content is a handler address -- flagged, not proved:
 * nothing in the C tree calls through it. */
extern void *gUnknown_03007FFC;

/* Wave 31, W31-A. An 8-byte ROM table sub_0802FA9C copies WHOLE onto an
 * 8-byte stack buffer with sub_0808B6E8 and then indexes as `ldrsh` at
 * `index * 2` -- so four SIGNED halfwords. The copy-then-index is the ROM's
 * own idiom, not an artefact: the buffer is what the `sub sp, #8` pays for. */
extern const s16 gUnknown_08090CA0[];

/* Wave 31, W31-A. 0x03007FF8 is the BIOS IntrCheck halfword. sub_0802E920
 * stores 1 into it with a bare `strh` before draining the DMA queue. u16 from
 * the store width; VOLATILE IS UNPROVEN -- a single unconditional store is
 * byte-identical either way, and no reader is known in the C tree. */
extern u16 gUnknown_03007FF8;

/* Wave 31, W31-A. A matched pair of 0x80-byte blobs that sub_08022878 pushes to
 * the same VRAM address 0x06003600, picking between them on bit 0 of
 * gUnknown_03004008 -- a two-frame animation. Non-const because sub_080228B8
 * hands 0x0809181C to sub_08011E54, whose first parameter is a plain `void *`;
 * 0x08091B9C gets the same type so the pair stays one kind of object. */
extern u8 gUnknown_0809181C[];
/* Wave 32 (W32-B): two 15-entry u16 palette-cycle tables sub_080228D8 picks
 * between, indexed by `(gUnknown_03004008 >> 2) % 15` (a LOGICAL shift and
 * __umodsi3, so the frame counter is read unsigned there) and handed to
 * sub_08013664, whose first parameter is `u16 *`. */
extern u16 gUnknown_08091C5E[];
extern u16 gUnknown_08091C9E[];
extern u8 gUnknown_08091B9C[];

/* Wave 31, W31-A. Two LZ blobs sub_08022A34 unpacks to 0x06016CA0 and
 * 0x06016A40. `u8 []` and non-const because Decompress takes `u8 *`. */
extern u8 gUnknown_081019C4[];
extern u8 gUnknown_08124268[];

/* Wave 31, W31-A. A 0x20-byte palette sub_08022A34 hands to ApplyPaletteExt at
 * offset 0x220; `u16 []` and non-const for the same reason gUnknown_08582754
 * is -- ApplyPaletteExt takes a plain `u16 *`. */
extern u16 gUnknown_08101904[];

/* Wave 31, W31-A. A 16-entry halfword colour table, and then a SECOND one
 * 0x20 bytes above it: sub_08022A6C reads
 * gUnknown_08101984[(gUnknown_03004008 >> 2) & 0xF] into gPal halfword 0x228
 * and gUnknown_08101984[0x10 + same] into 0x238. The same 16-phase frame
 * counter idiom as gUnknown_081A47E4, one octave slower. At least 32 entries.
 * `u16 []` non-const because sub_0801368C takes a plain `u16 *`. */
extern u16 gUnknown_08101984[];

/* Wave 31, W31-A. A list script blob sub_08022A08 hands to sub_0801537C, which
 * takes `const void *` and never dereferences it -- the same reading as
 * gUnknown_0849A108 and gUnknown_084C3244. */
extern const u8 gUnknown_08499B4C[];

/* Wave 31, W31-A. A proc script blob sub_08007B54 starts with
 * `sub_080152C0((s32)this, 0)`, keeping the s8 slot id it returns in
 * gUnknown_0200B0B0->unk6b. `const u8 []` plus the cast at the call site is the
 * spelling src/decomp/c_08005838.c and c_080059B4.c already use for
 * gUnknown_08488614 and gUnknown_0848863C, the neighbouring blobs. */
extern const u8 gUnknown_08488890[];

/* Wave 31, W31-A. A GLOBAL FUNCTION POINTER, called through
 * `bl _call_via_r4` -- the libgcc trampoline agbcc uses for every indirect
 * THUMB call, and r4 because r0-r3 are all taken by arguments. Five parameters:
 * sub_0802026C forwards its own last five, and sub_0802032C calls it as
 * `(unit->unk02, unit->unk03, unit->unk00, 0x78, 0)`, which is a cell column, a
 * cell row and a unit-type id. The widths are NOT settled -- every argument is
 * either an untouched register or a `ldrb` that a wider parameter accepts for
 * free -- so `int` is the weakest that fits. Void because both callers discard
 * the result. */
extern void (*gUnknown_030013EC)(int, int, int, int, int);

/* Wave 31, W31-A. Proc scripts. gUnknown_08615E08 is started as a child of
 * sub_0807A36C's own proc when A is held; gUnknown_08616A08 likewise in
 * sub_08081334, guarded by a Proc_Find on gUnknown_08616A40 so the two are a
 * "start unless that other tree is already up" pair. */
extern const struct ProcCmd gUnknown_08615E08[];
extern const struct ProcCmd gUnknown_08616A08[];
extern const struct ProcCmd gUnknown_08616A40[];

/* Wave 31, W31-A. A proc script blob sub_08029948 starts with
 * `sub_080152C0((s32)this, 0)`, then writes the caller's argument into
 * gUnknown_03001470[slot].unk22 of the entry it hands back. `const u8 []` plus
 * the cast at the call site, the same spelling gUnknown_08488614 uses. */
extern const u8 gUnknown_0849A080[];

/* Wave 31, W31-A. The link-session descriptor sub_08030C98 fills in and hands
 * to sub_0802EA5C. Every field below is read off sub_0802EA5C's own body, which
 * takes this object in r0: `ldr [r2]` at +0 into gUnknown_03000564, `ldrh
 * [r2,#6]` tested against 0 and defaulted to 10, `ldrh [r2,#8]` masked with 3
 * into gUnknown_03000560. sub_08030C98 adds +6 and +0xa, both `strh` of 0x66 --
 * so +6 is a timeout in frames and +0xa is its twin. */
struct Unk030040C0 /* >= 0x0c */
{
    /* 0x00 */ u32 unk00;
    /* 0x04 */ u8 filler_04[0x02];
    /* 0x06 */ u16 unk06;
    /* 0x08 */ u16 unk08;
    /* 0x0a */ u16 unk0a;
};
extern struct Unk030040C0 gUnknown_030040C0;
/* Wave 33, W33-B: the 12-byte ROM initialiser sub_0802F348 copies wholesale
 * into gUnknown_030040C0 (`ldm!/stm!` of three words = one struct assignment).
 * Same type, const because it is ROM. */
extern const struct Unk030040C0 gUnknown_08090C38;

/* Wave 31, W31-A. Two adjacent bytes read as a pair by sub_080468D4: [0] is
 * multiplied by 8 and added to the caller's pixel offset before a signed
 * divide by 8, and [1] goes straight into sub_08012BC8's third u16 parameter.
 * A scroll origin in tiles and a palette or window id. Plain `ldrb`, so u8;
 * nothing settles the signedness. */
extern u8 gUnknown_084C211C[];

/* Wave 49, W49-J. Six more plain `ldrb` byte tables in the same 0x084C2xxx
 * block, all read by the sub_08046778/sub_08046A84 pair that draws the seven
 * CO/army slots. Sizes are not proved -- every user indexes them with a runtime
 * loop counter -- so they stay incomplete arrays.
 *   gUnknown_084C20C0[i], i < 3  -- a unit-type id: both users multiply it by
 *     0x5c and index gUnknown_085D5ABC[] with it.
 *   gUnknown_084C20C3[i], i <= 6 -- goes straight into sub_0801F34C's first
 *     (x) argument.
 *   gUnknown_084C2112[]  -- read as (x, y) PAIRS, `[i*2]` and `[i*2+1]`, both
 *     into sub_0801F34C coordinates; the bound is
 *     gUnknown_085D583C[unit].unk10.
 *   gUnknown_084C212A[i], i <= 6 -- a slot index scaled by 32 into a
 *     struct CoModeData.unk18 movement-cost row.
 *   gUnknown_084C2131[] -- the same (x, y) pair layout as gUnknown_084C2112,
 *     indexed by a separate counter that only advances on drawn slots.
 * Nothing settles the signedness of any of them; `ldrb` alone is byte-neutral
 * between u8 and a value that is never compared. */
extern u8 gUnknown_084C20C0[];
extern u8 gUnknown_084C20C3[];
extern u8 gUnknown_084C2112[];
extern u8 gUnknown_084C212A[];
extern u8 gUnknown_084C2131[];

/* Wave 49, W49-J. An OAM template blob whose ADDRESS is passed, unmodified, as
 * sub_0801BD00's `void *` third argument by sub_08046A84 -- the same slot every
 * other caller fills from a gUnknown_08485Cxx pointer table. Only the address
 * is proved, so the element type is the weakest one that can spell it. */
extern u8 gUnknown_084C20A0[];

/* Wave 31, W31-A. A vertical scroll offset in pixels: sub_080771C0 pushes it
 * straight into REG_BG0VOFS and REG_BG2VOFS, and sub_0807774C and sub_08077C70
 * both pass `0xA8 - it` to sub_08077620. Word-wide (`ldr`); the `strh` into the
 * registers is the hardware's own truncation, not the variable's width, and
 * nothing settles the signedness. */
extern int gUnknown_0300064C;

/* Wave 31, W31-A. A 0x460-byte blob sub_08002E3C pushes to VRAM 0x06014D40
 * through sub_08011E54, whose first parameter is a plain `void *`. */
extern u8 gUnknown_0808D8AC[];

/* Wave 31, W31-A. An LZ blob sub_08086E2C unpacks to the tile block
 * gUnknown_03001FE8's chr_block selects. `u8 []` non-const because Decompress
 * takes `u8 *`. */
extern u8 gUnknown_0823FD7C[];

/* Wave 31, W31-A. The twin of gUnknown_030058F4 and written in the same breath
 * by sub_08086DF4: the two bytes at +0 and +1 of gUnknown_03003F68 are widened
 * into this pair with `ldrb` then `str`, so both are word-wide holders of a
 * byte value. */
extern u32 gUnknown_03005918;

/* ---- wave 33 (W33-C): the 0x08063 address-locality block ---- */

/* 0x08614258, 0x10 bytes -- FOUR word-sized VRAM base pointers, selected by the
 * u8 gUnknown_02028E40. sub_0806370C does `ldr` through the indexed slot and
 * hands the result (plus a 0x80-byte row offset) to sub_08011E54 as its source,
 * which is the only use in the ROM. `u8 *[]` rather than `void *[]` so the
 * `+ n * 0x80` arithmetic is defined, and not const for the same reason
 * gUnknown_080A5524 is not: sub_08011E54's first parameter is a plain `void *`
 * and passing a `const` would warn under -Werror. */
extern u8 *gUnknown_08614258[];

/* 0x03000604. The SECOND copy of the gUnknown_030005FC latch above, one slot
 * along and identical in shape: sub_08064500 compares it against
 * `gUnknown_03004008 - 1` and then assigns from it, and the s16 coordinate pair
 * that goes with it sits at 0x03000608/0x0300060A. Those two ARE INTERIOR
 * ADDRESSES and must not be declared, for exactly the reason the
 * gUnknown_030005FC note gives: aw2bhr.lds names gUnknown_03000604 and then
 * gUnknown_0300060C, so both fall inside this symbol's 8-byte region. Spell
 * them `*(s16 *)((u8 *)&gUnknown_03000604 + 4)` and `+ 6` -- that reproduces the
 * ROM's -fforce-addr words at 0x0816E0B8 and 0x0816E0BC, which hold 0x03000608
 * and 0x0300060A. See src/decomp/c_08064500.c. */
extern s32 gUnknown_03000604;

/* 0x0816E0B0..0x0816E0BC -- FOUR ROM WORDS HOLDING RAM POINTERS, and this is
 * the spelling that finally makes sub_08064474 both match and link. The ROM
 * words are 0x03000600, 0x03000602, 0x03000608 and 0x0300060A: the two s16
 * coordinate pairs that belong to the gUnknown_030005FC and gUnknown_03000604
 * latches. Those four addresses are INTERIOR to their latch symbols and cannot
 * be declared (see both notes above), and wave 32 recorded the resulting
 * deadlock -- naming them matched but failed the link, and reaching them as
 * `*(s16 *)((u8 *)&gUnknown_030005FC + 4)` links but does NOT match: agbcc CSEs
 * the one base and folds +4/+6 into `ldrsh rN,[base,rM]` displacements, which
 * is 96 bytes against the ROM's 140.
 *
 * DEREFERENCING THESE FOUR DOES NOT CLOSE IT EITHER, and the reason is the
 * useful part: `*gUnknown_0816E0B0` costs ONE EXTRA `ldr` per access (148
 * bytes, 40.7%), because agbcc force-addrs `&gUnknown_0816E0B0` into a .rodata
 * slot of its own and the read becomes three levels where the ROM has two.
 * These four words ARE agbcc's own -fforce-addr slots, holding
 * &gUnknown_03000600 &c -- the ROM loads them with a bare `ldr r6,=sym` and no
 * accompanying move, which is the signature of a compiler address constant
 * rather than a user local. They are declared here only so a draft can name
 * them; the real fix is two lines in aw2bhr.lds. The full measurement of all
 * four ruled-out spellings is in work/sub_08064474/sub_08064474.c.
 *
 * `s16 *const`: the pointee is written (`strh`), the pointer is not, and it
 * lives in the cartridge window. */
extern s16 *const gUnknown_0816E0B0;
extern s16 *const gUnknown_0816E0B4;
extern s16 *const gUnknown_0816E0B8;
extern s16 *const gUnknown_0816E0BC;

/* 0x08234AF0, 0x20 bytes -- the exact twin of gUnknown_08239F84 below, and read
 * by the same idiom: sub_0807C994 indexes it with `DivRem(Div(proc->unk48, 4),
 * 0x10) * 2` and hands the element's address to ApplyPaletteExt, so sixteen
 * halfwords is proved by the `% 0x10`, not guessed. The ROM bytes are a
 * symmetric brightness ramp (7FFF, 7BDF, 77BF, 6F7F ... 525F ... 7BDF) that
 * walks down and back up over the sixteen steps, which is what a 16-step
 * palette cycle looks like. Non-const for the same -Werror reason as
 * gUnknown_08239F84: ApplyPaletteExt's first parameter is a plain `u16 *`. */
extern u16 gUnknown_08234AF0[16];

/* 0x08239F84, 0x20 bytes -- sixteen halfwords, and the extent is proved rather
 * than guessed: sub_08064500 indexes it with `DivRem(Div(gUnknown_03004008, 4),
 * 0x10) * 2` and hands the element's address to ApplyPaletteExt, whose first
 * parameter is a plain `u16 *` (hence not const, the same -Werror reason as
 * gUnknown_0809181C). A 16-step palette cycle driven off the frame counter. */
extern u16 gUnknown_08239F84[16];

/* 0x0823DE18, 0x20 bytes -- the third member of the pair above, read by the
 * same idiom and with the same evidence for the extent: sub_08084700 indexes it
 * with `DivRem(Div(proc->unk4a, 2), 0x10)` and hands the element's address to
 * ApplyPaletteExt, so the `% 0x10` proves sixteen halfwords. data/data.s
 * already carves it at exactly 0x20 bytes. Non-const for the same -Werror
 * reason as its two twins. */
extern u16 gUnknown_0823DE18[16];

/* 0x03005950, five bytes. sub_0807C9EC clears it with a descending loop that
 * strength-reduces to a pointer walking from `&g[4]` down to `&g[0]` and a
 * signed `cmp; bge` against the base, so the extent is read off the loop rather
 * than guessed. `u8` from the `strb`; nothing matched reads it back yet, so the
 * signedness is unsettled. The address is already bound by aw2bhr.lds under
 * this exact name. */
extern u8 gUnknown_03005950[5];

/* 0x085802D8, 0x18 bytes -- TWO twelve-byte NUL-padded name strings,
 * "SELECT*MODE" at +0 and "SELECT*CO" at +0xC, carved as one blob by
 * data/data-0848B688.s (its neighbour gUnknown_085802CC holds "SELECT*MAP" in
 * the same 12-byte slot shape). sub_0807C9EC passes the SECOND one to
 * sub_08073304 as its `const void *`, which is why the reference there is
 * spelled `gUnknown_085802D8 + 0xC`: the ROM's -fforce-addr pool word at
 * 0x081D9340 holds 0x085802E4, and the split has no label at that address, so
 * an offset off the enclosing symbol is the only spelling that links. Anyone
 * who splits the blob should give +0xC its own symbol and simplify that call.
 * `u8 []` on the model of gUnknown_085802F0 above. */
extern u8 gUnknown_085802D8[];

/* ---- wave 33 (W33-C): the 0x08050 address-locality block ---- */

/* 0x02029C0C / 0x02029C10 -- a per-side halfword pair (`lsls #1` on the
 * gUnknown_03001470 slot's unk30) that sub_080500D0 subtracts a ROM-supplied
 * origin from to make sub_080155C0's screen coordinates. u16 from the `ldrh`;
 * the SUBTRACTION is what carries the sign, and it is re-narrowed
 * `lsls #0x10; asrs #0x10` into sub_080155C0's s16 parameters, so nothing here
 * needs a signed member. */
extern u16 gUnknown_02029C0C[];
extern u16 gUnknown_02029C10[];
/* 0x085644A0 -- a u16 lookup keyed by gUnknown_02029808[i].unk30[unk2e], with
 * 0xFF meaning "no entry": sub_080504A8 tests it against 0xFF and falls back to
 * its own argument. `ldrh` at the test and `ldrsh` at the use, which is combine
 * folding sign_extend(truncate(zero_extend(mem:HI))) into sub_0803B48C's s16
 * parameter and NOT evidence of a signed object -- see the note on
 * struct Unk02029A10's `x` for the same readout. */
extern u16 gUnknown_085644A0[];

/* ---- wave 33 (W33-F): address-locality blocks 0x08002 and 0x08057 ---- */

/* The gUnknown_08485D44 sprite-pointer table one and two tables along.
 * sub_08002964 and sub_080029F4 index them by their first argument and hand the
 * word straight to sub_0801BD00's `void *` third parameter with no arithmetic
 * in between, which is exactly how the matched sub_08002844 uses
 * gUnknown_08485D44 -- same model, same reason. */
extern void *const gUnknown_08485CF4[];
extern void *const gUnknown_08485D68[];
/* A word table of OBJ-graphics pointers selected by the u8 gUnknown_02028E40
 * (`ldrb` then `lsls #2`), read by sub_080029F4 on its 0x19 path and handed to
 * sub_08011E54's `void *` source parameter. */
extern void *const gUnknown_08489190[];

/* Three more of the gUnknown_0808D8AC group: sub_08002E5C pushes them to VRAM
 * 0x06016180 (0x200 bytes), 0x06016140 and 0x06016160 (0x20 each) through
 * sub_08011E54, whose first parameter is a plain `void *`. */
extern u8 gUnknown_0808DD0C[];
extern u8 gUnknown_0808DF0C[];
extern u8 gUnknown_0808DF2C[];

/* ONE 0x1c-stride ROM table seen through THREE overlapping linker symbols
 * (0x08553A1C, +8, +0xc), the way gUnknown_03004580 and gUnknown_03004582
 * overlap: sub_080576D4, sub_08057A24 and sub_080577E4 each hold a pool word
 * for their own view and then index it by the raw row number, so the column
 * offset is folded into the pool word and never appears as an `adds`.
 *   Each view is a signed tile x/y pair added to the caller's own x/y before
 * the `<< 5` row stride. s8 is measured, not assumed: unk00 is read with the
 * register-offset `ldrsb` (the only `ldrsb` addressing mode THUMB has) and
 * unk01 as `ldrb [r,#1]` + `lsls #0x18; asrs #0x18`, which is what a signed
 * byte at a non-zero constant displacement costs. The 0x1a filler is the
 * measured stride, not a proved layout. */
struct Unk8553A1C /* 0x1c */
{
    /* 0x00 */ s8 unk00;
    /* 0x01 */ s8 unk01;
    /* 0x02 */ u8 filler_02[0x1a];
};
extern struct Unk8553A1C gUnknown_08553A1C[];
extern struct Unk8553A1C gUnknown_08553A24[];
extern struct Unk8553A1C gUnknown_08553A28[];
/* Wave 48, W48-G. A FOURTH view of the same 0x1c-stride table, at +4:
 * sub_0805772C holds its own pool word for 0x08553A20 and reads unk00/unk01
 * off it with exactly the ldrsb / ldrb+shift pair described above. */
extern struct Unk8553A1C gUnknown_08553A20[];
/* Wave 49, W49-D. A FIFTH view, at +0x10. sub_08057860 reads TWO s8 pairs off
 * it, at +0 and +4, and 0x08553A30 is deliberately NOT declared beside it: the
 * ROM carries a single pool word for 0x08553A2C and derives the +4 base with
 * `adds r0,#4` (cse.c's use_related_value, which only relates constants built
 * from the same symbol). A second linker symbol emits a second pool word and
 * costs 4 bytes -- measured. See work/sub_08057860/sub_08057860.c for the
 * spelling that reproduces it and for the two others that do not. */
extern struct Unk8553A1C gUnknown_08553A2C[];
/* Wave 49, W49-D. A view of the same table FOUR BYTES BELOW gUnknown_08553A1C,
 * and the only one whose columns are halfwords: sub_08057BDC reads `ldrh [p]`
 * and `ldrh [p,#2]` off gUnknown_08553A18[idx] to build a `y * 32 + x` VRAM
 * offset, and hands the SAME element to sub_0805772C and sub_080577E4, whose
 * third parameter is `struct Unk8057Pos *` -- so this pair is that type's x/y.
 * It gets its own tag because struct Unk8057Pos is spelled per-file in
 * src/decomp/ (c_080576D4.c, c_080577E4.c, c_08057A24.c) rather than shared. */
struct Unk8553A18 /* 0x1c */
{
    /* 0x00 */ u16 unk00;
    /* 0x02 */ u16 unk02;
    /* 0x04 */ u8 filler_04[0x18];
};
extern struct Unk8553A18 gUnknown_08553A18[];
/* Wave 49, W49-D. A signed byte per gUnknown_03004508 value, guarded by
 * `cmp #8; bhi` at sub_08057BDC's head, so at least nine entries. `ldrsb` with
 * the register-offset form -- the s8-OBJECT tell -- and the value is both
 * subtracted from a tile column and passed as sub_08071900's third (width)
 * argument. */
extern s8 gUnknown_08553B04[];
/* Wave 49, W49-D. Three parallel u16 tables sub_08057164 reads with ONE index,
 * `side * 10 + row * 5 + slot`, into gUnknown_02029A10[row].entries[slot].unk1a:
 * 0x0855203C when the gUnknown_085D6A48 row's column 2 is 1, otherwise
 * 0x08551F60 or 0x08551E84 on that row's column 8. Declared flat because the
 * ROM forms the whole element index and scales it by 2 once at the end, rather
 * than summing three separately-scaled byte offsets. */
extern u16 gUnknown_08551E84[];
extern u16 gUnknown_08551F60[];
extern u16 gUnknown_0855203C[];
/* Wave 49, W49-D. The byte counterpart, indexed `row * 55 + side * 5 + slot`
 * (the 55 is `((y << 3) - y << 3) - y`) and stored into the same entry's unk00
 * and unk01 -- the two sides' values come from ONE table read twice with
 * different `side`. Flat for the same reason as the halfword trio above; the
 * 55 factors as 11 * 5, so the natural shape is [.][11][5]. */
extern u8 gUnknown_085521DC[];

/* Two word tables of ROM blobs, each entry going straight to sub_080718F8's
 * `u8 *` second parameter -- so `u8 *` and not const, for the same reason the
 * 0817D874 group above is not. 0x08553AC0 is indexed by sub_080576D4's raw row
 * number; 0x08553AD8 by `(gUnknown_02029B78[i & 1] + 9) / 10` in sub_080577E4,
 * a round-up divide by ten. */
extern u8 *gUnknown_08553AC0[];
extern u8 *gUnknown_08553AD8[];
/* A halfword pair read as `[i & 1]` by both sub_080576D4 and sub_080577E4, each
 * of which uses it as `(u16)(x * 0x1000)` -- `lsls #0x1c; lsrs #0x10`, the
 * palette nibble of the tile reference handed to sub_080718F8. Read as a
 * MULTIPLY, not a mask plus a shift; the 4-bit field is the u16 truncation of
 * the product. */
extern u16 gUnknown_08562124[];
/* Wave 48, W48-G. A ROM table of SEVEN-byte rows of tile numbers -- the seven
 * tiles of one digit strip -- read a byte at a time with a register-offset
 * `ldrb`. sub_0805772C picks the row `gUnknown_02029B78[i & 1] >> 1` (the `* 7`
 * is `lsls #3; subs`), except when that halfword is exactly 1, where it holds a
 * SECOND pool word for 0x085538B9 -- which is row 1 of the same table, the
 * overlapping-linker-symbol pattern again. The row count is unproved. */
extern u8 gUnknown_085538B2[][7];
extern u8 gUnknown_085538B9[];
/* The RAM counterpart, also indexed `[i & 1]` and also halfwords: sub_080577E4
 * divides `gUnknown_02029B78[i & 1] + 9` by ten to pick a gUnknown_08553AD8
 * row. Signed division (`__divsi3`), so the sum is an `int`. */
extern u16 gUnknown_02029B78[];
/* Wave 48, W48-G. The TARGET counterpart of gUnknown_02029B78, two elements
 * further on (0x02029B78 + 4) and reached through its own linker symbol and its
 * own -fforce-addr constant at 0x081361A8: sub_08057AE8 subtracts it from
 * gUnknown_02029B78 element-wise to get the s16 delta it turns into a per-frame
 * step. `ldrh`, so u16; the difference is narrowed to s16 by the reader. */
extern u16 gUnknown_02029B7C[];
/* Two more sub_080718F8 ROM blobs, selected by bit 0 of sub_08057A24's second
 * argument. `u8 []` and not const, as with the rest of that group. */
extern u8 gUnknown_0816D900[];
extern u8 gUnknown_0816D91C[];
/* Wave 51, W51-K. 0x0816D96C -- REAL DATA, not a -fforce-addr word: the symbol
 * goes straight to sub_0808B6E8 as the source of a 10-byte copy with no load in
 * front of it (sub_08059E00), the same argument that settled gUnknown_08090CF4
 * and gUnknown_08090D18. baserom.gba holds f4 01 e8 03 d0 07 a0 0f ff ff at
 * that offset -- 500, 1000, 2000, 4000, 0xFFFF -- so five ASCENDING u16
 * thresholds ending in a sentinel, which is also what the consumer does with
 * them: sub_08059E00 copies all ten bytes to a stack buffer and walks it
 * `while (v > buf[i]) i++`, returning i + 1 as a 1..5 band index. Unsigned
 * because that walk compares with `bls`. */
extern const u16 gUnknown_0816D96C[];
/* Wave 48, W48-G. Two more of the same group, both used as BASE ADDRESSES with
 * no load in front of the pool word (checked against baserom.gba: the words at
 * these addresses are 0x00240010 and 0x7FFF6AF7, i.e. payload, not pointers --
 * unlike the 0x081361A0 run). gUnknown_0816CABC is sub_08057AE8's Decompress
 * source into VRAM 0x06004000, so `u8 *`-compatible and non-const like the rest
 * of the group. gUnknown_0816D498 is a palette bank: the same function
 * CpuFastSets 8 words from `gUnknown_0816D498 + gUnknown_03004500[k] * 32` into
 * PLTT 0x05000140 and 0x05000120, which fixes the 0x20-byte stride. */
extern u8 gUnknown_0816CABC[];
extern u8 gUnknown_0816D498[];
/* Halfword tables sub_08057048 reads into the six-halfword record it hands to
 * sub_080570C4: 0x0855388C by its own (u16) first argument, 0x085538A2 by
 * `gUnknown_085D6A48[gUnknown_03004580[i][1]][1] * 2 + i`. */
extern u16 gUnknown_0855388C[];
extern u16 gUnknown_085538A2[];
/* `strh 1` at the tail of sub_08057048, immediately after the sub_080570C4
 * call -- a request flag in the same RAM run as gUnknown_03004538. Halfword. */
extern u16 gUnknown_03004534;

/* Wave 34, W34-K -- block 0x08069 / 0x0806B.
 *
 * gUnknown_08580E60 is a ROM-resident POINTER, not a buffer: sub_080697CC and
 * sub_08069FD0 both hold its ADDRESS in r4 across the whole body and reload
 * `ldr r1, [r4]` at each use, exactly as the already-declared gUnknown_08499580
 * and gUnknown_0849957C are used. The pointee is a VRAM-bound work buffer -- it
 * is CpuFastSet's 0x400-word fill destination, a Decompress destination, and
 * then sub_08011E54's source for 0x1000 bytes to 0x0600F000. `void *` rather
 * than `u16 *` because nothing in either function indexes through it; if a
 * later caller does, narrow it then. Not const: it is a destination. */
extern void *gUnknown_08580E60;
/* Six compressed blobs reached only by address (`ldr r0, =sym`, never a load
 * through them) and handed to Decompress's source parameter by sub_080697CC
 * and sub_08069FD0, which are near-twins differing only in the first
 * destination (0x06000000 vs 0x06008000) and one extra blob. Not spelled
 * const because Decompress's first parameter is a plain pointer. */
extern u8 gUnknown_08183A00[];
extern u8 gUnknown_08184FF4[];
extern u8 gUnknown_08185F0C[];
extern u8 gUnknown_0818616C[];
extern u8 gUnknown_0818633C[];
extern u8 gUnknown_08186460[];
/* Wave 50 (W50-C). Two more compressed blobs on the same model, reached only by
 * address in sub_08068E60 and handed to Decompress's source parameter -- 081837A0
 * to 0x06008000 and 081838EC to whatever gUnknown_08499580 points at. */
extern u8 gUnknown_081837A0[];
/* Wave 51, W51-H. Two more Decompress blobs from the same ROM run as
 * gUnknown_081837A0, both handed to Decompress by sub_080694EC as the bare
 * symbol -- CA8 to VRAM at 0x06004800 and A74 to whatever gUnknown_08499578
 * points at. Non-const because Decompress takes a plain `u8 *`. */
extern u8 gUnknown_08183CA8[];
extern u8 gUnknown_08184A74[];
extern u8 gUnknown_081838EC[];
/* Wave 50 (W50-C). ApplyPaletteExt's `u16 *` first parameter in sub_08068E60,
 * count 0x80 at palette 0 -- four palettes' worth, i.e. ApplyPalettes(x, 0, 4).
 * Typed from that parameter; the address is never dereferenced. */
extern u16 gUnknown_08183C28[];
/* ApplyPaletteExt's `u16 *` first parameter in sub_080697CC and sub_08069FD0
 * (count 0x20 at palette 0xc0 in both). Typed from that parameter, not from
 * any access -- the address is never dereferenced here. */
extern u16 gUnknown_081866D8[];
/* PutSpriteExt's `u16 *` fourth parameter in sub_0806BD84, the same model as
 * the other OAM blobs noted above. Handed over with no dereference. */
extern u16 gUnknown_0816E174[];
/* sub_080670F8's `const u8 *` argument in sub_08069864 -- same shape as the
 * 085819E4 entry noted elsewhere in this file. */
extern const u8 gUnknown_08581438[];
/* Two proc scripts: gUnknown_085819C4 is Proc_Start's in sub_0806B87C and
 * gUnknown_08581A24 is Proc_Start's in sub_0806BE7C. The proc gUnknown_08581A24
 * starts is the one sub_0806BD84 runs -- sub_0806BE7C writes +0x2c/+0x30/+0x58/
 * +0x5c/+0x64 and sub_0806BD84 reads exactly those five, which is a producer/
 * consumer pair agreeing independently on the layout. */
extern const struct ProcCmd gUnknown_085819C4[];
extern const struct ProcCmd gUnknown_08581A24[];
/* Wave 48 (W48-C). A third proc script in the same run: Proc_Start's first
 * argument in sub_0806BED8. The proc it starts is the SAME one gUnknown_08581A24
 * starts -- sub_0806BED8 seeds +0x2a[]/+0x54/+0x58/+0x5c/+0x60 and sub_0806BE7C
 * (already promoted, src/decomp/c_0806BD84.c) reads exactly those, so the two
 * agree on struct Unk6BE7CParent independently. sub_0806BED8 is the string
 * loader for it: it converts a NUL-terminated byte string into the +0x2a
 * halfword table and stores the length in +0x54, which is the bound
 * sub_0806BE7C then counts up to. */
extern const struct ProcCmd gUnknown_08581A34[];
/* A FLAT u16 table, not an array of 2-halfword structs. sub_08069D3C reads
 * element `a1 * 2` as `lsl #2; add; ldrh` and element `a1 * 2 + 1` as
 * `lsl #1; add #1; lsl #1; add; ldrh` -- the second keeps the +1 inside the
 * index expression instead of folding 2 into the load displacement, which a
 * struct member access could not do. Both results are stored to BYTE globals
 * (gUnknown_03002B40 and gUnknown_03002B4C). */
extern u16 gUnknown_08581478[];

/* ---- Wave 35 (W35-A): the 0x08040000-0x08045000 neighbourhood ---- */
/* A u16 counter/mode word in IWRAM. sub_08043834 zeroes it with `strh`;
 * sub_08043590 reads it `ldrh` twice, once as `(u16)(g - 1) <= 3` and once
 * straight into sub_0804423C(int). Named in the unknown-functions.h note on
 * sub_080436DC, which stores its third argument here. */
extern u16 gUnknown_030005D0;
/* Two ROM blobs sub_08043834 copies through sub_08011E54: 0x740 bytes to OBJ
 * VRAM 0x06010000 and 0xc0 bytes to 0x06010840. `u8 []` because sub_08011E54's
 * source parameter is `void *` and neither is indexed. Non-const on the same
 * grounds as gUnknown_081218BC above -- sub_08011E54 takes plain pointers. */
extern u8 gUnknown_08102824[];
extern u8 gUnknown_081259CC[];
/* A table of 16-colour palettes indexed by `gUnknown_08499598[n].unk1a - 1` in
 * sub_08043834 -- exactly the gUnknown_0810EA60 shape and declared the same
 * way: `[][16]` and not a flat `u16 []`, because the ROM's index math is
 * `(v - 1) << 5` off the bare symbol and a flat array folds the -1 into the
 * relocation's addend and loses the `subs`. */
extern u16 gUnknown_08104264[][16];
/* Two 16-entry u16 ramps sub_08043590 picks between on sub_0804423C's result,
 * indexed `(gUnknown_03004008 >> 2) & 0xf` and `(gUnknown_03004008 >> 1) & 0xf`
 * respectively and handed to sub_0801368C(u16 *, u16, u16). The shift is
 * LOGICAL (`lsrs`) in both, which is what makes the local holding
 * gUnknown_03004008 unsigned even though that global is declared s32. */
extern u16 gUnknown_08104304[];
extern u16 gUnknown_08104324[];
/* Sprite object-list blobs in ROM, each handed straight to PutSprite's or
 * PutSpriteExt's `u16 *` fourth parameter with no arithmetic -- the same shape
 * as gUnknown_084A0730 above and declared the same way. gUnknown_084A0790 and
 * gUnknown_084A07DA are the pair that note already predicted. */
extern u16 gUnknown_084A0024[];
extern u16 gUnknown_084A0052[];
extern u16 gUnknown_084A005A[];
extern u16 gUnknown_084A0756[];
extern u16 gUnknown_084A0790[];
extern u16 gUnknown_084A07DA[];

/* ---- Wave 36 (W36-B): the 0x08042-0x08045 blocks ---- */
/* Two more of the PutSprite `u16 *` blobs above, drawn one after the other by
 * sub_080436DC with OAM words 0x7000 and 0xE03A. They are 8 bytes apart, the
 * same one-blob-per-8-bytes packing as gUnknown_084A0052/gUnknown_084A005A. */
extern u16 gUnknown_084A0032[];
extern u16 gUnknown_084A003A[];
/* The tile blob sub_080436DC copies to OBJ VRAM 0x06010740, 0x100 bytes at a
 * time, at the byte offset `((gUnknown_08499598[n].unk1d * 8) & 0x3ff) * 0x20`
 * -- i.e. 8 tiles per record, wrapping at 0x400 tiles. `u8 []` for
 * sub_08011E54's `void *` source, the same model as gUnknown_08102824. */
extern u8 gUnknown_08102F64[];
/* NOT an address word, even though it sits inside the 0x08091350-0x0809138C
 * `-fforce-addr` pool block: the word at 0x0809136C is 0x0000003F, i.e. the
 * one-character string "?". sub_080436DC passes it to sub_080119A0's
 * `const char *` third parameter as the placeholder for a hidden funds value.
 * Dereferencing the address in baserom.gba is what separates it from its
 * neighbours, all of which hold 0x03xxxxxx/0x08xxxxxx pointers. */
extern const char gUnknown_0809136C[];

/* ---- wave 36 (W36-E): the 0x08075-0x08077 and 0x0807A-0x0807B blocks ---- */

/* Two 16-entry WORD tables in the 0x081CC4F8 run, both subscripted in
 * sub_08075F44 by the same value -- the top nibble of a map halfword -- with
 * one shared `lsls #2`. gUnknown_081CC4F8's element goes to sub_0802D5CC's
 * `int` first parameter and is then re-used to subscript gUnknown_081CC578[];
 * gUnknown_081CC538's goes to sub_0801F2AC's `int` first parameter. The plain
 * word `ldr` off an `lsls #2` index is the whole width evidence, and nothing
 * narrows or sign-tests either element, so `int` is the weakest type that
 * fits. The two run back-to-back, 0x40 bytes each, and end exactly where
 * gUnknown_081CC578 begins, which is what bounds them at 16 entries. */
extern int gUnknown_081CC4F8[];
extern int gUnknown_081CC538[];
/* Six halfwords immediately before the 0x081CC584 literal pool (see the
 * force-addr note below), subscripted by gUnknown_081CC4F8[]'s element with
 * `lsls #1; ldrh` in sub_08075F44 and used in turn to index
 * gUnknown_08610A38[]. Values 0x0CEF..0x0CF3 -- string ids, the same role the
 * gUnknown_08610A38 subscripts elsewhere in the ROM carry. */
extern u16 gUnknown_081CC578[];
/* FORCE-ADDR, NOT OBJECTS: 0x081CC584, 0x081CC588 and 0x081CC58C hold
 * 0x0202FDFC, 0x03002B6C and 0x030030B4 -- the addresses of gUnknown_0202FDFC,
 * gUnknown_03002B6C and gUnknown_030030B4. They are the -fforce-addr pool of
 * sub_08075F44 and sub_08076888, continuing the run W35-B audited at
 * 0x081CC590/594/598/59C, and both functions match with the objects named
 * directly. Deliberately NOT declared here: naming them would re-invent the
 * fictional globals that note is about. */
/* sub_08075F44 hands it to sub_080718F8's `u8 *` second parameter for 0x360
 * bytes and nothing dereferences it, so that parameter is the only evidence --
 * the same reading gUnknown_081D0BAC carries. */
extern u8 gUnknown_081D22C4[];
/* The twin of gUnknown_08614458 one record along, read exactly the same way:
 * sub_08076298 does `ldrsb` off a zero index register at
 * gUnknown_0861445C[proc->unk40] and hands the result to sub_08071900's `int`
 * third parameter. `s8` on the `ldrsb`, as for gUnknown_08614458. */
extern s8 gUnknown_0861445C[];
/* CORRECTED in wave 38 (W38-B). This was `extern u16 gUnknown_0202FE38;`,
 * generalised from the single `strh 0xFFFF` sub_08076888 aims at the address.
 * That store is real, but it writes the TERMINATOR of a 12-byte record array,
 * and the array's shape is now pinned by four functions that walk it:
 *   - Layout: sub_08074754 fills a record -- +0x00 the s16 id, +0x02/+0x04 the
 *     world x/y copied out of gUnknown_08615194[id].unk06/.unk08, +0x08 a
 *     `struct Unk0801C210 *` sprite handle -- and then stores -1 into the NEXT
 *     record's +0x00 with `strh [r6,#0xc]`, which is what fixes the stride at
 *     12. sub_08074834 copies a whole record with `ldm/stm {r5,r6,r7}`.
 *     +0x06 is untouched by every function that walks the array.
 *   - Terminated by -1 in +0x00, read `ldrsh`: sub_08074670 and sub_08074754
 *     both scan until that field is -1, and sub_080745C0 (matched) empties the
 *     list by storing -1 into the first record.
 *   - 16 entries: sub_08074834 scans 0..15 and compacts the tail over the same
 *     range, and gUnknown_0202FEF8 is allocated at 0x0202FE38 + 16*12.
 * NOT A SEPARATE OBJECT, which is the half the old comment had backwards: it
 * is `&gUnknown_0202FDFC.unk3c` reached as a record array, and sub_08074670
 * proves the original source spelled it that way. That function materialises
 * 0x0202FE38 in ONE pool word and then derives the struct base with
 * `adds r6,r5,#0 / subs r6,#0x3c` rather than loading a second pool word --
 * a CSE agbcc can only do when both addresses come from one symbol_ref plus a
 * compile-time offset. Two independent externs emit two pool words and cost
 * that function 4 bytes. sub_08074754 does load both as separate words, but
 * only because its own copy of the base has been advanced by the scan loop and
 * is dead by the time it needs gUnknown_0202FDFC.unk12.
 * Hence NO extern is declared here -- the type below is the whole declaration,
 * and the callers spell the object `(struct Unk0202FE38 *)&gUnknown_0202FDFC
 * .unk3c`, exactly as the matched c_080745C0.c already does for this address.
 * Do NOT fold the array into struct Unk0202FDFC: that struct is shared and
 * promoted code already reads +0x00..+0x3c through it. */
struct Unk0202FE38
{
    /* 0x00 */ s16 unk00;
    /* 0x02 */ s16 unk02;
    /* 0x04 */ s16 unk04;
    /* 0x06 */ s16 unk06;
    /* 0x08 */ struct Unk0801C210 *unk08;
};
/* FORCE-ADDR, NOT OBJECTS: the ROM words at 0x081CC4C0, 0x081CC4D4 and
 * 0x081CC4E4 hold 0x0202FE38, 0x0202FEF8 and 0x081D2930 -- the addresses of
 * the record array above, of gUnknown_0202FEF8 and of gUnknown_081D2930. They
 * are the -fforce-addr pools of sub_08074834, sub_08074AD0 and sub_08074D28
 * respectively (each function's base is `ldr rN,=<word>; ldr rM,[rN]`).
 * sub_08074834 and sub_08074D28 match with the objects named directly;
 * sub_08074AD0 is parked at 140/144 for an unrelated reason (see
 * work/sub_08074AD0). Deliberately not declared, for the same reason as the
 * 0x081CC584 run above. Note the batch index still lists them as `data_refs`,
 * so they read like ROM tables.
 *
 * WAVE 38 (W38-E) -- 0x081CC4D4 RE-TESTED AS A REAL POINTER OBJECT AND IT IS
 * NOT ONE. sub_08074AD0 reloads its base `ldr r5,[r7]` on every iteration of a
 * loop that stores through it, which argues for a genuine `s8 *` global whose
 * reload is forced by aliasing, since a constant-pool MEM is RTX_UNCHANGING_P
 * and LICM would hoist it. Declaring `extern s8 *gUnknown_081CC4D4;` and
 * indexing it does NOT replace the indirection -- agbcc applies force-addr to
 * the pointer variable as well and emits three levels,
 * `ldr r7,=<pool>; ldr r6,[r7]; ldr r0,[r6]`, plus an `R_ARM_ABS32 .rodata`
 * word the ROM does not have (53.5%, size exact). The force-addr reading above
 * is confirmed and this axis is closed; the in-loop reload is explained by
 * where the `ldr r7` lands relative to the loop guard, not by aliasing. See
 * docs/agbcc-codegen.md, "A `-fforce-addr` word is NOT a pointer object". */
/* sub_0801C210's sprite/animation script for the 0x08074000 block's overworld
 * markers: sub_08074754 and sub_08074D28 both pass it as that function's first
 * argument and nothing dereferences it, so the `void *` parameter is the only
 * evidence and `u8 []` follows the modelling used for every other
 * sub_0801C210 script. */
extern u8 gUnknown_081D2930[];
/* sub_08076888's setup blobs, each typed straight off the parameter it is
 * handed to and nothing else: 08614548 is sub_08012C58's `void *`; 081CC5F0,
 * 081D1644, 081D17A4 and 081D2A54 are Decompress `u8 *` sources for four fixed
 * VRAM destinations; 081D208C and 081D20CC are ApplyPaletteExt `u16 *` sources
 * for 0x20 bytes each, the pair it picks between on sub_0803866C. None is
 * const, because none of those parameters is. */
extern u8 gUnknown_08614548[];
extern u8 gUnknown_081CC5F0[];
extern u8 gUnknown_081D1644[];
extern u8 gUnknown_081D17A4[];
extern u8 gUnknown_081D2A54[];
extern u16 gUnknown_081D208C[];
extern u16 gUnknown_081D20CC[];
/* A byte table sub_0807A0C4 subscripts with the u16 at +0x52 of its proc and
 * whose element it turns into an OAM attribute-2 tile index (`+ 4`, `<< 12`,
 * or'd with 0x240). `ldrb`, so u8; nothing bounds it. */
extern u8 gUnknown_08615E48[];
/* Wave 51, W51-F. The same byte table one record earlier, subscripted by the
 * proc's +0x52 counter by sub_08079FAC and turned into the identical OAM
 * attribute-2 tile index (`+ 4`, `<< 12`, or'd with 0x240) as gUnknown_08615E48
 * above and gUnknown_08615E40 further up.
 *   THE EXTENT ON gUnknown_08615E40 ABOVE ("[12]") IS PROBABLY WRONG, and this
 * symbol is the evidence. baserom 0x08615E40 reads `02 03 04 05 02 03 04 05 02
 * 03 04 05` and then a run of address words -- so the wave that declared it
 * bounded the RUN correctly but read it as one 12-byte array. It is three
 * IDENTICAL 4-byte tables at 0x08615E40, 0x08615E44 and 0x08615E48, one per
 * caller, and all three offsets are separately referenced pool words in three
 * different functions. gUnknown_08615E48 was already declared separately for
 * the same reason. I have NOT narrowed gUnknown_08615E40 -- an extent on an
 * extern u8 array is byte-neutral and no oracle would catch a mistake either
 * way -- but the three-table reading is what the ROM bytes say. */
extern u8 gUnknown_08615E44[4];
/* 0x14-byte records: four Decompress `u8 *` blobs and one ApplyPaletteExt
 * `u16 *` palette, picked by sub_0807A99C on a 0..17 index (the stride is
 * `x * 5 << 2`). unk00/unk04/unk08 go to three fixed offsets inside the BG
 * char block gUnknown_0300251C.bits.chr_block selects, unk0c to the buffer
 * gUnknown_08499584 points at, and unk10 is the palette -- except on index 7,
 * where cse's record_jump_equiv folds the subscript and the ROM reads the
 * record-7 palette slot as a bare `+ 0x9c`. */
struct Unk08615E4C /* 0x14 */
{
    /* 0x00 */ u8 *unk00;
    /* 0x04 */ u8 *unk04;
    /* 0x08 */ u8 *unk08;
    /* 0x0c */ u8 *unk0c;
    /* 0x10 */ u16 *unk10;
};
extern const struct Unk08615E4C gUnknown_08615E4C[];
/* The proc script sub_0807BCF0 hands to Proc_Start with its own proc as the
 * parent, immediately before breaking itself. */
extern const struct ProcCmd gUnknown_08616548[];

/* ---- wave 36 (W36-J): the 0x08046-0x08049 address-locality block ---------- */

/* 0x02028E18. A one-bit toggle: sub_08047F70 does
 * `gUnknown_02028E18 = (gUnknown_02028E18 + 1) & 1;` (ldrb; adds #1; movs #1;
 * ands; strb) when L or R is held. u8 from the load; nothing else reads it in
 * matched code, so only the width and the 0/1 range are proved. */
extern u8 gUnknown_02028E18;

/* 0x084C30FC. sub_080499F8 hands the ADDRESS straight to sub_0801BD00's
 * `void *` third parameter with no arithmetic -- the same model as
 * gUnknown_0848B698 above. The first ROM word is 0x00000004, so it is a data
 * blob and not a pointer variable. */
extern u32 gUnknown_084C30FC[];

/* Two OAM sprite blobs sub_080499F8 draws four times each (x = 0, 0x20, 0x40,
 * 0x60), 0848B6C6 on row 0x1c and 0848B6BE on row 0x2c. `u16 []` because that
 * is PutSprite's fourth parameter, and NOT const for the same reason -- the
 * parameter is a plain `u16 *`. gUnknown_0848B6C6 is also sub_08048158's
 * case-1 blob. */
extern u16 gUnknown_0848B6BE[];
extern u16 gUnknown_0848B6C6[];

/* sub_08048158's case-3 and case-2 OAM blobs, on PutSprite's `u16 *` fourth
 * parameter with no arithmetic, alongside gUnknown_08615C4E (its case-0 blob,
 * declared above). Same non-const reason. */
extern u16 gUnknown_08615C12[];
extern u16 gUnknown_08615C20[];

/* Wave 51, W51-F. sub_080794E8's second PutSprite passes this straight into the
 * `u16 *` fourth parameter (`ldr r3, =gUnknown_08615BE4` with no dereference and
 * no arithmetic), exactly like the gUnknown_08615C12/C20 blobs above -- an OAM
 * blob, and a real symbol rather than a pool word for the same reason. Same
 * non-const reason as its neighbours. */
extern u16 gUnknown_08615BE4[];

/* 0x0823E550, a 16-colour palette on ApplyPaletteExt's `u16 *` first
 * parameter. sub_08048644 and sub_08048158 both index it
 * `DivRem(Div(0x40 - DivRem(gUnknown_03004008, 0x40), 4), 0x10)` -- a 16-step
 * cycle, same shape as gUnknown_08239F84 -- and both also pass the base
 * unindexed, so the extent is 16 and the element is `u16`. The 0x20 bytes at
 * 0x0823E550 read as palette data (first word 0x7BBF77BF). Not const:
 * ApplyPaletteExt takes a plain pointer. */
extern u16 gUnknown_0823E550[16];

/* 0x0812AF68. sub_08046E48's only use is sub_08071948's `const void *` fourth
 * parameter, by name with no dereference. NOT a -fforce-addr word despite the
 * 0x0812Axxx address: the ROM word there is 0x0007130D, which is not an
 * address, and the call site never does the second `ldr` a force-addr word
 * needs. (The neighbours 0x0812A118/11C/120/13C/154/16C ARE force-addr words --
 * see the note on those below.) */
extern u8 gUnknown_0812AF68[];

/* 0x0812C024, 0x80 bytes of tile data sub_08046E48 uploads to VRAM in four
 * sub_08011C68 calls: +0x00 (0x60 bytes) to 0x06014EE0, +0x20 (0x40) to
 * 0x06014F40, and +0x60 (0x20) to BOTH 0x06014F80 and 0x06014FA0. `u8 []`
 * because the offsets are byte offsets off the base (`adds r0, #0x20`,
 * `adds r4, #0x60`); sub_08011C68's first parameter is `const void *`, so the
 * element type is not constrained further. */
extern u8 gUnknown_0812C024[];

/* Two more tables of the same 20-byte record as gUnknown_085D583C, selected by
 * gUnknown_02028DD6 in sub_08046E48 (== 6 picks 084998A4, == 8 picks 0849982C,
 * anything else falls back to gUnknown_085D583C itself) and then indexed by
 * gUnknown_02028DD7 with the identical `lsls #2; adds; lsls #2` stride-20
 * arithmetic. Only +0x00 (a Decompress `u8 *`) and +0x04 (an ApplyPaletteExt
 * `u16 *`) are reached through these two; the tag is shared because the index
 * arithmetic and both member offsets are byte-identical across all three
 * arms. */
extern const struct Unk085D583C gUnknown_0849982C[];
extern const struct Unk085D583C gUnknown_084998A4[];

/* THE 0x0812Axxx WORDS ARE -fforce-addr ADDRESS CONSTANTS, NOT OBJECTS.
 * Wave 36, W36-J dereferenced all six in baserom.gba:
 *     0x0812A118 -> 0x03002EE0   0x0812A13C -> 0x03002EE0   (both &gpKeySt)
 *     0x0812A11C -> 0x02028DD5   0x0812A120 -> 0x02028DD6
 *     0x0812A154 -> 0x084C30F8   0x0812A16C -> 0x084C3240
 * 0x0812A118 and 0x0812A13C holding the SAME address is the decisive tell from
 * the brief -- N scattered symbols, one address. aw2bhr.lds puts gpKeySt at
 * 0x03002EE0, so the honest spellings are `gpKeySt->...` (sub_08046D30,
 * sub_08047F70, sub_08048F4C), `gUnknown_02028DD5` / `gUnknown_02028DD6`
 * (sub_08046E48) and `gUnknown_084C3240` (sub_08049C38). Do NOT declare a
 * `gUnknown_0812Axxx` object for any of them; an inherited draft that did
 * (`struct Unk03002EE0 **const gUnknown_0812A118`) was the fictional-global
 * pattern, and the honest spelling matched. */

/* THE 0x08090AC4-0x08090C30 RUN IS ONE TRANSLATION UNIT'S `.rodata`, NOT DATA.
 * Wave 36, W36-M dereferenced every word of it in baserom.gba. It is the
 * `.rodata` of the unit holding sub_080281F0..sub_0802DCB4 (code-0801D390.s),
 * and it interleaves STRING LITERALS with `-fforce-addr` address constants,
 * exactly as agbcc emits them -- one address word per (function, symbol) pair,
 * in address order:
 *     0x08090AC4  const char *[2] = { "OFF", "ON" }  (-> 0x08090AD0/0x08090ACC)
 *     0x08090ACC "ON"   0x08090AD0 "OFF"  0x08090AD4 "O"
 *     0x08090AD8 " MAP:%02d"   0x08090AE4 "SNOW:%s"  0x08090AEC "SAKU:%s"
 *     0x08090AF4 -> 0x03002EE0  &gpKeySt
 *     0x08090AF8 -> 0x03001FBC  &gUnknown_03001FBC
 *     0x08090AFC -> 0x03001470  &gUnknown_03001470
 *     0x08090B00 -> 0x03003FC0  &gUnknown_03003FC0        (sub_080281F0)
 *     0x08090B04 "R: SAKUTEKI ON"   0x08090B14 "R: SAKUTEKI OFF"
 *     0x08090B28 "1P"  0x08090B2C "CP"  0x08090B30 "2P"  0x08090B34 "VS"
 *     0x08090B38 "--"  0x08090B3C "PAUSE"
 *     0x08090B44 -> 0x03003FC0   0x08090B48 -> 0x03001FBC
 *     0x08090B4C -> 0x03001470                            (sub_080283E4)
 *     0x08090B5C -> 0x08499590  &gUnknown_08499590        (sub_080290B0)
 *     0x08090B60 -> 0x0849A00C   0x08090B64 -> 0x03002EE0 (sub_0802925C)
 *     0x08090B90 -> 0x0849A2A6   0x08090B94 -> 0x0849A284 (sub_0802A8DC)
 *     0x08090C18 -> 0x03001FBC   0x08090C1C -> 0x03002EE0 (sub_0802DA18)
 *     0x08090C20 -> 0x03002EE0  &gpKeySt                  (sub_0802DCB4)
 * Do NOT declare a `gUnknown_08090Bxx` object for any of these: write the
 * honest spelling (`gpKeySt->held`, `gUnknown_03001470[i]`, ...) and let agbcc
 * place its own copy. Note that the same source spelling gives TWO levels of
 * indirection in one function and THREE in another -- sub_0802DCB4 reads
 * gUnknown_08499590 with a plain inline pool word while sub_080290B0 gets the
 * `.rodata` reroute -- and the discriminator is reference count across a
 * control-flow merge, not the spelling. Neighbouring words 0x08090B50/B54/B58
 * (&gUnknown_08499598, &gUnknown_08499590, &gUnknown_020237B0) belong to
 * functions between these and are the same class. */

/* 0x0201E450. Only +0x04 and +0x06 are reached: sub_08028F84 `strh`s a scroll
 * position pair into them -- first a Q16 value divided down by 0x10000, then,
 * on the last frame of the slide, the destination pair straight out of the
 * proc. Nothing reads them in asm/, so the width is from the `strh` and the
 * signedness follows the signed division feeding it. Wave 36, W36-M. */
struct Unk0201E450
{
    /* 0x00 */ u8 filler_00[0x04];
    /* 0x04 */ s16 unk04;
    /* 0x06 */ s16 unk06;
};
extern struct Unk0201E450 gUnknown_0201E450;

/* 0x020237B0, and 0x80 bytes below gUnknown_02023830. A per-frame step table:
 * sub_08028F84 indexes it with a countdown halfword and adds the byte to an
 * accumulator (`ldrb`, added to a u16, no sign extension anywhere), so `u8 []`.
 * Wave 36, W36-M. */
/* Wave 38, W38-I. A step-ramp scratch buffer: sub_08028EF0 fills [0..n] with a
 * 1,2,3,...,8,8,8 acceleration ramp summing to a cursor-slide distance, one
 * `strb` per entry, then writes the remainder as a final short step -- so `u8 []`
 * is right and the extent is bounded by that distance, not pinned here.
 *   Its ROM pool word is 0x08090B58, which holds 0x020237B0 (dereferenced out of
 * baserom.gba, and 0x08090B54 / 0x08090B5C on either side both hold 0x08499590,
 * i.e. this is the middle of one unit's `.LC` block). agbcc's -fforce-addr copy
 * is what the ROM's two-level `ldr r5,=<word>; ldr r4,[r5]` is; the HONEST
 * `gUnknown_020237B0[...]` spelling reproduces it exactly and the build places
 * the word, per wave 18. Do NOT declare gUnknown_08090B58 as a `u8 **` and
 * dereference it -- that adds a fourth level, the error wave 20 recorded for
 * this same idiom. */
extern u8 gUnknown_020237B0[];

/* 0x08499F4C. Six halfword bounds read in pairs by sub_080281F0 -- [0]/[1] for
 * the gUnknown_03003FC0.unk02 field, [2]/[3] for .unk2c and [4]/[5] for .unk0d,
 * each pair being the lower and upper clamp of one debug-menu row. UNSIGNED and
 * measured: every compare against the u8 field is `bhs`/`bls`, never `bge`.
 * Wave 36, W36-M. */
extern const u16 gUnknown_08499F4C[];

/* Two OAM/sprite blobs handed to sub_0801BD00 as its `void *` third argument by
 * sub_0802A8DC, alongside the 0x11CA / 0x61E6 attribute words. Declared `const
 * u16 []` and cast at the call site, the same way c_08022A5C.c spells
 * gUnknown_0848930C. Wave 36, W36-M. */
extern const u16 gUnknown_0849A1F0[];
extern const u16 gUnknown_0849A240[];

/* Wave 37 (W37-E). Two more sub_080193B0 script openers, picked by
 * sub_080005FC on sub_08004E44()'s result. `const u8 []` because that is
 * sub_080193B0's declared parameter and nothing here dereferences them. */
extern const u8 gUnknown_084856FC[];
extern const u8 gUnknown_084857AC[];

/* Wave 37 (W37-E). Three ROM tables sub_080032EC memcpys to its own stack with
 * sub_0808B6E8, one call each, so the element type is what the STACK COPY is
 * read as and the extent is the byte count divided by it:
 *   0808D760  8 bytes  28 00 48 00 68 00 88 00  -> read `ldrh`, so u16[4].
 *             These are sub_0800C6E8's kind codes (0x28/0x48/0x68/0x88).
 *   0808D768  4 bytes  00 12 00 12              -> read `ldrb; lsls #0x18;
 *   0808D76C  4 bytes  D0 D0 E2 E2                 asrs #0x18`, so s8[4].
 *             An x and a y offset per lane; 0xD0/0xE2 are -48 and -30, which
 *             only make sense signed.
 * The neighbouring 0x0808D770 is NOT one of these -- it is an -fforce-addr
 * address constant holding &gUnknown_0200B0B0 (dereferenced in baserom.gba),
 * the same as 0x0808D6DC/0x0808D70C/0x0808D710/0x0808D714 in the same run. */
/* Wave 42, W42-A. Two sub_0808B6E8 (memcpy) sources copied to a caller's stack
 * buffer. gUnknown_0808D728 is 0x28 bytes read back as 20 HALFWORDS by
 * sub_08002F1C (`ldrh` off a stride-2 pointer over a 5x4 nest), so `u16 [20]`.
 * gUnknown_0808D750 is 4 bytes read back as 4 BYTES by sub_08002FE4 (`ldrb`,
 * masked `& 0x1f`); the 8-byte gUnknown_0808D754 immediately after it confirms
 * the extent. */
extern const u16 gUnknown_0808D728[];
extern const u8 gUnknown_0808D750[];
extern const u16 gUnknown_0808D760[];
extern const s8 gUnknown_0808D768[];
extern const s8 gUnknown_0808D76C[];

/* Wave 37 (W37-E). sub_08001DAC's two blobs: gUnknown_08485B2C is PutSprite's
 * `u16 *` fourth argument and gUnknown_084891C0 is ApplyPaletteExt's `u16 *`
 * first, 0x20 bytes applied at palette byte offset 0x260. Neither is
 * dereferenced here, so the element type is the parameter's and the extent is
 * unknown. */
extern u16 gUnknown_08485B2C[];
extern u16 gUnknown_084891C0[];

/* Wave 37 (W37-F). The tile-id table sub_0800F418 indexes with the 8-neighbour
 * bitmask it builds, so 0x100 entries. Read `movs r2,#0; ldrsh r0,[r1,r2]`,
 * and the ROM run at 0x084865C4 is full of 0xFFFF entries interleaved with
 * small positive ids (0x61, 0x83, 0x41, 0x42, 0xe0), so the 0xFFFF are -1
 * reject markers and the element type is SIGNED s16, not u16. */
extern s16 gUnknown_084865C4[];

/* Wave 37 (W37-H). sub_08039C70's OBJ blob: handed straight to
 * sub_0801BD00's `void *` third parameter with no arithmetic in front of it,
 * exactly like gUnknown_08499B6C and gUnknown_0849A3B8. Nothing indexes or
 * dereferences it here, so the element type is unconstrained and `u8 []` is the
 * weakest spelling that converts to `void *` without a const cast. */
extern u8 gUnknown_0849D824[];

/* 0x03000060 CANNOT BE DECLARED -- wave 37 (W37-G), the same class as
 * 0x03000600/0x03000602 above. aw2bhr.lds names gUnknown_0300005C at 0x5c and
 * then jumps to gUnknown_03000068, so 0x03000060 falls INSIDE the
 * gUnknown_0300005C region and an invented symbol has nowhere to live;
 * tools/proto_check.py flags it and the split build would fail with
 * `undefined reference` even though trymatch reports a match.
 *   Spell it `((u32 *)&gUnknown_0300005C)[1]`, which is what
 * work/sub_0801BB10 does.
 *   What it IS: the interrupt-enable shadow. sub_0801BB10 is its whole
 * interface -- a three-case switch that SETs / ANDs / ORs it with the caller's
 * mask and then pushes it to the hardware as
 * `REG_IE = g; REG_IME = (g & 0x10000) != 0`. A WORD (`ldr`/`str` at every
 * access) even though only the low halfword reaches REG_IE; bit 16 is the
 * software IME flag, which is what makes it 32 bits rather than a u16. Not
 * volatile: the single load at the tail feeds both the `strh` to REG_IE and
 * the 0x10000 test, i.e. agbcc CSEd it. Reached through the -fforce-addr
 * .rodata word at 0x0808F088, whose ROM content is 0x03000060, because the
 * switch names it on four separate arms. */

/* Wave 37 (W37-H). sub_08038C98 copies 0xa00 bytes of it to 0x06013940 with
 * sub_08011E54, with no arithmetic on the symbol -- a single tile blob, the
 * same shape as the 08122xxx/08123xxx run. `u8 []` is the weakest spelling
 * that reaches sub_08011E54's `void *` without a const cast. */
extern u8 gUnknown_080A1C24[];

/* Wave 37 (W37-N). The two-byte glyph pairs sub_080724D4 stamps when it
 * formats an integer into a text buffer: 08613F24 is the '0' glyph and
 * 08613F28 the '-' sign. Both are read a byte at a time (`ldrb [r0]`,
 * `ldrb [r0, #1]`) and the second byte of the '0' pair has the digit added to
 * it, so the pair is (attribute, character) and the characters are
 * consecutive. `u8 []` from the `ldrb`s; const because they live in ROM and
 * nothing writes them. */
extern const u8 gUnknown_08613F24[];
extern const u8 gUnknown_08613F28[];

/* Wave 37 (W37-N). The 21-entry scale ramp sub_08072CE4 indexes with
 * `unk64 - i * 4` (0..0x14 inclusive, anything above clamped to 0x100).
 * WORDS -- `ldr r2, [r0]` off a `<< 2` index -- and the value is handed
 * straight to `Div`'s s32 divisor, so `s32`. The array bound is exact:
 * 0x081CC01C - 0x081CBFC8 = 0x54 = 21 words, and 0x081CC01C is the next
 * symbol. */
extern const s32 gUnknown_081CBFC8[0x15];

/* Wave 37 (W37-N). An OAM sprite blob in ROM, reaching nothing but the `u16 *`
 * fourth parameter of PutSprite / PutSpriteExt in the 0x08072CE4-0x08073070
 * proc bodies -- the same model as gUnknown_0849B6C8. NOT const, because that
 * parameter is not. */
extern u16 gUnknown_081CC01C[];

/* Wave 37 (W37-G). sub_08018254 hands it to sub_08071948's `const void *`
 * fourth parameter with no arithmetic in front of it, the same shape as
 * gUnknown_0849D824 above.  The ROM word at 0x080D445C is 0x081F051D, an ODD
 * value, so it is data and not an address table; nothing here dereferences
 * it, which leaves `const u8 []` as the weakest spelling that fits. */
extern const u8 gUnknown_080D445C[];

/* Wave 37 (W37-H). sub_08035BC4's sprite pair: parameters 1 and 3 of the same
 * `sub_08015438(void *, int, void *, ...)` call that src/decomp/c_0803B264.c
 * fills with gUnknown_0849E700 / gUnknown_0849E6F8 and c_08027A50.c with
 * gUnknown_08499DE8 / gUnknown_08499DDC. Non-const `u8 []` on the same
 * reasoning as gUnknown_08499B6C: the callee's parameters are plain `void *`,
 * nothing here indexes or dereferences either symbol, and `u8 []` is the
 * weakest spelling that converts without a const cast. UNVERIFIED --
 * sub_08035BC4 did not close this wave, so no matched code reads them yet. */
extern u8 gUnknown_0849BDE0[];
extern u8 gUnknown_0849BFD8[];

/* Wave 37 (W37-I). The "turn banner" text block at 0x08090D90..0x08090E24,
 * read straight out of baserom.gba rather than guessed:
 *   0x08090D90  0000 0001 0002 0003 0003 0003 0002 0001 0000 0000  (u16 x 10)
 *   0x08090DA4  "NEXT TURN"        0x08090DB0  "PRESS A BUTTON"
 *   0x08090DC0/DD0, 0x08090DE0/DF0 the other two language pairs, and
 *   0x08090E04  " PR'OXIMA FASE"   0x08090E14  " PULSA BOT'ON A"
 * sub_08034AF8 indexes the first table with `(gUnknown_03004008 / 3) % 10` and
 * adds 0x68 to the entry to get the x of the second string, and hands the eight
 * string addresses to sub_08034A58's `const char *`. The u16 table is a
 * bare `lsls #1; adds; ldrh` off its own pool word, so it is a halfword array
 * and NOT a force-addr word; 0x08090E24 immediately after it IS one (it holds
 * 0x030033EC). */
extern const u16 gUnknown_08090D90[];
extern const char gUnknown_08090DA4[];
extern const char gUnknown_08090DB0[];
extern const char gUnknown_08090DC0[];
extern const char gUnknown_08090DD0[];
extern const char gUnknown_08090DE0[];
extern const char gUnknown_08090DF0[];
extern const char gUnknown_08090E04[];
extern const char gUnknown_08090E14[];

/* Wave 37 (W37-I). sub_080409E8 starts this script on tree 3 and then hands the
 * proc it gets back to Proc_Start(gUnknown_0849FD44, proc) as the parent, so the
 * two are the halves of one pair and take the same `const struct ProcCmd []`
 * gUnknown_0849FD44 already has. */
extern const struct ProcCmd gUnknown_0849FD84[];

/* Wave 37 (W37-I). Two four-entry s16 offset rows read only as
 * `movs rI,#{0,2,4,6}; ldrsh` off their own pool words by sub_0803EF70, which
 * adds entry i of each to a screen x/y before masking to 0x1ff / 0xff. SIGNED
 * from the `ldrsh`s; the [4] is what the four indices prove and the 8-byte gap
 * between the two symbols allows exactly that. */
extern const s16 gUnknown_0849F820[4];
extern const s16 gUnknown_0849F828[4];

/* Wave 37 (W37-I). sub_0803EF70's four graphics blobs: BE0 and 3D0 go to
 * Decompress's `u8 *` source, E34 to ApplyPaletteExt's `u16 *`, and F84/740 to
 * sub_0801C70C's `const void *` first parameter. */
extern u8 gUnknown_08113BE0[];
extern const u8 gUnknown_08113F84[];
extern u8 gUnknown_081143D0[];
extern const u8 gUnknown_08114740[];
extern u16 gUnknown_08114E34[];

/* Wave 37 (W37-I). sub_0803F6BC's shared 0x40-byte tail block: it is
 * sub_08011E54's `void *` source in six of the seven switch arms and never
 * indexed, so `u8 []` is the weakest spelling that converts. */
extern u8 gUnknown_08485A2C[];

/* Wave 37 (W37-I). gUnknown_08551D18 is the second COLUMN VIEW of
 * gUnknown_08551D0C's three-halfword rows (0x08551D0C + 0xc), reached through
 * its own pool word by sub_0804B180 with a bare `lsls #1` on the side index --
 * the same "one symbol per column" split gUnknown_0202972C / gUnknown_02029710
 * and gUnknown_03004582 / gUnknown_03004580 already have. Masked with 0x3ff and
 * placed at bit 0 of the OAM attr2 word, i.e. a tile number. */
extern u16 gUnknown_08551D18[];

/* Wave 37 (W37-I). The sub_0804C5A4 / sub_0804CA98 script tables, all four
 * indexed off the `side` (0/1) key:
 *   gUnknown_08553318  a flat halfword row indexed by gUnknown_020296B0.unk18,
 *                      compared for equality against gUnknown_03004508
 *   gUnknown_08553354  a PAIR per side, both halves stored into a
 *                      struct Unk56E28's unk04/unk06
 *   gUnknown_0855335C  five PAIRS per side (`side * 10 + step * 2 + half` in
 *                      halfword units), added to an entry's x and y
 *   gUnknown_08553C14  a BYTE pair per gUnknown_0300450C, added to
 *                      gUnknown_08553318's entry before the compare
 * The row lengths are what the index arithmetic proves; nothing bounds the
 * outer dimension. Signedness unproved -- every read is a bare `ldrh`/`ldrb`
 * feeding an add. */
extern u16 gUnknown_08553318[];
extern u16 gUnknown_08553354[][2];
extern u16 gUnknown_0855335C[][5][2];
extern u8 gUnknown_08553C14[][2];

/* Wave 37 (W37-I). sub_0804ABDC's four sprite blobs. The three `u16 []` ones
 * are sub_08013664's first parameter and are indexed by a 4-bit animation
 * phase scaled `lsls #1`, so they are halfword arrays; the two `u8 []` ones are
 * sub_0801BD00's `void *` third parameter and are never indexed.
 * gUnknown_0813204C is NOT gUnknown_0812A294 -- that address is a
 * `-fforce-addr` .rodata word holding this one (dumped from baserom.gba), and
 * the same is true of 0x0812A290 -> gUnknown_030044E0, 0x0812A298 ->
 * gUnknown_030030A0 and 0x0812A29C -> gUnknown_0848B6C6. */
extern u16 gUnknown_0813204C[];
extern u16 gUnknown_08131D8C[];
extern u16 gUnknown_08131DEC[];
extern u8 gUnknown_0848B688[];
extern u8 gUnknown_084C3B72[];

/* Wave 37 (W37-I). Two byte lookup rows sub_0804ABDC indexes with the s16
 * gUnknown_030044E0->unk1e / ->unk20 to build sub_0801BD00's x and y. Bare
 * `ldrb`, so byte-wide; nothing signs them. */
extern u8 gUnknown_084C3D14[];
extern u8 gUnknown_084C3D5C[];

/* Wave 37 (W37-I). The two `side` bytes sub_08041978 derives from
 * sub_08041D40's result and its complement (`1 - x`) and then uses as the row
 * index into gUnknown_03004580 and gUnknown_03004528. Bare `strb`/`ldrb`
 * pairs, so byte-wide; unsigned is unproved. */
extern u8 gUnknown_03003F50;
extern u8 gUnknown_03004484;

/* Wave 37 (W37-I). Two byte tables inside one blob at 0x08091318 (0x0809131E is
 * 0x08091318 + 6), both indexed with a bare `ldrb`: the first by the s16 at
 * gUnknown_030013D0 + 0x18 / gUnknown_030013B0 + 0x18, the second by the u8 at
 * the head of a gUnknown_08499594 record. The ROM bytes are
 * 00 01 01 01 01 02 | 00 00 01 02 03 04 05 06 07 06 ... -- a small remap, and
 * the two symbols overlap exactly as gUnknown_08551D0C / gUnknown_08551D18 do. */
extern const u8 gUnknown_08091318[];
extern const u8 gUnknown_0809131E[];

/* Wave 37 (W37-J3), block 0x08056. Six ROM tables that sub_08056D8C and
 * sub_08056EEC read, all with plain `ldrh`/`ldrb` and none of them signed by
 * any use:
 *   0x08553858  u8[12] = 0,0,0,1,1,2,2,3,3,4,4,0 -- subscripted by
 *               gUnknown_03004580[i][5] and [i][6] and used as the LOW index
 *               of gUnknown_08557914.
 *   0x08553864  u16[8], the multiples of 25 (0, 0x19, 0x32 .. 0xAF), indexed
 *               by sub_08056EEC's third argument.
 *   0x08553874  u16 = 0x0050, 0x0122; 0x08553884 u16 = 1, 0; 0x08553888 u16 =
 *               7, 8 -- all three indexed by the side index i, so a pair each.
 *   0x08553878  u16[6], indexed
 *               `gUnknown_085D6A48[gUnknown_03004580[i][1]][1] * 2 + i`, the
 *               same subscript sub_08057048 uses on gUnknown_085538A2.
 * All five halfword tables feed the six-halfword record sub_08056EEC hands to
 * sub_08056F8C, exactly as gUnknown_0855388C / gUnknown_085538A2 feed
 * sub_08057048's. */
extern u8 gUnknown_08553858[];
extern u16 gUnknown_08553864[];
extern u16 gUnknown_08553874[];
extern u16 gUnknown_08553878[];
extern u16 gUnknown_08553884[];
extern u16 gUnknown_08553888[];
/* Ten words -- 3, 5, 6, 7, 8 twice over -- read with `ldr` by sub_08056D8C as
 * `[gUnknown_03004580[i][0] * 5 + gUnknown_08553858[...]]` (five per row, two
 * rows) and stored straight into struct Unk02029BA8's `void *` unk10.
 * UNPROVEN: the CONTENTS are small integers and not addresses, so unk10 is
 * very probably an id rather than a pointer; `void *` is spelled here only so
 * the store agrees with the already-promoted struct, which nothing else
 * contradicts. Left flat rather than `[][5]` because the ROM computes
 * `(row * 5 + col) * 4` as one scaled index, not `row * 20 + col * 4`. */
extern void *gUnknown_08557914[];
/* Wave 48, W48-D. A [10][12] grid of 0x2c-byte records in EWRAM. Extents are
 * hard: sub_0806279C clears it with `for (i = 0; i <= 9; i++) for (j = 0;
 * j <= 0xb; j++)` and the row stride it computes is `((i << 5) + i) << 4` ==
 * i * 0x210 == 12 * 0x2c, so the inner dimension is exactly 12.
 * The record: ten words at 0x00 and two halfwords at 0x28/0x2a. The ten words
 * are ONE array and not two of five -- the clearing loop runs k = 0..4 storing
 * `unk00[k + 5]` then `unk00[k]` off a SINGLE induction variable
 * (`str r4,[r0,#0x14]; stm r0!,{r4}`). Spelling it as two five-word members
 * folds the 0x14 into the address constant instead, costs a second iv and a
 * stack slot, and does not match (probed, wave 48).
 * Signedness of the words and of unk28/unk2a is UNPROVEN -- the only promoted
 * reader stores zero. sub_0805F0EC, sub_080627F4 and sub_08062AE4 are the
 * three remaining users and are still assembly. */
struct Unk0202DAD8 {
    /* 0x00 */ int unk00[10];
    /* 0x28 */ u16 unk28;
    /* 0x2a */ u16 unk2a;
};
extern struct Unk0202DAD8 gUnknown_0202DAD8[][12];

/* Wave 37, W37-O1. The eight-slot array immediately below gUnknown_0200C528
 * (0x0200C528 - 0x0200C508 == 0x20 == 8 * 4), and aw2bhr.lds names both.
 * Element type is read off two independent sites: sub_08019348 stores its own
 * `const u8 *` parameter -- the one it also hands to sub_080193B0 -- into the
 * first NULL slot, and sub_08019380 loads a slot and passes it straight to
 * sub_080193B0(const u8 *) with no intervening instruction. A producer and a
 * consumer agreeing independently, so the element really is the script pointer
 * and not an opaque word. sub_080191B0 clears all eight with a `str` of 0. */
extern const u8 *gUnknown_0200C508[];

/* Wave 37, W37-K2. A ROM table with TWO-BYTE elements, indexed by
 * gUnknown_03003F40: sub_0806056C hands `&gUnknown_08576900[gUnknown_03003F40]`
 * to sub_080357E0's fifth (void *) parameter and the only scaling is `lsls #1`.
 * The other arm of the same call passes the u8 buffer gUnknown_03003110, so the
 * callee proves nothing about the element type -- only the stride is evidence,
 * and the signedness is unknown. */
extern u16 gUnknown_08576900[];
/* Wave 37, W37-K2. A whole-word counter incremented by sub_0806056C with
 * `ldr; adds #1; str` and NOT re-read after the store, so plain `int` rather
 * than the volatile shape gUnknown_030046D4 has. Signedness unproved. */
extern int gUnknown_03004774;

/* Wave 37, W37-K3. The block at 0x08061788..0x08061E98 carries an agbcc
 * `-fforce-addr` address-constant pool in ROM at 0x0816DB00..0x0816DB0F, and
 * data/data.s currently names the four words gUnknown_0816DB00/04/08/0C. They
 * are NOT globals: dumping baserom.gba gives 0x02029C54, 0x030046B4,
 * 0x03004784 and 0x085D5ABC -- three of which already have real declarations
 * above. So the honest spelling names the object and lets the build place the
 * pool word; nothing should ever declare a gUnknown_0816DBxx. (0x0816DB10 is
 * the same TU's 12-byte `.LC` initialiser that sub_08061E98 `ldm`s onto its
 * stack, three THUMB function pointers into this same block.) */

/* Wave 37, W37-K3. A 0x130-byte record. sub_08061A40 is its whole-struct
 * assignment (`*dst = *src`, 0x130 bytes of byte copy: 0x10 unrolled pairs then
 * a 24-trip loop of 0xc), and sub_08061788 subscripts the ROM table with a
 * `* 0x130` stride synthesised as `((x*4+x)*4-x)*16`. The contents are not
 * modelled -- only the size is proved, by the copy length and the stride
 * agreeing. */
struct Unk085771C4
{
    /* 0x00 */ u8 filler_00[0x130];
};
extern const struct Unk085771C4 gUnknown_085771C4[];
/* The RAM destination of the second sub_08061A40 in sub_08061788; one whole
 * record, and the linker script gives it its own symbol. */
extern struct Unk085771C4 gUnknown_02029D84;
/* Wave 37, W37-K3. The scratch copy that sub_08061788 fills and then hands to
 * gUnknown_02029D84 lives at 0x02029C54, which is gUnknown_02029C20 + 0x34 --
 * aw2bhr.lds names NO symbol there, so it must be reached through this one or
 * the split build breaks on an undefined reference (the wave-37 W37-G finding).
 * 0x02029C54 + 0x130 == 0x02029D84 exactly, i.e. the two records are adjacent
 * and the second one is the one the splitter happened to name. Typed `u8 []`
 * because nothing here reads gUnknown_02029C20's own first 0x34 bytes; the two
 * remaining users (sub_08060AB0, sub_08062C94) are still assembly. */
extern u8 gUnknown_02029C20[];

/* Wave 37, W37-K3. Four ROM bytes, indexed by gUnknown_030046B8 with an
 * explicit `<= 3` guard in sub_08061788 (`cmp #3; bhi`) that supplies 4 as the
 * out-of-range fallback -- so the extent 4 is proved by the bound, not guessed.
 * The byte it yields is the same row selector gUnknown_085C77A0.unk27 supplies
 * on the other arm. */
extern const u8 gUnknown_08576908[4];
/* Wave 37, W37-K3. Rows of NINETEEN bytes: sub_08061788 forms
 * `selector * 19 + gUnknown_08499598[army].unk1d` (the `(x*4+x)*4-x` synthesis
 * of *19) and `ldrb`s the result, then uses that byte as the subscript of
 * gUnknown_085771C4[]. So it maps (mode, unk1d) to a record id. The row count
 * is unknown; the column count is the one that is proved. */
extern const u8 gUnknown_0857690C[][19];
/* Wave 50, W50-M. The six printf FORMAT STRINGS of the map-editor debug
 * overlay, read straight out of baserom.gba: "UNIT  C0  C1", "PSQ(%1d)",
 * "ASQ(%1d)", "USQ(%1d)", "CPT(%1d)" and "(%1d)". sub_08062DF0 hands each to
 * sub_08013428, whose third parameter is `const char *` and which is VARARGS --
 * so the type is not inferred, it is the declared one.
 *   Their ROM spacing (0x10, 0xc, 0xc, 0xc, 0xc) is exactly agbcc's 4-byte
 * alignment of 13, 9, 9, 9, 9 and 6 bytes, which independently confirms both
 * the reading and each string's extent.
 *   These are NOT the -fforce-addr pool words at 0x0816DB00..0x0816DB1C: the
 * ADDRESS is the argument (`ldr r2,=sym; bl`), never loaded-then-dereferenced.
 *   RECORDED AS A COMPROMISE, not as a model of the original source: the honest
 * spelling is a string literal, the way matched c_080281F0.c spells its
 * "SNOW:%s" siblings, and it compiles to a .rodata section byte-identical to
 * ROM 0x0816DB40..0x0816DB86 -- but tools/trymatch.py rejects it. See the
 * docs/agbcc-codegen.md entry; when that is fixed, switch sub_08062DF0 to
 * literals and delete these six declarations. */
extern const char gUnknown_0816DB40[];
extern const char gUnknown_0816DB50[];
extern const char gUnknown_0816DB5C[];
extern const char gUnknown_0816DB68[];
extern const char gUnknown_0816DB74[];
extern const char gUnknown_0816DB80[];

/* Wave 37, W37-K3. Two ROM tables of nullary function pointers, stepped by the
 * shared cursor gUnknown_03004770: sub_08061B00 runs
 * `table[gUnknown_03004770++]()` and picks the table on
 * gUnknown_03003FC0.unk0d. `bl _call_via_r0` with no argument register set up,
 * which is the arity readout. gUnknown_085766E8 has 20 slots before
 * gUnknown_08576738 begins; neither extent is otherwise bounded. */
extern void (*const gUnknown_085766E8[])(void);
extern void (*const gUnknown_08576738[])(void);

/* Wave 45, W45-H. A third ROM table of nullary function pointers in the same
 * run, stepped by gUnknown_030040D8->unk07[4]: sub_0805F4CC clamps that byte to
 * 1 when it exceeds 7 and then runs `gUnknown_085768E0[byte]()` --
 * `lsls #2; adds; ldr; bl _call_via_r0` with no argument register set up, which
 * is the arity readout (see the _call_via_rN rule). The clamp bounds the extent
 * at 8 slots but does not prove it, so the extent is left open like its two
 * neighbours above. */
extern void (*const gUnknown_085768E0[])(void);

/* Wave 37, W37-K3. A whole-word accumulator: sub_08061CF8 zeroes it and then
 * adds `sub_08061DA8(n)` into it once per set bit of
 * gUnknown_08499598[gUnknown_030033EC].unk2c, with `ldr; adds; str` each time.
 * `int` because that is sub_08061DA8's declared return type; nothing compares
 * it, so the signedness is inherited rather than proved. */
/* Wave 52, W52-B ADDS the volatile, and it is a byte-exact measurement rather
 * than a style choice. sub_08059B4C's `if (a2 > gUnknown_03004788) a2 =
 * gUnknown_03004788;` compiles, non-volatile, to one `ldr` plus `adds r1,r0,#0`
 * -- CSE reuses the compared value for the assignment, and no spelling of that
 * statement (plain `if`, `?:`, a temporary, two divides) stops it. The ROM
 * holds the ADDRESS in r2 and issues a SECOND `ldr r1,[r2]`, which only a
 * volatile read produces. With the volatile the function is byte-for-byte
 * identical; without it, 97.8% with those 4 bytes as the entire residual.
 * Byte-neutral at the only other reader, sub_08061CF8 (src/decomp/c_08061CF8.c),
 * whose `= 0` and four `+= sub_08061DA8(n)` already emit ldr/add/str around a
 * call -- re-verified with trymatch after this edit. */
extern volatile int gUnknown_03004788;


/* Wave 37, W37-O2. The gUnknown_0200C528 script-command DISPATCH TABLE.
 * sub_08019404 indexes it with the current node's byte 0
 * (`gUnknown_0200C528[a].unk04->filler_00[0]`), loads a word and calls it
 * through `bl _call_via_r1` with the sign-extended slot index in r0, looping
 * while the result tests nonzero after `lsls #0x10`. The twelve entries read
 * out of baserom.gba as sub_08017A80, sub_08017A58, sub_08017B50 x3,
 * sub_08017B60, sub_08017B64, sub_08017B8C, sub_08017BB0, sub_08017BD4,
 * sub_08017BFC, sub_08017C24 -- exactly the `s16 (s16)` handler family
 * unknown-functions.h documents beside sub_08017A80, which is what fixes the
 * element type. ROM data, so `const`; the extent is unbounded because only the
 * indexed load reaches it. */
extern s16 (*const gUnknown_0848A244[])(s16);

/* Wave 37, W37-K3, and this is the `c_local` workaround, not a model of the
 * source: the two words below are the SAME `-fforce-addr` pool block described
 * beside gUnknown_085771C4 above, holding &gUnknown_03004784 and
 * gUnknown_085D5ABC. The honest spelling does not reproduce them in
 * sub_08061DCC and the measured reason is new: force-addr's reroute is decided
 * AFTER CSE has merged the references, and a POINTER global read twice around a
 * `bl __divsi3` collapses to ONE reference because a libcall does not clobber
 * memory. The honest draft then loses the ROM's recompute-per-statement as
 * well. Naming the pool words gives back both, and the relocation is checkable
 * because data/data.s really does define these symbols. Do NOT read them as
 * evidence that objects live at 0x0816DB08 / 0x0816DB0C.
 * The `volatile` is likewise a reproduction device and NOT a claim about the
 * object: it is what restores the ROM's re-dereference at every use (+8 bytes
 * over the plain declaration, measured this wave). `const` changes nothing
 * either way, as the -fforce-addr chapter of docs/agbcc-codegen.md records.
 * Only sub_08061DCC's parked draft names these two; nothing else should. */
extern u8 **volatile gUnknown_0816DB08;
extern struct UnitTypeData *volatile gUnknown_0816DB0C;


/* Wave 37, W37-O2. The 0x0808E59C..0x0808E5B8 run is agbcc's `-fforce-addr`
 * ADDRESS-CONSTANT POOL for the 0x08017xxx-0x08019xxx block, NOT a table of
 * globals. Dereferenced in baserom.gba:
 *
 *   0x0808E59C -> 0x0200C528   &gUnknown_0200C528
 *   0x0808E5A0 -> 0x0200C528   &gUnknown_0200C528   (a second private copy)
 *   0x0808E5A4 -> 0x03002EF0   &gUnknown_03002EF0   (sub_08019470)
 *   0x0808E5A8 -> 0x03002EE0   &gpKeySt
 *   0x0808E5AC -> 0x030033EC   &gUnknown_030033EC   (sub_080196F4)
 *   0x0808E5B0 -> 0x03003F2C   &gUnknown_03003F2C   (sub_080196F4)
 *   0x0808E5B4 -> 0x08499598   &gUnknown_08499598   (sub_08019940)
 *   0x0808E5B8 -> 0x03002EE0   &gpKeySt             (a second private copy)
 *
 * Two words holding the SAME address is the giveaway -- -fforce-addr gives
 * each function its own copy. Write the honest spelling (`gUnknown_030033EC`,
 * `gpKeySt->held`, ...) and let agbcc emit the word into the unit's own
 * `.rodata`; trymatch then reports `name different symbols that resolve to
 * the same address` and prints the address promotion must carry. Contrast
 * gUnknown_08499588/8C/98 at 0x08499xxx, which ARE real globals -- same
 * block, opposite answers, so dereference the candidate in baserom.gba
 * before deciding.
 *
 * Note the SECOND indirection this creates for gUnknown_08499598, which is
 * itself a pointer: sub_08019940 reads `ldr r0,[pool]` (= 0x08499598) then
 * `ldr r1,[r0]` (= the array base), both INSIDE its loop, and that is one
 * plain `gUnknown_08499598[i]` subscript, not two source dereferences. */

/* ---- wave 37 (W37-Q4): the 0x080439A8-0x08043F92 run ---- */

/* Two more of the 8-byte PutSprite object-list blobs packed through
 * 0x084A0024..0x084A0069 (see gUnknown_084A0052/gUnknown_084A005A above).
 * sub_080439A8 picks between them on its sixth argument and hands the chosen
 * one to PutSprite's `u16 *` fourth parameter with no arithmetic, so `u16 []`
 * exactly like its neighbours. Each holds a leading count of 1 followed by one
 * three-halfword OAM entry, which is what fixes the 8-byte stride. */
extern u16 gUnknown_084A0042[];
extern u16 gUnknown_084A004A[];
/* A 32-entry byte table filling 0x084A006E..0x084A008D, i.e. right up to
 * gUnknown_084A0090. sub_080439A8 subscripts it with `DivRem(x, 0x20)`, which
 * is what makes the extent exactly 32, and reads it with a bare `ldrb` whose
 * result is SUBTRACTED from a y coordinate -- a per-frame vertical bob offset.
 * Values are 06 00 01 01 02 02 02 01 01 and then zeros. */
extern u8 gUnknown_084A006E[];
/* Nineteen bytes at 0x084A077C, ending exactly where gUnknown_084A0790 begins:
 * `00 01 02 04 0f 03 05 10 06 07 12 08 09 11 0b 0c 0d 0e 0a`, a permutation of
 * 0x00..0x12. sub_08043CA0 walks i = 0..0x12 over it and feeds each byte to
 * sub_0803CAB8(u32) -- a CO display order, filtered down to the unlocked ones.
 * Reached through the 0x08091378 force-addr word, not by a direct pool `ldr`. */
extern u8 gUnknown_084A077C[];
/* The 0x180-byte (192 halfword) EWRAM staging buffer sub_08043E8C fills with
 * sub_08011C68 and then rewrites nibble by nibble into its caller's tile
 * buffer, 12 rows of 16 halfwords. `u16 []` from the `ldrh`/`adds r6,#2` walk;
 * 0x180 is both sub_08011C68's explicit byte count and 12*16*2, which is what
 * bounds it. */
extern u16 gUnknown_02017C50[];

/* Wave 41 (W41-A): the window-frame painter's four tile tables. sub_0801A240
 * indexes each with `lsls #0x10; asrs #0xf` on its s16 `kind` argument, i.e.
 * a 2-byte stride, and `ldrh`s the result -- so `u16 []`. data/data.s gives
 * 0848A46C/70/74 exactly 4 bytes each, so each table is two entries, and
 * sub_0801A474 only ever passes kind = 0 or 1. gUnknown_0848A478's run in
 * data.s is longer only because nothing after it is named yet. */
extern const u16 gUnknown_0848A46C[];
extern const u16 gUnknown_0848A470[];
extern const u16 gUnknown_0848A474[];
extern const u16 gUnknown_0848A478[];
/* Wave 41 (W41-A): the 4-byte template sub_0801A2E4 memcpy's onto its stack
 * and then reads as two alternating u16 tilemap entries -- so `u16 [2]`, and
 * 4 bytes is exactly the size data/rodata.s gives it. */
extern const u16 gUnknown_0808E5C0[];
/* Wave 41 (W41-A): 0x0808E5C4 IS NOT AN OBJECT and must not be declared as
 * one. It is agbcc's own -fforce-addr word for `&gUnknown_0849958C` (the ROM
 * word dereferences to 0x0849958C in baserom.gba), which is why sub_0801A474's
 * `ldr r0,[r0]` appears twice: one level is the force-addr slot, the other is
 * the pointer global's own read. MEASURED: declaring it `u16 **` and writing
 * `**gUnknown_0808E5C4` reproduces the instruction stream exactly and still
 * misses, because the honest name `gUnknown_0849958C` also creates its allocno
 * at the top of the function and that is what fixes the register allocation.
 * Write the global; trymatch places the pool word (rodata 0x0808E5C4).
 * Same for 0x0808EF58 in sub_0801ABF8 -- it holds &gUnknown_0200CC2C. */

/* Wave 42 (W42-C). The five-entry (dx, dy) direction table sub_08010ADC walks,
 * dereferenced in baserom.gba rather than inferred:
 *   0x0848897C: { 0, -1, 1, 0, 0 }   (dx)
 *   0x08488986: { 0,  0, 0, -1, 1 }  (dy)
 * so index 0 is the cell itself and 1..4 are left, right, up and down. They are
 * TWO separate arrays, not one 2x5 table: sub_08010ADC materialises dy through
 * its own inline pool word rather than indexing off dx's base register, which a
 * `tbl[1][i]` spelling cannot produce. s16 is measured, not assumed -- both are
 * read with `ldrsh` and the -1 entries need the sign.
 *
 * 0x0808D8A0 IS NOT AN OBJECT: it is agbcc's own -fforce-addr word holding
 * 0x0848897C, one slot before the 0x0808D8A4/0x0808D8A8 pair c_08010B34.c
 * already documents for &gUnknown_08499590. Naming gUnknown_0848897C honestly
 * reproduces sub_08010ADC's two-level load and promotion carries the rodata
 * entry. NOTE FOR PROMOTION: data/data.s currently swallows 0x0848897C inside
 * gUnknown_08488974's 0x12-byte blob, so that blob must be split at 0x0848897C
 * to give the symbol somewhere to live. */
extern const s16 gUnknown_0848897C[];
extern const s16 gUnknown_08488986[];
/* Wave 42 (W42-C). Two u16s at 0x0300308C: the base tile ids for the digit and
 * letter glyph runs, added to a character code to form a tilemap entry.
 * sub_08010F38 reads both (`ldrh [r4]` at +0 for characters <= 0x40, i.e.
 * digits, and `ldrh [r4,#2]` at +2 for letters); sub_08010EF8 reads +0 only.
 * NOT a pointer -- both are plain ldrh on the symbol's own address, and the
 * linker script already places it (aw2bhr.lds: `. = 0x00308C;`).
 * 0x0808DF8C is the -fforce-addr word holding 0x0300308C, not an object: its
 * neighbours at +4 and +8 BOTH hold 0x03001408, and one word per
 * (function, symbol) pair is what marks the whole run as address constants. */
extern u16 gUnknown_0300308C[];
/* ...and this is the c_local workaround for that word, needed by sub_08010EF8.
 * MEASURED, because the wave brief says to write the honest name first: the
 * honest `gUnknown_0300308C[0]` spelling reproduces sub_08010EF8 exactly except
 * that it is ONE INSTRUCTION short -- agbcc materialises the symbol with an
 * inline text pool word and reads `ldrh r1,[r6]`, where the ROM holds the
 * rodata word's address in r6 and reloads `ldr r1,[r6]; ldrh r1,[r1]` every
 * iteration. Four spellings were probed and none moves it: a `volatile` array,
 * a sized array, a variable index, and a plain pointer global (that one
 * overshoots to FOUR levels, the trap the wave-36 note warns about).
 * sub_08010F38 reads the same symbol at +0 and +2 and DOES take the honest
 * inline spelling -- it is matched -- so the two spellings coexist in one
 * original .c file and the choice is per (function, symbol), not per symbol.
 *
 * Wave 51 (W51-J) re-measured this and CORRECTS the sentence above about the
 * plain pointer global. "Any non-const spelling overshoots to four levels" was
 * generalised from SCALAR probes only, and it is false for the array form:
 * `extern u16 *gUnknown_0808DF8C[];` is non-const and still keeps the plain
 * inline pool word, so it is the closest shape anyone has produced -- three
 * levels, correct address handling, and only the LICM hoist wrong. It is where
 * the next attempt should start. What was also measured: const-ness and
 * volatile-ness do NOT decide the hoist. All four spellings hoist the pointer
 * load, so the lever is not this declaration at all -- see
 * work/sub_08010EF8/NOTES.md and the wave-51 W51-J chapter in
 * docs/agbcc-codegen.md. */
extern u16 *const gUnknown_0808DF8C;

/* Wave 43, W43-K -- blocks 0x0804A / 0x0804B.
 *
 * gUnknown_084C3D12 is a single ROM byte (0x20) read with a bare `ldrb` off a
 * plain address and compared for equality against gUnknown_030044E0->unk2c[i]
 * in sub_0804A6D8, so it is a u8 sentinel for that array's element type.
 * Nothing signs it. It sits immediately before the already-declared
 * gUnknown_084C3D14 table and is declared non-const to match its neighbours;
 * sub_0804A6D8's loop stores nothing, so const/non-const is byte-neutral here
 * and the choice is NOT evidence.
 *
 * gUnknown_084C398C is a proc script -- sub_0804A6D8's `ldr r0,=...; bl
 * sub_080193B0`, the same shape and the same declared `const u8 *` parameter
 * as gUnknown_084C38BC two lines of C away in sub_0804AE10.
 *
 * gUnknown_085519FC is a POINTER VARIABLE in ROM holding 0x02016450, and it is
 * NOT const: sub_0804B850 and sub_0804BB74 both re-load the pointer VALUE on
 * every iteration of a loop that stores through it (`ldr r1,[r5]; strh
 * r3,[r0]`), which is the aliasing reload a const pointer would have let LICM
 * hoist. Its address additionally appears as TWO private -fforce-addr .rodata
 * words, 0x08136040 (sub_0804BAC4) and 0x08136044 (sub_0804BB74), both holding
 * 0x085519FC -- one copy per function, exactly as the literal-pool chapter
 * predicts. Do not mistake either for a separate global.
 *
 * gUnknown_02028E64 is the 64-halfword scratch buffer sub_0804BD58 fills and
 * then hands to CpuFastSet. The ROM reaches it through the -fforce-addr word
 * at 0x08136048 (dumped from baserom.gba). */
extern u8 gUnknown_084C3D12;
extern const u8 gUnknown_084C398C[];
extern u16 *gUnknown_085519FC;
/* NO EXTERN CAN EXIST FOR 0x02028E64: upstream's aw2bhr.lds IWRAM table jumps
 * 0x028E5C -> 0x029664 and gen_lds.py passes it through verbatim, so the symbol
 * is unlinkable in the split build. Spell accesses as an offset into the
 * covering symbol instead -- `(u16 *)((u8 *)gUnknown_02028E5C + 8)` -- which
 * relocates as gUnknown_02028E5C+8 and resolves to the same address and pool
 * word. See work/sub_0804BD58/sub_0804BD58.c. Wave 43. */

/* Wave 44, W44-D. Two adjacent ROM words at 0x081D9440/0x081D9444, each a
 * POINTER and not an object: sub_08087040 holds both ADDRESSES in callee-saved
 * registers and does `ldr r3, [rN]` inside its loop, handing the loaded word
 * straight to PutSprite's `u16 *` fourth parameter with no arithmetic. `u16 *`
 * for that reason, and NOT `const` -- PutSprite's parameter is a plain `u16 *`.
 * The load is repeated every iteration rather than hoisted, which is what says
 * the pointer objects themselves are non-const. data/data.s gives each exactly
 * 4 bytes. */
extern u16 *gUnknown_081D9440;
extern u16 *gUnknown_081D9444;
/* Wave 44, W44-D. A table of 0x5c-byte records in ROM, indexed by a
 * gUnknown_02027F74.unk04[] unit id. sub_08087B74 reads
 * `gUnknown_085C77DC[id].unk01[i]` with a single `ldrb` and hands the byte to
 * sub_08043FA8's `int` first parameter.
 *
 * SETTLED IN WAVE 46 (W46-C), AND sub_08087B74 IS MATCHED. The 0x5c stride and
 * the byte at +1 were always solid; what was open was where the `+ 1` belongs,
 * and the answer is NEITHER spelling tried in wave 44. sub_08087B74 reaches the
 * table FLAT, as `((u8 *)gUnknown_085C77DC)[i + k]`, with the record offset
 * bound to a local in a statement of its own (`k = id * 0x5c + 1;`). That
 * statement is the whole trick: it stops fold re-associating the `+ 1` past the
 * loop counter, so the constant stays a runtime `adds r0, #1` against a CLEAN
 * pool word instead of migrating into the word's ADDEND
 * (`.word gUnknown_085C77DC+0x1`) or into the load displacement
 * (`ldrb r0, [r0, #1]`). Written inline in ANY association it does one or the
 * other -- that is what parked the function -- and the flat form only cost the
 * 0xc-stride giv because the `+ 1` was still inline to be folded into the biv's
 * own update. `k` is dead at the end of each iteration, so it costs no
 * register. The struct below is therefore NOT how the one known reader spells
 * the access; it is kept only as a description of the record's layout, and
 * nothing else names this symbol.
 *
 * NOTE the splitter also emits gUnknown_085C77E0 four bytes later -- that is
 * inside this table, not a separate object; the pool word here is clean
 * `.4byte gUnknown_085C77DC`. Only unk01 is evidenced; the extent is unproved. */
struct Unk085C77DC /* 0x5c */
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 unk01[0x5b];
};
extern struct Unk085C77DC gUnknown_085C77DC[];
/* Wave 44, W44-D. A 16-colour palette (0x20 bytes in data-08581E70.s) which
 * sub_08087B74 hands to ApplyPaletteExt(u16 *, u32, u16) for 0x20 bytes with no
 * arithmetic, so `u16 []` and non-const on the usual grounds -- ApplyPaletteExt
 * takes a plain pointer. */
extern u16 gUnknown_08614238[];
/* Wave 44, W44-G. The SRAM/backup-media descriptor, in ROM (data/data.s gives
 * it 0x128 bytes at 0x08485550). Only three functions in the ROM read it and
 * all three are the self-relocating SRAM trampolines sub_0808AE54 /
 * sub_0808AF00 / sub_0808AF74, so only two members are evidenced:
 *
 *   +0x18  u16, `ldrh`  -- the default transfer length sub_0808AF00 hands to
 *                          sub_0808AED0 as its byte count (sub_0808AF74 is the
 *                          same call with a caller-supplied length instead).
 *   +0x1c  u8,  `ldrb`  -- a SHIFT AMOUNT: all three do `lsls r4, rN` with it
 *                          on the u16 first parameter and add 0x0E000000, so
 *                          the parameter is a bank/sector index and this is
 *                          log2 of the bank size.
 *
 * The extent (0x128 bytes) is the splitter's, not measured; nothing here says
 * this is one object rather than a header followed by a table. Not `const`:
 * nothing proves it, and the reads are `ldrb`/`ldrh` through a pool word
 * either way. */
struct Unk08485550
{
    /* 0x00 */ u8 unk00[0x18];
    /* 0x18 */ u16 unk18;
    /* 0x1a */ u8 unk1a[2];
    /* 0x1c */ u8 unk1c;
};
extern struct Unk08485550 gUnknown_08485550;

/* Wave 46 (W46-A). An 8-byte-record ROM table that sub_08073228 LINEAR-SEARCHES
 * with no bound: for each byte of its source string it walks records until
 * `unk00 == c`, so the table is terminated by whatever key the strings can
 * contain and the extent is not pinned. `unk04` is then added to a u16
 * accumulator that becomes a per-character byte written into the proc.
 *
 * gUnknown_08614028 is NOT a second object -- it is this table's `unk04`
 * member. agbcc folds the +4 into the address constant, so the splitter
 * invented a symbol at 0x08614028 and lists it as a separate data_ref. */
struct Unk08614024
{
    /* 0x00 */ u8 unk00;
    /* 0x01 */ u8 filler_01[3];
    /* 0x04 */ int unk04;
};
extern const struct Unk08614024 gUnknown_08614024[];

/* Wave 46 (W46-A). 28 halfwords sub_08073658 copies into
 * gUnknown_0202F8DC[0x84 .. 0x9F] -- the bottom 28 entries of the 0xA0-entry
 * HBlank scanline table that DMA0 then feeds to REG_BLDY. Halfword stride is
 * from the `ldrh`; the count is the `cmp #0x1b; ble` bound. */
extern const u16 gUnknown_08614154[];

/* Wave 47 (W47-G). 0x085768B8, 0x28 bytes = TEN function pointers.
 * sub_0805BF3C selects an entry with `lsls #2` off a u8 index and calls it
 * through `_call_via_r3` with three arguments (its own x, y and out-pointer),
 * so the element type is a pointer to a three-argument function. Both call
 * sites in that function pass the same triple, and the second one indexes with
 * a byte taken out of a 25-byte stack table, which is what fixes the extent at
 * ten rather than at whatever the largest constant index happens to be.
 * Not const-qualified: the ROM section it lives in is shared with the
 * gUnknown_085768E0 / gUnknown_08576900 tables and nothing has proved it. */
extern void (*gUnknown_085768B8[])(int, int, u16 *);

/* Wave 50, W50-D. sub_080324C4's screen assets. The two palettes are the two
 * arms of one `if` and go to ApplyPaletteExt(u16 *, 0, 0x20); the three
 * compressed blobs go to Decompress(u8 *, void *). Not const, on the same
 * model as every other Decompress/ApplyPaletteExt source in this header --
 * the parameter types are non-const and nothing has proved the section. */
extern u16 gUnknown_081D8A14[];
extern u16 gUnknown_081D8A34[];
extern u8 gUnknown_081D3EE8[];
extern u8 gUnknown_081D6458[];
extern u8 gUnknown_081D2660[];
/* Wave 50, W50-D. sub_08032BCC's tile blob (its palette gUnknown_081D2224 is
 * already declared above). */
extern u8 gUnknown_081D2554[];
/* Wave 50, W50-D. Argument 1 of sub_08073304(const void *, void *, ...) in
 * sub_080324C4, i.e. the same slot as gUnknown_085802F0 / gUnknown_085826E0,
 * both of which this header already models as `u8 []`. */
extern u8 gUnknown_0849B644[];
/* Wave 50, W50-D. The link-lobby child proc's script: sub_08031638 hands it to
 * Proc_EndEach(const struct ProcCmd *), and work/sub_080312AC/NOTES.md reads
 * the same symbol going into Proc_Start there. */
extern const struct ProcCmd gUnknown_0849B1A0[];
/* Wave 50, W50-D. sub_080312AC's five lobby tables. Every one of them is
 * INDEXED by a computed subscript before being dereferenced, which is the
 * wave-49 inverse of the pool-word tell, so none of them is a -fforce-addr
 * address constant. The width each access fixes:
 *   gUnknown_0849B0C4  `ldrb` at `x % 30`, subtracted from 0x58  -> u8, >= 30
 *   gUnknown_0849B258  `ldr` at i*4, i = 0..3, straight into
 *                      PutSpriteExt's `u16 *` argument           -> u16 *[4]
 *   gUnknown_0849B268  same, but indexed by the RUNNING COUNT    -> u16 *[5]
 *   gUnknown_0849B27C  `ldrh` at i*2, i = 0..3                   -> u16 [4]
 *   gUnknown_081D3E68  `ldrh` at k*2, k = ((x >> 1) & 0xf)       -> u16 [16] */
extern u8 gUnknown_0849B0C4[];
extern u16 *gUnknown_0849B258[];
extern u16 *gUnknown_0849B268[];
extern u16 gUnknown_0849B27C[];
extern u16 gUnknown_081D3E68[];
/* Wave 50, W50-D. NOT a -fforce-addr pool word: baserom.gba holds FOUR
 * consecutive ROM pointers here (0x086103F8, 0x08610408, 0x0861041C,
 * 0x08610440), none of them a named symbol, so this is a real string table.
 * sub_08032BCC only ever reads element 0 (`ldr r1,[r0]`, no index) and hands
 * it to sub_0808B678(char *, const char *) as the source, i.e. a default
 * player name. Extent 4 is from the ROM run, not from a reader. */
extern const char *gUnknown_08613CDC[];

/* Wave 51, W51-O. sub_080790D0's three unnamed operands, all reached by a bare
 * `ldr rN, =sym` and used as the symbol itself with no dereference, so each is
 * the object rather than a pointer to one.
 *   08227F3C  handed straight to `Decompress(u8 *, void *)` with gUnknown_0200FC50
 *             as the destination, so `u8 []` and NOT const (Decompress's first
 *             parameter is non-const) -- the same model gUnknown_08234B10 carries.
 *   0822AA80  handed straight to `ApplyPaletteExt(u16 *, u32, u16)` for 0x40
 *             bytes at palette word 0x280 with no arithmetic, so `u16 []` and
 *             non-const for the same reason.
 *   02012790  an EWRAM source buffer: two `CpuFastSet(const void *, ...)` calls
 *             read it at +0 and +0x400 (0x10 words each) into 0x06015F00 /
 *             0x06015F40. Byte-addressed (`adds r4, r4, #0x400` off the symbol),
 *             so `u8 []`; extent unpinned. It sits 0x2B40 past gUnknown_0200FC50
 *             but the ROM holds its own pool word for the address rather than
 *             adding a displacement, which is what makes it a separate symbol. */
extern u8 gUnknown_08227F3C[];
extern u16 gUnknown_0822AA80[];
extern u8 gUnknown_02012790[];

#endif // UNKNOWN_GLOBALS_H
